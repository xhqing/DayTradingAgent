# Plan：多会话并行盯盘（方案 A）—— 港股实现

> 状态：待实现（2026-08-17 15:05 立项）。配套美股 Plan 见 `PLAN-parallel-monitor-US.md`（两市场共享同一套互斥公共模块，先读本文「公共模块」节）。
> 方案背景：单会话盯盘在多标的时「段间分析串行」是时间利用率的瓶颈——40 秒采样段结束后要串行分析所有标的，只数越多每只分析越浅、破位到决策的链路越长。方案 A = 多个 Claude Code 会话各盯各的标的池并行盯盘，**单持仓互斥**保证全局任意时刻至多一笔在场（风控语义不变、仍是单笔口径），资金并行留待方案 B（未立项）。

## 一、方案边界（做什么 / 不做什么）

**做**：

- 同一市场（港股）开 N 个会话（建议先 2 个），每个会话盯一个**静态分配的标的池**；
- 三层单持仓互斥（flock 锁 + 严口径闸门 + intent 日志）+ 第四层监控检测兜底，保证「凡脚本可控路径的双开仓彻底堵死；漏网的（用户手动下单等）40 秒内发现并处置」；
- caffeinate 引用计数、resume.py 断层检测按市场 scope——消除多会话下会互相干扰的两处全局副作用。

**不做（方案 A 明确不碰）**：

- **不做多持仓并行**（方案 B 范畴）：任一时刻全局仍只有一笔持仓，后到机会被互斥闸挡住、继续盯等下一机会（先到先得）；
- 不改风控预算语义（B = 2% × equity、f_max 10%、max_leverage 10 倍仍是单笔口径——单持仓互斥下语义自动成立）；
- 不改信号模式（signal）的 equity-log 累加链（signal 多开需另行改造，本 Plan 只覆盖 auto 模式）；
- 不做跨市场同时盯盘的时段重叠处理（港股与美股时段天然不重叠，见美股 Plan）。

## 二、架构总览

```
会话 1（盯池 A：如 HK.00700,HK.00981）          会话 2（盯池 B：如 HK.01810,HK.03688）
  preflight → 注册 watcher                        preflight → 注册 watcher
  futu_ws_segment 池A 40s 循环                    futu_ws_segment 池B 40s 循环
  段结束通知 → 分析池A → （机会）开仓 ──┐           段结束通知 → 分析池B → （机会）开仓 ──┤
                                       ▼                                                ▼
                            ┌─────────────────────────────┐
                            │  公共互斥模块 trade_mutex.py  │ ← 全局一把 flock 锁（不分市场）
                            │  ① flock 文件锁（临界区串行）  │
                            │  ② 开仓闸门（三口径检查）      │
                            │  ③ intent 日志（崩溃窗口）    │
                            └─────────────────────────────┘
                                       │ 任一闸不过 → 拒开仓、会话继续盯（先到先得）
  monitor_segment 每轮采样 → 持仓数检测（第四层兜底：>1 白名单外持仓 → 告警 + 平新仓）
```

**数据面**（已就绪，不用动）：行情全走富途链路（`futu_ws_segment.py` 逐笔 / `monitor_segment.py` 快照——老虎 WS 单连接互踢，多会话必须避开）；采样 log 按标的 + 日期 + mode 分文件；交易记录 `actions/YYYY-MM-DD-HKT-actions.md`（见下「共享文件并发写」风险节）。

## 三、公共模块 trade_mutex.py（港美共用，一次实现两市场复用）

> 位置：`.claude/skills/trade/scripts/trade_mutex.py`。港股 `open_position_tiger.py` 与美股 `open_position_tiger_us.py` 各自引用，避免两份逻辑漂移。**全局一把锁、不分市场**——分市场两把锁会放过「港股持仓违规过夜 + 美股会话开新仓」的跨市场叠加路径；港美时段不重叠使全局锁的实际代价为零。

### 3.1 第一层：flock 文件锁（内核级，把临界区序列化）

- 锁文件：`tmp/trade_mutex.lock`（`tmp/` 已 gitignore）；
- 实现：`fcntl.flock(fd, LOCK_EX)`，**锁住整个「查账户 → 下单 → 确认成交」临界区**（含降档重试循环全在内）；
- 特性：内核级（两会话进程在同一台 Mac 上排队等锁）；进程崩溃自动释放（内核回收 fd），无死锁残留；
- 用法骨架：开仓脚本入口 `with TradeMutex() as m:` 包住整个下单流程；拿不到锁时**阻塞等待**（不 try-lock 放弃——开仓机会不该因为对方在下单就丢弃，等对方下完、闸门判定后再决定）。

### 3.2 第二层：严口径开仓闸门（锁内执行，三口径任一命中即拒开）

在锁内（此时不可能有并发下单）依次检查：

1. **intent 日志有未决意图**（见 3.3）→ 拒开（可能有崩溃残留的在途订单，先人工 / 脚本确认）；
2. **当日订单流有「开仓方向成交且无对应平仓」**：查当日订单（`get_orders` 按 symbol 逐笔配对开平），存在已成交开仓（主单 Filled）而其对应平仓动作（止损触发成交 / 主动平仓成交）尚未发生 → 拒开。**查订单流而不是只查持仓**：与确认成交用同一数据源，规避「A 已成交、B 查持仓接口还看不到」的传播延迟窗口；
3. **有任一活动开仓方向挂单**（非终结状态的 LMT 挂单——成交只是时间问题，坑已被占）→ 拒开。

**常驻历史持仓处理（关键）**：账户有 2 笔 2026-08-03 之前的长持历史持仓（02800 多 1000 / 07709 空 200，保留不动）。闸门**不用**「账户已有任一持仓即拒开」的裸口径（会永远拒开），改走上面三条口径（只看本系统当日的开仓意图链，历史持仓不进链、不拦截）。config 增加常驻持仓白名单 `risk.resident_positions: ["02800", "07709"]`（数量与方向一并记录，供第四层检测用；增删常驻持仓时改这里）。

### 3.3 第三层：intent 日志（防崩溃窗口）

- 文件：`tmp/trade_intent.log`（JSON Lines，每行一条：`{ts, session_id, market, symbol, side, qty, order_id?, status}`）；
- 写入时机：**提交下单请求前**先追加一条 `status:"pending"`；脚本拿到券商确认（成交 / 被拒 / 撤单）后更新该行为终态（`filled` / `rejected` / `cancelled`）；
- 崩溃窗口闭合：进程若恰好崩在「已发请求、未确认」之间，锁释放后该行永远停在 `pending`——后到会话闸门口径 1 命中、拒开，并输出「发现未决 intent（ts / symbol），先查当日订单确认是否有在途成交」；人工确认无在途单后清掉该行（提供 `trade_mutex.py --clear-intent <行号>` 小工具）；
- 这是数据库写前日志（WAL）思路，与 2026-08-16 已立的「模糊超时停止降档、先查单确认」逻辑同向，补上跨会话的持久记录。

### 3.4 第四层：监控检测兜底（monitor_segment 每轮采样加检测）

`monitor_segment.py` 每轮采样本来就查当日订单（取最新止损价），同一次查询顺带检测：

- **持仓数 > 白名单外预期（即白名单外持仓 ≥ 2，或 =1 但当日已有另一标的的开仓成交记录）→ 立即在段输出打 ⚠️ 告警 + 系统通知**；
- AI 收到告警后处置：立即查两笔持仓，**平掉较新的那笔**（后开的仓位是漏网者；平仓脚本走 symbol 级隔离不会误伤先开仓位），恢复单持仓；
- 价值：把漏网（含用户手机 App 手动下单造成的双持仓——这是今天单会话模式下就存在的存量风险）从「静默双持仓、敞口翻倍直到收盘」变成「一个采样段内（≤40 秒 + 段间）被发现、立即处置」。

## 四、港股侧改动清单

| # | 文件 | 改动 | 层级 |
|---|---|---|---|
| 1 | `scripts/trade_mutex.py`（新建） | 公共互斥模块（3.1–3.3），全局一把锁 | 一~三层 |
| 2 | `scripts/open_position_tiger.py` | 入口包 `TradeMutex`；锁内跑闸门三口径；提交前写 intent、终态更新；闸不过输出结构化 JSON `{"ok":false,"blocked_by":"..."}`（AI 可据此继续盯盘不重试） | 一~三层 |
| 3 | `scripts/monitor_segment.py` | 每轮采样加持仓数检测 + 告警（3.4，读 config 白名单） | 四层 |
| 4 | `config.json` | 增 `risk.resident_positions` 白名单（标的 + 方向 + 数量） | 二、四层 |
| 5 | `scripts/preflight.py` | 输出当前互斥状态（intent 日志有无 pending、当日开仓链状态），多会话启动时一眼看到「谁在场」 | 辅助 |
| 6 | `.claude/skills/keep-awake/scripts/off.sh` | caffeinate 改**引用计数**：停盯收尾时若 `tmp/monitor_sessions.txt` 还有其它注册会话则不 kill，仅注销自己；最后一人关灯 | 跨会话 |
| 7 | `scripts/resume.py` | 断层检测按市场 scope（只查本市场 log 的最晚采样时间） | 跨会话 |
| 8 | `references/monitoring.md` | 补「多会话并行盯盘」节：启动序列、标的池分配约定、互斥行为说明 | 文档 |
| 9 | `SKILL.md` 硬性护栏第 4 条 | 补一句「单持仓由脚本级互斥闸强制（多会话并行时先到先得）」，纪律 + 工程双保险 | 文档 |

## 五、标的池分配约定

- **静态分配、启动时定**：每个会话启动盯盘时，用户指令里明确给本会话的标的池（如「会话 1 盯 00700/00981，会话 2 盯 01810/03688」）；preflight 输出本会话池 + 全局已在场会话池（读注册文件），启动即对齐、防重复；
- **换标的边界**：池内僵局可换标的（`hot_list` 刷新），但**只从本会话池关联的板块 / 备选列表里换**，不跨池抢标的最简约定 = 换标的前先 snapshot 查「该标的是否已在其它会话当日 log 里出现过」（读 `tmp/monitor_log_*` 文件名即可判断），出现过就不换过去；
- **允许撞车兜底**：约定失败也不出事故——两个会话盯上同一标的时，先开仓者得、后到者被互斥闸拒；同标的双会话只是浪费一路注意力，不是风险。

## 六、风险与对策（诚实清单）

| 风险 | 对策 | 残余 |
|---|---|---|
| 两会话几乎同时冲开仓闸（check-then-act） | flock 把临界区序列化，闸门在锁内执行 → 竞态从根上消除 | 无（本地脚本路径） |
| 进程崩在「已提交未确认」之间 | intent 日志 pending 残留 → 后到会话拒开 + 提示确认 | 需人工清一次 intent |
| 券商持仓数据传播延迟 | 闸门查当日订单流（与成交确认同源）而非持仓接口 | 理论窗口基本闭合 |
| 用户手机 App 手动开仓 | 闸门与锁均管不到 → 第四层监控检测 40 秒内告警 + 平新仓 | 存量风险（单会话今天同样存在），从无人知晓变 40 秒发现 |
| 会话 A 停盯 pkill caffeinate 影响 B | off.sh 引用计数（改动 #6） | 无 |
| resume 断层检测被 B 会话采样掩盖 | 按市场 scope + watcher 按会话判定兜底 | resume 自身不再失明 |
| actions/ 当日文件两会话并发追加写 | 追加写（append）+ 每条动作原子小段：macOS 上 ≤ PIPE_BUF 的单次 write 原子，`log_action.sh` 单条 append 实测安全；保险起见 log_action 前后加 flock 同锁 | 极低 |
| 多会话 token 成本 | 每会话完整 skill 上下文 + 持续分析流，成本近线性 ×N（用户知情决策） | 接受 |

## 七、实施顺序与验收

1. **trade_mutex.py + open_position_tiger.py 接锁**（一~三层）→ 验收：两个终端同时模拟跑开仓（dry 模式 / paper 账户小额），观察 flock 串行 + 后到者被闸拒的完整 JSON 输出；
2. **monitor_segment 持仓检测**（四层）→ 验收：paper 账户手工造双持仓（App 手动开一笔），观察 ≤ 一个采样段的告警；
3. **caffeinate 引用计数 + resume scope** → 验收：两会话场景 A 停盯后 B 的 caffeinate 仍存活；B 启动时 resume 不再报 A 市场的假断层；
4. **文档（monitoring.md / SKILL.md）** → 验收：新会话按文档能独立启动多会话盯盘；
5. **实盘日灰度**：首个交易日先 2 会话、各 2-3 只标的，观察一天互斥与采样稳定性后再放宽。
