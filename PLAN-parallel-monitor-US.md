# Plan：多会话并行盯盘（方案 A）—— 美股实现

> 状态：待实现（2026-08-17 15:05 立项）。配套港股 Plan 见 `PLAN-parallel-monitor-HK.md`——**公共互斥模块 trade_mutex.py 的设计与实现全在港股 Plan 第三节，本文不重复**，只写美股侧的差异与专有内容。实施顺序上建议先做港股（盘中可实测）、美股复用公共模块后接线。
> 方案背景与边界同港股 Plan「一、方案边界」节（多会话各盯各池、单持仓互斥、不做资金并行）。

## 一、美股侧的先天条件（与港股的差异）

| 维度 | 美股现状 | 对并行的影响 |
|---|---|---|
| 交易时段 | 美东 09:30–16:00 = 北京 21:30–次日 04:00（夏令时） | 与港股永不重叠 → 港美两会话之间零并发竞争；同市场两会话（两个美股会话）才有真竞态，flock 照常工作 |
| 行情源 | 老虎无美股行情权限，只能富途 OpenD（`get_quote_us` 已单源富途，2026-08-05 实测修复） | 天然规避老虎 WS 单连接互踢问题——美股会话本就全程富途链路，无额外改造 |
| 采样脚本 | `monitor_segment.py <targets> 40 10 --mode auto`（10 秒快照兜底；每次启动做一次逐笔实测，稳定则升 `futu_ws_segment.py` 每秒采样） | 多会话各自起 monitor_segment 进程，互不干扰（富途本地网关多客户端并存） |
| 下单脚本 | 独立一套 `*_tiger_us.py`（2026-08-05 与港股解耦） | 接公共互斥模块即可，互斥逻辑零差异 |
| watcher 守护 | **美股时段不守护**（2026-08-12 用户立：美股时段用户在休息、不通知） | 美股会话崩溃靠 task-notification 断流 + 用户发现——多会话不增加也不减少此风险（存量边界，非并行引入），知情即可 |
| equity | auto 走老虎账户 API（USD 口径 `get_assets`） | 与港股同账户错峰共享，时段不重叠 → 无并发语义问题 |
| log / 记录 | log 按美东交易日命名（2026-08-16 已修跨午夜口径）、`actions/*-ET-actions.md` | 已分文件，无需改 |

## 二、美股侧改动清单

| # | 文件 | 改动 | 依赖港股 Plan |
|---|---|---|---|
| 1 | `scripts/open_position_tiger_us.py` | 接 `trade_mutex.py`：入口包 `TradeMutex`、锁内跑闸门三口径、intent 写入（market 字段 = "US"） | 公共模块（港股 Plan 三节） |
| 2 | `scripts/monitor_segment.py` | 持仓数检测**无需美股专属改动**——同一脚本按市场读同一 config 白名单（`resident_positions` 全局共用，港美同账户一份） | 改动 #3（港股 Plan） |
| 3 | `scripts/resume.py` | 断层检测按市场 scope——美股会话只查 `monitor_log_US_*`（美东交易日口径），不再被港股 log 掩盖 / 反之 | 改动 #7（港股 Plan，公共） |
| 4 | `config.json` | 常驻持仓白名单若美股也有历史长持仓则一并录入（当前美股无；2026-08-05 实测仓位已清） | 改动 #4（港股 Plan） |
| 5 | `references/monitoring.md` | 「多会话并行盯盘」节补美股差异：美股 watcher 不守护、逐笔实测升级流程在多会话下各测各的 | 改动 #8（港股 Plan） |
| 6 | caffeinate 引用计数 | 美股会话**不注册** watcher（港股盘中判定），但 preflight 同样会启 caffeinate——off.sh 引用计数对美股会话同样生效（最后一个停盯的会话才能关灯） | 改动 #6（港股 Plan，公共） |

**美股专属注意（跨午夜）**：美股会话跨北京午夜（00:00 后仍在盘内）。两处已在 2026-08-16 修过口径（resume 按美东交易日 glob、monitor_segment 哨兵跨午夜推断），多会话下不变；intent 日志的 `ts` 用本地时间戳、market 字段区分归属，跨午夜不产生歧义。

## 三、美股多会话的典型时序（夏令时）

```
北京 21:30 前后：美股会话 1（盯池 A：如 US.MU,US.AMD）+ 会话 2（盯池 B：如 US.SPY 相关 ETF 池）
  各自 preflight（启 caffeinate——引用计数下第二家跳过、不冲突）
  各自 monitor_segment 池内标的 40s 循环（富途快照 / 实测后逐笔）
  （机会出现）→ 开仓走公共互斥闸 → 先到先得，后到者收 blocked JSON 继续盯
北京 ~23:00-00:00：用户睡觉前喊停 → 各会话停盯总结 + 主动平仓 → 引用计数下最后注销的会话才关 caffeinate
（少数情况不喊停：盯到美东 16:00 收盘前主动平 + 收尾）
```

## 四、港美并行的组合场景核对（解耦结论表）

| 资源 | 港 + 美并行 | 结论 |
|---|---|---|
| 互斥锁 | 全局一把（不分市场） | 时段不重叠 → 锁竞争为零；但拦住「港股违规过夜 + 美股开新仓」跨市场叠加 |
| 购买力 / 净值 | 同一老虎账户 | 错峰共享（无并发）；风控单笔语义由「任意时刻至多一笔」保证，自动成立 |
| 行情连接 | 港：老虎 WS 单连接或富途；美：仅富途 | 美股固定富途；港股多会话避开老虎 WS 即可（monitoring.md 已有预案） |
| caffeinate | 全局一进程 | 引用计数解决（最后一人关灯） |
| signal 模式 equity-log | 单一累加链跨市场 | **不解耦**（单一资金池设计使然）——本 Plan 只覆盖 auto 模式，signal 多开不在范围内 |
| 采样 log / 交易记录 | 按标的 + 市场标签分文件 | 已解耦 |

## 五、实施顺序与验收（美股侧）

前置：港股 Plan 七节的 1–3 步完成（公共模块就位）。

1. `open_position_tiger_us.py` 接锁 + 闸门 → 验收：美股盘中（或 paper 非盘中 dry 路径自测）单会话开仓全链路不回归——锁不影响单会话行为（拿锁无竞争、闸门口径对空账户全绿）；
2. 双会话模拟并发：两个终端同时触发美股开仓脚本（paper 小额），验证 flock 串行 + 后到者 blocked JSON；
3. resume scope 回归：美股会话启动时不再把港股 log 当自己的活动时间（反之亦然）；
4. 实盘日灰度：首个美股交易日 2 会话各 2-3 只，重点观察（a）凌晨时段两路采样稳定、（b）用户睡前喊停时两会话收尾互不干扰（caffeinate 引用计数生效）。
