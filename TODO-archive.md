# DayTradingAgent 待办归档（已处理）

> 本文件归档**已处理**的待办条目（`✅**已完成**` / `✅**已更新**`），保留可回溯性——回溯排查时看这里（当初打算做什么、做没做、什么时候做的、后来改没改主意）；活跃待办只看 `TODO.md`（项目根文件制，2026-08-05 撤销 todo-skill 定案；方法论见全局 CLAUDE.md「待办（TODO）管理」节，本文件与 `TODO.md` 同处项目根）。

## 2026-09-03 批量处理（T121 / T122 / T126 / T116 / T124 完成归档 + T119 首次对账 + T120 首批数据 + T138 新立）

> 背景：本批在**盘外**（2026-09-03 13:40~14:10，港股午市进行中但无本会话盯盘任务）处理。多数待办原判「只能盘中实测」，本次改为**从已发生的真实会话留痕里取证据**（actions 记录 + CC 会话转录里的真实脚本输出）+ **盘外 mock 场景构造**，把能盘外收口的全部收口；确实仍需盘中/实盘的部分保留在 `TODO.md` 并注明已验证到哪一步。

- ✅**已完成** **T121** （完成：2026-09-03 14:05）**自问平仓检查（self_check_close_question）实测验证**（记录：2026-08-25 09:37；机制 2026-08-30 落地，剩盘中实测形态）。**完成说明**：两条验证点全部从**真实生产输出**取到证据——① **输出形态正确**：2026-09-03 会话转录里 `open_position_tiger.py` 真实返回含 `"self_check_close_question": "开仓前自问平仓检查（T121，必答）：如果当前已持有 HK.00100 的多头持仓，现价 357.0 会不会让你想先卖出、等回落再接回（= 现价已是局部高点，不应买在顶部）？答「会」→ 不开仓、等回落的预期接回位再开；…"`，字段与文案按设计输出；② **AI 转录开仓记录确实带上回答**：`actions/2026-09-03-HKT-actions.md` 两条开仓记录分别写有「T121 自答：**不会**——若已持仓现价 357 不想先卖：结构位（351 箱顶支撑两测确认）+ VWAP +10 + 低点连续抬高，357 非局部顶，按突破回踩企稳形态持有」与「T121 自答：不会——574 是 VWAP 回踩位非局部顶，结构位依据充分」；09-02 两条记录同样带「自问平仓检查（T121）」行。**结论：工具在场打印确实驱动了 AI 作答并留痕**——T121 设立时担心的「脚本输出了但 AI 不看/不记」未发生。

- ✅**已完成** **T122** （完成：2026-09-03 14:05）**reason 提层实测验证——全部失败路径首层 `error` 带出具体 reason**（记录：2026-08-25 12:40；机制 2026-08-30 落地，剩盘中实测）。**完成说明**：从 CC 会话转录取到两笔**真实拒单输出**，首层 `error` 已按设计带上首个非空 reason——① `2026-09-02 14:09`（03308 做空）：`"error": "开仓全部失败（尝试 2 档）：Your available funds or buying power are insufficient"`，`failures` 里第一档 `status=Invalid / reason="Your available funds or buying power are insufficient"`、第二档 `status=Submitted / reason=""`；② `2026-09-01 12:12`（02513 9400 股）：同型 `"开仓全部失败（尝试 2 档）：Your available funds or buying power are insufficient"`。**旧行为对照**（2026-09-02 12:40 一笔超时撤单）：`"error": "开仓全部失败（尝试 1 档）"`、首层无 reason——说明改动确实生效、且 reason 为空时保留原文案不画蛇添足。T122 设立的痛点（具体拒因藏在 `failures` 数组里、排查要多挖一层）已消除。

- ✅**已完成** **T126** （完成：2026-09-03 14:05）**当日订单过滤修复后的盘中验证**（记录：2026-08-29 04:05；修复主体 2026-08-30 完成，剩盘中实测）。**完成说明**：两个验证点在 2026-09-03（真实开平 2 笔：00100 开 @356 → 止损触发平 @358.2；03750 开 @574 → 主动平 @572）**全部通过**——① **第四层「白名单外在场敞口」告警无误报**：当日十余个标的的 monitor log 与会话转录中检索「白名单外 / 在场敞口 / 第四层」，命中**全部来自 SKILL.md 文档文本、零条运行时告警**（对照：08-28 误报日就是该告警），且当日多会话并行（在场 15 会话）+ 跨会话多笔开平，正是旧 bug（近两周旧单混入当日净口）最易触发的场景；② **持仓状态行「最新止损价」提取正常**：10:16 段 `📌 持仓状态: HK.00100 16840股(做多) | 账户实持 16840股（默认（模拟）） | 活动止损单触发价 351.00 | 账户一致 ✅`，止损价与下单时附加 LOSS 腿一致；10:26 止损触发后正确切 🚨「账户实持 0 股」告警。注：② 与 T136 的持仓状态行是同一行输出，两条同日收口。

- ✅**已完成** **T116** （完成：2026-09-03 14:05）**限价单 + 止盈单机制实测收尾——三个剩余子项用盘外 mock 场景验证通过，7 子项全数收口**（记录：2026-08-23 22:30；前 4 子项 08-26~29 实盘实测已过）。**完成说明**：新落 `tmp/test_limit_target_paths.py`（mock 掉券商 API、走脚本真实代码分支，**不连网关、不下任何订单含模拟盘**），11 项断言全过——④**移盈 modify**：`move_target_tiger.py` 走主路径（`profit_method: "modify 止盈触发价（单步、无撤单 race）"`），`tc.modify_order` 同时传 `limit_price=105.0` 与 `aux_price=105.0`（券商按单型取其一，不必再纠结哪个字段生效），回读校验 `verified=true / trigger_price=105 / old_trigger=108`，未误走 fallback 下新单；⑥**无止损单平仓补设 STP**：`close_position_tiger.py` 在账户无任何活动条件单时对持仓 1000 股 `long` 调用 `submit_stop_order_tiger(symbol=HK.00700, side=Sell, qty=1000, trigger=100.0)`（触发价=现价、方向=平多、量=持仓量），循环日志记「无活动止损单 → 立刻重设止损 STP 触发价=100.0」；⑥b**止损单铁律恢复链全链**：`account_status.position_status` 在持仓在 + 无活动止损单时输出 🚨「止损单铁律被破坏：**持仓在、止损单必须存在**」并给出恢复命令行 `move_stop HK.00700 long 95.00 1000（失效前止损价 95.00）` → `move_stop_tiger.py` 走 `no_active_stop → submit new STP` 分支下出 STP 95.0/1000 股 → 再次 `position_status` 回到 `活动止损单触发价 95.00 | 账户一致 ✅`、铁律告警消失。**边界（如实保留）**：mock 只验代码分支与参数正确性，**券商端对止盈单 PROFIT 腿落成形态（LMT vs STP）的真实字段语义仍未被实盘校准**——真实移盈动作至今 0 次；下个交易日有真实移盈场景时仍建议看一眼成交回报确认触发价落在预期字段。

- ✅**已完成** **T124** （完成：2026-09-03 14:05）**税闸停用后新开个股单的费用占比观察**（记录/改写：2026-08-30 22:18）。**完成说明**：随 T119 首次对账一并完成（`reviews/2026-09-03-odds-reconciliation.md` 第二节，10 笔样本实算）——① 预设恶化判据**未触发**：样本内无「单笔税损 >0.5R 频现」，个股连亏也未出现印花税主导的形态，**不提请重开税闸**（`config.json stamp_tax_gate_enabled` 维持 false）；② 费用合计 60,552.81 HKD、占 |毛利| 64.4%，个股单税损未单独恶化到需干预的量级。**但本次对账暴露出一个比印花税严重得多、且提高赔率门槛治不了的结构问题**：实盘账户（equity ≈ 10.6 万 HKD、B ≈ 2,126）**单笔双边固定费 188~402 HKD = B 的 8.8%~18.9%（7 笔均值约 15%）**，实盘 7 笔合计**毛亏仅 −1,366 HKD、费用 2,232.51 HKD（费是毛亏的 1.63 倍）**——亏损主因是**按笔固定费在小预算上的摊薄**，不是方向判断、也不是印花税。该问题已单独立为 **TODO.md T139** 交用户裁定（三个方向：实盘暂停日内 / 提高 risk_fraction 摊薄固定费 / 维持现状继续攒样本）。

- ✅**已更新** **T119** （更新：2026-09-03 14:05）**开仓净赔率门槛 1.2 运行对账——首次对账完成**。样本区间 2026-08-28~09-03（1.2 门槛 + 无税闸 + 三阶段分母改净 max_loss 三重口径起点）**港股 auto 实成交 10 笔**（08-28 00700×2 / 09988×2、08-31 02513、09-01 02513、09-02 00100×2、09-03 00100 + 03750；03308 做空拒单未成交不计样本）。实测：**EV = −0.529R、胜率 30.0%（3 胜 7 负）、累计 −5.288R、净盈亏 −66,734.91 HKD**；初始预期 R 均值 2.054 → 落地均值 −0.529（**兑现率 −25.7%**）；分账户：**实盘 N=7 EV −0.705R / 模拟 N=3 EV −0.117R**；对照 2.4 时代（08-06~24，N=16）EV +0.23R（⚠️ 分母口径 08-28 已改，**不可做精确差值比对、只能看方向**）。③ 结论已写入 `trading-strategy.md`「门槛沿革」新增 **⓪-a** 段（含分账户数据与费用结构发现）。**仍保留**：② 门槛回撤预案（「期间日均亏 ≥3R 即回撤 1.5」）——按「期间总 R ÷ 交易日数」口径 −1.058R/日未触发，但 08-28 单日 −3.26R 已超 3R，**两种口径结论相反、交用户裁定**；④ 30-50 笔后复标兑现率与 P(EV>0)（现 10 笔，15 笔时再做一次中间对账）。

- ✅**已更新** **T120** （更新：2026-09-03 14:05）**被拦决策点反事实回填——首批数据开张（数据文件从无到有）**。此前机制落地（08-30）后 `tmp/blocked_decisions.csv` 一直未建立、样本 0。本次从 08-28 / 09-01 会话实录复原出 3 个被净赔率门槛拦下的决策点并 `add` 登记 + 富途分钟 K 回填完成：① `2026-08-28 14:23 HK.00700 long @458.2 / 止损 457.0 / 止盈 459.8 / 净赔率 1.10`（AI 自算「净赔率扣费后约 1.1 < 1.2 门槛，不达标不开仓」）→ 后 60 分钟 high 462.2 / low 456.2 / close 457.2，**stop_hit=1**、假想 R +3.333（乐观口径；保守口径应记 −1R）；② `2026-08-28 13:56 HK.01888 long @45.60 / 止损 44.20 / 止盈 46.50 / 净赔率 0.64`（「止盈距 0.90 远小于止损距 1.40，净赔率远低于 1.2」）→ high 45.6 / low 44.7 / close 44.9、假想 R 0.000；③ `2026-09-01 11:50 HK.02513 long @1223 / 止损 1210 / 止盈 1240 / 净赔率 0.82`（`actions/2026-09-01-HKT-actions.md` 实录的脚本赔率闸拒单，换止盈 1263 后 1.72 才达标）→ high 1232 / low 1222 / close 1230、假想 R +0.692。`stats` 现报「总数 3、已回填 3、待回填 0，样本 <5 先攒数据」。**成本教训（写进剩余动作）**：首批 3 条靠事后逐条翻会话实录复原、耗时远超当日顺手登记——**每日停盯后顺手 add 是唯一低成本路径**。

- ✅**已更新** **T125** （更新：2026-09-03 14:05）**影子交易样本 2 → 3 笔**。新增 `signals/2026-09-03-HKT-signals.md` 的 HK.00100 影子（11:25 开 @360 / 止损 358 / 止盈 368，被拦原因 `open_exposure_today`；11:45 结算，影子止损 358 触发（11:44:27 密采样低点 357.2 穿越 358）、净 −233,879、**落地 −1.000R**）。三笔累计 −1.33R / −1.000R / −1.000R。① 强化版 shadow_hint 引导效果：09-03 收到 blocked JSON 后正确落了影子仓，**但当日只有 1 次被拦事件**、无法判断「多次被拦只落一笔」的旧病是否复发，仍需一个多次被拦的多会话日再验。

- ✅**已更新** **T4** （更新：2026-09-03 14:05）**验证点④（auto 会话采样 log 落 `_auto` 后缀）实测通过**。2026-09-03 为**十五会话并行日**（`tmp/monitor_sessions.txt` 在场 15、会话转录里 `resume.py` 输出「在场盯盘会话数 15」），当日全部 monitor log（`monitor_log_HK_00100_20260903_auto.csv`、`_00700_`、`_09988_`、`_01888_`、`_07709_`、`_07747_`、`_03750_` 等十余个）后缀统一为 `_auto`；`monitor_log_40_20260903_auto.csv` / `_signal.csv` 两个 64 字节残留是 09:28 盘前一次性产物、不属采样主体。**09-01 笔录的 `analysis_beat_20260826_signal.csv` 前缀后缀错位现象未再出现**。①②③ 三项未取到运行时证据（转录中 pool_claim / 层2 / 层3 命中全为 SKILL.md 文档文本；`tmp/pool_claims.json` 停盯时已清空为 `{"date":"2026-09-03","claims":[]}`、事后不可回溯），保留待下个多会话并行日盘中看。

- ✅**已更新** **T136** （更新：2026-09-03 14:05）**模拟账户路径已实测通过，剩实盘路径一项**。09-03 为模拟账户会话（equity 7,772,750、B=155,455），持仓状态行正确输出 `📌 持仓状态: HK.00100 16840股(做多) | 账户实持 16840股（默认（模拟）） | 活动止损单触发价 351.00 | 账户一致 ✅`——**账户标签 / 实持量 / 止损单触发价三项全对**，平仓后正确切 🚨 告警。实盘路径仍无实测：09-02 下午 13:45 那笔实盘开仓（修复落地 12:58 之后）**未跑到采样段持仓状态行**（09-02 会话转录中 `持仓状态: HK.` 运行时命中为 0），故实盘口径仍未验证。

- ✅**已更新** **T137** （更新：2026-09-03 14:05）**验证点① 经实测判据无效、已作废**。查 `~/.zcode/cli/log/zcode-2026-09-03.jsonl` 三个 ZCode 会话（sess_cbe0efe3 / sess_69d1be7d ×2）的 `bootstrap.app.startup.plugins.completed` 事件，**全部 `hookCount: 0`**（同记录 `pluginCount: 7`、`enabledPluginCount: 4`、`commandRootCount: 0`、`skillRootCount: 4`、`stage: "resolve_plugins"`、`pluginStorageRoot: ~/.zcode/cli/plugins`）——该字段**属于插件解析阶段、只统计插件贡献的 hook**，与 config.json 的 `hooks.events` 挂载**无关**（本机无插件型 hook，故恒为 0）。T137 原定「hookCount 应为 7（项目 6 + 全局 1）」的期望值是错的，**已作废、不可再当验收判据**，收窄为剩 ②③ 行为验证（无害违规命令被 deny / Edit 后收到 CHANGELOG 注入）。**旁注**：2026-09-03 在 CodeBuddy 宿主上跑同型探针（`echo "probe: pkill -f vscode"`）返回的是 `Permission denied (safety_rule_deny): security.processPatternTooBroad`——**这是宿主自带安全规则、不是本项目 hook 的 `hookSpecificOutput` 结构，不能当 hook 生效证据**。

- ✅**已更新** **T123** （更新：2026-09-03 14:05）**补同源旁证、维持「下次实盘解锁后复核」**。2026-09-02 实盘 03308 做空 150 股被拒（`available funds or buying power are insufficient`），当日归因「高价股做空在 10 万级小账户受保证金结构性限制」——与本次对账发现的 T139 体量 / 费用结构问题**同源**，裁定 T139 时一并看；07747 的 short_initial_margin 复核仍需实盘解锁后跑 `get_contract`。

### 2026-09-03 14:15 用户裁定（T119 ② 门槛维持 1.2 + T139 实盘维持现状，两条收口）

- ✅**已完成** **T119 ② 门槛回撤预案裁定** （裁定：2026-09-03 14:15，用户）**维持 1.2、继续攒样本**。T119 首次对账（同日上文）给出两种相反口径：「期间总 R ÷ 交易日数」= −5.288 ÷ 5 = −1.058R/日（未触发）vs 08-28 单日 −3.26R（已超 3R）。**裁定采用「期间日均」口径——未触发、门槛维持 1.2**；08-28 单日 −3.26R **不单独作为回撤依据**。理由：样本仅 10 笔，不足以支撑「改变执行面」的门槛调整——符合本项目「交易逻辑建议必须有数理统计基础」硬约束（单日 / 单笔只能触发规则假设，不构成规则长期成立的依据）。`config.json risk.min_net_odds` 未改。**落地**：裁定结论已写入 `trading-strategy.md`「门槛沿革」⓪-a 段与对账报告第四节。**T119 剩余**：只剩 ④ 兑现率与 P(EV>0) 按 30-50 笔复标（现 10 笔），**15 笔时做一次中间对账、复标必须分账户看**。
- ✅**已完成** **T139 实盘小账户「按笔固定费摊薄」裁定** （立：2026-09-03 14:05；裁定：同日 14:15）**维持现状继续攒样本**。事实背景（10 笔实算）：实盘 equity ≈ 10.6 万 HKD、B ≈ 2,126，单笔双边固定费 188~402 HKD = B 的 8.8%~18.9%（7 笔均值约 15%）；实盘 7 笔合计毛亏 −1,366 HKD、费用 2,232.51 HKD（**费是毛亏的 1.63 倍**），EV 差异也主要来自此（实盘 −0.705R vs 模拟 −0.117R）。**裁定**：不暂停实盘、不提高 `risk_fraction`（放大 B 会同步放大单笔风险）、不改赔率门槛（门槛治不了费用）——固定费摊薄作为**已知的结构性拖累**接受，继续按 2% 单笔预算跑实盘 + 模拟攒样本。**落地**：结论已写入 `trading-strategy.md`「门槛沿革」⓪-a 段（「用户裁定（2026-09-03 14:15）」句）与对账报告第四节；**后续影响**：① T119 ④ 复标时**必须分账户看**（合并看会掩盖「费用」这个独立变量）；② T123（07747 做空拒单真因）与 T139 同源，下次实盘解锁复核时一并看；③ 「已知代价」如实保留——维持现状 = 在费用结构性拖累下继续实盘交易，1.2 时代样本大概率继续呈负 EV，后续对账数字要按此预期解读。

### 2026-09-03 17:25 T138 归档（CodeBuddy 宿主 hooks：端到端验证两项实证全过）

- ✅**已完成** **T138** （完成：2026-09-03 17:25）**CodeBuddy 宿主 hooks 端到端验证——PreToolUse 与 PostToolUse 两条投递链均实证生效，原「未挂载」误判彻底证伪**（记录：2026-09-03 14:05；更新：16:35 误判修正；17:25 归档）。**当日修复主体回顾（16:35 前已落地、17:25 复核未改）**：`live_auth_witness.py` 认 `ask_followup_question` + `_xml_question_answer_texts` 解析 XML 字符串 tool_response（四组样例回归过）；`changelog_guard.py` 兼容 IDE 驼峰 `filePath` 字段 + 修 `_PROJECT_ROOT` 三层 parent 真 bug（原两层算到 `.claude/`、项目根文件 `_should_remind` 恒 False = 上线以来对日常改动静默 no-op）；`pre-tool-use-guard.sh` 认 `execute_command`；项目 CLAUDE.md 改「三宿主」口径。**本次端到端实证（新 CodeBuddy 会话，非盘中）**：① **PreToolUse 链生效**——探针 `head -c 1 /tmp/nonexistent-xyz-probe/accounts.json`（假路径，不泄漏凭证）被 deny，拦截文案逐字来自本项目 `secret_guard.py` 的 stderr；且仅收到**一次** deny = `.claude/settings.json` 与 `.codebuddy/settings.json` 双份挂载被 CodeBuddy 官方自动去重、**无翻倍**，暂不需要删 `.codebuddy/settings.json` 兜底；② **PostToolUse 链生效**——编辑文件后 hook 被调用（`/tmp/hook_debug.log` 临时 debug 实证），payload `tool_name='Edit'`（IDE 内部已规范化为 CC 风格名，脚本内写死 `Edit` 判断可用）、`tool_input_keys=['filePath','old_str','new_str']`（驼峰字段到达 hook，`changelog_guard` 的 `filePath` 兼容正确消费）、`should_remind=True`；IDE 将 hook 输出转成 `<system-reminder>📌 CHANGELOG 记录提醒…` 注入 tool-result 的 `additionalContext` 字段（转录 `messages/*.json` 实证），**模型可见**——即改文件记 CHANGELOG 提醒链在本宿主真正打通（当日 `changelog_guard` 修复从 False→True 后的首个运行时证据）。**剩余（非阻塞、自然场景触发）**：① `live_auth_witness` 的实盘授权点击写凭证链路（`ask_followup_question` + XML answers）需真实实盘授权流程才能触发，脚本逻辑四组样例已回归过，宿主侧投递留待下个实盘授权点击顺带确认；② `monitor_guard` 的 Stop / TaskStop 提醒需盘中盯盘会话触发，本会话盘外无法构造。两条均为「场景到了自然验证」型，不入活跃 TODO（hooks 挂载与两条主投递链已实证，同配置同机制）。全局守卫 `pre-tool-use-guard.sh` 未挂 CodeBuddy 用户级 = 用户 2026-09-03 裁定不挂，维持现状。**归档依据**：本条目原始担忧（六条项目 hook 在 CodeBuddy 宿主因无挂载而失效 → 凭证泄漏 / 授权见证断 / CHANGELOG 提醒断）已被 PreToolUse deny + PostToolUse 注入两项运行时证据彻底推翻。

## 2026-09-02 批量处理（T136 修复主体落地）

- ✅**已更新** **T136** （更新：2026-09-02 12:58）**account_status / monitor_segment 按下单账户查持仓与订单——实盘会话误查模拟账户的持仓状态误报修复（修复主体）**：原条目（记录：2026-09-02 11:56）指出实盘开仓（00100，`--account live`）后采样段持仓状态行连续误报「账户实持 0股（默认（模拟））」+「无活动止损单」（实盘持仓与止损 / 止盈双腿实际都在场），根因 = 查询方一律查默认模拟账户、与本会话实际账户口径错配（当日 actions 记录漏写「账户」行、旧格式 intent 无账户字段，两个判据都失效）。本次修复：① **账户口径进 intent**——`trade_mutex.append_intent` / `TradeMutex` 增 account 字段（'live' / None），`open_position_tiger.py` / `open_position_tiger_us.py` 下单时传入，此后每笔开仓的账户口径在 intent 日志精确到笔（settle_intent 重写整行时解析后回写、新字段自然保留）；② **account_status.py 按标的解析账户**——新增 `resolve_symbol_accounts` 统一出口（intent account 字段优先 → 当日 actions「| 账户 | 实盘」行兜底 → 默认模拟账户垫底；旧格式 intent 记录无 account 字段**不进映射**、不会挡住兜底——本地冒烟实测抓出过这个坑），`_query_account` 改单一账户入参、`position_status` 按标的分组查询（同账户只查一次、查询失败按账户去重不刷屏、「actions 无记录持仓」按账户比对不跨账户误报、新增 account 显式参数）；③ **monitor_segment.py 同根因一并修**——`query_stop_prices` 按账户分查当日订单（实盘标的查实盘止损单），第四层单持仓检测改逐账户判（单持仓护栏是账户级口径，与互斥闸只查下单账户订单流的语义对齐，实盘一笔 + 模拟一笔不属违规）；④ 当日 actions 开仓记录补记「账户 | 实盘」行（原记录漏写，order_id 44488568990072832 可证）。**验证**：5 脚本 py_compile 过；账户解析链路本地冒烟过（当日旧格式 intent + 补记后 actions → HK.00100 正确解析为实盘；空仓时 position_status 返回 None、零 API，「空仓不查」规则不受影响）。**仍保留（见 TODO.md T136 新正文）**：下个实盘交易日开仓时盘中实测（状态行显示实盘账户 + 止损价带出 + 多会话不串台）。

## 2026-08-30 批量处理（T126 修复 + T122 / T121 / T127 落地 + T116 / T120 / T123 可做部分完成）

- ✅**已完成** **T126** （完成：2026-08-30 23:19）**`get_today_orders_tiger` 未过滤日期——「单持仓违规」告警误报根因**（记录：2026-08-29 04:05）。**完成说明**：① `trade_utils_tiger.get_today_orders_tiger` 改为取回后按 `order_time`（毫秒时间戳）转本地日期过滤当日（口径与 trade_mutex._order_local_date 一致：港股北京日期、美股跨夜归组；无时间戳订单保守保留、宁多不漏）——过滤下沉到函数本体，monitor_segment 第四层兜底、stop_gate 等全部调用方一并受益；② 美股版 `get_today_orders_us` 同源同病、同日一并修（结构对齐、查单失败仍上抛由闸门保守拒开）；③ 过滤逻辑 mock 单测通过（当日留 / 昨日与 25 天前剔除 / 无时间戳保留，3+1 → 2）。**盘中验证（剩余一项，随下个交易日顺带）**：真实开平一笔后告警应即时消除；`monitor_segment.py` 持仓状态行最新止损价提取同走本函数、修后顺带观察提取正常。

- ✅**已完成** **T122** （完成：2026-08-30 23:19）**开仓失败被拒 reason 提到首层 JSON error 字段**（记录：2026-08-25 12:40；2026-08-30 22:18 收窄为 reason 提层一项）。**完成说明**：`open_position_tiger.py` / `open_position_tiger_us.py` 开仓全部失败路径的首层 `error` 从「开仓全部失败（尝试 N 档）」改为追加「：+ 首个带非空 reason 的失败档原因」（最早的失败原因通常就是结构性原因；全部 reason 为空时保留原文案）——排查不用再挖 failures 数组。语法检查过；下个交易日开仓失败时实测输出形态。

- ✅**已完成** **T121** （完成：2026-08-30 23:19）**开仓前「自问平仓」检查落地（防开在局部极值点）**（记录：2026-08-25 09:37 用户立）。**完成说明**：按「文档规定必须尽可能配工具强制」评估——该检查是纯认知判断类（局部高 / 低点判断依赖盘面结构、无法机械判据），不做脚本硬拦，按第②层「决策时刻工具在场打印」落地：`open_position_tiger.py` / `open_position_tiger_us.py` 在净赔率校验后固定输出 `self_check_close_question` 字段（做多问「已持多仓会不会想先卖等回落接回」、做空问「已持空仓会不会想先买回等更高再卖」；答「会」→ 不开仓等预期接回位，答「不会」→ 依据栏写明理由后照常下单）——AI 读脚本输出必见、转录开仓记录必须带上回答。语法检查过；下个交易日开仓时实测输出形态。

- ✅**已完成** **T127** （完成：2026-08-30 23:19）**停盯总结「只总结本会话」规定的工具强制落地**（记录：2026-08-29 13:41 用户立规定；2026-08-29 18:00 评估候选、倾向在场打印）。**完成说明**：按既定倾向落地候选①——`monitor_unregister.sh`（停盯收尾最后一步）在注销 + 释放认领后固定打印「停盯总结范围提醒（T127）」三行：接下来的总结只总结本会话自身的标的池 / 交易 / 影子仓 / 采样分析执行情况，禁止混入其它会话交易与账户全口径数字。强制层级说明已写进脚本注释：总结是纯对话输出、不经 Write/Edit/Bash，PreToolUse 无处挂，只能用在场打印层。bash -n 语法过；下次停盯时生效。

- ✅**已更新** **T123** （更新：2026-08-30 23:19）**做空拒单真因复核——short 版购买力函数已补、实盘复核部分保留**：原条目（记录：2026-08-25 12:40）两个剩余动作，本次完成其一——`trade_utils_tiger.py` 新增 `get_short_buying_power_tiger`（与多头版共用 `_buying_power_impl`，只差保证金率字段：short 用 `get_contract().short_initial_margin`（SDK Contract 字段实存、已核源码），查不到按 1.0 全额最保守），做空预算不再借用 long_initial_margin 口径。**仍保留（依赖下次实盘解锁，见 TODO.md T123 新正文）**：用 short_initial_margin 复核 07747 口径下真实可空量、重写当日拒单归因。

- ✅**已更新** **T120** （更新：2026-08-30 23:19）**被拦决策点反事实回填机制脚本已落地、数据积累开始**：原条目（记录：2026-08-24 13:40；2026-08-30 22:18 删税闸维度）的剩余动作 ②「脚本化」，本次完成——新建 `scripts/blocked_backfill.py`（add 登记被拦决策点 + 同键去重 / backfill 富途分钟 K 回拉决策点后 60 分钟高低收 / stats 汇总假想 R 分布），数据文件 `tmp/blocked_decisions.csv`（列含 net_odds、high60/low60/close60、stop_hit、hypothetical_r；假想 R 为乐观口径——最优极值离场的理论上限，止损触及行真实场景按 -1R 单独看）。空数据路径自测过。**仍保留（见 TODO.md T120 新正文）**：① 停盯后 / 次日盘前按条目实际执行 add / backfill 攒数据（≥30 个被拦点）；③ 攒够后按假想 R 分布重标定门槛。

- ✅**已更新** **T116** （更新：2026-08-30 23:19）**auto-mode.md「实测状态」节已刷新**：原条目（记录：2026-08-23 22:30；2026-08-30 22:18 收窄为剩 3 子项 + 文档刷新）的文档刷新部分，本次完成——`auto-mode.md` 港股段（08-03/08-05 旧机制描述后）与美股段（08-05 SPY 实测后）各补「2026-08-23 双腿 BRACKETS 机制起实盘实测通过」段：① LMT+双腿提交、② PROFIT 腿落成形态（order 76 CANCELLED 核实）、③ BRACKETS 联动作废、④ 交替逼近平仓、⑤ 限价超时撤单五项已过（08-26~29 实盘实录），未实测三项（移盈 modify / 补设 STP / 止损铁律恢复链）显式标注为 T116 剩余。**仍保留（见 TODO.md T116 新正文）**：三个剩余子项的盘中实测（须刻意构造或等自然场景）。



## 🟢 绿色紧急度·2026-08-30 待办时效性排查（T5 归档 + 六条改写留痕）

- ✅**已完成** **T5** （完成：2026-08-30 22:18，判定依据 08-25~28 实盘实录）**实盘误开防护落地后的开盘时段补验**（记录：2026-08-21 19:15）：盘外已验 6 项（hook 触发、授权链端到端、会话绑定三场景、6 脚本 live_locked 拒单、实盘不挂代理、模拟盘照旧代理）；当时剩一件——首个实盘交易日补验「授权解锁后实盘查询链路通（AskUserQuestion 授权 → verify-auth → get_assets 实查请求受理正常）」。**完成说明（2026-08-30 22:18 归档）**：补验对象在后续四个实盘交易日（08-25 07747、08-26 07709、08-27 01888、08-28 00700/09988/NVDA/CRM）持续隐式发生——每日多个 `live-unlock.*.local` 会话凭证文件在位（08-28 当天 6 个），实盘开仓（LMT 成交 + 双腿激活）、equity 查询（actions 记录「equity 见本机 equity-log」实时取值）、平仓、多会话并行实盘下单全链路正常走通——「授权 → 解锁 → 实盘查询 / 下单受理」链路若不通，这些交易不可能发生，补验条件早已满足、只是无人回头收口，本次时效性排查中确认归档。

- ✅**已更新** **T116** （更新：2026-08-30 22:18）**限价单 + 止盈单机制实测收窄**：原条目（记录：2026-08-23 22:30）要求 paper 实测 7 个子项。时效性排查发现其中 4 项已在 2026-08-26~29 **实盘**实测通过（比原计划的 paper 验证更强）——① LMT+双腿提交（07709/01888/00700×2/09988×2/NVDA/CRM/PLTR 多次成交）、② PROFIT 腿落成形态（08-28 order 76 CANCELLED 核实）、③ BRACKETS 联动作废（08-28 两笔核实）、⑤ 交替逼近平仓（08-26/28/29 三次实录）、⑦ 限价超时撤单（08-28/29 两次实录，撤后重挂即成交）。剩余 ④ 移盈 modify、⑥ 无止损单平仓补设 STP、⑥b 止损单铁律恢复链（三项至今无真实触发场景）+ auto-mode.md「实测状态」节刷新——见 TODO.md T116 新正文。

- ✅**已更新** **T119** （更新：2026-08-30 22:18）**开仓门槛对账口径改写**：原条目（记录：2026-08-24 13:40；更新：2026-08-25 13:05）是「1.2 门槛 + 1.2% 税闸双阈值」口径。时效性排查发现税闸维度已随 2026-08-28 整体停用失效（税闸 1.2% 口径实际只跑了一个交易日 08-26）；门槛侧实跑素材已具备（08-26 07709 放行、08-29 PLTR 0.94<1.2 拒单）。改写为纯 1.2 门槛口径、对账基线改为「1.2 门槛 + 无税闸」时代（08-28 起）、补净口径切换警示（08-28 起分母净 max_loss、与历史毛口径不可混标）——见 TODO.md T119 新正文。

- ✅**已更新** **T120** （更新：2026-08-30 22:18）**被拦决策点回填删税闸维度**：原条目（记录：2026-08-24 13:40；更新：2026-08-25 13:05）的被拦原因含「赔率/税闸」两维。税闸 08-28 停用后被拦原因只剩净赔率门槛一项；机制本身仍未落地（tmp/ 无 blocked_decisions.csv、无 blocked_backfill.py，08-28 analysis_beat 决策点仍只有一行 log）、痛点现行，条目保留；补「与 T125 影子交易的分工」（被门槛拦 vs 被互斥闸拦，互补不重叠）——见 TODO.md T120 新正文。

- ✅**已更新** **T122** （更新：2026-08-30 22:18）**做空提价规则修复实测闭环、收窄为 reason 提层**：原条目（记录：2026-08-25 12:05；更新：2026-08-25 12:30）剩余 ③ paper+live 各实测一单限价空单 + ④ 被拒 reason 提首层 JSON。时效性排查发现 ③ 已于 2026-08-26 实盘实测通过（HK.07709 空单 LMT @39.06 成交、依据栏明写「做空挂 ask 符合提价规则」——生产网关实测，比 paper 更强）；④ 未做（源码首层 error 仍只有「开仓全部失败（尝试 N 档）」、reason 藏在 failures 数组）。收窄为只剩 ④——见 TODO.md T122 新正文。

- ✅**已更新** **T124** （更新：2026-08-30 22:18）**税闸专项对账收窄为费用占比观察**：原条目（记录：2026-08-27 11:01；更新：2026-08-27 11:30）的三条剩余跟踪动作（窄止损单税闸专项对账 / 预检「低」档拒单率观察 / 与 T119 双口径合并复盘）全部围绕税闸展开——税闸已于 2026-08-28 13:10 整体停用（`stamp_tax_gate_enabled: false`），无闸可对账、预检列已消失、税闸维度已不存在，跟踪对象整体消失。改写收窄为唯一仍有效的残留关切：停用后新开个股单的实际印花税损占比观察（T119 对账时顺带看，恶化再提请裁定重开）——见 TODO.md T124 新正文。

- ✅**已更新** **T125** （更新：2026-08-30 22:18）**影子交易补 08-28 首跑进度**：原条目（记录：2026-08-27 22:00）的 ① 首跑验证已部分发生——2026-08-28 完整影子交易 1 笔（HK.03308 开仓→平仓 −1.33R，signals 条目格式正确带【影子】标、状态文件可查），落仓链路通；但同日三次被拦只落一笔（两次凭错误记忆跳过、漏 2 笔），shadow_hint 文案 08-28 12:40 已强化、效果待再验。② 复盘链仍未实测（08-28 后无新 trades.csv）、③ 样本仅 1 笔、④ 注意力分流未观察——见 TODO.md T125 新正文。

## 🔴 红色紧急度·2026-08-30（T118 停盯边界守卫全链收尾）

- ✅**已完成** **T118** （完成：2026-08-30 18:55）**停盯边界守卫缺位——AI 曾在 11:48（距收盘 12 分钟）以「空仓+无信号」自行停盯收尾，违反「盯到用户喊停或收盘（取先到）」**（记录：2026-08-24；2026-08-24 12:00 修复主体落地；2026-08-30 18:55 剩余两项处理完毕、整体闭环归档）。**修复全链**：① `scripts/stop_gate.py` 时间闸（盘中距收盘 >5 分钟且无 `--force` → 拒绝；收盘窗口 ≤5 分钟 / 盘外自动放行；港股 09:30-12:00/13:00-16:00 + 美东 04:00-16:00 同源时段判定，5 场景单测通过）；② `monitor_unregister.sh` / `keep-awake off.sh` / `live_unlock.py revoke` 三处收尾入口全部接入闸；③ 四个采样脚本停盯边界提醒行全部补「⛔ 空仓 / 无信号 ≠ 停盯理由——盯到用户喊停或收盘（取先到），停盯收尾脚本已受 stop_gate 时间闸硬拦」在场打印——8-24 当日只补了主力源 `futu_ws_segment.py`，8-30 补齐 `monitor_segment.py` / `ws_segment.py`（对齐主力源措辞，py_compile 过）。**实盘验证（8-26 / 8-28 两个完整盯盘交易日实录）**：两日港股收尾段 log（`tmp/seg_94_26.log` 8-26 11:56 段、`tmp/seg_cur.log` 8-28 11:55:34 段）均打印「距HK停盯边界 4 分钟（≤5）：绝对不开仓窗口」——收尾时点落在收盘窗口内，stop_gate 按 `hk_close_window` 放行、未误拦正常收盘收尾；期间无盘中提前停盯违规。验证条件满足（首个完整交易日不误拦），归档闭环。



- ✅**已完成** **T1（第二步）** （完成：2026-08-26 14:32）**公开仓库敏感行为记录清理·第二步：git 历史重写 + 全仓财务信息中性化**（记录：2026-08-21 20:02；2026-08-23 22:30 第一步完成；2026-08-26 14:32 第二步执行完毕、T1 整体闭环归档）。**触发**：2026-08-26 全团队仓库敏感信息大排查（起因 ExecutiveAssistantAgent CHANGELOG 财务描述泄漏事故及全局规则同日增补「财务状况」敏感类型），排查发现本仓 GitHub 当前可见文件仍含节点 IP / 订阅商 / 报文 / 实盘净值购买力精确值（第一步中性化改动一直未 commit），用户指示「清理敏感信息，但前提是不影响正常功能」。**完成内容**（敏感值以类别表述、不在此复述）：① mirror clone 中 `git-filter-repo --replace-text` 重写 main + win 全部 63 个 commit——替换清单含全部节点 / 出口 IP、订阅商域名与节点名、白名单报文原文、实盘净值 / 购买力 / equity-log 权益系列数字、量级与本金规模表述（模拟账户系列数字与 2026-08-17 用户裁定保留的模拟账户号历史明文不动）；HTTPS 通道 force push 覆盖远程两分支（SSH 经代理推送断流）。② 工作区与暂存区 20+ 处实盘数字 / 量级口径中性化（CHANGELOG / SKILL.md / CLAUDE.md / notes / reviews / actions / 脚本注释 / equity_guard.py），「量级口径允许」旧规按 2026-08-26 全局新规收紧为「量级同样不写」。③ 本地对齐：分支指向新历史、reflog 清空、gc 清旧对象、两 worktree 纯重写差异文件 restore 对齐。**验证**：远程 main 实际内容扫描零命中、本地旧 commit 对象已物理删除、0 fork、无 Release 无 tag；老虎通用错误码（不含用户信息）保留。**遗留（需用户动作，另见 CHANGELOG 同日条）**：① `signals/equity-log.csv` 仍被跟踪且为功能数据（resume 依赖真值），历史版本已替换为标记——须 `git rm --cached signals/equity-log.csv` 停止跟踪（gitignore 对已跟踪文件无效）；② 暂存区为清理前 add 的旧版，提交前须重新 `git add` 全部改动文件，否则敏感句随提交回归；③ GitHub 悬挂旧对象待其自动 GC（页面导航不可达，风险低）。

## 🔴 红色紧急度·2026-08-23 批量处理（T1 第一步工作区中性化完成）

- ✅**已完成** **T1（第一步）** （完成：2026-08-23 22:30）**公开仓库敏感行为记录清理·第一步：工作区中性化改写**（记录：2026-08-21 20:02；2026-08-23 22:30 完成第一步、条目更新只剩第二步）。**原始背景**：探讨「交易项目公开的利弊」时发现——仓库虽已 gitignore 账户号 / 凭证 / 净值，但**代理交易的行为记录本身**被 git 跟踪、公开在 GitHub，等于书面自证「人在境内、经代理节点绕行下单」；趁 0 fork、2 star 清理代价最低。**完成内容**（敏感值以「文件 + 处理方式」表述、不在此复述具体 IP / 报文）：accounts.md 第 14/15/17/19 段（节点 IP 串、域名体系、监管报文引用、绕行实测故事线 → 中性技术结论 + 「节点清单与白名单镜像存本机配置」）、trade_utils_tiger.py docstring×3 + 报错提示、config.example.json 注释×2、SKILL.md 33 行境外网络段、live_unlock / proxy_guard / ws_segment / account_status 四脚本注释、CHANGELOG 12 处历史条目（保留「为什么改 + 改了什么」纪律、脱敏可定位细节与身份自述）、TODO-archive 30/63 行、auto-mode 1 处。**复扫验证**：`git grep` 全部敏感模式（节点 IP / 域名 / 订阅商名 / 监管错误码原文 / 订阅商英文名 / 本地出口俗称）零命中；「境外标的」3 处保留（跨境 ETF 跟踪的韩国股市标的、中性金融术语）。CHANGELOG 见 2026-08-23「T1 第一步」条。**第二步（git 历史重写 filter-repo + force push）仍在活跃 TODO T1**——破坏性操作、待用户确认后执行。

## 🟠 橙色紧急度·2026-08-23 批量处理（T3 commit 扫描检测面扩充完成）

- ✅**已完成** **T3** （完成：2026-08-23 22:30）**敏感内容扫描检测面扩充（防复发）**（记录：2026-08-21 20:02）：按原方案四方向落地——全局 `~/.claude/skills/commit/SKILL.md` 第 2 步敏感扫描新增「敏感行为记录」类别（① 公网 IPv4 字面量、② 代理服务商标识、③ 券商监管报文、④ 地域规避叙述——「境内拒境外受理」类故事线；合规要求中性表述不拦），扫描方式从「文件名匹配 + 抽样」升级为「全部暂存文件跑内容正则」；文档示例网段（192.0.2.x 等）与回环 / 内网 IP 放行防误报。镜像同步至 CapabilityManagerAgent `claude/skills/commit/`（diff 逐字节一致）、同步记录其 CHANGELOG。

## 🟢 绿色紧急度·2026-08-23 批量处理（T2 免责声明补齐完成）

- ✅**已完成** **T2** （完成：2026-08-23 22:30）**README / LICENSE 免责声明补齐（交易项目公开边界配套，两仓库）**（记录：2026-08-21 20:02）：① DayTradingAgent README.md / README_cn.md 风险声明段补「回测或模拟盘结果不代表实盘 + 用户对交易决策及损失承担全部责任」（两版同步）+ LICENSE.md 文末追加中英双语投资风险免责条款（不构成投资建议、回测不代表实盘、风险自担、补充而非替代 MIT 条款）；② QuantStrategistAgent 两版 README 同样补「风险自担」句 + swing/STRATEGY.md 头部 blockquote 一句话免责 + LICENSE.md 追加同款条款（措辞按量化研究语境微调）。两仓 CHANGELOG 各自记录。

## 🟢 绿色紧急度·长桥可做空标的扫描（2026-08-19 22:05 放弃归档，用户裁定不处理）

- ✅**已放弃** **T6** （放弃：2026-08-19 22:05）**长桥可做空标的全量扫描（港股盘中重跑，方法已验证）**（记录：2026-08-18 17:05）：2026-08-18 完成下单探测法验证与全量扫描脚本（`tmp/scan_longbridge_shortable.py`），当日 16:00 收盘后扫描结果全部作废（收盘后长桥对卖空探测一律 602315 监管拦截，须盘中重跑：删 INVALID 行 / `LONGPORT_REGION=global` / 探测间隔 ≥1.5s/笔 / 与老虎 1257 只对比产出差异清单；方法细节见 `tmp/longbridge_scan_method.md`）。**放弃理由（2026-08-19 用户立）**：不再需要这个对比，重跑步骤不执行。前置结论仍有效：两家做空池上限同为港交所名单（1234 只，见 2026-08-18 15:03 归档条目）、实际差异只在借券池深度；若日后需要长桥侧可空清单，方法与坑的留档在 `tmp/` 可复用（注意 tmp/ 属临时产物、git 不跟踪，正式需要时应重新验证）。

## 🟡 黄色紧急度·长期备忘·代理白名单（2026-08-19 21:52 归档，守护自动化后无需备忘）

- ✅**已完成** **T7** （完成：2026-08-19 21:52）**代理节点 IP 变动后同步老虎白名单（长期运维）**（记录：2026-08-17 13:05；2026-08-19 21:45 最后更新）：IP 白名单 2026-08-17 上线（`apply_scope: all` 模拟盘金丝雀，直连已被白名单拒绝）。2026-08-19 漂移实录（当日 13:44 处理橙色待办时发现）：代理订阅刷新后三节点 IP 全变、当前节点出口漂出白名单致老虎 API 全断，人工 `xpilot switch` 切回白名单内节点恢复——当日据此把本条从「人工流程备忘」升级为「守护自动化」落地：新增 `proxy_guard.py`（preflight 启动时 + monitor_watcher 盘中每 10 秒挂接）自动做「漂移检测 + 自动切白名单内节点恢复 + 响铃弹屏幕中心弹窗给加白 IP 串」，弹窗按钮确认制（老虎页加白后点「已添加」→ 本地 config.json `tiger_whitelist` 自动合并，2026-08-19 21:39 首次真实闭环 3 IP）。**归档理由（2026-08-19 21:52 用户立）**：守护 + 弹窗提醒已全自动覆盖该流程，无需再留在待办里当备忘——漂移发生时弹窗自会提醒，剩余两小步（老虎页粘贴 + 点已添加）见 `accounts.md`「老虎 IP 白名单 + 代理节点」节运维口径。

## 🟠 橙色紧急度·2026-08-20（watcher 中断警报 + 掩蔽提示删除，横幅待办合并收窄）

- ✅**已完成** **T8** （完成：2026-08-20 11:24；11:40 按「默认开启」再终结）**密采样守卫默认不开启、用户明确要求才开启**（记录：2026-08-20 09:58）：现行机制是 AI 启动盯盘时自主 `launchctl load` watcher、停盯 `unload`（2026-08-04 立「随盯盘开关」），且 launchd 列表当前已加载（2026-08-20 实测 `com.daytrading.monitor-watcher` 在跑）——用户没要求也会自动开启，配合守卫误报扰民。**完成说明（两段裁决）**：① 当日 10:35 用户下令关掉 watcher（unload 已执行）、11:24 下令**删除「中断警报 + 掩蔽提示」两个功能**（多会话误报扰民，见 CHANGELOG 2026-08-20 11:24 条）——watcher 收缩为只留「空转警报 + 白名单漂移检测」；② **11:40 用户再立：收缩后只剩这两个低频警告，恢复「盯盘开始默认开启」**——preflight 新增 `_ensure_watcher()` 自动部署 + load（幂等），停盯不联动 unload（常驻无害、盘外自动静默）。本条「默认不开启」决定被 ② 直接取代：主扰民源（中断 / 掩蔽连弹）已删，剩余提醒自带冷却不扰民，默认开启的前提成立；最终态 = 盯盘启动自动开启、仅用户明确说关才 unload（unload 后下次 preflight 会自动重新 load）。

## 🟠 橙色紧急度·2026-08-20（互斥闸口径缺陷，被并行会话修复）

- ✅**已完成** **T9** （完成：2026-08-20 04:50）**修复单持仓闸 today_open_exposure 口径缺陷：订单流净额把历史订单算成假敞口**（记录：2026-08-20 01:11，美股盯盘实测发现）：当日美股盘中一次合法开仓（US.MU 做多、自查全过）被闸门拒——`get_orders()` 返回近两周全量订单，`today_open_exposure` 把 2026-08-05 港股 00981 的历史开平链残留计数（净 +1）误判为「今日在场敞口」（实测账户零持仓）。**完成说明**：并行会话修复（CHANGELOG 2026-08-20 04:20「开仓互斥闸误拦：订单流加今日过滤」）——`trade_mutex.py` 新增 `_today_orders`（order_time 毫秒时间戳→本地日期过滤），`TradeMutex.blocked` 口径②③计算前统一过滤。我方停盯后独立验证：实跑全量 100 单 → 今日过滤后 0 单、敞口 set() 为空、闸门放行（详见 `tmp/analysis_beat_20260819_auto.csv` 04:53 行）。当日该误拦反而避开了 MU 一次假突破止损（939.85→935 跌破止损位 935.5），属运气、不改变缺陷性质。

## 🟠 橙色紧急度·2026-08-19 批量处理（2026-08-19 13:44，七项全清）

- ✅**已完成** **T10** （完成：2026-08-19 13:44）**平仓脚本输出 App 实测净利（net_pnl_app 字段）**（记录：2026-08-19 10:50）：`trade_utils_tiger.py` 新增 `attach_net_pnl_app()`、港美平仓脚本（close_position_tiger / close_position_tiger_us）四个成交出口（modify 主路径 / 无止损直接 MO / fallback 撤止损+MO / fallback 持仓复查为空）全部接入。取数三级优先（**当日实盘实测校验**：01810 第一笔 close 成交单 `realized_pnl`=473.93 与 App 已实现盈亏完全一致；第二笔毛 900 − 双边 commission 425.13 = 474.87 与 realized_pnl 一致——两条路径互验）：① close 成交单 `realized_pnl`（与 App 同源，AI 转录直接抄）；② 毛盈亏 − 双边 `commission`（开仓成交单 + 平仓成交单实收总费用，附 `fees_app` 字段）；③ fee_schedule 复算兜底（`net_pnl_source: estimate`，转录须注明待账单核对）。实现要点：`get_filled_orders` 的 `start_time` 必填（实测缺省报 code=1010）；查询失败不阻断平仓输出（只记 `net_pnl_warning`）；无 close_order_id 的 fallback-空仓路径按该标的最新成交单兜底定位。三路径离线实测验证通过（474.87 / 474.87 / 30.68）。
- ✅**已完成** **T11** （完成：2026-08-19 13:44）**fee_schedule.py 补印花取整 + 交收最低费规则**（记录：2026-08-19 10:50）：港股印花税向上取整到元（`math.ceil`）+ 交收费每笔最低 2 元（`max(CCASS_MIN, 比例)`），`HK_GOV_RATE` 同步剔除 CCASS 比例部分（单独走 max 计算、防重复计——实现中发现并修正两个自身 bug：初版 return 丢平台费 + CCASS 重复计）。**用当日实盘 01810 四笔成交单 SDK commission 逐笔对账**：BUY 137500→210.34 零差、BUY 138800→211.88 零差、SELL(STP) 139700→213.25 零差、SELL(STP) 138400 实测 215.74 vs 模型 211.71 仍差 +4.03（该 STP 单另有费项——App 侧费用合计 426.08 与 SDK commission 之和一致，差在券商实际收费、非模型漏项，待账单核）。旧纯比例模型四笔分别差 +0.50/+4.63/+0.20/+0.30。**三处共享链路回归全通过**：review.py 61 笔、bayes_evolution.py 同 CSV、盯盘前瞻 `_net_odds`（01810 27.76→28.49 止损 27.56 净赔率 3.221）。
- ✅**已完成** **T12** （完成：2026-08-19 13:44）**futu_ws_segment.py 订阅缓冲污染 high/low 列**（记录：2026-08-19 10:52）：富途 subscribe TICKER 成功瞬间推历史缓冲逐笔（含开盘以来成交）污染段统计。修法：订阅发起前取 `SUB_EPOCH`（epoch 秒），`TickerSink` 丢弃逐笔自带 time 早于该边界的行（不进 high/low/last/sec 统计）；time 解析失败保守丢弃（宁少一个点、不收来历不明极值）。时区要点：TICKER time 是本地墙钟字符串、用 `time.mktime` 本地解析与 `time.time()` 同系（勿用 `calendar.timegm`、差 8 小时——实现中踩过并修正）。`_row_epoch` 单测四场景通过（现在/5 分钟前/带毫秒/坏值）。
- ✅**已完成** **T13** （完成：2026-08-19 13:44）**account_status.py 实盘持仓查询误报「实持 0 股」**（记录：2026-08-19 10:52）：根因 = `_query_account` 里 `get_positions` 抛异常（当日实况：代理节点漂移出老虎 IP 白名单、access forbidden）被 except 静默吞成 `[]`——查询失败与真空仓在段输出里长得一样、都打「🚨 账户已无持仓」假告警。修法：查询异常不再吞——positions 记 None、错误文本带回段输出（「持仓状态查询失败」显式区分于「账户已无持仓」，前者不触发平仓处置、附 actions 口径持仓 + 排查指引）；空 symbol（合约缺代码 / 标题无代码的补充记录）不再进 holding 与 acct_syms（修「无记录持仓 ['']」假告警）。真实链路验证：monkeypatch 场景 + 真账户查询（当前实盘空仓、伪造在场持仓时告警正确触发）。
- ✅**已完成** **T14** （完成：2026-08-19 13:44）**account_status.py 跨会话告警串台（多会话并行时段输出打错会话）**（记录：2026-08-19 12:05）：修法与「mode 分文件」机制对齐——数据源用 `tmp/trade_intent.log` 当日 filled intent（开仓脚本在 TradeMutex 内写、自带 `CLAUDE_CODE_SESSION_ID`），`_session_of_symbol()` 建「标的 → 开仓会话」映射；`_holding_from_actions(only_session=...)` / `position_status()` / `blast_check()` 带 `CLAUDE_CODE_SESSION_ID` 时只核对**本会话**开的仓（别的会话的仓由别的会话核对、告警不串台、空仓会话不再每段收到别会话持仓核对输出）；无会话 id（手动跑脚本）退回全核对旧行为。A/B 会话隔离单测 + 真实账户链路验证通过；monitor_segment 的 blast_check 调用签名不变、无需改动。
- ✅**已完成** **T15** （完成：2026-08-19 13:44）**Watcher 提醒写入持久 log（通知历史可追溯）**（记录：2026-08-18 13:41）：`notify()` 加追加写 `tmp/watcher_notify.log`（每行「时间戳 + key + 消息全文」，`"a"` 模式不覆盖——旧 stamp 的 `"w"` 模式同 key 再触发会整体覆盖上一条、提醒历史只剩最后一条）；写盘失败静默忽略（与 stamp 同容错）；stamp 冷却机制不动（职责是冷却计时、不是日志）。单测：日志追加、同 key 冷却拦截、不同 key 正常发，全通过。
- ✅**已完成** **T16** （完成：2026-08-19 13:44）**Watcher 警报改屏幕中心弹窗（横幅 → 强制确认弹窗）**（记录：2026-08-18 13:29）：`display notification` 横幅改 `display dialog` 屏幕中心弹窗 + 「知道了」按钮（用户点击才关）+ `with icon caution`；带 `giving up after 30`（30 秒无操作自动关、视为一次已触达，防 display dialog 阻塞 watcher 主循环——launchd 每 10 秒触发）、subprocess timeout 调 35 秒兜底；铃声 Basso 保留（弹窗后单独一条 osascript、不互相阻塞）；弹窗文案转义双引号防破 AppleScript 字面量。冷却机制（5 分钟/key）不变、不叠加成窗口风暴。弹窗语法真实 osascript 实测通过。
- 附注（2026-08-19 13:44）：处理待办 1 的实测前置时发现**代理节点漂移出老虎 IP 白名单**（当前节点出口漂出白名单；代理订阅刷新后三节点 IP 已全部变化）——已 `xpilot switch` 切到白名单内节点、API 恢复。这正是长期备忘「代理节点 IP 变动后同步老虎白名单」预警的情形：**白名单需要用户去老虎开发者信息页补当前节点 IP**（漂移 IP 以 `xpilot node list` 实查为准、实值见本机配置），AI 无法代办。

## 🟢 绿色紧急度·长桥可做空探测方法验证（2026-08-18 17:05，方法验证完成、全量结果待盘中重扫）

- ✅**已更新** **T17** （更新：2026-08-18 17:05）**评估长桥与老虎主账户港股可做空标的数量对比**（原记录：2026-08-18 14:07）：探测方法已实证可行（详见下方条目），但**全量扫描结果因收盘后探测全部作废**（实证：15:50 盘中 700.HK 受理、17:00 收盘后同命令 602315——收盘后长桥对卖空探测一律监管拦截）。当前判断保留：老虎可做空 1257 只（港交所名单 1234 全覆盖 + 23 特殊代码）、长桥可做空 ⊆ 名单上限；**真正的数量差异与具体标的清单需明日盘中全量重扫**（TODO.md 已新建「长桥可做空标的全量扫描（港股盘中重跑）」条目，方法与坑见 `tmp/longbridge_scan_method.md`）。
- ✅**已完成** **T18** （完成：2026-08-18 17:00）**长桥可做空探测方法验证（下单探测法）**：对标的挂 Sell 空单（LO 限价=现价×20、1 手、TIF day、`LONGPORT_REGION=global`）探测——`603301` = 标的不支持做空（名单外）、`603302` = 名单内但券池不足、`602315` = 内地监管禁空（长桥对部分标的独立禁空）、受理（order_id）后立即撤单 = 可做空（后续大概率 Rejected 购买力不足但过了资格校验）。对照实验：700.HK 腾讯（名单内）受理 Rejected 购买力不足 ✓可做空；8223.HK ZYY（名单外细价股）603301 ✓不可做空——方法可区分。安全实证：探测单不成交（限价远上方 + TIF day + 买力 0，700.HK 单最终 Rejected 无成交无残留）。**三个铁律**：① 必须在港股盘中探测（收盘后一律 602315，当日 16:00 后扫的 1257 只全作废）；② 必须 global 国际区（中国区按账户身份一律 602315，与 IP 无关）；③ 必须控频（0.25s/笔触发 429002 限频污染判定，建议 ≥1.5s/笔）。另发现：长桥 CLI `order sell` 帮助明确「HK short selling requires SBL agreement signing，API 返回 602301 before signed」——本主账户探测未遇 602301，说明 SBL 已签。

## 🟢 绿色紧急度·长桥与老虎港股可做空标的对比（2026-08-18 15:03，评估完成）

- ✅**已完成** **T19** （完成：2026-08-18 15:03；2026-08-18 15:55 补充复核）**评估长桥与老虎主账户港股可做空标的数量对比**（记录：2026-08-18 14:07）：**老虎主账户可做空 1257 只**（剔除 90/91 未上市预留码口径；其中 83xxx RMB 柜台 48 只、港币柜台 1209 只），与港交所 2026-08-14 生效版指定可卖空名单（1234 只：股本证券 813 + 基金 421）交叉验证——名单内 1234 只**全部**在老虎 `get_stock_details.shortable>0` 集合内（覆盖率 100%），老虎多出的 23 只经富途逐个核身为已除牌旧代码（有赞旧、禾赛旧等）、政府银债、美元柜台 ETF 等特殊标的。**长桥侧 API 无法统计**——SDK 三条路径查实堵死：security_list 只有名称无做空标记、static_info 无做空字段（GitHub issue #359 请求加字段被官方关闭）、estimate_max_purchase_quantity 的 Sell 侧返回 0/0 不提供融券额度；唯一可靠途径是长桥 App 个股页「可沽空」额度（人工查）。**2026-08-18 15:55 补充复核**：用户指出 env-sg 不是要查的主账户，改走 CLI 授权登录（`longbridge auth login` 浏览器授权，token valid；主账户资产特征 net_assets=0 / max_finance 70474.32 与 env-sg 明显不同）——CLI 全接口复核（margin-ratio / max-qty sell / static / security-list【夜盘名单全是窝轮】）结果与 SDK 一致：**长桥 OpenAPI/CLI 无「可做空标的名单」接口、与账户无关**。**判断**：两家做空池上限同为港交所名单（1234），不存在「长桥能空、老虎不能空」的合规标的缺口；实际差异只在借券池深度（需逐标的查老虎 `get_contract.shortable_count` / 长桥 App），名单数量对比无法概括。另：长桥主账户当前 net_assets=0（做空前需入金）。数据与结论存 `tmp/shortable_compare_2026-08-18.md`（明细 `tmp/tiger_hk_shortable.csv` 3980 行 + 港交所名单原件 `tmp/hkex_ds_list.csv`）。

## 🟢 绿色紧急度·多会话并行盯盘方案 A（2026-08-17 23:23，港美全量落地）

- ✅**已完成** **T20** （完成：2026-08-17 23:23）**实现港股多会话并行盯盘（方案 A：单持仓互斥 + 多会话各盯各池）**（记录：2026-08-17 15:05）：按 `PLAN-parallel-monitor-HK.md` 九项改动全量落地——① `trade_mutex.py` 公共互斥模块（flock 全局锁 + 三口径开仓闸门【pending intent / 当日在场敞口 / 活动开仓挂单】+ intent 写前日志 WAL + status / --clear-intent 命令行工具）；② `open_position_tiger.py` 整个下单临界区接锁（闸拒输出 blocked_by JSON、intent 终态结算、查单失败保守拒开）；③ `monitor_segment.py` 第四层兜底（每轮采样同一次查单检测白名单外双持仓、告警直到消除）；④ `config.json` `risk.resident_positions: ["02800","07709"]` 常驻白名单；⑤ `preflight.py` 互斥状态输出（intent pending / 白名单 / 在场会话数）；⑥ `keep-awake/scripts/off.sh` caffeinate 引用计数（最后停盯的会话才解除防睡眠）；⑦ `resume.py` 断层检测按市场 scope（传标的参数只查本市场 log）；⑧ `monitoring.md` 新增「多会话并行盯盘」节；⑨ `SKILL.md` 硬性护栏第 4 条补「纪律 + 工程双保险」句。**验证**：trade_mutex 单元七场景 + 双进程端到端并发验收（flock 串行 1.17s、持仓在场时后到者 blocked_by=pending_intent）+ 第四层检测五场景 + caffeinate 引用计数双会话实测 + resume scope 实跑 + preflight 实跑 + 24 脚本全量编译，全部通过。**边界**：paper 盘中真实双开仓实测（PLAN 验收 1/2 步双终端小额）以进程级模拟替代、下个交易日盘中灰度前补；首个交易日先 2 会话各 2-3 只灰度。
- ✅**已完成** **T21** （完成：2026-08-17 23:23）**实现美股多会话并行盯盘（方案 A：复用公共互斥模块 + 美股侧接线）**（记录：2026-08-17 15:05）：随港股项一并完成——`open_position_tiger_us.py` 接同一把全局锁（market="US"；分市场两把锁会放过「港股持仓违规过夜 + 美股开新仓」跨市场叠加路径）、`trade_utils_tiger_us.py` 补 `get_today_orders_us`（查询失败上抛、调用方保守拒开——闸门口径须区分「查无订单」与「查询失败」，与港股采样降级口径不同）、`monitor_segment.py` 持仓检测按同一 config 白名单港美共用（美股无历史长持、白名单无美股条目）、`resume.py` 市场 scope 公共改动复用、caffeinate 引用计数对美股会话生效（美股不注册 watcher、但 preflight 启 caffeinate 走同一引用计数）、`monitoring.md`「多会话并行盯盘」节含美股差异（watcher 不守护、逐笔实测各测各的）。**验证**：美股开仓脚本编译 + 闸门逻辑与港股同一套单元验证覆盖；美股盘中双终端 paper 实测归入灰度步骤（PLAN 美股侧验收 1-4）。

## 🟡 黄色紧急度·2026-08-17 批量处理（2026-08-17 23:23，文档 9 项 + 脚本卫生 6 项全清）

- ✅**已完成** **T22** （完成：2026-08-17 23:23）**文档类 9 项**（记录：2026-08-16 14:27）：① signal-mode.md L16 长桥 MIT 残留改「附加腿 LOSS（落成独立 STP）」；② actions/README.md 按双市场双目录现实重写（复盘数据源 = signals + actions 两目录）；③ 中英主 README 删 A 股表述（标题 / 徽章 / 正文 / 署名四处、两版同步）；④ SKILL.md 6 要素第 3 条参考价定义改「snapshot 实价（须满足净初始预期赔率 ≥ 2.4 才用它开仓）」；⑤ signals/README 按当前实际格式更新（## 标题不强制框线、equity 口径如实描述、顺带修 alert.sh 旧时序与旧五类 emoji）；⑥ 美股脚本 LMT 残留改 MKT 表述（open_position_tiger_us.py 文件头 + trade_utils_tiger_us.py 注释，历史实测记录注明当时为 LMT）；⑦ 2026-08-13-HKT-review.md 两小错修正（17 个 → 两目录 23 个；1.96R 源口径与 +2.01R 复盘口径标注）；⑧ 08-12 / 08-10 记录小错加「事后更正」标注（不改历史数据本身）；⑨ trade_utils_tiger.py symbol 补零注释改陈述实际行为（不补零、调用方必须传带前导零 5 位码）。
- ✅**已完成** **T23** （完成：2026-08-17 23:23）**脚本卫生 6 项**（记录：2026-08-16 14:27）：① round_to_tick 恒 floor docstring 补方向含义（卖方限价更激进、做多止损触发价压低一个 tick）；② monitor_summary 三处（4 段均价切片不丢点 / 稀疏列守卫改 len(turnovers) / VWAP 段连接 try-finally 关闭）；③ bayes_evolution 三处（FS 局部变量遮蔽改名 / 单样本 xs[-1] 守卫 / log(1+fR) 定义域保护）；④ review.py R=0 统一归败（Ld=[R<=0]，与 summarize 口径一致）；⑤ kline.py 斐波那契区分趋势方向（上涨出回调支撑位、下跌出反弹阻力位）；⑥ 其余低危串（log_action.sh / ring.sh 逗号转义、monitor_log_gap_check 午休豁免、美东时差 zoneinfo 化、hot_list market 参数显式报错、REIT 费率档注释、to_futu_symbol_us BRK.B 修复、close 脚本 --account 缺值文案、交收费 / 印花税取整待核实注释）。详见 CHANGELOG 同日条目。

## 🟠 橙色紧急度·平台费固定模式改造（2026-08-17 21:07，费率口径全面切换；节名原词「橙灯」随 2026-08-17 命名弃「灯」用「色」改写）

- ✅**已完成** **T24** （完成：2026-08-17 21:07）**平台费口径全面改固定模式、弃阶梯式（成本计算统一）**（记录：2026-08-17 19:11）：所有成本计算的平台费从阶梯式（按当月累计订单数）改**固定模式**——港股 15 HKD/笔；美股顺带把整个费用结构按官网核改到位（旧「佣金 0.029% 最低 15 按额」是简化、结构错配）：佣金 0.0039 USD/股（上限 0.5%×额）、平台费 0.004 USD/股（每笔最低 1 USD、上限 0.5%×额，cap 只在低价股 < 0.8 USD/股 触发——**实现中发现并修正一个反向 max() bug：初版把 cap 当下限，MU 106 股大单被算 458 USD 平台费、正确值 1 USD**）、代收近似 0.00396 USD/股。改动面：① `fee_schedule.py` 重写（港美分流、美股按股函数、阶梯表降为备查）；② `trade_utils_tiger.py`——`build_fee_ctx` 退化为纯属性打包（不再查账户）、`get_month_order_count_tiger` 移出费率链路留诊断用途、`_net_odds` 调 `FS.fee_per_side` 传 shares；③ `review.py` / `bayes_evolution.py`——删「(自然月, 市场)」订单计数器，费率与订单数解耦；④ 文档六处同步（risk-management.md「费率与成本」整节重写 + SKILL.md 6 要素第 4 条 + trading-strategy.md 赔率三阶段 + review-and-evaluation.md 净盈亏定义 + auto-mode.md / signal-mode.md 平仓必填节）；⑤ signal 模式「为算阶梯平台费查当月订单数的只读查询例外」随固定模式取消（费率不再需要任何账户查询）。**验证**：七脚本 py_compile 通过；fee_schedule 自检（港股大单 stock 14.32 bps / etf 4.32 bps、美股 MU 106 股单边 1.83 USD）；review.py 实跑港股 42 笔（EV +0.47）与合并 61 笔（EV +0.24，含美股 20 行按股计费）；bayes_evolution 出图 7 张。费率来源：港股 https://www.itigerup.com/hans/help/detail/68697332 、美股 https://www.itigerup.com/hans/help/detail/74820992 （2026-08-17 实查核实；官网美股佣金行未列最低收费，0.99 最低是外部机构费及交易活动费的、非佣金——TODO 原文「佣金最低 0.99」系误记，按无最低实现）。关联黄色紧急度「fee_schedule.py 美股佣金最低 15 按美元计系简化」随之一并完成核改，归档见本节同日条目。
- ✅**已完成** **T25** （完成：2026-08-17 21:07）**`fee_schedule.py` 美股佣金最低 15 按美元计系简化（保守方向）**（记录：2026-08-16 14:27；原列黄色紧急度「其余低危」）：随橙色紧急度「平台费固定模式」改造一并核改到位——美股佣金改按股 0.0039 USD/股（cap 0.5%×额）、平台费 0.004 USD/股（最低 1/笔、cap 0.5%×额），与港股按额结构彻底分流。详见上条橙色紧急度条目。

## 🔴 红色紧急度·盯盘空转修复（2026-08-17 20:05，四步修法全落地；节名原词「红灯」随 2026-08-17 命名弃「灯」用「色」改写）

- ✅**已完成** **T26** （完成：2026-08-17 20:05）**盯盘空转：采样在跑但分析链死亡、全部防护不可见（2026-08-17 实录）**（记录：2026-08-17 15:50）：auto 双会话（实盘 + 模拟）各空转——一会话 14:02 后 0 分析文本 / 52 次纯重启采样；另一会话被用户 15:27 纠正「每段给判断」后 10 分钟内复发。根因：① 2026-08-07 采样源迁 ws_segment / futu_ws_segment 时，monitor_segment.py 段输出的强制锚点（📊 VWAP 检查 + ⏰ 10 分钟方向重估提醒）没跟着搬，ws 段输出只剩裸数字；②「每段三层分析」是 reference 软约束（与 2026-07-28 兆易「规定在 skill 里却没调用」同根因）；③「盯盘输出精简」与「每段分析」的张力被化解为近零输出。**四步修法全部落地（2026-08-17 20:05）**：① ws_segment.py / futu_ws_segment.py 段结束输出补齐三锚点（📊 VWAP 检查【ws 用富途 OpenD 补查 avg_price，OpenD 不可用降级为强制提示不静默】+ ⏰ 10 分钟重估提醒【marker 去重，逻辑同 monitor_segment】+ 👉 空转防护提示行「先给本段判断 + 写心跳再重启」）；② monitoring.md 新增「每段最小输出模板 + 分析心跳」节——一行式判断行模板（现价/关键位/VWAP/判断/下次段时间）+ 分析心跳文件规矩 +「精简 ≠ 不输出」衔接条款，SKILL.md 监控循环步骤同步带指针；③ 分析心跳文件 tmp/analysis_beat_{date}_{mode}.csv（AI 每段分析时 echo 追加）+ monitor_watcher.py 第四检查（采样在跑但心跳停更 >180 秒 → 空转系统通知；无心跳文件且采样已跑 ≥180 秒 → 从未分析警报；「采样在跑 → 一律不报」的空转盲区已补上）；④ monitor_guard.py 空转硬门（盘中重启密采样段时心跳停更 >180 秒 / 零心跳且采样 ≥180 秒 → PreToolUse exit 2 阻断，逼 AI 先补判断 + 写心跳再重启；刚启动【无心跳且 log <3 分钟】放行不误伤）。**验证**：两脚本编译通过；futu_ws_segment 实跑 8 秒段三锚点全输出（VWAP 实查 163.50/163.33）；构造 12 分钟 log 实测两脚本重估提醒触发；guard 盘中模拟六场景（无心跳+采样10h 拦 / 心跳新鲜放 / 停更 400s 拦 / 非采样命令不拦 / && 连跑仍拦 / 刚启动放行）全符合；watcher 三场景（停更报警 / 新鲜不报 / 零心跳报警）全符合；顺手把两 hook 的「采样已跑时长」从 ctime 口径改为读 log 首行时刻（ctime 会因 rename 重置、实测踩坑）。/goal 命令维持不采用（见原条目）。

## 安全 / 凭证（2026-08-17 11:45）

- ✅**已完成** **T27** （完成：2026-08-17 11:45）**`~/.tigeropen/tiger_openapi_config.properties.bak` 旧副本清理**（记录：2026-08-17 10:20）：与主文件仅 account 字段不同（bak 是 7 位实盘号、主文件是 17 位模拟号），含私钥的冗余副本。API 密钥轮换（同日完成，旧密钥已作废）后旧 .bak 成纯泄漏面，删除闭环。
- ✅**已完成** **T28** （完成：2026-08-17 13:05）**实盘凭证平台层硬隔离（IP 白名单 + 模拟盘金丝雀）**（记录：2026-08-17 10:20，历经 11:45 / 12:05 / 12:35 三次方案演进）：老虎开发者级 IP 白名单上线——用户后台把代理订阅全部节点入口 IP 以 `;` 分隔填入白名单（IP 清单见本机配置）；SDK `apply_scope` 改 `all`（模拟盘也走代理，金丝雀设计——模拟盘断 = 提前暴露代理 / 白名单 / 节点问题）。**验证**：模拟盘走代理行情 + 资产查询通、实盘走代理资产查询通、**直连（本地出口）被拒「access forbidden: request ip is not in ip whitelist」**（负向验证白名单真实生效）。剩余长期运维（节点 IP 变动同步白名单）拆出为独立 🟢 条目留活跃。方案决策过程：一个开发者一套密钥无多 key、白名单开发者级不区分账户（按账户粒度隔离不存在）→ 用户拍板「模拟盘可折腾、跟着实盘走代理测链路 + 配白名单」。
- ✅**已更新** **T29** （更新：2026-08-17 13:35）**git 历史含模拟 17 位账户号明文（3 个 commit）——用户拍板不处理**（记录：2026-08-17 10:20）：commit 2514d76 / 74e0476 / c36ecb0 的 SKILL.md / CHANGELOG 里明文写过模拟账户号（当前 HEAD 已清除，历史永久可见、仓库 public）。**不处理的决定性理由（2026-08-17 与用户确认）**：git 历史只泄漏了模拟号这一个值，对实盘每条攻击路径都断——① 模拟号 ≠ 实盘号（各自独立分配、无推导关系）；② API 鉴权靠 tiger_id + RSA 私钥 + license（均从未入库），账户号只是「操作哪个账户」的参数、单独无鉴权能力；③ App 登录走手机号 + 密码，与账户号无关；④ IP 白名单已上线（同日），即便凭证全泄漏、非白名单出口调 API 直接被拒（实测验证）。改写历史（filter-repo + 强推）零实盘收益、成本是弃整个仓库历史。**边界（重新评估触发条件）**：「无威胁」的前提 = 私钥从未入库 + IP 白名单在位；若将来撤掉白名单且私钥又泄漏，应重新评估整体凭证暴露面（极端叠加场景，非当前状态）。

## 🟡 中危批量处理（2026-08-17 09:56，26 条全清 + 1 条更新计数留活跃）

- ✅**已完成** **T30** （完成：2026-08-17 09:56）**`check_order_filled_tiger` 把 PartiallyFilled 当全额成交**（记录：2026-08-16 14:27）：`"Filled" in status` 改严格 `status == "Filled"`（港美共用，美股别名 `check_order_filled_us` 同步生效）——部分成交继续轮询到超时、超时返回 status='PartiallyFilled'；新增 `get_order_filled_qty_tiger`（读 `Order.filled` 字段），开仓降档循环遇部分成交超时即停（已成交部分已建仓、不降档叠仓）、按实际成交量输出 `part_filled: true` + `quantity=实际成交量` + warning 提示复核持仓。轮询异常同修（见第 9 条）。
- ✅**已完成** **T31** （完成：2026-08-17 09:56）**显式传量绕过风控上限 + risk_fraction / f_max 硬编码**（记录：2026-08-16 14:27）：① 港美开仓脚本各加 `_risk_params_from_config()`——risk_fraction / f_max 从 skill config.json 读（缺失回退 0.02/0.10），config「修改本文件即可调参」契约对这两参数生效；② 各加 `_enforce_explicit_quantity_risk()`——显式传量路径同样校验 max_loss ≤ equity×f_max、市值 ≤ equity×max_leverage、整手，超限**拒绝下单**（ok:false，不再只 warning）。mock 实测：58,400 股×6 止损距超 f_max 拒、28.4M 市值超杠杆拒、整手量通过。
- ✅**已完成** **T32** （完成：2026-08-17 09:56）**下单提交异常盲重试 3 次**（记录：2026-08-16 14:27）：① 港美 6 个 submit 函数（open/mkt/stp × 港美）加 `_is_ambiguous_timeout_error` 判定——超时类模糊失败（请求可能已达券商）**不再自动重试**、直接抛出并注明「须先查当日订单确认、未确认前不得再下单」（mock 实测超时只尝试 1 次即抛）；② 新增 `has_active_open_order_tiger/_us` 重复下单防抖——降档循环每档下单前查同标的活动开仓方向委托单（排除止损单），存在即拒绝继续降档（防上一档超时实际在场导致双倍持仓）；③ 降档循环对模糊失败立即 break 不降档续下。
- ✅**已完成** **T33** （完成：2026-08-17 09:56）**开仓附加止损触发价不做 tick 取整**（记录：2026-08-16 14:27）：港美开仓脚本对 stop_loss 先 `round_to_tick_tiger`（价位表）/ `round_to_tick_us`（0.01）取整再提交，与 move_stop / close 脚本口径一致；发生取整改动时输出 `stop_loss_adjusted`（原值 → 新值）留痕。实测 485.5 → 485.4（(200,500] 区间 tick 0.2）。
- ✅**已完成** **T34** （完成：2026-08-17 09:56）**close_position 主路径只处理第一个活动止损单**（记录：2026-08-16 14:27）：港美 close 脚本加 `_cancel_residual_stops()`——主路径（modify 触发）与无止损直接 MO 路径平仓成交后都复查并撤掉该标的全部残留止损单（`cancel_all_stop_orders`，排除刚成交单），撤单失败不阻断输出（warning 提示手动处理），堵「兄弟止损单日后零持仓触发反向开仓」路径（与 move_stop 的 ≥2 清理对齐）。
- ✅**已完成** **T35** （完成：2026-08-17 09:56）**move_stop fallback 异常归因错误**（记录：2026-08-16 14:27）：港美 move_stop fallback 改分步报告——新 STP 提交成功 + 撤旧失败 = `ok: true` + `fallback_cancel_warning`（仓位已有新止损保护、提示手动撤旧），不再整体误报 ok:false 致 AI 误信「无止损」再补挂多重止损；新 STP 提交失败时报「旧止损仍在场、触发价未变」+ 超时模糊失败提示先查订单再补挂。
- ✅**已完成** **T36** （完成：2026-08-17 09:56）**港股脚本缺 TRAIL / LOSS 腿识别**（记录：2026-08-16 14:27）：港股三处对齐美股口径——`close_position_tiger._is_stop_order`、`move_stop_tiger._is_stop_order` 补 TRAIL + LOSS 附加腿；`cancel_all_stop_orders_tiger` 的 is_stop 判定从仅 STP 扩为 STP/STOP/TRAIL/LOSS 四类（App 手动挂的港股 TRAIL 单平仓后也能撤掉，不再残留）。
- ✅**已完成** **T37** （完成：2026-08-17 09:56）**`filled` 变量未初始化 UnboundLocalError**（记录：2026-08-16 14:27）：港美开仓降档循环前 `filled = False` 初始化——全部档位 submit 抛异常时循环耗尽后走干净的失败 JSON，不再 traceback。
- ✅**已完成** **T38** （完成：2026-08-17 09:56）**check_order_filled 轮询无异常捕获 + 平仓显式传参不校验持仓 + 持仓查询不过滤市场**（记录：2026-08-16 14:27）：① 轮询 get_orders 网络异常捕获——瞬态异常等下一轮、超时持续失败返回 `(False, None, 'poll_error:<err>', '')`（订单已在场、AI 拿得到干净输出不会误判重开仓）；② close 港美显式传参路径加持仓复核——持仓不存在 / 方向不匹配 / 超量均拒绝（防 direction=short 无空仓时 Buy MO 凭空开多）、小于持仓量输出 warning；③ `get_open_position_tiger` 只收港股持仓（symbol isdigit 过滤，美股裸代码排除）——港股一键平仓不再拿到 US.xxx。

### 监控链路（10 条）

- ✅**已完成** **T39** （完成：2026-08-17 09:56）**resume.py 美股断层检测跨午夜失明**（记录：2026-08-16 14:27）：① log glob 按市场对齐日期——港股 `monitor_log_HK_*_{北京日期}`、美股 `monitor_log_US_*_{北京 −12h}`（美东交易日口径，与 monitor_segment 命名一致），北京 00:00-04:00 美股后半场不再「今日无采样记录」假安心；美股 log 末行时分 > 当前时分时按跨午夜回退一天拼 datetime；② ring-log 末行校验当日——非当日响铃返回 None，周一开盘不再拿周五响铃报假断层。
- ✅**已完成** **T40** （完成：2026-08-17 09:56）**monitor_summary 买卖比行 r_last=None 崩溃**（记录：2026-08-16 14:27）：第 96 行补 `and r_last is not None` 双非 None 守卫（对齐量比行口径），r_last=None 时输出「N/A（log 无此字段或仅半段有值）」——上午 segment 写（ratio 有值）+ 下午 ws 接管写（ratio 空）的当日全貌摘要不再 TypeError。
- ✅**已完成** **T41** （完成：2026-08-17 09:56）**monitor_guard 美股时段硬编码夏令时 + STALE 分层**（记录：2026-08-16 14:27）：① `in_us_session` 改 zoneinfo 美东判定（对齐 preflight；实测断言：北京周六 00:30 → 判盘中（美东周五 12:30，旧代码失效 4h 的洞补上）、北京周一凌晨 → 判休市（旧误激活）、11 月切冬令时自动跟随）；② STALE_SECONDS 300 单层改分层——进程在 / log <120s = 在跑；120-300s = 「疑似断了」；>300s = 断（盲窗从最长 5 分钟收紧到 2 分钟）。
- ✅**已完成** **T42** （完成：2026-08-17 09:56）**断层哨兵无午休感知**（记录：2026-08-16 14:27）：monitor_segment 哨兵加港股午休感知——last 采样在 11:50-12:00、now 在 12:55-13:05（工作日非美股标的）判为正常午休 gap，输出「ℹ️ 午休间隔……非断层」降级提示，每个交易日 13:00 重启首段不再必报「疑似断层」（狼来了效应消除）。
- ✅**已完成** **T43** （完成：2026-08-17 09:56）**`stop_prices` 缓存只增不清**（记录：2026-08-16 14:27）：`stop_prices.update(fresh)` 改 `stop_prices = dict(fresh)`——fresh 非空即整体替换（止损单撤销/成交后不再显示旧触发价）；fresh 为空（查询失败）保留上一轮值兜底，与「每轮现查、不凭记忆」意图一致。
- ✅**已完成** **T44** （完成：2026-08-17 09:56）**watcher 无告警冷却 + 采样进程存活掩蔽一切会话**（记录：2026-08-16 14:27）：① notify 加 key 冷却——同一会话同一类中断 5 分钟内不重复报（stamp 文件 mtime 判定），launchd 每 10 秒触发不再每分钟 6 条通知风暴；② 掩蔽标注——「采样在跑」分支下对 state 明确异常（waiting_input/idle）或 jsonl 长期停更的注册会话发一条低频提示（key=masked_hint 冷却限制），不再静默吞掉被掩蔽的崩溃会话。
- ✅**已完成** **T45** （完成：2026-08-17 09:56）**bayes_evolution direction 解析只认 long/做多**（记录：2026-08-16 14:27）：改 import `review._direction`（十种别名口径），CSV 出现「多」「buy」等别名时不再静默当空头翻转 R 符号——两脚本解析统一。
- ✅**已完成** **T46** （完成：2026-08-17 09:56）**monitor_guard A4 连跑检测只数 monitor_segment**（记录：2026-08-16 14:27）：① 检测扩到 monitor_segment / ws_segment / futu_ws_segment 三脚本；② 改按 shell 串联操作符（&&/;/||/换行）切分子命令计数、只数「真正独立执行采样段」的子命令——`py_compile` / `cat` / `diff` 等工具引用脚本名不再误拦（实测：py_compile 两文件计数 0 放行、&& 连跑两段计数 2 阻断）。
- ✅**已完成** **T47** （完成：2026-08-17 09:56）**ws_segment 不认 --mode + log 日期口径 + 破位重复报 + 每 tick 一行**（记录：2026-08-16 14:27）：① 补 `--mode` 解析（parse_mode 复用，兼环境变量；--mode 先剥再解析 targets，不再被当标的）；② log 日期按市场对齐（美股 −12h 美东交易日，与 monitor_segment/summary 同口径）；③ 破位告警加 reported_up/dn 去重标记（同段不重复报，对齐「创新高才报」意图）；④ 按秒聚合——每秒只落一行（取该秒最后价），不再每 tick 一行虚增点数。
- ✅**已完成** **T48** （完成：2026-08-17 09:56）**monitor_segment 段结束统计空列表除零**（记录：2026-08-16 14:27）：ratio_avg / vr_avg 空列表跳过该指标（输出 N/A（ws log 无此列）），纯 ws 采样文件（ratio/vr 列全空）不再 ZeroDivisionError、不连坐剩余标的统计。

### 文档与口径（7 条）

- ✅**已完成** **T49** （完成：2026-08-17 09:56）**risk-management.md「权益更新」禁令与 signal 模式矛盾**（记录：2026-08-16 14:27）：该节补模式分叉声明——auto 模式账户 API 真实总资产（原禁令口径不变）；signal 模式 equity 走 equity-log 累加值（正当口径、非违规，无记录回退 initial_equity）；signal 的只读查询例外（费率算订单数允许、equity 口径不变）。
- ✅**已完成** **T50** （完成：2026-08-17 09:56）**港股脚本 ETF 白名单仅 1 成员 vs classify 30 个**（记录：2026-08-16 14:27）：`_HK_ETF_WHITELIST` 改 import `classify_hk_security.HKEX_ETF_WHITELIST`（单一权威源、classify 更新自动跟随；skill 根加 sys.path）——实测 30 个成员，02800 盈富判 etf（不再多收 0.1% 印花税错杀）、00700 腾讯仍 stock、07709 仍 etf。
- ✅**已完成** **T51** （完成：2026-08-17 09:56）**平台费查询失败计费方向与注释相反**（记录：2026-08-16 14:27）：`build_fee_ctx` 查询失败时 order_idx 置 1/2（按阶梯最高档 30/笔计）——真保守（原 None 不计平台费偏乐观，会把不达标交易算成达标，与 stderr「保守档」提示方向相反的矛盾消除）；`get_month_order_count_tiger` docstring「实盘账户」改「当前 config 账户（默认 paper）」与行为对齐。mock 实测失败路径 idx=1/2、最高档费用 > 低档。
- ✅**已完成** **T52** （完成：2026-08-17 09:56）**同一笔交易两套费率两套 R 并存**（记录：2026-08-16 14:27）：裁定写入 `auto-mode.md`（平仓赔率规范后新增「费用与成交价的唯一权威口径」块）——费用 / 成交价 / max_loss 唯一权威 = 脚本 JSON 输出 + fee_schedule.py 复算，动作记录直接抄脚本输出值、禁止手算；复盘统计一律以 review.py / fee_schedule 口径为准、源记录不一致处以复算值校正并注明。08-13 HKT 开仓条目同步修正：「实际 max_loss 80,000」改「预期 max_loss（参考价口径）」（实际 = 73,600，按成交价 379.6）；初始赔率「踩线达标」改如实表述（净 2.12 < 2.4 门槛未过闸、×0.5 折扣后 1.06 亦未过净口径闸），与 ET 副本对齐。
- ✅**已完成** **T53** （完成：2026-08-17 09:56）**08-11 MU 两笔记录数字混用**（记录：2026-08-16 14:27）：全部实算核对后修正——① 第 1 笔成交价统一 855.33（脚本 avg_fill_price 唯一权威；平仓表原 855.31 为转录误差、毛亏 −12,969 系按 855.31 算出，保留净盈亏 −13,804 并注明差异）；② 平仓表分母 14,895/14,865 两写统一 14,865（−13,804 ÷ 14,865 = −0.929R）；③ 第 2 笔 max_loss「止损距 4.255」无来源改实算 4.25 × 2394 = 10,175（原 10,186 保留在赔率算式注明、差异 0.002R 不影响结论）；④ equity 差 +815 标注（1,028,832 记账 vs 1,029,647 实查，疑实时净值随行情浮动、以各自时点实查为准）。
- ✅**已完成** **T54** （完成：2026-08-17 09:56）**既有存量待办（购买力校验）**（记录：2026-08-12 10:14）：① `trade_utils_tiger.py` 新增 `get_buying_power_tiger`——buying_power（get_assets().summary）× long_initial_margin（get_contract，实测 00100=0.75）÷ 参考价，实测字段全可用（bp 4,037,135 × 0.75 ÷ 380 = 7,968 股上限）；② 港美开仓脚本算出目标量后预检购买力上限、超限**下单前主动降档**（输出 `capped_by_buying_power: true` + 上限 + warning），降到 0 拒绝下单，被动降档保留兜底；③ `auto-mode.md`「下单失败处置」节补「购买力上限 = 单标的保证金率约束」条目；④ CHANGELOG 8-06 条目旧推断（边界 1,111 万～1,717 万待实测）标注 8-12 实测结论修正（保证金率 0.75、817.5 万 ÷ 0.75 ≈ 1,090 万 = 37,484 股）。
- ✅**已更新** **T55** （更新：2026-08-17 09:56）**既有存量待办（三阶段赔率标定）**（记录：2026-08-11 19:25）：**样本量实测未达 30、不重标定、条目留活跃**——以 reviews/2026-08-13-trades.csv（61 行合并）实数：×0.5 门槛执行（2026-08-06 起）后样本仅 **15 笔**（港股 9 + 美股 6），远 < 30。原「补 9 笔执行后样本」为 08-14 计数（当时 9 笔），现最新计数 15 笔。维持 ×0.5 系数不变，≥30 笔后按合并分布重标定（见 TODO.md 留存条目）。

## 🔴 高危批量处理（2026-08-16 15:42，11 条全清）

- ✅**已完成** **T56** （完成：2026-08-16 15:42）**开仓门槛 1.2 残留清理：统一为「净赔率 ≥ 2.4」单一门槛**（记录：2026-08-16 14:27）：9 处现行规则里的旧「≥1.2」全部改为 2.4——`SKILL.md` 开盘观察期条款（EV ≥ 2.4）、`references/auto-mode.md` 开仓定义 ⑧ 与开仓表格、`references/signal-mode.md` 开仓信号表格、`references/trading-strategy.md` 五处（止损空间注、开仓两道闸、赔率三阶段公式、止盈可达性两处、停止盯盘前注）、`references/review-and-evaluation.md` 预估净胜赔率公式。历史教训 / 标定背景叙述里的「1.2」（带日期的事故记录、「12 笔初始 ≥1.2 样本」等）按约定保留不动。
- ✅**已完成** **T57** （完成：2026-08-16 15:42）**删除「降频」决策树残留**（记录：2026-08-16 14:27）：`SKILL.md` 与 `references/auto-mode.md` 的「无机会时自主决策树」原 ②「降频（整个市场无活跃机会时放宽采样间隔）」改为「② 换标的不降频（保持 40 秒密采样、持续刷 hot_list，绝无降频死等合法出口）」，决策列表里的「降频」字样同步移除——与硬性护栏第 8 条、`references/monitoring.md`「降频出口已于 2026-08-04 撤销」不再矛盾。
- ✅**已完成** **T58** （完成：2026-08-16 15:42）**close_position_tiger.py 修 --account 空格形式被静默丢弃**（记录：2026-08-16 14:27）：`_parse_args` 增加 `expect_account` 捕获状态——`--account live`（空格形式）的值现在真正赋给 account（原实现只跳过值、从未赋值，切实盘平仓会静默操作默认模拟账户）。三种传参形式（空格 / 等号 / 缺省）+ 缺值边界单测通过，与 open/move 脚本口径一致。
- ✅**已完成** **T59** （完成：2026-08-16 15:42）**cancel_all_stop_orders_tiger 枚举裸 str() 比较失效**（记录：2026-08-16 14:27）：实测老虎 SDK（tigeropen 3.6.0）`OrderType` / `OrderStatus` 均为普通 Enum——`str(OrderType.STP)` 得 `'OrderType.STP'`，原 `str(order_type) == "STP"` 永远 False（独立 STP 单撤不掉），且 status 终结过滤同样失效。status / order_type 统一改为 `.value` 优先、str 兜底（枚举 / 纯串两形态六组合模拟验证全对，与 `_is_stop_order` 口径一致）。
- ✅**已完成** **T60** （完成：2026-08-16 15:42）**get_open_position_tiger side 张冠李戴**（记录：2026-08-16 14:27）：symbol 分支 `_, p, qty = matches[0]` 改为 `s, p, qty = matches[0]`、返回 `"side": s`——目标持仓自己的 side 不再被收集循环残留值覆盖（多空混持时一键平仓不再方向反做加倍）。
- ✅**已完成** **T61** （完成：2026-08-16 15:42）**美股开仓 quantity=0 自动算仓位 ZeroDivisionError**（记录：2026-08-16 14:27）：`open_position_tiger_us.py` 补 `if quantity > 0 else None` 守卫（对齐港股版）——「数量传 0 = 自动算仓位」的文档化主用法在美股恢复可用，价格范围检查回退旧百分比口径、算出仓位后照常复核。
- ✅**已完成** **T62** （完成：2026-08-16 15:42）**开仓 / 移损缺「止损价方向正确性」硬校验**（记录：2026-08-16 14:27）：① `_net_odds` 止损距 ≤0 从返回 inf 改为 raise ValueError（方向错以最诱人形态放行 → 改为硬报错）；② `check_price_in_range` 捕获该错误、odds_at_current 记 −inf 且 in_range 强制 False（现价漂过止损侧 = 拒绝开仓）；③ 港股 / 美股开仓脚本各加「做多 stop < entry_ref、做空 stop > entry_ref」硬校验；④ 港股 / 美股 move_stop 各加「新止损价未穿越现价」校验（做多 ≥ 现价 / 做空 ≤ 现价即拒绝并提示用 close_position 主动平仓），桩测试三分支（做多穿越拒 / 做空穿越拒 / 正常移损过）验证通过。
- ✅**已完成** **T63** （完成：2026-08-16 15:42）**futu_ws_segment.py 写 log 列错位一列**（记录：2026-08-16 14:27）：写行 last 后补齐 4 个空位（11 字段对齐 11 列表头）；对照确认 ws_segment.py 写行本就正确。已污染数据（比 TODO 记录的 08-14 更多——08-07 / 08-10 / 08-11 / 08-14 共 18 个文件、97,835 行 10 字段错位行）先备份到 `tmp/log_fix_backup_20260816/` 再按错位映射无损重排（原 vr 列位置的真 high、原 high 列位置的真 low 各归其位），全部行复验 11 字段、抽样语义核对正确。
- ✅**已完成** **T64** （完成：2026-08-16 15:42）**monitor_summary.py 额增速系统性高估 10 倍**（记录：2026-08-16 14:27）：额增速换算不再假设「每点 = 1 秒」，改用 turnover 序列自身首末时间戳推算平均采样间隔（秒/点）、窗口时长 = 点数 × 间隔 ÷ 60；混合数据源文件（ws 每秒行与 segment 10 秒行交错）按 turnover 自身间隔而非全文件平均推算。实算验证：10 秒采样 5 分钟 2 亿增量 → 0.40 亿/分（旧代码 4.00）；08-14 真实文件复核近 30 点窗口真实时长 16 分钟、均速 0.331 亿/分。输出行标注窗口分钟数与采样间隔。
- ✅**已完成** **T65** （完成：2026-08-16 15:42）**08-13 动作时间倒流 + 同笔交易双写 + equity-log 漏记两笔美股 + log_action.sh 根治**（记录：2026-08-16 14:27）：① 08-13 HKT 两条动作时间戳按表格内真实时间改正（10:20:54 / 10:31:07）并注明修正原因；② 08-13 ET 文件里的港股双写副本加迁移说明（权威记录在 HKT、复盘只认 HKT 副本、本副本不计入——按「文件操作慎用删除」规则保留留痕不删）；③ equity-log 补记 08-05 NVDA（−2,347 HKD = −300.86 USD × 7.80）与 08-06 MU（−2,324 HKD = −298 USD × 7.80）两笔漏记（汇率 7.80 与既有 08-03 NVDA 行口径一致），下游 equity_after 连锁更新、08-07 注资行 P 值相应调整保链条连续（定值结果 408640 不变）、全链连续性验证 0 问题；胜宏 R 值改为实算 3.42（14,238÷4,160，原记 3.71 无出处、修正留痕）；④ **根治**：`log_action.sh` market / mode 两参数改为必填（缺参报错退出、去掉默认值向后兼容——默认值已两次诱发事故），沙箱测试缺参 / 非法值 / 正常路径全过。注：TODO 原记「两笔合计约 −6,076 HKD」无法从复盘净亏口径复算（−598.86 USD ≈ −4,671 HKD @7.80），按实算为准。
- ✅**已完成** **T66** （完成：2026-08-16 15:42）**08-13 信号文件预估 max_loss 自相矛盾**（记录：2026-08-16 14:27）：预估 max_loss 按实算改正为 HK$1,705（1550 股 × 止损距 1.1 = 参考价 94.6 − 止损 93.5；原记 2,325 用了无来源的止损距 1.5）；移损行「+0.4 利润 ≈ +0.4R 保本」改正为「+0.4 利润/股 ≈ +620 HKD ≈ +0.57R 保本」（R = 620 ÷ 实际 max_loss 1,085；原表述把每股利润当 R 值）。两处均注明「2026-08-16 修」留痕。

## 交易 / 持仓处置

- ✅**已完成** **T67** （完成：2026-08-03 11:03）**关闭 MU 误开多头仓位**（记录：2026-08-01 22:54）：2026-07-31 smart_order 市价单被拒却误报成交 → close_position 反向买入开多 158 股 @ 828.575 USD（实测 2026-08-01 持仓确认）。计划下一个美股开盘（21:30 HKT）用 `close_position.py`（一键平仓）平掉，开盘前重新 `get_open_position` 核实持仓再执行。

## 代码 / 机制

- ✅**已完成** **T68** （完成：2026-08-12 11:05）**Watcher 注册与检查的市场范围错配根治 + 判定逻辑重构为「采样在跑就不报」**（记录：2026-08-12 10:26）：2026-08-12 上午港股盯盘中 watcher 一直响，原待办判定根因 = 美股会话 `6ede8d93` 残留在注册列表没注销 + 注册源头不分市场（preflight 无条件全局注册、watcher 只港股检查）。**排查中定位到更深一层根因（原待办未覆盖）**：即便只有港股会话、窗口显示 Running、采样在跑时 watcher 也可能误报——旧 2026-08-11 逻辑只认「jsonl 停更 > 50s」，但盯盘 Running 时 AI 回合卡在采样脚本 Bash 调用里、采样段 40s 期间 jsonl 完全不更新，50s 阈值踩在采样段尾端（40s + 开销），某段跑到 51s 即误报（代码注释自己也承认「正常无输出间隔最少 ≈ 40s」、阈值压刀刃）。**用户立新逻辑：盯盘窗口在 Running 且后台采样脚本在正常采样 → 不报，两者都不活跃才报**。**落地三件事（原待办根治点 + 用户新逻辑一并完成）**：
  1. **watcher 判定逻辑重构（用户新逻辑落地，根治主因）**：[`monitor_watcher.py`](.claude/hooks/monitor_watcher.py) `main` 改三层判定——① 死会话自动剔除（jsonl 停更 > 30 分钟 = 已结束，自动从注册列表移除、不再报）；② 新增 `sampling_running()` 用 `pgrep -f` 查 `monitor_segment.py` / `ws_segment.py` / `futu_ws_segment.py` 任一存活即判正常、本轮一律不报（采样进程是「在正常运行」最稳铁证，绕开「采样期间 jsonl 停更」固有盲区）；③ 采样没在跑 + jsonl 停更 > 90s 才报中断（`STALE_SECONDS` 50→90s，采样在跑已盖住采样段、本阈值只在分析阶段生效，不再踩刀刃）。
  2. **preflight 注册源头收窄为仅港股（原待办根治点）**：[`preflight.py`](.claude/skills/trade/scripts/preflight.py) `_register_monitor_session()` 加港股盘中判断（周一至周五 09:00-12:00 / 13:00-16:00，09:00 起覆盖盘前预热），美股 / 夜间 / 盘外跳过注册，从根上杜绝美股 session 进注册列表。
  3. **回写文档**：[`references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「多层防护」⑪ 更新为 2026-08-12 新判定（采样在跑就不报 + 死会话剔除 + 仅港股注册）；[`monitor_watcher.py`](.claude/hooks/monitor_watcher.py) docstring、[`plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) 注释、[`monitor_register.sh`](.claude/skills/trade/scripts/monitor_register.sh) 注释、[`SKILL.md`](.claude/skills/trade/SKILL.md) 停盯动作同步更新；[`CHANGELOG.md`](CHANGELOG.md) 记本次根因 + 修复。已重新部署 plist + reload，实测盘中采样在跑时 watcher 在判定第②步 return、不报。

  **2026-08-12 同日再次升级（第③步从阈值改为读 Running 标签 state 文件）**：用户指出第③步「采样没在跑 + jsonl 停更 > 90s」仍是阈值猜测，应直接读会话 Running 标签判定。查证发现补丁 002 的 Running 标签状态（session 对象 busy/pendingInput）只在 extension.js 进程内存、不写盘、外部进程读不到。解法：跨项目给 PatchClaudeAgent 新增 patch 012（session-state-to-file）——给 extension.js 的 `updateSessionState` 注入写盘（每次 state 变化写 `~/.claude/session_running/<sid>.txt`），watcher 读它 = 读会话 Running 标签的地面真相。watcher 第③步改为：running/thinking 不报、waiting_input/idle 报中断、state 文件无（补丁 012 未生效）fallback 到原 jsonl 判定（过渡期兼容）。patch 012 已应用到本机 2.1.226，待盯盘会话重载扩展后 state 写盘生效。详见 CHANGELOG 2026-08-12 第二条。

- ✅**已完成** **T69** （完成：2026-08-11 19:25）**00100 计划仓位下达失败排查（auto 模式，2026-08-06 复盘发现）**（记录：2026-08-06 20:42）：8-06 港股 MINIMAX-W 做多计划仓位 58,400 股无法一次下达——58,400 空仓时被拒（原因未明）、500/200/120 股因「cross-trading with your pending sell order」被拒（券商端禁止挂止损单后加仓）、最终只成交 10,000 股（max_loss 56,000 仅单笔预算 163,507 的 34%，收益被仓位缩水拖累）。待办：① 查明 58,400 单笔被拒的真实原因（单笔上限？资金？）；② 设计「挂单后加仓」的合规路径（先撤附加止损再加仓再补止损？或分批下达？）；③ 完善下单失败后的仓位调整流程（当前只试到 10,000 即停，未二分穷尽可下上限）。**完成说明（2026-08-11）**：① 查老虎当日订单历史确认——58,400 两次尝试均 Invalid 且 reason 为通用文案（SDK 对被拒订单无具体原因）、空仓时被拒排除 cross-trading；结合同日 01888（名义 ≈1,111 万 HKD、净值 781 万）成功 vs 58,400（名义 ≈1,717 万 HKD、净值 817 万）失败，推算最可能为**单笔名义金额 / 购买力限制**（边界 1,111 万～1,717 万 HKD 之间，确切上限待盘中实测）；② `auto-mode.md` 新增「下单失败处置」节：cross-trading 处置（先撤旧止损再开仓）+ 分批建仓路径（撤旧止损 → 第二批开仓不带腿 → 补挂一张独立 STP 覆盖总仓，两步都在开仓决策时确定、与「禁止加仓减仓」护栏边界写清）；③ `check_order_filled_tiger` 返回 4 元组输出 reason（Order.reason 字段，具体原因不再丢失）、港股美股开仓脚本加**失败自动降档重试**（目标量被拒按降档序列逐次减半取整手重试到可下上限，降档成交输出 downscaled + target_quantity + failed_attempts + warning；cross-trading 与挂起超时两种失败不降档）；降档序列单测 + 三场景模拟测试 + 8 脚本编译全过。详见 CHANGELOG 2026-08-11。**2026-08-11 补充（用户纠正，本条目部分决定作废）**：① cross-trading 归因修正——开仓 = 空仓建仓、与已有止损单无关，reason 实为账户已有同标的**未成交委托单**（之前下单残留），处置改为改单或撤销该挂单后重新挂单；② **分批建仓路径移除**——分批补足 = 同一标的先后两笔同向开仓 = 加仓、违反「禁止加仓减仓」护栏，一次性开仓设止损即可，降档缩水的仓位接受不补足（auto-mode.md / CHANGELOG 8-11 条目已同步修正）。

- ✅**已完成** **T70** （完成：2026-08-02 18:59）**`load_equity` 改用账户 API 取真实总资产**（记录：2026-08-01 22:54）：`trade_utils.load_equity`（open_position 自动算仓位用）仍读 `signals/equity-log.csv`（旧信号模式累加值、不真实）；应改为像 `preflight.py` / `resume.py` 那样从长桥 `account_balance().net_assets` 取（2026-07-31 已修 preflight/resume，open_position 的自动仓位路径漏改）。

- ✅**已更新** **T71** （更新：2026-08-05 16:52）**实盘实测 `submit_order_with_stop`（开仓附加订单）**（记录：2026-08-01 22:54）：该函数（REST 主单 LO + attached_params STOP_LOSS）签名逻辑已对照文档核对、REST 签名复用已验证的可工作实现，但尚未端到端实跑过一笔真实（paper）开仓。计划在美股开盘时用 paper 账户实测一笔小仓位开仓（确认主单成交 + 附加止损进入监控状态 + 成交回查拿到 fill_price），测完即平。**2026-08-05 更新**：长桥已放弃、`submit_order_with_stop`（长桥 trade_utils.py）已随长桥脚本删除，不再实测；老虎开仓附加腿 OrderLeg('LOSS') 已在 2026-08-03 开盘实测通过（见下方「开盘实测港股老虎三动作全链路」完成条目）。

- ✅**已完成** **T72** （完成：2026-08-03 10:58）**港股长桥三动作开盘实测**（记录：2026-08-01 22:54）：`open_position_hk.py` / `close_position_hk.py` / `move_stop_hk.py` 已建（解耦独立一套）+ 盘后只读功能实测通过；下单链路（开仓 LO+附加MIT、移损先新增后撤旧、平仓 MO）需港股开盘时用 paper 账户端到端实测一笔小仓位。仅当用户特别说明用长桥时执行。

- ✅**已更新** **T73** （更新：2026-08-05 16:52）**美股 open_position 端到端实测**（记录：2026-08-01 22:54）：美股 symbol bug 修复后（to_lb_symbol），`open_position.py` 全链路（get_quote→submit_order_with_stop→成交回查）需开盘时实测一笔小仓位确认（此前因 symbol bug 未成功跑过）。**2026-08-05 更新**：长桥已放弃、原对象（美股长桥 open_position.py）已删除；美股默认账户改老虎，最新意图见下方新待办。

- ✅**已完成** **T74** （完成：2026-08-02 18:59）**港股 lot_size 从行情接口取真实每手股数（当前硬编码 100）**（记录：2026-08-02 13:54）：`open_position.py` 自动算仓位时港股 lot_size 硬编码 100（line 130-132），应改为从行情接口取真实每手股数（原代码注释提及富途 snapshot；港股转老虎后需确认实际行情源与取数字段）。

- ✅**已完成** **T75** （完成：2026-08-05 16:52）**`open_position.py` 自动仓位币种换算 bug**（记录：2026-08-04 01:12）：[open_position.py:136-141](.claude/skills/trade/scripts/open_position.py#L136-L141) quantity=0 自动算仓位时，`load_equity` 返回 HKD equity（账户基础币种 HKD、net_assets 390329）直接除 USD 止损距算仓位——单位错位，做空 MU 止损距 8 USD 会算出 ~975 股、实际 max_loss 7800 USD（≈6 万 HKD）是预算 B 的 8 倍且超 f_max。2026-08-04 实测发现、手动传 quantity 绕过。修法：美股（symbol US.xxx）时把 equity 按汇率换算成 USD 再算仓位（或把止损距换算成 HKD），需查实时 USD/HKD 汇率（富途 US.USDHKD 不认，待找正确代码或数据源）。**完成方式（2026-08-05）**：equity 口径修复（`load_equity_tiger` 加 `base_currency` 参数、港股 'HKD' 取 `get_prime_assets` 折算净值；美股 `load_equity_us` 直取 USD 净值）——美股老虎开仓脚本自动算仓位用 USD 净值 ÷ USD 止损距、单位一致，错位 bug 消除；原对象（长桥 open_position.py）已随长桥删除。

- ✅**已更新** **T76** （更新：2026-08-05 16:52）**`close_position.py` 不支持 `--mode` 参数（与 `open_position.py` 不一致）**（记录：2026-08-04 01:12）：[close_position.py:46-48](.claude/skills/trade/scripts/close_position.py#L46-L48) 参数是位置参数 `[symbol] [direction] [quantity]`，不解析 `--mode`——平仓时传 `close_position.py US.MU --mode auto` 会把 `auto` 当 quantity 报 ValueError，耽误平仓约 1 分钟（2026-08-03 MU 空单教训：MU 从 787 冲到 793 触发止损）。`open_position.py` 用 `parse_mode()` 支持 `--mode`，`close_position` 应一致。注意：close_position 默认用 `load_config()`（env-paper 模拟盘）、不连账户 equity，`--mode` 对它本无实际用途——但参数接口应一致避免误用（或文档明确不含 --mode）。**2026-08-05 更新**：长桥已放弃、原对象（长桥 close_position.py）已删除；同款问题存在于老虎平仓脚本（`close_position_tiger.py` / `close_position_tiger_us.py` 均不解析 --mode），最新意图见下方新待办。

- ✅**已更新** **T77** （更新：2026-08-05 16:52）**附加止损单平仓后撤不掉（反向开仓隐患）**（记录：2026-08-04 01:12）：开仓附加 STOP_LOSS MIT 止损单，平仓后 `close_position` 的 `cancel_all_stop_orders` 返回 0（附加子单不在 today_orders 主列表、按 trigger_price 筛选漏撤），SDK `cancel_order(子单oid)` + REST DELETE `/v1/trade/order` 均 601029「无法查询到订单」——附加子单不可单独撤。REST 查主单详情见附加止损 status=**VarietiesNotReported（品种未上报）**，疑似模拟盘附加止损未真正激活监控（与港股 NOT_USED 同义）；2026-08-04 MU 多单 816 未触发、持仓已净 0、无实际危害，但属工具缺陷（残留附加止损撤不掉、若激活触发会反向开仓）。修法：① 平仓后用 REST 查主单详情拿附加子单 oid + 状态、确认是否激活；② 若 VarietiesNotReported = 未激活、标注无危害；③ 若激活、找正确的撤单 API（研究长桥附加订单撤单接口）；④ `close_position` 平仓后强制核实附加止损状态。**2026-08-05 更新**：长桥已放弃、该缺陷随之消失无需后续工作——老虎体系下开仓附加腿落成的 STP 单可独立撤销（2026-08-03 实测）、平仓走「modify 止损触发价=现价」无 race 路径（2026-08-05），无残留止损反向开仓隐患。

- ✅**已完成** **T78** （完成：2026-08-05 16:30）**核实老虎 paper 账户币种口径（标 USD vs 港股 HKD）**（记录：2026-08-04 11:40）：auto 模式港股仓位计算用老虎账户净值（`get_assets().summary.net_liquidation` 返回 1,000,035、currency 字段标 USD），但港股标的止损距是 HKD 计价——8-3 起沿用「净值数字直接当 HKD 用」的保守口径（8-3 07709、8-4 阿里均按此算仓位，实际风险敞口被低估、仓位偏小）。需核实老虎 paper 账户基础币种真实语义：① 若确为 USD、需按 USD/HKD 汇率换算后算仓位（仓位应更大）；② 若 paper 账户内部 1:1 当 HKD 用（老虎 paper 不做真实汇率）、则当前保守口径符合实际。修法：查老虎 paper 账户文档或实测一笔港美股混合交易看净值变化，确定币种语义后回写 `trade_utils_tiger.load_equity_tiger` 注释与 auto-mode.md 币种口径说明。**2026-08-05 实测结论**（第 ① 种成立）：老虎 paper 单账户多币种现金段（`get_prime_assets` 的 `currency_assets` = USD 1,000,000 / HKD -24,058.59（港股交易后 HKD 现金为负）/ CNH 0，USDHKD ≈ 7.8436 实时），**`get_prime_assets(base_currency='HKD')` 直接返回 HKD 计价的证券段净值**（实测 7,819,536.41 = 996,932.71 USD × 7.8436，全账户折算、汇率来自老虎自身）；`load_equity_tiger` 已加 `base_currency` 参数按此实现（港股 'HKD' / 美股 'USD'），preflight / resume / 开仓脚本自动算仓全部生效，注释与 CHANGELOG 2026-08-05「equity 口径修复」已回写。

- ✅**已完成** **T79** （完成：2026-08-05 20:59）**monitor_segment.py 日期处理 bug（date_str 未定义 + log 文件名日期跳变）**（记录：2026-08-04 11:40）：2026-08-04 港股盯盘实测两个相关症状：① 每段采样输出「重估提醒检查 err:name 'date_str' is not defined」——`date_str` 变量未定义致「每 10 分钟方向重估提醒」功能失效（虽偶有段正常触发，但多数报错，AI 改靠手动 10/20/30…分钟重估兜底）；② log 文件名日期跳变——前 3 段采样写 `monitor_log_HK_*_20260803.csv`（昨日日期）、第 4 段起突然改写 `20260804.csv`（今日），疑似 `date_str` / 文件名日期的计算有竞态或初始化 bug。影响：自动重估提醒不可靠、monitor_summary 读 log 累积统计可能跨日污染（「完整采样统计」读到昨日点数、今日「点数=1844」即此症状）。修法：查 monitor_segment.py 的 `date_str` 定义与 log 文件名日期生成逻辑，修复未定义 + 日期跳变。**完成说明（2026-08-05 20:59 确认闭环）**：修复早已在提交 c58203f（2026-08-04 16:49「Add monitor-sampling guard hooks」）随「log 按市场交易日命名」改造落地——`trading_date_str()` 按市场区分（港股北京日期 / 美股美东交易日 -12h）、`date_str = trading_date_str(syms[0])` 正确定义、重估提醒检查与文件名共用同一日期；本条目记录后未及时闭环，本次核查工作区版本（date_str 定义在 [monitor_segment.py:155](.claude/skills/trade/scripts/monitor_segment.py#L155)、log 文件名 [monitor_segment.py:162](.claude/skills/trade/scripts/monitor_segment.py#L162)、编译通过）确认症状两处均已修复，无需新改动。

- ✅**已完成** **T80** （完成：2026-08-05 20:59）**老虎平仓脚本不支持 `--mode`（长桥 close_position 同款问题转移）**（记录：2026-08-05 16:52）：`close_position_tiger.py` / `close_position_tiger_us.py` 参数为位置参数 `[symbol] [direction] [quantity]`、不解析 `--mode`——误传 `--mode auto` 会把 `auto` 当 quantity 报 ValueError 耽误平仓（2026-08-03 MU 空单教训同款）。`--mode` 对平仓脚本本无实际用途（不连账户 equity），修法为过滤 --mode 参数或文档明确不支持。**完成说明（2026-08-05 20:59）**：两个平仓脚本加 `_parse_args()` 过滤 `--mode`（含 `--mode auto` 后跟值、`--mode=xxx` 两种写法），main() 改从过滤后参数解析；6 个单测用例全过（含 --mode 在前 / 在后 / --mode= 形式）。

- ✅**已完成** **T81** （完成：2026-08-05 20:59）**bayes_evolution.py 的 trades.csv 路径与输出文件名硬编码，每次复盘需手动改**（记录：2026-08-03 17:13）：脚本第 32 行硬编码读取 `reviews/2026-08-03-trades.csv`、7 张图输出命名 `2026-08-03-*`，每次复盘都要手动 sed 改两处（本次复盘即手动改过）。建议改为支持命令行参数（如 `--date`）或自动找 `reviews/` 下最新的 trades.csv，把日期抽成脚本头部变量，避免每次手改。**完成说明（2026-08-05 20:59）**：加 `_resolve_date()`——`--date YYYY-MM-DD` 指定，不传自动找 `reviews/` 下最新 `*-trades.csv`（按文件名日期取最大）；日期抽成 `DATE` / `CSV_PATH` 变量、7 处输出文件名全部参数化；`--date 2026-08-04` 与自动找最新两条路径实跑通过（7 张图正常生成）。

- ✅**已完成** **T82** （完成：2026-08-05 20:59）**复盘时平仓原生记录 mfe_R / mae_R（过程指标可算）**（记录：2026-08-03 17:13）：复盘过程指标（MAE / MFE / 回吐 / 锁利效率 η）需要每笔持仓期间的 high/low，现有 trades.csv 未提供 raw_high/raw_low、历次复盘全部跳过。推荐做法：平仓时原生记录 `mfe_R` / `mae_R` 两个字段（盯盘时本就在采样价格、零额外成本），复盘直接读、不必每次回拉历史 K（见 review-and-evaluation.md「⚠️ 数据约束」）。**完成说明（2026-08-05 20:59）**：`trade_utils_tiger.calc_position_extremes_tiger`（美股 `trade_utils_tiger_us` 别名复用）从盯盘 log 取当日采样极值近似持仓期间 high/low（按市场交易日 + 模式匹配最新日期文件）；两个平仓脚本成交后 `_attach_process_metrics()` 自动输出 `mfe_R` / `mae_R`（+ entry_price / raw_high / raw_low，口径与 review.py 一致：正 MFE_R / 负 MAE_R，止损距 = 开仓价 − 活动止损触发价毛值）；无盯盘 log / 缺止损价则不加字段（复盘按缺失处理）。配套文档：auto-mode.md 平仓动作表格加「过程指标」行 + 引用说明、review-and-evaluation.md「⚠️ 数据约束」更新为「方案 b 已落地」。单测通过（临时 log 返回极值正确、不存在标的返回 None）。

- ✅**已完成** **T83** （完成：2026-08-05 23:17）**盘前→开盘唤醒衔接机制缺失（AI 不会自动在开盘时醒来盯盘）**（记录：2026-08-05 09:48）：2026-08-05 港股盯盘，盘前 09:19 用户启动 → 09:24 盘前准备完成（preflight + watcher 修复 + hot_list + 方向研判）回合结束 → **09:30 开盘 AI 没有自动醒来盯盘**，直到 09:40 用户手动提醒。根因：AI 是请求-响应式、回合结束就静止，唤醒源只有 ① 用户新消息 ② 后台 task-notification ③ 主动定的定时唤醒；盘前没启动 monitor_segment（段结束通知是盘中标准唤醒源但盘前没用它）+ 没安排定时唤醒 → 09:30 无人叫醒 AI。caffeinate 只防系统睡眠、watcher 只发 macOS 通知给用户，都不唤醒 AI session。**修法（两选一，待定）**：① 盘前准备完成若距开盘有时间差，提前启动 `monitor_segment.py` 盯候选标的（段结束 task-notification 从盘前持续驱动 AI、跨开盘自然进盘中——最贴合现有机制、确定可行；代价是盘前几段采到静态价无意义，但唤醒价值大于此）；或 ② 用 `ScheduleWakeup` 定在开盘前 ~30 秒唤醒（待验证：该工具描述绑定 /loop dynamic mode，非 /loop 上下文能否调用未知，需实测）。方案择一写进 `monitoring.md` 盯盘启动段 + SKILL.md 启动序列（「盯盘靠用户指令启动、AI 不自主启动」要补「但盘前准备完成后须安排开盘唤醒」）。改 skill 须先读 skill-creator。触发：用户「为什么没准时唤醒？排查原因，解决问题」。**完成说明（2026-08-05 23:17）**：修法两选一实测定案——方案②（ScheduleWakeup 定时唤醒）**实测不可用**（非 /loop 上下文调用直接报「Wakeup not scheduled. Either the /loop dynamic runtime gate is off or the loop reached its maximum duration」），方案①（盘前提前启动 monitor_segment）落地为唯一方案：monitoring.md「盘前启动只预热」节新增规则（盘前启动盯盘、准备完成距开盘 ≤ 15 分钟立即提前启动 monitor_segment 盯候选标的、段结束 task-notification 从盘前持续驱动 AI 跨开盘进盘中、盘前段 AI 只重启下一段 + 一句话汇报）+ SKILL.md 硬性护栏第 7 条同步；monitoring.md「采样链防中断」备份唤醒条更新为「ScheduleWakeup 已实测不可用、备份唤醒仅存于 /loop 场景、普通盯盘回退核心靠 AI 自检 + 大动作复查」、盯盘启动清单不再尝试。改 skill 已先读 skill-creator。

- ✅**已完成** **T84** （完成：2026-08-05 23:17）**美股老虎 `open_position_tiger_us` 下单链路待盘中实测**（记录：2026-08-05 16:52）：2026-08-05 美股默认账户改老虎（长桥已放弃），`open_position_tiger_us.py` / `close_position_tiger_us.py` / `move_stop_tiger_us.py` 下单链路（开仓 LMT+附加止损、平仓「modify 止损触发价=现价」、移损 modify aux_price）待美股盘中（晚上 21:30 开盘）用 paper 账户端到端实测一笔小仓位。**完成说明（2026-08-05 23:17）**：美股盘中（美东约 11:1x）SPY 2 股小仓位端到端实测三动作全通过——开仓 LMT Filled @773.68 + 附加止损腿 OrderLeg('LOSS') 激活为独立 STP、移损 modify aux_price 770.61→771.71 verified=true、平仓 modify 触发价=现价、止损单触发 Sell MO Filled @773.44、持仓归零无残留止损单（平仓过程指标 mfe_R/mae_R 正常输出）。**实测发现并修复 1 个 bug**：`get_quote_us` 用老虎 `get_stock_briefs` 取美股行情报 code=4 msg=4000 permission denied(US market)——老虎账户美股无行情权限（account-tools.md「数据源分工总则」早已记录「美股只能靠富途单源」）→ 改**富途 OpenD 单源**。4 个脚本 docstring「⏳ 待实测」更新为「✅ 实测通过 + 行情数据源修复」+ auto-mode.md 美股段落实测状态同步。纯基础设施验证（开仓即平、无策略意义）、按 8-03 实测惯例不入 actions/。

- ✅**已更新** **T85** （更新：2026-08-11 18:50）**信号模式支持飞书 CLI 发信号 + 多会话并发盯盘**（记录：2026-08-11 13:21）：**本条已加入 win 分支的 TODO.md——此功能在 win 分支开发，不在 main 分支开发；等 win 分支开发好再合并到 main 分支，合并后本条在 main 归档闭环（2026-08-11 转出）**。原待办内容：① 信号模式下通过飞书 CLI 把交易信号发到飞书（信号除当前会话内输出外，再推送飞书，提升信号可达性）；② 支持同时开多个信号模式盯盘会话（当前信号模式会话与盯盘会话的关系、并发时的信号归属 / 记录去重需设计）。具体方案（飞书 CLI 选型、多会话并发机制）待定。

## 港股老虎模拟账户三动作（默认账户，独立解耦一套）

> 港股默认老虎，要实现和长桥一样的三动作，与港股长桥（备选）+ 美股代码分开（分而治之，2026-08-01 用户立）。三动作已建好（2026-08-02）；2026-08-03 起全项目默认模式为 signal、auto 需用户特别说明才用（见下节）——本节的「港股默认老虎」指 auto 模式下的港股默认账户，仅在用户切 auto 时启用。

- ✅**已更新** **T86** （更新：2026-08-02 18:59）**研究老虎 SDK 港股细节**（记录：2026-08-01 22:54）（开盘 + paper 实测才能确认，盘后做不了）：
  - 配置加载：`get_client_config(props_path=...)` 失败，要研究老虎 paper 配置正确加载方式（`~/.tigeropen/tiger_openapi_config.properties` + 私钥）。
  - 港股 symbol 格式：`stock_contract` 接受 `02800`/`HK.02800`/`2800.HK`，但老虎 API 实际认哪个要实测（行情/下单）。
  - lot_size：老虎 `get_contract` 查字段；tick：`get_contract` 或港交所价位表。
  - 附加止损 `order_leg('LOSS', price)` 实测（主单 LMT + 附加止损，对应长桥 attached STOP_LOSS）。
  - 老虎下单/平仓/止损机制逐一实测，**不能从长桥外推**（verify-facts：券商行为只信直接实测）。
- ✅**已完成** **T87** （完成：2026-08-02 18:59）**建 `trade_utils_tiger.py`**（记录：2026-08-01 22:54）（老虎封装，自包含不依赖长桥/美股）：配置加载、港股 symbol/lot/tick、place_order（开仓 LMT+附加止损）、平仓 MO、移损止损单（先新增后撤旧）、查持仓/资产/订单、撤单、成交回查。
- ✅**已完成** **T88** （完成：2026-08-02 18:59）**建 `open_position_tiger.py` / `close_position_tiger.py` / `move_stop_tiger.py`**（记录：2026-08-01 22:54）（与规范一致：开仓 LO+附加止损、平仓 MO+撤止损、移损先新增后撤旧且量=持仓）。
- ✅**已完成** **T89** （完成：2026-08-03 10:47）**开盘实测港股老虎三动作全链路**（记录：2026-08-01 22:54）（paper 账户小仓位）。

- ✅**已更新** **T90** （更新：2026-08-02 19:06）**老虎 paper 接入：17 位模拟账户号 + 开盘实测三动作下单链路**（记录：2026-08-02 18:59）：2026-08-02 盘后已完成「研究老虎 SDK 港股细节」的可盘后部分（详见上条 `✅**已更新**` 标记与 CHANGELOG 2026-08-02 记录）：① 配置加载根因 = `get_client_config` 必须先传 private_key_path（None 直接 TypeError），正确姿势 `TigerOpenClientConfig(props_path=~/.tigeropen/)`（私钥自动从 private_key_pk1/pk8 读，实测加载成功）；② paper 判定 = account 为 17 位纯数字账户号即 is_paper=True、网关自动走 license-PAPER 域名（domain_conf 已含 TBNZ-PAPER，实测确认）；③ 港股 symbol 只认 5 位带前导 0 裸代码（'02800'/'00700' 实测 OK，HK.02800/2800.HK/700.HK 均拒）；④ lot_size / tick 从 `get_contract` 取（实测 02800=500、00700=100、tick_sizes 完整价位表）；⑤ TradeClient API 结构与只读链路（get_assets→summary.net_liquidation / get_positions / get_orders / cancel_order / create_order+place_order / OrderLeg('LOSS') 附加腿仅限价单支持）全部实测通过；`trade_utils_tiger.py` + 三动作脚本已建（下单链路标注待实测）。**剩余**：a) 用户提供 17 位 paper 模拟账户号写入 `~/.tigeropen/tiger_openapi_config.properties` 的 account 字段（当前是 7 位实盘号 <HK_LIVE_ACCOUNT>，且该实盘账户未开通交易/资产权限——get_assets 返回全 0）；b) 港股开盘时用 paper 账户实测三动作全链路（开仓 LMT+附加止损腿提交与激活、平仓 MKT、移损 STP 先新增后撤旧、成交回查），实测结果回写脚本与文档。

- ✅**已完成** **T91** （完成：2026-08-03 10:47）**老虎 paper 账户开盘实测三动作下单链路（账户号已接入，2026-08-02 19:06 完成接入）**（记录：2026-08-02 19:06）：17 位模拟账户号 `<HK_PAPER_ACCOUNT>`（从 git 历史 2514d76 恢复）已写入 `~/.tigeropen/tiger_openapi_config.properties` 的 account 字段（原 7 位实盘号备份在 `.bak`），实测：is_paper=True、网关自动走 sandbox 域名、`get_assets` 净值 1,000,007.56 USD（初始资金 $1,000,000）、`get_positions` 有 **2 笔历史持仓**（02800 盈富多 500 股 @26.3639、07709 三星 2x 杠杆空 200 股 @43.0638）——开盘实测时先核实这两笔怎么处置（平掉 / 保留）。**剩余**：港股开盘时用 paper 账户实测三动作全链路——开仓 LMT+附加止损腿（OrderLeg('LOSS')）提交与激活、平仓 MKT、移损 STP 先新增后撤旧、成交回查；实测结果回写 `trade_utils_tiger.py` 三脚本与 `auto-mode.md` 实测状态标注（实测前标注为「待实测、不得真实下单」）。

- ✅**已完成** **T92** （完成：2026-08-03 14:56）**修 `close_position_tiger.py` 平仓顺序：必须先撤止损单再平仓（老虎与长桥不同）**（记录：2026-08-03 14:47）：2026-08-03 午盘实测：持空仓（07709 38,500 股）时下 Buy 平仓单（MKT/LMT、38,500/10,000/100 股）全部被拒 EXPIRED，原因 `The order quantity you entered exceeds your current holdings`（持仓 salable_qty=0）；用户实测提示「老虎和长桥不一样，需要先撤销所有止损单才可以主动平仓」——**先 `cancel_all_stop_orders_tiger` 撤掉全部止损单，再下 Buy 市价单即立即成交**。现状脚本顺序是「先 MKT 平仓、成功后才撤止损」（close_position_tiger.py，沿长桥逻辑）——对老虎是错的，须改为「先撤止损 → 再平仓」，并回写 `auto-mode.md` 实测状态。**已修复**：close_position_tiger.py 改为先撤全部止损单再 MKT 平仓（语法验证通过），trade_utils_tiger.py / auto-mode.md 同步，CHANGELOG 2026-08-03 记录；该笔 07709 空单因基础设施问题作废未计入 actions/。

- ✅**已更新** **T93** （更新：2026-08-03 14:55）**老虎模拟盘 T+2 交收限制：当日开空当日不可平（salable=0）**（记录：2026-08-03 14:47）：**错误推断，用户 2026-08-03 14:55 纠正——不是 T+2**。平仓被拒的真实根因 = **挂着的止损单占用可平额度**：老虎把「有挂单（如开仓附加腿落成的 STP 止损单）的持仓」标记为不可平（salable=0 = 持仓 − 挂单数量），新 Buy 平仓单因「exceeds holdings」被拒；**先撤止损再平仓即立即成交**（2026-08-03 午盘实测验证：撤 3 笔止损单后 Buy MKT 38,500 立即 Filled）。旁证：7-31 开空 200 股时其 STP 被 REJECTED（没挂成止损），故 8-03 10:58 平它无阻碍；本笔 38,500 空挂有 STP 所以被拒。与 T+2 无关、当日开空当日可平（先撤止损即可）。修复已由上方「修 close_position_tiger.py 平仓顺序」条目覆盖（先撤止损 → 再平仓）。

## 默认模式 = 信号（signal），自动模式需特别说明（2026-08-03 用户立，取代「港股 default 从 signal 切 auto」）

- ✅**已更新** **T94** （更新：2026-08-02 14:14）**港股完全转自动交易**（记录：2026-08-01 22:54）：港股从信号模式（AI 发信号 `signals/`、手动执行）转自动交易（AI 调脚本下单、`actions/` 记动作）。**信号模式描述已随 2026-08-01 双模式重构处理**——signal 已作为独立可切换 reference（`references/signal-mode.md`）恢复、主 `SKILL.md` 改双模式（默认 auto + 可切 signal），不再是「要改掉的旧模式」。剩余待办：港股老虎三动作建好后把港股 default 切 auto（港股目前仍 signal——老虎三动作未建，见上方「港股老虎模拟账户三动作」节）。**依赖港股老虎三动作先建好**（否则港股 auto 文档 ahead of 代码）。

- ✅**已更新** **T95** （更新：2026-08-03 09:55）**港股 default 从 signal 切 auto（signal 模式保留，两者共存）**（记录：2026-08-02 14:14）：当前决定 = 信号模式与自动交易模式二者共存，不是放弃 signal 改纯 auto。港股目前 default 仍为 signal（AI 发信号 `signals/`、手动执行）；待港股老虎三动作建好后把港股 default 切 auto（AI 调脚本下单、`actions/` 记动作）。**signal 模式保留为可切换模式**——用户说「只发信号 / 手动执行」时仍切 signal；两种模式唯一区别 = 是否使用证券账户，交易策略完全通用（主 `SKILL.md` 已是双模式公共骨架 + `references/auto-mode.md` + `references/signal-mode.md`，2026-08-01 重构完成，此处不重复）。**依赖港股老虎三动作先建好**（否则港股 auto 文档 ahead of 代码）。

- ✅**已完成** **T96** （完成：2026-08-03 09:55）**默认模式改为信号（signal），自动交易（auto）需用户特别说明才用**（记录：2026-08-03 09:55）：2026-08-03 用户决定把全项目默认执行模式翻转为 signal（AI 只发信号、用户手动执行、不碰账户；auto 仅在用户特别说明「自动下单 / 自动交易 / auto」等时才启用），取代上方「港股 default 从 signal 切 auto」的方向（默认不是 auto，而是 signal）。已落地：SKILL.md（description / 模式判定 / 模式提醒顺序 / --mode 说明 / 当前阶段）、references/auto-mode.md 与 signal-mode.md（开头默认说明）、项目 .claude/CLAUDE.md、scripts/trade_utils.py 与 trade_utils_tiger.py 的 parse_mode() 与 load_equity 默认值（不传 --mode 默认 signal、equity 走 equity-log 不连账户），明细见 CHANGELOG 2026-08-03。港股老虎三动作（auto 模式下港股默认账户）照常保留，仅当用户切 auto 时使用。

## 交易规则 / 策略

- ✅**已完成** **T97** （完成：2026-08-11 19:25）**美股突破类入场加「量比达标」硬闸**（记录：2026-08-06 20:42）：8-06 复盘发现连续两笔 signal 美股「无量突破」失败——08-05 NVDA（量比 3.1，开仓 7 分钟后突破失败止损 −1.17R）、08-06 MU（量比 0.8，突破 926 后回踩 921 未守住止损 −1.13R），两笔开仓记录的风险区都自己标注了「突破量能不足」仍开仓。待办：在 `references/trading-strategy.md` 突破类入场判据中加量比门槛（如量比 < 1 不开突破单；量比 < 2 需其它确认叠加），防「明知无量仍开突破单」（与早期 SOXL 假突破教训同型）。**完成说明（2026-08-11）**：`trading-strategy.md`「量价正向确认」节后新增「突破类入场量比硬闸」小节——量比 < 1 不开突破单（无量硬闸）/ 1 ≤ 量比 < 2 须板块共振等确认叠加 / 量比 ≥ 2 按正常突破判据；明确「必要非充分」（NVDA 3.1 达标也失败——量比只过滤「明知无量仍开」，不保证突破必成，突破成败仍由形态 / 位置 / 板块共振决定）；三条边界（早盘量比天然偏高结合分时量能看 / 门槛按标的流动性参照 / 只约束突破类入场、回踩企稳类不受限）。详见 CHANGELOG 2026-08-11。

- ✅**已完成** **T98** （完成：2026-08-05 20:59）**把「多头趋势日 VWAP 企稳顺势做多」教训写进 trading-strategy.md**（记录：2026-08-04 00:11）：2026-08-03 美股盯盘，从美东 11:29（北京 23:29，板块 VWAP 企稳反弹、第 68 段）到 12:08（北京 00:08），SOXL/MU 在 VWAP 上方**震荡上行**（SOXL 112.11→114.18 +1.85%、MU 807.95→816.27 +1.03%，NVDA 持平 206-207 滞涨），AI **全程错过**、定性为「高位震荡无合格入场」。**根因（两条）**：① **判断失误**——把「VWAP 上方震荡上行」当成「高位震荡无机会」，只盯 NVDA 滞涨、用 NVDA 的「无突破」否决了 SOXL/MU 的顺势上行；② **入场条件太严**——只等「突破阻力（NVDA 207.33 / SOXL 116.81 / MU 828.44）」或「回踩 VWAP 企稳」两类信号，但 SOXL/MU 走的是「VWAP 上方震荡上行」（既没回踩 VWAP、也没突破阻力），正好卡在两个条件中间被漏。**与既有教训同根**：和「纪律性不追高错过 SOXL/MU 大涨」是同一根因——**没区分「多头趋势日顺势做多」与「普通追高」**。**规范应补（trading-strategy.md「开仓基准」或「市场结构」节）**：多头趋势日（开盘板块全面站上 VWAP + 持续上行 + 买卖比改善）里，**VWAP 上方企稳 = 顺势做多入场点**，止损放 VWAP 下方（或近期低点）、目标看前高 / 阻力，**不等突破也不等深回踩**（多头不回头、等深回踩等不到）；「等突破 / 等回踩」是震荡市或弱市的入场法，多头趋势日里会整段漏掉。改 skill 内容须先读 skill-creator 按其结构写。**完成说明（2026-08-05 20:59）**：trading-strategy.md 新增「多头趋势日：VWAP 上方企稳 = 顺势做多入场点（2026-08-03 美股教训，补「做多合格入场只有两种」）」节——含为什么不等突破/回踩、与「做多合格入场只有两种」及「纪律性不追高」的关系区分（追高 vs 顺势三问）、反面教训、四条边界（市况确认限定 / 企稳失效即止损 / 不在拉伸高点追 / 3x ETF 止损按形态）。已按 skill-creator 方法论（references 详情文件增量、大白话讲因果）写入。

- ✅**已完成** **T99** （完成：2026-08-05 20:59）**跳空强势股做空禁令加严（写入 trading-strategy.md）**（记录：2026-08-04 12:35）：2026-08-04 药明康德（跳空暴涨 +10.8% 后三次反抽 VWAP 遇阻做空、多头韧性致价格 14 分钟横盘不跌、扣费后 −0.06R）+ 此前 07709 教训——逆跳空强势做空「形态对 ≠ 能跌」（反抽遇阻形态成立，但跳空暴涨股多头韧性使价格跌不动、盘口偏空却横盘）。trading-strategy.md 已有 07709 跳空相关教训，需补「跳空暴涨（当日 +8% 以上）股的反抽遇阻做空 = 高风险形态」明确：要求额外确定性（如跌破今日低 / 板块共振走弱）才出手、或干脆不做（等跳空回补后常态再空）。改 skill 内容须先读 skill-creator 按其结构写。**完成说明（2026-08-05 20:59）**：trading-strategy.md 新增「跳空暴涨股的反抽遇阻做空 = 高风险形态（2026-08-04 药明康德教训，加严 07709 跳空教训）」节——含为什么（多头韧性致横盘、无下跌空间还倒贴费）、反面教训、处理二选一（额外确定性才出手 / 干脆不做、默认 ②）、三条边界（+8% 经验阈值按量能资金流判断 / 当日已转弱仍可按「下跌趋势确认」框架空 / 不放松「反抽破前高放弃做空」）。已按 skill-creator 方法论写入。

- ✅**已完成** **T100** （完成：2026-08-03 10:34）**缩短 SKILL.md「早盘开盘首小时只观察」规定（60 分钟太长 + 一刀切，2026-08-03 立）**（记录：2026-08-03 10:18）：现行 [SKILL.md:90](.claude/skills/trade/SKILL.md#L90) 规定港股 09:30-10:30 / 美股 09:30-10:30 不发任何交易动作（含开仓、平仓、移损），只观察行情性质，10:30 后才允许开仓（2026-07-31 立，见 CHANGELOG 第 71 条）。三个问题：① **时间太长**——开盘最混乱的其实是前 15-20 分钟冲动盘消化期，30 分钟后趋势往往已较清晰；卡满 60 分钟等于放弃全天流动性最好、波动最大的黄金时段（项目自己反复强调「早盘必密采样、机会与风险都在这」）。港股上午盘 09:30-12:00 仅 2.5 小时、砍首小时后上午只剩 1.5 小时可交易，对港股伤害远大于美股（美股全天 6.5 小时、砍 1 小时影响小）。② **一刀切、不分类，与既有实战教训冲突**——07-17 MU 头肩底 09:48 破颈线确认做多（[trading-strategy.md:138](.claude/skills/trade/references/trading-strategy.md#L138)，之后涨 +29 点）、07-17 MU 连破四道阻力执念等反弹踏空整段（[trading-strategy.md:268](.claude/skills/trade/references/trading-strategy.md#L268)，核心是「方向变了立刻切、别固守太久」），首小时一刀切既会错过 09:48 这类好信号、又与项目一贯的「动态修正方向」精神相悖。③ **连平仓 / 移损都禁是风控隐患**——正常有「持仓不过夜」纪律、首小时无仓；但万一前日漏平（美股夜盘到美东开盘尤其可能），首小时价格急速反向却不能移损 / 平仓 = 裸奔。平仓、移损是风控动作，任何时候都应照常可用，不该被「观察期」挡住。

  **建议改法（3 处）**：a) 时间从 60 分钟缩到 15-20 分钟；或干脆不绑死时长，改为条件触发——跑完 `monitor_summary` 判出行情性质（震荡 / 趋势）+ VWAP 方向明确 + 量价配合，**条件一满足就允许开仓**（可能 09:45 满足、也可能 10:05 才满足），与「每 10 分钟动态重估」精神一致、比机械卡表强；b) 平仓 / 移损解除限制——观察期只禁开仓，风控动作任何时候照常；c) 暴跌标的的「开盘 30 分钟内不开多、等双底 / 头肩底确认」单独规则保留不动（策略层已有、针对接飞刀更精准，不该靠「全市场禁交易一小时」这种过宽手段覆盖）。

  **改前注意**：改 skill 内容须先读 `~/.claude/skills/skill-creator/` 按其结构规范来（主文件精简、详情拆 references，不违反 Progressive Disclosure）；改完记 CHANGELOG（含「缩短首小时观察期」的原因 + 评估对既有教训 07-17 头肩底 / 07-17 破阻力踏空 的影响——预期是修复冲突而非回归）。

- ✅**已完成** **T101** （完成：2026-08-05 23:17）**预估赔率打折系数：开仓判 EV 时对初始预期赔率打折**（记录：2026-08-04 12:35）：2026-08-04 复盘（N=38）5 笔新增交易三阶段赔率全部「预估乐观、落地打折」——初始预期 1.5~2.9R，修正后降一档、落地再降一档，仅 NVDA 1 笔兑现（+1.06R）、其余 4 笔从预估 2R 上下打到打平 / 止损（HK.02359 1.93→2.09→−0.06R、MU 空 2.86→2.06→−0.97R、MU 多 2.80→2.63→−0.60R、HK.09988 1.50→1.00→−0.30R）。两个打折模式：① 滑点 + 止损距重算（修正 < 初始）；② 平仓时机 / 形态失效（落地 < 修正）。建议开仓判 EV（≥1.2 门槛）时对「初始预期赔率」打经验折扣（如 ×0.5）再判、避免系统性过度乐观。需用历史三阶段打折分布标定折扣系数（当前 5 笔样本太小、待积累）。**完成说明（2026-08-05 23:17）**：用 8-03~8-05 全部可配对三阶段记录（12 笔初始 ≥ 1.2 样本）实算标定——75%（9/12）落地 ≤ 0、落地/初始 比值中位数 −0.22 均值 −0.08（初始均值 2.69R、落地均值 −0.11R），「预估乐观、落地打折」确认为系统性现象；**×0.5 折扣规则落地**：trading-strategy.md「开仓基准」改写（初始预期赔率 ≥ 1.2 改为打 ×0.5 折扣后口径、等价 ≥ 2.4）+ 新增「预估赔率系统性打折」节（标定数据、×0.6/×0.5/×0.4 敏感性、四条边界），SKILL.md 三处同步（6 要素第 3 条 / 操作原则第 1 条两道闸 / 开仓前自查清单 EV 判据）。**系数为 12 笔保守粗标定、待 ≥30 笔重新标定**（新跟踪条目已记入 TODO.md）。

- ✅**已完成** **T102** （完成：2026-08-05 23:17）**震荡市策略失效应对（近 10 笔 EV 转负的归因行动）**（记录：2026-08-04 12:35）：2026-08-04 复盘暴露——账面正 EV（+0.22R）全靠 4 笔 ≥2R 大胜撑起（占总 R 和 170%、剔除 top2 后剩 36 笔 EV≈0），且 4 笔大胜集中在 7-17~7-21 半导体大波动期；8 月以来近 10 笔 EV −0.29R、胜率 30%（大胜来源消失后整体转负）。策略本质是「趋势日 / 暴跌顺势大胜捕获器」、震荡 / 无趋势市况天然不适配。需研究震荡市应对：① 减少出手（无大胜土壤时控亏损）；② 改进小赚小亏出场（避免扣费后转负）；③ 前置识别「趋势日 vs 震荡日」决定是否出手 / 频次。属策略层大问题、待用更多样本验证假设。**完成说明（2026-08-05 23:17）**：三件事全部落地为 trading-strategy.md「震荡市策略失效应对」节（「震荡市策略」节后）——① **前置识别趋势日 vs 震荡日、震荡日减少出手**（monitor_summary 行情性质判别 + VWAP 方向：震荡日出手频次减半、只做区间边沿 + 高置信 + ×0.5 折扣后赔率仍厚的交易、其余空仓观望；趋势日维持正常出手）；② **改进小赚小亏出场避免扣费转负**（港股窄止损止损距 < 1.5% 从严按折扣后赔率估、浮盈 < 2R 用「止损单两种用法」用法 B 放趋势反转位让利润奔跑避免保本就平、接近止损按纪律走不拖延）；③ **趋势日敢捕捉**（防识别错方向：不在趋势日里套震荡日小心尺度）。边界：假设待样本验证（结构性克制而非已证伪）、减少出手 ≠ 停手、窄止损从严不动止损位定法。假设验证由每次复盘窗口分析自然覆盖、无需单独跟踪。

## 密采样多层防护（12 层全落实，2026-08-04 立）

> 2026-08-04 盯盘期间 AI 擅自用 cron + 直接 snapshot 绕过 monitor_segment 40 秒密采样降频（详见 `monitoring.md`「2026-08-04 教训」+ CHANGELOG）。**12 层防护全落实**（A1/A3/B4/C7/D10 首批 5 层 + 下列 7 层），非 100% 银弹——AI 仍能 kill 进程绕过，盯盘纪律最终靠外部监督 + 多层防护 + AI 自觉。

- ✅**已完成** **T103** （完成：2026-08-04 12:24）**A2：TaskStop hook**（记录：2026-08-04 12:04）：A3/B4 hook 拦得住「调 snapshot 替代」「回合结束未重启」，但 AI 仍能 TaskStop / `kill` 主动停掉 monitor_segment 进程。计划：`settings.json` hooks 加对 TaskStop 工具的 PreToolUse 检查（停 monitor_segment 相关 task 时提醒「盯盘期间禁止停密采样，除非已到停盯边界」），或限定 TaskStop 仅停非密采样任务。难点：TaskStop 有正当用途（停出错进程），需精细区分。**已落实**：`monitor_guard.py` 加 taskstop 模式 + settings.json PreToolUse matcher TaskStop，盘中 TaskStop 且 monitor_segment 在跑时提醒确认（不阻断，避免误伤停出错进程）。
- ✅**已完成** **T104** （完成：2026-08-04 12:24）**B5：resume.py 强化时间断层哨兵针对「主动停」**（记录：2026-08-04 12:04）：现有 resume.py / monitor_segment 启动时的时间断层哨兵检查「距上次采样 ≥5 分钟」判断层，但主要针对断网 / 故障。计划：强化为针对「主动停密盯」——resume.py 检测到断层时输出强警告「疑似主动停密采样（非断网），立即重启 monitor_segment 恢复循环」，并在末尾自动建议重启命令。**已落实**：resume.py 盘中（hk/us_status 含「盘中」）断层输出「疑似主动停密采样、立即重启」强警告，盘外才按断网/故障处理。
- ✅**已完成** **T105** （完成：2026-08-04 12:24）**B6：外部 watcher 守护进程（launchd）**（记录：2026-08-04 12:04）：周期（如每 2 分钟）检查 monitor_segment 是否在跑、没跑自动重启——AI 主动停了也会被外部重启，最硬（超出 Claude Code 工具范围）。计划：写 launchd plist 或独立 python watcher，盘中时段自动守护 monitor_segment。难点：需正确传标的 / 关键位参数、与 AI 段间分析节奏协调（watcher 重启的进程 AI 怎么接管）。**已落实（部分）**：写 `monitor_watcher.py`（盘中检查 monitor_segment + 没跑发 macOS 系统通知，独立于 session）+ `com.daytrading.monitor-watcher.plist` launchd 模板（每 120s 触发）。**未做自动重启**（需当前盯盘标的参数、watcher 无法知道）——只检查 + 系统通知用户、由用户决定回 session 重启。部署需手动 `launchctl load` + 改 plist 占位路径。
- ✅**已完成** **T106** （完成：2026-08-04 12:24）**C8：monitor_log 采样时间戳复盘降频**（记录：2026-08-04 12:04）：复盘时遍历 monitor_log 采样时间戳，段间 gap >2 分钟标记为降频疑点，写进复盘报告（让降频在复盘暴露）。计划：review.py 或独立脚本加 gap 检查，复盘报告列降频疑点段。**已落实**：独立脚本 `monitor_log_gap_check.py`（>120s 标记降频疑点 + gap 统计，实跑验证药明 8-04 log 164 点 / 平均 12s / 最大 61s / 无降频疑点）；复盘时手动跑该脚本检查各标的 log（暂未集成进 review.py、待后续）。
- ✅**已完成** **T107** （完成：2026-08-04 12:24）**C9：用户监督（最有效但非工程化）**（记录：2026-08-04 12:04）：这次违规就是用户抓住的——用户监督是最有效一道，但需用户在场、不可工程化。计划：本条仅作提醒记入（不做工程实现），盯盘时用户留意段结束输出连续性（C7 指标）+ 段间节奏，发现降频即纠正。**已落实**：monitoring.md「多层防护」说明第⑫层明确「用户监督最有效一道、非工程化」，提醒用户盯盘时留意 C7 连续性指标 + 段间节奏。
- ✅**已完成** **T108** （完成：2026-08-04 12:24）**D11：盯盘自查清单加一条**（记录：2026-08-04 12:04）：`SKILL.md`「开仓前自查清单」或 `monitoring.md` 加一条「是否持续 monitor_segment 循环、未擅自降频（cron / 直接 snapshot 替代）」。软约束（会忘），作兜底。**已落实**：monitoring.md「防中断三件套」加第 4 条「每段自查未擅自降频」。
- ✅**已完成** **T109** （完成：2026-08-04 12:24）**D12：preflight.py 启动检查 monitor_segment 入口**（记录：2026-08-04 12:04）：`preflight.py` 盯盘启动时检查 + 提醒「密采样唯一入口是 monitor_segment、禁 cron/snapshot 替代」，并在启动序列里强调。软约束（启动时一次性提醒），作兜底。**已落实**：preflight.py 末尾加 print「密采样提醒：唯一入口是 monitor_segment.py 40 秒循环，禁 cron/snapshot 替代」。

## 🟢 绿色紧急度·2026-08-19 批量处理（2 条全清）

- ✅**已完成** **T110** （完成：2026-08-19 23:14）**auto 模式开仓成功后响铃提醒**（记录：2026-08-19 11:22）：自动交易模式下 AI 直接调脚本开仓，用户若不盯屏幕感知不到已开仓——开仓成功（脚本确认成交）后要响铃提醒，把用户注意力拉回来。**完成说明（2026-08-19）**：① `ring.sh` 加 `RING_LOG` 环境变量（auto 响铃写 `signals/ring-log-auto.csv`，与 signal 的 `ring-log.csv` 隔离——后者末行被 `log_action.sh`（signal 拍板时刻）与 `resume.py`（signal 断层检测）消费，auto 写入会污染）；② `trade_utils_tiger.py` 新增 `ring_after_fill()`（异步调 ring.sh、静默失败不碍交易链路）；③ 港美两开仓脚本（`open_position_tiger.py` / `_us.py`）在**全额成交与部分成交两条路径**的成交确认后内置响铃（工具强制、不靠 AI 记得）；④ `references/auto-mode.md` 新增「开仓成交响铃」节。验证：ring.sh 两路径实测（默认仍写 ring-log.csv / RING_LOG 隔离写入）、`ring_after_fill` 实跑、三脚本 py_compile 通过。遗留：真机成交后的实盘音效验证待下次 auto 盘中开仓自然确认。
- ✅**已完成** **T111** （完成：2026-08-19 23:14）**多会话并行的标的池自动划分（互斥分配）**（记录：2026-08-18 14:33）：多会话并行盯盘（方案 A）的标的池原是用户手动给每个会话划分——用户不想再做这一步，要求自动切分成互斥子池。要点：互斥（同一标的不落两会话）、会话数可变、承载形式实现时定。**完成说明（2026-08-19）**：采用**认领制**（claim，非一次性均分）——会话启动时从共享候选池认领一段未占用标的，天然支持会话数动态变化与先后启动。落地四件：① 新脚本 `scripts/pool_claim.py`（claim / status / release 三命令；flock 锁内「检查占用→登记」原子完成；死会话自动释放——jsonl 停更 >30 分钟同 monitor_watcher 口径；认领表 `tmp/pool_claims.json` 当日有效跨日重置）；② `monitor_guard.py` 加跨池抢标的拦截（采样段 targets 含其它会话已认领标的 → PreToolUse 阻断；无认领表 / 无会话 id 不误伤）；③ `monitor_unregister.sh` 联动 `release --all`（停盯注销时同步释放）；④ `preflight.py` 打印当前全局划分。文档：monitoring.md「多会话并行盯盘」节改写（手动划分 → 自动认领五条机制）、SKILL.md 启动序列与停盯段同步。验证：多会话认领冲突互斥、死会话自动释放、增量合并、释放单个/全部、unregister 联动、hook 三场景（冲突阻断/无冲突放行/自己认领放行）、完整 preflight 全链路实测通过。

## 2026-08-21 批量处理（4 条全清：watcher 铃声去横幅 / 美股购买力查询 / 删空转警报 / 实盘误开防护落地）

- ✅**已完成** **T112** （完成：2026-08-21 19:15）**密采样守卫提醒不弹横幅**（记录：2026-08-20 09:49，09:53 用户修订：只去横幅不去弹窗；11:24 更新——剩余两类提醒仍适用）：monitor_watcher.py 的 notify() 铃声经 `display notification "" sound name "Basso"` 发出、该调用本身会弹一条空内容横幅——提醒触发时铃声横幅仍会弹出。改法：只去横幅、不动弹窗——铃声改用不弹横幅的方式发出。**完成说明（2026-08-21 19:15）**：铃声改 `afplay /System/Library/Sounds/Basso.aiff` 直接播放（不产生任何横幅 / 通知），弹窗与 tmp/watcher_notify.log 持久日志照旧；afplay 实测可播放、py_compile 过。注：空转警报 2026-08-21 已删（见下条），本条最终只对白名单漂移提醒适用。
- ✅**已完成** **T113** （完成：2026-08-21 19:15）**美股开仓脚本购买力降档查询报「老虎脚本只支持港股」**（记录：2026-08-20 04:20，美股盯盘实测发现）：`open_position_tiger_us.py` 的购买力主动降档逻辑调 `get_buying_power_tiger`（只认港股代码），美股标的触发「⚠️ 购买力查询失败 US.MU」警告——主动降档失效、正常路径烧降档轮次。需补美股版查询（buying_power × long_initial_margin ÷ 参考价）。**完成说明（2026-08-21 19:15）**：`trade_utils_tiger_us.py` 新增 `get_buying_power_us`（与港股版同构、USD 同币种无汇率换算），`open_position_tiger_us.py` 改调本市场版；mock 三分支单测 + 模拟账户实测（US.MU 保证金率 0.3）全过。
- ✅**已完成** **T114** （完成：2026-08-21 19:15）**删除「空转警报」功能**（记录：2026-08-21 13:01）：monitor_watcher.py 的空转检测（采样进程在跑但分析心跳停更 ≥180 秒 → 弹窗报警）整个移除——含空转检查逻辑、相关通知冷却适配、注释与文件头描述同步清理；删除后 watcher 仅剩「老虎白名单漂移检测」一类提醒。注意与橙色「密采样守卫提醒不弹横幅」条的联动：该条只剩白名单漂移提醒适用。**完成说明（2026-08-21 19:15）**：monitor_watcher.py 空转检查逻辑（analysis_beat_fresh / _sampling_log_age_seconds / sampling_running + main 空转分支）与常量全删、main 仅剩白名单漂移检测；plist 注释同步并已部署到 ~/Library/LaunchAgents/；preflight.py _ensure_watcher 文案、monitoring.md 心跳节与沿革段、SKILL.md 停盯节同步；盯盘空转防护由会话内 monitor_guard 空转硬门独扛（未动）。
- ✅**已完成** **T115** （完成：2026-08-21 19:15）**实盘误开防护「会话级解锁 + 网络层物理拒单」方案落地**（记录：2026-08-21 11:40，设计收敛完成）：完整设计见本条原文与 SKILL.md / accounts.md 同步节。**完成说明（2026-08-21 19:15）**：八步清单全落地——① PostToolUse 对 AskUserQuestion 触发实测通过（payload 含 tool_response.answers + session_id，临时 hook 写日志坐实后删）；② `trade_utils_tiger.py` apply_proxy 实盘解锁闸（无有效解锁 → 跳过挂代理保持本地直连、被 IP 白名单连接层物理拒；模拟盘零改动）；③ 新建 `scripts/live_unlock.py`（token 校验 / verify-auth / revoke / status / 下单脚本前置闸 / --init-salt，salt 已初始化且值不经 AI 上下文）+ 新建 `hooks/live_auth_witness.py`（PostToolUse matcher AskUserQuestion 见证真实点击写 10 分钟 HMAC 短时凭证，含糊提问不产生凭证）；④ 6 个下单脚本（open/close/move_stop × 港美）全部接 `live_gate_for_order_scripts`（`blocked_by:"live_locked"` 结构化拒单），顺带修 close_position_tiger_us.py 的 `--account` 空格形式解析 bug（原静默走模拟盘）；⑤ .gitignore 加 live-unlock.local / live-auth.local；⑥ SKILL.md / auto-mode.md / accounts.md / config.example.json / accounts.example.json 五处文档同步；⑦ CHANGELOG 三条目已记（含回归风险评估）；⑧ 实测：hook 四场景单测、授权链端到端（点击→凭证→解锁→焚毁→revoke）、会话绑定三场景（本会话 ok / 伪造 sid 拒 / 人工终端放行）、全 6 脚本 live_locked 拒单、load_config(live) 不挂代理 + 模拟盘照旧代理——全过；「解锁后实盘查询链路通」一项留首个实盘交易日开盘时段补验。残余边界（环境变量冒用 sid / 主动绕过 / 24h 开窗）如实记录在 accounts.md。

- ✅**已完成** **T117** （完成：2026-08-24 12:05）**live_unlock token 的 sid 绑定在多会话 / VSCode 扩展环境下校验失败（bad_token）——授权后实盘 API 仍物理不可达**（记录：2026-08-24 10:03，原条含根因分析：hook 写 token 用 stdin payload sid、`live_unlock.py` 验 token 用 Bash 环境变量 sid，两处口径在 VSCode 扩展 / 会话恢复场景不一致；多会话并行盯盘是设计目标、用户要求多会话都有下单权限）。**完成说明（2026-08-24 12:05）**：解锁机制改为**按会话 ID 分文件**——`live-unlock.<sid>.local` / `live-auth.<sid>.local`（`scripts/live_unlock.py` + `hooks/live_auth_witness.py` + `.gitignore` 通配），各会话授权各持解锁、互不覆盖互不偷凭证；本会话（ad9943d6）实测走完整授权链解锁成功、并行会话（cccb62d4）解锁文件无损共存，13 项功能测试全过（跨会话隔离 / 拷贝改名 bad_token 拒 / revoke --all 等）。取 sid 口径问题同时消除：本会话内 hook payload sid 与 Bash 环境变量 sid 一致（ad9943d6 双处验证），若仍遇不一致场景属新问题、按新条登记。`status` 增列 `other_sessions_unlocked`、`revoke` 增 `--all`。SKILL.md / accounts.md 授权链描述已同步，CHANGELOG 已记（含回归风险评估）。

## 2026-09-01 批量处理（8 条全清：红色 2 / 黄色 4 / 绿色 2，红色紧急度清零）

- ✅**已完成** **T130** （完成：2026-09-01 19:42）**「开仓非拦截警告累计 ≥2 条 → 降一档置信度、不开仓」做成脚本硬拦（2026-08-28 立规、当天盘中失守，2026-08-31 复盘实证）**（记录：2026-08-31 12:50）。**完成说明（2026-09-01 19:42）**：`open_position_tiger.py` / `open_position_tiger_us.py` 在净赔率校验后汇总本单全部非拦截警告字段（`warning_` 前缀 / `_warning` 后缀自动收集——后续新增警告字段无需改此处），同单 ≥2 条硬拦拒单（`blocked_by:"warnings_cumulative_2"` + `warnings_counted` 明细 + 处置提示），1 条保留在场打印不拦；与 trading-strategy.md「同位置警告累计两条 = 降一档置信度」同语义（🟢→🟡 与不开仓同义、脚本直接拦死不留裁量）。顺带补齐美股版 `warning_target_unreachable` 止盈可达性警告（此前美股版缺此警告源、该防线在美股只有 stability 一条永不触发；文案不引用港股样本统计）——两市场防线同构。
- ✅**已完成** **T131** （完成：2026-09-01 19:42）**一级降频线工具强制（连败计数 + 开仓闸），堵 08-28 漏执行**（记录：2026-08-31 12:50）。**完成说明（2026-09-01 19:42）**：① `trade_utils_tiger.py` 新增 `update_losing_streak()`（平仓成交后结算 R = net_pnl_app ÷ 净 max_loss（缺失时 fee_schedule 估兜底），写 `tmp/losing_streak.json`：跨日累计连败数 streak + 最近 5 笔 R 序列）+ `check_losing_streak_gate()`（连败 ≥3 或 ≥2 且最近两笔亏满型（净 R ≤ −0.95）**且**最近一笔亏损平仓在今日 → 拦；昨日触发今日放行 =「次日再战」）——连败判定按规则原文「当日或跨日累计」口径实现（T131 原文「按交易日重置」若照字面会漏跨日连败判定，以 review-and-evaluation.md 规则原文为准）；② 港美两 close_position 脚本平仓成交后自动调用（输出 `losing_streak` 字段供转录）；③ 港美两 open_position 脚本前置闸（`blocked_by:"losing_streak"` + `--force` 用户决策覆盖通道，AI 不得自主加）；④ 新脚本 `update_losing_streak.py`（CLI，供 BRACKETS 止损/止盈腿自动触发平仓的补记场景用——该路径不走 close 脚本，与 T132 闭环检查联动）。五场景冒烟全过：三连败拦 / 次日放行 / 盈利清零 / 两笔亏满拦 / 亏满+微亏（streak=2 非都亏满）放行。
- ✅**已完成** **T129** （完成：2026-09-01 19:42）**log_action.sh 时间戳插入位置对空行脆弱（NR==2 硬编码），需改为「首个标题行后插入」**（记录：2026-08-31 11:25）。**完成说明（2026-09-01 19:42）**：awk 逻辑从「NR==2 固定行号插入」改为「首个以 🟢🔴🟡🔵 开头的行（动作标题行）后插入」（index 按字节匹配 UTF-8 前缀、BSD awk 安全）；整个内容无 emoji 标题行时兜底追加末尾 + stderr 警示（位置错在末尾不再破坏 `_parse_actions` 分节——标题前错位正是 08-31 误告警根因）。三场景实测过：标准格式 / 框线+空行+标题（事故形态，时间行正确落标题后）/ 无 emoji（兜底）。
- ✅**已完成** **T132** （完成：2026-09-01 19:42）**盯盘中断恢复后回查持仓状态 + 补记平仓（01888 08-27 漏记 4 天类防线）**（记录：2026-08-31 12:50）。**完成说明（2026-09-01 19:42）**：新建 `scripts/actions_check.py`（当日 actions 开平闭环检查）：解析当日 HKT+ET actions 文件（分节正则与 account_status._parse_actions 同源、代码提取扩展认美股），按标的数量配平——close 无量信息时按全平核销（close_position 转录模板平仓节不带量行，2026-09-01 当日实测曾因此误报 02513 未闭环，回退提取「开仓价｜…（4700 股…」括号内股数 + 兼容「补录时间」），未闭环标的查账户实持判「仍持仓（正常）」/「已平仓未补记（🚨 给补记指引：核对账户口径 → 查当日订单拿触发价 / K 线推定 → log_action.sh 补记 → update_losing_streak.py 计入连败）」，退出码 1 醒目警示。两个触发点挂钩：`resume.py` 收尾自动跑（中断恢复固定动作，透传 --account）+ `monitor_unregister.sh` 停盯总结前置（if 内失败不触发 set -e）。实测：2026-09-01 当日文件闭环通过（退出码 0）、08-27 补录文件解析正确、未闭环构造场景验证。
- ✅**已完成** **T135** （完成：2026-09-01 19:42）**pool_claim 存量裸数字键认领清理 + release 路径同样过归一**（记录：2026-09-01 11:58）。**完成说明（2026-09-01 19:42）**：① 当日认领表已全归一键（`_norm_sym` 上午修复后新 claim 全走归一、上午裸键条目被死会话清理消解，收盘后实查确认表内 5 会话全部 HK. 前缀、无裸键残留）；② `cmd_release` 参数解析已走 `_norm_sym`、`cmd_status` 无标的参数无需归一（核对代码确认）；③ 三子命令裸格式冒烟：claim `700`/`09990`、release `09990`、status 全链路通过（测试会话造活 jsonl 避开死会话清理干扰、测后清理无残留）。**冒烟暴露并修复新漏洞**：`_norm_sym` 只补 5 位裸数字（01211），3/4 位短格式（700 / 0700）不进任何分支、原样落键——改为「裸数字（≤5 位）一律先补 HK. 前缀再 zfill 到 5 位」，等价类断言全过（700/0700/HK.700/hk.00700 → HK.00700 等）。
- ✅**已完成** **T133** （完成：2026-09-01 19:42）**代理 IP 漂移提醒给出「当前全部可用 IP」，支持用户先清空老虎后台白名单再一次性加入**（记录：2026-08-31 13:16）。**完成说明（2026-09-01 19:42）**：`proxy_guard.py` ① 节点全景改 `xpilot node list` 实查（新 `fetch_nodes_live()`，解析 id/name/protocol/IP/当前节点 `*` 行；失败回退 nodes.json，打印数据来源）；② 提醒 IP 串从「漂移新 IP」改为**全量可用清单**（实查全部节点出口 IP——白名单数量上限下「先清空 → 一次性全量粘贴」的正确操作对象；死 IP 不给，清空重配正是清掉它们），三分支（漂出/全漂出/建议）文案全改带操作步骤；③ 点「已添加」的本地同步从 `merge_whitelist`（合并，残留老虎页已清掉的死 IP）改为 `replace_whitelist`（**全量替换**，与老虎页「先清空再全量加入」操作对应），log 记录替换前后数量。accounts.md「老虎 IP 白名单 + 代理节点」节运维口径同步。--dry-run 实测：实查 6 节点、白名单全景一致、退出码 0。
- ✅**已完成** **T128** （完成：2026-09-01 19:42）**白名单提醒窗支持最小化到 Dock（替换 osascript 模态弹窗载体）**（记录：2026-08-31 10:18）。**完成说明（2026-09-01 19:42）**：选型 **AppKit 面板（Swift 单文件）**——osascript display dialog 是模态窗无最小化按钮、通知中心方案需 terminal-notifier 外部依赖（本机未装）、Swift 面板零依赖且体验最正（真窗口 + 黄色最小化钮 + Dock 图标唤回）。落地：① 新源码 `scripts/alert_panel.swift`（~200 行：NSWindow(.miniaturizable) + 可滚动正文 + 双按钮（取消/已添加）或单按钮（知道了）两模式 + 超时自动关 + 结果文件 `{"clicked":...}` 通信 + --self-test），编译产物 `tmp/alert_panel`（gitignore、不入库；proxy_guard 首次使用自动 swiftc 现场编译，无 Xcode CLT 回退 osascript 旧载体保底）；② `proxy_guard.py` await_confirm 换面板 + 轮询结果文件（ok → 全量替换 / cancel·timeout → 删 pending），防叠加闸 `dialog_pending()` 匹配新模式（alert_panel）+ 旧模式兼容；③ `.claude/hooks/monitor_watcher.py` 告知弹窗同载体切换（单按钮「知道了」30 秒超时 = 旧 giving up 语义；detached 弹出不阻塞 watcher 主循环——旧 osascript 同步阻塞至多 35 秒；watcher 不做现场编译避免 launchd 10 秒节奏被拖）。实测：swiftc 一次编译通过、pending → 2 秒超时 → result `{"clicked":"timeout"}` 链路验证。
- ✅**已完成** **T134** （完成：2026-09-01 19:42）**交易标签系统：trades CSV 增 mode 列（四枚举）+ review.py / bayes_evolution.py 按 mode 分组统计**（记录：2026-08-31 13:30）。**完成说明（2026-09-01 19:42）**：① `review.py` CSV 新增 `mode` 列（signal/auto_paper/auto_live/shadow 四值，非法值报错；老 CSV 无该列时按 shadow 列兼容推断、其余 unknown 并提醒补标）；② `reviews/2026-08-31-trades-hk.csv` 58 笔全量回溯打标（signal 22（07 月信号时代）/ auto_paper 20（08-03~17 模拟，含 08-17 模拟单）/ auto_live 14（08-18 起实盘）/ shadow 2）——分组结果 signal +0.497R / auto_paper +0.271R / auto_live −0.390R 与 08-31 复盘的分阶段退化结论（+0.50/+0.33/−0.42）量级一致（小差异正是旧日期近似口径的误差、mode 列消除之）；③ `review.py --mode <m>` 单跑子集（--shadow-only 统一映射 --mode shadow 向后兼容）+ 输出【按模式分组】表（N/胜率/EV/平均R，复盘报告「分阶段统计」以此为准、日期近似切分口径废止）+ 样本明细带模式列；④ `bayes_evolution.py --mode <m>` 演化图子集（子集图文件名带 `-mode-<mode>` 段不覆盖全样本图——初版曾覆盖 08-31 全样本图，已修 + 重跑恢复）；⑤ 规范落地：review-and-evaluation.md 新增「trades CSV 的 mode 列与按模式分组（复盘必含项）」节（转录侧必带 mode 列 + 报告必含分组表）、review-input.example.csv 示例带 mode 列。实测：全量跑分组表正确、--mode auto_live 单跑 14 笔正确、example CSV 跑通。
