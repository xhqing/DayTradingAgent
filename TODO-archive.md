# DayTradingAgent 待办归档（已处理）

> 本文件归档**已处理**的待办条目（`✅**已完成**` / `✅**已更新**`），保留可回溯性——回溯排查时看这里（当初打算做什么、做没做、什么时候做的、后来改没改主意）；活跃待办只看 `TODO.md`（项目根文件制，2026-08-05 撤销 todo-skill 定案；方法论见全局 CLAUDE.md「待办（TODO）管理」节，本文件与 `TODO.md` 同处项目根）。

## 🟠 橙灯·平台费固定模式改造（2026-08-17 21:07，费率口径全面切换）

- ✅**已完成**（完成：2026-08-17 21:07）**平台费口径全面改固定模式、弃阶梯式（成本计算统一）**（记录：2026-08-17 19:11）：所有成本计算的平台费从阶梯式（按当月累计订单数）改**固定模式**——港股 15 HKD/笔；美股顺带把整个费用结构按官网核改到位（旧「佣金 0.029% 最低 15 按额」是简化、结构错配）：佣金 0.0039 USD/股（上限 0.5%×额）、平台费 0.004 USD/股（每笔最低 1 USD、上限 0.5%×额，cap 只在低价股 < 0.8 USD/股 触发——**实现中发现并修正一个反向 max() bug：初版把 cap 当下限，MU 106 股大单被算 458 USD 平台费、正确值 1 USD**）、代收近似 0.00396 USD/股。改动面：① `fee_schedule.py` 重写（港美分流、美股按股函数、阶梯表降为备查）；② `trade_utils_tiger.py`——`build_fee_ctx` 退化为纯属性打包（不再查账户）、`get_month_order_count_tiger` 移出费率链路留诊断用途、`_net_odds` 调 `FS.fee_per_side` 传 shares；③ `review.py` / `bayes_evolution.py`——删「(自然月, 市场)」订单计数器，费率与订单数解耦；④ 文档六处同步（risk-management.md「费率与成本」整节重写 + SKILL.md 6 要素第 4 条 + trading-strategy.md 赔率三阶段 + review-and-evaluation.md 净盈亏定义 + auto-mode.md / signal-mode.md 平仓必填节）；⑤ signal 模式「为算阶梯平台费查当月订单数的只读查询例外」随固定模式取消（费率不再需要任何账户查询）。**验证**：七脚本 py_compile 通过；fee_schedule 自检（港股大单 stock 14.32 bps / etf 4.32 bps、美股 MU 106 股单边 1.83 USD）；review.py 实跑港股 42 笔（EV +0.47）与合并 61 笔（EV +0.24，含美股 20 行按股计费）；bayes_evolution 出图 7 张。费率来源：港股 https://www.itigerup.com/hans/help/detail/68697332 、美股 https://www.itigerup.com/hans/help/detail/74820992 （2026-08-17 实查核实；官网美股佣金行未列最低收费，0.99 最低是外部机构费及交易活动费的、非佣金——TODO 原文「佣金最低 0.99」系误记，按无最低实现）。关联黄灯「fee_schedule.py 美股佣金最低 15 按美元计系简化」随之一并完成核改，归档见黄灯节同日条目。
- ✅**已完成**（完成：2026-08-17 21:07）**`fee_schedule.py` 美股佣金最低 15 按美元计系简化（保守方向）**（记录：2026-08-16 14:27；原列黄灯「其余低危」）：随橙灯「平台费固定模式」改造一并核改到位——美股佣金改按股 0.0039 USD/股（cap 0.5%×额）、平台费 0.004 USD/股（最低 1/笔、cap 0.5%×额），与港股按额结构彻底分流。详见上条橙灯条目。

## 🔴 红灯·盯盘空转修复（2026-08-17 20:05，四步修法全落地）

- ✅**已完成**（完成：2026-08-17 20:05）**盯盘空转：采样在跑但分析链死亡、全部防护不可见（2026-08-17 实录）**（记录：2026-08-17 15:50）：auto 双会话（实盘 + 模拟）各空转——一会话 14:02 后 0 分析文本 / 52 次纯重启采样；另一会话被用户 15:27 纠正「每段给判断」后 10 分钟内复发。根因：① 2026-08-07 采样源迁 ws_segment / futu_ws_segment 时，monitor_segment.py 段输出的强制锚点（📊 VWAP 检查 + ⏰ 10 分钟方向重估提醒）没跟着搬，ws 段输出只剩裸数字；②「每段三层分析」是 reference 软约束（与 2026-07-28 兆易「规定在 skill 里却没调用」同根因）；③「盯盘输出精简」与「每段分析」的张力被化解为近零输出。**四步修法全部落地（2026-08-17 20:05）**：① ws_segment.py / futu_ws_segment.py 段结束输出补齐三锚点（📊 VWAP 检查【ws 用富途 OpenD 补查 avg_price，OpenD 不可用降级为强制提示不静默】+ ⏰ 10 分钟重估提醒【marker 去重，逻辑同 monitor_segment】+ 👉 空转防护提示行「先给本段判断 + 写心跳再重启」）；② monitoring.md 新增「每段最小输出模板 + 分析心跳」节——一行式判断行模板（现价/关键位/VWAP/判断/下次段时间）+ 分析心跳文件规矩 +「精简 ≠ 不输出」衔接条款，SKILL.md 监控循环步骤同步带指针；③ 分析心跳文件 tmp/analysis_beat_{date}_{mode}.csv（AI 每段分析时 echo 追加）+ monitor_watcher.py 第四检查（采样在跑但心跳停更 >180 秒 → 空转系统通知；无心跳文件且采样已跑 ≥180 秒 → 从未分析警报；「采样在跑 → 一律不报」的空转盲区已补上）；④ monitor_guard.py 空转硬门（盘中重启密采样段时心跳停更 >180 秒 / 零心跳且采样 ≥180 秒 → PreToolUse exit 2 阻断，逼 AI 先补判断 + 写心跳再重启；刚启动【无心跳且 log <3 分钟】放行不误伤）。**验证**：两脚本编译通过；futu_ws_segment 实跑 8 秒段三锚点全输出（VWAP 实查 163.50/163.33）；构造 12 分钟 log 实测两脚本重估提醒触发；guard 盘中模拟六场景（无心跳+采样10h 拦 / 心跳新鲜放 / 停更 400s 拦 / 非采样命令不拦 / && 连跑仍拦 / 刚启动放行）全符合；watcher 三场景（停更报警 / 新鲜不报 / 零心跳报警）全符合；顺手把两 hook 的「采样已跑时长」从 ctime 口径改为读 log 首行时刻（ctime 会因 rename 重置、实测踩坑）。/goal 命令维持不采用（见原条目）。

## 安全 / 凭证（2026-08-17 11:45）

- ✅**已完成**（完成：2026-08-17 11:45）**`~/.tigeropen/tiger_openapi_config.properties.bak` 旧副本清理**（记录：2026-08-17 10:20）：与主文件仅 account 字段不同（bak 是 7 位实盘号、主文件是 17 位模拟号），含私钥的冗余副本。API 密钥轮换（同日完成，旧密钥已作废）后旧 .bak 成纯泄漏面，删除闭环。
- ✅**已完成**（完成：2026-08-17 13:05）**实盘凭证平台层硬隔离（IP 白名单 + 模拟盘金丝雀）**（记录：2026-08-17 10:20，历经 11:45 / 12:05 / 12:35 三次方案演进）：老虎开发者级 IP 白名单上线——用户后台把 [订阅商] 6 节点入口 IP（[IP已移除];[IP已移除];[IP已移除];[IP已移除];[IP已移除];[IP已移除]）以 `;` 分隔填入白名单；SDK `apply_scope` 改 `all`（模拟盘也走代理，金丝雀设计——模拟盘断 = 提前暴露代理 / 白名单 / 节点问题）。**验证**：模拟盘走代理行情 + 资产查询通、实盘走代理资产查询通、**直连（出口 [IP已移除]）被拒「[白名单拒绝报文已移除]」**（负向验证白名单真实生效）。剩余长期运维（节点 IP 变动同步白名单）拆出为独立 🟢 条目留活跃。方案决策过程：一个开发者一套密钥无多 key、白名单开发者级不区分账户（按账户粒度隔离不存在）→ 用户拍板「模拟盘可折腾、跟着实盘走代理测链路 + 配白名单」。
- ✅**已更新**（更新：2026-08-17 13:35）**git 历史含模拟 17 位账户号明文（3 个 commit）——用户拍板不处理**（记录：2026-08-17 10:20）：commit 2514d76 / 74e0476 / c36ecb0 的 SKILL.md / CHANGELOG 里明文写过模拟账户号（当前 HEAD 已清除，历史永久可见、仓库 public）。**不处理的决定性理由（2026-08-17 与用户确认）**：git 历史只泄漏了模拟号这一个值，对实盘每条攻击路径都断——① 模拟号 ≠ 实盘号（各自独立分配、无推导关系）；② API 鉴权靠 tiger_id + RSA 私钥 + license（均从未入库），账户号只是「操作哪个账户」的参数、单独无鉴权能力；③ App 登录走手机号 + 密码，与账户号无关；④ IP 白名单已上线（同日），即便凭证全泄漏、非白名单出口调 API 直接被拒（实测验证）。改写历史（filter-repo + 强推）零实盘收益、成本是弃整个仓库历史。**边界（重新评估触发条件）**：「无威胁」的前提 = 私钥从未入库 + IP 白名单在位；若将来撤掉白名单且私钥又泄漏，应重新评估整体凭证暴露面（极端叠加场景，非当前状态）。

## 🟡 中危批量处理（2026-08-17 09:56，26 条全清 + 1 条更新计数留活跃）

- ✅**已完成**（完成：2026-08-17 09:56）**`check_order_filled_tiger` 把 PartiallyFilled 当全额成交**（记录：2026-08-16 14:27）：`"Filled" in status` 改严格 `status == "Filled"`（港美共用，美股别名 `check_order_filled_us` 同步生效）——部分成交继续轮询到超时、超时返回 status='PartiallyFilled'；新增 `get_order_filled_qty_tiger`（读 `Order.filled` 字段），开仓降档循环遇部分成交超时即停（已成交部分已建仓、不降档叠仓）、按实际成交量输出 `part_filled: true` + `quantity=实际成交量` + warning 提示复核持仓。轮询异常同修（见第 9 条）。
- ✅**已完成**（完成：2026-08-17 09:56）**显式传量绕过风控上限 + risk_fraction / f_max 硬编码**（记录：2026-08-16 14:27）：① 港美开仓脚本各加 `_risk_params_from_config()`——risk_fraction / f_max 从 skill config.json 读（缺失回退 0.02/0.10），config「修改本文件即可调参」契约对这两参数生效；② 各加 `_enforce_explicit_quantity_risk()`——显式传量路径同样校验 max_loss ≤ equity×f_max、市值 ≤ equity×max_leverage、整手，超限**拒绝下单**（ok:false，不再只 warning）。mock 实测：58,400 股×6 止损距超 f_max 拒、28.4M 市值超杠杆拒、整手量通过。
- ✅**已完成**（完成：2026-08-17 09:56）**下单提交异常盲重试 3 次**（记录：2026-08-16 14:27）：① 港美 6 个 submit 函数（open/mkt/stp × 港美）加 `_is_ambiguous_timeout_error` 判定——超时类模糊失败（请求可能已达券商）**不再自动重试**、直接抛出并注明「须先查当日订单确认、未确认前不得再下单」（mock 实测超时只尝试 1 次即抛）；② 新增 `has_active_open_order_tiger/_us` 重复下单防抖——降档循环每档下单前查同标的活动开仓方向委托单（排除止损单），存在即拒绝继续降档（防上一档超时实际在场导致双倍持仓）；③ 降档循环对模糊失败立即 break 不降档续下。
- ✅**已完成**（完成：2026-08-17 09:56）**开仓附加止损触发价不做 tick 取整**（记录：2026-08-16 14:27）：港美开仓脚本对 stop_loss 先 `round_to_tick_tiger`（价位表）/ `round_to_tick_us`（0.01）取整再提交，与 move_stop / close 脚本口径一致；发生取整改动时输出 `stop_loss_adjusted`（原值 → 新值）留痕。实测 485.5 → 485.4（(200,500] 区间 tick 0.2）。
- ✅**已完成**（完成：2026-08-17 09:56）**close_position 主路径只处理第一个活动止损单**（记录：2026-08-16 14:27）：港美 close 脚本加 `_cancel_residual_stops()`——主路径（modify 触发）与无止损直接 MO 路径平仓成交后都复查并撤掉该标的全部残留止损单（`cancel_all_stop_orders`，排除刚成交单），撤单失败不阻断输出（warning 提示手动处理），堵「兄弟止损单日后零持仓触发反向开仓」路径（与 move_stop 的 ≥2 清理对齐）。
- ✅**已完成**（完成：2026-08-17 09:56）**move_stop fallback 异常归因错误**（记录：2026-08-16 14:27）：港美 move_stop fallback 改分步报告——新 STP 提交成功 + 撤旧失败 = `ok: true` + `fallback_cancel_warning`（仓位已有新止损保护、提示手动撤旧），不再整体误报 ok:false 致 AI 误信「无止损」再补挂多重止损；新 STP 提交失败时报「旧止损仍在场、触发价未变」+ 超时模糊失败提示先查订单再补挂。
- ✅**已完成**（完成：2026-08-17 09:56）**港股脚本缺 TRAIL / LOSS 腿识别**（记录：2026-08-16 14:27）：港股三处对齐美股口径——`close_position_tiger._is_stop_order`、`move_stop_tiger._is_stop_order` 补 TRAIL + LOSS 附加腿；`cancel_all_stop_orders_tiger` 的 is_stop 判定从仅 STP 扩为 STP/STOP/TRAIL/LOSS 四类（App 手动挂的港股 TRAIL 单平仓后也能撤掉，不再残留）。
- ✅**已完成**（完成：2026-08-17 09:56）**`filled` 变量未初始化 UnboundLocalError**（记录：2026-08-16 14:27）：港美开仓降档循环前 `filled = False` 初始化——全部档位 submit 抛异常时循环耗尽后走干净的失败 JSON，不再 traceback。
- ✅**已完成**（完成：2026-08-17 09:56）**check_order_filled 轮询无异常捕获 + 平仓显式传参不校验持仓 + 持仓查询不过滤市场**（记录：2026-08-16 14:27）：① 轮询 get_orders 网络异常捕获——瞬态异常等下一轮、超时持续失败返回 `(False, None, 'poll_error:<err>', '')`（订单已在场、AI 拿得到干净输出不会误判重开仓）；② close 港美显式传参路径加持仓复核——持仓不存在 / 方向不匹配 / 超量均拒绝（防 direction=short 无空仓时 Buy MO 凭空开多）、小于持仓量输出 warning；③ `get_open_position_tiger` 只收港股持仓（symbol isdigit 过滤，美股裸代码排除）——港股一键平仓不再拿到 US.xxx。

### 监控链路（10 条）

- ✅**已完成**（完成：2026-08-17 09:56）**resume.py 美股断层检测跨午夜失明**（记录：2026-08-16 14:27）：① log glob 按市场对齐日期——港股 `monitor_log_HK_*_{北京日期}`、美股 `monitor_log_US_*_{北京 −12h}`（美东交易日口径，与 monitor_segment 命名一致），北京 00:00-04:00 美股后半场不再「今日无采样记录」假安心；美股 log 末行时分 > 当前时分时按跨午夜回退一天拼 datetime；② ring-log 末行校验当日——非当日响铃返回 None，周一开盘不再拿周五响铃报假断层。
- ✅**已完成**（完成：2026-08-17 09:56）**monitor_summary 买卖比行 r_last=None 崩溃**（记录：2026-08-16 14:27）：第 96 行补 `and r_last is not None` 双非 None 守卫（对齐量比行口径），r_last=None 时输出「N/A（log 无此字段或仅半段有值）」——上午 segment 写（ratio 有值）+ 下午 ws 接管写（ratio 空）的当日全貌摘要不再 TypeError。
- ✅**已完成**（完成：2026-08-17 09:56）**monitor_guard 美股时段硬编码夏令时 + STALE 分层**（记录：2026-08-16 14:27）：① `in_us_session` 改 zoneinfo 美东判定（对齐 preflight；实测断言：北京周六 00:30 → 判盘中（美东周五 12:30，旧代码失效 4h 的洞补上）、北京周一凌晨 → 判休市（旧误激活）、11 月切冬令时自动跟随）；② STALE_SECONDS 300 单层改分层——进程在 / log <120s = 在跑；120-300s = 「疑似断了」；>300s = 断（盲窗从最长 5 分钟收紧到 2 分钟）。
- ✅**已完成**（完成：2026-08-17 09:56）**断层哨兵无午休感知**（记录：2026-08-16 14:27）：monitor_segment 哨兵加港股午休感知——last 采样在 11:50-12:00、now 在 12:55-13:05（工作日非美股标的）判为正常午休 gap，输出「ℹ️ 午休间隔……非断层」降级提示，每个交易日 13:00 重启首段不再必报「疑似断层」（狼来了效应消除）。
- ✅**已完成**（完成：2026-08-17 09:56）**`stop_prices` 缓存只增不清**（记录：2026-08-16 14:27）：`stop_prices.update(fresh)` 改 `stop_prices = dict(fresh)`——fresh 非空即整体替换（止损单撤销/成交后不再显示旧触发价）；fresh 为空（查询失败）保留上一轮值兜底，与「每轮现查、不凭记忆」意图一致。
- ✅**已完成**（完成：2026-08-17 09:56）**watcher 无告警冷却 + 采样进程存活掩蔽一切会话**（记录：2026-08-16 14:27）：① notify 加 key 冷却——同一会话同一类中断 5 分钟内不重复报（stamp 文件 mtime 判定），launchd 每 10 秒触发不再每分钟 6 条通知风暴；② 掩蔽标注——「采样在跑」分支下对 state 明确异常（waiting_input/idle）或 jsonl 长期停更的注册会话发一条低频提示（key=masked_hint 冷却限制），不再静默吞掉被掩蔽的崩溃会话。
- ✅**已完成**（完成：2026-08-17 09:56）**bayes_evolution direction 解析只认 long/做多**（记录：2026-08-16 14:27）：改 import `review._direction`（十种别名口径），CSV 出现「多」「buy」等别名时不再静默当空头翻转 R 符号——两脚本解析统一。
- ✅**已完成**（完成：2026-08-17 09:56）**monitor_guard A4 连跑检测只数 monitor_segment**（记录：2026-08-16 14:27）：① 检测扩到 monitor_segment / ws_segment / futu_ws_segment 三脚本；② 改按 shell 串联操作符（&&/;/||/换行）切分子命令计数、只数「真正独立执行采样段」的子命令——`py_compile` / `cat` / `diff` 等工具引用脚本名不再误拦（实测：py_compile 两文件计数 0 放行、&& 连跑两段计数 2 阻断）。
- ✅**已完成**（完成：2026-08-17 09:56）**ws_segment 不认 --mode + log 日期口径 + 破位重复报 + 每 tick 一行**（记录：2026-08-16 14:27）：① 补 `--mode` 解析（parse_mode 复用，兼环境变量；--mode 先剥再解析 targets，不再被当标的）；② log 日期按市场对齐（美股 −12h 美东交易日，与 monitor_segment/summary 同口径）；③ 破位告警加 reported_up/dn 去重标记（同段不重复报，对齐「创新高才报」意图）；④ 按秒聚合——每秒只落一行（取该秒最后价），不再每 tick 一行虚增点数。
- ✅**已完成**（完成：2026-08-17 09:56）**monitor_segment 段结束统计空列表除零**（记录：2026-08-16 14:27）：ratio_avg / vr_avg 空列表跳过该指标（输出 N/A（ws log 无此列）），纯 ws 采样文件（ratio/vr 列全空）不再 ZeroDivisionError、不连坐剩余标的统计。

### 文档与口径（7 条）

- ✅**已完成**（完成：2026-08-17 09:56）**risk-management.md「权益更新」禁令与 signal 模式矛盾**（记录：2026-08-16 14:27）：该节补模式分叉声明——auto 模式账户 API 真实总资产（原禁令口径不变）；signal 模式 equity 走 equity-log 累加值（正当口径、非违规，无记录回退 initial_equity）；signal 的只读查询例外（费率算订单数允许、equity 口径不变）。
- ✅**已完成**（完成：2026-08-17 09:56）**港股脚本 ETF 白名单仅 1 成员 vs classify 30 个**（记录：2026-08-16 14:27）：`_HK_ETF_WHITELIST` 改 import `classify_hk_security.HKEX_ETF_WHITELIST`（单一权威源、classify 更新自动跟随；skill 根加 sys.path）——实测 30 个成员，02800 盈富判 etf（不再多收 0.1% 印花税错杀）、00700 腾讯仍 stock、07709 仍 etf。
- ✅**已完成**（完成：2026-08-17 09:56）**平台费查询失败计费方向与注释相反**（记录：2026-08-16 14:27）：`build_fee_ctx` 查询失败时 order_idx 置 1/2（按阶梯最高档 30/笔计）——真保守（原 None 不计平台费偏乐观，会把不达标交易算成达标，与 stderr「保守档」提示方向相反的矛盾消除）；`get_month_order_count_tiger` docstring「实盘账户」改「当前 config 账户（默认 paper）」与行为对齐。mock 实测失败路径 idx=1/2、最高档费用 > 低档。
- ✅**已完成**（完成：2026-08-17 09:56）**同一笔交易两套费率两套 R 并存**（记录：2026-08-16 14:27）：裁定写入 `auto-mode.md`（平仓赔率规范后新增「费用与成交价的唯一权威口径」块）——费用 / 成交价 / max_loss 唯一权威 = 脚本 JSON 输出 + fee_schedule.py 复算，动作记录直接抄脚本输出值、禁止手算；复盘统计一律以 review.py / fee_schedule 口径为准、源记录不一致处以复算值校正并注明。08-13 HKT 开仓条目同步修正：「实际 max_loss 80,000」改「预期 max_loss（参考价口径）」（实际 = 73,600，按成交价 379.6）；初始赔率「踩线达标」改如实表述（净 2.12 < 2.4 门槛未过闸、×0.5 折扣后 1.06 亦未过净口径闸），与 ET 副本对齐。
- ✅**已完成**（完成：2026-08-17 09:56）**08-11 MU 两笔记录数字混用**（记录：2026-08-16 14:27）：全部实算核对后修正——① 第 1 笔成交价统一 855.33（脚本 avg_fill_price 唯一权威；平仓表原 855.31 为转录误差、毛亏 −12,969 系按 855.31 算出，保留净盈亏 −13,804 并注明差异）；② 平仓表分母 14,895/14,865 两写统一 14,865（−13,804 ÷ 14,865 = −0.929R）；③ 第 2 笔 max_loss「止损距 4.255」无来源改实算 4.25 × 2394 = 10,175（原 10,186 保留在赔率算式注明、差异 0.002R 不影响结论）；④ equity 差 +815 标注（1,028,832 记账 vs 1,029,647 实查，疑实时净值随行情浮动、以各自时点实查为准）。
- ✅**已完成**（完成：2026-08-17 09:56）**既有存量待办（购买力校验）**（记录：2026-08-12 10:14）：① `trade_utils_tiger.py` 新增 `get_buying_power_tiger`——buying_power（get_assets().summary）× long_initial_margin（get_contract，实测 00100=0.75）÷ 参考价，实测字段全可用（bp 4,037,135 × 0.75 ÷ 380 = 7,968 股上限）；② 港美开仓脚本算出目标量后预检购买力上限、超限**下单前主动降档**（输出 `capped_by_buying_power: true` + 上限 + warning），降到 0 拒绝下单，被动降档保留兜底；③ `auto-mode.md`「下单失败处置」节补「购买力上限 = 单标的保证金率约束」条目；④ CHANGELOG 8-06 条目旧推断（边界 1,111 万～1,717 万待实测）标注 8-12 实测结论修正（保证金率 0.75、817.5 万 ÷ 0.75 ≈ 1,090 万 = 37,484 股）。
- ✅**已更新**（更新：2026-08-17 09:56）**既有存量待办（三阶段赔率标定）**（记录：2026-08-11 19:25）：**样本量实测未达 30、不重标定、条目留活跃**——以 reviews/2026-08-13-trades.csv（61 行合并）实数：×0.5 门槛执行（2026-08-06 起）后样本仅 **15 笔**（港股 9 + 美股 6），远 < 30。原「补 9 笔执行后样本」为 08-14 计数（当时 9 笔），现最新计数 15 笔。维持 ×0.5 系数不变，≥30 笔后按合并分布重标定（见 TODO.md 留存条目）。

## 🔴 高危批量处理（2026-08-16 15:42，11 条全清）

- ✅**已完成**（完成：2026-08-16 15:42）**开仓门槛 1.2 残留清理：统一为「净赔率 ≥ 2.4」单一门槛**（记录：2026-08-16 14:27）：9 处现行规则里的旧「≥1.2」全部改为 2.4——`SKILL.md` 开盘观察期条款（EV ≥ 2.4）、`references/auto-mode.md` 开仓定义 ⑧ 与开仓表格、`references/signal-mode.md` 开仓信号表格、`references/trading-strategy.md` 五处（止损空间注、开仓两道闸、赔率三阶段公式、止盈可达性两处、停止盯盘前注）、`references/review-and-evaluation.md` 预估净胜赔率公式。历史教训 / 标定背景叙述里的「1.2」（带日期的事故记录、「12 笔初始 ≥1.2 样本」等）按约定保留不动。
- ✅**已完成**（完成：2026-08-16 15:42）**删除「降频」决策树残留**（记录：2026-08-16 14:27）：`SKILL.md` 与 `references/auto-mode.md` 的「无机会时自主决策树」原 ②「降频（整个市场无活跃机会时放宽采样间隔）」改为「② 换标的不降频（保持 40 秒密采样、持续刷 hot_list，绝无降频死等合法出口）」，决策列表里的「降频」字样同步移除——与硬性护栏第 8 条、`references/monitoring.md`「降频出口已于 2026-08-04 撤销」不再矛盾。
- ✅**已完成**（完成：2026-08-16 15:42）**close_position_tiger.py 修 --account 空格形式被静默丢弃**（记录：2026-08-16 14:27）：`_parse_args` 增加 `expect_account` 捕获状态——`--account live`（空格形式）的值现在真正赋给 account（原实现只跳过值、从未赋值，切实盘平仓会静默操作默认模拟账户）。三种传参形式（空格 / 等号 / 缺省）+ 缺值边界单测通过，与 open/move 脚本口径一致。
- ✅**已完成**（完成：2026-08-16 15:42）**cancel_all_stop_orders_tiger 枚举裸 str() 比较失效**（记录：2026-08-16 14:27）：实测老虎 SDK（tigeropen 3.6.0）`OrderType` / `OrderStatus` 均为普通 Enum——`str(OrderType.STP)` 得 `'OrderType.STP'`，原 `str(order_type) == "STP"` 永远 False（独立 STP 单撤不掉），且 status 终结过滤同样失效。status / order_type 统一改为 `.value` 优先、str 兜底（枚举 / 纯串两形态六组合模拟验证全对，与 `_is_stop_order` 口径一致）。
- ✅**已完成**（完成：2026-08-16 15:42）**get_open_position_tiger side 张冠李戴**（记录：2026-08-16 14:27）：symbol 分支 `_, p, qty = matches[0]` 改为 `s, p, qty = matches[0]`、返回 `"side": s`——目标持仓自己的 side 不再被收集循环残留值覆盖（多空混持时一键平仓不再方向反做加倍）。
- ✅**已完成**（完成：2026-08-16 15:42）**美股开仓 quantity=0 自动算仓位 ZeroDivisionError**（记录：2026-08-16 14:27）：`open_position_tiger_us.py` 补 `if quantity > 0 else None` 守卫（对齐港股版）——「数量传 0 = 自动算仓位」的文档化主用法在美股恢复可用，价格范围检查回退旧百分比口径、算出仓位后照常复核。
- ✅**已完成**（完成：2026-08-16 15:42）**开仓 / 移损缺「止损价方向正确性」硬校验**（记录：2026-08-16 14:27）：① `_net_odds` 止损距 ≤0 从返回 inf 改为 raise ValueError（方向错以最诱人形态放行 → 改为硬报错）；② `check_price_in_range` 捕获该错误、odds_at_current 记 −inf 且 in_range 强制 False（现价漂过止损侧 = 拒绝开仓）；③ 港股 / 美股开仓脚本各加「做多 stop < entry_ref、做空 stop > entry_ref」硬校验；④ 港股 / 美股 move_stop 各加「新止损价未穿越现价」校验（做多 ≥ 现价 / 做空 ≤ 现价即拒绝并提示用 close_position 主动平仓），桩测试三分支（做多穿越拒 / 做空穿越拒 / 正常移损过）验证通过。
- ✅**已完成**（完成：2026-08-16 15:42）**futu_ws_segment.py 写 log 列错位一列**（记录：2026-08-16 14:27）：写行 last 后补齐 4 个空位（11 字段对齐 11 列表头）；对照确认 ws_segment.py 写行本就正确。已污染数据（比 TODO 记录的 08-14 更多——08-07 / 08-10 / 08-11 / 08-14 共 18 个文件、97,835 行 10 字段错位行）先备份到 `tmp/log_fix_backup_20260816/` 再按错位映射无损重排（原 vr 列位置的真 high、原 high 列位置的真 low 各归其位），全部行复验 11 字段、抽样语义核对正确。
- ✅**已完成**（完成：2026-08-16 15:42）**monitor_summary.py 额增速系统性高估 10 倍**（记录：2026-08-16 14:27）：额增速换算不再假设「每点 = 1 秒」，改用 turnover 序列自身首末时间戳推算平均采样间隔（秒/点）、窗口时长 = 点数 × 间隔 ÷ 60；混合数据源文件（ws 每秒行与 segment 10 秒行交错）按 turnover 自身间隔而非全文件平均推算。实算验证：10 秒采样 5 分钟 2 亿增量 → 0.40 亿/分（旧代码 4.00）；08-14 真实文件复核近 30 点窗口真实时长 16 分钟、均速 0.331 亿/分。输出行标注窗口分钟数与采样间隔。
- ✅**已完成**（完成：2026-08-16 15:42）**08-13 动作时间倒流 + 同笔交易双写 + equity-log 漏记两笔美股 + log_action.sh 根治**（记录：2026-08-16 14:27）：① 08-13 HKT 两条动作时间戳按表格内真实时间改正（10:20:54 / 10:31:07）并注明修正原因；② 08-13 ET 文件里的港股双写副本加迁移说明（权威记录在 HKT、复盘只认 HKT 副本、本副本不计入——按「文件操作慎用删除」规则保留留痕不删）；③ equity-log 补记 08-05 NVDA（[盈亏值已移除] HKD = −300.86 USD × 7.80）与 08-06 MU（[盈亏值已移除] HKD = −298 USD × 7.80）两笔漏记（汇率 7.80 与既有 08-03 NVDA 行口径一致），下游 equity_after 连锁更新、08-07 注资行 P 值相应调整保链条连续（定值结果 408640 不变）、全链连续性验证 0 问题；胜宏 R 值改为实算 3.42（14,238÷4,160，原记 3.71 无出处、修正留痕）；④ **根治**：`log_action.sh` market / mode 两参数改为必填（缺参报错退出、去掉默认值向后兼容——默认值已两次诱发事故），沙箱测试缺参 / 非法值 / 正常路径全过。注：TODO 原记「两笔合计约 −6,076 HKD」无法从复盘净亏口径复算（−598.86 USD ≈ −4,671 HKD @7.80），按实算为准。
- ✅**已完成**（完成：2026-08-16 15:42）**08-13 信号文件预估 max_loss 自相矛盾**（记录：2026-08-16 14:27）：预估 max_loss 按实算改正为 HK$1,705（1550 股 × 止损距 1.1 = 参考价 94.6 − 止损 93.5；原记 2,325 用了无来源的止损距 1.5）；移损行「+0.4 利润 ≈ +0.4R 保本」改正为「+0.4 利润/股 ≈ +620 HKD ≈ +0.57R 保本」（R = 620 ÷ 实际 max_loss 1,085；原表述把每股利润当 R 值）。两处均注明「2026-08-16 修」留痕。

## 交易 / 持仓处置

- ✅**已完成**（完成：2026-08-03 11:03）**关闭 MU 误开多头仓位**（记录：2026-08-01 22:54）：2026-07-31 smart_order 市价单被拒却误报成交 → close_position 反向买入开多 158 股 @ 828.575 USD（实测 2026-08-01 持仓确认）。计划下一个美股开盘（21:30 HKT）用 `close_position.py`（一键平仓）平掉，开盘前重新 `get_open_position` 核实持仓再执行。

## 代码 / 机制

- ✅**已完成**（完成：2026-08-12 11:05）**Watcher 注册与检查的市场范围错配根治 + 判定逻辑重构为「采样在跑就不报」**（记录：2026-08-12 10:26）：2026-08-12 上午港股盯盘中 watcher 一直响，原待办判定根因 = 美股会话 `6ede8d93` 残留在注册列表没注销 + 注册源头不分市场（preflight 无条件全局注册、watcher 只港股检查）。**排查中定位到更深一层根因（原待办未覆盖）**：即便只有港股会话、窗口显示 Running、采样在跑时 watcher 也可能误报——旧 2026-08-11 逻辑只认「jsonl 停更 > 50s」，但盯盘 Running 时 AI 回合卡在采样脚本 Bash 调用里、采样段 40s 期间 jsonl 完全不更新，50s 阈值踩在采样段尾端（40s + 开销），某段跑到 51s 即误报（代码注释自己也承认「正常无输出间隔最少 ≈ 40s」、阈值压刀刃）。**用户立新逻辑：盯盘窗口在 Running 且后台采样脚本在正常采样 → 不报，两者都不活跃才报**。**落地三件事（原待办根治点 + 用户新逻辑一并完成）**：
  1. **watcher 判定逻辑重构（用户新逻辑落地，根治主因）**：[`monitor_watcher.py`](.claude/hooks/monitor_watcher.py) `main` 改三层判定——① 死会话自动剔除（jsonl 停更 > 30 分钟 = 已结束，自动从注册列表移除、不再报）；② 新增 `sampling_running()` 用 `pgrep -f` 查 `monitor_segment.py` / `ws_segment.py` / `futu_ws_segment.py` 任一存活即判正常、本轮一律不报（采样进程是「在正常运行」最稳铁证，绕开「采样期间 jsonl 停更」固有盲区）；③ 采样没在跑 + jsonl 停更 > 90s 才报中断（`STALE_SECONDS` 50→90s，采样在跑已盖住采样段、本阈值只在分析阶段生效，不再踩刀刃）。
  2. **preflight 注册源头收窄为仅港股（原待办根治点）**：[`preflight.py`](.claude/skills/trade/scripts/preflight.py) `_register_monitor_session()` 加港股盘中判断（周一至周五 09:00-12:00 / 13:00-16:00，09:00 起覆盖盘前预热），美股 / 夜间 / 盘外跳过注册，从根上杜绝美股 session 进注册列表。
  3. **回写文档**：[`references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「多层防护」⑪ 更新为 2026-08-12 新判定（采样在跑就不报 + 死会话剔除 + 仅港股注册）；[`monitor_watcher.py`](.claude/hooks/monitor_watcher.py) docstring、[`plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) 注释、[`monitor_register.sh`](.claude/skills/trade/scripts/monitor_register.sh) 注释、[`SKILL.md`](.claude/skills/trade/SKILL.md) 停盯动作同步更新；[`CHANGELOG.md`](CHANGELOG.md) 记本次根因 + 修复。已重新部署 plist + reload，实测盘中采样在跑时 watcher 在判定第②步 return、不报。

  **2026-08-12 同日再次升级（第③步从阈值改为读 Running 标签 state 文件）**：用户指出第③步「采样没在跑 + jsonl 停更 > 90s」仍是阈值猜测，应直接读会话 Running 标签判定。查证发现补丁 002 的 Running 标签状态（session 对象 busy/pendingInput）只在 extension.js 进程内存、不写盘、外部进程读不到。解法：跨项目给 PatchClaudeAgent 新增 patch 012（session-state-to-file）——给 extension.js 的 `updateSessionState` 注入写盘（每次 state 变化写 `~/.claude/session_running/<sid>.txt`），watcher 读它 = 读会话 Running 标签的地面真相。watcher 第③步改为：running/thinking 不报、waiting_input/idle 报中断、state 文件无（补丁 012 未生效）fallback 到原 jsonl 判定（过渡期兼容）。patch 012 已应用到本机 2.1.226，待盯盘会话重载扩展后 state 写盘生效。详见 CHANGELOG 2026-08-12 第二条。

- ✅**已完成**（完成：2026-08-11 19:25）**00100 计划仓位下达失败排查（auto 模式，2026-08-06 复盘发现）**（记录：2026-08-06 20:42）：8-06 港股 MINIMAX-W 做多计划仓位 58,400 股无法一次下达——58,400 空仓时被拒（原因未明）、500/200/120 股因「cross-trading with your pending sell order」被拒（券商端禁止挂止损单后加仓）、最终只成交 10,000 股（max_loss 56,000 仅单笔预算 163,507 的 34%，收益被仓位缩水拖累）。待办：① 查明 58,400 单笔被拒的真实原因（单笔上限？资金？）；② 设计「挂单后加仓」的合规路径（先撤附加止损再加仓再补止损？或分批下达？）；③ 完善下单失败后的仓位调整流程（当前只试到 10,000 即停，未二分穷尽可下上限）。**完成说明（2026-08-11）**：① 查老虎当日订单历史确认——58,400 两次尝试均 Invalid 且 reason 为通用文案（SDK 对被拒订单无具体原因）、空仓时被拒排除 cross-trading；结合同日 01888（名义 ≈1,111 万 HKD、净值 781 万）成功 vs 58,400（名义 ≈1,717 万 HKD、净值 817 万）失败，推算最可能为**单笔名义金额 / 购买力限制**（边界 1,111 万～1,717 万 HKD 之间，确切上限待盘中实测）；② `auto-mode.md` 新增「下单失败处置」节：cross-trading 处置（先撤旧止损再开仓）+ 分批建仓路径（撤旧止损 → 第二批开仓不带腿 → 补挂一张独立 STP 覆盖总仓，两步都在开仓决策时确定、与「禁止加仓减仓」护栏边界写清）；③ `check_order_filled_tiger` 返回 4 元组输出 reason（Order.reason 字段，具体原因不再丢失）、港股美股开仓脚本加**失败自动降档重试**（目标量被拒按降档序列逐次减半取整手重试到可下上限，降档成交输出 downscaled + target_quantity + failed_attempts + warning；cross-trading 与挂起超时两种失败不降档）；降档序列单测 + 三场景模拟测试 + 8 脚本编译全过。详见 CHANGELOG 2026-08-11。**2026-08-11 补充（用户纠正，本条目部分决定作废）**：① cross-trading 归因修正——开仓 = 空仓建仓、与已有止损单无关，reason 实为账户已有同标的**未成交委托单**（之前下单残留），处置改为改单或撤销该挂单后重新挂单；② **分批建仓路径移除**——分批补足 = 同一标的先后两笔同向开仓 = 加仓、违反「禁止加仓减仓」护栏，一次性开仓设止损即可，降档缩水的仓位接受不补足（auto-mode.md / CHANGELOG 8-11 条目已同步修正）。

- ✅**已完成**（完成：2026-08-02 18:59）**`load_equity` 改用账户 API 取真实总资产**（记录：2026-08-01 22:54）：`trade_utils.load_equity`（open_position 自动算仓位用）仍读 `signals/equity-log.csv`（旧信号模式累加值、不真实）；应改为像 `preflight.py` / `resume.py` 那样从长桥 `account_balance().net_assets` 取（2026-07-31 已修 preflight/resume，open_position 的自动仓位路径漏改）。

- ✅**已更新**（更新：2026-08-05 16:52）**实盘实测 `submit_order_with_stop`（开仓附加订单）**（记录：2026-08-01 22:54）：该函数（REST 主单 LO + attached_params STOP_LOSS）签名逻辑已对照文档核对、REST 签名复用已验证的可工作实现，但尚未端到端实跑过一笔真实（paper）开仓。计划在美股开盘时用 paper 账户实测一笔小仓位开仓（确认主单成交 + 附加止损进入监控状态 + 成交回查拿到 fill_price），测完即平。**2026-08-05 更新**：长桥已放弃、`submit_order_with_stop`（长桥 trade_utils.py）已随长桥脚本删除，不再实测；老虎开仓附加腿 OrderLeg('LOSS') 已在 2026-08-03 开盘实测通过（见下方「开盘实测港股老虎三动作全链路」完成条目）。

- ✅**已完成**（完成：2026-08-03 10:58）**港股长桥三动作开盘实测**（记录：2026-08-01 22:54）：`open_position_hk.py` / `close_position_hk.py` / `move_stop_hk.py` 已建（解耦独立一套）+ 盘后只读功能实测通过；下单链路（开仓 LO+附加MIT、移损先新增后撤旧、平仓 MO）需港股开盘时用 paper 账户端到端实测一笔小仓位。仅当用户特别说明用长桥时执行。

- ✅**已更新**（更新：2026-08-05 16:52）**美股 open_position 端到端实测**（记录：2026-08-01 22:54）：美股 symbol bug 修复后（to_lb_symbol），`open_position.py` 全链路（get_quote→submit_order_with_stop→成交回查）需开盘时实测一笔小仓位确认（此前因 symbol bug 未成功跑过）。**2026-08-05 更新**：长桥已放弃、原对象（美股长桥 open_position.py）已删除；美股默认账户改老虎，最新意图见下方新待办。

- ✅**已完成**（完成：2026-08-02 18:59）**港股 lot_size 从行情接口取真实每手股数（当前硬编码 100）**（记录：2026-08-02 13:54）：`open_position.py` 自动算仓位时港股 lot_size 硬编码 100（line 130-132），应改为从行情接口取真实每手股数（原代码注释提及富途 snapshot；港股转老虎后需确认实际行情源与取数字段）。

- ✅**已完成**（完成：2026-08-05 16:52）**`open_position.py` 自动仓位币种换算 bug**（记录：2026-08-04 01:12）：[open_position.py:136-141](.claude/skills/trade/scripts/open_position.py#L136-L141) quantity=0 自动算仓位时，`load_equity` 返回 HKD equity（账户基础币种 HKD、net_assets 390329）直接除 USD 止损距算仓位——单位错位，做空 MU 止损距 8 USD 会算出 ~975 股、实际 max_loss 7800 USD（≈6 万 HKD）是预算 B 的 8 倍且超 f_max。2026-08-04 实测发现、手动传 quantity 绕过。修法：美股（symbol US.xxx）时把 equity 按汇率换算成 USD 再算仓位（或把止损距换算成 HKD），需查实时 USD/HKD 汇率（富途 US.USDHKD 不认，待找正确代码或数据源）。**完成方式（2026-08-05）**：equity 口径修复（`load_equity_tiger` 加 `base_currency` 参数、港股 'HKD' 取 `get_prime_assets` 折算净值；美股 `load_equity_us` 直取 USD 净值）——美股老虎开仓脚本自动算仓位用 USD 净值 ÷ USD 止损距、单位一致，错位 bug 消除；原对象（长桥 open_position.py）已随长桥删除。

- ✅**已更新**（更新：2026-08-05 16:52）**`close_position.py` 不支持 `--mode` 参数（与 `open_position.py` 不一致）**（记录：2026-08-04 01:12）：[close_position.py:46-48](.claude/skills/trade/scripts/close_position.py#L46-L48) 参数是位置参数 `[symbol] [direction] [quantity]`，不解析 `--mode`——平仓时传 `close_position.py US.MU --mode auto` 会把 `auto` 当 quantity 报 ValueError，耽误平仓约 1 分钟（2026-08-03 MU 空单教训：MU 从 787 冲到 793 触发止损）。`open_position.py` 用 `parse_mode()` 支持 `--mode`，`close_position` 应一致。注意：close_position 默认用 `load_config()`（env-paper 模拟盘）、不连账户 equity，`--mode` 对它本无实际用途——但参数接口应一致避免误用（或文档明确不含 --mode）。**2026-08-05 更新**：长桥已放弃、原对象（长桥 close_position.py）已删除；同款问题存在于老虎平仓脚本（`close_position_tiger.py` / `close_position_tiger_us.py` 均不解析 --mode），最新意图见下方新待办。

- ✅**已更新**（更新：2026-08-05 16:52）**附加止损单平仓后撤不掉（反向开仓隐患）**（记录：2026-08-04 01:12）：开仓附加 STOP_LOSS MIT 止损单，平仓后 `close_position` 的 `cancel_all_stop_orders` 返回 0（附加子单不在 today_orders 主列表、按 trigger_price 筛选漏撤），SDK `cancel_order(子单oid)` + REST DELETE `/v1/trade/order` 均 601029「无法查询到订单」——附加子单不可单独撤。REST 查主单详情见附加止损 status=**VarietiesNotReported（品种未上报）**，疑似模拟盘附加止损未真正激活监控（与港股 NOT_USED 同义）；2026-08-04 MU 多单 816 未触发、持仓已净 0、无实际危害，但属工具缺陷（残留附加止损撤不掉、若激活触发会反向开仓）。修法：① 平仓后用 REST 查主单详情拿附加子单 oid + 状态、确认是否激活；② 若 VarietiesNotReported = 未激活、标注无危害；③ 若激活、找正确的撤单 API（研究长桥附加订单撤单接口）；④ `close_position` 平仓后强制核实附加止损状态。**2026-08-05 更新**：长桥已放弃、该缺陷随之消失无需后续工作——老虎体系下开仓附加腿落成的 STP 单可独立撤销（2026-08-03 实测）、平仓走「modify 止损触发价=现价」无 race 路径（2026-08-05），无残留止损反向开仓隐患。

- ✅**已完成**（完成：2026-08-05 16:30）**核实老虎 paper 账户币种口径（标 USD vs 港股 HKD）**（记录：2026-08-04 11:40）：auto 模式港股仓位计算用老虎账户净值（`get_assets().summary.net_liquidation` 返回 1,000,035、currency 字段标 USD），但港股标的止损距是 HKD 计价——8-3 起沿用「净值数字直接当 HKD 用」的保守口径（8-3 07709、8-4 阿里均按此算仓位，实际风险敞口被低估、仓位偏小）。需核实老虎 paper 账户基础币种真实语义：① 若确为 USD、需按 USD/HKD 汇率换算后算仓位（仓位应更大）；② 若 paper 账户内部 1:1 当 HKD 用（老虎 paper 不做真实汇率）、则当前保守口径符合实际。修法：查老虎 paper 账户文档或实测一笔港美股混合交易看净值变化，确定币种语义后回写 `trade_utils_tiger.load_equity_tiger` 注释与 auto-mode.md 币种口径说明。**2026-08-05 实测结论**（第 ① 种成立）：老虎 paper 单账户多币种现金段（`get_prime_assets` 的 `currency_assets` = USD 1,000,000 / HKD -24,058.59（港股交易后 HKD 现金为负）/ CNH 0，USDHKD ≈ 7.8436 实时），**`get_prime_assets(base_currency='HKD')` 直接返回 HKD 计价的证券段净值**（实测 7,819,536.41 = 996,932.71 USD × 7.8436，全账户折算、汇率来自老虎自身）；`load_equity_tiger` 已加 `base_currency` 参数按此实现（港股 'HKD' / 美股 'USD'），preflight / resume / 开仓脚本自动算仓全部生效，注释与 CHANGELOG 2026-08-05「equity 口径修复」已回写。

- ✅**已完成**（完成：2026-08-05 20:59）**monitor_segment.py 日期处理 bug（date_str 未定义 + log 文件名日期跳变）**（记录：2026-08-04 11:40）：2026-08-04 港股盯盘实测两个相关症状：① 每段采样输出「重估提醒检查 err:name 'date_str' is not defined」——`date_str` 变量未定义致「每 10 分钟方向重估提醒」功能失效（虽偶有段正常触发，但多数报错，AI 改靠手动 10/20/30…分钟重估兜底）；② log 文件名日期跳变——前 3 段采样写 `monitor_log_HK_*_20260803.csv`（昨日日期）、第 4 段起突然改写 `20260804.csv`（今日），疑似 `date_str` / 文件名日期的计算有竞态或初始化 bug。影响：自动重估提醒不可靠、monitor_summary 读 log 累积统计可能跨日污染（「完整采样统计」读到昨日点数、今日「点数=1844」即此症状）。修法：查 monitor_segment.py 的 `date_str` 定义与 log 文件名日期生成逻辑，修复未定义 + 日期跳变。**完成说明（2026-08-05 20:59 确认闭环）**：修复早已在提交 c58203f（2026-08-04 16:49「Add monitor-sampling guard hooks」）随「log 按市场交易日命名」改造落地——`trading_date_str()` 按市场区分（港股北京日期 / 美股美东交易日 -12h）、`date_str = trading_date_str(syms[0])` 正确定义、重估提醒检查与文件名共用同一日期；本条目记录后未及时闭环，本次核查工作区版本（date_str 定义在 [monitor_segment.py:155](.claude/skills/trade/scripts/monitor_segment.py#L155)、log 文件名 [monitor_segment.py:162](.claude/skills/trade/scripts/monitor_segment.py#L162)、编译通过）确认症状两处均已修复，无需新改动。

- ✅**已完成**（完成：2026-08-05 20:59）**老虎平仓脚本不支持 `--mode`（长桥 close_position 同款问题转移）**（记录：2026-08-05 16:52）：`close_position_tiger.py` / `close_position_tiger_us.py` 参数为位置参数 `[symbol] [direction] [quantity]`、不解析 `--mode`——误传 `--mode auto` 会把 `auto` 当 quantity 报 ValueError 耽误平仓（2026-08-03 MU 空单教训同款）。`--mode` 对平仓脚本本无实际用途（不连账户 equity），修法为过滤 --mode 参数或文档明确不支持。**完成说明（2026-08-05 20:59）**：两个平仓脚本加 `_parse_args()` 过滤 `--mode`（含 `--mode auto` 后跟值、`--mode=xxx` 两种写法），main() 改从过滤后参数解析；6 个单测用例全过（含 --mode 在前 / 在后 / --mode= 形式）。

- ✅**已完成**（完成：2026-08-05 20:59）**bayes_evolution.py 的 trades.csv 路径与输出文件名硬编码，每次复盘需手动改**（记录：2026-08-03 17:13）：脚本第 32 行硬编码读取 `reviews/2026-08-03-trades.csv`、7 张图输出命名 `2026-08-03-*`，每次复盘都要手动 sed 改两处（本次复盘即手动改过）。建议改为支持命令行参数（如 `--date`）或自动找 `reviews/` 下最新的 trades.csv，把日期抽成脚本头部变量，避免每次手改。**完成说明（2026-08-05 20:59）**：加 `_resolve_date()`——`--date YYYY-MM-DD` 指定，不传自动找 `reviews/` 下最新 `*-trades.csv`（按文件名日期取最大）；日期抽成 `DATE` / `CSV_PATH` 变量、7 处输出文件名全部参数化；`--date 2026-08-04` 与自动找最新两条路径实跑通过（7 张图正常生成）。

- ✅**已完成**（完成：2026-08-05 20:59）**复盘时平仓原生记录 mfe_R / mae_R（过程指标可算）**（记录：2026-08-03 17:13）：复盘过程指标（MAE / MFE / 回吐 / 锁利效率 η）需要每笔持仓期间的 high/low，现有 trades.csv 未提供 raw_high/raw_low、历次复盘全部跳过。推荐做法：平仓时原生记录 `mfe_R` / `mae_R` 两个字段（盯盘时本就在采样价格、零额外成本），复盘直接读、不必每次回拉历史 K（见 review-and-evaluation.md「⚠️ 数据约束」）。**完成说明（2026-08-05 20:59）**：`trade_utils_tiger.calc_position_extremes_tiger`（美股 `trade_utils_tiger_us` 别名复用）从盯盘 log 取当日采样极值近似持仓期间 high/low（按市场交易日 + 模式匹配最新日期文件）；两个平仓脚本成交后 `_attach_process_metrics()` 自动输出 `mfe_R` / `mae_R`（+ entry_price / raw_high / raw_low，口径与 review.py 一致：正 MFE_R / 负 MAE_R，止损距 = 开仓价 − 活动止损触发价毛值）；无盯盘 log / 缺止损价则不加字段（复盘按缺失处理）。配套文档：auto-mode.md 平仓动作表格加「过程指标」行 + 引用说明、review-and-evaluation.md「⚠️ 数据约束」更新为「方案 b 已落地」。单测通过（临时 log 返回极值正确、不存在标的返回 None）。

- ✅**已完成**（完成：2026-08-05 23:17）**盘前→开盘唤醒衔接机制缺失（AI 不会自动在开盘时醒来盯盘）**（记录：2026-08-05 09:48）：2026-08-05 港股盯盘，盘前 09:19 用户启动 → 09:24 盘前准备完成（preflight + watcher 修复 + hot_list + 方向研判）回合结束 → **09:30 开盘 AI 没有自动醒来盯盘**，直到 09:40 用户手动提醒。根因：AI 是请求-响应式、回合结束就静止，唤醒源只有 ① 用户新消息 ② 后台 task-notification ③ 主动定的定时唤醒；盘前没启动 monitor_segment（段结束通知是盘中标准唤醒源但盘前没用它）+ 没安排定时唤醒 → 09:30 无人叫醒 AI。caffeinate 只防系统睡眠、watcher 只发 macOS 通知给用户，都不唤醒 AI session。**修法（两选一，待定）**：① 盘前准备完成若距开盘有时间差，提前启动 `monitor_segment.py` 盯候选标的（段结束 task-notification 从盘前持续驱动 AI、跨开盘自然进盘中——最贴合现有机制、确定可行；代价是盘前几段采到静态价无意义，但唤醒价值大于此）；或 ② 用 `ScheduleWakeup` 定在开盘前 ~30 秒唤醒（待验证：该工具描述绑定 /loop dynamic mode，非 /loop 上下文能否调用未知，需实测）。方案择一写进 `monitoring.md` 盯盘启动段 + SKILL.md 启动序列（「盯盘靠用户指令启动、AI 不自主启动」要补「但盘前准备完成后须安排开盘唤醒」）。改 skill 须先读 skill-creator。触发：用户「为什么没准时唤醒？排查原因，解决问题」。**完成说明（2026-08-05 23:17）**：修法两选一实测定案——方案②（ScheduleWakeup 定时唤醒）**实测不可用**（非 /loop 上下文调用直接报「Wakeup not scheduled. Either the /loop dynamic runtime gate is off or the loop reached its maximum duration」），方案①（盘前提前启动 monitor_segment）落地为唯一方案：monitoring.md「盘前启动只预热」节新增规则（盘前启动盯盘、准备完成距开盘 ≤ 15 分钟立即提前启动 monitor_segment 盯候选标的、段结束 task-notification 从盘前持续驱动 AI 跨开盘进盘中、盘前段 AI 只重启下一段 + 一句话汇报）+ SKILL.md 硬性护栏第 7 条同步；monitoring.md「采样链防中断」备份唤醒条更新为「ScheduleWakeup 已实测不可用、备份唤醒仅存于 /loop 场景、普通盯盘回退核心靠 AI 自检 + 大动作复查」、盯盘启动清单不再尝试。改 skill 已先读 skill-creator。

- ✅**已完成**（完成：2026-08-05 23:17）**美股老虎 `open_position_tiger_us` 下单链路待盘中实测**（记录：2026-08-05 16:52）：2026-08-05 美股默认账户改老虎（长桥已放弃），`open_position_tiger_us.py` / `close_position_tiger_us.py` / `move_stop_tiger_us.py` 下单链路（开仓 LMT+附加止损、平仓「modify 止损触发价=现价」、移损 modify aux_price）待美股盘中（晚上 21:30 开盘）用 paper 账户端到端实测一笔小仓位。**完成说明（2026-08-05 23:17）**：美股盘中（美东约 11:1x）SPY 2 股小仓位端到端实测三动作全通过——开仓 LMT Filled @773.68 + 附加止损腿 OrderLeg('LOSS') 激活为独立 STP、移损 modify aux_price 770.61→771.71 verified=true、平仓 modify 触发价=现价、止损单触发 Sell MO Filled @773.44、持仓归零无残留止损单（平仓过程指标 mfe_R/mae_R 正常输出）。**实测发现并修复 1 个 bug**：`get_quote_us` 用老虎 `get_stock_briefs` 取美股行情报 code=4 msg=4000 permission denied(US market)——老虎账户美股无行情权限（account-tools.md「数据源分工总则」早已记录「美股只能靠富途单源」）→ 改**富途 OpenD 单源**。4 个脚本 docstring「⏳ 待实测」更新为「✅ 实测通过 + 行情数据源修复」+ auto-mode.md 美股段落实测状态同步。纯基础设施验证（开仓即平、无策略意义）、按 8-03 实测惯例不入 actions/。

- ✅**已更新**（更新：2026-08-11 18:50）**信号模式支持飞书 CLI 发信号 + 多会话并发盯盘**（记录：2026-08-11 13:21）：**本条已加入 win 分支的 TODO.md——此功能在 win 分支开发，不在 main 分支开发；等 win 分支开发好再合并到 main 分支，合并后本条在 main 归档闭环（2026-08-11 转出）**。原待办内容：① 信号模式下通过飞书 CLI 把交易信号发到飞书（信号除当前会话内输出外，再推送飞书，提升信号可达性）；② 支持同时开多个信号模式盯盘会话（当前信号模式会话与盯盘会话的关系、并发时的信号归属 / 记录去重需设计）。具体方案（飞书 CLI 选型、多会话并发机制）待定。

## 港股老虎模拟账户三动作（默认账户，独立解耦一套）

> 港股默认老虎，要实现和长桥一样的三动作，与港股长桥（备选）+ 美股代码分开（分而治之，2026-08-01 用户立）。三动作已建好（2026-08-02）；2026-08-03 起全项目默认模式为 signal、auto 需用户特别说明才用（见下节）——本节的「港股默认老虎」指 auto 模式下的港股默认账户，仅在用户切 auto 时启用。

- ✅**已更新**（更新：2026-08-02 18:59）**研究老虎 SDK 港股细节**（记录：2026-08-01 22:54）（开盘 + paper 实测才能确认，盘后做不了）：
  - 配置加载：`get_client_config(props_path=...)` 失败，要研究老虎 paper 配置正确加载方式（`~/.tigeropen/tiger_openapi_config.properties` + 私钥）。
  - 港股 symbol 格式：`stock_contract` 接受 `02800`/`HK.02800`/`2800.HK`，但老虎 API 实际认哪个要实测（行情/下单）。
  - lot_size：老虎 `get_contract` 查字段；tick：`get_contract` 或港交所价位表。
  - 附加止损 `order_leg('LOSS', price)` 实测（主单 LMT + 附加止损，对应长桥 attached STOP_LOSS）。
  - 老虎下单/平仓/止损机制逐一实测，**不能从长桥外推**（verify-facts：券商行为只信直接实测）。
- ✅**已完成**（完成：2026-08-02 18:59）**建 `trade_utils_tiger.py`**（记录：2026-08-01 22:54）（老虎封装，自包含不依赖长桥/美股）：配置加载、港股 symbol/lot/tick、place_order（开仓 LMT+附加止损）、平仓 MO、移损止损单（先新增后撤旧）、查持仓/资产/订单、撤单、成交回查。
- ✅**已完成**（完成：2026-08-02 18:59）**建 `open_position_tiger.py` / `close_position_tiger.py` / `move_stop_tiger.py`**（记录：2026-08-01 22:54）（与规范一致：开仓 LO+附加止损、平仓 MO+撤止损、移损先新增后撤旧且量=持仓）。
- ✅**已完成**（完成：2026-08-03 10:47）**开盘实测港股老虎三动作全链路**（记录：2026-08-01 22:54）（paper 账户小仓位）。

- ✅**已更新**（更新：2026-08-02 19:06）**老虎 paper 接入：17 位模拟账户号 + 开盘实测三动作下单链路**（记录：2026-08-02 18:59）：2026-08-02 盘后已完成「研究老虎 SDK 港股细节」的可盘后部分（详见上条 `✅**已更新**` 标记与 CHANGELOG 2026-08-02 记录）：① 配置加载根因 = `get_client_config` 必须先传 private_key_path（None 直接 TypeError），正确姿势 `TigerOpenClientConfig(props_path=~/.tigeropen/)`（私钥自动从 private_key_pk1/pk8 读，实测加载成功）；② paper 判定 = account 为 17 位纯数字账户号即 is_paper=True、网关自动走 license-PAPER 域名（domain_conf 已含 TBNZ-PAPER，实测确认）；③ 港股 symbol 只认 5 位带前导 0 裸代码（'02800'/'00700' 实测 OK，HK.02800/2800.HK/700.HK 均拒）；④ lot_size / tick 从 `get_contract` 取（实测 02800=500、00700=100、tick_sizes 完整价位表）；⑤ TradeClient API 结构与只读链路（get_assets→summary.net_liquidation / get_positions / get_orders / cancel_order / create_order+place_order / OrderLeg('LOSS') 附加腿仅限价单支持）全部实测通过；`trade_utils_tiger.py` + 三动作脚本已建（下单链路标注待实测）。**剩余**：a) 用户提供 17 位 paper 模拟账户号写入 `~/.tigeropen/tiger_openapi_config.properties` 的 account 字段（当前是 7 位实盘号 <HK_LIVE_ACCOUNT>，且该实盘账户未开通交易/资产权限——get_assets 返回全 0）；b) 港股开盘时用 paper 账户实测三动作全链路（开仓 LMT+附加止损腿提交与激活、平仓 MKT、移损 STP 先新增后撤旧、成交回查），实测结果回写脚本与文档。

- ✅**已完成**（完成：2026-08-03 10:47）**老虎 paper 账户开盘实测三动作下单链路（账户号已接入，2026-08-02 19:06 完成接入）**（记录：2026-08-02 19:06）：17 位模拟账户号 `<HK_PAPER_ACCOUNT>`（从 git 历史 2514d76 恢复）已写入 `~/.tigeropen/tiger_openapi_config.properties` 的 account 字段（原 7 位实盘号备份在 `.bak`），实测：is_paper=True、网关自动走 sandbox 域名、`get_assets` 净值 1,000,007.56 USD（初始资金 $1,000,000）、`get_positions` 有 **2 笔历史持仓**（02800 盈富多 500 股 @26.3639、07709 三星 2x 杠杆空 200 股 @43.0638）——开盘实测时先核实这两笔怎么处置（平掉 / 保留）。**剩余**：港股开盘时用 paper 账户实测三动作全链路——开仓 LMT+附加止损腿（OrderLeg('LOSS')）提交与激活、平仓 MKT、移损 STP 先新增后撤旧、成交回查；实测结果回写 `trade_utils_tiger.py` 三脚本与 `auto-mode.md` 实测状态标注（实测前标注为「待实测、不得真实下单」）。

- ✅**已完成**（完成：2026-08-03 14:56）**修 `close_position_tiger.py` 平仓顺序：必须先撤止损单再平仓（老虎与长桥不同）**（记录：2026-08-03 14:47）：2026-08-03 午盘实测：持空仓（07709 38,500 股）时下 Buy 平仓单（MKT/LMT、38,500/10,000/100 股）全部被拒 EXPIRED，原因 `The order quantity you entered exceeds your current holdings`（持仓 salable_qty=0）；用户实测提示「老虎和长桥不一样，需要先撤销所有止损单才可以主动平仓」——**先 `cancel_all_stop_orders_tiger` 撤掉全部止损单，再下 Buy 市价单即立即成交**。现状脚本顺序是「先 MKT 平仓、成功后才撤止损」（close_position_tiger.py，沿长桥逻辑）——对老虎是错的，须改为「先撤止损 → 再平仓」，并回写 `auto-mode.md` 实测状态。**已修复**：close_position_tiger.py 改为先撤全部止损单再 MKT 平仓（语法验证通过），trade_utils_tiger.py / auto-mode.md 同步，CHANGELOG 2026-08-03 记录；该笔 07709 空单因基础设施问题作废未计入 actions/。

- ✅**已更新**（更新：2026-08-03 14:55）**老虎模拟盘 T+2 交收限制：当日开空当日不可平（salable=0）**（记录：2026-08-03 14:47）：**错误推断，用户 2026-08-03 14:55 纠正——不是 T+2**。平仓被拒的真实根因 = **挂着的止损单占用可平额度**：老虎把「有挂单（如开仓附加腿落成的 STP 止损单）的持仓」标记为不可平（salable=0 = 持仓 − 挂单数量），新 Buy 平仓单因「exceeds holdings」被拒；**先撤止损再平仓即立即成交**（2026-08-03 午盘实测验证：撤 3 笔止损单后 Buy MKT 38,500 立即 Filled）。旁证：7-31 开空 200 股时其 STP 被 REJECTED（没挂成止损），故 8-03 10:58 平它无阻碍；本笔 38,500 空挂有 STP 所以被拒。与 T+2 无关、当日开空当日可平（先撤止损即可）。修复已由上方「修 close_position_tiger.py 平仓顺序」条目覆盖（先撤止损 → 再平仓）。

## 默认模式 = 信号（signal），自动模式需特别说明（2026-08-03 用户立，取代「港股 default 从 signal 切 auto」）

- ✅**已更新**（更新：2026-08-02 14:14）**港股完全转自动交易**（记录：2026-08-01 22:54）：港股从信号模式（AI 发信号 `signals/`、手动执行）转自动交易（AI 调脚本下单、`actions/` 记动作）。**信号模式描述已随 2026-08-01 双模式重构处理**——signal 已作为独立可切换 reference（`references/signal-mode.md`）恢复、主 `SKILL.md` 改双模式（默认 auto + 可切 signal），不再是「要改掉的旧模式」。剩余待办：港股老虎三动作建好后把港股 default 切 auto（港股目前仍 signal——老虎三动作未建，见上方「港股老虎模拟账户三动作」节）。**依赖港股老虎三动作先建好**（否则港股 auto 文档 ahead of 代码）。

- ✅**已更新**（更新：2026-08-03 09:55）**港股 default 从 signal 切 auto（signal 模式保留，两者共存）**（记录：2026-08-02 14:14）：当前决定 = 信号模式与自动交易模式二者共存，不是放弃 signal 改纯 auto。港股目前 default 仍为 signal（AI 发信号 `signals/`、手动执行）；待港股老虎三动作建好后把港股 default 切 auto（AI 调脚本下单、`actions/` 记动作）。**signal 模式保留为可切换模式**——用户说「只发信号 / 手动执行」时仍切 signal；两种模式唯一区别 = 是否使用证券账户，交易策略完全通用（主 `SKILL.md` 已是双模式公共骨架 + `references/auto-mode.md` + `references/signal-mode.md`，2026-08-01 重构完成，此处不重复）。**依赖港股老虎三动作先建好**（否则港股 auto 文档 ahead of 代码）。

- ✅**已完成**（完成：2026-08-03 09:55）**默认模式改为信号（signal），自动交易（auto）需用户特别说明才用**（记录：2026-08-03 09:55）：2026-08-03 用户决定把全项目默认执行模式翻转为 signal（AI 只发信号、用户手动执行、不碰账户；auto 仅在用户特别说明「自动下单 / 自动交易 / auto」等时才启用），取代上方「港股 default 从 signal 切 auto」的方向（默认不是 auto，而是 signal）。已落地：SKILL.md（description / 模式判定 / 模式提醒顺序 / --mode 说明 / 当前阶段）、references/auto-mode.md 与 signal-mode.md（开头默认说明）、项目 .claude/CLAUDE.md、scripts/trade_utils.py 与 trade_utils_tiger.py 的 parse_mode() 与 load_equity 默认值（不传 --mode 默认 signal、equity 走 equity-log 不连账户），明细见 CHANGELOG 2026-08-03。港股老虎三动作（auto 模式下港股默认账户）照常保留，仅当用户切 auto 时使用。

## 交易规则 / 策略

- ✅**已完成**（完成：2026-08-11 19:25）**美股突破类入场加「量比达标」硬闸**（记录：2026-08-06 20:42）：8-06 复盘发现连续两笔 signal 美股「无量突破」失败——08-05 NVDA（量比 3.1，开仓 7 分钟后突破失败止损 −1.17R）、08-06 MU（量比 0.8，突破 926 后回踩 921 未守住止损 −1.13R），两笔开仓记录的风险区都自己标注了「突破量能不足」仍开仓。待办：在 `references/trading-strategy.md` 突破类入场判据中加量比门槛（如量比 < 1 不开突破单；量比 < 2 需其它确认叠加），防「明知无量仍开突破单」（与早期 SOXL 假突破教训同型）。**完成说明（2026-08-11）**：`trading-strategy.md`「量价正向确认」节后新增「突破类入场量比硬闸」小节——量比 < 1 不开突破单（无量硬闸）/ 1 ≤ 量比 < 2 须板块共振等确认叠加 / 量比 ≥ 2 按正常突破判据；明确「必要非充分」（NVDA 3.1 达标也失败——量比只过滤「明知无量仍开」，不保证突破必成，突破成败仍由形态 / 位置 / 板块共振决定）；三条边界（早盘量比天然偏高结合分时量能看 / 门槛按标的流动性参照 / 只约束突破类入场、回踩企稳类不受限）。详见 CHANGELOG 2026-08-11。

- ✅**已完成**（完成：2026-08-05 20:59）**把「多头趋势日 VWAP 企稳顺势做多」教训写进 trading-strategy.md**（记录：2026-08-04 00:11）：2026-08-03 美股盯盘，从美东 11:29（北京 23:29，板块 VWAP 企稳反弹、第 68 段）到 12:08（北京 00:08），SOXL/MU 在 VWAP 上方**震荡上行**（SOXL 112.11→114.18 +1.85%、MU 807.95→816.27 +1.03%，NVDA 持平 206-207 滞涨），AI **全程错过**、定性为「高位震荡无合格入场」。**根因（两条）**：① **判断失误**——把「VWAP 上方震荡上行」当成「高位震荡无机会」，只盯 NVDA 滞涨、用 NVDA 的「无突破」否决了 SOXL/MU 的顺势上行；② **入场条件太严**——只等「突破阻力（NVDA 207.33 / SOXL 116.81 / MU 828.44）」或「回踩 VWAP 企稳」两类信号，但 SOXL/MU 走的是「VWAP 上方震荡上行」（既没回踩 VWAP、也没突破阻力），正好卡在两个条件中间被漏。**与既有教训同根**：和「纪律性不追高错过 SOXL/MU 大涨」是同一根因——**没区分「多头趋势日顺势做多」与「普通追高」**。**规范应补（trading-strategy.md「开仓基准」或「市场结构」节）**：多头趋势日（开盘板块全面站上 VWAP + 持续上行 + 买卖比改善）里，**VWAP 上方企稳 = 顺势做多入场点**，止损放 VWAP 下方（或近期低点）、目标看前高 / 阻力，**不等突破也不等深回踩**（多头不回头、等深回踩等不到）；「等突破 / 等回踩」是震荡市或弱市的入场法，多头趋势日里会整段漏掉。改 skill 内容须先读 skill-creator 按其结构写。**完成说明（2026-08-05 20:59）**：trading-strategy.md 新增「多头趋势日：VWAP 上方企稳 = 顺势做多入场点（2026-08-03 美股教训，补「做多合格入场只有两种」）」节——含为什么不等突破/回踩、与「做多合格入场只有两种」及「纪律性不追高」的关系区分（追高 vs 顺势三问）、反面教训、四条边界（市况确认限定 / 企稳失效即止损 / 不在拉伸高点追 / 3x ETF 止损按形态）。已按 skill-creator 方法论（references 详情文件增量、大白话讲因果）写入。

- ✅**已完成**（完成：2026-08-05 20:59）**跳空强势股做空禁令加严（写入 trading-strategy.md）**（记录：2026-08-04 12:35）：2026-08-04 药明康德（跳空暴涨 +10.8% 后三次反抽 VWAP 遇阻做空、多头韧性致价格 14 分钟横盘不跌、扣费后 −0.06R）+ 此前 07709 教训——逆跳空强势做空「形态对 ≠ 能跌」（反抽遇阻形态成立，但跳空暴涨股多头韧性使价格跌不动、盘口偏空却横盘）。trading-strategy.md 已有 07709 跳空相关教训，需补「跳空暴涨（当日 +8% 以上）股的反抽遇阻做空 = 高风险形态」明确：要求额外确定性（如跌破今日低 / 板块共振走弱）才出手、或干脆不做（等跳空回补后常态再空）。改 skill 内容须先读 skill-creator 按其结构写。**完成说明（2026-08-05 20:59）**：trading-strategy.md 新增「跳空暴涨股的反抽遇阻做空 = 高风险形态（2026-08-04 药明康德教训，加严 07709 跳空教训）」节——含为什么（多头韧性致横盘、无下跌空间还倒贴费）、反面教训、处理二选一（额外确定性才出手 / 干脆不做、默认 ②）、三条边界（+8% 经验阈值按量能资金流判断 / 当日已转弱仍可按「下跌趋势确认」框架空 / 不放松「反抽破前高放弃做空」）。已按 skill-creator 方法论写入。

- ✅**已完成**（完成：2026-08-03 10:34）**缩短 SKILL.md「早盘开盘首小时只观察」规定（60 分钟太长 + 一刀切，2026-08-03 立）**（记录：2026-08-03 10:18）：现行 [SKILL.md:90](.claude/skills/trade/SKILL.md#L90) 规定港股 09:30-10:30 / 美股 09:30-10:30 不发任何交易动作（含开仓、平仓、移损），只观察行情性质，10:30 后才允许开仓（2026-07-31 立，见 CHANGELOG 第 71 条）。三个问题：① **时间太长**——开盘最混乱的其实是前 15-20 分钟冲动盘消化期，30 分钟后趋势往往已较清晰；卡满 60 分钟等于放弃全天流动性最好、波动最大的黄金时段（项目自己反复强调「早盘必密采样、机会与风险都在这」）。港股上午盘 09:30-12:00 仅 2.5 小时、砍首小时后上午只剩 1.5 小时可交易，对港股伤害远大于美股（美股全天 6.5 小时、砍 1 小时影响小）。② **一刀切、不分类，与既有实战教训冲突**——07-17 MU 头肩底 09:48 破颈线确认做多（[trading-strategy.md:138](.claude/skills/trade/references/trading-strategy.md#L138)，之后涨 +29 点）、07-17 MU 连破四道阻力执念等反弹踏空整段（[trading-strategy.md:268](.claude/skills/trade/references/trading-strategy.md#L268)，核心是「方向变了立刻切、别固守太久」），首小时一刀切既会错过 09:48 这类好信号、又与项目一贯的「动态修正方向」精神相悖。③ **连平仓 / 移损都禁是风控隐患**——正常有「持仓不过夜」纪律、首小时无仓；但万一前日漏平（美股夜盘到美东开盘尤其可能），首小时价格急速反向却不能移损 / 平仓 = 裸奔。平仓、移损是风控动作，任何时候都应照常可用，不该被「观察期」挡住。

  **建议改法（3 处）**：a) 时间从 60 分钟缩到 15-20 分钟；或干脆不绑死时长，改为条件触发——跑完 `monitor_summary` 判出行情性质（震荡 / 趋势）+ VWAP 方向明确 + 量价配合，**条件一满足就允许开仓**（可能 09:45 满足、也可能 10:05 才满足），与「每 10 分钟动态重估」精神一致、比机械卡表强；b) 平仓 / 移损解除限制——观察期只禁开仓，风控动作任何时候照常；c) 暴跌标的的「开盘 30 分钟内不开多、等双底 / 头肩底确认」单独规则保留不动（策略层已有、针对接飞刀更精准，不该靠「全市场禁交易一小时」这种过宽手段覆盖）。

  **改前注意**：改 skill 内容须先读 `~/.claude/skills/skill-creator/` 按其结构规范来（主文件精简、详情拆 references，不违反 Progressive Disclosure）；改完记 CHANGELOG（含「缩短首小时观察期」的原因 + 评估对既有教训 07-17 头肩底 / 07-17 破阻力踏空 的影响——预期是修复冲突而非回归）。

- ✅**已完成**（完成：2026-08-05 23:17）**预估赔率打折系数：开仓判 EV 时对初始预期赔率打折**（记录：2026-08-04 12:35）：2026-08-04 复盘（N=38）5 笔新增交易三阶段赔率全部「预估乐观、落地打折」——初始预期 1.5~2.9R，修正后降一档、落地再降一档，仅 NVDA 1 笔兑现（+1.06R）、其余 4 笔从预估 2R 上下打到打平 / 止损（HK.02359 1.93→2.09→−0.06R、MU 空 2.86→2.06→−0.97R、MU 多 2.80→2.63→−0.60R、HK.09988 1.50→1.00→−0.30R）。两个打折模式：① 滑点 + 止损距重算（修正 < 初始）；② 平仓时机 / 形态失效（落地 < 修正）。建议开仓判 EV（≥1.2 门槛）时对「初始预期赔率」打经验折扣（如 ×0.5）再判、避免系统性过度乐观。需用历史三阶段打折分布标定折扣系数（当前 5 笔样本太小、待积累）。**完成说明（2026-08-05 23:17）**：用 8-03~8-05 全部可配对三阶段记录（12 笔初始 ≥ 1.2 样本）实算标定——75%（9/12）落地 ≤ 0、落地/初始 比值中位数 −0.22 均值 −0.08（初始均值 2.69R、落地均值 −0.11R），「预估乐观、落地打折」确认为系统性现象；**×0.5 折扣规则落地**：trading-strategy.md「开仓基准」改写（初始预期赔率 ≥ 1.2 改为打 ×0.5 折扣后口径、等价 ≥ 2.4）+ 新增「预估赔率系统性打折」节（标定数据、×0.6/×0.5/×0.4 敏感性、四条边界），SKILL.md 三处同步（6 要素第 3 条 / 操作原则第 1 条两道闸 / 开仓前自查清单 EV 判据）。**系数为 12 笔保守粗标定、待 ≥30 笔重新标定**（新跟踪条目已记入 TODO.md）。

- ✅**已完成**（完成：2026-08-05 23:17）**震荡市策略失效应对（近 10 笔 EV 转负的归因行动）**（记录：2026-08-04 12:35）：2026-08-04 复盘暴露——账面正 EV（+0.22R）全靠 4 笔 ≥2R 大胜撑起（占总 R 和 170%、剔除 top2 后剩 36 笔 EV≈0），且 4 笔大胜集中在 7-17~7-21 半导体大波动期；8 月以来近 10 笔 EV −0.29R、胜率 30%（大胜来源消失后整体转负）。策略本质是「趋势日 / 暴跌顺势大胜捕获器」、震荡 / 无趋势市况天然不适配。需研究震荡市应对：① 减少出手（无大胜土壤时控亏损）；② 改进小赚小亏出场（避免扣费后转负）；③ 前置识别「趋势日 vs 震荡日」决定是否出手 / 频次。属策略层大问题、待用更多样本验证假设。**完成说明（2026-08-05 23:17）**：三件事全部落地为 trading-strategy.md「震荡市策略失效应对」节（「震荡市策略」节后）——① **前置识别趋势日 vs 震荡日、震荡日减少出手**（monitor_summary 行情性质判别 + VWAP 方向：震荡日出手频次减半、只做区间边沿 + 高置信 + ×0.5 折扣后赔率仍厚的交易、其余空仓观望；趋势日维持正常出手）；② **改进小赚小亏出场避免扣费转负**（港股窄止损止损距 < 1.5% 从严按折扣后赔率估、浮盈 < 2R 用「止损单两种用法」用法 B 放趋势反转位让利润奔跑避免保本就平、接近止损按纪律走不拖延）；③ **趋势日敢捕捉**（防识别错方向：不在趋势日里套震荡日小心尺度）。边界：假设待样本验证（结构性克制而非已证伪）、减少出手 ≠ 停手、窄止损从严不动止损位定法。假设验证由每次复盘窗口分析自然覆盖、无需单独跟踪。

## 密采样多层防护（12 层全落实，2026-08-04 立）

> 2026-08-04 盯盘期间 AI 擅自用 cron + 直接 snapshot 绕过 monitor_segment 40 秒密采样降频（详见 `monitoring.md`「2026-08-04 教训」+ CHANGELOG）。**12 层防护全落实**（A1/A3/B4/C7/D10 首批 5 层 + 下列 7 层），非 100% 银弹——AI 仍能 kill 进程绕过，盯盘纪律最终靠外部监督 + 多层防护 + AI 自觉。

- ✅**已完成**（完成：2026-08-04 12:24）**A2：TaskStop hook**（记录：2026-08-04 12:04）：A3/B4 hook 拦得住「调 snapshot 替代」「回合结束未重启」，但 AI 仍能 TaskStop / `kill` 主动停掉 monitor_segment 进程。计划：`settings.json` hooks 加对 TaskStop 工具的 PreToolUse 检查（停 monitor_segment 相关 task 时提醒「盯盘期间禁止停密采样，除非已到停盯边界」），或限定 TaskStop 仅停非密采样任务。难点：TaskStop 有正当用途（停出错进程），需精细区分。**已落实**：`monitor_guard.py` 加 taskstop 模式 + settings.json PreToolUse matcher TaskStop，盘中 TaskStop 且 monitor_segment 在跑时提醒确认（不阻断，避免误伤停出错进程）。
- ✅**已完成**（完成：2026-08-04 12:24）**B5：resume.py 强化时间断层哨兵针对「主动停」**（记录：2026-08-04 12:04）：现有 resume.py / monitor_segment 启动时的时间断层哨兵检查「距上次采样 ≥5 分钟」判断层，但主要针对断网 / 故障。计划：强化为针对「主动停密盯」——resume.py 检测到断层时输出强警告「疑似主动停密采样（非断网），立即重启 monitor_segment 恢复循环」，并在末尾自动建议重启命令。**已落实**：resume.py 盘中（hk/us_status 含「盘中」）断层输出「疑似主动停密采样、立即重启」强警告，盘外才按断网/故障处理。
- ✅**已完成**（完成：2026-08-04 12:24）**B6：外部 watcher 守护进程（launchd）**（记录：2026-08-04 12:04）：周期（如每 2 分钟）检查 monitor_segment 是否在跑、没跑自动重启——AI 主动停了也会被外部重启，最硬（超出 Claude Code 工具范围）。计划：写 launchd plist 或独立 python watcher，盘中时段自动守护 monitor_segment。难点：需正确传标的 / 关键位参数、与 AI 段间分析节奏协调（watcher 重启的进程 AI 怎么接管）。**已落实（部分）**：写 `monitor_watcher.py`（盘中检查 monitor_segment + 没跑发 macOS 系统通知，独立于 session）+ `com.daytrading.monitor-watcher.plist` launchd 模板（每 120s 触发）。**未做自动重启**（需当前盯盘标的参数、watcher 无法知道）——只检查 + 系统通知用户、由用户决定回 session 重启。部署需手动 `launchctl load` + 改 plist 占位路径。
- ✅**已完成**（完成：2026-08-04 12:24）**C8：monitor_log 采样时间戳复盘降频**（记录：2026-08-04 12:04）：复盘时遍历 monitor_log 采样时间戳，段间 gap >2 分钟标记为降频疑点，写进复盘报告（让降频在复盘暴露）。计划：review.py 或独立脚本加 gap 检查，复盘报告列降频疑点段。**已落实**：独立脚本 `monitor_log_gap_check.py`（>120s 标记降频疑点 + gap 统计，实跑验证药明 8-04 log 164 点 / 平均 12s / 最大 61s / 无降频疑点）；复盘时手动跑该脚本检查各标的 log（暂未集成进 review.py、待后续）。
- ✅**已完成**（完成：2026-08-04 12:24）**C9：用户监督（最有效但非工程化）**（记录：2026-08-04 12:04）：这次违规就是用户抓住的——用户监督是最有效一道，但需用户在场、不可工程化。计划：本条仅作提醒记入（不做工程实现），盯盘时用户留意段结束输出连续性（C7 指标）+ 段间节奏，发现降频即纠正。**已落实**：monitoring.md「多层防护」说明第⑫层明确「用户监督最有效一道、非工程化」，提醒用户盯盘时留意 C7 连续性指标 + 段间节奏。
- ✅**已完成**（完成：2026-08-04 12:24）**D11：盯盘自查清单加一条**（记录：2026-08-04 12:04）：`SKILL.md`「开仓前自查清单」或 `monitoring.md` 加一条「是否持续 monitor_segment 循环、未擅自降频（cron / 直接 snapshot 替代）」。软约束（会忘），作兜底。**已落实**：monitoring.md「防中断三件套」加第 4 条「每段自查未擅自降频」。
- ✅**已完成**（完成：2026-08-04 12:24）**D12：preflight.py 启动检查 monitor_segment 入口**（记录：2026-08-04 12:04）：`preflight.py` 盯盘启动时检查 + 提醒「密采样唯一入口是 monitor_segment、禁 cron/snapshot 替代」，并在启动序列里强调。软约束（启动时一次性提醒），作兜底。**已落实**：preflight.py 末尾加 print「密采样提醒：唯一入口是 monitor_segment.py 40 秒循环，禁 cron/snapshot 替代」。
