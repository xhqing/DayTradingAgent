# Changelog

本文件记录 DayTradingAgent 每个版本的主要变更，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

### 变更（T140 办结当日回归：恢复用户级两条挂载并 headless 双探针实证生效 + 修项目级死配置旧路径 + zcode-cli 侧登记开发主体，2026-09-04 12:07）

- **为什么改**：用户指令「把 T140 待办直接办了」——上一条目登记的回归（11:41 用户级配置重写冲掉 secret_guard / changelog_guard、凭证防线洞开）当日修复，并完成 T140 本项目侧全部可办动作。
- **改了什么**：
  - `~/.zcode/cli/config.json`（ZCode 用户级）：改前备份 `tmp/zcode-user-config-backup-20260904-pre-restore.json`，恢复 PreToolUse(Bash) 的 `secret_guard.py pretool` 与 PostToolUse(Edit|Write) 的 `changelog_guard.py` 两条挂载（与 guard.sh 并列、timeout 10、路径用项目现址）；回读验证 JSON 合法、挂载数 PreToolUse(Bash)=2 / PostToolUse(Edit|Write)=1，provider / model / permission / usage 段完整保留（含当日新增 glm-5.3-flash）。
  - `.zcode/config.json`（项目级死配置）：7 处旧路径（6 条挂载命令 + 1 条 disallowedTools）全部由旧址 `/Users/xhq/Documents/Projects/DayTradingAgent` 替换为现址 `/Users/xhq/Developer/DayTradingAgent`，JSON 校验合法——未来 ZCode 放开项目级 hooks 时六条可原生生效。
  - `zcode-cli` 项目（跨项目登记，T140 ① 的开发主体）：新建其项目根 `TODO.md` 并登记 **T1**（bootstrap 传 `workspaceHookTrustEnabled: true`，含 vendor/zcode.cjs 三处 offset 定位材料、完成后验证口径、同源风险提示「设置保存用旧快照整体重写 config.json」），其 CHANGELOG 已记对应条目。
  - `TODO.md`：T140 补办结段、降回橙色、移回橙色节；头部摘要更新。
- **验证（headless 新会话双探针，运行时端到端）**：① cat 假凭证路径 `/tmp/nonexistent-xyz-probe/accounts.json` **被拦**（deny 文案逐字来自 secret_guard）——PreToolUse 拦截链恢复实证；② Write 项目根探针文件收到**「📌 CHANGELOG 记录提醒」完整注入**（changelog_guard 经 `hookSpecificOutput.additionalContext` 通道）——PostToolUse 注入链恢复实证。探针文件即建即删、净变更为零。首次 changelog_guard 探针误用 tmp/ 路径而静默——tmp/ 在脚本 `_EXCLUDE_DIRS` 排除清单内属设计内行为，换项目根路径后实证（探针有效性辨析已写入 T140）。
- **重写来源结论（⓪b，间接证据链）**：写入动作未留日志，但 7 个 09-02 19:12~19:15（hooks 迁移当晚、早于 C 方案落盘）启动的后台 zcode-cli 进程至今存活（ps 实测 PID 29313/29363/29795/29863/29917/30951 等），其内存配置快照 = guard.sh 单挂，任一进程做设置保存即用旧快照整体重写 config.json——与实测「重写后 = 单挂 + glm-5.3-flash」完全吻合。**处置建议已报用户（关闭这批残留进程）；防再冲口径：后续改 ZCode 配置一律文本编辑 + 改后回读校验挂载数，不从客户端设置入口保存。**
- **回归风险评估**：① 两条挂载内容与 09-03 C 方案落盘版逐字同源（同脚本、同 matcher、同 timeout），CC / CodeBuddy 宿主零影响（互不相通的配置文件）；② 本会话（改动者）hooks 快照为改动前状态，本会话内两条不生效——已由独立 headless 新会话验证替代；③ 剩余风险 = 旧快照进程再次设置保存会再冲（用户关闭进程后消除）；④ zcode-cli 项目只新增 TODO.md 与 CHANGELOG 条目、无代码改动。

### 发现（T140 回归：ZCode 用户级 C 方案挂载被配置重写冲掉——secret_guard / changelog_guard 丢失、凭证防线再次洞开，T140 升回红色，2026-09-04 11:51）

- **为什么查**：用户问「本项目的所有 hook 目前 ZCode 都能正常使用吗」，在 ZCode 会话内实测核对（而非凭文档回答）。
- **发现（三探针实测 + 配置 / 日志取证）**：
  - **回归事实**：用户级 `~/.zcode/cli/config.json` 当日 **11:41:15 被整体重写**（mtime 实测），09-03 T140 C 方案上移的 secret_guard（PreToolUse）与 changelog_guard（PostToolUse）两条挂载**丢失**，只剩 guard.sh 一条；重写后内容 = 09-03 落盘前备份（`tmp/zcode-user-config-backup-20260903.json`）+ 新增 `glm-5.3-flash` 模型条目——高度疑似 ZCode 客户端某次设置保存用旧配置快照整体重写（写入动作未留日志、写入者身份未坐实，T140 前插待查项）。
  - **本会话三探针（11:41:16 起的新 ZCode 会话，加载的即缺两条的配置）**：① cat 假凭证路径 `/tmp/nonexistent-xyz-probe/accounts.json` **放行**（secret_guard 缺席）；② `echo "probe: pkill -f vscode"` **被拦**（guard.sh 在场、ZCode deny 链正常，文案逐字来自脚本）；③ Write 项目内 tmp 探针文件**无「📌 CHANGELOG 记录提醒」注入**（changelog_guard 缺席；探针文件即建即删、净变更为零）。
  - **副发现**：项目 `.zcode/config.json`（项目级死配置）六条挂载的脚本路径仍是项目旧位置 `/Users/xhq/Documents/Projects/DayTradingAgent/`——实测该路径已不存在（项目现址 `/Users/xhq/Developer/DayTradingAgent`），未来 ZCode 放开项目级 hooks、六条原生生效前须先修路径。
- **改了什么文件**：`TODO.md`（T140 升回红色、移入红色节、补 2026-09-04 回归段与前插两项剩余动作「恢复两条挂载 + 查明重写来源防再冲」+ 副发现登记；头部摘要追加）；本 CHANGELOG 条目。**运行时零改动**（未动任何 hook 脚本与挂载配置——恢复两条挂载涉及用户级配置、写入者未查明，待用户拍板后执行）。
- **回归风险评估**：本次为诊断 + 文档登记，无代码 / 配置改动面；登记的回归本身（secret_guard / changelog_guard 在 ZCode 缺席）恢复到 09-03 19:15 之前的状态，影响面 = ZCode 会话内 Bash 读凭证无拦、CHANGELOG 提醒缺失——CC / CodeBuddy 宿主不受影响（挂载在项目 `.claude/settings.json` / `.codebuddy/settings.json`，与用户级文件互不相通）。

### 变更（T140 深化：trust grant 已授信但 zcode-cli 会话层仍禁用——根因定位到 runtime 宿主参数 `workspaceHookTrustEnabled`，开发移交 zcode-cli 项目，2026-09-03 21:04）

- **为什么改**：用户提出「zcode-cli 是自己基于官方后端开发的，能否自己开发项目级 hook 功能」，授权实测 grant + 新会话验证——目标是让项目级六条挂载原生生效（覆盖比 C 方案两条全），零开发最好。
- **做了什么 / 发现**：
  - **runtime 已内置完整信任体系（非没做）**：状态机 `pending_trust → trusted_persistent`、声明 sha256 + bundle 摘要绑定、持久 trust store `workspace-hook-trust-v1.json`、命令族 `zcode hooks trust status/grant/revoke`（实测可用）。`status` 完整识别本项目六条挂载并逐条给 digest。
  - **grant 授信成功（20:35）**：`zcode hooks trust grant --workspace <本项目> --all-current --bundle-digest 30d62941…` → 六条全部 `trusted_persistent`，trust store 持久化。
  - **但 grant 后三个 headless 新会话（20:40/20:41/20:49）日志仍全部打 `feature_disabled`**——信任层开了、宿主能力层仍关。runtime 源码定位：`n = e.workspaceHookTrustEnabled === true`，zcode-cli 构造会话时**从未传该参数**（客户端源码 grep 零命中）→ `enabled:false` → 整个体系禁用。runtime 内部已有 `workspaceHookTrustEnabled:!0` 的调用样例（offset≈11922028，带 reviewHost 的完整构造）可参考。
  - **探针辨析修正**：echo 拼接形态（`echo "probe: cat …/accounts.json"`）在 secret_guard 的「段首动词为读取类」分段判定下**本就放行**——T137 当晚用它作「项目级 PreToolUse 未运行」的交叉证据属无效探针（该结论靠 changelog_guard 无注入 + 日志 warning 仍成立，T140 已注记瑕疵）；有效探针 = cat 不存在的凭证路径（命中拦截条件且无泄漏风险），grant 后实测**被拦**（文案逐字来自 secret_guard）——但拦的是**用户级 C 方案那份**（项目级仍禁用），C 方案兜底实证有效。
  - **结论**：零开发路线不通，需 zcode-cli 侧传参启用（一行级改动 + 发布）——**移交 zcode-cli 项目 / Atlas**，定位材料已写入 T140（构造点 offset≈11703354 `len()`、bootstrap 消费点 ≈11762913、`!0` 样例 ≈11922028；headless 可不传 reviewHost，授权走 CLI grant 已持久化）。
- **改了什么文件**：`TODO.md` T140 更新（grant 已授信、根因深化、剩余动作改为「等 zcode-cli 升级 → 验证 → 撤回用户级上移切项目级单源」）；本 CHANGELOG 条目；memory 同步。**运行时零改动**（用户级 C 方案挂载保持原样、未撤回）。
- **回归风险评估**：无运行时回归面（只读验证 + 文档更新）；grant 授信写入 trust store 属持久状态但可随时 `zcode hooks trust revoke` 撤销，且授信对象仅为用户自己的六条挂载声明。

### 变更（T140 C 方案落地：secret_guard + changelog_guard 上移 ZCode 用户级挂载，堵「ZCode 项目级 hooks 官方禁用」暴露的凭证防线缺口，2026-09-03 19:15）

- **用户裁定与为什么改**：T137 端到端验证发现 ZCode 官方整体禁用项目级 hooks（详见上条 T137 归档），致本项目四守卫自 09-02 迁移起在 ZCode 会话从未生效——其中 secret_guard 缺席 = ZCode 会话内 Bash 读 accounts.json / ~/.tigeropen 凭证无任何拦截（`permission.disallowedTools` 只挡 Read 工具）。用户裁定采 **C 方案（最小补救）**：只上移跨项目安全的两条，其余留 CC / CodeBuddy 宿主。
- **改了什么**：
  - `~/.zcode/cli/config.json`（ZCode 用户级）：PreToolUse(Bash) 追加 `secret_guard.py pretool`（与既有 guard.sh 并列）、新增 PostToolUse(Edit|Write) 挂 `changelog_guard.py`——改前备份 `tmp/zcode-user-config-backup-20260903.json`，回读验证 JSON 合法、挂载数 PreToolUse 2 / PostToolUse 1。
  - `.claude/CLAUDE.md`（= AGENTS.md 软链）「hooks 三宿主」节修订：标注「ZCode 走 `.zcode/config.json`」已失效（官方禁用）、新增「ZCode 修订」段（C 方案内容、ZCode 会话不用于盯盘 / 实盘执行的约束、挂载同步口径改为用户级三条、项目 `.zcode/config.json` 保留为未来兼容的死配置）。
  - `TODO.md` T140 更新（C 方案落地、红色→橙色、剩新会话验证）+ 头部摘要；memory 同步。
  - **不改**：两个 hook 脚本本身一字未动（CC / CodeBuddy 侧行为零变化）；项目 `.claude/settings.json` / `.codebuddy/settings.json` / `.zcode/config.json` 均保持原样。
- **为什么对 CC / CodeBuddy 零影响**：`~/.zcode/cli/config.json` 仅 ZCode 读取；CC 读 `~/.claude/settings.json` + 项目 `.claude/settings.json`，CodeBuddy 读项目 `.claude/settings.json` + `.codebuddy/settings.json`，三家配置文件互不相通。跨项目影响也已验证：changelog_guard 项目判定从脚本路径推导、其它项目文件静默（实测）；secret_guard 拦通用凭证路径特征、跨项目安全。
- **验证**：样例 payload 四分支实测全过——secret_guard 拦凭证命令 exit 2 + stderr「⛔ 凭证读取守卫」、普通命令 exit 0 静默；changelog_guard 项目内文件（mtime 新于 CHANGELOG.md）输出 additionalContext JSON、其它项目文件静默。**hooks 无热加载，运行时端到端生效须待新 ZCode 会话**（T140 剩两条探针收尾：① `echo` 拼凭证路径应被拦；② Edit 项目内文件应收「📌 CHANGELOG 记录提醒」注入）。
- **回归风险评估**：① 用户级配置改动经回读验证、且原配置已备份可秒回滚；② 最坏情形 = 新会话探针不过 → 用户级挂载未生效，退回 T137 已验证状态（guard.sh 单挂），不会比改动前更差；③ secret_guard 误拦面 = 命令同时含凭证路径特征 + 读取动作特征（本来就该拦的形态），正常交易链路（跑 trade 脚本、git 只读）白名单放行逻辑未动；④ 本会话（改配置的会话）hooks 快照为旧配置，本会话内两条新挂载不生效——不影响任何已开会话，仅新会话生效。

### T137 归档（ZCode 宿主 hooks 端到端验证：② 全局 PreToolUse deny 链实测生效；③ 项目级挂载被 ZCode 官方整体禁用、此路不通，新立 T140 补救，2026-09-03 18:58）

- **为什么改**：T137 要求对「2026-09-02 CC hooks 全量迁移 ZCode」做端到端行为验证（① hookCount 判据已作废，剩 ② PreToolUse deny 链、③ PostToolUse 注入链两项）。2026-09-03 晚间在 ZCode 会话内实测完成，发现影响超出验证本身的官方限制。
- **改了什么 / 验证结论**：
  - **② 通过**：探针 `echo "probe: pkill -f vscode"`（无害 echo）被拦、未执行，拦截文案逐字来自全局 `~/.claude/hooks/pre-tool-use-guard.sh:29`——**用户级 `~/.zcode/cli/config.json` 挂载的 PreToolUse 投递 + deny + reason 回显全链实证生效**（区别于 CodeBuddy 宿主自带 `safety_rule_deny`，本次是本项目 hook 的 `permissionDecisionReason`）。
  - **③ 不通过，根因 = ZCode 官方禁用项目级 hooks（非配置错误）**：changelog_guard 探针（Write 项目根临时文件、mtime 晚于 CHANGELOG.md）无任何注入；交叉实证——secret_guard 探针 `echo "probe: cat .claude/skills/trade/accounts.json"`（字符串同时命中其「凭证路径 + 读取动作」两条件）**直接放行执行**，项目级 PreToolUse 链同样未运行；日志有 `config.project_hooks.pending_trust`（"remain blocked"）与 `workspace_hook.feature_disabled`（09-02 迁移当天即有——**项目四守卫自迁移起在 ZCode 会话从未跑过一次**）；**官方文档定论**（zcode.z.ai/en/docs/hooks）：「Project-level hooks are not executed in the current version」，`<workspace>/.zcode/config.json` 的 hooks **整体忽略（regardless of `hooks.enabled`）**，设置页已隐藏 workspace scope，官方建议插件分发；用户级配置不受影响（解释 ② 为何生效）。
  - **新立 🔴 T140**（secret_guard 缺席 = ZCode 会话内 Bash 读凭证无任何拦截，凭证防线洞开）：补救选项 A 全量上移用户级 / B 插件化 / C 最小补救只上移 secret_guard + changelog_guard，**待用户拍板**；`.zcode/config.json` 六条挂载保留不删。另：本地 zcode-guide 插件文档（0.1.0）「配置文件 hooks 无 trust 门槛」与官方文档及实测矛盾，以官方文档为准。
  - 文档动作：`TODO.md` 移除 T137、新立 T140、头部摘要更新；`TODO-archive.md` 加「2026-09-03 T137 完成」归档节。探针临时文件 `hook-probe.md` 即建即删、净变更为零。
- **回归风险评估**：本次**零代码改动**（只读验证 + 文档更新），无运行时回归面；唯一环境动作 = 删除自建探针文件。但**发现既有缺口**：ZCode 会话内 secret_guard / monitor_guard / changelog_guard / live_auth_witness 自 09-02 起全部缺席（该状态为迁移以来一直如此、非本次引入），补救方案见 T140。

### 新增（auto 模式模拟盘恒开对齐实盘：算仓位的下注金额 / 总购买力一律取实盘口径，2026-09-03）

- **用户裁定（2026-09-03，机制二问）**：① **当日快照制**——实盘数据进模拟会话不走模拟会话直连实盘 API（默认态被 IP 白名单连接层拦截），而是把实盘总资产 / 购买力快照到 `tmp/live_reference.json`（含取数时间），模拟会话算仓位读快照、快照缺失 / 非当日拒开；② **auto + 模拟盘恒开、无关闭开关**。
- **为什么**：交易记录按净 R 口径统计盈亏能力；模拟盘（约 789 万 HKD）与实盘资产量级差太大 → 每笔成交额差异巨大 → 净 R 口径里按笔固定费 / 整手离散随仓位量级摊薄不同，统计出的盈亏能力失真。模拟盘必须严格模拟实盘交易，模拟样本才能代表实盘能力（2026-09-03 用户立，规则详见 `SKILL.md` 护栏 1、`auto-mode.md`「auto 模式算仓位口径」节、`risk-management.md`「权益更新」节）。
- **改了什么**：
  - `trade_utils_tiger.py` 新增「实盘参考快照」模块：`fetch_live_reference`（查实盘 HKD/USD 净值 + buying_power 写快照）、`read_live_reference` / `is_live_reference_fresh` / `load_live_reference_checked`（读 + 北京日历日新鲜度校验，缺失 / 非当日返回拒单文案）、`auto_sizing_equity` / `auto_sizing_fx_hkd`（算仓位 equity 与 HKD 折算汇率的统一入口：实盘执行 = 实时查实盘自身；模拟盘执行 = 实盘当日快照）；`load_equity` auto 分支对模拟盘账户改走实盘快照、缺失 / 非当日 **fail-closed 返回 None**（不回退模拟资产 / equity-log）；`get_buying_power_tiger` / `get_short_buying_power_tiger` / `_buying_power_impl` / `get_buying_power_us` 加 `bp_usd` 覆盖参数（模拟盘执行时传实盘快照购买力，不再查模拟盘自身）；CLI 增加 `--refresh-live-reference`；`__main__` 块从文件中段移到末尾（原位置会先于后续函数定义执行、挡 CLI 扩展）。
  - `open_position_tiger.py` / `open_position_tiger_us.py`：自动算仓位 / 显式传量风控校验的 equity、购买力降档的 bp，模拟盘执行一律改走实盘当日快照；快照缺失 / 非当日 → `blocked_by:"live_reference_required"` 拒开（**fail-closed，跳过校验 = 放任超限**）；实盘执行（`--account live`）行为零改动（照旧实时查实盘自身）；开仓结果新增 `sizing_source` 字段留痕。
  - `preflight.py` / `resume.py`：auto + 模拟盘会话的 B 显示走实盘口径；快照缺失 / 非当日打印 🚨 警示 + 自动尝试刷新一次；auto + 实盘会话顺手把快照刷成当日（供同日模拟盘用）。
  - 文档：`SKILL.md` 护栏 1、`references/auto-mode.md`（新增专节）、`references/risk-management.md`（权益更新节）、`config.json` / `config.example.json`（`_comment_sim_live_alignment` 说明）。
- **刷新入口**（均需在当日已实盘解锁的会话执行）：实盘会话 `preflight.py --mode auto --account live` / `resume.py` 自动刷；任一已解锁会话跑 `python3 .claude/skills/trade/scripts/trade_utils_tiger.py --refresh-live-reference`。
- **验证**：6 个改动脚本 py_compile / ast 全过、read_lints 零告警；快照读写 / 北京日历日新鲜度 / 缺失与过期拒单文案 / auto_sizing 三口径纯函数自测通过（隔离 /tmp 路径，未触碰真实 tmp）；`load_equity` auto+paper 无快照返回 None（不回退）、signal 路径照常读 equity-log。
- **回归风险评估**：① 实盘执行路径（`--account live`）所有分支保持「实时查实盘自身」原逻辑，唯一触碰 = equity 经 `auto_sizing_equity` 薄包装返回（语义不变），实盘下单链路无行为改动；② 模拟盘执行从此**必须有当日实盘快照才能开仓**——无快照时开仓被拒属**有意行为**（fail-closed），但机制测试 / 只想跑模拟盘而当日无实盘会话的场景会因此被挡，须先刷快照（文档已给三条入口）；③ 新增 CLI 需在已实盘解锁会话执行，未解锁直接报错并给指引（不静默）；④ 快照文件在 `tmp/`（已 gitignore），含实盘净值 / 购买力（非敏感信息，2026-08-29 裁定可写），不含任何凭证；⑤ 若未来需按模拟盘自身资产跑（如纯机制测试），需先推翻「恒开」裁定、非本版本范围。

### T138 归档（CodeBuddy 宿主 hooks 端到端验证：PreToolUse + PostToolUse 两条投递链实证生效，2026-09-03 17:25）

- **端到端实证（新 CodeBuddy 会话，非盘中）**：① **PreToolUse 链**——探针 `head -c 1 /tmp/nonexistent-xyz-probe/accounts.json`（假路径、不泄漏凭证）被 deny，拦截文案逐字来自本项目 `secret_guard.py`，且仅一次 deny（`.claude/settings.json` + `.codebuddy/settings.json` 双份挂载被官方自动去重、**无翻倍**）；② **PostToolUse 链**——编辑文件后 `changelog_guard` 被调用（payload `tool_name='Edit'`、`tool_input_keys=['filePath',…]`、`should_remind=True`），IDE 将 hook 输出转成 `<system-reminder>📌 CHANGELOG 记录提醒…` 注入 tool-result 的 `additionalContext`（转录实证、模型可见）——**changelog_guard 修复后首个运行时证据，改文件记 CHANGELOG 提醒链在本宿主真正打通**。
- **剩余（非阻塞、自然场景触发，不入活跃 TODO）**：`live_auth_witness` 实盘授权点击写凭证需真实实盘授权流程、`monitor_guard` Stop/TaskStop 需盘中盯盘会话——脚本逻辑均已回归过、挂载与两条主投递链已实证，场景到了自然验证即可。
- **回归风险评估**：本次仅文档改动（`TODO.md` 移除 T138、`TODO-archive.md` 加归档记录、CHANGELOG 本条）+ 对 `changelog_guard.py` 的临时 debug 已完全还原（git diff 确认仅剩当日修复主体，py_compile 过）——**无运行时回归面**。

### 待办批量处理（2026-09-03 盘外：T121 / T122 / T126 / T116 / T124 完成归档 + T119 首次对账 + T120 首批数据 + T138 新立）

- **背景**：盘外处理（13:40~14:10，无本会话盯盘任务）。多数待办原判「只能盘中实测」，改为**从已发生的真实会话留痕取证据**（actions 记录 + CC 会话转录里的真实脚本输出）+ **盘外 mock 场景构造**，把能盘外收口的全部收口。
- **实测通过并归档（4 条）**：
  - **T121 自问平仓检查**——09-03 真实脚本输出含 `self_check_close_question` 字段与完整文案，`actions/2026-09-03-HKT-actions.md` 两条开仓记录均带「T121 自答」行（09-02 两条同），「脚本输出了但 AI 不看/不记」的担心未发生。
  - **T122 reason 提层**——09-02 14:09（03308）/ 09-01 12:12（02513）两笔真实拒单输出首层 `error` 均为「开仓全部失败（尝试 2 档）：Your available funds or buying power are insufficient」，具体拒因已从 `failures` 数组提到首层；reason 为空时（09-02 12:40 超时撤单）保留原文案。
  - **T126 当日订单过滤**——09-03 真实开平 2 笔、十五会话并行下第四层「白名单外在场敞口」告警**零误报**（检索命中全为 SKILL.md 文档文本），持仓状态行止损价提取正常（351.00 与下单 LOSS 腿一致）。
  - **T116 限价单 + 止盈单机制收尾**——三个剩余子项（④ 移盈 modify / ⑥ 无止损单平仓补设 STP / ⑥b 止损单铁律恢复链）用盘外 mock 验证通过（11 项断言全过），7 子项全数收口。新增 `tmp/test_limit_target_paths.py`。
- **T119 首次运行对账完成**（产物 `reviews/2026-09-03-odds-reconciliation.md`）：区间 2026-08-28~09-03 港股 auto 实成交 **N=10**，**EV = −0.529R、胜率 30.0%、累计 −5.288R、净盈亏 −66,734.91 HKD**，初始预期 R 均值 2.054 → 落地 −0.529（兑现率 −25.7%）；分账户 **实盘 N=7 EV −0.705R / 模拟 N=3 EV −0.117R**；对照 2.4 时代（N=16）EV +0.23R（⚠️ 08-28 起分母改净 max_loss，**不可做精确差值比对**）。结论已写入 `trading-strategy.md`「门槛沿革」新增 **⓪-a** 段。门槛回撤预案（「期间日均亏 ≥3R 回撤 1.5」）按「期间总 R÷交易日数」口径 −1.058R/日未触发，但 08-28 单日 −3.26R 已超 3R——**两口径结论相反、交用户裁定**。
- **T124 完成归档 + 暴露更严重的结构问题（新立 T139）**：印花税维度恶化判据未触发（不重开税闸）；但实盘账户（equity ≈ 10.6 万、B ≈ 2,126）**单笔双边固定费 188~402 HKD = B 的 8.8%~18.9%**，实盘 7 笔合计**毛亏 −1,366、费用 2,232.51（费是毛亏的 1.63 倍）**——**实盘小账户亏损主因是按笔固定费摊薄，不是方向判断、也不是印花税，提高赔率门槛治不了**。已立 T139 交用户裁定（实盘暂停日内 / 提高 risk_fraction / 维持现状）。
- **T120 首批数据开张**：`tmp/blocked_decisions.csv` 从无到有，3 个被拦点（08-28 14:23 HK.00700 净赔率 1.10、08-28 13:56 HK.01888 净赔率 0.64、09-01 11:50 HK.02513 净赔率 0.82）已从会话实录复原并富途分钟 K 回填；样本 <5、`stats` 提示先攒数据。
- **T137 判据作废**：实测 ZCode 三个会话 `hookCount` 恒为 0，该字段属 `stage=resolve_plugins`、只统计**插件贡献的 hook**，与 config.json 的 `hooks.events` 挂载无关——原「hookCount 应为 7」的期望值错误，已作废，收窄为剩行为验证（违规命令被 deny / Edit 收到 CHANGELOG 注入）。
- **新立 T138（CodeBuddy 宿主未挂 hooks）**：实测当前会话宿主是 **CodeBuddy**（运行时写 `~/.codebuddy/`），项目 `.codebuddy/` 只有两个 symlink、**无 settings.json / 无 hooks 挂载**——`secret_guard`（凭证防泄漏）、`live_auth_witness`（实盘授权见证）、`monitor_guard`（盯盘硬拦）、`changelog_guard`（CHANGELOG 提醒）四条在该宿主全程不生效。**配置协议已查证（官方文档 https://www.codebuddy.ai/docs/ide/Features/hooks）**：配置放 `<workspace>/.codebuddy/settings.json`（项目级）+ `~/.codebuddy/settings.json`（用户级），**扁平结构、与 Claude Code 同款**（`{"hooks":{"PreToolUse":[{"matcher":...,"hooks":[{"type":"command","command":...,"timeout":...}]}]}}`），**无 ZCode 那层 `enabled`/`events` 包裹，不可照抄 ZCode 配置**；阻断用 exit 2（stderr 传模型）、exit 0 时靠 stdout JSON 的 `hookSpecificOutput.additionalContext` 注入（与 ZCode 同义，本项目双通道输出可复用）。**关键坑**：**IDE 宿主下 hook 脚本收到的 `tool_name` 是 IDE 风格名**（`execute_command`/`write_to_file`/`replace_in_file`…），matcher 虽支持双向别名匹配，**但脚本内部 `tool_name == "Bash"` 的硬判断会全部落空**——只挂配置不改脚本 = 挂着也不生效，须给四个脚本加别名兼容（详见 TODO.md T138）。
- **进度更新**：T125 影子样本 2→3 笔；T4 验证点④（log 落 `_auto`）09-03 十五会话并行日实测通过、剩①②③；T136 模拟账户路径实测通过、剩实盘路径；T123 补 09-02 03308 同源旁证。
- **用户裁定（2026-09-03 14:15，两条交易决策收口）**：
  - **T119 ② 门槛回撤预案 → 维持 1.2、继续攒样本**：预案「期间日均亏 ≥3R 即回撤 1.5」按「期间总 R ÷ 交易日数」口径 = −1.058R/日判**未触发**；08-28 单日 −3.26R 不单独作为回撤依据。理由：样本仅 10 笔不足以支撑「改变执行面」的门槛调整（本项目「交易逻辑建议必须有数理统计基础」硬约束——单日 / 单笔不构成规则长期成立的依据）。`config.json risk.min_net_odds` **未改**。
  - **T139 实盘小账户费用结构 → 维持现状继续攒样本**：不暂停实盘、不提高 `risk_fraction`、不改赔率门槛——固定费摊薄作为**已知结构性拖累**接受，继续按 2% 单笔预算攒样本。
  - **落地**：两条结论已写入 `trading-strategy.md`「门槛沿革」⓪-a 段与 `reviews/2026-09-03-odds-reconciliation.md` 第四节；T119 收窄为只剩 ④（15 笔中间对账、30-50 笔复标，**复标必须分账户看**）；T139 归档。**已知代价如实保留**：维持现状 = 在费用结构性拖累下继续实盘交易，1.2 时代样本大概率继续呈负 EV。
- **回归风险评估**：本次不改动任何交易/盯盘脚本（唯一新增文件是 `tmp/test_limit_target_paths.py` 测试脚本，`tmp/` 已 gitignore）；文档改动为 `trading-strategy.md` 新增 ⓪-a 段（纯追加实测数据、未改动既有结论）、`reviews/` 新增对账报告、`TODO.md` / `TODO-archive.md` 条目更新——**无运行时回归面**。

### 修正（T138 CodeBuddy hooks「未挂载」误判 + IDE 差异脚本适配；T123 做空拒单真因收窄；T120 增至 4 条，2026-09-03 16:35）

- **T138 误判修正（重要）**：14:05 曾判「CodeBuddy 宿主未挂 hooks、四条防护全程不生效」——**16:20 / 16:27 两次实证推翻**：CodeBuddy 会话里 `secret_guard` 成功拦截 AI 读 accounts.json 的命令、拦截文案逐字来自本项目 secret_guard.py，而该 hook 只挂在项目 `.claude/settings.json` 一处 → **CodeBuddy 官方兼容 Claude Code hooks 规范、直接读项目 `.claude/settings.json`，六条项目 hook 本就生效**（`monitor_guard` / `secret_guard` 不看 tool_name、只读 `tool_input.command`，天然跨宿主）。新增项目 `.codebuddy/settings.json` 作显式兜底（matcher 同时写 CLI 与 IDE 两种工具名）。全局守卫 `pre-tool-use-guard.sh` 不挂 CodeBuddy 用户级（用户裁定）。
- **IDE 差异脚本适配（单源改一处、三宿主同享，全部向后兼容）**：① `live_auth_witness.py` 认 CodeBuddy 的 `ask_followup_question` 工具名 + 新增 `_xml_question_answer_texts()` 解析其 XML 字符串形态 tool_response（解析不出按无答案处理=不写凭证，保守方向安全）；② `changelog_guard.py` 兼容 IDE 驼峰字段 `filePath`；③ `pre-tool-use-guard.sh` 认 `execute_command`。
- **顺带修复 changelog_guard 的 `_PROJECT_ROOT` 少算一层 bug（该 hook 自上线以来大部分场景从未触发过）**：原 `_PROJECT_ROOT = Path(__file__).resolve().parent.parent` 算到 `.claude/` 而非项目根 → 项目根与 `.claude` 之外路径的文件 `relative_to` 全部抛 ValueError、`_should_remind` 一律 False——**即 CHANGELOG 提醒 hook 对日常改的代码 / 文档（根目录 TODO.md、references/*.md 等）自 2026-09-02 上线起一直是静默 no-op**，只有 `.claude/` 子树内的文件碰巧能触发。改为三层 `parent.parent.parent`。验证：`_should_remind(刚改的 hook)` 由 False → True；排除项回归（tmp/ / accounts.json / CHANGELOG.md 自身）仍为 False；驼峰 `filePath` 与蛇形 `file_path` 两种入参均能触发提醒。
- **文档**：项目 CLAUDE.md「hooks 双宿主挂载」节改「hooks 三宿主：CC / ZCode / CodeBuddy 同一份脚本」，写明 CodeBuddy 走 `.claude/settings.json` 的实证依据、IDE 工具名 / 字段名差异与三处脚本适配点、`.codebuddy/settings.json` 的兜底定位与「提醒翻倍则删」的判据。
- **T123 做空拒单真因复核（盘外只读查询完成）**：`get_contract` 实查 **07747 `short_initial_margin = 0.7`**（CSOP Samsung Electronics Daily 2x Leveraged Product，long 0.65、lot 100、shortable=True）；富途分钟 K 实取 08-25 10:15 实价 **69.10 HKD**；实算最大可空 = 443,843.84 ÷ 0.7 ÷ 69.10 ≈ **9,176 股**，4100 股占用保证金 198,317 HKD——在「购买力」口径下完全够，**保证金率口径解释不了拒单、原候选一排除**。**真因收窄**：4100 股所需保证金 = 实盘 equity（≈106,358）的 **186.5%** → 大概率是 **buying_power（含杠杆购买力 44.4 万）≠ available funds（现金可用）** 的口径差（做空保证金从现金出）；次要候选 = 借券可用性（`shortable_count = None`）。剩：下次实盘解锁取 available funds 实测值终验。
- **T120 增至 4 条**：新增 `2026-09-03 11:15 HK.06869 长飞 long @164.2 / 止损 162.2 / 止盈 167.5 / 净赔率 1.18`（当日盯盘实录实时登记，与前三条靠事后翻录不同）→ 回填 high 166.5 / low 163.2、假想 R +1.150。
- **回归风险评估**：三处脚本改动均为「多认一个名字 / 多认一个字段」的增量分支，CC / ZCode 原路径（`AskUserQuestion`、`file_path`、`Bash`）行为不变；CC 原样例 payload 回归通过。TODO / CHANGELOG 为文档。无交易链路改动。

### 修复（account_status.py query_err 未初始化——持仓查询成功路径 NameError，2026-09-03）

- **为什么改**：2026-09-03 盘中开仓 00100 后，采样段「📌 持仓状态」行连续报 `[持仓状态检查 err:cannot access local variable 'query_err' where it is not associated with a value]`——持仓状态监控（2026-08-18 立的工具强制）完全失效。根因：`_query_account` 的 `query_err` 只在 `get_positions` 抛异常的 except 分支赋值，持仓查询**成功**路径上从未初始化；下方止损单查询的 except 分支（`query_err + '；' if query_err else ...`）与函数返回行引用它时即 NameError。
- **改了什么**：`_query_account` 内止损查询段前补 `query_err = ''` 初始化（成功路径兜底空串，except 分支照旧拼接）。
- **验证**：py_compile / ast.parse 通过；下一段采样「持仓状态」行实跑确认恢复正常。
- **回归风险评估**：纯加一行初始化，异常路径（query_err 已赋值）逻辑不变，无回归面。

### 新增（CC hooks 全量迁移 ZCode：双宿主工具强制同挂，2026-09-02）

- **为什么改**：本项目工具强制体系（「文档规定必须尽可能配工具强制」）此前只挂在 Claude Code（`.claude/settings.json` hooks），ZCode 会话在同一项目里跑时全部 hook 防护不在场（密采样守卫 / 凭证读取守卫 / 实盘授权见证 / CHANGELOG 提醒 / 杀 VSC 与远程窗口守卫），双 agent 单源共用架构下防护只盖一半。用户要求把 CC 体系下全部 hook 功能迁到 ZCode、功能 100% 一致。
- **改了什么**：
  - **协议实证先行**：本会话 hooks 只在会话启动时加载（bootstrap 日志 `hookCount: 0`，中途改配置不热加载），无法在会话内触发实测——改从 ZCode runtime 源码（`zcode-cli/vendor/zcode.cjs`）逐段提取 hook 执行器实现，实证协议事实：stdin payload 字段与 CC 同名同构（`hook_event_name` / `session_id` / `tool_name` / `tool_input` / `tool_response`，另带 `agent_type` / `permission_mode` / 临时 `transcript_path`）；exit 2 阻断 = deny 且 stderr 优先作 reason（与 CC 一致）；**exit 0 时 stderr 被完全忽略、stdout trim 后必须以 `{` 开头才解析 JSON**（纯文本 stdout 静默丢弃）；JSON 输出 schema 严格但 `hookSpecificOutput{hookEventName,permissionDecision,permissionDecisionReason}` 与 CC 同构；顶层 `additionalContext` 键注入对话（生命周期事件走消息历史附件）；matcher 按管道分割精确匹配 + 别名表 `ApplyPatch→[Write,Edit]`、`Agent↔Task`；ZCode 会话持久 transcript 在 `~/.zcode/cli/rollout/model-io-<sid>.jsonl`（sid 带 `sess_` 前缀，活跃持续追加）。
  - **新建 `.zcode/config.json`**（项目级）：`hooks.enabled: true` + 六条挂载与 CC settings.json 一一对应（PreToolUse/Bash→monitor_guard pretool、PreToolUse/Bash→secret_guard pretool、PreToolUse/TaskStop→monitor_guard taskstop、PostToolUse/AskUserQuestion→live_auth_witness、PostToolUse/Edit|Write→changelog_guard、Stop→monitor_guard stop），命令直接引用 `.claude/hooks/` 现有脚本（单源共用、不复制，同 `.zcode/skills` 软链思路）；另带 `permission.disallowedTools` 迁 CC 的 `Read(accounts.json)` deny（CC deny 的 CronCreate 在 ZCode 无此工具、无对应面）。
  - **`~/.zcode/cli/config.json`**（用户级）：新增 hooks 段挂全局 `pre-tool-use-guard.sh`（timeout 10 秒同 CC），对全部 ZCode 项目生效——对应 CC 全局 `~/.claude/settings.json` 的同款挂载。
  - **脚本双宿主适配**（改动均在 `.claude/hooks/` 原文件，CC/ZCode 同一份）：
    - `monitor_guard.py`：① 新增 `_emit_reminder()`——提醒类输出改双通道（stderr 文本照旧给 CC + stdout JSON `additionalContext` 给 ZCode），stop / taskstop 两分支改用它；② `_claim_holder_alive()` 双路径判活——`sess_` 前缀 sid 查 ZCode rollout jsonl、裸 UUID 查 CC projects jsonl，两套会话并行认领互认活死（此前只认 CC 路径，ZCode 会话的认领一律被判死、标的池互斥层1 跨池冲突失效）。
    - `changelog_guard.py`：stdout 纯文本改 JSON `{"additionalContext": ...}`（ZCode 下原纯文本被静默丢弃；CC 的 PostToolUse 同样支持该键注入，双宿主一致）。
    - `live_auth_witness.py`：失败提示改 `_emit_note()` 双通道（stderr + stdout JSON）；docstring 补 ZCode tool_response 同构实证（questions 数组 + answers「问题文本→答案文本」字典，与脚本期望零差异）。实证凭证文件名下划线被 `_sid_tag` 过滤（`sess_x` → `sessx`）——`live_unlock.py` 用同款过滤，写读两口一致、解锁链不受影响，不改。
    - `pre-tool-use-guard.sh`（全局，修两个**原有 bug**）：① 规则 1 第二层 `grep -iE` 漏 `-q`——匹配行直接打进 stdout，JSON 前多一行文本，ZCode 的「stdout 必须以 { 开头」判定会整体丢弃 deny 决策（CC 下同样污染输出），补 `-q`；② 规则 2 reason 文本含未转义 ASCII 双引号，拼出的 JSON 非法（两宿主都解析失败、deny 丢失），改无引号表述。
- **验证**：按 ZCode 源码构造同构样例 payload 逐脚本喂测——monitor_guard pretool 正常命令 exit 0 / && 连跑两段 exit 2+stderr；secret_guard cat accounts.json exit 2+stderr；changelog_guard 改新文件输出 additionalContext JSON；stop / taskstop 双通道（stderr+stdout JSON）输出到位；live_auth_witness 实盘授权确认 payload 凭证写出（`sess_` 过滤形态与 live_unlock 一口）后测试凭证已删；guard.sh 三路径（杀 VSC deny JSON / vscode:// URI deny JSON / 授权标记放行）stdout 全部合法 JSON 或空；`_claim_holder_alive` 真实 rollout 文件 True / 虚构 sess_ False / 裸 UUID False。**CC 侧回归端到端实测（2026-09-02 晚，headless `claude -p` + 会话 transcript 取证）**：读 CC 2.1.226 源码实证其 hook stdout 为逐行提取 JSON（多行也能命中）+ 校验失败宽容降级；两轮 headless 会话 transcript 记录——① `cat accounts.json` 工具调用被 secret_guard 以 `PreToolUse:Bash hook error` 阻断、stderr 原文完整返回模型（阻断链完好）；② Stop 提醒以 hookSpecificOutput.additionalContext 形态注入、模型 thinking 确认收到（注入链完好）。**宿主端到端（ZCode 侧配置加载、事件投递、注入）须新 ZCode 会话验证**（启动时加载），已立 T137 跟踪。
- **补充升级（同日，CC 回归验证中发现）**：提醒类输出从顶层 `additionalContext` 升级为 `hookSpecificOutput{hookEventName, additionalContext}` 标准形态——读 CC 源码发现顶层 additionalContext 非 CC 顶层合法键（同步校验降级 plainText、不注入），而 hookSpecificOutput.additionalContext 是 CC 与 ZCode 两宿主 schema 都原生支持的注入通道（CC 每个事件均含该字段，Stop 事件官方描述「feedback delivered to the model」；ZCode Jxn discriminatedUnion 各 case 同构）。对 CC 是纯增强（原 stderr 通道保留 + 新增确定注入），对 ZCode 行为不变；`_emit_reminder` 按子命令传真实事件名（两宿主均强校验 hookEventName 必须匹配）。
- **回归风险评估**：① 脚本改动全部为「加输出通道 / 加判活路径 / 修输出 bug」，原 CC 行为路径（exit 2+stderr 阻断、CC stderr 提醒）一字未动；② changelog_guard 从纯文本改 JSON 在 CC 下同样注入（PostToolUse additionalContext 是 CC 支持键），提醒效果不降；③ guard.sh 两处修复对 CC 是让输出更干净（消除 stdout 污染与非法 JSON），deny 语义不变——无「修复引入回归」面；④ `.zcode/config.json` 进 git 跟踪（含本机绝对路径、无敏感值，与 `.claude/settings.json` 同口径）；⑤ 双宿主同挂后同一 hook 在 CC 会话与 ZCode 会话各自由宿主触发，互不重复调用。

### 修复（monitor_guard Stop 提醒加「盯盘会话」门槛——非盯盘会话不再被无限唤醒，2026-09-02）

- **为什么改**：2026-09-02 晚实录——同日 `_emit_reminder` 升级把 Stop 提醒接入 hookSpecificOutput 注入通道（见上「CC hooks 全量迁移 ZCode」条的补充升级）后，Stop 提醒从「stderr 无人看见」变成「注入上下文、唤醒模型」；而 monitor_guard 的 stop 分支只判「盘中 + 无采样」、从不判「本会话是不是盯盘会话」——美股盘中窗口（美东 04:00-16:00 = 北京 16:00-次日 04:00）里一个纯 `/commit` 会话（无盯盘任务、港股已收盘）每个回合结束都被提醒唤醒，回复「忽略」后回合又结束、又被提醒，无限循环 160+ 轮（用户两次指出「这个不是盯盘会话呀」「不是盯盘会话不要提醒」）。根因 = 提醒对象不限定的设计缺口，在注入通道升级后从「无害噪声」放大成「死循环」。
- **改了什么**：`monitor_guard.py` stop 分支加 `_is_monitoring_session(sid)` 门槛（全部只读、无副作用）——三判据任一命中才提醒：① 会话在盯盘注册表 `tmp/monitor_sessions.txt`（preflight 港股盯盘注册、monitor_unregister.sh 停盯注销）；② 当日认领表 `tmp/pool_claims.json` claims 含本 sid；③ 当日 `tmp/trade_intent.log` 含本 sid 的开仓行（开仓会话必然在盯盘）。另加前提：今天全项目无任何采样日志（`tmp/monitor_log_*_<今日>_*.csv` 一个都没有）= 没有任何会话开过盯盘 → 直接不提醒。判定链任何异常保守按盯盘会话处理（宁可多提醒、不漏拦真断采样）。pretool / taskstop 分支不动。
- **验证**：py_compile 过；五路冒烟——本会话 sid（三判据不命中、今天有日志）无输出 ✅、注册表内 sid 输出提醒 ✅、当日开仓 sid 输出提醒 ✅、无 sid（手动跑）无输出 ✅、pretool 正常命令回归 exit 0 ✅。
- **回归风险评估**：① 真盯盘会话（港股注册 / claim 认领 / 当日开仓）三判据全覆盖，断采样仍会被提醒——防 2026-08-04 降频教训的原防护不弱化；② 已知窄缝：纯信号美股盯盘会话（按 2026-08-12 规矩不注册 watcher、纯 signal 不开仓、claim 是港股池概念）若断采样，stop 分支不再提醒——由 pretool 分支的密采样阻断与用户监督兜底，且该缝的漏提醒代价远小于死循环代价；③ 判定异常时保守放行为提醒（fail-safe 方向与原行为一致）；④ CC 与 ZCode 双宿主同一脚本，门槛对两宿主同时生效。

### 修复（持仓状态行 / 止损单查询按下单账户选账户——实盘会话误查模拟账户，2026-09-02 T136）

- **为什么改**：2026-09-02 早盘实盘首单（00100，`--account live`）后，采样段「📌 持仓状态」行连续多段报「账户实持 0股（默认（模拟））| 活动止损单触发价 无活动止损单 —— 账户已无持仓但 actions 无平仓记录」，而实盘持仓与止损 / 止盈双腿实际都在场——`account_status.py` 一律查默认（模拟）账户，与本会话实盘口径错配（当日 actions 记录漏写「账户」行、旧格式 intent 无账户字段，账户判定的两个依据同时失效）；`monitor_segment.py` 的止损单查询同病（「无活动止损单」误报同根因）。实盘开仓后持仓状态监控形同虚设、AI 若轻信会漏盯实盘持仓。
- **改了什么**：
  - `trade_mutex.py`：`append_intent` / `TradeMutex` 增 account 字段（'live' / None=默认模拟账户），开仓时随 intent 落盘——每笔开仓的账户口径此后精确到笔（`settle_intent` 解析后回写、新字段自然保留）。
  - `open_position_tiger.py` / `open_position_tiger_us.py`：`TradeMutex` 传 `account=`（live 才标实盘）。
  - `account_status.py`：新增 `resolve_symbol_accounts` 统一解析（intent account 字段优先 → 当日 actions「| 账户 | 实盘」行兜底 → 默认模拟账户垫底；旧格式 intent 记录无 account 字段不进映射、不会挡住兜底）；`_query_account` 改单一账户入参；`position_status` 按标的分组查询（同账户只查一次、查询失败按账户去重、「actions 无记录持仓」按账户比对不跨账户误报、新增 account 显式参数）。
  - `monitor_segment.py`：`query_stop_prices` 按账户分查当日订单（实盘标的查实盘止损单）；第四层单持仓检测改逐账户判（单持仓护栏是账户级口径，与互斥闸只查下单账户订单流的语义对齐）。
  - `actions/2026-09-02-HKT-actions.md`：开仓记录补记「账户 | 实盘」行（原记录漏写，order_id 44488568990072832 可证）。
- **验证**：5 脚本 py_compile 通过；账户解析链路本地冒烟通过（含冒烟抓出的一处坑：旧格式 intent 无 account 字段时若以 None 进映射会挡住 actions 兜底判定，已修——旧记录不进映射）；空仓时 `position_status` 返回 None（零 API，「空仓不查」规则不受影响）。
- **回归风险评估**：① 账户解析三分支全部走既有 `load_config(account=)` 通道，实盘账户的代理路由 / is_paper 显式设定等既有逻辑不变；② intent 记录新增 account 字段为纯增量，`pending_intents` / 清理等消费方只读既有字段不受影响；③ 第四层单持仓检测改逐账户判——单账户内 ≥2 白名单外敞口才告警，消除了跨账户混判的方向性缺陷（此前该路径查不到实盘订单、对实盘违规是漏报）；④ 查询失败语义不变（positions=None 显式带错误文本，不吞成空仓假告警）。

### 补充记录（CHANGELOG 记录纪律的「记录不用问」修订 —— 补项目内足迹，2026-09-02）

- **为什么补记**：2026-09-02 审计「CHANGELOG 记录不用问」这条规矩的历史足迹时发现——「CHANGELOG 记录纪律」本体（2026-07-30 立）在项目 CHANGELOG 有独立条目（见本文档下方「全局 CLAUDE.md 新增『CHANGELOG 记录纪律』规则」条），但 **2026-08-21 用户立的补充修订「记录不用问」（凡有变更都直接记录、不需要先问用户）从未在项目 CHANGELOG 留痕**——同一纪律的补充条款缺项目内记录，与本体并列时形成「本体有记、修订无记」的不对称。本次补记，使项目内对该纪律的完整足迹与全局 CLAUDE.md 一致。
- **改了什么**：仅补记本条（纯文档，无代码 / 机制变更）。
- **验证**：本条与其上方「changelog_guard hook」条、下方「交易逻辑建议必须有数理统计基础」条同处 [Unreleased]，CHANGELOG 记录纪律在项目内的足迹补齐（本体 2026-07-30 + 记录不用问 2026-08-21 + 工具强制 2026-09-02）。
- **回归风险评估**：纯补记、无任何行为影响；与全局 CLAUDE.md「CHANGELOG 记录纪律」原文一致，不改变该纪律的效力。

### 新增（CHANGELOG 记录纪律配工具强制：changelog_guard hook，2026-09-02）

- **为什么改**：2026-09-02 stability 撤销（4+ 文件改动）收尾时，AI 把「记 CHANGELOG」搁置、反问用户「要不要记」——全局 CLAUDE.md「CHANGELOG 记录纪律」（2026-07-30 立、2026-08-21 修订「记录不用问」）靠文本存在但靠 AI 记忆执行再次衰减（同 2026-08-18 用户对照实验：散文规定上下文压缩后衰减、工具强制不会）。用户两次纠正「CHANGELOG 任何改动都要记录，不要问」+「全局规则已经说明了，你为什么还忘记」。按项目方法论「文档规定必须尽可能配工具强制」，给这条纪律配 PostToolUse 提醒。
- **改了什么**：
  - 新建 `.claude/hooks/changelog_guard.py`（PostToolUse Edit|Write）：检测改动的文件属 git 跟踪的代码/文档（排除 tmp/ signals/ actions/ reviews/ archive/ cache/ 运行时数据、accounts.json / *.local 敏感配置、CHANGELOG.md / VERSION 本身），且 CHANGELOG.md mtime 早于被改文件（说明该文件比 CHANGELOG 新、可能未记）时，打印「按 CHANGELOG 记录纪律顺手记入（不用问用户）」提醒。走「决策时刻工具在场打印」层（第②层），只提醒不硬拦（该记 vs 不必记依赖语义判断、无法机械判定）。
  - `.claude/settings.json` PostToolUse 新增 `Edit|Write → changelog_guard.py` 注册。
- **验证**：hook py_compile 通过；settings.json JSON 合法；逻辑自测——改 `open_position_tiger.py` / `SKILL.md` → 提醒 True；改 `tmp/` / `signals/` / `CHANGELOG.md` / `accounts.json` / `*.local` → 不提醒 False。
- **回归风险评估**：① 纯新增提醒、不阻断任何工具调用，现有 hook（monitor_guard / secret_guard / live_auth_witness）不受影响；② mtime 启发式是噪声信号——批内改多个文件可能漏提醒或重复提醒，但提醒无害、AI 自行过滤（同批已记过忽略）；③ 项目标配 CHANGELOG.md 缺失时提醒（真该建），不存在误导。

### 新增（交易逻辑建议必须有数理统计基础，2026-09-02 用户立）

- **为什么改**：用户裁定方法论硬约束——stability 护栏撤销暴露出的根本问题：交易逻辑护栏建立在单笔事故复盘上、从未做过全样本统计验证。用户明确：**凡给出本项目的交易逻辑方面的建议（开仓 / 平仓 / 止损 / 止盈 / 移损 / 移盈 / 仓位 / 护栏 / 策略规则的新增、修改、撤销、调参），必须有数理统计的数学基础支撑**，不能建立在单笔交易的复盘决策上。
- **改了什么**：项目 `.claude/CLAUDE.md` 工作规则区新增「交易逻辑建议必须有数理统计基础」节——范围（交易逻辑建议全类别）、为什么（交易盈利靠全部交易总和、单笔复盘只触发规则不证明规则，stability 反面典型 + 08-27 止盈闸回放正面先例）、怎么用（提建议先问「有无统计依据」，无则标注待验证假设）、工具强制评估（认知判断类、无法脚本硬拦、按第③层决策时刻在场打印落地，trading-strategy.md「护栏的统计依据与撤销」节已固化）、边界（数据获取 / 行情分析 / 执行类操作不受限）。
- **验证**：重读确认新增节已在 CLAUDE.md 规则区就位。
- **回归风险评估**：纯新增规则、无代码变更，不影响任何执行机制；与 trading-strategy.md「护栏的统计依据与撤销」节互为呼应（CLAUDE.md 管普适约束、trading-strategy.md 管 stability 撤销来龙去脉）；对「文档规定必须尽可能配工具强制」无冲突——本条已明确自身工具强制落点（认知判断类走第③层）。

### 移除（stability 企稳维持时长在场打印护栏，2026-09-02）

- **为什么改**：用户裁定（方法论元规则）——开仓护栏应建立在**对全体历史交易数据的数理统计**（回放有闸/无闸对比 P(EV>0)、总盈亏）上，不能建立在单笔交易的复盘决策上；**只要某护栏不提高整体 P(EV>0)，就没有必要保留**。stability 闸的三层来源（2026-08-17 MINIMAX −1.05R → 2026-08-21 07709 −1.18R → 2026-08-28 09988 −1.70R）**每一层都是单笔事故复盘驱动**，从未做过全样本回放对比——与已被 2026-08-27 回放撤掉的「止盈可达性硬闸」同型（`reviews/2026-08-27-gate-review.md`：被拦 35 笔平均 +0.30R 仍正期望、闸整体负贡献）。2026-09-02 实盘长飞（06869）当日 +10.7%（158.5→175.5），stability 连拦三笔净赔率 1.47/1.37/1.73 的合格入场（含两笔 1.5R+ 的强势突破 + 回踩企稳）——拦掉正期望交易的活例。
- **改了什么**：
  - `open_position_tiger.py` / `open_position_tiger_us.py`：移除 `_stability_minutes` 函数定义与 `stability_minutes` / `stability_warning` 字段输出。
  - **联动**：「同位置警告累计两条 = 降一档置信度」的脚本硬拦（`warnings_cumulative_2`）随之失去 stability 这条来源——当前非拦截警告仅剩 `warning_target_unreachable` 一类，该硬拦在无第二条来源前不再触发（脚本注释已标注；认知判断层面的「警告累计降档」纪律保留在 trading-strategy.md）。
  - `trading-strategy.md`：①「企稳判定记忆依赖已工具强制」节删 stability 在场打印项、标注撤销；②「同位置警告累计两条」节去掉 stability 举例、注明撤销；③ 新增「护栏的统计依据与撤销（2026-09-02 用户立）」节——方法论元规则全文（全样本回放取舍标准、单笔事故复盘只触发规则不证明规则、08-27 先例、撤销清单、企稳四要素保留边界、与「文档规定必须配工具强制」的层次区分）。
  - `SKILL.md` 开仓前自查清单「置信度 = 🟢 高？」项：非拦截警告举例去掉 stability、注明撤销后来源。
- **验证**：两脚本 `py_compile` 通过；AST 代码级确认无 `_stability_minutes` / `stability_minutes` 引用（仅注释历史描述）；实跑一次（午休时间闸拒单路径）输出 JSON 确认无 stability 字段、不会误下单。
- **回归风险评估**：① **企稳四要素（真支撑 / ≥2 次测试 / 维持 ≥10 分钟 / 确认事件）本身保留不动**——它是右侧入场形态判据（认知判断类），撤销的只是「维持时长机械化在场打印」那一层，右侧纪律未弱化；② 「警告累计 ≥2 条硬拦」实际不再触发——但该规则 2026-08-28 的原始形态本就是「认知判断 + 自查清单」层、脚本硬拦是 2026-08-31 T130 补的，回退到「只有止盈越日高一条警告来源、不触发硬拦」不构成对原始纪律的破坏；③ 若未来新增其它非拦截警告来源（如新的结构类警告），`warnings_cumulative_2` 会自动重新生效（字段自动收集机制保留）；④ 单笔事故复盘仍会触发规则（发现问题、提出护栏假设），只是不再作为规则长期成立的依据——本条不改变「文档规定必须尽可能配工具强制」对已成立纪律的约束力（两者层次不同，已在新节写明）。

### 变更（撤销项目级 keep-awake skill，防睡眠脚本与规定并入 trade，2026-09-01 22:26）

- **为什么改**：用户裁定。项目版 keep-awake 自 2026-07-27 起已无独立触发（SKILL.md 无 frontmatter、功能并入 trade 盯盘流程），单独留着与全局通用版（`~/.zcode/skills/keep-awake`、`~/.claude/skills/keep-awake` 同名同路径体系）形成同名遮蔽歧义——尤其 2026-09-01 建 `.zcode/skills → ../.claude/skills` 软链接后，ZCode 里项目级副本被全局版遮蔽、名义上是「项目 skill」实际读的是全局版。裁定：不留同名目录，相关规定和脚本并入 trade、撤销项目级 keep-awake。
- **改了什么**：
  - 脚本迁入 `trade/scripts/`（逻辑逐行保留、仅路径重推与迁入注释）：`keep-awake/scripts/on.sh` → `trade/scripts/keepawake_on.sh`（手动备用启用，盯盘主链路仍是 preflight 内联启用、不经脚本）；`keep-awake/scripts/off.sh` → `trade/scripts/keepawake_off.sh`（引用计数版 + 停盯时间闸原样保留；`REG_FILE` 由三层 `../../../` 改四层 `../../../../`（trade/scripts 比旧目录深一层），`STOP_GATE` 改同目录 `$SCRIPT_DIR/stop_gate.py`）。
  - 规定并入：`monitoring.md`「多会话并行」节新增「防睡眠机制」段——原 keep-awake SKILL.md 的有效内容（2026-07-24 复盘根因、AC 电源前提、preflight 无条件启用、停盯 off 收尾纪律、引用计数、2026-08-24 T118 停盯时间闸、手动备用 on.sh）全部收拢入内。
  - 引用更新：`trade/SKILL.md:143` 停盯第一动作命令改 `bash .claude/skills/trade/scripts/keepawake_off.sh`；`monitoring.md:283` 跨会话副作用句路径同步；`preflight.py` 防睡眠注释块路径与沿革更新；`settings.json` `permissions.allow` 两行（off.sh / on.sh）改新路径。
  - 删除 `.claude/skills/keep-awake/` 整目录（含 `scripts/__pycache__/keepawake.cpython-311.pyc` 死缓存——对应模块 2026-08-09 前已删，仅剩缓存）。
- **验证**：两新脚本 `bash -n` 通过；`preflight.py` py_compile 通过；`settings.json` JSON 校验合法；`keepawake_off.sh` 路径推算干跑正确（`../../../../tmp/` 存在、`stop_gate.py` 同目录命中）；`stop_gate check` 只读干跑得盘中拒解（exit 2，美股距收盘 335 分钟）——时间闸真实生效旁证；全项目 grep 无活路径引用残留（CHANGELOG / TODO-archive / PLAN-parallel-monitor-HK 历史记录与各文件迁入沿革注释有意保留）；`.zcode/skills/` 软链接下现在只剩 trade。
- **回归风险评估**：① 引用计数与停盯时间闸逻辑逐行保留、仅路径重推，机制无弱化；② **在途会话过渡风险**——改动时点美股盘中、caffeinate 在跑，若有并行盯盘会话的上下文还载着旧版 SKILL.md（旧路径 off.sh），其停盯收尾会跑旧路径报「文件不存在」——届时该会话按新路径 `trade/scripts/keepawake_off.sh` 执行即可（目录已删、机制未变，一次性过渡成本）；③ `settings.json` 白名单已同步新路径，停盯跑新脚本不触发权限询问；④ 全局通用版 keep-awake（`~/.zcode` / `~/.claude` 两份逐字节一致）不受影响，任意场景手动启停能力保留在全局侧；⑤ ZCode 项目级 skills 经软链接只剩 trade，同名遮蔽歧义消除。

### 新增（`.zcode/skills/` 软链接指向 `.claude/skills/`，2026-09-01 21:54）

- **为什么改**：ZCode 的项目级 skills 目录在 `<repo>/.zcode/skills/`，与 CC 的 `.claude/skills/` 分属两套路径。此前 ZCode 在本项目只能读到根 `AGENTS.md` 指令（经 `AGENTS.md → .claude/CLAUDE.md` 软链接），trade、keep-awake 两个 skill 对 ZCode 不在场。建软链接后两类 agent 共用同一份 skill 权威源，单一源头、不产生第二份副本（与 2026-09-01 12:27 的 AGENTS.md 软链接同一模式）。
- **改了什么**：新建 `.zcode/` 目录 + 软链接 `.zcode/skills` → `../.claude/skills`（相对路径，与 AGENTS.md 软链接同风格）。
- **验证**：透过软链接可完整列出 `keep-awake`、`trade` 两个 skill 及其文件；`git status` 仅出现 `?? .zcode/`（git 不穿透软链接遍历，进仓库的只有链接本身）。
- **回归风险评估**：纯新增、未改动 `.claude/skills/` 任何内容；敏感文件（`trade/config.json`、`accounts.json`、`live-unlock.*.local` 等）仍按真实路径被 `.gitignore` 忽略，软链接不引入泄露面。注意：ZCode 侧同名 skill 用户级（`~/.zcode/skills/`）优先于项目级——用户级已有 keep-awake，项目里的同名副本会被遮蔽（内容为全局通用能力，遮蔽即正常）。

### 批量落地（TODO 盘外可完成项 8 条全清：警告累计硬拦 / 连败降频闸 / 动作时间戳 / 持仓闭环检查 / 白名单全量清单与可最小化面板 / 认领归一补漏 / 交易模式标签，2026-09-01 19:42）

- **为什么改**：用户指令「把待办事项全部处理完」。2026-09-01 盘后批量处理 TODO.md 中全部可在盘外代码落地的 8 条待办（T130 / T131 / T129 / T132 / T133 / T135 / T128 / T134，红色紧急度清零）；剩余 10 条全部依赖交易日盘中实测或数据积累（T126 / T123 / T121 / T122 / T119 / T120 / T124 / T125 / T116 / T4），盘外无可做部分。多项待办背景是 2026-08-28 实盘漏执行与 2026-08-31 复盘实证的「规矩写在文档里但盘中无工具在场必失守」类问题——本次全部按「文档规定必须尽可能配工具强制」原则落地为脚本硬拦 / 机械检查。
- **改了什么**（T 编号对应 `TODO-archive.md`「2026-09-01 批量处理」节，各条完成说明含完整验证记录）：
  - **T130 警告累计 ≥2 条硬拦（红）**：港美两 `open_position` 脚本净赔率校验后自动汇总非拦截警告字段（`warning_` 前缀 / `_warning` 后缀），同单 ≥2 条拒单（`blocked_by:"warnings_cumulative_2"`）；顺带补齐美股版 `warning_target_unreachable`（此前缺失、该防线在美股永不触发）。
  - **T131 一级降频线连败闸（红）**：`trade_utils_tiger.py` 新增 `update_losing_streak()`（平仓结算 R 写 `tmp/losing_streak.json`，跨日累计口径对齐 review-and-evaluation.md 规则原文）+ `check_losing_streak_gate()`；两 close 脚本平仓后自动更新、两 open 脚本前置闸（连败 ≥3 或 ≥2 且亏满型且最近亏损在今日 → `blocked_by:"losing_streak"` 拒单，`--force` 用户决策覆盖）；新建 `update_losing_streak.py` CLI 供 BRACKETS 自动触发平仓的补记场景。五场景冒烟全过。
  - **T129 动作时间戳插入修（黄）**：`log_action.sh` awk 从「NR==2 固定行号」改「首个 🟢🔴🟡🔵 开头标题行后插入」（08-31 事故形态实测修复），无 emoji 标题兜底追加末尾 + 警示。
  - **T132 持仓闭环检查（黄）**：新建 `actions_check.py`（当日 actions 开平配平 + 账户实持核对 + 已平仓未补记的补记指引）；挂进 `resume.py` 收尾（中断恢复固定动作）与 `monitor_unregister.sh`（停盯总结前置）；顺带修平仓记录无量行的配平（回退提取「开仓价（N 股」+ 全平核销）。
  - **T135 认领归一冒烟（黄）**：确认当日认领表已全归一键、release 已过 `_norm_sym`；冒烟暴露并修复 3/4 位裸数字（`700`）不归一漏洞——裸数字（≤5 位）一律补 `HK.` 再补零。
  - **T133 白名单全量清单（黄）**：`proxy_guard.py` 节点全景改 `xpilot node list` 实查（失败回退 nodes.json）；提醒 IP 串改**全量可用清单**（支持老虎页「先清空 → 一次性全量粘贴」）；「已添加」同步从合并改**全量替换**（`replace_whitelist`，与老虎页操作对应）；accounts.md 运维口径同步。
  - **T128 可最小化提醒面板（绿）**：新建 `alert_panel.swift`（AppKit 面板：可最小化到 Dock、双 / 单按钮两模式、超时自动关、结果文件通信；编译产物 `tmp/alert_panel` gitignore、缺失自动 swiftc 现场编译、无 CLT 回退 osascript）；`proxy_guard` 确认窗与 `monitor_watcher` 告知窗双载体切换（watcher 面板化后不再阻塞主循环）。
  - **T134 交易模式标签（绿）**：trades CSV 新增 `mode` 列（signal / auto_paper / auto_live / shadow，老 CSV 按 shadow 列兼容推断）；58 笔港股全量回溯打标（22/20/14/2，分组 +0.497R / +0.271R / −0.390R 与 08-31 复盘分阶段结论一致）；`review.py --mode` + 【按模式分组】表（分阶段统计从日期近似切分改为 mode 分列）、`bayes_evolution.py --mode`（子集图独立命名不覆盖全样本图）；review-and-evaluation.md 新增复盘必含项、example CSV 更新。
- **验证**：全部 13 个改动脚本 py_compile / bash -n 通过；T131 五场景冒烟、T129 三场景、T132 三日文件实测（含当日闭环通过 + 08-27 补录文件解析）、T135 三子命令冒烟 + 等价类断言、T133 `--dry-run` 实查 6 节点、T128 面板 pending→result 链路（2 秒超时自动关）、T134 全量跑 + `--mode` 单跑 + example CSV 全过。
- **回归风险评估**：① 开仓脚本新增两道前置闸（警告累计 / 连败）只增拦不放松，既有放行路径不受影响；`--force` 通道保留用户决策权；② `log_action.sh` 插入位置变化只影响「首两行非标题」的非常规输入，标准格式输出不变；③ `actions_check` 是纯新增检查、不改写任何 actions 文件（补记仍由 AI 按指引执行）；④ `proxy_guard` 的「已添加」从合并改替换——若用户仍按旧习惯只加新 IP（不先清空）就点「已添加」，本地副本会与老虎页不一致（面板文案已明确新操作步骤，guard 漂移判定失真的场景可自愈：按提示重配一遍即可）；⑤ mode 列为可选列、老 CSV 与 `--shadow-only` 行为完全向后兼容（统一映射 `--mode shadow`）；⑥ `monitor_watcher` 面板缺失时回退旧 osascript 载体，无 Xcode CLT 的环境不降级。

### 变更（`.claude/rules/` 两条规则迁入 CLAUDE.md 内联、删除 rules 目录，2026-09-01 12:38）

- **为什么改**：用户指令迁移。rules 文件的加载依赖 CLAUDE.md `@` 引用（CC 解析、ZCode 等其它 agent 不解析 `@` 引用），内联进 CLAUDE.md 后两类 agent 都能直接看到规则全文，加载机制更稳；也减少一层目录与引用维护（全局「rules 文件必须在 CLAUDE.md 中 @ 引用」的核对负担随之消失）。
- **改了什么**：① `.claude/CLAUDE.md`「工作规则」节由两条 `@` 引用改为内联全文——`verify-facts-before-stating.md`（陈述前先验证）与 `output-and-writing-style.md`（输出与写作风格）两条规则内容一字未动、仅标题各降一级（`#`→`##` / `##`→`###`）以嵌入文档层级，节标题去掉「显式引用」字样并注明迁移来源；② 同文件「文档规定必须尽可能配工具强制」条文的文档位置列举去掉 `rules/*.md`（目录已删，避免指向不存在路径）；③ `trade` skill `references/monitoring.md` 对 `.claude/rules/output-and-writing-style.md` 的引用改为指向 `.claude/CLAUDE.md` 工作规则「输出与写作风格」节；④ 删除 `.claude/rules/` 目录（含上述两个文件）。项目根 `AGENTS.md` 软链接指向 `.claude/CLAUDE.md`，自动同步、无需单独改。
- **验证**：全项目 grep `.claude/rules` 仅剩 CLAUDE.md 迁移来源说明一处（有意保留的溯源记录）；经 `AGENTS.md` 读出的是新内联内容，软链接同步正常。
- **回归风险评估**：规则内容零改动、仅位置与标题层级变化，无行为回归；`@` 引用机制删除后 CC 侧由内联全文替代（内容等价），ZCode 侧反而从不解析 `@`（规则原本就不在场）变为直接可见——一致性提升。

### 新增（项目根 AGENTS.md 软链接指向 .claude/CLAUDE.md，2026-09-01 12:27）

- **为什么改**：ZCode 等以 AGENTS.md 为项目指令入口的 agent 找不到 `.claude/CLAUDE.md`（CC 的私有路径），本项目规则对这类 agent 不在场。建立软链接后两类 agent 共用同一份权威内容，单一源头、不产生第二份副本。
- **改了什么**：新建项目根软链接 `AGENTS.md` → `.claude/CLAUDE.md`。目标用相对路径，git 只跟踪链接本身（不复制内容），clone 到任何机器均可解析。
- **验证**：`readlink AGENTS.md` 输出 `.claude/CLAUDE.md`；读 `AGENTS.md` 能完整解析出 CLAUDE.md 全文。

### 修复（pool_claim 裸数字代码不归一 HK. 前缀致两会话重复认领，2026-09-01 09:35 实录，2026-09-01 12:00 修复）

- **为什么改**：2026-09-01 早盘多会话并行盯盘实录——会话 2 用裸数字格式认领 `01211`、本会话（会话 3）用 `HK.01211` 认领，`_norm_sym` 只做大写与补前导零、**不补 `HK.` 前缀**，两个字符串作为不同键分别登记成功——同一标的被两会话同时持有，认领互斥失效（互斥语义依赖「键唯一」，键不归一则互斥形同虚设）。
- **改了什么**：`_norm_sym`（[pool_claim.py](.claude/skills/trade/scripts/pool_claim.py)）增加裸 5 位数字归一规则——`01211` → `HK.01211`，与 `HK.01211` 同键；验证 7 种输入形态（`HK.01211` / `01211` / `hk.700` / `00700` / `MU` / `us.mu` / 带空白）全部正确归一。
- **回归风险评估**：纯归一化增强、不影响既有 `HK.xxxxx` / `US.XXX` 键的登记与查询；当日重复认领的存量数据（会话 2 的 `02513, 09615, 01810` 裸数字键）仍以裸键存储，与新归一逻辑下的 `HK.02513` 等仍不同键——**遗留问题已记 TODO**（存量裸键认领清理 + release 时同样过归一）。

### 新增（港股历史全量复盘 · 2026-08-31：56 笔入账 + 01888 08-27 平仓补录 + 一级降频线漏执行实证，2026-08-31 12:50）

- **为什么复盘**：用户指令「复盘一下到目前为止的所有港股交易」。全量对账 `signals/` + `actions/` 全部 HKT 文件后发现两件事：① 08-27 01888 开仓后当日盯盘 13:47 中断、平仓事件漏记 4 天（直到本次对账才发现）；② 08-28 当日三连败后仍开第 4 笔——一级降频线（2026-08-17 立「连败 3 笔当日停开新仓」）首次实际漏执行。
- **改了什么**：
  - **新增 8 笔实盘样本**（08-25 07747 / 08-26 07709 空 / 08-27 01888 / 08-28 00700×2 + 09988×2 / 08-31 02513）进 [reviews/2026-08-31-trades-hk.csv](reviews/2026-08-31-trades-hk.csv)（48→56 笔）+ **2 笔影子样本**（08-28 03308 / 08-31 03690，标 `shadow=1`）+ ts 版（时间戳 + 富途 1 分钟 K 回拉的 raw high/low 全量）。
  - **补录 01888 08-27 平仓**：[actions/2026-08-27-HKT-actions.md](actions/2026-08-27-HKT-actions.md) 按证据链补平仓记录——富途 1 分钟 K 实证止损 42.60 未触（开仓后最低 42.76）、15:34 首触 44.00（段高 44.22）→ BRACKETS PROFIT 腿（LMT @44.00）自动成交，净 +801 / **+1.34R**（推定口径、成交价可能更优；实盘当日订单因 live 解锁 24h 过期不可直查，已标注）。
  - **新增复盘报告** [reviews/2026-08-31-HKT-review.md](reviews/2026-08-31-HKT-review.md)：终局统计（N=56、EV +0.195R、P(EV>0)=88.6% 跌破 90%、频率派 CI [−0.12,+0.51] 跨零）、三阶段退化（signal +0.50 → 模拟 +0.33 → 实盘 −0.42R，实盘 15 笔 4 胜二项 P≈5.5% 越过显著线）、过程指标（η=0.50 三期不动）、7 张演化图各配四要素解读、科学选 f 四步（f 维持 2%、不亏约束全网格无解）、亏损归因四类、暂停建议（一级降频明日停开 + 建议半修整 1~2 日；二级线 σ 下界距 80% 只剩 2pp）。
  - 7 张演化图 png（`bayes_evolution.py --date 2026-08-31 --suffix -hk`）。
- **核心发现**：① P(EV>0) 九期连跌 97.4→88.6%、胜率十一连跌 59.5→51.8%——阴跌无反弹；② 剔 Top3 大胜（全是 7~8 月旧样本）后 EV≈+0.007——**实盘时代零大胜单，edge 引擎断供**；③ MFE 从 +2.14 降至 +1.66（盈利单行情吃不满）；④ 08-28 阿里#1 双警告（stability + 越日高）在案仍开仓——当天新立的「警告累计两条降档」无工具在场；⑤ **影子样本 2 笔纳入复盘（规则修订：2026-08-31 用户立「复盘所有港股交易 = 真实 + 影子」）**——两笔均 −1.000R（净口径统一实算，03308 当时记录 −1.330R 为旧毛分母、03690 为净口径），单独一组统计（N=2、EV −1.000R），主统计仍剔除影子行；两笔与同日真实交易同向同亏，指向当日判断框架失效。
- **改进项（复盘产出，待入 TODO）**：① 「警告累计两条降档」脚本硬拦；② 一级降频线工具强制（连败计数 + open_position 闸）；③ 盯盘中断恢复后回查持仓 + 补记平仓（01888 类漏记防线）；④ 「反抽乏力做空」可核验定义（02513 类）；⑤ 实盘小额个股单费用占比警告（08-28 阿里#2 费吃 88% 毛利类）。

### 变更（开仓挂单超时 8→30 秒 + log_action.sh 时间戳插入位置缺陷记录，2026-08-31）

- **为什么改**：2026-08-31 港股实盘 02513（智谱）做空实录——12 次挂单全部在 8 秒超时窗口内未成交被自动撤单（3 次提价规则拒 + 9 次超时撤），标的秒级跳动 + 逐笔稀疏，挂价总是瞬时错过。逐秒采样回放（2,338 点）实测：12 次挂单价格回到挂价最快也要 61 秒，8/10/20 秒窗口零改善；放宽到 30 秒可覆盖「1 分钟内回到挂价」的部分场景，且不至于让挂单脱离监控太久（60-90 秒期间盘口结构可能全变、成交时形态已不成立）。用户裁定直接改 8→30 秒、不加待办。
- **改了什么**：`open_position_tiger.py` 与 `open_position_tiger_us.py` 的开仓主单成交轮询 `check_order_filled_tiger/_us(config, order_id, timeout=8)` → `timeout=30`（调用点带注释说明依据）。平仓脚本的 `timeout=12` 不动（平仓是风控动作、触发价=现价的循环逼近语义不同，超时短反而保证循环快速推进）。
- **验证**：两文件 `ast.parse` 语法过；grep 确认全项目开仓链路无残留 `timeout=8` 调用点（`check_order_filled_*` 函数签名默认值 8 保留——其它调用方平仓链路不受影响）。
- **回归风险评估**：① 超时只影响「挂单后等多久撤」——8 秒变 30 秒意味着未成交场景下脚本总耗时 +22 秒，期间 AI 阻塞在轮询、无法做别的分析（本就是单线程动作脚本，可接受）；② 挂单在场 30 秒期间价格结构可能变化、成交时形态与拍板时不一致——这是用户知情选择的取舍（12 次全部错过 vs 偶尔形态漂移，前者代价明确更大）；③ 模糊失败 / cross-trading / Invalid 被拒等路径行为完全不变（超时只作用于「已提交未终结」状态）；④ 平仓链路 timeout=12 未动，止损循环响应速度无回归。
- **同日另记**：`log_action.sh` 时间戳插入位置缺陷（awk NR==2 对「框线+空行」输入格式把时间行插到标题前 → account_status 解析平仓节 ts 为空 → 持仓推导误报残留）——当日已手工修复 actions 文件，脚本缺陷记 TODO T129（黄色紧急度）。
- **同日新建**：项目根 `candidates.md`（候选方案登记，与 CHANGELOG 互补：记「想做但被搁置的候选」）——首条 C1 = 开仓挂单超时后 modify 改价追价的候选（含支持/反方理由与评估判据：30 秒新政攒撤单样本后统计「价格 30-60 秒后回来型」占比再定）。

### 变更（TODO 全量批量处理：T126 当日订单过滤修复 + T121/T122/T127 规定工具强制落地 + T116/T120/T123 可做部分完成——4 条归档、3 条收窄，2026-08-30 23:19）

- **为什么改**：用户要求「把所有待办处理完」。逐条评估后按「能否当日完成」分两类处理——代码 / 文档类当场落地（T126 误报根因修复、T121 自问平仓在场打印、T122 reason 提层、T127 停盯总结范围提醒、T123 short 版购买力函数、T120 回填脚本、T116 实测状态刷新），依赖交易日实测或数据积累的（T126 盘中验证、T123 实盘复核、T119 对账、T125 样本、T4 三层互斥实跑等）收窄为剩余项保留。
- **改了什么**：
  - **T126（根因修复，归档）**：`trade_utils_tiger.get_today_orders_tiger` / `trade_utils_tiger_us.get_today_orders_us` 从裸 `tc.get_orders()` 改为取回后按 `order_time`（毫秒时间戳）转本地日期过滤当日（口径与 trade_mutex._order_local_date 一致：港股北京日期、美股跨夜归组；无时间戳订单保守保留、宁多不漏）——08-29「单持仓违规」告警误报根因（08-04/05 旧单混入当日净口、净口恒 +1）消除。过滤下沉到函数本体，monitor_segment 第四层兜底、止损价提取、stop_gate 等全部调用方一并受益（此前只有 trade_mutex 闸门侧 08-20 修过、采样侧没同步）。美股版同源同病一并修（查单失败仍上抛由闸门保守拒开，行为不变）。mock 单测过（当日留 / 昨日与 25 天前剔除 / 无时间戳保留）。
  - **T121（规定落地，归档）**：`open_position_tiger.py` / `open_position_tiger_us.py` 净赔率校验后固定输出 `self_check_close_question` 在场打印（2026-08-25 用户立的「开仓前自问平仓」检查）——做多问「已持多仓会不会想先卖等回落接回」、做空问「已持空仓会不会想先买回等更高再卖」；答「会」→ 不开仓、答「不会」→ 依据栏写理由后照常下单。工具强制评估结论写进脚本注释：纯认知判断类、不做脚本硬拦，按第②层「决策时刻工具在场打印」落地。
  - **T122（改动落地，归档）**：两开仓脚本全部失败路径的首层 `error` 追加「：+ 首个带非空 reason 的失败档原因」（原首层只有「开仓全部失败（尝试 N 档）」、reason 藏 failures 数组要多挖一层）。
  - **T127（规定落地，归档）**：`monitor_unregister.sh`（停盯收尾最后一步）注销 + 释放认领后固定打印「停盯总结范围提醒（T127）」——总结只总结本会话自身的标的池 / 交易 / 影子仓 / 采样分析执行情况，禁止混入其它会话交易与账户全口径数字。强制层级说明写进脚本注释（总结是纯对话输出、PreToolUse 无处挂，只能用在场打印层）。
  - **T123（部分完成，收窄）**：`trade_utils_tiger.py` 新增 `get_short_buying_power_tiger`——与多头版 `get_buying_power_tiger` 共用新抽的 `_buying_power_impl`，short 用 `get_contract().short_initial_margin`（SDK Contract 字段实存、已核源码；查不到按 1.0 全额最保守）——做空预算不再借用 long_initial_margin 口径。剩实盘解锁后复核 07747 真因。
  - **T120（机制落地，收窄）**：新建 `scripts/blocked_backfill.py`（add 登记被拦决策点 + 同键去重 / backfill 富途 1 分钟 K 回拉决策点后 60 分钟高低收 / stats 汇总假想 R 分布），数据文件 `tmp/blocked_decisions.csv`。假想 R 口径为乐观上限（最优极值离场）、止损触及行单独标（真实场景大概率 -1R，stats 提示保守版算法）。空数据路径自测过。剩日常执行攒 ≥30 个被拦点后重标定门槛。
  - **T116（文档刷新，收窄）**：`auto-mode.md` 港股段（08-03/08-05 旧机制描述后）与美股段（08-05 SPY 实测后）各补「2026-08-23 双腿 BRACKETS 机制起实盘实测通过」段——五项已过（LMT+双腿提交 / PROFIT 腿落成形态 / BRACKETS 联动作废 / 交替逼近平仓 / 限价超时撤单，08-26~29 实盘实录）、三项未测（移盈 modify / 补设 STP / 止损铁律恢复链）显式标注为 T116 剩余，消除「实测状态停在旧机制、与实际脱节」。
  - **配套**：TODO.md 重写（活跃 10 条——4 条收窄后的验证项 + T119 / T120 / T124 / T125 / T116 / T4；红色紧急度清零）、TODO-archive.md 新增「2026-08-30 批量处理」节（T126/T121/T122/T127 完成归档 + T116/T120/T123 已更新留痕）。
- **验证（全部实做）**：① 四个改动 Python 文件 `ast.parse` 语法过、monitor_unregister.sh `bash -n` 过；② T126 过滤逻辑 mock 单测（4 条订单：当日 1 留、25 天前与昨日 2 剔、无时间戳 1 保守留 → 结果 2，符合预期）；③ blocked_backfill.py 空数据路径自测（stats 输出「样本 <5 先攒数据」）；④ SDK 字段核实——`TradeClient.get_orders` 签名有 start_time/end_time（本次选取回后过滤而非传参过滤：与 trade_mutex 既有口径一致、且对「API 传参与 SDK 版本兼容性」零依赖）、`Contract` 源码确认 `short_initial_margin` 字段实存；⑤ auto-mode.md 两处实测状态 grep 确认已生效。
- **回归风险评估**：① T126 过滤后 monitor_segment 持仓状态行「最新止损价」提取——本就应只看当日活动止损单（query_stop_prices 只认活动状态 STP、时间上今日挂的单才可能活动），旧单都是终态不会被选中，过滤对其无行为改变；唯一理论风险是「跨日仍活动的 GTC 止损单被误滤」——本项目止损单 TIF 全部 DAY（trade_utils docstring 记录），且虎 get_orders 默认窗口本就有限，风险可忽略；② 闸门口径②③（today_open_exposure / active_open_orders）在 open_position_tiger.py 里喂的是 `get_today_orders_tiger` 返回（现已被函数过滤）+ trade_mutex.blocked 内部再过一次 `_today_orders`——双重过滤幂等、无叠加偏差；③ T121/T127 在场打印是纯输出字段 / echo 行，不改判定逻辑；T122 只改失败输出的 error 文案拼接，成功路径零改动；④ T123 抽公共实现后 `get_buying_power_tiger` 原签名 / 行为逐字段不变（仅错误提示文案加 direction 标注），现有调用方（open_position_tiger.py 购买力降档）无感知。

### 变更（TODO 全量时效性排查：T5 归档 + T116/T119/T120/T122/T124/T125 六条改写 + MEMO M2 更新——清理税闸停用后失效维度与已完成未收口条目，2026-08-30 22:18）

- **为什么改**：用户要求「排查待办内容是否过时、结合整个项目所有文件核对所有待办事项的逻辑是否还成立」。逐条核对 TODO.md 全部活跃待办（T4/T5/T116/T119-T127）与 CHANGELOG、`config.json`、脚本源码（trade_utils_tiger / trade_mutex / monitor_segment / open_position 等）、actions/signals 实录、tmp 运行数据后发现 7 条存在时效性问题——最大根源是 **2026-08-28 印花税闸整体停用**（`stamp_tax_gate_enabled: false`）使 3 条待办的税闸维度全部失效，另有 3 条的实测验证实际已在 08-26~29 实盘发生但条目未收口。
- **改了什么（按处理类型归组）**：
  - **归档 1 条（T5）**：实盘误开防护的「首个实盘交易日补验授权解锁后查询链路通」——08-25~28 四个实盘交易日（07747/07709/01888/00700/09988/NVDA/CRM 全链路实盘成交）已隐式完成该验证，归档留判定依据。
  - **删失效维度改写 3 条（T119 / T120 / T124）**：T119 开仓门槛对账从「1.2 门槛 + 1.2% 税闸双阈值」改写为纯 1.2 门槛口径（对账基线改为 08-28 起「1.2 门槛 + 无税闸」时代，补净口径切换警示），并把已发生的门槛实跑素材（08-26 放行 / 08-29 拒单实录）并入；T120 被拦决策点回填删「税闸不过」拒绝维度（被拦原因只剩净赔率一项），补与 T125 影子交易的分工说明（被门槛拦 vs 被互斥闸拦）；T124 税闸专项对账三条跟踪动作全部随闸停用失效，收窄为唯一残留关切「停用后新开个股单的印花税损占比观察」（T119 对账时顺带看）。
  - **已完成子项收口 2 条（T116 / T122）**：T116 限价单 + 止盈单 7 个实测子项中 4 项（① LMT+双腿提交、② PROFIT 腿落成形态、③ BRACKETS 联动作废、⑤ 交替逼近平仓、⑦ 限价超时撤单）已按 08-26~29 实盘实录核实通过（比原计划的 paper 验证更强），收窄为剩 ④ 移盈 modify、⑥ 无止损单平仓补设 STP、⑥b 止损单铁律恢复链三项 + auto-mode.md「实测状态」节刷新（该节停在 08-03/08-05 旧机制描述）；T122 做空提价规则的 live 实测已于 08-26 通过（HK.07709 实盘空单 LMT @39.06 成交、「挂 ask 符合提价规则」在生产网关实测），收窄为剩「被拒 reason 提到首层 JSON error 字段」一项（核实源码首层 error 仍只有「开仓全部失败」、reason 藏在 failures 数组）。
  - **进度更新 1 条（T125）**：影子交易 08-28 首跑实录并入——1 笔完整影子交易（HK.03308 −1.33R）落仓链路通、但当日三次被拦只落一笔（shadow_hint 已于 08-28 12:40 强化、效果待再验），复盘链仍未实测、样本仅 1 笔。
  - **MEMO.md M2 更新**：原「样本 ≥30 笔后重标定打折系数」停在 08-17 的 15 笔计数——实际 08-24/25 门槛下调复盘已用 48 笔全量样本实算（兑现率 0.62R、推翻旧 25% 标定），触发线已满足且重标定目的已被该实算实质覆盖；条目更新为剩口径收尾（下一轮复标须全用净 max_loss 口径、与 T119 对账合并执行）。
  - **配套**：TODO.md 头部说明段补本次批量处理记录；TODO-archive.md 新增「🟢 绿色紧急度·2026-08-30 待办时效性排查」节（T5 完成归档 + 六条已更新留痕，原条目正文与判定依据保留可回溯）。
  - **核实后不动的条目**：T4（三层互斥实跑验证——四验证点无系统核对记录，补 08-28 多会话日未核对注记）、T121（自问平仓检查未落地）、T123（short 版购买力查询未做）、T126（monitor_segment 第四层检测仍喂未过滤订单流、误报路径现行）、T127（monitor_unregister.sh 无范围提醒句）——逻辑全部成立、无需改动（T4 补一条注记）。
- **验证（全部实做）**：① 税闸停用状态核实——`config.json` 读 `stamp_tax_gate_enabled: false`、CHANGELOG 2026-08-28 13:10 条明文三工具停用范围；② 实测证据核实——T116 四子项 / T122 实测 / T125 首跑 / T5 隐式验证全部对 actions/08-26~29 文件逐条 grep 到实录原文；③ T122 ④ 未做的源码核实——`open_position_tiger.py` 首层 error 仍为「开仓全部失败（尝试 N 档）」；④ T126 现行性核实——`monitor_segment.py` 第四层检测直接喂 `get_today_orders_tiger` 原始返回（未过 `_today_orders` 过滤，闸门侧 08-20 已修但采样侧未同步）；⑤ 改写后逐条重读 TODO.md / TODO-archive.md / MEMO.md 确认条目编号、时间戳、状态标记格式正确。
- **回归风险**：① T124 收窄后「窄止损单专项对账」失去独立跟踪位——但其关注点（个股单费用占比）已并入新正文与 T119 对账，且 ETF 优先认领闸（08-28 立）在池侧拦截大部分个股开仓，风险敞口有限；② T116 剩余三项（移盈 / 补设 STP / 铁律恢复链）至今无真实触发场景——收窄后条目明确要求「刻意构造或等自然场景验证」，防止「实测状态已刷新」掩盖三项未验事实；③ T119 对账基线改为 08-28 起——08-26/27 两日样本（税闸尚在）归入过渡期、不入对账基线，样本数按 7 笔起算，与原「双阈值时代」口径不同属有意区分；④ 无代码改动，全部为文档 / 待办数据变更，不触碰任何交易逻辑。

### 变更（T118 停盯边界守卫收尾：monitor_segment / ws_segment 补「空仓/无信号 ≠ 停盯理由」在场打印 + 两交易日实盘验证闸不误拦，2026-08-30 18:55）

- **为什么改**：TODO T118 主体（stop_gate.py 时间闸 + 三收尾入口接入 + futu_ws_segment 提醒行）已于 2026-08-24 落地，剩两项收尾——① `monitor_segment.py` / `ws_segment.py` 两采样脚本的停盯边界提醒行未同步补「空仓 / 无信号 ≠ 停盯理由」句（当时只补了主力源 futu_ws_segment，这两脚本作为备用采样链在场打印缺位）；② 「首个完整交易日实盘验证闸不误拦正常收盘收尾」未核实。本次补齐①并完成②核实，T118 整体闭环归档。
- **改了什么**：① `monitor_segment.py` / `ws_segment.py` 停盯边界提醒（>5 分钟分支）各补一句「⛔ 同时：空仓 / 无信号 ≠ 停盯理由——盯到用户喊停或收盘（取先到），停盯收尾脚本已受 stop_gate 时间闸硬拦（2026-08-24 立，T118）」——与 futu_ws_segment 主力源措辞逐字一致；两脚本 py_compile 语法验证通过。② 实盘验证核实（不改代码）：2026-08-26（`tmp/seg_94_26.log` 11:56 段）与 2026-08-28（`tmp/seg_cur.log` 11:55:34 段）两个完整盯盘交易日的港股收尾段 log 均打印「距HK停盯边界 4 分钟（≤5）」——收尾时点落在收盘窗口内，stop_gate 按 `hk_close_window` 放行、未误拦正常收盘收尾，期间无盘中提前停盯违规，验证条件满足。
- **回归风险评估**：① 补充的是纯打印行（提醒句），不改变任何判定逻辑 / 阈值，对采样链零行为影响；② 停止盯盘收尾时点若在收盘窗口外（>5 分钟），闸会拦——这是 T118 设计行为（用户喊停加 `--force` 放行），非回归；③ stop_gate 现时点（周日盘外）实跑 `outside_trading_hours` 放行正常。

### 变更（全项目逻辑矛盾审计修复：20 处口径分裂 / 残留旧规 / 失效引用统一到现行口径，2026-08-30）

- **为什么改**：用户要求全面审计项目所有文件的逻辑矛盾（特别是交易策略），审计发现 20 处——含 2 处直接影响开仓判定的口径分裂（净赔率公式双版本、EV 负值表述与自身公式数学冲突）、多处 2026-08-23/28 规则改版后未同步的旧文案残留、失效交叉引用与数字不同步。按「以最新规定为基准」统一修复。
- **改了什么（20 项，按主题归组）**：
  - **开仓判据口径**：① `SKILL.md` 开仓自查清单 EV 判据公式从「毛止损距分母 + 单边费百分比近似」改为「赔率三阶段净口径」（净初始预期赔率 = [|止盈−参考价|×预期仓位 − 开仓边费 − 止盈价平仓边费] ÷ 初始预期 max_loss），消除与脚本硬校验（odds_at_current 净口径）的口径分裂——该分裂正是文档明令杜绝的「毛净口径混用」事故类型的现行版；② `trading-strategy.md` 门槛沿革「1.2 边界单在胜率 <55% 时 EV 为负」改为实算正确表述——不打折口径 EV = 2.2p−1、EV 转负须 p<45.45%；打折口径（兑现率 0.62）EV 转负须 p<57.3%、p=55% 时 EV=−0.04 为负，两种口径分开写明（原句与自身 EV 公式数学冲突）。
  - **平仓 / 止损理念衔接**：③ 三处（auto-mode 两处 + signal-mode 一处）平仓理由「做多时回踩前高遇阻（上涨遇阻）」改为「做多持仓冲前高遇阻回落 + 形态确认」并引用「三角形持续形态」节——原措辞与 trading-strategy「做多时回踩前高/支撑不破 = 持有时刻」直接矛盾、且单次遇阻即平与「遇阻 + 形态未破不平」矛盾；④ 「止盈完全靠盘面实时判断」节改题「止盈的决策框架：盘面实时判断为主、条件单为兜底」，新增「无固定止盈位 vs 开仓必挂止盈单」的分工澄清（止盈单 = 兜底 + 初始预期参考，趋势延续须及时移盈、92% 移损出场统计的含义解释），脚本注释引用同步；⑤ 止盈可达性两节（2026-08-03 三条检查 vs 2026-08-27 越日高裁量）加互引衔接——明确「三条检查 = 默认从严基准、趋势日三条件齐备 = 例外」的判别顺序；⑥ 「连亏不是停手理由」补边界段——与复盘「是否暂停交易」两级判据（连败 3 笔当日停开等）的分工写明：本条管盯盘心态、复盘线是给用户的建议，两轨不矛盾；⑦ SKILL.md 持仓不过夜纪律 + 边界表三行补「范围限定 = 仅 AI 当日新开仓位、不含 config.resident_positions 常驻持仓」——消除「平掉所有持仓」全称表述误平常驻长持（02800/07709）的风险；⑧ 「上升趋势做多位置」浅回踩判否处补 V 反例外互引（以板块共振已确认为前提，见「V 反趋势日两段式应对」②段），消除与该节的表面矛盾。
  - **动作体系四类化残留**：⑨ 六处「三类 / 3 类」改四类（SKILL.md 两处描述、signal-mode 两处自查、monitoring 一处、auto-mode 动作 emoji 行补 🔵 移盈）——2026-08-23 新增移盈后旧文案漏更，signal-mode 自查「三类格式」照查会漏移盈信号；⑩ 移损格式标题「（浮盈中上移锁利）」三处改为「止损永远放最新趋势反转位」口径、signal-mode 理由栏「锁利/保本」改为「新结构位依据（锁利只是顺带效果、不是目的）」——对齐 2026-08-07 用法 A 作废后的移损唯一用法；⑪ review-and-evaluation 三处批次体系表述改为现行口径（一笔交易 = 一次开仓到一次平仓；旧批次口径标注「仅适用 2026-07-30 前旧记录」）+ 新增 🔵 emoji 历史撞号解析规则（旧 = 加仓、2026-08-23 起 = 移盈、按日期区分）；⑫ auto-mode 影子交易结算描述「max_loss 毛口径分母」改净口径——与 shadow_trade.py 实际实现（2026-08-28 净口径）对齐；⑬ auto-mode 美股脚本描述「3bps 费」改为 fee_schedule.py 真实费率按股结构（旧笼统口径 2026-08-12 已废）。
  - **失效引用 / 工具名**：⑭ review-and-evaluation「见『信号模式总则』第 5 条」改指 SKILL.md「操作原则（公共）」第 3 条（旧编号已变、原引用指向无关内容）；⑮ 三处 `static` 命令改 `snapshot`（SKILL.md 启动序列 + monitoring.md + trading-strategy.md 热度榜流程——项目无 static 脚本，照跑会找不到命令）；⑯ risk-management 美股换算「exchange-rate 命令查」改为实际汇率取值途径（① 老虎 API forex_rate / ② 富途快照或 WebSearch 实测、③ 7.80 兜底常量仅降级用）——项目内本无此命令；⑰ monitoring.md 两个老虎 WS 旧节改写（「重要数据日优先 WebSocket」→ 默认富途逐笔 / 老虎备用；「优先用老虎 WebSocket 替代轮询」→ 留档标注不要按旧指引新起老虎轮询）——对齐 2026-08-18 主次反转，SKILL.md 两处清单措辞同步。
  - **数字 / 事实同步**：⑱ risk-management 09988 教训两处数字修正（税闸差 0.005→0.006 实算、实际净赔率 1.35→1.38 复核实算值，与 CHANGELOG 2026-08-28 归因修正段一致）；⑲ equity-baseline.local 头注释从「本机文件、已 gitignore、equity_guard 消费」改为退役口径（无消费者、文件现为 git 跟踪的历史产物）——对齐 2026-08-29 净值解禁裁定后的实际状态；⑳ monitoring.md 两处美股开盘首小时「21:30-22:30 北京夏令时口径」改为「09:30-10:30 ET = 北京夏令时 21:30-22:30、冬令时 22:30-23:30」——消除冬令时期间窗口误判一小时的风险（其余文档均双时制标注，此处是唯一单口径残留）。
- **验证（全部实做）**：① 每项修改后逐条 grep 核对（旧措辞清零、新内容在位）——20 项全部通过；② 修正数字全部实算复核（EV 转负阈值 45.45%/57.34%、p=55% 打折口径 EV=−0.041、税闸线 0.4%×114.0=0.456 差 0.006）；③ 改动的 open_position_tiger.py（仅注释行）py_compile 语法过。
- **回归风险**：① EV 判据公式改为净口径后，AI 心算自查与脚本硬校验口径一致（原分裂下自查偏乐观、脚本拒单——统一后无「自查过、下单拒」的空耗，方向是消除事故面、无既有修复被弱化）；② 常驻持仓例外补写后，「持仓不过夜」的执行范围明确缩小到当日新开仓——与互斥闸 / 第四层兜底检测的 resident_positions 白名单机制一致（本就是机制语义，只是条文补齐）；③ 止盈框架澄清不改任何机制（止盈腿照挂、移盈照做），只把「为什么 92% 统计与必挂止盈单不矛盾」写明；④ 其余为措辞统一 / 引用修复 / 数字同步，不改变任何现行规则语义。

### 变更（实盘净值解禁：废止「实盘净值禁止写入被跟踪文件」旧规 + equity_guard 全链退役，2026-08-29 17:50 用户裁定）

- **为什么改**：2026-08-29 `/commit` 敏感扫描命中 7 个文件的实盘净值 / 权益链数字（equity-log 恢复真值、accounts.md / CHANGELOG / trade_utils_tiger.py 注释的量级口径、TODO-archive / reviews / signals 的历史数字恢复），用户裁定放行——**实盘账户的钱只是个人全部财产的一小部分、不能完全代表个人财务状况，交易账户净值不属敏感财务信息，照常可写**。据此废止 2026-08-20 立的「实盘净值禁止写入被跟踪文件」整条旧规（含 2026-08-26 收紧的「量级口径同样不写」）。
- **改了什么**：① 项目 `.claude/CLAUDE.md` 专节改写为「实盘净值不属敏感信息、照常可写」（08-20 旧规 + 08-26 收紧的沿革保留、裁定理由与豁免边界写明）；② `equity_guard.py` 从 `.claude/settings.json` hooks 摘除、脚本头部标记退役（保留文件沿革留档、勿再注册）；③ `trade_utils_tiger.py` `note_equity_baseline` 退役为空操作、`load_equity_tiger` 两处调用点移除、docstring 同步；④ `preflight.py` / `resume.py` 的实盘防泄漏在场打印删除；⑤ `.gitignore` 删去 `signals/equity-log.csv` 与 `.claude/hooks/equity-baseline.local` 两条忽略（equity-log 恢复入库跟踪、黑名单文件不再产生）；⑥ `SKILL.md`（两处）/ `signal-mode.md`（权益更新节恢复「equity 本体照常写」）/ `auto-mode.md`（记录条目净值照常写）/ TODO T127（equity_guard 不可作候选）同步新口径。
- **不受裁定影响的敏感类别（照旧禁写）**：账户号打码、密钥 / token / 盐值、代理节点 IP / 订阅商域名体系 / 白名单拒绝报文 / 地域规避叙述（2026-08-23 中性化口径不变），以及全局规则中个人财务状况**描述性**信息（收入目标、资产负债全貌、财务紧迫程度等）——本豁免仅覆盖交易账户净值 / 购买力数字本身。
- **⚠️ 回归风险提醒**：① 本裁定使 2026-08-26 filter-repo 历史清洗中对「实盘净值 / equity-log 系列数字」的替换**方向反转**——今后新提交的净值数字将随推送重新公开可见（历史替换标记 `[equity-log值已移除]` 等仍是旧清洗产物、不影响新提交）；若日后反悔，净值类数字将再次需要历史清洗才能彻底清除，成本如实告知；② `signal-mode.md` 恢复「信号文件写 equity 本体」后，`signals/*.md` 正文将再现净值数字——这是裁定授权的预期行为，非泄漏事故；③ equity_guard 退役后写入侧无机械拦截，净值类数字的写入把关全靠本裁定口径本身（无回归对象——旧拦截的目的就是阻止现在被授权的行为）。

### 变更（停盯总结范围钉死为「只总结本会话」，2026-08-29 13:41 用户立）

- **为什么改**：2026-08-28 美股会话（标的池 SMH/AVGO）停盯总结时连环两错——① 把模拟账户「今日订单」接口（实为最近 100 笔、跨多个交易日）当成 8/28 当日盈亏，01888/02359/00100/MU/LITE 等历史交易全被错算进当日；② 把其它并行会话开的 NVDA/CRM/PLTR 三笔与港股动作混进总结、总结范围从美股跑到港股，被用户两次纠正（「让你盯的美股，你总结港股干嘛」「这 3 笔都是你这个会话开仓的吗」）。根因：多会话共用同一账户时「账户今日订单」是全池混合口径、不等于本会话战绩，而停盯总结的范围此前无明文约束，AI 顺手把能查到的都堆进去了。
- **改了什么**：`SKILL.md`「停盯总结」节新增范围约束段——停盯总结**只能总结本会话自身的盯盘任务执行过程和结果**（本会话认领的标的池、本会话开/平的交易、本会话的影子仓、本会话的采样与分析执行情况），禁止把其它并行会话的交易、其它标的池的盈亏、账户全口径数字混进来；用户明确要全局/跨会话汇总时才做全口径并标明数据来源与归属。
- **回归风险**：无——纯文档约束新增，不改任何代码与既有交易规则；原有「三阶段赔率必含」等总结内容要求不变。

### 新增（「参考价归纳公式、现价代入推理」理解框架留档——trading-strategy.md 赔率三阶段节新增专小节，2026-08-28 16:20 用户立）

- **为什么记**：用户在理解决策路径时逐步推导出完整心智模型（参考价 = 归纳公式的锚、现价校验 = 修正赔率的事前估计、两遍 1.2 判定 = 形态质量闸 + 防漂移闸），担心时间久了忘记细节重新陷入理解困难，裁定留档。
- **记了什么**：trading-strategy.md「赔率三阶段」节下新增「参考价与现价：一个归纳公式、一个代入推理」专小节——① 完整决策路径图（参考价归纳计划参数 → 现价代入推理过闸 → 成交价代入修正 → 平仓价代入落地）；② 核心心智模型（参数冻结、只换入场价变量——用户原话「一个用来归纳公式，一个用公式来推理」）；③ 两遍 1.2 门槛的分工（第一遍参考价口径判形态质量、第二遍现价口径防漂移，串联两道闸非重复计算）；④ 第二遍校验 = 修正赔率事前估计（滑点常态方向变差、刚过线单修正后跌破属常态偏差非 bug）；⑤ 三阶段差异归因读法（初始→修正差 = 漂移+滑点代价、修正→落地差 = 持仓过程代价）；⑥ 价格范围闸与净赔率闸的分层（宽松兜底拦离谱漂移 vs 精确闸拦边缘漂移）；⑦ 选档算法备忘（±2 档选净 M 最近 B、与简单向下取整的区别、实跑例 ETF B2200 选 800 股超取整档 700；用户指出 ±2 数学冗余、等价 ±1）。
- **验证**：选档「可比取整档大」的例子实跑核实（ETF lot200 距 3 B2200 → 取整 700、实选 800 净 M 2496）；个股例实测选 600（净费随股数涨、上邻档净 M 偏离更大）——备忘按实测结果修正原拟的个股例子。
- **回归风险**：零——纯文档新增（理解框架记录），不改任何代码与既有规则。



- **为什么改**：净口径改版落地时发现既有缺口——开仓脚本的执行顺序是「赔率硬校验（vs min_net_odds）在前、自动算仓位在后」，而净赔率公式依赖股数（费按成交额×股数算、按笔固定费摊每股随股数变），quantity=0 时股数未定、硬校验只能退「百分比估费（HK 0.18%双边）+ 毛分母」的每股近似口径——该近似看不见按笔固定费（佣金最低 15/边 + 平台费 15/边）在小仓位上的摊薄爆炸，实测 200 股例：近似口径 1.98 过门槛放行、真实净赔率仅 0.97（固定费占毛止损额 40%+）。用户裁定「当然要先算仓位才能精确计算出 max_loss」——按定义股数定了 max_loss 和净赔率才都是真数。
- **改了什么**：① 港美两版开仓脚本各做一次块重排——「自动算仓位（quantity=0）」段整体上移到「取行情 → 价格范围检查 + 净赔率硬校验」之前，费上下文从「quantity>0 才建」改为两路径统一 `build_fee_ctx`（真实费率、净分母），旧「quantity=0 退百分比估费」路径废止；② 拒单文案升级——报错自带仓位股数、净 max_loss 与口径说明（「按当前实价 + 真实股数 × 真实费率 × 净分母口径不达标」），并提示小仓位单固定费占比高的处理方向；③ auto-mode.md「下单失败处置」节补「执行顺序」说明块。
- **验证（全部实跑）**：① 两脚本 ast.parse 语法过、各只剩一处 calc_position_size 调用（无重复段残留）；② 端到端口径模拟——场景 A（equity 9000 → 自动算出 200 股 → 真实净赔率 0.965 < 1.2 应拦 ✅ vs 旧近似口径 1.979 会放行 = 缺口实证）；场景 B（equity 10 万 → 2400 股 → 1.410 ≥ 1.2 放行 ✅ 正常单不受影响）；③ 重排后变量定义顺序粗查（equity / lot_size 首用先于首赋的两处警告核实均为注释文字、非代码）。
- **回归风险**：① 拒单场景多查一次 equity + lot_size（只读 API、秒级）——原设计「校验挡掉的单不白查账户」的省查询考量被口径正确性取代（用户裁定）；② 小仓位单（B 小 / lot 大被迫选小档）开仓门槛实际收紧——这正是改版目的（旧口径放行的此类单真实 edge 为负）；③ 原位置残留段已删净（result_base 落档信息移至费上下文之后、拒单报错里可见），下游「显式传量风控校验」「购买力上限」逻辑未动。



- **为什么改**：用户问「max_loss 用 R 单位看不是 −1R 对吧」引出——旧口径分母是毛值（仓位×止损距），止损价精确成交时净亏损 = 毛 M + 开仓费 + 止损价平仓费，净 R 落到 −1.2~−1.8（实算 00981 −1.297、07747 −1.758，窄止损单费占毛 M 可达 54%），「冒 1R 风险」与实际亏 1.3R 脱节。用户裁定：**max_loss 加上止损价成交的手续费，让最大亏损不深于 −1R**——止损价精确成交时 R 恰好 −1.000，唯一可低于 −1 的只剩止损滑点（真实滑点、非口径问题）。
- **两个 max_loss 变量（用户口述定义落地）**：**初始预期 max_loss**（开仓前）= 预期仓位 × |开仓参考价 − 止损价| + 参考价开仓边费 + 止损价平仓边费；**修正 max_loss**（成交后）= 实际仓位 × |实际成交价 − 止损价| + 实际成交价开仓边费 + 止损价平仓边费（开仓时点算死、移动止损不改基准）。三阶段赔率分母统一：初始预期赔率 = [|止盈−参考价|×预期仓位 − 参考价开仓费 − 止盈价平仓费] ÷ 初始预期 max_loss；修正预期赔率同式换实际成交价/仓位（止盈价一般同预期、可修正）；实际落地赔率 = (毛盈亏 − 开仓费 − 实际平仓价平仓费) ÷ 修正 max_loss。两边手续费**分开按各自价格算再加总**（开仓价与平仓价不同、费不同）。
- **改了什么**：① `trade_utils_tiger.py`——新增 `net_max_loss(market, sec_type, shares, entry, stop)`（净 M 单一实现，港美通用）；`_net_odds` 分母改净口径（fee_ctx 齐备时分子扣开仓费+止盈费、分母=止损距+开仓费+止损费；quantity=0 股数未知时退旧百分比估分子、毛分母，算出仓位后复核）；`calc_position_size` 选档目标与 f_max cap 均改净口径（新增 sec_type/market/stop_price 参数，net 上界逐档下退校正，返回值多一个 net_ok 标志，旧调用四元组解包改为兼容——所有调用点已同步）；`trade_utils_tiger_us.py` re-export `net_max_loss`。② 开仓脚本（open_position_tiger / _us）——自动算仓位传齐净口径参数、输出 `max_loss_basis` 标注口径；显式量风控校验（`_enforce_explicit_quantity_risk`）改净 max_loss 判 f_max cap；降档成交的 `actual_max_loss` 改净口径 + `actual_max_loss_basis` 标注。③ `review.py`——R 分母改净 M（CSV 无止损价列，由 M/shares 反推止损价算止损边费、数学精确；做多 entry−d、做空 entry+d）；MAE_R/MFE_R 分母同步净 M；明细表「max_loss」列改「净M」；头尾口径说明更新；CSV `max_loss` 列仍记毛值（口径不变、避免历史数据迁移）。④ `bayes_evolution.py` 同口径同步。⑤ `shadow_trade.py`——影子平仓结算 R 分母改净 M（`close_shadow` 重算 fee_open/fee_exit/fee_stop 三边费、开仓条目「预估 max_loss」标注净口径说明）。⑥ 平仓脚本（close_position_tiger / _us）——`_attach_process_metrics` 分母改净 M（新增 quantity 参数与 `process_metric_basis` 字段，quantity 缺失退毛口径并标注）。⑦ 文档同步——trading-strategy.md「赔率三阶段」整节重写（含「两个 max_loss 变量」专段 + 为什么分母含费 + 口径切换对照）；risk-management.md（B/M 概念、选仓位、拆两笔 f_max 计算、利润=R×max_loss、价格范围公式）；auto-mode.md / signal-mode.md（开仓表格「预估 max_loss」改「初始预期 max_loss」净口径、平仓必填赔率定义、mfe/mae 口径、App 权威口径段）；SKILL.md（硬性护栏第 1 条、停盯总结三阶段赔率定义）；review-and-evaluation.md（Notation 与 R-multiple 定义重写：R_i≥−1 严格成立、历史数字与新口径不可直接比）。
- **验证（全部实跑）**：① 9 个改动脚本 ast.parse 语法过；② 止损价精确成交恰 −1R 单元验证（00981：净M 30771.7 = 毛 23725 + 开仓费 3540.3 + 止损费 3506.4，止损成交 R = −1.000000）；③ review.py 全量 48 笔港股样本新旧对照——胜率不变 0.562（R 符号不变）、胜赔率 1.249→1.016、败赔率 −0.765→−0.617、EV +0.368R→+0.302R、s 1.492→1.222；明细中 7 笔止损价离场单（#8/15/21/28/32/39/47）全部精确 −1.000、#48 为止损滑点单 −1.055；④ bayes_evolution.py 复跑出图正常（f=2% P(g>0) 63.2%）；⑤ 选仓位对比（equity 10 万 B 2000 距 3 lot 200 个股 100 元）：净口径 600 股/净 max_loss 1998 与旧毛口径 600 股同档、选档不变；⑥ 旧版 review.py（git HEAD 版）跑同一样本取旧口径对照值确认差异来源纯粹是分母口径。
- **回归风险与代价（如实记录）**：① 同一结构算出的赔率数字整体变小（分子扣费不变、分母加费），**min_net_odds=1.2 门槛语义偏严**——部分旧口径恰好过门槛的单会改判不达标；门槛是否下调由后续复盘兑现率数据定，本次不动 config（阈值标定属复盘层决策）；② 历史 R 值（2026-08-28 前的报告 / trades.csv 统计）与新口径不可直接比大小——复盘对照时须注明口径；③ 价格范围 [0.6,10] 的 80%/3/8 经验参数沿旧口径标定、未随净分母重标（risk-management.md 已注明，发现边界失准再调）；④ quantity=0 自动算仓位前的价格范围检查退旧口径（股数未知无法算绝对费额）、算出仓位后由净口径复核——该路径赔率数字偏乐观一层，实际下单前硬校验（odds_at_current 按真实费率+净分母）会拦住不达标单；⑤ ETF 净 M 相对增幅比个股大（免印花税但固定平台费占比高），窄止损 ETF 单的赔率数字降幅最明显（实测 ETF 例旧 11.3→新 7.3）。
- **影响面**：盘中即时生效——本次改动后所有开仓赔率计算、选仓位、f_max 校验、平仓落地赔率、复盘统计、影子结算全部走净口径；历史 trades.csv 不回改（毛值列口径不变、脚本自动算净 M）。

### 新增（ETF 优先认领闸——认领时只要存在可认领 ETF 就必须先认领 ETF、个股只在无可认领 ETF 时才能认领，`pool_claim.py` 硬拦，2026-08-28 13:30）

- **为什么改**：2026-08-28 上午实盘 00700 第二笔止损 −1R 里 **0.42R 是印花税**（同笔若为 ETF 只亏 −1.19R）——个股开仓的印花税损耗被当场量化。「港股优先 ETF」旧规只是偏好（税闸停用前闸过了就跳过 ETF 核查、停用后更无强制），用户立新规：认领标的时只要存在未被占用且类型为 ETF 的候选，就必须先认领 ETF，个股只有在不存在可认领 ETF 时才能认领。
- **改了什么**：① `scripts/pool_claim.py` 新增 ETF 优先认领闸——`_sec_types()`（复用 `classify_hk_security` 批量判类型）+ cmd_claim 锁内判定：候选里含「可认领 ETF」（本次新拿到或本会话池已持有、类型判定为 etf）时，本次候选中的个股（stock/reit）直接拒绝登记、输出 `🚫 ETF 优先认领闸…已拒 <个股>`；② 编辑过程中误删「登记段」（`if got: if mine: new_syms… else: claims.append… _save_claims`）→ 当日修复补回并验证（ETF 认领不再丢表）；③ 文档同步——SKILL.md 硬性护栏第 5 条「港股偏好 ETF」改「港股优先 ETF」+ 指向认领闸；monitoring.md「标的池自动划分」新增第 6 条。
- **闸口径（脚本内置注释详述）**：只拦「实际可认领」的 ETF（被别的会话认走的不算——互斥语义优先、不逼 AI 跨池抢）；本会话已持有 ETF 同样构成约束；类型判定失败（`?`）不拦（判不了不瞎拦，认领链路不因分类器故障停摆）；美股候选不参与（美股个股无印花税劣势）；拒绝是「本次不登记」而非全局拉黑——`release` 掉 ETF 后可再 claim 个股。
- **2026-08-28 午重构为「按盯盘方向」（用户裁定）**：初版强制把热榜全部未认领 ETF 灌进本会话池，导致「一认个股就要 release 一堆强加 ETF、池子巨大难盯」——用户明确否决全市场口径、裁定按盯盘方向：候选里 + 热榜里与候选**同方向**的 ETF 必须优先认，不强制灌全市场。新增 `_direction_similar()`（代码 2 位前缀判定，如 03067 恒科 ETF 与 03110 恒科 ETF 同属 03 段）+ `_hot_etf_candidates()`（热榜 ETF，供按方向匹配）。另修两处编辑期缺陷：① `held_etfs` 原实现漏了 `got`（候选内带的 ETF 没进约束 → 同批个股漏拦）——实测候选 `03067(ETF),00981(股)` 未拦后修复；② `release --all` 清空 released 清单时被「记放弃清单」段又塞回 removed——加 `not release_all` 条件修复。`released` 放弃清单新增（release 掉的 ETF 不自动认回，否则 release 后一 claim 又灌回来、个股永远放行不了）。
- **验证**：端到端场景（活会话文件 + 完整 claim 命令）——① 认个股 `00700`（无同方向 ETF）→ 放行；② 候选 `03067(ETF),00700(股)` → ETF 登记、个股拒 + 拦截提示；③ 池已持有 `03067(ETF)` 再认 `00700` → 拒；④ `release 03067` 后再认 `00700` → 放行（放弃不认回）；⑤ 认美股 `US.MU` → 放行（美股不参与）。测试数据已全部清理、认领表归空。
- **回归风险**：低——改动仅限认领登记的候选筛选层（拒绝 = 不登记），认领互斥语义、死会话释放、release 逻辑均未动；「池内僵局换标的」换入前仍走 claim（本闸同样适用，符合换标规则）。边界：本闸不强制「为了免税去认领一个不相关的 ETF 顶替个股」——候选与热榜均无同方向可认领 ETF 时认个股照常放行。

### 变更（印花税闸整体停用——config 开关 stamp_tax_gate_enabled=false，2026-08-28 13:10 用户裁定）

- **为什么改**：2026-08-28 实盘 09988 两笔实证税闸与「窄止损宽止盈」核心理念直接冲突——结构止损（113.95，距 0.25）被税闸强制拉宽到 0.456+ 才放行，止损距每次放大都在透支止盈可达性、稀释赔率实现概率（同日第二笔方向全对、落地仅 +0.07R）；且 2026-08-27 历史回放已实证单笔盈亏与止损距无单调关系（分桶 +1.11R/−0.30R/+2.03R 无序）、闸筛的是与盈亏无关的维度。用户裁定整体关闭。
- **改了什么**：① `config.json` 新增 `risk.stamp_tax_gate_enabled: false` 总开关（`stamp_tax_gate_pct: 0.004` 阈值保留，重开改 enabled=true 即可恢复）；② `open_position_tiger.py`——下单硬校验接开关（关闭时个股不再验闸拒单），新增 `_stamp_gate_enabled_from_config()`；③ `hot_list.py`——榜单接开关（关闭时不显示「税闸止损≥」「预期止损距%/预检」两列与预检脚注，改显示停用说明），新增 `_stamp_gate_enabled()`；④ `pool_claim.py`——认领预检接开关（关闭时 `_stamp_gate_check` 直接返回空 dict、不查快照不警告）；⑤ `fee_schedule.py` 费率链路**不动**——印花税 0.1%/边仍是真实成本照常计入净赔率与盈亏（关的是「止损距 ≥0.4% 才许开」的拦截机制、不是印花税本身）；⑥ 文档同步——SKILL.md 硬性护栏第 5 条 / hot_list 工作流条 / 开仓前自查清单税闸条改为停用口径；risk-management.md「港股个股印花税闸」节标头改「已停用、留档备查」+ 停用状态说明块；monitoring.md「税闸拒 = 换标的」条划线标停用。
- **验证**：三脚本 py_compile 语法过；开关三处读值均 false；`_stamp_gate_check(['HK.09988','HK.00700'])` 关闭态返回空 dict（短路生效）；hot_list 意外实跑一次（import 触发模块级代码）——税闸列与预检列已消失、脚注显示停用说明、榜单正常出数，即关闭态端到端实证。
- **回归风险**：① 窄止损个股单费用占比升高——印花税 0.1%/边照常计入净赔率硬校验（odds_at_current 按真实费率算），费吃赔率的单自然被 1.2 门槛拦住，无需税闸兜底（数值层已有防线）；② 「税闸拒 = 立即换标的」触发源消失——但该机制的替代（形态不成立就换 / hot_list 刷新）仍在 monitoring.md 换标的条款里；③ 停用是开关非删除，重开一行 config 即恢复，阈值与全部推导留档无损。
- **影响面**：盘中（2026-08-28 午市）起即生效——个股开仓不再需要「止损距 ≥0.4%」、不再需要「印花税闸已过」动作记录标注、认领个股不再有预检警告。

### 变更（shadow_hint 文案强化：写明额度「未平才占用」并禁止凭记忆预判跳过落仓——修复 2026-08-28 影子样本漏落 2 笔，2026-08-28 12:40）

- **为什么改**：2026-08-28 早市实盘盯盘，本会话三次开仓被单持仓互斥闸拦下（10:08 多 03308、10:20 空 03308、11:04 空 03308），但只落了第一笔影子交易——10:12 第一笔影子仓触损平掉后额度实际已释放，AI 却凭错误记忆（「用过一次额度就没了」）在 10:20 / 11:04 两次被拦时自行跳过落仓，少收 2 条影子样本（事后重建假想轨迹：10:20 空 ≈ +1.5R、11:04 空 ≈ −1R，一赚一亏恰是对照价值最高的样本）。根因：文档规则（决策点 2「每会话**同时**至多一笔**未平**影子仓」）语义无歧义、写对了，但 AI 靠记忆执行时丢掉「未平才占用」半句——而 shadow_hint 在场打印本就是设计好的工具强制路径（直接照 hint 执行、由脚本前置校验兜底），AI 用错误记忆替代了工具判断，有防呆没用上。
- **改了什么**：`open_position_tiger.py` / `open_position_tiger_us.py` 的 `shadow_hint` 文案强化——原「每会话至多一笔」改为「同时至多一笔【未平】——平仓结算后额度即释放、可再开新仓；不要凭记忆预判额度、跳过落仓，直接照本行执行、由脚本校验兜底」。被拦瞬间 AI 读 blocked JSON 必然看到该文案（在场打印层工具强制），把正确语义钉在决策时刻。
- **验证**：两脚本 ast.parse 语法过；grep 确认新文案已落两处；hint 生成逻辑（blocked_by ∈ {open_exposure_today, active_open_order} 才生成）未动。
- **回归风险**：低——纯文案层改动，不碰 shadow_trade.py 校验逻辑（脚本侧「已有未平拒记」照旧兜底）、不碰互斥闸与下单链路；文案变长不影响 JSON 输出解析。
- **工具强制现状**：本修复本身就是工具强制——把规则语义写进决策时刻必然在场的 hint 文案（第②层在场打印）；更硬的第①层（hook 拦截「收到 shadow_hint 却没跑 shadow_trade」）不可行——hook 无法判断 AI 是否已正确处理 hint（同段真实开仓失败原因多样），误拦会阻塞盯盘循环，不采用。
- **补记纪律**：2026-08-28 漏掉的 2 笔不补录——影子样本价值在「决策时刻价 + 决策时刻完整计划」，事后补记 = 事后知情污染的伪造数据，宁可缺失（已与用户确认处理方式）。

### 新增（止损距宽窄核心理念：窄止损宽止盈、预期赔率实现概率才高，2026-08-28 12:10）

- **为什么改**：2026-08-28 实盘 09988 第二笔（突破回踩追入 @114.2）——方向判断完全正确（价格一路走到 114.7），但止损距放到 0.7、止盈不变 → 净赔率从结构位的 2.29 稀释到 1.35，最终费用吃掉 88% 毛利、落地仅 +0.07R。用户点出核心理念：**窄止损宽止盈，预期赔率实现的概率才大；止损距太大，预期赔率实现的概率就很低**——同样过 1.2 门槛的两个数字，结构不同、兑现概率天差地别，此前文档只有「止损位 = 趋势反转位」的位置规则、没有「止损距宽窄与赔率兑现概率关系」的理念层。
- **改了什么**：`risk-management.md` 新增「止损距宽窄的核心理念」节——① 窄止损宽止盈 vs 宽止损的兑现概率对比（判断对吃满宽分子、判断错只亏窄分母 vs 两头都难）；② 数学直觉（分母可以随便拉大、分子拉不大——止损距每次放大都在透支止盈可达性，与「止盈不可达」教训同构）；③ 三条实操含义（止损距能窄则窄先问失效点真有这么远吗 / 被税闸逼着放宽后必须重看净赔率不做心理锚 / 入场点质量 = 止损距质量）；④ 2026-08-28 09988 第二笔反面教训实录。`SKILL.md` 开仓前自查清单「止损价已定」项补「止损距够窄吗」检查点与指针。
- **归因修正（2026-08-28 12:20 用户纠正；12:30 二次修正）**：初版教训把止损距 0.7 归因为「被税闸两轮放宽」；用户两连问（「税闸不是 0.4% 吗」「113.55 被拒、113.7 为什么能过闸」）后实测拆分，先后揪出两个错——① 税闸基准价搞错：脚本按**下单时现价 current_price** 判定（做多要求 |现价−止损| ≥ 0.4%×现价），不是参考价、更不是最终成交价；② 我给出的「113.7 贴线可过闸、净赔率 1.93」是把**成交价 114.2** 当基准的错算（114.2−113.7=0.44% 看似过闸，但下单时现价 114.0/114.1 下 113.7 距仅 0.26~0.35%、比被拦的 113.55 更过不了闸；且税闸方向是「止损放得更低才过」）。实测账：11:19:20 挂 113.55（现价 114.0，距 0.45=0.395%）被拦 → 11:19:33 挂 113.5（距 0.5=0.44%）过闸但限价未成交撤 → 11:22 现价 114.1、沿用 113.5 没重算贴线（税闸线已上移至 113.64）→ 成交 114.2。止损距 0.7 分解 = 0.25 结构位 + ~0.31 税闸强制 + ~0.14 沿用未贴线 + 0.10 入场滑点；贴线 113.64 净赔率 ≈1.72 vs 实际 1.35（复核实算 1.38）。教训原文已二次改写：三条正解 = 贴线过闸且现价变动后重算、结构止损距与个股税闸不兼容时换入场点或换 ETF、税闸方向是止损更低才过。
- **验证**：三处修改落盘后 grep 核对生效；归因修正后实测数字（税闸线 113.54/113.64、贴线 1.72、实放 1.35~1.38）已实算核对；脚本判定逻辑以 `open_position_tiger.py` 源码 L349-373 为准核实。
- **回归风险**：低——纯增量理念规定，不改「止损位 = 趋势反转位」的位置规则（止损仍按逻辑失效点定、不反向追求窄而脱离结构）、不改防插针缓冲；与印花税闸的关系是「放宽后重看赔率」而非「为窄对抗闸门」——过不了闸该放弃就放弃。
- **工具强制现状**：认知判断类规定、无脚本硬拦——「止损距偏大」的判定依赖结构位与入场点的相对位置（盘面裁量），脚本侧印花税闸 + 净赔率硬校验（odds_at_current 按实际止损距算）已在数值层兜底；已在文档正文承载、不自建硬闸（符合「三层不适用时注明理由」——本条与「警告累计两条」同属置信度裁量层）。

### 新增（置信度新规：开仓脚本非拦截警告同单累计 ≥ 2 条强制降一档置信度，2026-08-28 12:00）

- **为什么改**：2026-08-28 实盘 09988 第一笔（10:13 开多 @113.9、10:22 止损 −1.70R）——开仓脚本同时在场打印两条非拦截警告（stability 3.6 分钟不足企稳定义 + 止盈 115.5 越日高 114.3），两条都在说「结构偏早 / 预期偏乐观」，AI 却仍判 🟢 高置信开仓。用户指出该教训需沉淀成规则：多条独立维度的警告同时出现 = 维度矛盾信号，与「禁止 🟢/🟡 滑过渡」「识别风险 ≠ 处理风险」同根。
- **改了什么**：`trading-strategy.md` ① 新增「同位置警告累计两条 = 降一档置信度」节（判据、用法、2026-08-28 反面教训、边界：只约束非拦截警告、硬拦截类不涉及、突破追入不受 stability 单条约束但两条累计仍触发）；② 「置信度分级」🟢 定义补一项「开仓脚本非拦截警告同单 < 2 条」；`SKILL.md` 开仓前自查清单「置信度 = 🟢 高？」项补指针。
- **工具强制现状**：本条为认知判断类规定、暂无脚本硬拦——两条警告的组合语义需结合盘面裁量（强趋势日越日高让利润奔跑是正确决策），机械「两警告即拒单」会误杀；已在文档中如实注明「无工具强制 + 理由」，并预留升级条件（再发同类事故即升脚本硬拦）。这符合项目「文档规定必须尽可能配工具强制」的边界条款（三层不适用时显式注明理由）。
- **验证**：同日 09988 第二笔（11:22 突破追入、仅越日高一条警告 + 趋势日裁量）+0.07R 与第一笔（两警告 −1.70R）方向对照一致——样本 1、待更多样本验证，规则按「宁严勿松」先立。
- **回归风险**：低——纯增量规定（新增降档触发条件），不改既有 🟢/🟡/🔴 判级框架与「禁止滑过渡」纪律；越日高警告在强趋势日的裁量空间保留（单条时仍可裁量开仓）。

### 变更（修复 account_status 会话归属双格式匹配 bug——持仓状态行静默消失、止损单铁律监控失效，2026-08-28 10:20）

- **为什么改**：实盘开仓 09988（1100 股）后，段输出「📌 持仓状态」行未出现——排查发现 `_session_of_symbol()` 返回的归属映射 key 是富途格式 `HK.09988`，而 `_parse_actions()` 解析 actions 得到的 sym 是裸代码 `09988`，`_holding_from_actions(only_session=...)` 里直接 `sym_owner.get(e['sym'])` 永远查不到 → 本会话自己开的仓被过滤条件误判为「别的会话的仓」剔除 → `position_status()` 返回 None → 段输出不再打印持仓状态行 → **每 40 秒的止损单铁律监控（持仓在 + 无止损单告警）与用户改单检测对当前实盘持仓静默失效**。这是红色级工具故障（实盘持仓的护栏监控失明），当日当段发现并修复。
- **改了什么**：`scripts/account_status.py` 新增 `_owner_of(owner_map, sym)` 双格式查归属辅助（裸代码 / `HK.` 前缀都认），`_holding_from_actions()` 与 `_last_recorded_stop()` 两处会话过滤改用它；顺带修正过滤条件里 `not in (only_session, '')` 的语义（无归属的标的应保留为「无归属」而非与空字符串比较——改为 `not in (only_session, None)`，行为等价于原意图但不再依赖空串哨兵）。
- **验证**：四视角回归——本会话（cd3da32d…）视角正确带出 09988；00700 归属会话视角为空（该仓已被其会话平仓，close 记录在案）；无关会话视角为空（隔离仍生效）；无过滤视角含 09988。修复前本会话视角为 `{}`（bug 复现），修复后 `{'09988': {'qty': 1100, 'dir': '做多'}}`。
- **回归风险**：低——改动仅限会话过滤的 key 匹配层，持仓推导主逻辑（open/close 事件累积）未动；跨会话隔离语义经 00700 视角验证仍成立。

### 新增（影子交易 shadow：auto 模式被单持仓互斥闸拦下的机会的纸面样本记录——多会话并行下被拦机会不再删失，2026-08-27 22:00）

- **为什么改**：多会话并行盯盘走方案 A（全局至多一笔在场，`trade_mutex.py` 三口径闸先到先得），后到会话的开仓机会全被拦下删失——被拦样本永远无法验证、复盘只统计到「第一名机会」，单持仓互斥与样本搜集在并行场景结构性冲突、浪费搜集交易样本的机会（2026-08-27 用户提出并定案四决策点）。影子交易 = auto 模式下开仓被互斥闸拦（blocked_by=open_exposure_today / active_open_order，即「别人在场」）时，不真下单、把该机会按信号式记录落成一笔模拟交易，打【影子】标签——属 auto 模式的分支交易路径，与 signal 模式（用户手动执行的整会话模式）、auto 模拟账户交易（真实订单进账户）三者严格区分，不可混称。
- **改了什么**：① 新建 `scripts/shadow_trade.py`——影子开仓 / 平仓 / 移损 / 状态查询（CLI），条目自动打【影子】标追加到当日 `signals/` 文件，平仓自动结算净盈亏 / R（fee_schedule 同源费率、max_loss 毛口径分母与真实仓一致），状态存 `tmp/shadow_positions.json`（不入库）；内置两条硬校验——blocked_by 非「别人在场」口径拒记（决策点 3：pending_intent 状态不明先人工排查）、同会话已有未平影子仓拒记（决策点 2：每会话至多一笔）；不碰 intent 日志 / flock 锁 / equity-log（与互斥机制、equity 链完全隔离）；② `open_position_tiger.py` / `open_position_tiger_us.py`——互斥闸 blocked 输出在「别人在场」口径时新增 `shadow_hint` 字段（在场打印，直接给出可复制的 shadow_trade.py open 命令行）；③ `auto-mode.md` 新增「影子交易」主档节（定义、三者区分表、落地动作、四条硬边界、互斥隔离设计、工具强制落点）；`SKILL.md` 模式路由 + 硬性护栏第 4 条、`monitoring.md` 互斥行为节、`review-and-evaluation.md` 数据源节各补影子指针与口径；④ `review.py` 支持 CSV `shadow` 列（可选，老 CSV 无该列行为不变）——主统计默认剔除 shadow=1 行（真实样本口径不变），新增 `--shadow-only` 反向只统计影子子集（CSV 无影子行时报错防误跑空样本）；⑤ `bayes_evolution.py` 读 CSV 时剔除 shadow=1 行（演化图画真实资金曲线的信念累积，影子是近似样本不混入）。
- **验证**：shadow_trade.py 函数级单测 7 场景过（pending_intent 拒记 / 正常开仓 / 同会话第二笔拒 / 移损 / 平仓结算 R / 平后再开 / status）+ CLI 全链路实测过（open→move→close→status，测试数据已清理）；review.py 新建 CSV 双模式实测（含影子剔除打印 N 剔除正确 + --shadow-only 子集正确）+ 老 CSV（2026-08-24-trades-hk.csv，无 shadow 列）回归 48 笔行为不变 + --shadow-only 空 CSV 正确报错；bayes_evolution.py 老 CSV 回归跑通（解析段无报错，测试生成 png 已清理）；三个改动脚本 py_compile 全过。
- **回归风险**：① 影子样本的成交价是决策时刻价近似（无响铃时刻价口径、无真实滑点）——与真实样本不同质，混入主统计会污染胜率 / EV；已由 review.py / bayes_evolution.py 双脚本默认剔除 + 文档「禁止混入真实统计」双重防线，但 trades.csv 人工转录时仍需按【影子】标正确填 shadow 列（转录环节无工具强制，靠 AI 对账时核【影子】标）；② 影子仓与真实仓并行盯对 AI 注意力有分流——决策点 1 已定「真实仓优先」，影子平仓时机判断允许粗于真实仓（纸面不紧急）；③ shadow_hint 是提示非强制——AI 收到 hint 不跑 shadow_trade 不落影子，损失的是样本（非资金），属可接受的弱强制（样本搜集是影子交易的目的本身，不落 = 白白放弃，下次复盘「先到 vs 被拦」对照缺数据自会暴露）。
- **效果预期**：并行盯盘日的被拦机会从「删失」变为「可统计样本」，复盘可做「先到 vs 被拦」两组对照（互斥机制的机会成本第一次可量化），为方案 B（资金并行）立项提供数据地基。

### 变更（止盈可达性闸降级为警告不拒单——方案 A：历史回放实证硬闸净效应 -35.6 万/-10.6R，2026-08-27 12:50）

- **为什么改**：2026-08-27 早盘 auto 实盘盯盘，06166 剑桥科技两次开仓尝试（11:19 破 103.6 追多止盈 107.5、11:27 破 105 追多止盈 107.5）均被「止盈可达性闸」（2026-08-21 立：止盈越过当日高点/低点即拒单）拦下，随后该股上涨 +8%。用户指令把闸回放到全部历史交易对比有闸/无闸总盈亏。回放方法：从 signals/ + actions/ 提取 2026-07-07~08-26 全部 53 笔港股开仓记录（含止盈价），富途 5 分钟 K 还原每笔**开仓时刻**的当日高低（闸的真实判定口径），与 reviews/2026-08-24-trades-hk.csv 48 笔 + 8-25/26 两笔对齐得 50 笔实际盈亏。**结论推翻硬闸**：① 金额口径——无闸（实况）+417,622 HKD vs 有闸（回放）+61,644 HKD，硬闸拦掉 35 笔净效应 **-355,979 HKD**；② R 口径（消除仓位量级，8-06 模拟巨仓单笔 +35.7万 占比过大）——被拦 35 笔平均 **+0.30R/笔仍为正期望**（错杀利润 +50.3万 远大于避掉亏损 -14.7万），剔除最大正负极值后仍 +8.3万；③ **92% 的历史交易实际靠移动止损/主动平仓出场**（50 笔中仅 4 笔出场价≈止盈价）——闸惩罚的是从未被兑现的字段却否决整笔交易；④ 强趋势日（利润最肥的日子）创新高瞬间挂单、任何有意义止盈永远在当日高点上方——硬闸与趋势跟随盈利模式结构性冲突（错杀含 7-17 智谱四笔 +6.4万、8-10 药明 +5.9万）；⑤ 闸「不可达」判定准确率仅 76%（37 笔中 9 笔当日实际到过止盈价=错杀）。闸的筛选区分度真实存在（放行组 +0.83R/笔 vs 被拦组 +0.30R/笔），故用户裁定**方案 A：降级为警告不废除**。复盘报告 `reviews/2026-08-27-gate-review.md`（数据 `tmp/gate_review/`）。
- **改了什么**：① `open_position_tiger.py`——止盈越过当日高低时不再 `ok:false` + `exit(1)` 拒单，改为 `ok:true` 放行 + `target_reachable:false` + 新增 `warning_target_unreachable` 字段（含距日高百分比 + 历史统计「该类笔平均落地 +0.30R vs 日高内 +0.83R」+ 趋势日/震荡日裁量指引），docstring 记降级依据；② `SKILL.md` 开仓前自查清单「止盈定价顺序」条——「脚本硬校验」改「脚本警告（2026-08-27 从硬闸降级，方案 A）」；③ `references/trading-strategy.md`「止盈定价顺序」节——工具强制段同步改警告口径 + 降级依据。美股脚本 `open_position_tiger_us.py` 无此闸、无需改。
- **验证**：`py_compile` 通过；单元验证警告分支（target 越日高 → ok=True + target_reachable=false + warning 在场）；真链路实测两次——同款被拦参数（06166 止盈 107.5）不再被止盈闸拦（本次拒因是现价净赔率 0.84<1.2，属预期且合理），构造越日高参数（止盈 110 > 日高 107.7）输出完整警告字段且不因止盈拒单；实盘误开防护（live_locked）、时间闸、税闸、现价赔率闸均不受影响照常拦截。
- **回归风险**：① 警告不拦单后，震荡日止盈定远的坏单会重新进场——历史被拦组平均 +0.30R 为正，但该组含 8-13 00100 -8.35万、8-17 -2.78万等大亏笔，风险真实存在；缓解 = 警告在场打印历史统计（决策时刻信息强制）+ 止盈定价顺序/同尺度规矩不变；② AI 裁量替代机械闸属弱强制（2026-08-18 实证散文规定会衰减）——若后续样本「止盈越日高」笔落地赔率显著差于 +0.30R 历史 均值，重估回硬闸或改方案 B/C；③ 复盘口径本身有局限（5 分钟 K 近似开仓时刻高低 ±1 根误差、被拦后不重试的保守假设、50 笔样本功效有限），已在报告「方法学局限」节声明。

### 变更（印花税闸下调 1.2% → 0.4%：判据改组合 P(g>0) 口径，config + 三工具回退常量 + 四文档全链路同步，T124 落地，2026-08-27 11:35）

- **为什么改**：用户 2026-08-27 定调（两轮）——① 税闸按整体盈利基准设计（单笔税损多一点没关系，整体能盈利即可、开仓机会越多越好）；② 严谨目标 g>0（对数增长率，含方差惩罚），在不影响 P(g>0) 前提下尽可能放松；③ 不等 T120 反事实数据，指示直接改 0.4%。实算依据（同日完成）：基线 P(g>0)=99.6% 贴顶（闸降到 0.25% 仍 ≥99.3%，字面约束不 binding）；硬底线 = 增量单税后 EV 转负点 ≈ 0.26% 止损距（历史 48 笔最小 0.39%、无一触底）；真实截断池 bootstrap 闸 0.8% 组合 g_hat 反升 +9.9%、闸 0.4% 仅降 6.4%（税后 EV +0.490R）；分桶实证单笔盈亏与止损距无单调关系——旧闸拦的是与盈亏无关的维度。
- **改了什么**：① `config.json risk.stamp_tax_gate_pct` 0.012 → 0.004（注释同步组合口径依据与完整沿革）；② `open_position_tiger.py` 回退常量 0.012 → 0.004 + docstring 更新；③ `hot_list.py` / `pool_claim.py` 回退常量 STAMP_STOP_PCT 同步 0.004（注释含沿革）；④ `SKILL.md` 两处（硬性护栏第 5 条 + 开仓前自查清单）阈值与沿革更新，含预检口径「70%×1.67%」过时数字修正为「70%×税闸门槛」；⑤ `risk-management.md`「港股个股印花税闸」节重写——现行判据（组合口径 + 实算四条 + 0.4% 取值理由 + T124 跟踪条款）置顶，旧单笔 20% 占比口径标注废止留档，常见标的止损距示例按 0.4% 重算（腾讯 440→≥1.8 元等）；⑥ `monitoring.md` 税闸拒换标的条的阈值沿革补 0.4%。
- **验证**：三工具实测读值一致（open_position_tiger / hot_list / pool_claim 全部 0.004）；hot_list 实跑「税闸止损≥」列按 0.40% 输出、预检列联动（低 / 近 / 过 档位分布随新阈值变化，如阿里 0.46 门槛从「低」变「近」、中芯 0.29 从「低」变「过」）；下单硬校验边界模拟——止损距 0.49% 放行、0.29% 拒单（blocked_by:stamp_tax_gate），行为符合预期；全项目 grep 无 1.2% 税闸残留。
- **回归风险**：① 0.4~1.2% 止损距带的单全部解锁——这批单的税损 0.17~0.5R，历史实测该带 EV 为正（+0.816R@0.8~1.2 带），但历史≠未来，头 10~15 笔做 T124 专项对账、偏离超预期回升；② 21 笔子集是无闸时代样本（未经右侧确认 / 止盈可达性校验等现行质量闸筛选）——若现行闸堆筛掉的单恰是窄止损带里的坏单，实际增量单 EV 会低于实测 +0.770R；底线保护 = 0.26% 转负点远低于闸值 + P(g>0) 监控兜底；③ hot_list 预检「低」档警告面收窄（更多个股判「过」可认领）——若盘中税闸实测拒单率上升（预检漏报），把预检折扣 0.4 收紧，此为独立参数不受本次影响。
- **效果预期**：0.8~1.2% 带（旧闸拦下的机会带）历史税后 EV +0.816R/笔——按「整体能盈利前提下机会越多越好」原则，预计开仓机会显著增多（以 02269 型标的为例，止损距 1.09% 的形态旧闸拒、新闸放行）。

### 变更（TODO T124 实算定稿：税闸判据升级为组合 P(g>0) 口径、闸值目标 1.2%→0.5%（候选 0.4%），落地动作明确，2026-08-27 11:30）

- **为什么改**：用户两轮定调税闸设计基准——① 整体盈利基准（单笔税损多一点没关系，整体能盈利即可，开仓机会越多越好）；② 严谨目标是从 EV>0 升级为 **g>0**（对数增长率、含方差惩罚），「在不影响 P(g>0) 前提下尽可能放松税闸」；③ 裁定不等 T120 被拦决策点反事实数据（每日复盘已确信绝大多数未下单是税闸拦的）。据此完成实算：基线 P(g>0)=99.6% 贴顶（闸降到 0.25% 仍 ≥99.3%，字面约束不 binding）；真正硬底线是增量单税后 EV 转负点 ≈ 0.26% 止损距（历史 48 笔最小 0.39%、无一触底）；真实截断池 bootstrap 显示闸 0.8% 组合 g_hat 反升 +9.9%、闸 0.4% 仅降 6.4%；分桶实算（0.39~0.55% 桶 +1.11R / 0.65~0.96% 桶 −0.30R / 0.96~1.19% 桶 +2.03R）证明单笔盈亏与止损距无单调关系——税闸筛的是与盈亏无关的维度。
- **改了什么**：`TODO.md` T124 由「口径评估」更新为「待执行落地」——config `stamp_tax_gate_pct` 1.2%→0.5（激进 0.4%）、三个工具（open_position 硬校验 / hot_list / pool_claim 预检）读 config 自动跟但需各验证一次、risk-management.md 推导节改写为组合口径（保留 0.26% 底线与分桶实证）、下调后头 10~15 笔专项对账。
- **回归风险**：本条只更新 TODO 不改执行逻辑（闸值仍 1.2%）；落地时的风险点已写入 T124——21 笔子集是无闸时代样本未经现行质量闸筛选、0.65~0.96% 桶集中亏损是小样本噪声还是真实结构待分辨，专项对账偏离超预期即回升闸值。

### 新增（TODO T124：印花税闸改「整体盈利基准」口径评估——组合 EV 判据 + 窄止损单实证，2026-08-27 11:01）

- **为什么记**：用户在税闸口径讨论中提出设计基准之问——策略盈利建立在多笔交易整体盈亏上，单笔税损多一点没关系，只要均摊到整体能盈利即可；整体能盈利的前提下开仓机会越多越好（没有开仓就没有盈利机会）。现行税闸（`risk-management.md`「港股个股印花税闸」）以「单笔税损 ≤ 该笔期望落地 20%」为判据，是单笔视角；用户主张组合视角。对话中实算验证：① 48 笔样本里 21 笔止损距 <1.2% 的无闸时代单实测平均 +0.770R/笔（12/21 盈利），高于全样本均值 0.616R——「窄止损单 EV 更低」的担忧不成立，用户方向有数据支持；② 按「增量单税后 EV>0」组合判据，盈亏平衡止损距 ≈ 0.26%，含小样本安全边际后闸值理论可从 1.2% 降至 0.3%~0.5%。口径变更（若做）牵动 config 阈值、open_position 硬校验、hot_list / pool_claim 预检列与推导文档，属策略级调整，不能对话里顺手改——立 T124 待与 T119（1.2 口径运行对账）、T120（被拦决策点反事实回填）数据合流后定终值。
- **改了什么**：`TODO.md` 🟠 橙色紧急度新增 T124（含用户论点、实证数据、caveat——21 笔为无闸时代样本非干净对照、剩余动作与 T119/T120 的依赖关系）。
- **回归风险**：无——本条只记 TODO 不改任何执行逻辑；闸值本身未动（仍 1.2%），相关脚本与文档不变。

### 变更（公开仓库敏感内容彻底清除：T1 第二步 git 历史重写 + 实盘财务数字全面中性化，2026-08-26 14:32）

- **为什么改**：2026-08-26 全团队仓库敏感信息大排查（起因 ExecutiveAssistantAgent CHANGELOG 财务描述泄漏事故、全局规则同日增补「个人 / 公司 / 组织财务状况」敏感类型）发现本仓两层残留——① 已提交历史（即 GitHub 当前可见版本）的 accounts.md 含节点 IP 明文、订阅商域名体系与白名单拒绝报文、实盘净值 / 购买力精确值（T1 第一步的工作区中性化改动此前一直未 commit），CHANGELOG / reviews / signals 含 equity-log 实盘权益轨迹（含初始本金与逐笔净值）；② 工作区与暂存区多处量级口径与实盘数字——2026-08-20 立的「量级口径允许」与 2026-08-26 全局新规「财务状况描述一律不写」冲突，按新规收紧。用户指令「清理敏感信息，但前提是不影响正常功能」。
- **改了什么**：① 在 mirror clone 中以 `git filter-repo --replace-text` 重写 main + win 全部 63 个 commit（替换清单：全部节点 / 出口 IP、订阅商域名与节点名、白名单报文原文、实盘净值 / 购买力 / equity-log 系列数字、量级与本金规模表述；模拟账户系列数字与用户 2026-08-17 裁定保留的模拟账户号历史明文不动），HTTPS 通道 force push 覆盖远程两分支；② 工作区与暂存区 20+ 处实盘数字 / 量级口径中性化（CHANGELOG / SKILL.md / `.claude/CLAUDE.md` / notes / reviews / actions / 四个下单与取值脚本注释 / equity_guard.py 注释），保留「已入金」「B 派生指标」「偏差 68 倍」等功能信息；③ 「量级口径允许」旧规（`.claude/CLAUDE.md` 专节 + SKILL.md）改为「量级口径同样不写」；④ 本地对齐（分支指向新历史 + reflog 清空 + gc 清旧对象），主 / win 两 worktree 纯重写差异文件 restore 对齐（T1 归档见 `TODO-archive.md` 2026-08-26 节）。
- **验证**：远程 main 两关键文件实测扫描零命中；本地旧 commit 对象已物理删除（cat-file 报不存在）；0 fork、无 Release 无 tag、无第三方残留渠道；`code=4 access forbidden` 等老虎通用错误码（不含用户信息，排障需要）保留。
- **⚠️ 回归风险与遗留（需用户动作）**：① `signals/equity-log.csv` 仍被 git 跟踪且工作区为真值（resume 功能依赖）——git 历史版本已被替换为标记、若被 checkout 会破坏 resume 读取，须尽快 `git rm --cached signals/equity-log.csv` 停止跟踪（gitignore 对已跟踪文件无效），真值保留本机；② 暂存区为清理前 add 的旧版（含敏感句），**提交前须重新 `git add` 全部改动文件**，否则敏感句随提交回归；③ GitHub 服务端悬挂旧对象在自动 GC 前可能按完整 SHA 直链访问（页面导航不可达、无 fork，风险低）；④ 历史记录文本中的替换标记（`[IP已移除]` 等）仅影响历史可读性，不影响脚本运行。

### 新增（account_status 用户改单检测：账户止损 ≠ 记录即明示「用户在 App 手改」+ 方向风险提示，2026-08-26 12:15）

- **为什么改**：2026-08-26 早市实盘 07709 做空持仓中，用户在 App 手动改止损触发价 39.5→40.46（误按做多习惯上移，实为空单 = 放宽风险）。AI 段输出的持仓状态行只显示数字、无归因，AI 误判为「下单参数被改」的生产 bug 修正（move_stop 改回 39.5）、停盯小结还留了一条「附加腿落成价 ≠ 下单参数」的待排查项——事后用户澄清是自己改的，排查项撤销。根因：段输出缺少「这个差异是谁造成的」的归因层——AI 每次移损都写 actions 记录、脚本下的止损价不会自己变，账户价与记录不符时排除法唯一解释就是用户在 App 改的，但工具没把这层推断打印出来。
- **改了什么**：`account_status.py` 的 `position_status()` 持仓一致分支新增「用户改单检测」——账户活动止损触发价 ≠ 当日 actions 最新记录止损价（容差 0.005）时，输出「👤 用户改单：账户止损 X ≠ 记录 Y（判定为您在 App 手动修改，非脚本异常）」+ 方向风险判断（较记录「放宽（离场更远、风险变大）」/「收紧（离场更近、风险变小）」）；放宽且方向与持仓相反（做空上移 / 做多下移，即按做多习惯改空单这类误操作特征）时追加 ⚠️ 复核提醒。已用三个模拟场景（空单上移 40.46=今日实录 / 空单下移 39.2 / 多单情形）验证文案与方向判断正确。顺手修连带 bug：`_parse_actions` 的移损事件判定原只认标题「移损」、不认实际记录格式「移动止损」，当日移损记录一直没进事件流（此前靠开仓记录兜底未暴露）——判定扩为「移损」或「移动止损」都认，已验证 move 事件恢复解析。
- **回归风险评估**：① 检测读的是「当日 actions 最新止损记录」——若 AI 当天忘写移损记录、或记录格式再变（如改用其它标题词），会误把 AI 自己的移损当用户改单（文案口径「判定为用户改」出错）；缓解：move 判定刚按实际格式修过、log_action 模板稳定，且误报方向只会多提示一句、不触发交易动作；② 容差 0.005 对港股 tick 0.02/0.05 档足够，但若券商回报 aux_price 带浮点尾差（如 39.49999）可能临界误报——abs 差 ≥0.005 已排除常见尾差量级（1e-5 级）；③ 判定本身不自动改回止损价——尊重用户手动操作（用户可能有意收紧），只提示不干预，无越权风险。

### 新增（止损位防插针缓冲规则，2026-08-25 14:35）

- **为什么改**：2026-08-25 午市实盘 07747（南方两倍做多三星电子）做多 1900 股 @72.34，止损挂双底颈线 72.2 **正位**（既是显眼结构位又撞整数关口，双重拥挤），13:59 一根插针 72.08 打掉止损成交 72.17、随即 V 型收回 72.3+ 并续走 72.5-72.8，单笔 −476 HKD / −1.79R。止损纪律执行本身无瑕疵（破颈线即离场、未抗单），但**放的位置**可优化——插针最爱打全市场止损单的拥挤区（颈线正位 + 整数关口），止损单是券商侧毫秒级条件单、AI 侧采样再密都拦不住，只能从「放哪」上做文章。
- **改了什么**：`risk-management.md` 新增「止损位防插针缓冲」节——① 缓冲距离 = 结构位外侧 0.25×5 分钟 ATR（做多放支撑下方、做空放阻力上方，方向是「离开」结构位更远而非贴挂）；② 避开整数 / 半整数关口（能放 72.15 不放 72.2）；③ 明示代价是保险费（止损距变大→仓位变小、赔率变低、真破位多亏缓冲距离），方向只能「挪位置」绝不能「松纪律」；④ 留复盘量化题——统计「止损触发后 N 分钟收回结构位」的插针回收率来标定 / 修正 0.25 系数。`SKILL.md` 两处同步：6 要素「止损价」条 + 开仓前自查清单「止损价已定」条，均加防插针缓冲检查项。
- **回归风险评估**：① 缓冲使止损距变大 → 同预算下仓位变小、净赔率变低——**个股可能因此过不了印花税闸（止损距 ≥1.2%×股价）**，此时按税闸优先（结构位外侧同时挪缓冲与税闸双达标，或换 ETF）；② 0.25×ATR 是未经样本标定的经验值，可能过小（挡不住针）或过大（白多亏缓冲）——已内置第 4 条量化题闭环，复盘时用样本说话；③ 纪律方向不变（止损单该触发就触发），本条只改「放哪」、不改「松紧」，不引入 03308「死守远位」类回归风险。

### 变更（futu_ws_segment 补剥 --account 参数，2026-08-25 13:24）

- **为什么改**：午市实盘盯盘启动密采样时传 `--account live`，`futu_ws_segment.py` 的 `parse_targets` 只剥 `--mode` 不剥 `--account`——'live' 被当标的解析，订阅报 `format of code live is wrong`、且为 'live'/'--account'/'40' 三个伪标的各建了空 log 文件，段空转 40 秒。
- **改了什么**：`parse_targets` 的剥参白名单从 `('--mode',)` 扩为 `STRIP_FLAGS = ('--mode', '--account')`（含 `--flag=value` 形态与跟值 token），已实测确认剥净；顺手删除 3 个错误命名的空 log（`monitor_log_live/_--account/_40_20260825_auto.csv`）。行为不变：账户归属由 `account_status.py` 按当日 actions 的「账户 | 实盘」标注判定，采样脚本本身不需要 account 参数、只需不误解析。

### 新增（TODO T123：实盘 buying_power 币种混淆排查定案 + 做空拒单真因待复核，2026-08-25 12:40）

- **为什么改**：用户指出 2026-08-25 上午停盯小结里「实盘真实购买力仅 5.66 万 HKD」数据错误，要求排查。排查定案：**币种混淆——把 USD 口径的 buying_power 当成了 HKD**。当时临时跑 `get_assets().summary.buying_power` 取 56637.5 就下结论，但该字段 USD 计价（同账户 `summary.currency='USD'`；`trade_utils_tiger.py` 的 `get_buying_power_tiger` docstring 2026-08-18 修时就明文记录过「buying_power 为 USD 口径、与 HKD 直接相除有 ~7.8x 偏差」；算术复核：同刻 open_position 脚本输出的 443843.84 ÷ 56637.5 = 7.8366，正是老虎内部 USDHKD 汇率——同一笔数据，脚本换算了币种、我的临时查询没换）。正确购买力 ≈ 44.4 万 HKD ≈ 4× 净值（语义自洽）；5.66 万 HKD 口径只有 0.51× 净值，融资账户不成立。**连锁影响**：按正确购买力，当日 4100 股空单（35% 保证金 ≈9.9 万 < 44.4 万）本应能过却报 insufficient——「资金不足」的拒单归因存疑，真因待下次实盘解锁后用 `get_contract` 的 short_initial_margin（做空保证金率）复核；且我据错误口径把后续仓位压到 1000-2500 股（缩水 4 倍），属错误数据驱动的错误决策（单未成交、无实际损失）。
- **改了什么**：`TODO.md` 橙色紧急度新增 T123——记录排查定案（铁证三件：currency 字段、docstring 明文、汇率反推）+ 剩余动作（① 下次实盘解锁后用 short_initial_margin 复核 07747 口径真实可空量、重写当日拒单归因；② `trade_utils_tiger.py` 补 `get_buying_power_tiger` 的 short 版本——现只有 long_initial_margin 口径，做空预算借用多口径本就不严谨）。本次仅排查 + 登记，未改代码。
- **回归风险评估**：无代码改动、无风险。教训归档：数值结论下前未做交叉验证（×汇率、与净值比值合理性），且 docstring 就在项目里没读——违反 verify-facts-before-stating「数值必须有据、实算」；T122 已含「被拒 reason 提到首层 JSON」改进，本条的 short 保证金复核与之互补，共同把实盘拒单的归因链补全。

### 变更（印花税闸下单硬校验落地 + 税闸 2.22%→1.2% / 净赔率门槛 1.8→1.2 双降 + 双参数进 config 统一调参，2026-08-25 13:05）

- **为什么改**：2026-08-25 上午实盘 auto 盯盘零交易收场，复盘暴露两层问题。① **税闸靠 AI 临场手算、无工具强制**：10:19 给 01888 定入场框架时税闸用参考价 37.6 算（止损 36.7，距 2.39% 判「过」）、入场计划却是回踩 37.1（真实止损距 1.08% 不过闸）——同一框架里净赔率用入场价、税闸用参考价，口径混用让「双闸达标」成假象，按假框架空耗 40 分钟等回踩，11:09 下单前精算才发现死结（止损放 37.5 税闸拒、放 37.2 赔率只剩 1.10，两门槛互斥）。② **税闸阈值标定过时 + 门槛整体偏紧**：用 48 笔历史样本实算——真实兑现率 0.62R（个股 0.66R），旧税闸按 25% 兑现率标定、低估近 3 倍；40 笔个股中 34 笔（85%）止损距不足 2.22% 会被旧闸拦掉，这 34 笔**已扣税**真实落地合计 +24.3R（22 笔盈利）、实际税损÷实际净利 ≈ 17-20% 恰在原设计上限内——错杀大于保护。用户裁定：落地硬校验 + 税闸降 1.2% + 净赔率门槛降 1.2。
- **改了什么**：**脚本 4 个**——① `open_position_tiger.py` 新增印花税闸硬校验（`blocked_by:"stamp_tax_gate"` 拒单）：按**实际入场价 current_price** 口径（口径混用事故的直接补丁）、只拦港股个股（ETF 免、REIT 落 stock 档保守照拦）、执行顺序先于止盈可达性与净赔率校验（死结单最先被拦）、报错带「过闸所需最低止损位 + 赔率互斥提示」；② 同脚本与 `open_position_tiger_us.py` 的净赔率硬校验 `< 1.8` 改读 config（`< _min_odds`）；③ `hot_list.py` / `pool_claim.py` 税闸常量改读 config（回退常量同步 0.012）。**config 2 个新参数（唯一权威源）**——`risk.stamp_tax_gate_pct: 0.012`（个股止损距 ≥ 1.2%×入场价；真值公式 1.2R×0.62×20% → 1.34%，取 1.2% 略激进）+ `risk.min_net_odds: 1.2`（净赔率门槛，沿革 2.4→1.8→1.2），联动关系与同步要求写入 `_comment`。**文档 6 个**——`SKILL.md`（6 要素、硬护栏 5 条、质量优先、EV 判据、自查清单税闸条：全部改 config 口径 + 硬校验说明）、`risk-management.md`（税闸节：规则改 1.2%、新增第 5 步 08-25 下调实证、工具落地节新增下单硬校验为 ①、修正「无法工具硬拦」旧结论——校验点移到下单时刻后止损价已是确定参数）、`trading-strategy.md`（开仓基准 19 处 + 门槛沿革新增「⓪ 1.2 时代（08-25 现行）」段）、`auto-mode.md` / `signal-mode.md`（开仓/信号表格门槛）、`monitoring.md`（税闸阈值沿革口径）。**验证**：四脚本语法过；config 联动读取断言过（改 1.5% 测得 1.5%、还原后 1.2%）；单元判定 4 场景过（含当天死结参数复现：38.10/37.5 距 1.57% → 旧阈值拒、新阈值过）；hot_list 实跑新阈值生效（税闸止损≥列全按 1.2% 重算）；当天死结单（01888 38.1/37.5/39.2）新双阈值下税闸 1.57%≥1.2% ✅ 净赔率 1.66≥1.2 ✅ 双闸全过。
- **回归风险评估**：① **放行带变宽**——1.2~1.8 赔率带与 1.2~2.22% 止损距带同时放开，旧「1.2 门槛 75% 落地 ≤0」的担忧带重启，但该结论产出于右侧确认 / 现价校验 / 企稳核验等质量闸不存在的时代，现行闸堆不同；对冲 = T119 对账机制保留（改为 1.2 口径跟踪）+ 30-50 笔实盘新样本后按真实分布复标；② **税损占比上限从 20% 升到 22%**（0.167R ÷ 0.744R 期望落地）——压线单笔税损上限 367 HKD（B=2200 口径），非压线单更低，接受；③ **硬校验只拦下单时刻**——AI 定框架阶段仍可能口径混用空耗盯盘时间（今天 40 分钟那种），但开仓结果已不可能被错口径放行，属结果安全 ≠ 过程效率，过程侧靠 hot_list 预检列在场提醒；④ REIT 落 stock 档被闸拦（fee_schedule 2026-08-17 注的保守口径延续），港股 REIT 开仓会被要求 1.2% 止损距——REIT 日内交易本就少，接受；⑤ config 缺失时回退 1.2%/1.2 与新默认一致，不会静默回旧值。

### 变更（港股做空限价改挂 ask：修复与提价规则的系统性冲突 T122，2026-08-25 12:30）

- **为什么改**：2026-08-25 早盘实盘 auto 盯盘实录——07709（SK 海力士 2x ETF）区间空信号全要件成立（下破 33.3 + 反抽 4 次不过 33.44 + VWAP 下方、净赔率 2.95），三次限价空单全被生产网关拒绝（reason=`short sales cannot be made below the reference price`）。查港交所官方「受监管的卖空活动」页确认规则原文：**持续交易时段卖空价不得低于当时最好沽盘价（ask）**（开市前 / 收市竞价时段另各有参考价）。根因是脚本 short 分支取 `quote["bid"]` 做限价（「做空挂 bid 主动卖」），bid 恒 ≤ ask → 实盘结构性必拒。模拟盘未暴露的原因：① 模拟撮合器是券商自建简化仿真，交易所级规则校验从宽；② 限价空单在模拟盘从未实测过（08-05 之后空单全用 MKT，08-23 改限价唯一后模拟未再测空单）——「订单参数 × 交易所规则」组合在模拟侧双盲。当日错过入场窗口（事后反抽至 34.94，若成交大概率 −1R——没成交是运气不是防护）。
- **改了什么**：① `open_position_tiger.py` short 限价 `quote["bid"]` → `max(quote["bid"], quote["ask"])`（= ask，与最低卖单同价排队卖——提价规则下做空唯一合法挂法；语义上写成 max 让「保证 ≥ ask」意图自明）；无盘口兜底 current_price（last ≥ 满足规则参考）、仅 bid 无 ask 也兜底 last；② `open_position_tiger_us.py` 同步改（美股无提价规则，同口径无副作用：挂 ask 排队卖与挂 bid 主动卖在 1-tick 点差下成交价相同，且防未来券商侧加风控）；③ 两脚本 docstring、mkt 拒单提示文案、`auto-mode.md` 开仓条同步新口径。验证：`py_compile` 过；short 定价逻辑单测 7 场景全过（当日两次实拒盘口 33.34/33.38→33.38 与 33.40/33.42→33.42、锁盘 bid==ask、无盘口兜底、仅 bid、仅 ask）。**美股做空挂 ask 的成交语义变化**：从「砸 bid 立即成交」变「与最低卖单同价排队」——极端快速下行中排队单可能不成交，按既有「8 秒超时撤单退出、AI 重新决策」路径处理，行为与港股一致。
- **回归风险评估**：① **做多路径零改动**（仍挂 ask 主动买）；② **港股做空成交模式变化**——挂 ask 非主动砸盘，理论上从「立即成交」变「排队等买方吃上来」，点差 1-2 tick 时差异极小，但快速下行中可能排不上（提价规则本意即禁止做空主动打压，这是合规必付的代价）；③ 兜底链（无盘口 → last）在 last 严重滞后时可能仍低于实时 ask——属极端数据缺失场景，接受；④ 剩余待办转 T122 尾巴：paper + live 各实测一单限价空单、被拒 reason 提到首层 JSON（当日 reason 藏在 failures 里、首层只见「开仓全部失败」，排查要多挖一层）。

### 新增（TODO T121：开仓前「自问平仓」检查待办登记，2026-08-25 09:37）

- **为什么改**：用户提出开仓位置偏高的防护思路——开仓与平仓用同一套价位判断，一个价位如果自己都会想「先平掉等回落再接回」，说明该价位对自己已偏局部极值，此时开仓等于主动接在局部高点（做多）/ 局部低点（做空）。思路包含两向：做多开仓前自问「已有持仓的话这个价位会不会想先卖出、等回落再接回」，会则等预期接回价位再开；做空开仓前自问「已有空头的话这个价位会不会想先买回平仓、等更高再重新卖空」，会则等更高位置再卖空。
- **改了什么**：`TODO.md` 橙色紧急度新增 T121——含两向自问逻辑、落地要求（按「文档规定必须尽可能配工具强制」评估：候选手段为 open_position 开仓前校验 / SKILL.md 自查清单在场打印，属认知判断类检查、若不脚本硬拦则至少要决策时刻在场打印）。仅登记待办、未改任何代码。
- **回归风险评估**：无——纯待办登记，不触碰任何现有开仓闸（净赔率 1.8、右侧确认、止盈可达性、税闸等行为不变）；落地时再评估与现有开仓闸的叠加关系。

### 变更（开仓净赔率门槛 2.4 → 1.8 下调 + 印花税闸联动重算 1.67% → 2.22%，2026-08-24 13:40）

- **为什么改**：用户对 2.4 门槛的由来与效果提出质疑（「最初设 1.2，复盘后听 AI 建议提到 2.4，当时犹豫但没深入分析；之后开仓很难」），当日逐笔实算验证质疑成立：① **2.4 从未基于「初始↔落地相关性」**——它是 08-05 复盘从「落地 ≈ 初始 ×0.5」均值假设回推的算术缓冲（1.2÷0.5），历史敏感性里 ×0.6 档（≥2.0）合计反而更高（+0.98R vs +0.84R）；② **过闸样本内初始↔落地相关 ≈ 0**（15 笔斯皮尔曼 ρ=−0.21，初始 ≥4R 组落地均值 −0.38R 反而差于 <4R 组 +0.46R）——2.4 在 2.4 以上的单里没有选择力；③ 08-14 曾引「门槛后 9 笔兑现率 25%」支撑门槛，但与 08-07 用法 B 锁利修复同期、归因混杂；④ 2.4 标定基线过时——标定时（08-05）右侧确认总则（08-17）、印花税闸（08-18）、止盈可达性校验（08-21）、现价赔率校验（08-21）全不存在，质量闸层层后加、整个闸堆从未重平衡；⑤ 盯盘日志 08-18~24 约 20 个「赔率不足放弃」决策点净赔率中位 1.46、75 分位 1.77——2.4 把 1.8~2.2 的厚机会也拦掉了；⑥ 开仓效率（盯盘 token 成本 ÷ 开仓笔数，08-24 约 6 会话 ~467M 输入 token 只开 1 笔）随门槛单调恶化。**用户裁定先降到 1.8**（不直接回 1.2 的理由：0.7~1.4 被拦带正是旧标定「75% 落地 ≤0」带与费吃毛利重灾带；近期 regime 胜率 30% 下 1.2 边界单 EV = −0.34R）。
- **改了什么**：**脚本 8 个**——`open_position_tiger.py` / `open_position_tiger_us.py`（现价净赔率硬校验 `< 2.4` → `< 1.8` + 止盈可达性提示语）、`monitor_segment.py` / `futu_ws_segment.py` / `ws_segment.py`（停盯边界提醒行）、`trade_utils_tiger.py`（开仓时间闸放行提示）、`hot_list.py` / `pool_claim.py`（税闸常量 `STAMP_STOP_PCT` 0.0167 → 0.0222 + 提示文案）。**文档 7 个**——`SKILL.md`（参考价条、质量优先条、自查清单 EV 判据 + 税闸条 + 硬性护栏第 5 条）、`trading-strategy.md`（开仓基准改 1.8、新增「门槛沿革」节完整记录 1.2→2.4→1.8 三段依据与实算数据、归因混杂警示、边界重写）、`risk-management.md`（税闸推导节按新期望落地 0.45R = 1.8×25% 重算：阈值 0.09R、止损距 ≥2.22%、常见标的数值表更新、保留旧值括注）、`auto-mode.md` / `signal-mode.md` / `review-and-evaluation.md` / `monitoring.md`（开仓/发信号/预估 EV 门槛表述 + 税闸口径）。**税闸联动推导**：期望落地 = 1.8R × 25% 兑现率 = 0.45R；印花税阈值维持期望落地的 20% = 0.09R；止损距 ≥ 0.002/0.09 = 2.22%×股价（腾讯 442 → ≥9.8 元、阿里 124 → ≥2.8 元、中芯 76 → ≥1.7 元、小米 26 → ≥0.58 元）。**新增 TODO**：T119（1.8 运行 10~15 笔对账，日均亏 ≥3R 回撤 2.0 预案）+ T120（被拦决策点反事实回填机制，攒 ≥30 点后按真实分布重标定终值）。验证：8 脚本 `py_compile` 全过；常量断言（0.0222 两处、`< 1.8` 两处且无 `< 2.4` 残留）全过；全 skill 活规则 2.4/1.67 扫描清零（历史记录语境括注保留）。
- **回归风险评估**：① **放行带变宽 = 旧病复活风险**——1.8~2.4 带正是被拦点最集中的带（20 个决策点中约 4~6 个），若这些单落地差，1.2 时代末期「75% 落地 ≤0」可能重演；对冲 = 其它质量闸全保留（🟢 置信硬闸、右侧确认总则、止盈可达性校验、现价赔率校验）+ T119 十到十五笔对账 + 回撤预案；② **税闸收紧（1.67%→2.22%）= 个股可用形态变少**——部分原先过闸的窄止损个股形态现在会被拦（这是门槛下调的必要联动：期望落地变小、同一税项占比变大），副作用是个股交易进一步向 ETF 倾斜（免印花税不受闸）；③ 文档历史语境（旧值 2.4/1.67）全部以括注保留、复盘引用旧数字时口径可追溯，无统计断链；④ `open_position` 现价校验语义不变（只是阈值数字变），07709 追高事故的防护逻辑完整保留；⑤ hot_list / pool_claim 预检列的「过/近/低」分界随闸值整体平移，行为一致性不受影响。

### 新增（认领前税闸预检：过闸概率低的个股不值得认领，2026-08-24 12:25）

- **为什么改**：2026-08-24 早盘实录——认领 MINIMAX（个股）后 10:19「反抽 323.4 遇阻回落」开仓形态成立，但印花税闸实测不过（止损距 0.68% 股价 < 1.67% 门槛）+ 净赔率最高 1.49R < 2.4，双闸皆拒；此后继续盯到午休空耗一上午。复盘发现**认领时点（09:48）用已有快照数据就能算出「过闸概率低」**（现价 321.8 / 当日低点 320 / VWAP 327.9 → 两侧预期止损距折后 0.22% / 0.76%，远低于门槛）——税闸从「开仓末端拦截」前移到「认领前预筛」，低概率标的从一开始就不认领、不浪费盯盘资源。用户裁定立此预检。
- **改了什么**：① `hot_list.py` 加「预期止损距%」+「预检」列——个股当日预期止损距上限 = 多空两侧结构距离（现价↔当日低点 / 现价↔min(VWAP,当日高点)，剔跳空：高价跳空上沿不算阻力）较大者 × 0.4 折扣（入场与止损各贴结构位吃缓冲，2026-08-24 MINIMAX 两形态实测折后 0.3~0.5 取中）；预检分级：过 / 近（70%~100% 门槛）/ 低（< 70% 门槛）/ 免（ETF/REIT）。② `pool_claim.py claim` 认领港股个股时现查富途快照跑同款预检——⚠️ 警告「过闸概率低、不值得认领」（工具在场打印，AI 必须直面数字：release 换标的或带明确理由继续盯）；🟡 边缘提示；ETF/REIT 免检直过；富途查询失败降级提示不阻断认领。③ SKILL.md 硬性护栏第 5 条 + 工作流第 3 步 + 开仓自查清单补「认领个股前必看预检」；**新增「形态成立但被税闸拒 = 立即换标的触发」**（该标的当日结构宽度天花板已被证实，继续死盯无意义）。④ `monitoring.md`「标的不值得密盯时优先换标的」节补「税闸拒 = 不值得密盯的最强证据」+ 两道工具强制说明 + 当日反面实录。实测：hot_list 榜单新列输出正常（MINIMAX 实时预检「低」与盘中实测一致）；pool_claim 认领个股触发 ⚠️、ETF 免检直过、release 正常。
- **回归风险评估**：① 预检是**警告不是硬拦**——低概率 ≠ 零概率（新结构会出现、历史大波动日仍放行），认领互斥语义不变（预检不撤销登记），只是把「过闸概率」数字摆到 AI 面前强制直面；② 0.4 折扣与 0.7 警告线是经验参数（今日两形态标定，样本 n=2）——偏保守方向（宁可多警告不漏警告），后续形态积累后可校准；③ `_exp_stop_pct` 对快照缺字段返回 None 显示 `-`，不阻断榜单；④ pool_claim 的富途查询在认领关键路径加 ~1 秒，查询失败整链降级不阻断；⑤ ETF / 美股认领路径零改动。

### 变更（实盘解锁 / 授权凭证按会话 ID 分文件：多会话并行解锁互不冲突，2026-08-24 10:00）

- **为什么改**：2026-08-24 早盘实盘 auto 盯盘实录——两个会话并行都要实盘下单，会话 A（cccb62d4）先走完授权链解锁成功；会话 B（ad9943d6）用户也点了「确认授权」，但**解锁文件（`live-unlock.local`）与授权凭证（`live-auth.local`）都是单文件**：B 的 verify-auth 发现凭证已被 A 先消费（no_fresh_authorization）、且若 B 重新解锁会**覆盖** A 的解锁文件致 A 实盘下单能力失效。用户裁定设计意图本就是「多会话并行、各自授权各持解锁、互不冲突」——单文件实现与设计意图不符，属实现 bug。
- **改了什么**：① `scripts/live_unlock.py` 解锁文件 / 授权凭证路径改按会话 ID 生成——`live-unlock.<sid>.local` / `live-auth.<sid>.local`（无 sid 的人工终端用 `manual` 段；sid 只留字母数字与连字符防路径注入）；`live_unlock_valid()` 校验本会话文件 + token 用同一 sid 重算双重绑定；`revoke` 只删本会话文件并提示其它会话状态、新增 `revoke --all` 整体收回；`status` 增列 `other_sessions_unlocked`。② hook `live_auth_witness.py` 凭证按 payload 里的 session_id 分文件写。③ `.gitignore` 改通配 `live-unlock.*.local` / `live-auth.*.local`（git check-ignore 验证生效）。④ 旧单文件 `live-unlock.local` 已迁移为 `live-unlock.<原sid>.local`（另一会话解锁无损延续）。⑤ SKILL.md / accounts.md 授权链描述同步。测试 13 项全过：分文件路径生成 / 默认态拒 / 端到端解锁链 / 跨会话隔离（no_file）/ 拷贝改名文件 bad_token 拒 / 双会话并行各自解锁互不覆盖 / revoke 本会话与 --all / status 其它会话提示 / hook 按会话写凭证 / 下单闸未解锁拒已解锁放行。
- **回归风险评估**：① token 仍绑 sid + 文件名含 sid 双重隔离，单会话安全性不降（拷贝改名攻击照样 bad_token 拒）；② 人工终端从「放宽校验」改为共享 `manual` 文件——语义等价（用户自己操作自己电脑）且更严格（也验 token）；③ `revoke` 从「全删」变「只删本会话」——用户喊「撤销实盘」时 AI 应跑 `revoke --all`（SKILL / accounts.md 已写明）；④ 6 个下单脚本与 apply_proxy 闸走 `live_unlock_valid()` 公共入口、无需逐个改。

### 新增（T118 停盯边界时间闸落地，2026-08-24 12:00）

- **为什么改**：2026-08-24 早盘 AI 在 11:48（距收盘 12 分钟）以「空仓+无信号」自行停盯收尾，违反「盯到用户喊停或收盘（取先到）」——散文规定靠 AI 记忆执行会衰减（2026-08-18 同型结论再验证），改为机械强制。
- **改了什么**：① 新建 `scripts/stop_gate.py`（停盯时间闸：盘中距收盘 >5 分钟且无 `--force` 拒绝放行；收盘窗口 ≤5 分钟 / 盘外自动放行；时段判定与 monitor_guard / preflight 同源，5 场景单测通过）；② `monitor_unregister.sh`、`keep-awake/scripts/off.sh`、`live_unlock.py revoke` 三个停盯收尾入口全部接入闸（盘中任一收尾动作都会被拒并提示理由）；③ `futu_ws_segment.py` 停盯边界提醒行补「空仓/无信号 ≠ 停盯理由」在场打印。实测：收盘窗口自动放行 ✓、模拟盘中拦截 ✓、`--force`（用户喊停）放行 ✓。
- **回归风险评估**：`--force` 出口保证「用户喊停」永远优先于机械闸（不会被闸困住停不了盘）；收盘窗口 / 盘外自动放行保证正常收盘收尾不受影响；stop_gate 缺失时 revoke 宁放勿断（revoke 是安全方向）；不影响其它会话（闸只约束收尾动作、不碰采样链）。

### 新增（TODO T117：live_unlock sid 绑定 bug 登记，2026-08-24 10:03）

- **为什么改**：实盘 auto 盯盘启动实录——用户 AskUserQuestion 点「确认授权实盘」成功、hook 见证写了解锁 token，但同会话 Bash 的 `live_unlock.py` 校验 bad_token（hook 见证取 stdin payload 的 harness sid 写 token、`live_unlock.py` 用 Bash 环境变量 sid 验 token，两处口径在 VSCode 扩展 / 会话恢复场景不一致），实盘请求直连被 IP 白名单拒、下单物理不可达。多会话并行盯盘是设计目标且用户要求多会话都有下单权限，当前「token 绑单一 Bash sid」语义不满足。
- **改了什么**：`TODO.md` 红色紧急度新增 T117（bug 现象、根因、修复要点：解锁语义改为一次授权全局有效或按授权发起链、同步更新 accounts.md / SKILL.md 描述、多会话并行实测）。仅登记待办、未改任何代码（另一会话正在紧急修理，避免并发写冲突）。

### 新增（港股全量复盘 2026-08-24：48 笔入账实盘 6 笔、一级降频线首次触发）

- **为什么改**：用户复盘指令——距上期复盘（08-17，42 笔）新增 08-18 / 08-19 / 08-21 三个交易日共 6 笔实盘 auto 交易未入账，且需按全量对账纪律重跑全套统计与 7 张演化图。
- **改了什么**：① 全量对账 signals（17 个）+ actions（10 个）HKT 文件，新增 6 笔实盘交易入 CSV（腾讯两笔 / 小米两笔 / 海力士 / MINIMAX，合计 −2.20R、净 −3,937 HKD）；08-18 腾讯第二笔的 14:25 与 14:33 两条平仓记录归并为同一事件（order 16、fee_schedule 复算口径 −346.2、M=720）；② 产出 `reviews/2026-08-24-HKT-review.md` + `trades-hk.csv`（48 笔）+ `trades-hk-ts.csv`（含新 6 笔 raw high/low；修正 08-18 腾讯#1 raw_low 口径 443.2→442.2）+ 7 张港股演化图；③ 修正过程指标聚合的做空 MAE 符号错误（此前未按方向翻转，盈利单 MAE −0.15 → −0.35、亏损单 −0.03 → −0.60，为复盘脚本外临时聚合代码、非 review.py 本体）；④ 终局 N=48：EV +0.368R、胜率 56.2%、P(EV>0)=95.5%、频率派 CI [−0.05, +0.79] 跨零（上期全正的确认状态倒退）；三阶段退化（signal +0.59R → 模拟 auto +0.63R → 08-12 后 −0.55R）；⑤ 一级降频线首次实际触发（08-21 两笔亏满型连败），建议 08-24 当日不开新仓；二级暂停线不触发（P(g>0)=94.7% 距 80% 线 15pp）。
- **回归风险评估**：无——纯复盘产物新增 + 数据口径修正；trades CSV 追加不改动既有 42 笔历史数据（ts csv 仅改 08-18 腾讯#1 一处 raw_low 记录口径，MAE 从误记的浮盈回落点改为开仓即最低、与 actions 过程指标记录一致性提升）。

### 变更（下单必须用限价单：市价单脚本级禁用，港美开仓主单 / 平仓普通单全部改限价，2026-08-23 用户立）

- **为什么改**：用户立规「下单改成必须用限价单」——市价单在快速波动中成交价不可控（尤其 3x ETF 滑点），限价单锁住成交价格范围；此前 2026-08-07 为高波动标的成交率改的市价单默认废止。
- **改了什么**：① 港股 `open_position_tiger.py` 默认 lmt、`--order-type mkt` 直接拒单（`blocked_by:"market_order_forbidden"`）；② 美股 `submit_order_with_stop_us` 从硬编码 MKT 改回 LMT（限价 = 盘口对价取整 tick）；③ ~~港美股平仓改对价限价单~~（当日修订撤销——平仓不下普通订单，见下方修订说明；`submit_limit_order_tiger / _us` 保留供移盈脚本等场景用）；④ `submit_market_order_tiger / _us` 函数体改为直接 raise（禁用期保留，AI 例行下单不可达）；⑥ SKILL.md / auto-mode.md 同步（含市价止损 / 市价止盈条件单**不受限**的边界说明——条件单触发后的成交方式是券商条件单语义，见「禁止限价止损」节）。
- **⚠️ 当日修订（2026-08-23 用户纠正口径）**：「下单必须用限价单」**只管开仓**——平仓**不下普通订单**（限价 / 市价都不下），一律走「止损单触发价 = 现价」让止损单市价触发成交（情况不对立马跑路）；**没有止损单就再设一个止损单、止损价 = 现价**。当日早些时候误改的「平仓直发 / fallback 对价限价单」两条路径已删除、平仓脚本重写为无条件走交替逼近循环（见下一条止盈单机制条目的当日修订段）。
- **回归风险评估**：① 开仓成交率回退——限价 + 8 秒超时在高波动标的可能错过成交（2026-08-07 MINIMAX 教训场景），此为用户明确取舍、超时按「挂起退出、AI 决策」处理；② 平仓确定性——市价触发成交保障完整保留（止损单触发即市价离场），无回退；③ 止损 / 止盈条件单（STP/LOSS/PROFIT 腿）触发后的市价成交**未改**——「禁止限价止损」规矩（2026-07-31 立）仍然生效。

### 变更（止盈单机制：开仓双腿 LOSS+PROFIT + 新增移盈动作 + 主动平仓改「止损/止盈交替逼近」循环，2026-08-23 用户立）

- **为什么改**：用户指令——① 开仓主订单的附加单里除止损单（市价触发）还必须有止盈单（市价触发）；② 新增移动止盈动作（理念与移动止损一致放趋势反转位，但止盈放结构性高点、可双向移动，止损永远单向）；③ 主动平仓改为「先把止损单触发价改为现价 → 没触发再把止盈单触发价设为现价 → 仍没触发循环逼近，止损价与止盈价不断接近逼迫平仓」；④ 止损单失效（插针触发但平仓失败）必须立刻重设止损单、触发价设为现价。
- **改了什么**：① `trade_utils_tiger.py` / `trade_utils_tiger_us.py` 的 `submit_order_with_stop_*` 加 `profit_price` 参数——LOSS+PROFIT 双腿齐发时 SDK 自动序列化为 `attach_type='BRACKETS'` 括号订单（查证：SDK 源码 `model.py _parse_leg_param` + 官方 placeOrder 文档「附加订单的主订单类型暂时仅支持限价单」，与同日限价单新规正好吻合；任一边触发成交另一边自动作废、主单撤则两腿同步取消）；② 开仓脚本（港美）把 target 传作 PROFIT 腿触发价；③ 新建 `move_target_tiger.py` / `move_target_tiger_us.py`（移盈动作：modify 止盈单触发价为主路径、0 个补新、≥2 撤多余、modify 失败 fallback 先下新+撤旧；方向硬校验「新止盈未穿越现价」——穿越 = 平仓语义归 close 脚本）；④ `close_position_tiger*.py` 主路径重写为「converge_loop 交替逼近」——奇数轮 modify 止损单触发价→现价、偶数轮 modify 止盈单触发价→现价、每轮先复查持仓（条件单已成交而轮询滞后时按已平收尾，防超卖反向开仓）、止损轮发现无活动止损单立刻重设 STP 触发价=现价（失效保护）、循环上限 8 轮耗尽如实上报不撤条件单；⑤ 止损单判定函数（move_stop / close × 港美 6 处）全部排除止盈单（PROFIT 腿 / attr_desc 含止盈 / 带 parent_id 的附加单落成单），防误改误撤；新增 `_is_profit_order` 判定与 `_cancel_residual_profits`（平仓后撤残留止盈单兜底）；⑥ 动作体系从 3 类扩为 4 类（开仓 🟢 / 平仓 🔴 / 移损 🟡 / 移盈 🔵），SKILL.md / auto-mode.md / signal-mode.md / trading-strategy.md 全部同步（含用户口述的移动止盈理念：止盈放结构性高点、高点没有更高则放当前高点（双顶/头肩顶形态）、趋势还在则远移、止盈可双向止损单向）。
- **验证**：全部 10 脚本语法校验通过；止损/止盈判定函数 6 文件 8 场景单元测试全过（STP/TRAIL/PROFIT 腿/attr_desc/parent_id/普通挂单）；PROFIT 双腿序列化参数单元验证（attach_type='BRACKETS' + profit_taker_price）；**paper 端到端待下个交易日实测**（PROFIT 腿落成后的 order_type / attr_desc 真实取值、双腿一次提交成交、移盈 modify、交替逼近平仓全链路——已记 TODO）。
- **⚠️ 当日修订（2026-08-23 用户纠正口径）**：平仓脚本当日早些版本保留了「无活动条件单 → 直发对价限价单」与「fallback 撤止损 + 限价平仓」两条普通订单路径——用户纠正后**全部删除**：平仓唯一机制 = 交替逼近循环（含无止损单时补设止损单触发价=现价，循环第④步），港美两脚本重写、`_fallback_cancel_and_mo` 函数删除。
- **回归风险评估**：① 平仓主路径从「modify 止损单一步」变「交替逼近循环」——首轮行为与旧主路径等价（先改止损触发价），新增止盈轮与失效重设都是增强、无既有保护被删；② 平仓后残留清理从「撤止损单」扩为「撤止损 + 止盈单」，BRACKETS 理论一边成交另一边自动作废、复查兜底双保险；③ 旧持仓（无止盈单）平仓时循环自动跳过止盈轮（无活动止盈单 → 切回止损轮），兼容无止盈单开仓的历史持仓。

### 变更（止损单铁律：持仓在 → 止损单必须存在 + 失效恢复路径，2026-08-23 用户立）

- **为什么改**：用户立兜底规定——只要持仓在，止损单就必须存在；持仓期间实时监测止损单状态；瞬时插针触发止损但平仓失败、价格反弹后若不想立马平仓，必须立刻恢复之前失效的止损单再继续盯盘。此前的持仓状态行只把「无活动止损单」当普通文本显示、无告警级别与处置指引。
- **改了什么**：① `account_status.py` 持仓状态行升级——「账户仍有持仓 + 无活动止损单」输出 🚨 铁律告警 + 恢复命令行（move_stop + 失效前止损价）；② `_parse_actions` 事件加 `stop` 字段（开仓「止损」行 / 移损「新止损」行解析止损价）+ 新增 `_last_recorded_stop`（取该标的当日最新记录止损价，供恢复指引）；分节正则兼容「## 🟢 开仓」与无 ## 前缀两种历史格式（原 `\n## ` 切分漏掉无前缀节）；③ 恢复路径复用 `move_stop_tiger*.py` 既有「无活动止损单 → 下新 STP」分支（传失效前止损价即可，方向校验天然放行——反弹后现价已回到安全侧），不新建脚本；④ SKILL.md 硬性护栏第 4 条 + auto-mode.md 移损条后写入铁律条文（含「二选一、当段执行、不许裸奔继续盯盘」）。
- **验证**：`_parse_actions` 对 2026-08-18 / 08-21 真实 actions 文件解析验证通过（开仓止损 40.50 / 330.0、移损新止损 443.0 全部取到）；语法校验全过。盘中端到端（插针场景告警 → 恢复）待交易日实测（并入 T116）。
- **回归风险评估**：① 分节正则变宽只影响解析覆盖面（多解析到历史漏掉的节），持仓推导行为不变差；② 告警新增不改变既有「账户已无持仓」「数量不一致」告警；③ move_stop 分支 0 是既有路径、零改动（仅补注释）。

### 变更（T1 第一步：公开仓库敏感行为记录清理——工作区 8 文件中性化改写，2026-08-23 执行）

- **为什么改**：2026-08-21 排查定案（TODO T1）——仓库虽已 gitignore 账户号 / 凭证 / 净值，但代理交易的行为记录本身被 git 跟踪公开在 GitHub（节点 IP 明文清单、订阅商名、监管拒单报文原文、境内规避实测故事线），等于书面自证；趁 0 fork 清理代价最低。
- **改了什么**：8 类文件中性化——① `accounts.md` 第 14/15 段（节点 IP 串 / 域名体系 / 监管拒单故事线 → 「节点清单与白名单镜像存本机配置」+ 中性技术结论保留）、第 17/19 段（本地出口俗称 → 中性表述）；② `trade_utils_tiger.py` docstring × 3 处 + 报错提示；③ `config.example.json` 注释 × 2；④ `SKILL.md` 33 行「实盘下单网络环境」段；⑤ `live_unlock.py` / `proxy_guard.py`（订阅商名 → 代理节点 × 6）/ `ws_segment.py` / `account_status.py`；⑥ `CHANGELOG.md` 12 处历史条目（IP / 域名 / 节点代号 / 报文 / 灰色地带定性段脱敏，**为什么改 + 改了什么纪律保留**）；⑦ `TODO-archive.md` 30/63 行；⑧ `auto-mode.md` 1 处。**复扫验证**：节点 IP / 入口域名 / 订阅商标识 / 监管错误码与报文原文 / 本地出口俗称全部零命中（「境外标的」3 处保留——指跨境 ETF 跟踪的韩国股市、中性金融术语）。
- **边界**：本条只是第一步（工作区）；第二步 git 历史重写（filter-repo + force push）属破坏性操作，待用户确认后单独执行（T1 第二步待办仍在）。

### 变更（T2：README 双版免责声明补齐 + LICENSE 追加投资风险免责条款）

- **为什么改**：交易项目公开边界配套（TODO T2）——MIT 的 AS-IS 条款只覆盖软件缺陷、不覆盖投资损失，须显式写。
- **改了什么**：① README.md / README_cn.md 风险声明段补「任何回测或模拟盘结果亦不代表实盘表现 + 用户对交易决策及损失承担全部责任」句（两版同步）；② LICENSE.md 文末追加中英双语「投资风险免责条款」段（不构成投资建议、回测不代表实盘、风险自担、补充而非替代 MIT 条款）。
- **回归风险**：无——纯文档追加，不动 MIT 条款原文。

### 修复（TODO / MEMO 编号加粗脚本截断事故：批量脚本切片 bug 把条目正文截空，从多恢复源全量重建）

- **为什么改**：上一条「全量补编号」执行时，第二步「编号加粗」脚本存在切片 bug（`m.group(0)[m.end(3)+1:]` 起点算错），把所有被匹配条目的正文截成空壳（只剩 `- [ ] **Tn** `），共波及 3 个文件 117 条。发现后立即启动恢复（无 Time Machine / APFS 快照可用）。
- **改了什么**：多恢复源重建并回写——① git 暂存区 / HEAD 旧版（TODO.md / TODO-archive.md）；② Claude Code file-history 检查点（Edit 前快照，TODO.md（87ba25da）、TODO-archive（4cfa2ad8@v2，106 条）、MEMO（9424cd2c@v2）+ 归档尾段 heredoc 转写）；③ 会话转写重放（按时间序重放历史 Edit / heredoc 写入，补齐检查点之后的新增条目，如 DayTradingAgent 今晚新增的 5 条活跃待办与「2026-08-21 批量处理」4 条归档）。重建后统一按规则加粗编号（**Tn** / **Mn**），DayTradingAgent 连续 T1~T115、DayTradingAgent-win 连续 T1~T44，正文经抽样与恢复源逐字一致。受损壳快照留存本机 tmp（/tmp/todo-damage-backup/）。
- **边界**：恢复目标是「截断事故前的状态」（即编号未加粗、但已编号的正文完整版）；编号加粗为规则要求的新格式。git 未提交的其它改动不受影响。
- **补遗（同晚复核发现）**：MEMO.md 头部说明在恢复基线（08-19 22:26 检查点）之后 8 分钟有一笔措辞 Edit（「移入 TODO-archive 归档」→「移入专属 MEMO-archive.md」），首轮重建漏收、当晚用户追问「是否 100% 复原」时经 gap 写入全量审计发现并补回。补回后经四道验证（事故前快照前缀比对 16/16、gap 写入 7/7 覆盖、file-history 终点旁证一致、条目数与编号连续性全对），174 条条目确认无内容丢失。

### 变更（TODO / MEMO 条目全量补编号：按新立待办编号规则一次性补齐存量）

- **为什么改**：2026-08-21 用户新立全局规则「每条待办必须有唯一待办编号」（格式 T+序号 / M+序号，如 T11 / M11，连写、项目内递增、永不复用、归档保留），并指示存量待办与归档待办全部补上编号——编号用于用户与 AI 针对性沟通（「T11 处理了吗」），避免复述长正文。
- **改了什么**：TODO.md 5 条 + TODO-archive.md 110 条待办补编号（T1~T115）；MEMO.md 2 条备忘补编号（M1~M2）。正文内容零改动（只插入编号，不改写、不重排、时间戳不变）；编号顺序 = 活跃文件在前、归档在后、文件内按行序。
### 新增（实盘误开防护：会话级解锁 + 网络层物理拒单，2026-08-21 19:20）

- **为什么改**：auto 模式防 AI「不小心」开仓到实盘（误传 `--account live`、上下文残留、混淆型失守）。2026-08-18 早盘实证：散文双闸（用户明说 + AskUserQuestion）靠 AI 记忆执行会衰减——一早三起违规、工具在场的全守住、靠记忆的全失守；且现状唯一防线只剩散文双闸，下单脚本只解析 `--account live` 不拦，`apply_scope: all` 下 live 开仓自动挂代理、白名单放行，误传 live 即真钱成交。方案核心（用户提出）：老虎按「指令发出网络环境」判定 + IP 白名单只认代理节点入口 IP（本地直连实测被拒「access forbidden」、连接层就断）——改造后**默认态实盘请求不挂代理 → 本地直连 → 被白名单物理拒**，条件成交概率从 ≈1 压到接近零。
- **改了什么**：
  - **开工第一步实测（设计前置疑点）**：PostToolUse hook 对 AskUserQuestion **触发**、payload 含 `tool_response.answers`（用户点选答案由 harness 生成、AI 伪造不了）与 `session_id`——临时 hook 写日志坐实后删除，授权链走 hook 方案、无需 osascript fallback。
  - 新建 [`live_unlock.py`](.claude/skills/trade/scripts/live_unlock.py)：解锁文件校验（token = sha256(salt + 实盘账户号 + 到期戳 + session_id)，原料存 accounts.json 内部、AI 读不到 → **AI 写了也无效**）+ `verify-auth`（验短时凭证 → 写解锁 → 焚凭证）+ `revoke` + `status`（打码口径）+ `live_gate_for_order_scripts`（下单脚本前置闸）+ `--init-salt`（salt 生成写入，值不经 AI 上下文；已初始化）。
  - 新建 [`live_auth_witness.py`](.claude/hooks/live_auth_witness.py)（PostToolUse matcher AskUserQuestion，[settings.json](.claude/settings.json) 注册）：见证真实点击——问题命中实盘授权特征（「实盘」+ 打码号 / `--account live`）且答案为确认词 → 写 10 分钟短时凭证 `live-auth.local`（HMAC 密钥 = salt，用后即焚、一次点击只换一次解锁）；含糊提问 / 非确认答案不产生凭证。
  - **检查点①（下单脚本闸）**：6 个下单脚本（open/close/move_stop × 港美）解析到 `--account live` 后即调 `live_gate_for_order_scripts`——解锁无效输出 `{ok:false, blocked_by:"live_locked"}` 拒单；顺带修 `close_position_tiger_us.py` 的 `--account` 空格形式解析 bug（原版只跳过不赋值，`--account live` 落 None = 静默走模拟盘、live 闸对其失效）。
  - **检查点②（网络层闸）**：[`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `apply_proxy` 加实盘解锁闸——当前账户为实盘且解锁无效 → 跳过挂代理保持本地直连（白名单连接层物理拒）+ stderr 打印授权路径；解锁校验异常也按未解锁处理（fail-closed）。模拟盘路径零改动（照旧挂代理，金丝雀保留、`apply_scope: all` 不动）。
  - 配套：[.gitignore](.gitignore) 加 `live-unlock.local` / `live-auth.local`；[accounts.example.json](.claude/skills/trade/accounts.example.json) 加 `live_unlock_salt` 占位说明；[config.example.json](.claude/skills/trade/config.example.json) proxy 节补解锁闸说明；[SKILL.md](.claude/skills/trade/SKILL.md)「auto 模式的账户选择」、[auto-mode.md](.claude/skills/trade/references/auto-mode.md) 总则、[accounts.md](.claude/skills/trade/accounts.md) 新增「实盘误开防护」节同步。
- **验证**：① hook 四场景单测（实盘+确认写凭证 / 实盘+拒绝不写 / 普通问题不写 / 非 Ask 工具不写）全过；② **授权链端到端**（真发授权式 AskUserQuestion → 用户点击 → hook 写凭证 → verify-auth 写解锁 → 凭证焚毁 → revoke 收回）全通；③ 会话绑定三场景（本会话 ok / 伪造其它 sid bad_token / 人工终端无 sid 放行）全对；④ 全 6 脚本误传 `--account live` 全部 `live_locked` 拒单；⑤ 无解锁时 `load_config(account='live')` 实测不挂代理（stderr 打印「实盘代理未解锁」）、模拟盘照旧挂代理；⑥ 11 个改动 py 文件 py_compile 过、3 个 JSON 合法、敏感扫描无完整账户号 / 净值泄漏。
- **⚠️ 回归风险提醒**：① **实盘默认行为变化（修复目的本身）**——`--account live` 从「挂代理直接可用」变「默认拒单 + 不挂代理」，实盘交易日起步须先走授权流程（AskUserQuestion 点击 + verify-auth，24h 有效、resume 换 sid 后需重新授权一次）；② `close_position_tiger_us.py` 的 `--account` 解析修复顺带改变其行为——旧版 `--account live` 静默走模拟盘，新版正确切 live（然后被闸拦）——无既有依赖被破坏（旧行为本身就是 bug）；③ 下单脚本前置闸在时间闸 / 行情查询之前——误传 live 的单不再产生任何 SDK 网络请求，正常模拟盘单完全不受影响；④ 残余边界如实保留：环境变量冒用会话身份（须翻 transcript 的多步蓄意动作）、自写代码主动挂代理（券商侧白名单 + 监管拒单兜底）、解锁后 24h 开窗为设计内窗口（钥匙在用户手里）。
- **首个实盘交易日待实测三件**（沿设计清单⑧）：模拟盘走代理照常下单 ✅（本条验证⑤已覆盖代理部分）、误传 `--account live` 被拦已验 ✅（开盘时段复跑一次）、解锁后实盘查询链路通（开盘时段授权 + get_assets 验证——当日未做，实盘日第一天补）。

### 变更（watcher 收缩与体验修复：删除空转警报功能 + 铃声去横幅，2026-08-21 19:20）

- **为什么改**：① **空转警报删除（用户 2026-08-21 13:01 立）**——watcher 的「采样在跑但分析心跳停更 ≥180 秒 → 弹窗」整体移除：盯盘空转防护已由会话内 monitor_guard 空转硬门（PreToolUse 阻断 + 引导写心跳）独扛，watcher 侧的外部检测属重复层；② **铃声去横幅（用户 2026-08-20 09:49 立、09:53 修订）**——原铃声经 `display notification "" sound name "Basso"` 发出，该调用在响铃的同时弹一条空内容横幅，与 2026-08-19「横幅 → 屏幕中心弹窗」初衷相悖（主提醒已由弹窗承载，横幅纯属多余）。
- **改了什么**：① [monitor_watcher.py](.claude/hooks/monitor_watcher.py)——空转检查逻辑（`analysis_beat_fresh` / `_sampling_log_age_seconds` / `sampling_running` + main 空转分支）、`ANALYSIS_BEAT_STALE_SECONDS` 常量、注释与文件头描述全删，main 仅剩白名单漂移检测；铃声改 `afplay /System/Library/Sounds/Basso.aiff`（不产生任何横幅），弹窗与 tmp/watcher_notify.log 持久日志照旧；② [com.daytrading.monitor-watcher.plist](.claude/hooks/com.daytrading.monitor-watcher.plist) 注释同步（已同步部署到 ~/Library/LaunchAgents/）；③ [preflight.py](.claude/skills/trade/scripts/preflight.py) `_ensure_watcher` 打印文案同步（「白名单漂移检测，低频不扰民」）；④ [monitoring.md](.claude/skills/trade/references/monitoring.md) 分析心跳节（watcher 引用改为 monitor_guard 独扛）与 watcher 沿革段、[SKILL.md](.claude/skills/trade/SKILL.md) 停盯节同步。
- **验证**：monitor_watcher.py / preflight.py py_compile 过；`afplay` 实测可播放；plist `plutil -lint` OK 且已同步到 LaunchAgents（launchd 项在跑）；全仓 grep 确认无「watcher 空转警报」残留表述。
- **⚠️ 回归风险提醒**：① **删空转警报 = 外部空转监督层退场**——AI 空转（只采样不分析）现在只有会话内 monitor_guard 空转硬门拦（hook 阻断重启采样、逼先写心跳）+ 用户监督；若连 monitor_guard 也被绕过（如 kill 会话后采样进程残留），watcher 不再报警——这是用户明确的取舍（2026-08-20~21 两轮收缩均用户裁定），如实记录；② 铃声从 osascript 换 afplay——若系统音量为 0 或 Basso.aiff 路径变化（系统升级）铃声静默，弹窗仍在（主提醒不受影响）；③ 2026-08-17 立的「空转检查补盲区」修复随本删除一并退场——monitor_guard 空转硬门（2026-08-17 修法④）不受影响、仍全量在场。

### 修复（美股开仓购买力降档查询误调港股版：补美股版查询，2026-08-21 19:20）

- **为什么改**：2026-08-20 04:20 美股盯盘实测发现——`open_position_tiger_us.py` 的购买力主动降档逻辑调 `T.get_buying_power_tiger`（港股版），其内部 `to_tiger_symbol` 只认 `HK.xxx`、遇美股代码抛「老虎脚本只支持港股」→ 主动降档恒失效（未阻断主路径、下单仍走被动降档兜底，但正常路径会烧降档轮次、输 ⚠️ 警告）。
- **改了什么**：[trade_utils_tiger_us.py](.claude/skills/trade/scripts/trade_utils_tiger_us.py) 新增 `get_buying_power_us`（口径与港股版同构、美股更简单——buying_power 与 ref_price 同为 USD 无汇率换算：可买股数上限 = buying_power × long_initial_margin ÷ ref_price，查不到保证金率按 1.0 全额保守）；[open_position_tiger_us.py](.claude/skills/trade/scripts/open_position_tiger_us.py) 改调本市场版。
- **验证**：mock 三分支单测（正常 25 股 / 无保证金率按 1.0 / 查询失败返回 None）全过；模拟账户实测 `US.MU @200` 返回 (6033, 4022610.43, 0.3)——真实链路不再抛错、保证金率取到 0.3；两文件 py_compile 过。
- **⚠️ 回归风险提醒**：仅换查询实现、降档逻辑与阈值不变；港股版 `get_buying_power_tiger` 未动（港股调用方不受影响）。美股购买力口径此前从未真正生效过（恒失败走兜底），本次首次生效——首笔美股大额单留意 `capped_by_buying_power` 字段是否符合预期。

## [Unreleased]（2026-08-21 13:00 前条目）

### 新增（企稳判定工具强制：开仓脚本当前价净赔率硬校验 + 企稳维持时长在场打印，2026-08-21 13:00）

- **为什么改**：2026-08-21 早市实盘 07709 追高事故复盘——AI 把「VWAP 贴线一瞬（采样 log 中仅 1 秒）当企稳」开多 @40.82（信号点 40.56 后 4 分钟、偏离 VWAP +0.6%），2 分钟止损 -1.18R。「企稳四要素」（trading-strategy.md「右侧入场总则」，真支撑 + ≥2 次测试不破 + 维持 ≥10 分钟 + 确认事件）写进文档但 AI 靠记忆执行会衰减（同 MINIMAX 2026-08-17 分钟级「三段低点抬升」冒充企稳、2026-08-18 用户「散文规定在上下文压缩后会衰减，工具强制不会」对照实验）——且发现**脚本层有两处真空**：① 开仓脚本 `check_price_in_range` 只在价格超出 [0.6, 10] 机械范围时拒单，从不拦 `odds_at_current < 2.4`——事故当天参考价 40.70 口径赔率 3.82 达标、当前实价 40.90 口径 1.41 不达标仍放行（参考价过旧/现价偏离信号位，AI 按旧参考价通过门槛）；② 「企稳维持 ≥10 分钟」无可机械核验手段，全靠 AI 记忆。
- **改了什么**：
  - [open_position_tiger.py](.claude/skills/trade/scripts/open_position_tiger.py) / [open_position_tiger_us.py](.claude/skills/trade/scripts/open_position_tiger_us.py) 各加两道工具强制：**硬校验「当前实价净赔率 < 2.4 一律拒单」**（`odds_at_current` 净口径、与 2.4 门槛同口径；输出 `{ok:false, error:…}` 并提示刷新参考价为最新实价/等回踩到赔率达标位再评估）+ **企稳维持时长在场打印**（`_stability_minutes` 读当日采样 log 统计现价 ±0.3% 区间最近连续维持分钟数，< 10 分钟输出 `stability_minutes` + `stability_warning` 提醒「不满足企稳定义『维持 ≥10 分钟』」，突破入场不受限、仅提醒不硬拦）。
  - [trading-strategy.md](.claude/skills/trade/references/trading-strategy.md)「企稳的可操作定义」节末尾补工具强制说明段（两道强制 + 事故复测数据）。
- **验证**：`_stability_minutes` 对今日实盘 log 复测——40.56（事故信号点）附近维持 **0.0 分钟**（一瞬贴线精确被抓）、40.44 维持 0.35 分钟；事故场景模拟（现价 40.90、参考价 40.70）`odds_at_current` 1.13 < 2.4 → 新硬校验**拒单**；正常回踩场景（现价 40.56 = 参考价）13.2 ≥ 2.4 放行；两脚本 py_compile 过。
- **⚠️ 回归风险提醒**：① **开仓脚本新增拒单路径**——按当前实价净赔率 < 2.4 的单从「照常下」变「拒单」：方向上正是「初始净预期赔率 ≥ 2.4 才开」门槛的工具化，属修复目的本身；但突破追高场景（现价已在信号位上方、赔率按现价算不达标）会被拦——这正是今天事故的应有拦截，AI 收到拒单后按提示等回踩即可，非不可绕过；② **企稳维持时长是只提醒不硬拦**（突破入场等形态不要求维持时长，硬拦会误伤）——仍是「决策时刻工具在场打印」层，非全自动，与 2026-08-18「文档规定尽可能配工具强制」三层中的②对应；③ 今日早市已平仓一笔 07709（-1.18R，actions/2026-08-21-HKT-actions.md），本规则为事后落地，不影响已发生交易。

## [Unreleased]

### 新增（入场点候选清单 + 止盈定价顺序规则落地：SKILL.md 自查两条 + 开仓脚本止盈可达性硬校验，2026-08-21 12:15）

- **为什么改**：2026-08-21 早市 MINIMAX（HK.00100）实盘一笔复盘暴露两个深层缺陷——① **入场点「视而不见」**：09:54-10:09 箱体 330 回踩企稳（真支撑 + 2 次测试不破 + 维持 20 分钟，满足既有「企稳」定义）已成立却未入场，因 AI 脑中剧本是「先深回踩 316 → 再突破 336.6」，注意力挂在剧本节点上、剧本外真实出现的合格入场点被忽略；10:22 二次上攻 337-338 **无新高**（实为双顶右肩）却被当「突破确认」追入 @337.2，最终止损 330 被动触发 −1.14R（4.17R → 3.03R → −1.14R）。② **止盈定价从赔率反推**：止盈 360 是先设「净赔率 ≥2.4」反推出的数字，当日高点 338 就在头顶（越过 +6.5%），当日不可能走到——「目标止盈位必须大概率可达」规则（2026-08-03 立）文档里早有却没被执行，属「文档规定必须尽可能配工具强制」的活案例：散文规定衰减、工具强制不衰减。
- **改了什么**：
  - [SKILL.md](.claude/skills/trade/SKILL.md) 开仓前自查清单新增 2 条：**入场点候选清单**（候选 1 条件满足即执行、禁止跳过等候选 2；「突破回踩确认」只适用于历史/近期显著结构位，日内刚创新高附近反复测试不创新高 = 测试/双顶雏形禁止追入；策略连续切换 = 停下评估箱体位置，顶部只平不追）+ **止盈定价顺序**（先定止盈结构目标 → 再算赔率 → 达标才开；禁止从赔率门槛反推止盈价；止盈止损同尺度，日内交易一律以当日/近期结构为准，引用日线级别目标判不合格）。
  - [trading-strategy.md](.claude/skills/trade/references/trading-strategy.md)「止盈完全靠盘面实时判断」节补「止盈定价顺序：先结构目标、后赔率」子节（与 2026-08-03「目标止盈位必须大概率可达」衔接，明确禁止反推 + 同尺度 + 脚本校验说明）。
  - [open_position_tiger.py](.claude/skills/trade/scripts/open_position_tiger.py) 价格范围检查后新增**止盈可达性硬校验**：做多止盈越过当日高点 / 做空止盈跌破当日低点 → `{ok:false, error:…, target_reachable:false}` 拒单并提示调低重估或换入场（确有放量突破/新催化等增量信号才允许上调——边界：只拦死越过高点的明显不可达目标，≤当日高点放行）；行情对象 high/low 取自既有 `get_quote_tiger` 返回的 `high_price` / `low_price` 字段，零新增 API 调用。
- **验证**：校验逻辑模拟四场景全过（MINIMAX 实况 360 vs 当日高 338 → 拒；修正 ≤当日高 → 放行；做空跌破当日低 → 拒；正常做多 → 放行）；py_compile 过；SKILL.md / trading-strategy.md 新条目 grep 确认就位。
- **⚠️ 回归风险提醒**：① **开仓脚本新增拒单路径**——止盈越过当日高点的单从「照常下」变「拒单」：方向上正是「目标止盈必须大概率可达」规则的工具化，属修复目的本身；但极端行情下（放量突破当日高点后继续追、目标价合理越过当日高）会被误拦——提示文案已写明「确有增量信号才允许上调」，AI 收到拒单后按提示人工评估、确有增量信号可重下（脚本不拦重试），非不可绕过；② **入场点候选清单 / 止盈定价顺序是自查清单条目（决策时刻在场约束）**——无法完全脚本化（需结构判断），仍属「文档规定」层，靠自查清单在场打印约束，与既有其它自查条目同待遇；③ 今日早市已平仓一笔 MINIMAX（−1.14R，actions/2026-08-21-HKT-actions.md），本规则为事后落地，不影响已发生交易。

### 修复（标的池互斥失效：guard 管道绕过 bug + 三层拦截扩容，2026-08-21 00:20；00:55 修正归因）

- **为什么改**：2026-08-20 港股下午盘四会话并行盯盘实录——**四个会话用户指令全为「开始盯盘，自动模式，实盘」（全是 auto 会话**，转录首条用户消息逐一核实；初版 CHANGELOG 误按 monitor_log 后缀 `_signal` 把其中两个归为 signal 会话，00:55 修正——后缀错的真正原因见同条「futu_ws_segment mode 缺陷」），00100 / 01810 / 09988 / 09992 等标的同时被两会话采样（monitor_log 两份日志时间重叠 13:00-14:00 铁证），重复盯一小时、`monitor_guard` 的标的池拦截 **0 次命中**。排查确认**双重根因**：
  - **根因 1（guard 管道绕过 bug）**：真实采样命令几乎都带输出管道 `…futu_ws_segment.py 40 HK.… 2>&1 | grep -vE '…'`，而 guard 的「提及未执行」排除正则 `\b(cat|head|tail|diff|grep|…)\b` 是**整条子命令**匹配——管道右侧的 grep 把正在执行的采样段误判为「提及脚本文件」剔出 exec_seg，`_pool_conflict`（连同 A4 连跑 / 跑法 / 空转三项检查）对管道形态命令**全部失明**。自 2026-08-05 该排除逻辑引入起所有管道形态采样命令都不受 guard 约束。
  - **根因 2（先启动会话按条件语跳过认领）**：SKILL.md 启动序列把认领写成「（多会话并行时 pool_claim.py claim …）」的条件步骤。实录时序：先启动的两个会话（a344f08a / b429649d）启动时认领表为空、preflight 显示在场会话数 1，按条件语合理判断「单会话不需要认领」跳过 claim（b429649d thinking 原文：「现在在场会话数 1，不需要 pool_claim」）；**后启动**的两个会话（2802d79a / a234f4f6）跑了 claim——但先启动者已在盯、其 targets 不在认领表里，跨池冲突检测（只查表内标的）对先启动者**天然失效**（「未认领 ≠ 未占用」），两两会话重复盯一小时无人拦。互斥语义败在时序：认领检查是启动时一次性的，没有持续校验。
- **改了什么**：
  - [monitor_guard.py](.claude/hooks/monitor_guard.py) ① **管道首段判断**（根因 1 修复）：「提及未执行」排除词只看**管道首段**（执行体在首段 `python3 … segment.py …`，尾部 grep / head 只处理输出、不是执行体）——首段干净即算「正在执行采样段」，管道形态命令回归四项检查覆盖面；② **互斥扩三层**（根因 2 修复）：层1 跨池冲突（原语义）+ 层2 **本会话未认领就采样**（认领是采样的前置——不认领就不给采样）+ 层3 **孤儿标的**（targets 含无人认领的标的，堵「先斩后奏」加标的）；层2/3 只在认领表存在活认领时生效（单会话 / 认领制未用 / 认领者全死 → 不拦，不误伤）——层2 同时封掉实录的时序洞：先启动者跳过认领后，后认领者一出现、先启动者的下一段采样即被层2 拦下（每段启动都重新校验，不是启动时一次性）；③ **死会话 hook 侧只读剔除**：判定认领占用时同 pool_claim 口径（jsonl 停更 >30 分钟 = 死，其认领不占坑）——只读不写回（hook 高频跑不宜持 pool_claim 的 flock），真正落盘清理由认领者下次跑 pool_claim 完成；④ 会话标识改 payload `session_id` 优先、环境变量兜底（实测两者一致，payload 更稳）。
  - [SKILL.md](.claude/skills/trade/SKILL.md) 启动序列：认领从「（多会话并行时…）」的条件步骤改为**无条件步骤**（signal / auto 同规，认领表有活认领时不认领就被拦）；[monitoring.md](.claude/skills/trade/references/monitoring.md)「标的池自动划分」第 1 / 5 条同步（第 5 条改三层拦截说明 + 管道 bug 沿革）。
  - [preflight.py](.claude/skills/trade/scripts/preflight.py) 认领状态打印补「在场提醒」：表里有别人的认领且本会话无认领时，打印「采样前先 claim，否则会被拦」（决策时刻工具在场层）。
- **验证**：[tmp/test_pool_guard.py](tmp/test_pool_guard.py)（临时测试脚本）21 项全过——A 组管道误剔修复 7 项（实录管道形态被认出 / grep / cat / py_compile 引用仍排除 / && 连跑计数不回归）、B 组三层互斥 11 项（跨池拦 / 自己认领放行 / 未认领拦 / 孤儿拦 / 死会话不占坑 + 全死 = 认领制停用放行 / 空表 / 跨日 / 无 sid 放行 / HK.700 补 0 同口径）、C 组在跑会话采自己标的放行、D 组真实 hook 端到端（管道命令 + 当日认领表 → 层2 拦）。另：修复期间本排查会话的 heredoc 测试命令（含采样命令形态）被修复后的空转门实际拦截——管道形态命令已进 guard 覆盖面的实证。
- **⚠️ 回归风险提醒**：① **管道首段判断放宽了 exec_seg 认定**（旧：整条子命令无排除词；新：首段无排除词即算）——理论上「`python3 … segment.py … | grep …` 首段恰好无排除词但整条另有 cat」的组合会从「排除」变「计入」，方向是**多拦不漏拦**（guard 设计取向即宁可多提醒），且 A4 连跑计数已回归验证；② **层2/3 是新增硬门**——已认领的多会话场景下，「没跑 claim 的会话」从「静默重复盯」变「被拦停」——这是修复目的本身，但首个交易日实跑时若 AI 被拦后不按提示 claim 而是反复重试采样，会形成阻断循环（提示已写明处理路径，风险可控）；③ 死会话剔除是 hook 侧只读判定——与 pool_claim 落盘清理存在最长 30 分钟口径窗（jsonl 停更 30 分钟内 hook 仍认占坑），与 pool_claim 原口径一致、非新增风险；④ 旧 CHANGELOG「⑰ guard A4 按串联操作符切分子命令计数」修复（2026-08-05）在管道形态下的失效本次一并修复——非回归、是补全。

### 修复（采样 mode 标识错档：futu_ws_segment 不认 --mode、parse_mode 不读环境变量，2026-08-21 00:55）

- **为什么改**：2026-08-20 港股四会话实录中两个 auto 会话（a344f08a / b429649d）的采样 log 全部落成 `_signal` 后缀（monitor_log / reassess_marker 错档，resume 按 mode 查断层也读不到）——排查发现**两层缺陷叠加**：① [futu_ws_segment.py](.claude/skills/trade/scripts/futu_ws_segment.py) 的 mode **只读环境变量**（`MODE = os.environ.get('MODE', 'signal')`），2026-08-16 三采样脚本统一 `parse_mode()`（认 `--mode` 参数）时把它漏掉了——另两个正常会话恰好用 `MODE=auto python3 …` 前缀形态才没踩坑，这两个会话什么都没带、默认落 signal；② [trade_utils_tiger.py](.claude/skills/trade/scripts/trade_utils_tiger.py) `parse_mode()` **只解析命令行、不读环境变量**——ws_segment 2026-08-16 切到 parse_mode 时的注释声称「兼容 --mode 与旧 MODE 环境变量两种来源」，实际环境变量分支从未实现（git 历史证实切换时直接删了 `os.environ.get`），`MODE=auto python3 ws_segment.py …` 至今落 signal（潜伏缺陷，靠会话用 futu_ws_segment 才未爆发）。
- **改了什么**：① `parse_mode()` 补环境变量分支——优先级 `--mode` 参数 > `MODE` 环境变量 > 默认 signal（三采样脚本 monitor / ws / futu_ws 统一走它，两种来源都认、行为一致）；② futu_ws_segment.py 切到 `parse_mode()` + `parse_targets` 先剥 `--mode` 参数再解析标的（对齐 ws_segment 做法，否则 `--mode` token 被当标的解析报错）。
- **验证**：`parse_mode` 七场景实测（无来源默认 signal / `--mode auto` / `--mode=auto` / `MODE=auto` env / 参数优先于 env / env+参数组合 / env 非法值回退 signal）全过；`parse_targets` 剥参四形态实测（纯 targets / 尾带 `--mode auto` / `--mode=auto` / 裸标的多参数）全过；两脚本 py_compile 过。
- **⚠️ 回归风险提醒**：① **futu_ws_segment 的 mode 判定从「只认 env」变「参数优先、env 兜底」**——既有 `MODE=auto python3 …` 前缀形态行为不变（env 分支保留），新增 `--mode auto` 形态可用，无行为回归路径；② **ws_segment 的 mode 判定实际行为变化**——`MODE=auto` 前缀在旧版 ws_segment（2026-08-16 前）有效、08-16 切 parse_mode 后静默失效、本次恢复——若有会话在 08-16~08-21 间用 `MODE=` 前缀跑过 ws_segment 且落了 `_signal` log，那些 log 文件名的 mode 档位是错的（复盘读 log 时留意，重命名即可修正）；③ monitor_segment 一直走 parse_mode、本次只是 env 分支新增——`MODE=` 前缀从无效变有效，无既有行为被改变。

### 新增（实盘净值防泄漏：禁写被跟踪文件 + equity_guard hook 工具强制 + 存量清理，2026-08-20 18:40）

- **为什么改**：2026-08-20 用户立——本仓库开源，实盘账户的资产净值（net_liquidation / 总资产）与购买力禁止写入任何被 git 跟踪的文件。此前 2026-08-17~20 实盘盯盘期间，实测净值数字多次被当作「验证证据」写进 CHANGELOG、SKILL.md、accounts.md、脚本注释、actions / signals 记录（账户号有打码规矩、净值却裸写），并已随提交（5a84d77 等）进入 git 历史推上 GitHub——账户号能定位到人，净值直接暴露个人财务状况，性质同等敏感。
- **改了什么**：
  - **规定落地**：项目 [CLAUDE.md](.claude/CLAUDE.md) 新增「实盘净值禁止写入被跟踪文件」专节（怎么写：量级口径或只写派生指标 B；精确值只写被 gitignore 的本机文件）；[SKILL.md](.claude/skills/trade/SKILL.md) 账户选择段、[auto-mode.md](.claude/skills/trade/references/auto-mode.md) 记录规范、[signal-mode.md](.claude/skills/trade/references/signal-mode.md) 权益更新、[signals/README.md](signals/README.md) 记录字段同步改口径——signal 模式 equity-log 累加链自 2026-08-10 起与实盘对齐，故 signals 正文同样禁写净值本体（改写 B + 「见本机 equity-log」）。
  - **工具强制（依「文档规定必须尽可能配工具强制」）**：① 新建 [.claude/hooks/equity_guard.py](.claude/hooks/equity_guard.py)（PreToolUse Write/Edit/NotebookEdit）——写入内容命中基准黑名单特征串且目标文件未被 gitignore 时 exit 2 阻断；② 基准黑名单 [.claude/hooks/equity-baseline.local](.claude/hooks/equity-baseline.local)（本机、已 gitignore）+ **自动维护**——[trade_utils_tiger.py](.claude/skills/trade/scripts/trade_utils_tiger.py) 新增 `note_equity_baseline()`，`load_equity_tiger(account='live')` 查到实盘净值即自动把多种写法（带/不带逗号、整数）追加进黑名单，保证「刚查到的最新值」必然拦得住；③ **在场打印**——preflight / resume 以 `--account live` 取净值时输出防泄漏提醒（实盘盯盘每段可见）。
  - **存量清理（打码口径替换）**：CHANGELOG 四处（08-20 账户配套条目、08-17 WS 代理条目、08-17 密钥轮换条目、08-17 权益重设条目标题与正文）、SKILL.md 入金状态、accounts.md WS 实测段、trade_utils_tiger.py / preflight.py / resume.py 注释、actions/2026-08-18（3 处）、actions/2026-08-19（1 处）、signals/2026-08-12（3 处）、signals/2026-08-13（3 处）——实盘净值改「量级口径」或「见本机 equity-log」，模拟账户值（7,886,546 / 7,919,412 / 8,003,878 等，假钱）保留不动。
  - **equity-log.csv 移出跟踪**：`.gitignore` 加 `signals/equity-log.csv`（权益链精确净值只留本机）与 `.claude/hooks/equity-baseline.local`。
- **验证**：equity_guard hook 三路径自测（命中 → exit 2 + 提示；普通内容 / gitignore 目标文件 → exit 0）+ 函数级单测（基准加载 / 命中 / 未命中模拟值 / gitignore 判定全过）；`note_equity_baseline` 幂等追加实测（自测假值已从黑名单清除）；三脚本 py_compile 过；全仓正则扫描（实盘净值全部已知写法）确认为空。
- **⚠️ 遗留（需用户决定）**：① **git 历史存量无法靠改文件清除**——5a84d77 等历史提交里的净值数字仍在 GitHub（改动历史须 filter-repo 重写 + force push，影响所有 clone，须用户亲自裁定是否做）；② `signals/equity-log.csv` 仍被 git 跟踪（.gitignore 对已跟踪文件不生效），**需用户执行 `git rm --cached signals/equity-log.csv` 后再 commit 才真正移出**（AI 依「暂存区禁止增删改」规矩不代执行）；③ hook 已知局限——黑名单是特征串匹配，拆算式或等值改写可绕过，属兜底而非银弹，第一道防线仍是写之前的自觉。

### 修复（实盘盯盘账户配套：preflight / resume 支持 --account live 取实盘净值，2026-08-20 13:00）

- **为什么改**：2026-08-20 13:00 用户下达「盯盘，自动模式，实盘」后，preflight 实测输出 equity 7,886,546.38 HKD（**默认模拟账户**净值）——而用户已确认切实盘账户（67****91，实盘净值的量级与精确值一律见本机 equity-log / 实查，不写入被跟踪文件）。实盘盯盘时若继续用模拟账户净值算 B（2%×equity）、f_max、市值杠杆上限，单笔风险预算偏差 **68 倍**：模拟净值算出的 B（约 15.8 万）是实盘 B（精确值见本机实查）的 67.9 倍——按错位 B 开仓等于实盘账户一次性烧掉总净值 1.36 倍，属会直接下错单的红色级事故（SKILL「文档规定必须尽可能配工具强制」：净值取数不能靠 AI 记得传参，必须脚本层支持）。
- **改了什么**：① [trade_utils_tiger.py](.claude/skills/trade/scripts/trade_utils_tiger.py) `load_equity()` / `load_equity_tiger()` 新增 `account` 参数（None/paper=模拟、'live'=实盘），透传给 `load_config(account)`——取净值与下单同账户（下单脚本 open/close/move_stop 早已支持 `--account live`，净值取数本次对齐）。② [preflight.py](.claude/skills/trade/scripts/preflight.py) 支持 `--account live|paper`，传给 `load_equity`；equity 来源行打印账户标记（实盘 / 默认（模拟））。③ [resume.py](.claude/skills/trade/scripts/resume.py) 同上；另修其可选标的刷新参数解析——原逻辑只过滤 `--mode` 前缀，`--mode auto` 的 `auto`、`--account live` 的 `live` 会被误当标的（实测报 `format of code auto is wrong`），改为正确跳过带取值的选项及其取值。
- **验证**：`preflight.py --mode auto --account live` 实测输出「equity 来源：老虎账户 get_prime_assets(base_currency=HKD) 证券段净值（实盘）」+ 实盘净值与 B 的精确值不写入被跟踪文件（与实盘 get_assets net_liquidation USD 数按汇率折算一致）；`resume.py --mode auto --account live` 无标的时不报错、正确跳过参数。
- **⚠️ 回归风险提醒**：⚠️ **实盘与模拟的净值口径差异**——本修复后 preflight/resume 在实盘授权下取实盘净值，B 缩小 68 倍，开仓仓位相应缩小（单笔 max_loss 变小），这是正确行为；但若用户后续改回模拟盘（`--account paper` 或省略），净值又回到模拟口径，B 恢复大值——模拟盘开大仓是正常的（模拟资金）。真正风险点是「**实盘授权消失后仍传 --account live**」——权限控制仍靠 SKILL 双闸（用户明确说切实盘 + AskUserQuestion 确认），脚本层不额外拦（下单脚本同口径）。另：resume 的标的刷新参数解析改动只影响位置参数提取，`resume.py HK.00700` 旧用法行为不变（回归风险低）。

### 变更（watcher 恢复盯盘开始默认开启：preflight 自动 load，2026-08-20 11:40）

- **为什么改**：2026-08-20 11:24 watcher 删掉「中断警报 + 掩蔽提示」后只剩「空转警报 + 老虎白名单漂移检测」两个低频警告（自带 5 分钟冷却 + 仅港股盘中触发，不扰民），用户立：守卫恢复**盯盘开始默认开启**——低频警告常开的价值（空转兜底 + 白名单盘中自动处置）大于打扰成本（基本为零）。同日 09:58「默认不开启」待办的决定被本条取代（其针对的扰民源已删）。
- **改了什么**：① [preflight.py](.claude/skills/trade/scripts/preflight.py) 新增 `_ensure_watcher()`——盯盘启动时检查 launchd 项 `com.daytrading.monitor-watcher`：已 load 跳过（打印在跑）；未部署先从项目 `.claude/hooks/` 复制 plist 到 `~/Library/LaunchAgents/` 再 `launchctl load`（幂等，失败打印手动命令）。**停盯不联动 unload**（watcher 常驻无害、盘外 in_trading_session 自动静默）。② [com.daytrading.monitor-watcher.plist](.claude/hooks/com.daytrading.monitor-watcher.plist) 头注释重写——删旧的中断判定描述（已不存在），改为两个低频检查 + 自动部署说明 + 「unload 后下次 preflight 会自动重新 load、彻底关闭须改 preflight 或删 plist」的提醒；本机 `~/Library/LaunchAgents/` 副本已同步。③ [monitoring.md](.claude/skills/trade/references/monitoring.md) 多层防护段的 watcher 沿革补开关口径；[TODO-archive.md](TODO-archive.md)「默认不开启」归档条补 ② 段裁决（被「默认开启」取代）。
- **验证**：盘中实跑 preflight 两次——第一次 watcher 从 unload 状态被自动 load（launchctl list 出现该项、打印「已开启」），第二次打印「已在跑」跳过（幂等）；plutil -lint 通过；两处 plist diff 一致。
- **⚠️ 回归风险提醒**：⚠️ **「关 watcher」从一次性动作变成会被 preflight 翻转的状态**——用户手动 unload 后，下次任何会话跑 preflight（盯盘启动）都会自动重新 load；要彻底关闭须改 preflight 的 `_ensure_watcher()` 或删 plist（plist 头注释已写明）。这与 2026-08-04「随盯盘开关」的差异：旧版停盯联动 unload，现版停盯不卸载（只剩低频警告、常驻无害）；2026-08-20 09:58「默认不开启」决定的扰动源（中断 / 掩蔽连弹）已随功能删除消失，默认开启不再有扰民回归。

### 变更（watcher 删除中断警报与掩蔽提示，收缩为空转警报 + 白名单检测，2026-08-20 11:24）

- **为什么改**：用户 10:35 下令关掉 watcher（launchctl unload），随后下令删除「中断警报 + 掩蔽提示」两个功能。2026-08-20 早盘实录（tmp/watcher_notify.log 13 条）：这两类提醒在多会话场景下大量误报扰民——「活着但 idle」的已停盯残留会话反复触发掩蔽提示（09:47-10:28 共 5 条）、waiting_input / 陈旧 running 误判触发中断警报（6 条，含同一状况先报掩蔽再报中断的成对重复），连弹打断盯盘。其守护价值已由更强机制覆盖：采样链断流由会话内 monitor_guard.py hook（STALE 分层自检）+ task-notification 断流 + 用户监督兜住。
- **改了什么**：① [monitor_watcher.py](.claude/hooks/monitor_watcher.py) **整体收缩重写**——删除 interrupted_sessions / session_state / prune_dead_sessions / jsonl_age 及 STATE_DIR / TRANSCRIPT_DIR / REG_FILE / RUNNING_SILENCE_SECONDS / STALE_SECONDS / DEAD_SESSION_SECONDS 等中断 / 掩蔽判定全套（含死会话自动剔除），watcher 不再读会话注册表、不再逐会话判定；**保留空转警报**（采样在跑但分析心跳停更 >3 分钟 → 弹窗）**与老虎白名单漂移检测**（proxy_guard.py 盘中兜底），通知冷却、强制弹窗、持久日志机制不动。② [monitoring.md](.claude/skills/trade/references/monitoring.md) 多层防护段的 watcher 沿革补 2026-08-20 收缩记录、心跳「为什么」段与标的池「死会话口径」段、美股差异段同步措辞。③ [SKILL.md](.claude/skills/trade/SKILL.md) 停盯段改写——注销的必要性从「watcher 会误报中断」改为「caffeinate 引用计数误判不关灯」。④ [TODO.md](TODO.md)「守卫默认不开启」条目按「被功能删除取代」归档（watcher 当前为卸载状态，剩余两类提醒自带冷却不扰民，默认开启的前提已变）；「不弹横幅」条目收窄表述（适用对象改为剩余两类提醒）。⑤ [accounts.md](.claude/skills/trade/accounts.md) 白名单运维口径注明 watcher 收缩后该检测仍保留。
- **验证**：重写后 ast.parse 语法通过；盘中实跑 `python3 .claude/hooks/monitor_watcher.py` 退出码 0、无异常输出（当前 11:2x 港股盘中、白名单检测 + 空转检查路径正常走完）；残留引用 grep 确认仅剩保留功能自身的 ANALYSIS_BEAT_STALE_SECONDS。
- **⚠️ 回归风险提醒**：① **删掉的是外部监督层**——采样链断流的外部弹窗提醒没了（watcher 不再报中断），断链发现收窄为「会话内 monitor_guard hook + task-notification 断流 + 用户自觉」；有持仓时若盯盘会话卡死且 hook 也不在场（如用户手动跑了采样脚本），不再有系统级叫醒——用户已知悉并裁定（同日 10:35 主动关 watcher 的延续）。② 监控盲区：空转警报的「采样在跑」判据是全局 pgrep（任一采样进程在跑就算），单会话空转但另一会话在采样时仍会漏报——此盲区为收缩前已存在（非本次引入）。③ monitor_sessions.txt 注册表保留、preflight 注册 / off.sh 引用计数 / unregister 联动全部不动——watcher 删除的功能不再消费注册表，但 caffeinate 关灯逻辑依赖它，停盯注销流程照旧必须执行。

### 变更（待办更新：密采样守卫改为默认不开启、用户明确要求才开启，2026-08-20 09:58）

- **为什么改**：用户立——密采样守卫（外部 watcher，launchd `com.daytrading.monitor-watcher`）改成用户明确要求才开启、默认不开启。现行为是 AI 启动盯盘时自主 load（2026-08-04「随盯盘开关」决定），用户没要求也会自动开启，叠加守卫误报（同日 09:49 条待办的场景：今早连弹 5 条打断盯盘）持续扰民。
- **改了什么**：[TODO.md](TODO.md) 橙色紧急度原「密采样启动之前不要开启密采样守卫」条目（09:47 记录）**改写升级**为「密采样守卫默认不开启、用户明确要求才开启」——覆盖面更宽（不止启动前静默，而是全程默认关闭；09:47 旧条目被本条合并、不再单列）；含执行清单（改 monitoring.md / SKILL.md / monitor_unregister.sh 联动、unload 当前已加载的 launchd 项）。本待办取代 2026-08-04「watcher 随盯盘开关（AI 自主 load / unload）」决定。
- **验证**：旧表述条目已不在 TODO.md / TODO-archive.md 残留，新条目在位、带时间戳 09:58。
- **⚠️ 回归风险提醒**：无——纯待办记录，代码未改。执行时需回溯 2026-08-04「watcher 随盯盘开关」CHANGELOG 条目（该决定被本待办取代，不是删除防护、是改开关权归属——从 AI 自主改为用户明确要求）。（**后续**：2026-08-20 11:24 本待办已随 watcher 功能收缩归档终结，见当日 11:24 CHANGELOG 条。）

### 变更（新增待办：密采样守卫提醒不弹横幅，2026-08-20 09:49；09:53 修订为只去横幅不去弹窗）

- **为什么改**：用户立——密采样守卫的提醒不要弹出横幅。[monitor_watcher.py](.claude/hooks/monitor_watcher.py) 的 notify() 主提醒是屏幕中心强制弹窗（2026-08-19「横幅 → 弹窗」改造），但铃声经 display notification 发出、该调用本身也弹一条空横幅；2026-08-20 早盘 09:30-09:47 密采样未启动误报场景连弹 5 条（tmp/watcher_notify.log 实录），打断盯盘。用户随后修订：**只去掉横幅、不要去掉弹窗**——弹窗（2026-08-19 立）保留。
- **改了什么**：[TODO.md](TODO.md) 橙色紧急度新增待办——铃声改用不弹横幅的方式发出（如 afplay 直接播放 Basso），弹窗与持久日志照旧；初版「去掉弹窗与横幅」表述已按用户修订改正（时间戳 09:53）。
- **验证**：条目已写入 TODO.md 橙色紧急度节、带修订后时间戳。
- **⚠️ 回归风险提醒**：无——纯待办记录，代码未改；2026-08-19「横幅 → 弹窗」决定继续有效（不取代）。（**后续**：2026-08-20 11:24 条目收窄为对剩余两类提醒（空转 / 白名单）适用，09:30-09:47 连弹场景属已删除的中断 / 掩蔽误报，见当日 11:24 CHANGELOG 条。）

### 修复（美股开仓脚本 build_fee_ctx 缺失即崩，2026-08-20 04:20）

- **为什么改**：当日美股盘中合法开仓（US.MU 做多、自查全过）调 `open_position_tiger_us.py` 直接 AttributeError 崩溃——脚本 184 行调 `U.build_fee_ctx`，但 `trade_utils_tiger_us.py` 在 2026-08-17 费率改造时只删了旧别名没补新别名（注释写「复用 trade_utils_tiger 的 build_fee_ctx」却没真接上），美股开仓链路从此一调即崩。
- **改了什么**：[trade_utils_tiger_us.py](.claude/skills/trade/scripts/trade_utils_tiger_us.py) 补 `build_fee_ctx = T.build_fee_ctx` 别名转发（纯属性打包、与市场无关，美股按股费率在 `_net_odds` 内按 market='US' 分支处理）。
- **验证**：修复后实跑开仓脚本，价格范围检查输出正常（range_low/range_high/odds_at_ref 完整），不再崩溃。
- **⚠️ 回归风险提醒**：无——纯补转发，港股路径不受影响。

### 修复（开仓互斥闸误拦：订单流加今日过滤；美股盘前快照 pre_price 取价，2026-08-20 04:20）

- **为什么改**：① 美股盘中一次合法开仓（US.MU 做多、自查全过）被互斥闸拒——`get_today_orders_us` 实际返回老虎 `get_orders()` 近两周全量订单，`today_open_exposure` 把 2026-08-05 港股 00981 的历史开平链残留计数（净 +1）与 07709 的 8-03 历史买单误判为「今日在场敞口」（`blocked_by=open_exposure_today`）；函数名与注释都写「当日订单流」、实际算的是两周，口径错位。② 美股盘前 `monitor_segment` 采样用 `last_price` 恒停昨收不动（实测 MU 盘前 last_price 恒 940.76、盘前真实价在 `pre_price`），VWAP 检查同病——盘前段输出完全失真，AI 只能靠 bid/ask 侧推。
- **改了什么**：① [trade_mutex.py](.claude/skills/trade/scripts/trade_mutex.py) 新增 `_order_local_date` / `_today_orders`（order_time 毫秒时间戳→本地日期过滤），`TradeMutex.blocked` 里口径②③计算前统一 `_today_orders(orders)` 过滤——历史残留链不再误拦，当日敞口语义与函数名一致；② [monitor_segment.py](.claude/skills/trade/scripts/monitor_segment.py) 采样与 VWAP 检查改为「`pre_price` 非空用盘前字段（pre_price/pre_high_price/pre_low_price），否则用常规字段」，并注明盘前 VWAP（avg_price）为昨日盘中口径、代表性弱。
- **验证**：① 修复后实跑 `get_today_orders_us`（100 单全量）→ `_today_orders` 过滤得 0 单、敞口 set() 为空（修复前误报 {'00981','07709'}）；本地日期过滤对港股当日 / 美股跨夜订单均按实际下单日归组正确；② 盘前实测段输出 last 948.89→952.9 随盘前价实时动、VWAP 检查现价同步 pre_price；盘中（pre_* 为 nan）回退常规字段不受影响。
- **⚠️ 回归风险提醒**：① 今日过滤只收窄闸门输入——当日开仓成交仍照常拦截（口径不变），但**跨日持仓过夜场景**（违规或隔夜单）不再被口径②拦（昨日开仓不在今日订单流）——本就由持仓不过夜纪律 + `monitor_segment` 第四层白名单外双持仓兜底覆盖，风险可控；② 盘前取价在 pre_price 与昨收相同且 pre_volume=0 时回退 last_price（避免盘前无成交时段误用旧值），正常盘前（有成交）pre_price 持续更新、无影响。

### 变更（多会话标的池自动划分：认领制替代手动划分，2026-08-19 23:14）

- **为什么改**：多会话并行盯盘（方案 A）的标的池此前由用户手动给每个会话划分——用户不想再做这一步（TODO 2026-08-18）；且「不跨池抢标的」原是散文约定（查别会话 log 文件名），靠 AI 记忆会衰减，按「文档规定必须尽可能配工具强制」应换工具强制。
- **改了什么**：① 新增 [pool_claim.py](.claude/skills/trade/scripts/pool_claim.py)——**认领制**自动划分（会话启动时从候选池认领未占用标的；flock 锁内「检查占用→登记」原子互斥；死会话 jsonl 停更 >30 分钟自动释放、认领表当日有效跨日重置；claim / status / release 三命令）；② [monitor_guard.py](.claude/hooks/monitor_guard.py) 加跨池抢标的 PreToolUse 拦截（采样 targets 含其它会话已认领标的 → 阻断；无认领表 / 无会话 id 时跳过不误伤单会话）；③ [monitor_unregister.sh](.claude/skills/trade/scripts/monitor_unregister.sh) 停盯注销时联动 `release --all`；④ [preflight.py](.claude/skills/trade/scripts/preflight.py) 打印当前全局池划分；⑤ [monitoring.md](.claude/skills/trade/references/monitoring.md)「多会话并行盯盘」节改写为自动认领机制、[SKILL.md](.claude/skills/trade/SKILL.md) 启动序列与停盯段同步。
- **验证**：多会话认领冲突互斥、死会话自动释放、增量合并、单标的/全部释放、unregister 联动、hook 三场景（冲突阻断 / 无冲突放行 / 自己认领放行）、完整 preflight 全链路，均实测通过。
- **⚠️ 回归风险提醒**：无——新增能力不改旧路径；`pool_claims.json` 不存在时 hook 检查完全跳过，单会话 / 未启用划分场景行为与从前一致。

### 变更（auto 开仓成交后响铃提醒，2026-08-19 23:14）

- **为什么改**：auto 模式 AI 直接调脚本开仓，用户若不盯屏幕感知不到已开仓（TODO 2026-08-19 11:22）——成交后响一声把注意力拉回来；按「文档规定必须尽可能配工具强制」做进开仓脚本本身、不靠 AI 记得调 ring.sh。
- **改了什么**：① [ring.sh](.claude/skills/trade/scripts/ring.sh) 加 `RING_LOG` 环境变量——auto 响铃写 `signals/ring-log-auto.csv`，与 signal 的 `ring-log.csv` 隔离（后者末行被 `log_action.sh`（signal 拍板时刻）与 `resume.py`（signal 断层检测）消费，auto 写入会污染 signal 会话数据）；② [trade_utils_tiger.py](.claude/skills/trade/scripts/trade_utils_tiger.py) 新增 `ring_after_fill()`（异步调 ring.sh，静默失败不影响交易链路）；③ [open_position_tiger.py](.claude/skills/trade/scripts/open_position_tiger.py) / [open_position_tiger_us.py](.claude/skills/trade/scripts/open_position_tiger_us.py) 在全额成交与部分成交两条路径的成交确认后内置响铃；④ [auto-mode.md](.claude/skills/trade/references/auto-mode.md) 新增「开仓成交响铃」节（AI 无需再手动调 ring.sh）。
- **验证**：ring.sh 默认路径行为不变（仍写 ring-log.csv）、RING_LOG 隔离写入、`ring_after_fill` 实跑异步写 `ring-log-auto.csv`、三脚本 py_compile 通过；真机成交音效待下次 auto 盘中开仓自然确认。
- **⚠️ 回归风险提醒**：低——`ring-log.csv` 消费方（`log_action.sh` signal 分支 / `resume.py` signal 分支）均不受影响（auto 走新文件）；signal 模式响铃路径零改动。

### 变更（备忘归档改专属文件 MEMO-archive.md，2026-08-19 22:15）

- **为什么改**：用户立——MEMO.md 的归档不用 TODO-archive.md，用专属文件 `MEMO-archive.md`（待办归档与备忘归档各自成体系、互不混放）。
- **改了什么**：① 新建项目根 [MEMO-archive.md](MEMO-archive.md)（头部说明平行分工 + 暂无条目）；② [MEMO.md](MEMO.md) 头部「条目格式」改写——完成条目移入 MEMO-archive（原写 TODO-archive）；③ 全局 `~/.claude/CLAUDE.md`「备忘录 MEMO.md」小节「完成与归档」同步改写（含三种状态标记），已镜像同步到 CapabilityManagerAgent `claude/CLAUDE.md`（diff 一致，同步记 Prometheus CHANGELOG）。
- **验证**：三文件（MEMO / MEMO-archive / 全局规则）表述一致；全局与镜像 diff 逐字节一致。
- **⚠️ 回归风险提醒**：无——纯文件分工调整；当前 MEMO 体系尚无已完成条目、无迁移负担。

### 变更（长桥可做空扫描重跑待办放弃归档，2026-08-19 22:05）

- **为什么改**：用户裁定该对比不再需要、重跑不执行（放弃处理）——按 2026-08-19 用户立的口径「说放弃 = 不需要放在待办里，移除的待办都归档」，从 TODO.md 移除并归档。
- **改了什么**：[TODO.md](TODO.md) 绿色紧急度节删除该条（节注记录归档去向）；[TODO-archive.md](TODO-archive.md) 新增「🟢 绿色紧急度·长桥可做空标的扫描」归档节（✅已放弃 + 放弃理由 + 前置结论仍有效说明 + tmp/ 方法留档可复用但属临时产物的提示）。
- **验证**：TODO.md 绿色紧急度仅剩 2 条（auto 开仓响铃、标的池自动划分）；归档条目完整保留方法要点。
- **⚠️ 回归风险提醒**：无——纯待办状态管理；方法脚本在 `tmp/`（git 不跟踪），若日后真需要长桥可空清单须按归档条目里的坑重新验证。

### 变更（新建备忘录 MEMO.md：「长期备忘」撤出 TODO 体系，2026-08-19 22:00）

- **为什么改**：用户立 TODO.md 功能定位——待办清单里的内容都是**要尽可能尽快处理**的、**要及时清空**，不应有「长期备忘」这种无完成时点的分类挂着不动；比绿色紧急度还低的超低频需求（周期性 / 条件触发、无排期压力）应专门放一个备忘录文档。
- **改了什么**：① 新建项目根 [MEMO.md](MEMO.md)（备忘录：定位说明 + 备忘条目区；条目格式与 TODO 一致带时间戳，做完移 TODO-archive 归档或就地更新状态；触发时机 = 相关场景出现时人工 / AI 对照，不设自动提醒）；② 密钥轮换（3~6 个月一次，补「下次不早于 2026-11-17」锚点）、三阶段赔率标定（样本 ≥30 笔后重标定）两条从 TODO.md「长期备忘」节迁入 MEMO.md；③ [TODO.md](TODO.md) 头部说明补功能定位（只放要尽快处理的内容、不设长期备忘分类、备忘进 MEMO.md），「长期备忘」节删除、黄色紧急度节注记录迁移去向。
- **验证**：TODO.md 无「长期备忘」节、两条备忘在 MEMO.md 内容完整（时间戳 / 历史计数原样保留）；三个文件（TODO / MEMO / TODO-archive）分工无重叠。
- **⚠️ 回归风险提醒**：无代码行为变更；唯一注意点 = 三阶段标定的「样本够了要重标定」原本挂在 TODO 里、复盘场景下 AI 会翻 TODO 看到——现迁 MEMO，**复盘时若样本已 ≥30 笔需翻 MEMO.md 对照**（MEMO 头部已写触发时机，且 review.py 产样本数时可在复盘流程中带一句 MEMO 对照）。

### 变更（白名单长期备忘归档：守护自动化后无需备忘，2026-08-19 21:52）

- **为什么改**：IP 漂移问题已由 proxy_guard 全自动解决（漂移检测 + 自动切换 + 弹窗按钮确认制），用户裁定不再需要在 TODO 里保留长期备忘——漂移发生时弹窗自会提醒，备忘条目已无存在必要。
- **改了什么**：[TODO.md](TODO.md)「长期备忘」节删除该条（黄色紧急度节注同步更新），[TODO-archive.md](TODO-archive.md) 新增归档节（✅已完成，含 2026-08-19 漂移实录 + 守护落地摘要 + 归档理由）；橙色紧急度节注里「见长期备忘」的指向改为「已守护自动化解决」。
- **验证**：TODO.md 与 TODO-archive.md 无重复条目、节注指向一致。
- **⚠️ 回归风险提醒**：无——纯待办状态管理，不改任何代码行为；运维口径仍在 accounts.md「老虎 IP 白名单 + 代理节点」节（弹窗触达时不依赖 TODO）。

### 变更（白名单提醒改按钮确认制：点「已添加」自动同步本地 config，2026-08-19 晚·用户立）

- **为什么改**：此前流程里「老虎页加白后还要手改 config.json 的 tiger_whitelist 镜像」是剩余人工动作——手改 JSON 易格式错、易忘。用户立新口径：弹窗按钮改「已添加」，先去老虎开放平台加白、回窗点「已添加」确认，代码收到点击信号后自动把该批 IP 合并进本地 config——人只做「老虎页粘贴 + 点一下按钮」两步。
- **改了什么**：[proxy_guard.py](.claude/skills/trade/scripts/proxy_guard.py) 重构提醒链路——`popup()`（单按钮「知道了」、无信号回收）升级为 `remind()`（写 pending 到 tmp/proxy_guard_pending.json + 派生 `--await-confirm` 独立子进程，start_new_session 脱离守护进程，守护退出 / 下轮巡检不影响等待）；子进程跑 `display dialog` 双按钮「取消 / 已添加」（默认按钮 = 已添加、cancel button = 取消，**仍无自动关闭、必须点击**），点「已添加」→ `merge_whitelist()` 把 pending 里的 IP 去重保序合并写回 config.json + log_event 留痕；点「取消」/ Esc → 不合并、删 pending（5 分钟冷却后条件仍成立再提醒）。新增 `merge_whitelist()`（读改写保缩进 2 与原格式一致）。[config.json](.claude/skills/trade/config.json) / [config.example.json](.claude/skills/trade/config.example.json) 注释同步新口径（日常全自动、仅删减 IP 时手改）。`--test-alert` 复用同链路（ips 空 → 点已添加不改 config，可安全测信号链）。
- **验证**：端到端两轮——① 模拟漂移弹窗（测试 IP <测试 IP>）→ 用户点击「已添加」→ config.json 自动合并 ✅（log 留痕「已添加确认：合并 <测试 IP>」）→ 测试 IP 手动还原；② **真实闭环（21:39）**：真实「建议加白」提醒（<漂移节点 IP> 等 3 漂移 IP）→ 用户去老虎页加白 → 点「已添加」→ 3 IP 自动合并进 config（现 9 IP）、guard 复查「节点 IP 全景与白名单一致 ✅」。preflight 集成输出正常（去重后单次调用）。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① 上一条「弹窗必须点击才消失」的修复完整保留（双按钮仍无自动关闭、Popen/独立子进程异步不阻塞主流程、dialog_pending 防叠加闸不删）；② 点「已添加」即视为老虎页已加白，代码不二次验证（老虎无 API 可查白名单，弹窗文案明确「先加白再点」）——误点会让本地白名单超前老虎页、guard 可能把漂移节点当已加白（连通验证测节点出网、测不出老虎层拒绝），此边界已写进 proxy_guard 头注释「误点边界」；③ merge_whitelist 写 config.json 失败时打 ❌ 提示手动补 + log 留痕（不静默丢）；④ pending 文件残留（子进程崩溃）会被下次 remind 覆盖写、await-confirm 读不到时提示「内容丢失、不合并 IP」，不误合并。
- **TODO 联动**：长期备忘「代理节点 IP 变动后同步老虎白名单」的剩余人工动作从两步（老虎页加白 + 手改 config）减为一步（老虎页加白后点「已添加」）。

### 修复（白名单弹窗改为必须点击才消失，2026-08-19 晚·用户反馈）

- **为什么改**：proxy_guard 初版弹窗带 `giving up after 30`（30 秒无操作自动关）——用户反馈「还没点知道了窗口就消失了」= 漏看加白 IP 串，弹窗形同虚设。
- **改了什么**：[proxy_guard.py](.claude/skills/trade/scripts/proxy_guard.py) `popup()` 去掉 `giving up after 30`，**必须用户点「知道了」才关闭**；配套把 `subprocess.run`（阻塞等点击）改为 `subprocess.Popen`（异步）——弹窗不关也不阻塞主流程（自动切换 / 连通验证 / 退出照常），并新增 `dialog_pending()`（pgrep 探测在屏未确认弹窗、有则跳过新弹防叠加，冷却 5 分钟后条件仍成立会再弹、信息不丢）。
- **验证**：端到端实跑——popup 后 guard 主流程立即退出，弹窗 3 秒 / 33 秒 / 93 秒+ 三时点均在屏（无限期等待点击确认；期间排查掉一个测试假象：`ps | grep 中文文案` 在本终端 locale 下假阴性，改用 `pgrep -f "osascript -e display dialog"` 判存活）；Popen 子进程（osascript）在父进程退出后存活实测确认。
- **⚠️ 回归风险提醒**：① 弹窗永不自动关 + launchd 每 10 秒触发——防叠加闸（dialog_pending）+ 5 分钟冷却双保险，不会窗口风暴；② osascript 进程会驻留到点击（用户不点 = 一直挂着，属预期——信息必须触达）；③ monitor_watcher 的 notify 弹窗仍带 `giving up after 30` 不动（那是盯盘中断提醒、另一条产品决策，本次只改 proxy_guard）。

### 新增（老虎 IP 白名单守护 proxy_guard.py：漂移检测 + 自动切节点 + 响铃弹窗，2026-08-19 晚）

- **为什么改**：2026-08-19 实录——代理订阅刷新后 <三节点> 三节点 IP 全变、当前节点出口漂出老虎 IP 白名单，`get_filled_orders` 被 `access forbidden: request ip is not in ip whitelist` 拒（apply_scope=all 下模拟 + 实盘全断）；当日靠人工 `xpilot switch` 切回白名单内节点恢复。老虎白名单无 API 可改（只能开发者信息页手改），但「漂移后自动恢复 + 提醒补白」可全自动；用户拍板需求口径 = 遇 IP 不可用自动切可用 IP、检测到节点 IP 变化响铃 + 屏幕中心弹窗（非横幅）提醒、弹窗给出待加白 IP 串（";" 分隔）。
- **改了什么**：
  - 新增 [proxy_guard.py](.claude/skills/trade/scripts/proxy_guard.py)：读 `~/.config/xpilot/nodes.json`（节点真实入口 IP）与 config.json 新增的 `proxy.tiger_whitelist`（白名单本地镜像）比对。三分支：① 当前节点漂出白名单 → 响铃 + 弹窗（给加白 IP 串）→ 自动 `xpilot switch` 切白名单内 vless 优先节点 + `xpilot test --current` 连通验证，候选全败或全漂出时报「全漂出警报」退出码 2；② 当前节点正常但其它节点漂移 → 低频提醒加白（不切不动）；③ 全一致 → 一行 ✅。通知 5 分钟冷却（tmp stamp）+ 事件追加 log（tmp/proxy_guard.log）；弹窗用 `display dialog` 屏幕中心模态窗（giving up after 30，非横幅）+ Basso 铃声，与 monitor_watcher 同款样式。`--dry-run`（不切不弹）/ `--test-alert`（测试弹窗）两调试口。
  - [config.json](.claude/skills/trade/config.json) proxy 节新增 `tiger_whitelist`（当前老虎页配置的 6 IP 镜像）——改白名单后同步改这里。
  - [preflight.py](.claude/skills/trade/scripts/preflight.py) 挂接（工具强制①：盯盘启动必过闸）——漂出时 preflight 时点即自动恢复，退出码 2/3 时显式告警行。
  - [monitor_watcher.py](.claude/hooks/monitor_watcher.py) 挂接（工具强制②：launchd 港股盘中每 10 秒兜底）——`main()` 开头调 `check_proxy_whitelist()`，盘中途订阅刷新 / 漂移 AI 不在场也自动处置；检测独立于盯盘会话状态（网络层问题与采样状态正交），失败静默不伤主守护。
- **验证**：py_compile 全过；真实环境三分支实跑——正常态（当前节点在白名单 + 3 漂移节点低频提醒，实弹窗响铃触达）；dry-run 模拟当前节点漂出（正确选中 vless 候选 <白名单内节点>、展示切换路径）；模拟全漂出（退出码 2 + 全部 IP 加白串）；`--test-alert` 真实弹窗点击「知道了」实测（屏幕中心模态、Basso 响）；preflight 集成实跑输出守护段落。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① watcher `main()` 前插 `check_proxy_whitelist()`——盘外仍直接 return（不变）、盘中多一次子进程调用（timeout 90s 内、capture 不阻塞判定），launchd 每 10 秒触发、proxy_guard 自带 5 分钟弹窗冷却不会风暴；② preflight 新增段包 try/except、守护失败不阻断 preflight 其余检查；③ `xpilot switch` 真实切换只在「当前节点漂出白名单」分支触发、正常态零副作用；④ config.json 新增字段向后兼容（缺字段时代码退出码 3 + 提示，不炸）。**遗留（无法工具强制 + 理由）**：去老虎开发者页粘贴新 IP 这步必须人工——老虎不提供白名单修改 API、后台登录需手机验证码；config.json 的 `tiger_whitelist` 同步也人工（老虎页改了才有镜像对象）。
- **TODO 联动**：长期备忘「代理节点 IP 变动后同步老虎白名单」更新——守护自动化后剩余人工动作只剩「弹窗提示时去老虎页加白 + 同步 config 镜像」。

### 批量处理（橙色紧急度七项全清，2026-08-19 午后）

七项待办一次处理完毕（详录见 `TODO-archive.md`「🟠 橙色紧急度·2026-08-19 批量处理」节），此处记要点：

- **新增（平仓脚本输出 App 实测净利 net_pnl_app——「盈亏直接从 App 取」的工具强制落地）**：为什么改 = 2026-08-19 实盘小米平仓 AI 手算净利 506 vs App 实测 473.93、用户裁定转录直接抄 App 口径（当日早间 CHANGELOG「盈亏净额口径」条的目的）。改了什么 = [trade_utils_tiger.py](.claude/skills/trade/scripts/trade_utils_tiger.py) 新增 `attach_net_pnl_app()`（三级取数：① close 成交单 `realized_pnl`【与 App 已实现盈亏同源，当日实测 473.93 分毫不差】→ ② 毛盈亏 − 双边 `commission`【实测互验 474.87】→ ③ fee_schedule 复算标 `estimate`），[close_position_tiger.py](.claude/skills/trade/scripts/close_position_tiger.py) / [close_position_tiger_us.py](.claude/skills/trade/scripts/close_position_tiger_us.py) 四个成交出口全接入。验证 = 当日实盘两笔四单真实数据三路径离线实测（474.87 / 474.87 / 30.68）+ 三条链路（review 61 笔 / bayes 同 CSV / 盯盘 `_net_odds`）回归通过。**回归风险**：低——纯附加字段、查询失败不阻断平仓输出。
- **变更（fee_schedule.py 补印花取整 + 交收最低费）**：为什么改 = 旧纯比例模型对当日实盘四笔 commission 分别差 +0.50/+4.63/+0.20/+0.30（复盘复算与 App 实测差 ~32 的主要来源）。改了什么 = 港股印花税 `math.ceil` 向上取整到元 + 交收费 `max(2.0, 比例)` 每笔最低 2 元；`HK_GOV_RATE` 剔除 CCASS 比例部分防重复计（实现中修正自身两个 bug：初版丢平台费 + CCASS 双计）。验证 = 四笔对账三笔零差、一笔 STP 平仓单残差 +4.03（券商侧实际收费、App 费用合计与 SDK commission 一致证非模型漏项、待账单核）；review.py / bayes_evolution.py / 盯盘前瞻三链路回归通过（61 笔统计量无异常漂移）。**回归风险**：复盘历史样本的费用与 R 会小幅系统性变化（每股双边费约 +0.1~0.6 HKD）——全样本同口径重算、一致性保持。
- **修复（futu_ws_segment.py 订阅缓冲污染 high/low）**：为什么改 = 富途 subscribe TICKER 瞬间推历史缓冲逐笔，当日 3 次「↓破支撑」误报（小米段低 26.0 vs 日低 26.38 等）。改了什么 = 订阅发起前记 `SUB_EPOCH`、逐笔 time 早于该边界的行丢弃（不进 high/low/last/sec）；time 解析失败保守丢弃。**注意时区**：TICKER time 为本地墙钟、用 `time.mktime` 解析（`timegm` 差 8 小时、实现中修正）。**回归风险**：低——首 1-2 秒真实逐笔可能误杀（毫秒级推送、可忽略），缓冲极值不再进段统计。
- **修复（account_status.py「实持 0 股」误报 + 跨会话告警串台 + 空 symbol 假告警，三合一）**：为什么改 = 当日持仓期间连续假告警（根因① get_positions 抛异常被吞成 []——查询失败与真空仓长得一样；根因② actions 目录全会话共享、A 会话核 B 会话的仓；根因③ 空 symbol 进持仓推导产生「['']」告警）。改了什么 = ① 查询异常不再吞：positions=None + 错误文本带回段输出（「查询失败」显式区别于「已无持仓」、附 actions 口径持仓与排查指引）；② 会话视角：`CLAUDE_CODE_SESSION_ID` + `tmp/trade_intent.log` 当日 filled intent 建「标的→开仓会话」映射，段输出只核对本会话的仓（无会话 id 退回全核对）；③ 空 symbol 不进 holding / acct_syms。验证 = A/B 会话隔离单测 + 查询失败路径 + 真实账户链路全通过。**回归风险**：低——monitor_segment 的 blast_check 调用签名不变；无会话 id 手动跑脚本行为不变。
- **变更（monitor_watcher.py 通知持久 log + 强制确认弹窗）**：为什么改 = ① stamp 文件 "w" 模式同 key 覆盖、提醒历史不可追溯（2026-08-18 实况只剩各 key 最后一条）；② 横幅通知数秒消失、有持仓时漏看 = 漏移损风险。改了什么 = ① `notify()` 追加写 `tmp/watcher_notify.log`（时间戳 + key + 全文，失败静默）；② 横幅改 `display dialog` 屏幕中心弹窗 + 「知道了」按钮 + `giving up after 30`（防阻塞 watcher 周期）+ Basso 铃声不变 + 文案转义。验证 = 冷却 / 追加 / 弹窗语法真实 osascript 实测通过。**回归风险**：低——冷却机制（5 分钟/key）不变。
- **运维发现（代理节点漂移出老虎 IP 白名单）**：实测 get_filled_orders 被 `access forbidden: request ip <漂移节点 IP> is not in ip whitelist` 拒——代理订阅刷新后 <三节点> 三节点 IP 全变（当前 <当前节点> 出口 <漂移节点 IP> 不在白名单六 IP 内）。已 `xpilot switch` 切白名单内的 <白名单内节点>（<白名单内节点 IP>）恢复。**白名单补新 IP 需用户在老虎开发者信息页操作**（<漂移节点 IP> + <漂移后新节点 IP>，以 `xpilot node list` 实查为准），对应 TODO 长期备忘「代理节点 IP 变动后同步老虎白名单」的既定流程。

### 新增（加速赶极端检测——「动能将竭主动平仓」工具强制，2026-08-19 用户立）

- **为什么改**：2026-08-19 实盘小米两笔，10:31（20 秒 +0.58% 破前高远离 VWAP）与 11:12-11:13 两次急速赶顶均为 SKILL.md「动能将竭 → 立即主动平仓 🔴」条文的命中形态，AI 却盯「回踩观察」未执行——散文规定靠记忆必衰减（同日对照：工具打印的 VWAP 检查 / 持仓状态全守住，靠记忆的全失守）。用户问「如何避免下次赶顶不作为」→ 按「文档规定必须尽可能配工具强制」规矩落地为决策时刻工具在场打印。
- **改了什么**：[`account_status.py`](.claude/skills/trade/scripts/account_status.py) 新增 `blast_check(vwap_by_sym, prices_by_sym)`——持仓标的段内三条件同时命中（①段内涨速 ≥0.4% ②现价=段新高 ③距 VWAP ≥±0.8% 超买/超卖）即返回 ⚡⚡⚡ 赶顶/赶底警报（内嵌「主动平仓 vs 让利润奔跑」决断提示 + 「本警报必须显式回应、不得静默跳过」约束）；空仓静默（与「空仓不查」同规则）。挂载：[`futu_ws_segment.py`](.claude/skills/trade/scripts/futu_ws_segment.py)（港股主源，VWAP 快照复用传参）与 [`monitor_segment.py`](.claude/skills/trade/scripts/monitor_segment.py)（美股 10 秒采样，价格序列从当日 log 全量取）段结束输出、紧随 VWAP 检查之后。
- **验证**：py_compile 三脚本通过；五场景回放（做多赶顶触发✓ / 空仓静默✓ / 做空赶底触发✓ / 缓涨静默✓ / seg 风格 dict 传参触发✓）；用 2026-08-19 10:31 实测数据回放正确触发（段内 +0.83% / 段新高 / 距 VWAP +1.86%）；阈值标定自当日三次赶顶实测（涨速 0.58%/20s → 0.4%/40s 保守化；VWAP 距离实测 +0.5~+1.9% → 取 0.8% 中偏上）。
- **边界**：警报是决断提示不是机械平仓指令——趋势健康续涨（突破后动能延续 + 买盘维持）时该让利润奔跑，由 AI 按 trading-strategy.md「突破后动能是否延续」判；但必须显式回应（平 / 不平 + 理由写入分析心跳），不得静默跳过。ws_segment.py（老虎备用源）未挂载——港股主源是富途，老虎源当天被踢场景已由富途覆盖，挂载留待需要时。

### 修复（account_status.py 持仓匹配恒失败的 symbol 取值 bug，2026-08-19 午休修复）

- **为什么改**：当日早市实盘持仓期间（01810 两笔各 5000 股），段结束「📌 持仓状态」连续 8 次误报「账户已无持仓但 actions 无平仓记录」+ 每次伴随「⚠️ 账户存在 actions 无记录的持仓 ['']」——AI 当场错误归因为「读仓 API 偶发空返回」（实查用 `p.contract` 路径所以总能看到持仓，掩盖了工具侧 bug），午休复盘被用户追问才定位真因。根因：SDK `Position` 对象**没有顶层 `symbol` 属性**（代码挂在 `p.contract.symbol`，[position.py](file:///opt/homebrew/lib/python3.11/site-packages/tigeropen/trade/domain/position.py) 实测验证），`account_status.py` 用 `getattr(p, 'symbol', '')` 恒取默认值 `''`——匹配恒失败 → 持仓量恒算 0 → 持仓期间**每段必误报**（非偶发）；同时 extra 检查把 `''` 当未知持仓、误报「用户手动开仓」。
- **改了什么**：[`account_status.py`](.claude/skills/trade/scripts/account_status.py) 新增 `_pos_symbol(p)` 辅助函数（取 `p.contract.symbol`、contract 为 None 兜底返回 None），两处误用点（`acct_pos` 匹配、`acct_syms` 集合）全部改走它；离线单测（假 Position 验证匹配命中 / 空串消除 / None 兜底）+ 模块自测通过。止损价提取部分本就从 `order.contract.symbol` 取、全天正常，未动。
- **回归风险**：低——修复只影响「有持仓时」的账户持仓比对路径，空仓路径（不查账户）与止损价路径不变；唯一行为变化是误报消失（告警只在账户真无持仓时触发）。连带待办：与「跨会话告警串台」待办（见下条）正交，本次未处理会话归属过滤。

### 待办记录（account_status.py 跨会话告警串台，2026-08-19 午休发现）

- **为什么记**：港股早市两会话并行盯盘（实盘会话盯 09888/01347、模拟会话盯 01810/00700 且后者用实盘账户开了两笔小米），实盘会话的段结束「📌 持仓状态」连收 4 次 01810 告警——其中 2 次是模拟会话仓位的真实平仓（告警内容真、归属串台）、2 次是 account_status 持仓匹配 bug 误报（已在 TODO 另条）。根因：`account_status.py` 的 `_today_action_files()` 读全项目共用的 `actions/` 目录（当日 HKT/ET 全读），不区分开仓属于哪个会话。危害 = 假告警稀释真实告警（狼来了效应叠加）+ 空仓会话被别的会话持仓每段打断 + 并行时可能误把别会话的平仓当自己的仓位被动触发去「补记录」污染 actions。
- **改了什么**：本次只记录待办（TODO.md 橙色紧急度新增条目，含修法方向：actions 开仓记录追加会话标识、段输出按会话过滤），代码未改（盘中不动基础设施）。
- **回归风险**：无代码改动；不修的持续风险 = 多会话并行时段输出可信度下降。

### 变更（盈亏净额口径：App 实测为唯一权威，2026-08-19 用户立）

- **为什么改**：2026-08-19 实盘小米平仓，AI 手算净利 +506 HKD、App 实测 +473.93，差 32——根因是 AI 拆算费率无论多细都会漏费项（本次漏交收费 / 交易征费 / 财汇局征费三项 + 印花税按笔取整到元规则）、且 App 成本价口径含费（27.5421 = 27.50 + 开仓费摊薄），两套口径必然有差。用户裁定：**action 记录的盈亏数据直接从 App 取，避免算错**。
- **改了什么**：[`auto-mode.md`](.claude/skills/trade/references/auto-mode.md)「费用与成交价的唯一权威口径」条修订——盈亏净额（含费净利润）唯一权威改为券商 App 实测（取数方式：老虎 SDK `get_filled_orders` 的 `commission` 字段 = 该笔实收总费用 + App 成本价口径，或用户 App 已实现盈亏）；成交价 / max_loss 仍以脚本 JSON 输出为权威。**工具强制落地**（按「文档规定配工具强制」规矩）：平仓脚本（close_position_tiger / _us）成交后自动查成交单 `commission` 字段、输出 `net_pnl_app`（毛盈亏 − 双边实收费）供 AI 直接转录——此部分为待落地方目（盘后实现，见 TODO）。复盘统计的费率口径仍以 review.py / fee_schedule 为准（全样本一致性），单笔动作记录以 App 实测为准、不一致时复盘校正。
- **同步修正**：当日小米平仓动作记录（[actions/2026-08-19-HKT-actions.md](actions/2026-08-19-HKT-actions.md)）盈亏按 App 实测 473.93 校正、实际落地赔率 +0.42R → +0.39R。
- **回归风险**：fee_schedule.py 若不补「印花取整 + 交收最低 2 元」规则，复盘复算口径与 App 实测会持续有小差（本次 4.02 HKD/双边）——已记 TODO 盘后校准（用腾讯 2 单 + 00100 单 + 本次小米单实测费校验过方向正确）；不影响交易决策（开仓赔率门槛的费率口径偏保守方向）。

### 修复（futu_ws_segment.py 启动写 CSV 表头，2026-08-19 盘中发现）

- **为什么改**：当日 auto 实盘盯盘启动后跑 `monitor_summary.py` 报 `KeyError: 'last'`——futu_ws_segment.py 写 monitor_log 不带表头行，csv.DictReader 把首条数据行当列名，全貌摘要（开仓自查①必跑）完全不可用。
- **改了什么**：[`futu_ws_segment.py`](.claude/skills/trade/scripts/futu_ws_segment.py) 启动时 log 文件不存在则先写表头 `time,code,last,bid,ask,ratio,vr,high,low,turnover_yi,stop_price`（对齐 monitor_segment / ws_segment 列名）。当日已删坏 log 重建、验证 monitor_summary 正常输出。
- **回归风险**：无——只对新建文件补表头，既有文件与下游解析（monitor_summary / monitor_guard / review 的 log 读取）均按 DictReader 预期表头存在。

### 变更（美股可交易窗口扩展到盘前：美东 04:00-16:00，2026-08-18 用户立）

- **为什么改**：用户立规——美股交易增加盘前时段，即美股盘中和盘前时段都可以交易（原规定「三类动作 / 信号只在盘中执行、美股仅美东 09:30-16:00」废止，改为「美股可交易窗口 = 盘前 + 盘中 = 美东 04:00-16:00」；16:00 收盘边界与不留仓过夜纪律不变）。时段规定一动，全链路时段判定必须同步——按项目「文档规定必须尽可能配工具强制」规矩，时间闸 / 守卫窗口 / 下单参数三层工具同步落地。
- **改了什么**：
  - **下单链路（auto）**：[`trade_utils_tiger_us.py`](.claude/skills/trade/scripts/trade_utils_tiger_us.py) `_make_order_us` 增 `outside_rth` 参数（默认 True，`create_order` 原生支持、SDK 签名与参数透传已实测确认）——不带该标志的订单盘前提交会被券商拒单或挂到 09:30 开盘才成交，与「盘前可交易」矛盾；开仓（MKT + 附加止损腿）、平仓 fallback MO、独立止损 STP 三个提交路径全部生效。[`close_position_tiger_us.py`](.claude/skills/trade/scripts/close_position_tiger_us.py) / [`move_stop_tiger_us.py`](.claude/skills/trade/scripts/move_stop_tiger_us.py) 的 `modify_order` 调用补 `outside_rth=True`（SDK modify_order 签名含该参数，实测确认）——盘前 modify 止损触发价（平仓主路径 / 移损主路径）不被时段拒绝。
  - **开仓时间闸**：[`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `minutes_to_session_end("US")` 起点从美东 09:30 扩到 04:00（zoneinfo 分支 + zoneinfo 不可用回退分支同步改，回退分支北京口径相应从 21:30 改为 16:00 起点）；拒单文案更新。`check_open_time_gate` 的「距停盯 ≤5 分钟拒开仓」逻辑不变、终点仍是 16:00。
  - **时段状态输出**：[`preflight.py`](.claude/skills/trade/scripts/preflight.py) / [`resume.py`](.claude/skills/trade/scripts/resume.py) 的美股状态判定改为「可交易（盘前 / 盘中·美东 04:00-16:00）」，旧「盘前只预热不发信号」状态废止；`resume.py` 的断层强调判据同步含「盘前」段。
  - **守卫窗口**：[`monitor_guard.py`](.claude/hooks/monitor_guard.py) `in_us_session` 从「美东 09:30-16:00」扩为「美东 04:00-16:00」——盘前盯盘（采样链守卫、跑法拦截、空转硬门）与盘中同权受保护；回退分支同步。
  - **规范文档**：[`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 7 条改写（美股可交易窗口 = 盘前 + 盘中、盘前交易执行差异 outside_rth、盘前流动性须知、「盘前只预热」限定港股）+ 时点平仓表美股行补「16:00 边界不因盘前扩段改变」+ 恢复协议第 2 步同步；[`monitoring.md`](.claude/skills/trade/references/monitoring.md) 「盘前启动只预热」节改写为「港股只预热、美股盘前即可交易」（美股盘前段照常分析、美东 04:00 前启动的准备段才按预热处理）+ 密采样适用窗口 / 早盘必密同步 04:00 起点；[`signal-mode.md`](.claude/skills/trade/references/signal-mode.md) 发信号时段自查条更新 + 提示用户盘前单需 App 开「盘前交易」选项；[`trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md) 主动平仓兜底时点表述同步；README 中英双语同步（Session-bound 条目）。
- **验证**：时间闸构造 9 个时点实测（盘前 05:00/09:29 放行、尾盘 15:59 拒、收盘后 / 美东 04:00 前 / 周末拒、港股口径不变）；`in_us_session` 6 时点实测；preflight / resume 实跑（2026-08-18 21:18 北京 = 美东 09:18 盘前，正确输出「美股可交易(盘前·EDT)·可发信号」）；SDK `create_order` / `modify_order` 的 outside_rth 参数签名与透传 inspect 确认；py_compile 全部改动脚本通过；全项目 grep 终扫无旧口径残留。
- **回归风险**：① `outside_rth=True` 对盘中订单无影响（只解除盘前限制），但**盘后 16:00-20:00 理论上订单也会被券商受理**——防线是时间闸拒开仓（16:00 后 `minutes_to_session_end` 返 0）+ 停盯纪律（16:00 前主动平仓），文档已显式写明「盘后仍不可交易」；② 盘前流动性低、点差宽，市价单（开仓默认 MKT）盘前成交价滑点可能显著大于盘中——策略层已加「盘前形态确认要求更严、宁可等盘中确认」条文，但这是软约束（形态判定无法脚本硬拦，与印花税闸同类理由）；③ 老虎模拟账户（paper）对盘前单的受理行为与实盘可能不同，首次盘前实盘下单时需留意成交回报；④ zoneinfo 不可用回退分支按夏令时硬编码（北京 16:00 起），冬令时会晚 1 小时——与既有回退分支同类局限，主路径 zoneinfo 不受影响。

### 新增（港股个股印花税闸 + ETF 优先升格硬性规定 + hot_list 类型列，2026-08-18 用户立）

- **为什么改**：2026-08-18 午市实盘腾讯两笔（止损距 0.50%/0.45% 股价）毛口径 +300 HKD、净口径 −512.5 HKD——印花税 532 HKD 是最大成本项、占免佣后费用 40%。复盘发现「港股偏好 ETF」写在护栏里但从未起作用：热度榜天然以个股为主、输出无类型列，筛选漏斗前端看不到 ETF 信息，到开仓末端自查才第一次跑 classify（那时已锁定个股）。用户裁定三条全部落地：① hot_list 加类型列；② 费用对比进开仓自查；③ 「偏好 ETF」升格为硬性闸，并要求给出印花税阈值的推理过程。
- **改了什么**：
  - [`hot_list.py`](.claude/skills/trade/scripts/hot_list.py) 港股榜单新增「类型」列（复用 classify_hk_security 白名单判定，判定失败不阻断输出）+ 「个股税闸止损≥」列（1.67%×股价）+ 榜尾 ETF/个股统计行——ETF 偏好从开仓末端前移到找标的第一步在场。修复过程中发现并修正一个接口坑：榜单代码带 `HK.` 前缀，classify 的 normalize_symbol 只认 `.HK` 后缀，原样传入会 miss 白名单误判 stock（07709 实测复现），传参前先剥前缀；
  - [`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 5 条：「港股偏好 ETF」升格为「港股优先 ETF + 个股必须过印花税闸并说明无 ETF 替代」；开仓前自查清单新增硬性条目：个股止损距 ≥ 1.67%×股价（= 双边印花税 R 损耗 ≤ 0.12R）才可开个股，不足时换同板块 ETF 替代或放弃，动作记录注明「印花税闸已过」；
  - [`risk-management.md`](.claude/skills/trade/references/risk-management.md) 新增「港股个股印花税闸」节：完整推导（期望落地 0.6R 基准 ← 2.4R 门槛 × 25% 历史兑现率；印花税 R 损耗公式 = 0.2%×入场价÷止损距、与仓位无关；阈值取期望落地 20% 的三条理由——含佣金 6-10%/平台费 2-7% 参照系的精确口径：佣金 R 损耗 = 0.058%×股价÷止损距、平台费 R 损耗 = 30÷max_loss；1.67% 换算；腾讯事故标定）+ 工具落地说明 + 无法脚本硬拦的原因（止损位由形态现场判定，闸只能在自查环节执行）。用户复核时追问「佣金约 10%」的出处，实算补全两公式并把参照系数值精确化（原「约 10%」只在止损距 1% 的单里成立，闸值 1.67% 下实为 6%）。
- **验证**：hot_list 实测（收盘后）——07709 正确判 ETF、腾讯显示税闸 7.39、榜尾统计「ETF 1 只 / 个股 7 只」；py_compile 通过；SKILL.md / risk-management.md 改动后 grep 核对落位。
- **回归风险**：① 闸只约束港股个股，ETF/REIT 与美股不受影响（美股无印花税）；② 1.67% 阈值基于 25% 兑现率的小样本（9 笔），样本扩大后兑现率若变化应重标闸值（已在推导节留档依据）；③ hot_list 对每只榜单标的跑 classify（白名单本地缓存查询、无网络开销），榜单输出耗时无感变化。

### 新增（长桥可做空探测方法验证：下单探测法，2026-08-18；全量结果待盘中重扫）

- **为什么改**：用户指出要对比长桥主账户与老虎主账户港股可做空标的的**数量差异 + 具体标的清单**——长桥 OpenAPI/CLI 无「可做空名单」接口（见下方条目），用户提出用「特定标的下单看返回提示」探测，需验证该方法可行性并全量应用。
- **改了什么**：探测方法实证可行——对标的挂 Sell 空单（LO 限价=现价×20、1 手、TIF day、`LONGPORT_REGION=global` 国际区），按返回码判定：`603301` 标的不支持做空（名单外）/ `603302` 名单内但券池不足 / `602315` 内地监管禁空（长桥对部分标的独立禁空）/ 受理（order_id）后立即撤单 = 可做空。对照实验：700.HK（名单内）受理后 Rejected 购买力不足 ✓可做空；8223.HK（名单外细价股）603301 ✓不可做空——可区分。脚本 `tmp/scan_longbridge_shortable.py`（断点续跑 + 结果 CSV）。**关键教训**：① **收盘后长桥对卖空探测一律返回 602315**（实证 15:50 盘中 700.HK 受理、17:00 收盘后同命令 602315）——当日 16:00 后全量扫描的 1257 只判定全部作废；② 密集探测（0.25s/笔）触发 429002 限频污染判定，建议 ≥1.5s/笔；③ 中国区网关按账户身份一律 602315（与出口 IP 无关）。另坐实：长桥 CLI 帮助明确 HK 卖空需先签 SBL 协议（未签报 602301），本主账户未遇说明已签。
- **验证**：对照实验双标的 + 700.HK 探测单状态回查（Rejected 无成交无残留）+ 全量脚本收盘后运行发现判定异常 → 手动复现 602315 坐实作废 → 全部判定标注 INVALID。
- **回归风险**：无代码改动（全部产物在 `tmp/` 临时目录）；方法文档与盘中重扫计划已沉淀（`tmp/longbridge_scan_method.md` + TODO.md 新条目「长桥可做空标的全量扫描（港股盘中重跑）」）。

### 新增（持仓状态工具强制 + 空仓不查规则落地，2026-08-18 用户两起实盘事故后）

- **为什么改**：2026-08-18 实盘 00700 连续两起事故——① AI 开仓后用户在 App 把止损 440.0 移到 442.6，AI 未知悉、仍按 440 记忆盯盘；② 止损单 14:04:19 触发成交、持仓归零，AI 空盯约 29 分钟未察觉。根因同一：AI 只信开仓脚本输出与行情采样、从不向账户核对。SKILL.md / auto-mode.md 虽明文规定「每轮采样查账户当日订单获取最新止损价」「最新止损价不能凭记忆」，但散文规定靠 AI 记忆执行、上下文压缩后必衰减（2026-08-18 用户立「文档规定必须尽可能配工具强制」）；用户同时纠正「空仓时不必查持仓状态（别人开的仓不用管）、自己开仓后才高频核对账户订单 + 持仓」。
- **改了什么**：
  - 新增 [`scripts/account_status.py`](.claude/skills/trade/scripts/account_status.py)：读当日 `actions/` 判定 AI 认为的持仓（开仓未平）→ **无持仓返回 None（零 API 调用，遵循「空仓不查」）**；有持仓才查老虎账户——输出账户实持量 + 该标的活动止损单最新触发价（用户 App 手动移损当场可见，不凭记忆）+ 状态比对告警（账户已无持仓但 AI 未记录平仓 → 🚨；数量不一致 → ⚠️；账户有 actions 无记录持仓 → ⚠️ 漏网提示）。账户选择：当日 actions 含「实盘」字样 → `load_config(account='live')`，否则默认账户。
  - [`scripts/futu_ws_segment.py`](.claude/skills/trade/scripts/futu_ws_segment.py)（主力采样源）`finish()` 破位输出后接入持仓状态行——持仓期间每段结束自动打印「📌 持仓状态」，把「查账户」从 AI 记忆责任变成段输出自带。
  - [`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 硬护栏 1 + [`SKILL.md`](.claude/skills/trade/SKILL.md) 硬护栏 4 补「持仓状态工具强制 + 空仓不查」条文（含 2026-08-18 事故背景、AI 收到 🚨 后的处置）。
- **验证**：py_compile 两脚本通过；自测三场景——当前空仓 → 返回 None 不查账户 ✅；mock「有持仓 + 账户实持 0」→ 输出 🚨 告警（复现昨日事故场景，工具当场暴露）✅；mock 有持仓走 `futu_ws_segment.finish()` 完整段输出 → 持仓状态行正常打印 ✅；真实 8 秒短段空仓 → 静默无报错 ✅。
- **回归风险**：低——`futu_ws_segment.py` 改动仅在 finish() 加一段 try/except 调用（失败打印 err 行、不影响主流程）；`account_status.py` 为新增独立模块不改既有行为；备用源 `monitor_segment.py` 的每轮止损查询与第四层双持仓兜底保持不变（该源空仓也查属既有安全件设计，不因本改动退化）。

### 新增（长桥与老虎主账户港股可做空标的数量对比评估，2026-08-18，只读调研）

- **为什么改**：TODO 绿色紧急度条目（2026-08-18 14:07）——做空标的池大小直接影响空头策略的标的可选范围，需实测两家主账户在港股的可做空标的数、对比哪个更多，作为选账户执行空头信号的参考依据。
- **改了什么**：无代码 / 文档改动，纯只读调研 + 产物落盘 `tmp/`（按临时产物规则不进 git）——`tmp/shortable_compare_2026-08-18.md`（结论报告）、`tmp/tiger_hk_shortable.csv`（老虎 3980 代码明细）、`tmp/hkex_ds_list.csv`（港交所名单原件）、`tmp/tiger_hk_shortable_summary.json`（摘要）、`tmp/scan_tiger_shortable.py` / `tmp/scan_tiger_fix.py`（扫描脚本）。对应 TODO 条目已完成归档（TODO-archive.md「🟢 绿色紧急度·长桥与老虎港股可做空标的对比」节）。
- **核心结论**：**老虎主账户可做空 1257 只**（`get_stock_details.shortable>0`、剔除 90/91 未上市预留码口径；其中 83xxx RMB 柜台 48 只）——与港交所 2026-08-14 生效版指定可卖空名单 1234 只（股本 813 + 基金 421）交叉验证，名单内标的** 100% 覆盖**（1234/1234、缺口 0），多出的 23 只为已除牌旧代码 / 政府银债 / 美元柜台 ETF。**长桥侧 API 无法统计**（security_list 无做空标记、static_info 无做空字段【GitHub issue #359 加字段请求被官方关闭】、estimate_max_purchase_quantity 的 Sell 侧返 0/0），只能 App 人工查「可沽空」额度。**判断**：两家做空池上限同为港交所名单，不存在「长桥能空、老虎不能空」的合规标的缺口；实际差异只在借券池深度（逐标的维度，名单数量对比无法概括）。另实测：长桥主账户（env-sg，token 至 2026-10-13 有效）当前 net_assets=0。
- **验证**：老虎侧 3980 代码全量扫描（限频 10 次/分钟下两轮完成、失败批次补扫归零）；港交所名单官网下载解析；集合交集 / 差集 Python 实算；23 只差异标的富途逐个核身；长桥 API 三条路径逐一实测（连接成功 + 接口真实返回结构确认）。
- **回归风险**：无——纯只读调研（全部为账户查询与公开数据下载，无下单、无文件改动进 git 跟踪范围）。

### 修复（港股购买力币种偏差：buying_power USD 当 HKD 用，小账户误拒开仓，2026-08-18）

- **为什么改**：2026-08-18 午市实盘首单（00700 腾讯做多 1300 股）被开仓脚本主动拒单——`buying_power（USD 口径，值略）× 0.3 ÷ 441.8（HKD 价）` 算出远小于 1 手 100 的股数，降档到 0 直接拒绝。根因：`get_buying_power_tiger` 把 USD 口径的 buying_power 直接除以 HKD 标的价，上限被压低 ~7.8 倍；原注释称「保守方向安全」，但小购买力账户（bp 折 HKD 后本可开 300 股）会被这条「保守」直接压成 0——保守过头同样开不了仓。paper 账户 bp 巨大（403 万 USD）从未暴露此 bug，切实盘小账户即现形。
- **改了什么**：[`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `get_buying_power_tiger` 新增 `to_hkd` 参数——传入汇率即把 bp 折成 HKD 再算可买上限，拿不到汇率时按 7.80 保守常量兜底（低于实测 ~7.84，方向保守），返回的 bp 也改为折算后 HKD 值；[`open_position_tiger.py`](.claude/skills/trade/scripts/open_position_tiger.py) 调用处先从 `load_equity_tiger` 双币种净值（HKD÷USD）推老虎自身 forex_rate 传入。
- **验证**：py_compile 两脚本通过；实盘账户实跑（不下单）——汇率 7.8445、购买力上限足以开整手 300 股（bp 折 HKD 具体值略），整手 300 股 max_loss 540 HKD，修复前同参数误拒。
- **回归风险**：① 7.80 兜底常量低于真实汇率，极端情况下上限略保守（可接受，方向安全）；② 美股脚本 `open_position_tiger_us.py` 未查是否同样受影响——美股标的价就是 USD、与 bp 同币种，天然无此偏差，不受影响。

### 变更（实盘确认请求必须用 AskUserQuestion 工具发起，2026-08-18 用户立）

- **为什么改**：2026-08-18 午市实盘盯盘启动时，AI 切实盘账户的确认请求用纯文本发出（用户回「确认」后才算授权）——纯文本提问的确认存在歧义空间：AI 可能误解用户回复的指向、或把不明确的回复当默认同意；实盘授权是「误操作无缓冲」的最后闸口，确认动作本身必须无歧义。
- **改了什么**：[`SKILL.md`](.claude/skills/trade/SKILL.md)「模式开关与触发首动作」节「auto 模式的账户选择」段修订：切实盘前的额外确认**一律用 `AskUserQuestion` 工具发起**（不许纯文本提问）——问题写明实盘打码账户号 + 本次授权范围（单笔风险上限 B、开仓市值上限），选项含「确认授权 / 切回模拟 / 停止盯盘」，用户显式点选后才算授权。这正是「文档规定配工具强制」的落地：确认动作被工具结构钉死，不靠 AI 临场组织措辞。
- **验证**：SKILL.md 改动后该段已含「AskUserQuestion 工具发起」硬性表述与理由；本次午市实盘授权已按新规走 AskUserQuestion 完成（用户点选「确认授权」）。
- **回归风险**：无——只改确认请求的发起形式，授权流程本身（明确说切实盘 + 额外确认两道闸）不变；AskUserQuestion 在盯盘期间仅用于实盘授权这一次结构化确认，不与「盯盘期间不 AskUserQuestion 中断」冲突（那条管的是盯盘决策类提问，授权确认是安全闸、恰是要中断的那一次）。

### 新增（项目级立规：文档规定必须尽可能配工具强制，2026-08-18）

- **为什么改**：工具强制四件套（同日上一条）验证了「工具在场 vs 靠记忆」的执行率差异后，用户把它上升为项目级通用规定——所有写进文档的规定都不能只靠 AI 自觉执行。
- **改了什么**：[`.claude/CLAUDE.md`](.claude/CLAUDE.md) 新增「文档规定必须尽可能配工具强制（2026-08-18 用户立）」节：新写 / 修改 `CLAUDE.md` / `rules/*.md` / `skills/*.md` 里的任何规定时，同步评估并落地工具强制（① hook/脚本硬拦 → ② 决策时刻工具在场打印 → ③ 定时重读，按强制力降序选），三层都不适用时须在规定条文处显式注明「无法工具强制 + 理由」。
- **验证**：CLAUDE.md 改动重读确认落位（第 10 行节标题）。
- **回归风险**：无——纯规定新增；对存量规定的工具化补齐按接触一条做一条推进。

### 新增（工具强制防「忘记规矩」四件套：hook 跑法拦截 + 下单时间闸 + 边界提醒 + 护栏重读，2026-08-18）

- **为什么改**：2026-08-18 早盘 auto 实盘盯盘连续三起「规矩写在文档里但没执行」——① 11:30 自设「午前开仓截止线」违反「距停盯 >5 分钟开仓资格就在」（trading-strategy.md 2026-08-17 立，同款违规两天两次）；② 切富途逐笔源时把段启动方式误改成 nohup+sleep 轮询，数据滞后 40-60 秒、错过入场判定（规定 = run_in_background + task-notification 通知驱动）；③ 老虎 ws 段与并行会话互踢后处置迟缓。用户裁定：「加上工具强制，把忘记规矩导致违反规矩的概率尽可能降低」——当日对照实验证明凡是工具在场打印的（VWAP 检查/心跳提示/hook 拦截）全部守住，凡靠 AI 记忆的全部失守，故把四类规则下沉到工具层。
- **改了什么**：
  - [`monitor_guard.py`](.claude/hooks/monitor_guard.py) PreToolUse 新增**跑法拦截**：盘中采样段命令含 nohup / 行尾 `&` / 拼接 sleep → exit 2 阻断，提示「段启动必须 run_in_background + task-notification 通知驱动，切数据源只换脚本名不换启动方式」；
  - [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) 新增 `minutes_to_session_end(market)`（HK 按当前半场 12:00/16:00、US 按美东 16:00 zoneinfo 动态）+ `check_open_time_gate(market)`（距停盯 >5 分钟放行并打印「开仓资格仍在」、≤5 分钟或盘外拒单）；
  - [`open_position_tiger.py`](.claude/skills/trade/scripts/open_position_tiger.py) / [`open_position_tiger_us.py`](.claude/skills/trade/scripts/open_position_tiger_us.py) main() 参数解析后接**时间闸**：`check_open_time_gate` 不过即输出 `{"ok":false,"blocked_by":"time_gate"}` 拒单——同时堵「该开不开」（AI 自设截止线）与「不该开乱开」（临场抢开）两头；
  - [`monitor_segment.py`](.claude/skills/trade/scripts/monitor_segment.py) / [`futu_ws_segment.py`](.claude/skills/trade/scripts/futu_ws_segment.py) / [`ws_segment.py`](.claude/skills/trade/scripts/ws_segment.py) 段结束新增**停盯边界提醒**：距停盯 ≤60 分钟每段打印剩余分钟 + 「>5 分钟开仓资格仍在、按压缩止盈实算赔率照常评估、禁止自设截止线」，≤5 分钟打印「绝对不开仓窗口」；
  - 同三脚本**重估方向提醒文案**追加「另：重读 SKILL.md 硬性护栏全 8 条刷新记忆」——每 10 分钟定时刷新规则记忆，对冲上下文压缩后的规则衰减。
- **验证**：hook 跑法拦截三用例（nohup+& 拦 / sleep 拼接拦 / 合法 run_in_background 段未被跑法规则误拦——被空转防护拦属真实状态正当拦截）；时间闸六场景（HK 11:20 放行 / 11:56 拒 / 12:30 午休拒 / 15:20 放行 / US 北京 22:00 放行 / 03:58 拒）全对；三段脚本 py_compile 过 + 8 秒段实测输出正常（重估文案已带护栏重读）。
- **回归风险**：① 时间闸只拦开仓不拦平仓/移损（风控动作任何时候都不该被卡，SKILL.md 已有此边界）；② 边界提醒 ≤60 分钟窗口每段打印一条（约 60-80 条/小时），有输出噪音——换取决策时刻规则在场，值得；③ hook 跑法拦截的行尾 `&` 正则只匹配三段脚本名结尾形态，普通后台命令不误伤。

### 变更（段内动作顺序钉死：拿到段数据立刻分析、机会当场执行，其余一切靠后，2026-08-18）

- **为什么改**：2026-08-18 港股早市实录——AI 在段结束拿到数据后先「启动下一段采样」再回来分析（为压采样空窗自作主张的顺序），被用户连续三次纠正：行情瞬息万变，「先启下一段再分析」会把手里最新鲜的数据放旧几秒，下单时机差几秒价就可能不同；正确顺序 = 拿到数据**立刻分析**（期间不做任何别的事）→ 有机会**当场下单**（不排队不等下一轮）→ 没机会才做剩下的事（写分析心跳、重启下一段）。另同日早些时候用户已纠正「固定 sleep 48 等段结束」改为 1 秒粒度等进程退出。
- **改了什么**：[`references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「每段最小输出模板 + 分析心跳」节新增硬规矩段「段内动作顺序 = 分析最先、其余一切靠后」（优先级高于本节其它顺序表述，含为什么与实录引用）；[`ws_segment.py`](.claude/skills/trade/scripts/ws_segment.py) / [`futu_ws_segment.py`](.claude/skills/trade/scripts/futu_ws_segment.py) 段结束「👉 空转防护」提示行文案改为新顺序（立刻分析 → 有机会当场执行 → 没机会才写心跳 + 重启下一段），两脚本 docstring 同步。
- **验证**：monitoring.md / 两脚本改动后 grep 核对新顺序文案已落位；两脚本 py_compile 通过。
- **回归风险**：无——只改提示行文案与文档规矩，采样逻辑零改动；与 2026-08-17 立的「先分析再重启下一段」空转防护同向收紧（把「先分析」进一步钉死为「分析期间不做任何别的事，执行紧跟分析」）。

### 变更（港股每秒采样默认源反转：富途为主、老虎降备用，2026-08-18）

- **为什么改**：2026-08-18 早盘两会话并行盯盘连续两起事故——① auto 会话的老虎 `ws_segment` 段与并行 signal 会话互踢（code 4001 kick out，老虎 PushClient 单账户单连接固有产品限制，方案 A 并行模式下必然发生）；② 富途逐笔与老虎 ws 段并行时 `futu_ws_segment.py` 对 00981 间歇错收深市价（沃尔核材 10.6x，与真实价 77.x 差 7 倍，根因未定位但复现稳定，疑似 OpenD 同码多协议订阅串扰）。用户裁定：每秒采样默认就用富途，老虎作为备用。
- **改了什么**：[`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 8 条——港股每秒采样默认源改为 `futu_ws_segment.py`（富途逐笔，本地网关多客户端互不踢），老虎 `ws_segment.py` 降为备用源（仅富途 OpenD 不可用时顶上、用完切回，并行场景即便富途故障也知老虎互踢风险）；[`references/monitoring.md`](.claude/skills/trade/references/monitoring.md) 「每秒采样」节主次同步反转（标题加注 2026-08-18 反转 + 默认源理由 + 备用源触发条件与并行限制）、脚本清单两条互换主次、「老虎连接被占用时切」条改为「误用处置（默认源反转后基本不再触发）」。
- **验证**：SKILL.md / monitoring.md 改动后 grep 核对「默认富途」表述已落位；富途逐笔链路当日全程实测稳定（阿里 09988 每秒采样 2.5 小时无错价）。
- **回归风险**：无——富途逐笔当日已是实际主力（老虎被踢后一直在用），本次是把事实固化为默认；老虎链路代码不动、降为条件触发。

### 修复（黄色紧急度脚本卫生 6 项 + 低危串：切片丢点 / 变量遮蔽 / 崩溃守卫 / 口径统一 / 方向语义 / 转义与硬编码，2026-08-17）

- **为什么改**：2026-08-15 全项目逻辑审计遗留的黄色紧急度卫生条目（TODO 2026-08-16 14:27 记录）——不影响主链路正确性，但属「边界情况出错 / 卫生问题」：切片丢尾部点、变量遮蔽定时炸弹、极端样本整脚本崩、同文件口径打架、输出语义颠倒、CSV 转义与时令硬编码。
- **改了什么**：
  - [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) / [`trade_utils_tiger_us.py`](.claude/skills/trade/scripts/trade_utils_tiger_us.py)：`round_to_tick` 恒 floor 的方向性后果写进 docstring（卖方限价低于 bid 更激进、做多止损触发价压低一个 tick 致实际 max_loss 略大于计划）——行为不动（历史一致），说明补齐；
  - [`monitor_summary.py`](.claude/skills/trade/scripts/monitor_summary.py)：4 段均价切片改浮点边界索引（`int(n*i/4)`），不整除时不再丢尾部最多 3 个最新点；VWAP 段 `_q.close()` 挪进 try/finally、异常时连接必关（recent_n 守卫一项经核对 2026-08-16 已连带改对，未重复改）；
  - [`bayes_evolution.py`](.claude/skills/trade/scripts/bayes_evolution.py)：局部列表 `FS` 改名 `FEE_FRACS`（不再遮蔽 `import fee_schedule as FS` 模块别名）；`len(trades) < 2` 守卫（单样本不再 IndexError 崩整脚本）；
  - [`review.py`](.claude/skills/trade/scripts/review.py)：新增 `_log1p_fR`（`max(1+fR, 1e-12)` 压下限，f=0.5 档 R≤−2 不再 ValueError 崩复盘 / 演化）4 处调用统一替换；过程指标分组 `Ld` 改 `R <= 0`（R=0 归败，与 summarize 口径统一、n 加总 = N）；
  - [`kline.py`](.claude/skills/trade/scripts/kline.py)：斐波那契区分趋势方向——上涨出回调支撑位 `lo+span×r`、下跌出反弹阻力位 `hi−span×r`（原只输出前者、下跌段语义颠倒）；
  - 低危串 8 小项：`ring.sh` NOTE 逗号按 CSV 规范双引号包裹（`resume.py` csv.reader 实测解析回 4 列）；`monitor_log_gap_check.py` 港股午休 12:00-13:00 gap 豁免（跨午夜不误判，豁免项单列展示）；美东时差硬编码四处全改 `zoneinfo` 动态取美东日期（`monitor_summary` / `monitor_segment` / `ws_segment` / `resume`，−12h 留 zoneinfo 不可用兜底，11 月切冬令时起生效）；`hot_list.py` 非法 market 参数 stderr 报错 + exit 1（不再静默落美股）；`fee_schedule.py` REIT 落 stock 档与交收费 / 印花税取整两处待核实注释（数字不动）；`to_futu_symbol_us` 改 `replace(".","")`（BRK.B → US.BRKB，实测 MU / BF.B 均正确）；`move_stop` / `close` 四脚本 `--account` 缺值统一报「需要一个值：live / paper」（实测 'None' 文案的实际出处是 move_stop 两脚本、close 是静默落默认 paper）。
- **验证**：15 个改动脚本 py_compile 全过；`review.py` 实跑 42 笔全流程正常；`bayes_evolution` 出 7 张图与已提交版本 md5 逐字节一致（证明正常样本数值不受影响）；BRK.B 转换 / log1p 保护（R=−2.5 → −27.63 有界不崩）/ R=0 归败 / kline 方向分支抽查复核通过；ring.sh 转义、午休豁免、zoneinfo 口径实测通过。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① `_log1p_fR` 把 1+fR 压到 1e-12 只影响**爆亏样本**（正常样本 max 不触发、逐位不变——md5 对照已证）；② `Ld=[R<=0]` 使过程指标亏损组可能多出 R=0 的笔（这正是修复目的，与终局统计对齐）；③ `ring.sh` 引号包裹改变 ring-log.csv 新增行的字面格式（无引号先例），消费方 `csv.reader`（正确解析）与 `cut -d','`（只取首列）已实测兼容——若有仓外脚本按「无引号字面」解析 NOTE 列需自查。

### 变更（多会话并行盯盘·方案 A 全量落地：单持仓互斥四层防线 + 跨会话副作用消除，2026-08-17）

- **为什么改**：单会话盯盘在多标的时「段间分析串行」是时间利用率瓶颈——40 秒采样段结束后串行分析所有标的，标的越多每只分析越浅、破位到决策链路越长。方案 A = 多个 Claude Code 会话各盯各的标的池并行盯盘，**单持仓互斥**保证全局任意时刻至多一笔在场（风控语义不变、仍是单笔口径）。绿灯待办（港股 + 美股两项），实现计划见 `PLAN-parallel-monitor-HK.md` / `PLAN-parallel-monitor-US.md`，本次按计划九项改动全量落地。
- **改了什么**：
  - **公共互斥模块** [`trade_mutex.py`](.claude/skills/trade/scripts/trade_mutex.py)（新建，港美共用、全局一把锁不分市场）：① flock 文件锁（`tmp/trade_mutex.lock`）把「查单 → 闸门 → 下单 → 确认」临界区内核级串行，进程崩溃自动释放；② 三口径开仓闸门（锁内执行）——pending intent / 当日在场敞口（按订单流「开仓成交 − 平仓成交」，与成交确认同数据源、规避持仓接口传播延迟）/ 活动开仓挂单，任一命中拒开；③ intent 写前日志（`tmp/trade_intent.log` JSON Lines，submit 前写 pending、确认后 settle 终态，崩溃窗口闭合）；命令行小工具 `status` / `--clear-intent <行号>`。
  - **港股开仓接锁** [`open_position_tiger.py`](.claude/skills/trade/scripts/open_position_tiger.py)：整个下单流程（含降档重试循环）包进 `TradeMutex`；闸门拒开输出 `blocked_by` 结构化 JSON（AI 据此继续盯盘不重试）；intent 终态结算（filled / part_filled / cancelled / rejected；模糊失败保留 pending 逼人工确认）；查单失败保守拒开。
  - **美股开仓接锁** [`open_position_tiger_us.py`](.claude/skills/trade/scripts/open_position_tiger_us.py)：同港股口径（market="US"、同一把全局锁——分市场两把锁会放过「港股持仓违规过夜 + 美股开新仓」跨市场叠加路径）；[`trade_utils_tiger_us.py`](.claude/skills/trade/scripts/trade_utils_tiger_us.py) 补 `get_today_orders_us`（查询失败上抛由调用方保守拒开，与港股采样降级口径不同、闸门口径须区分「查无」与「查询失败」）。
  - **第四层兜底** [`monitor_segment.py`](.claude/skills/trade/scripts/monitor_segment.py)：每轮采样的止损价查询同一次 `get_orders` 顺带检测白名单外双持仓（`check_position_discipline`，复用 `today_open_exposure` 口径）——脚本拦不住的漏网（用户手机 App 手动开仓等）从一个采样段内（≤40 秒 + 段间）告警（每轮输出直到消除），AI 处置 = 平掉较新的那笔。订单缓存 `_today_orders_cache` 避免多打一次 API。
  - **config 白名单** [`config.json`](.claude/skills/trade/config.json)：`risk.resident_positions: ["02800", "07709"]`——2026-08-03 前的常驻历史持仓不进开仓意图链、不拦截闸门（裸口径「有持仓即拒开」会永远拒开）。
  - **preflight 互斥状态** [`preflight.py`](.claude/skills/trade/scripts/preflight.py)：启动时输出 intent pending / 常驻白名单 / 在场会话数——多会话启动一眼看到「谁在场」。
  - **caffeinate 引用计数** [`keep-awake/scripts/off.sh`](.claude/skills/keep-awake/scripts/off.sh)：停盯先注销本会话，注册文件还有别人 = 保留 caffeinate（最后一人停盯才解除防睡眠）；单会话行为不变。消除「会话 A 停盯 pkill 杀掉 B 的防睡眠」跨会话副作用。
  - **resume 按市场 scope** [`resume.py`](.claude/skills/trade/scripts/resume.py)：`last_monitor_time` 加 market 参数，传标的参数时按主标的市场只查本市场 log——港股会话断层检测不再被美股采样掩盖、反之亦然；无参保持旧全市场口径。
  - **文档**：[`monitoring.md`](.claude/skills/trade/references/monitoring.md) 新增「多会话并行盯盘」节（启动序列、互斥语义、标的池分配约定、跨会话副作用、美股差异）；[`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 4 条补「纪律 + 工程双保险」句、盯盘工作流指针补节名。
- **验证**：trade_mutex 单元七场景（空账户放行 / 敞口拒 / 挂单拒 / pending 拒 / clear 后放行 / flock 双线程临界区串行 / 白名单读数）全过；**双进程端到端并发验收**（模拟双终端开仓）——flock 串行（后到者等锁 1.17s）、时序不重叠、持仓在场（pending 残留）时后到者收 `blocked_by=pending_intent`；monitor_segment 第四层检测五场景（双开仓告警 / 一开一平过 / 白名单标的成交不误报 / 止损触发算平仓 / 挂单不算在场）全过；caffeinate 引用计数双会话实测（B 停盯保留、A 最后停盯关灯判定正确）；resume scope 实跑（HK scope 只查港股 log）；preflight 实跑（互斥状态输出就绪）；trade scripts 全量 24 个 py_compile 通过。
- **边界（如实说明）**：① 互斥只覆盖脚本可控路径——用户手机 App 手动下单由第四层监控 40 秒内发现（存量风险从「静默双持仓」变「一个采样段发现」，非本次引入）；② signal 模式多开不在本次范围（equity-log 单一累加链需另行改造，Plan 已声明只覆盖 auto）；③ **paper 盘中真实双开仓实测**（PLAN 验收第 1/2 步的双终端小额实测）尚未做——本次用进程级模拟验收替代，下个交易日盘中灰度前补；④ 常驻持仓白名单当前按港股两笔长持录 config，美股无历史长持（2026-08-05 实测仓位已清）。
- **首个交易日灰度口径（PLAN 第七节）**：先 2 会话、各 2-3 只标的，观察一天互斥与采样稳定性后再放宽。

### 变更（实盘账户无授权全场景禁下单：SKILL.md 模式选择节 + 硬性护栏第 0 条，2026-08-17）

- **为什么改**：用户 2026-08-17 立——实盘账户未经授权，**任何时候都不能用来下真实交易订单，不限于盯盘场景**（复盘、测试、实验、演示等任何场景一律同禁）；需要交易测试时只能用模拟盘。原规定只有「用户要切实盘时」的两道闸流程（明确说 + 额外确认），缺「用户没要切实盘时」的绝对禁区表述——本次补上，作为护栏的护栏。
- **改了什么**：[`SKILL.md`](.claude/skills/trade/SKILL.md) ①「auto 模式的账户选择」节插入全场景禁区段（与两道闸的关系：两道闸管「用户要切实盘时」的流程、本条管「用户没要切实盘时」的禁区——无论何种场景，AI 不得以任何理由【测试、验证、演示、顺手】对实盘账户发起下单）；②「硬性护栏」节新增**第 0 条（绝对禁区，优先于本节所有条目）**——第 1-8 条都过、只要账户是未经授权的实盘，一样不得执行。

### 变更（TODO 紧急度命名弃「灯」用「色」：TODO.md / TODO-archive.md 分节标题与正文同步，2026-08-17）

- **为什么改**：用户 2026-08-17 立——TODO 紧急度命名不用「灯」这个字，改用「色」，如「红色紧急度」。分级标准与排序不变，只改命名词形。全局 CLAUDE.md 已同步修订（该变更记 CapabilityManagerAgent CHANGELOG），本项目侧同步自己的两个 TODO 文件。
- **改了什么**：[`TODO.md`](TODO.md) 四个分节标题「🔴 红灯 / 🟠 橙灯 / 🟡 黄灯 / 🟢 绿灯」→「🔴 红色紧急度 / 🟠 橙色紧急度 / 🟡 黄色紧急度 / 🟢 绿色紧急度」，头部说明与三条备忘条目正文里的级别词一并换；[`TODO-archive.md`](TODO-archive.md) 两个历史节名改写并注原词（「橙灯」→「橙色紧急度」等）、正文级别词同步。

### 变更（平台费口径全面改固定模式 + 美股费用结构按官网核改：成本计算统一，2026-08-17）

- **为什么改**：2026-08-12 立的阶梯平台费口径与账户实际计费套餐不符——所有成本计算（前瞻净赔率 / 复盘净盈亏 / EV / 贝叶斯）的平台费改用**固定模式**（用户 2026-08-17 19:11 立，橙灯待办）；且旧美股费率「佣金 0.029% 最低 15 按额计」是把港股结构硬套美股的简化——老虎美股实际**按股计**（官网 https://www.itigerup.com/hans/help/detail/74820992 2026-08-17 实查核实：佣金 0.0039 USD/股、平台费固定式 0.004 USD/股 每笔最低 1 USD、两者上限均为总交易额 × 0.5%）。固定模式另一收益：费率与订单数解耦，**不再需要查当月累计订单数**——signal 模式「为算阶梯平台费查当月订单数的只读查询例外」随之取消，费率计算不再需要任何账户查询。
- **改了什么**：
  - [`fee_schedule.py`](.claude/skills/trade/scripts/fee_schedule.py) 重写：港美分流——港股按额 + 按笔（佣金 max(15, ×0.029%) + 印花税个股 0.1%/ETF 免 + 征费 + 固定平台费 15 HKD/笔）、美股按股（佣金 0.0039/股 cap 0.5%×额 + 平台费 0.004/股 最低 1/笔 cap 0.5%×额 + 代收近似 0.00396/股）；阶梯表降为备查（`PLATFORM_TIERED_HK`，不再被调用）；`fee_per_side` 新增 `shares` 参数（美股必传）、旧 `order_index_in_month`/`platform_mode` 参数保留签名兼容但被忽略。港股大单口径收敛值从 ≈14.17/4.17 bps 变为 ≈14.32/4.32 bps（含固定平台费）。
  - [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py)：`build_fee_ctx` 退化为纯属性打包 {shares, sec_type, market}（不再查账户订单数）；`get_month_order_count_tiger` 移出费率链路、留诊断用途（docstring 标注已废弃）；`_net_odds` 调 `FS.fee_per_side` 显式传 shares。
  - [`review.py`](.claude/skills/trade/scripts/review.py) / [`bayes_evolution.py`](.claude/skills/trade/scripts/bayes_evolution.py)：删「(自然月, 市场)」订单序号计数器与月分档逻辑，fee 调用统一传 shares。
  - 文档六处同步：[`risk-management.md`](.claude/skills/trade/references/risk-management.md)「费率与成本」整节重写（港美分流结构 + 固定模式 + signal 只读查询例外简化）；[`SKILL.md`](.claude/skills/trade/SKILL.md) 6 要素第 4 条；[`trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md) 赔率三阶段费率表述；[`review-and-evaluation.md`](.claude/skills/trade/references/review-and-evaluation.md) 净盈亏定义公式；[`auto-mode.md`](.claude/skills/trade/references/auto-mode.md) / [`signal-mode.md`](.claude/skills/trade/references/signal-mode.md) 平仓必填节。
- **实现中发现并修正一个反向 max() bug**：美股平台费 cap（总交易额 × 0.5%）官网语义是**上限**（低价股场景封顶），初版实现写成 `max(按股值, 最低 1, cap)` 把 cap 当成了下限——MU 106 股 @865 的大单被算出 458 USD 平台费（正确值 1 USD，按股值 0.424 触发每笔最低 1）。实算对照发现后修正为 `min(max(按股值, 最低 1), cap)`，cap 只在股价 < 0.8 USD 时触发的正确语义。
- **验证**：七脚本 py_compile 通过；fee_schedule 自检（港股大单 stock 14.32 bps / etf 4.32 bps；美股 MU 106 股 @865 单边 1.83 USD；低价股 1000 股 @0.5 cap 触发 8.96）；review.py 实跑港股 42 笔（EV +0.47）与港美合并 61 笔（EV +0.24，美股 20 行按股计费正常）；bayes_evolution 出图 7 张无报错。
- **口径影响（如实说明）**：历史统计数字会随费率口径变化——港股 42 笔 EV 由阶梯口径的值略变（固定 15/笔 vs 阶梯 30/15/10 混合，费差不大）；美股费用大幅下降（旧口径 MU 单笔双边佣金 ≥30 USD，新口径 ≈2.5 USD + 平台费 2 USD），合并 61 笔 EV 从旧实现的 −0.07 修正为 +0.24（旧美股按额佣金结构高估成本）。历史复盘报告中的 EV 数字是当时口径的结果，不回改。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① 与 2026-08-12「真实费率改造」条目对照——本次保留其「费率统一走 fee_schedule 单一来源」架构，只改费率表与结构，三处共享（复盘 / 前瞻 / bayes）不分叉的设计不动；② `get_month_order_count_tiger` 未删（2026-08-12 立的接口），仅移出费率链路——若日后切回阶梯套餐，把 `build_fee_ctx` 的订单数查询恢复即可；③ `fee_per_side` 旧位置参数 `order_index_in_month` 若有外部脚本按位置传第四参，现在会被当 `shares` 误读——已 grep 确认仓库内无此类调用（review / bayes / trade_utils 均已改为关键字传参），但仓外个人脚本若有直调需自查。

### 修复（老虎 WebSocket 白名单下 access forbidden：WS 链路补挂 socks 代理，2026-08-17）

- **为什么改**：IP 白名单当日 13:05 上线后，`ws_segment.py`（老虎 PushClient 行情推送）从 13:28 起全部连不上——`code=4 access forbidden`。排查实锤根因：**PushClient 的 WebSocket 走 SDK transport 层的 `socket.create_connection` 裸 TCP 直连，从不经过 `apply_proxy` 换掉的 `web_utils.http_pool`（那只覆盖 REST）**——白名单上线前直连放行（无 IP 限制）、上线后直连出口（本地出口 IP <本地出口 IP>，非白名单成员）被网关拒。WS 从未走过代理，白名单把这个盲区暴露。三层对照实证：REST 直连被拒（报文带 `request ip ... is not in ip whitelist`）、REST 走代理通（出口 <代理出口 IP> = 白名单 代理节点）、WS 直连被拒（同 REST 直连根因）。**实盘下单不受影响**（下单走 REST、一直在代理内；实测实盘走代理查资产 net_liquidation 与 buying_power 均 USD 1.5 万 / 5.9 万量级、查询正常——精确值不写入被跟踪文件）。
- **改了什么**：
  - [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) 新增 `apply_socket_proxy(proxy_url)`：把进程内 `socket.create_connection` monkeypatch 成「先经 xpilot socks5 建链」（http 代理端口 1087 按约定 −7 = socks 1080），幂等可重复调用；socks 端口不通 / PySocks 缺失时保留直连并 stderr 警告（不炸进程）。`apply_proxy` 挂 REST 代理成功后**自动连带调用**——`load_config` 路径（全部下单 / 查询脚本）REST + WS 双链路全覆盖。
  - [`ws_segment.py`](.claude/skills/trade/scripts/ws_segment.py) 连接前按 config.json proxy 节单独调用 `apply_socket_proxy`（它直接用 TigerOpenClientConfig、不走 load_config，需自行接入）。
  - [`accounts.md`](.claude/skills/trade/accounts.md) 白名单节补「WebSocket 行情推送链路补挂代理」段（根因 / 修复 / 实测 / 运维口径全记录）。
- **验证**：ws_segment 8 秒段连接 + 订阅成功（盘外无行情推送属预期，待下个交易日盘中确认每秒推送恢复）；同进程 REST（ProxyManager）+ WS（socks）双链路均通；xpilot 代理链路本身健康（出口 IP 在白名单、generate_204 通、路由规则含 `domain:openapi.tigerfintech.com`）。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：`apply_socket_proxy` patch 的是**进程级** `socket.create_connection`——影响该进程内所有后续 `socket.create_connection` 调用（不止老虎 WS）。当前调用方（下单 / 查询 / ws_segment 脚本）进程内的其它连接只有富途 OpenD（`127.0.0.1:11111` 本地回环）——xpilot 路由规则 `[DIRECT] geoip:private` 含本地回环、且实测富途 OpenD 快照经此路径仍正常返回（ws_segment 段结束 VWAP 检查走的就是它）。若日后某脚本在同一进程内连其它**外网直连目标**，会经代理绕一圈——目标可达性不变（代理全通出站），只多一跳；如遇该场景需评估。与 2026-08-14 立「apply_proxy 失败回退直连」的防护同构：apply_socket_proxy 失败同样回退直连（警告不炸）。

### 变更（Visitors 徽章更名 Visits/day (14d)：alt 文本与 xhqing 集中统计新 label 对齐）

- **为什么改**：用户要求（2026-08-17）访问量徽章名需表达「最近半月日均访问量」口径——xhqing 集中统计侧的 badge JSON label 已从 `Visitors` 改为 `Visits/day (14d)`（`Visits/day` 是 shields.io 表达日均的惯例写法、`(14d)` 标注 14 天滚动窗口），各仓 README 的徽章 alt 文本同步对齐，避免 alt 与徽章实际显示文字脱节。
- **改了什么**：README 徽章区 `alt="Visitors"` → `alt="Visits/day (14d)"`，仅改 alt 文本，endpoint URL、数据源、徽章口径均不变（口径改动记 xhqing 仓库 CHANGELOG，本仓只改 alt）。

### 变更（盯盘空转四步修法落地：分析锚点回补 + 一行式模板 + 分析心跳 + watcher/guard 空转检测，2026-08-17）

- **为什么改**：2026-08-17 午后 auto 双会话实录空转——采样链正常但**分析链死亡**（一会话 14:02 后 0 分析文本 / 52 次纯重启采样；另一会话被用户纠正后 10 分钟复发），现有全部防护只检测「采样链断」（采样在跑一律放行），空转时全系统绿灯。根因三条：① 2026-08-07 采样源迁 ws_segment / futu_ws_segment 时，monitor_segment 段输出的强制锚点（📊 VWAP 检查 + ⏰ 10 分钟重估提醒）没跟着搬，ws 段输出只剩裸数字；②「每段三层分析」是 reference 软约束（会衰减）；③「输出精简」与「每段分析」的张力被化解为近零输出。有持仓时空转 = 漏移损 / 漏平仓 = 直接亏钱面（🔴 红灯待办，本次全量落地）。
- **改了什么**（四步修法镜像「采样链防断」三层架构）：
  - **① 锚点回补**：[`ws_segment.py`](.claude/skills/trade/scripts/ws_segment.py) / [`futu_ws_segment.py`](.claude/skills/trade/scripts/futu_ws_segment.py) 段结束输出补三样——📊 VWAP 检查（ws 系老虎推送无 VWAP 字段，段末用富途 OpenD 快照查 avg_price；OpenD 不可用降级为「强制提示 AI 自行补查」而非静默）、⏰ 每 10 分钟方向重估提醒（marker 文件去重，逻辑同 monitor_segment）、👉 空转防护提示行（先给本段判断 + 写心跳、再重启下一段）。
  - **② 最小输出模板**：[`monitoring.md`](.claude/skills/trade/references/monitoring.md) 新增「每段最小输出模板 + 分析心跳」节——一行式判断行模板（`[HH:MM] 标的 现价 | 关键位 | VWAP 位置 | 判断一句话 | 下段时间`）+「精简 ≠ 不输出」衔接条款（精简约束判断行长度、不豁免判断行本身）；三层分析节与「盯盘对话输出要精简」节各补指针；[`SKILL.md`](.claude/skills/trade/SKILL.md) 监控循环第 6 步带同指针。
  - **③ 分析心跳 + watcher 第四检查**：AI 每段分析时 echo 追加 `tmp/analysis_beat_{date}_{mode}.csv`（分析链唯一的外部可检测信号）；[`monitor_watcher.py`](.claude/hooks/monitor_watcher.py) 在「采样在跑」分支内加空转检查——心跳停更 >180 秒 → 空转系统通知；当天零心跳且采样已跑 ≥180 秒 → 「从未分析」警报（补上原「采样在跑 → 一律不报」的空转放行盲区）。
  - **④ guard 空转硬门**：[`monitor_guard.py`](.claude/hooks/monitor_guard.py) PreToolUse 对「重启密采样段」命令检查心跳——停更 >180 秒 / 零心跳且采样 ≥180 秒 → exit 2 阻断，stderr 指引先补判断 + 写心跳再重启；刚启动（无心跳且 log <3 分钟）放行不误伤。
- **验证**：两脚本编译通过；futu_ws_segment 实跑 8 秒段三锚点全输出（VWAP 实查 HK.00005 现价 163.50 / VWAP 163.33）；构造 12 分钟 log 实测两脚本 ⏰ 重估提醒触发；guard 盘中模拟六场景（无心跳+采样 10h 拦 / 心跳新鲜放 / 停更 400s 拦 / 非采样命令不拦【空转门只挂采样命令】/ && 连跑两段仍被原 A4 拦 / 刚启动放行）全符合；watcher 三场景（停更报警 / 新鲜不报 / 零心跳报警）全符合。
- **顺手修**：两 hook 的「采样已运行时长」函数初版用 ctime 口径，实测发现 rename 会重置 ctime（一次测试 rename 还原让 15 个文件 ctime 全变「刚刚」）——改为读 monitor_log 首行采样时刻（跨午夜按「首行时分 > 当前时分视作昨天」推断，同 monitor_segment 哨兵口径），语义稳定。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① guard 空转硬门是新增阻断路径——若 AI 按新规矩写心跳则永不触发；但**旧习惯只重启不分析时会被拦**（这是设计意图，阻断信息里带完整补救指引）；② watcher 空转通知复用既有冷却机制（5 分钟一条），不会风暴；③ ws 段结束新增 OpenD 快照调用（一次、段末），OpenD 掉线时降级为提示不报错、不阻断采样；④ 心跳文件在 tmp/（已 gitignore），无敏感信息入库问题。历史条目对照：2026-08-04 立「多层防护」与 2026-08-12 立「采样在跑 + state 双判据」均只覆盖采样链——本批在其上叠加分析链检测，不改既有判定，无回归。

### 变更（CHANGELOG 凭证文件名与路径脱敏：不暴露密钥文件名 / 文件路径，2026-08-17 用户立）

- **为什么改**：用户立规——CHANGELOG 不得暴露密钥文件名和文件路径。此前记录凭证防泄漏、密钥轮换等过程时，把 SDK 配置文件的具体文件名（无后缀主文件及 live / paper / .bak 变体）、家目录下的凭证目录路径、项目内账户配置文件名、deny 规则里的绝对路径明文写入 CHANGELOG（会进 git / 公开仓库）；其中 08-17 新条目里还有一处 17 位模拟账户号明文、一处本机直连出口真实 IP。文件名 + 路径虽非凭证本体，但等于把「去哪找密钥」画了地图，与「敏感信息禁止写入未被 .gitignore 忽略的文件」纪律的精神一致。
- **改了什么**：全文件 20 余处统一改抽象表述——具体文件名 → 功能描述（「SDK 默认加载的无后缀主配置文件」「项目内已 gitignore 的账户配置文件」）、家目录绝对路径 → 「本机 SDK 凭证目录」、deny 规则原文 → 「对 SDK 凭证目录整目录的 Read 拦截」（具体规则见本机 settings 文件）；顺带把 17 位账户号明文改「17 位明文」表述、本机直连出口 IP 打码。历史条目（08-02 / 08-04 / 08-12 / 08-13）同步改写，语义零丢失（为什么改 / 改了什么两要素完整保留）。
- **边界**：已提交进 git 历史的旧表述不重写历史（与 2026-08-17「git 历史模拟账户号明文不处理」同口径——模拟号无鉴权能力、私钥从未入库）；SKILL / references / settings 等运行文档中的文件名属正常业务信息（AI 要靠它们跑交易链路），不在本条脱敏范围。
- **回归风险**：无（纯文档表述脱敏，不改任何脚本 / 规范 / 配置行为）。

### 新增（盯盘空转事故实录 + 修法立项，🔴 红灯待办）

- **为什么改**：2026-08-17 午后 auto 双会话（实盘 + 模拟）实测空转——采样链正常（段循环节奏正常、log 持续更新）、**分析链死亡**（一会话 14:02 后 0 分析文本 / 52 次纯重启采样；另一会话被用户 15:27 纠正「每段给判断」后 10 分钟内复发），两会话均无上下文压缩。现有全部防护（monitor_guard 三关卡 / watcher 双判据 / stop hook）只检测「采样链断」（采样在跑一律放行），空转时全系统绿灯、唯一检测器是用户。有持仓时空转 = 漏移损 / 漏主动平仓 = 直接亏钱面。
- **改了什么**：[`TODO.md`](TODO.md) 🔴 红灯新增「盯盘空转」待办：根因 = ws 系采样脚本（ws_segment / futu_ws_segment）段输出缺 monitor_segment.py 已有的强制锚点（📊 VWAP 检查 + ⏰ 10 分钟方向重估提醒，2026-08-07 采样源迁移时未跟着搬）+「每段三层分析」是 reference 软约束 +「输出精简」与「每段分析」的张力被化解为近零输出。修法（镜像采样链防断三层架构）：① ws 脚本段输出补锚点 + 「先给本段判断再重启」提示行；② monitoring.md 定义每段最小输出模板（一行式）；③ 分析心跳文件 + watcher 第四检查（采样在跑但心跳 >3 分钟 → 空转警报，补检测盲区）；④ 可选 hook 硬门。另：/goal 命令方案（当日盈利金额 / 交易笔数）评估为**不采用**——与空转根因不对口（同为一次性软指令、衰减曲线同 15:27 纠正），且盈利 / 笔数目标与「≥2.4R + 右侧确认、宁可观望」策略直接冲突（无 setup 日形成逼单压力 = 2026-08-17 早 −1.05R 左侧入场失败模式的放大器）；若要目标感应设在过程合规（可工程化检测）而非结果指标。
- **回归风险**：无（本次仅记录事故与立项，不改任何脚本 / 规范行为）。

### 新增（方案 A 多会话并行盯盘两份实现 Plan + TODO 两条绿灯待办立项）

- **为什么改**：用户提出「按标的池不同多开会话窗口并行盯盘、提高时间利用率」，经探索评估（多会话共享下单账户的冲突点 + 单持仓互斥可达成度 + 港美解耦条件三轮论证）拍板方案 A：多会话各盯各的标的池 + **单持仓互斥**（风控语义不变、不做资金并行的方案 B），分港股 / 美股两份实现计划立项。
- **改了什么**：
  - 新增项目根 [`PLAN-parallel-monitor-HK.md`](PLAN-parallel-monitor-HK.md)：港股实现计划——公共互斥模块 `trade_mutex.py`（flock 全局锁把「查账户→下单→确认成交」临界区串行化 + 三口径开仓闸门【intent 未决 / 当日开仓链无对应平仓 / 活动开仓挂单】+ intent 日志防崩溃窗口）+ `monitor_segment.py` 每轮采样持仓数检测兜底（漏网双持仓 40 秒内告警 + 平新仓，连用户手动下单的存量风险也纳入）+ caffeinate 引用计数（off.sh 无差别 pkill 在多会话下会误杀对方防睡眠）+ resume.py 断层检测按市场 scope + 常驻历史持仓白名单（`risk.resident_positions`，闸门不走「有持仓即拒」裸口径）+ 标的池分配约定 + 风险对策表 + 五步实施验收（含首个交易日 2 会话灰度）。
  - 新增项目根 [`PLAN-parallel-monitor-US.md`](PLAN-parallel-monitor-US.md)：美股实现计划——复用同一把全局锁（不分市场：港美时段不重叠使锁竞争为零、同时拦住「港股违规过夜 + 美股开新仓」跨市场叠加）、`open_position_tiger_us.py` 接线、美股先天条件表（行情仅富途→天然无老虎 WS 互踢、watcher 美股时段不守护的存量边界）、港美解耦结论表（signal 模式 equity-log 单一累加链**不解耦**、本 Plan 只覆盖 auto）、跨午夜注意、四步实施验收。
  - [`TODO.md`](TODO.md) 绿灯新增两条待办（港股实现 / 美股实现，各带 Plan 链接与验收要点；美股以前置港股公共模块就位为依赖）。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：纯新增（两份 Plan 文档 + TODO 条目），不动任何脚本 / 规范 / 配置，无行为回归。互斥设计对既有单会话路径的影响（锁无竞争时零行为差异、闸门对空账户全绿）已在 Plan 验收步骤里显式覆盖（美股 Plan 第五节步骤 1）。

### 变更（TODO 分级随全局改四级紧急度「红 / 橙 / 黄 / 绿」）

- **为什么改**：用户 2026-08-17 两轮指示——先把「高危 / 中危 / 轻危」改为三级紧急度命名、随即定为四级终态：**红（原高危）/ 橙（原中危）/ 黄（原轻危）/ 绿（计划类新功能实现，独立一级）**。计划类与卫生类两类性质不同不再混装：文档措辞 / 脚本卫生归黄灯，新功能实现独占绿灯。
- **改了什么**：[`TODO.md`](TODO.md) 头部说明改四级分节标准；分节重排为 🔴 红灯 → 🟠 橙灯（原中危节，含三阶段标定条目改标 🟠）→ 🟢 绿灯（仅「计划实现」小节：两条方案 A 待办）→ 🟡 黄灯（原轻危的文档 / 脚本卫生条目全部迁入，条目 emoji 原标 🟢、节注声明「接触一条顺手改 🟡」）；橙灯节下「安全 / 凭证」两条长期运维备忘（白名单同步、密钥轮换周期化）随四级标准改标 🟡（运维备忘属黄灯「不影响正确性的优化」类，非计划类新功能）。`TODO-archive.md` 历史节名保留原词（归档节名是历史事实）。
- **配套全局变更**：四级分级规则落在全局 `~/.claude/CLAUDE.md`「优先级分类」条目（已随自动同步规矩镜像到 CapabilityManagerAgent `claude/CLAUDE.md`，按「同步只记权威源 CHANGELOG」规矩记在**该项目** CHANGELOG、不重复记本文件）。
- **后续同日修订（15:48）**：用户再立标注方式新规——**颜色只标分节标题、条目不标颜色 emoji**（避免两处颜色不一致）；据此把 [`TODO.md`](TODO.md) 全部 20 条活跃条目的颜色 emoji 清除（只留四个 `##` 节标题与头部说明带色）、两条改判注释里的 🟡 字样改文字表述；全局「怎么标」条目同步改写并镜像。
- **后续同日修订（15:52）**：节序修正——绿灯节（计划实现）此前误排黄灯前，按四级紧急度正确顺序重排为 🔴 红 → 🟠 橙 → 🟡 黄 → 🟢 绿。
- **回归风险**：无（纯文档分级命名与条目重排，不动代码与交易逻辑）。

### 新增（复盘结尾必含「是否暂停交易」建议 + 两级判据，2026-08-17 用户立）

- **为什么改**：2026-08-17 全量复盘（N=42）做了连败暂停规则专题分析——蒙特卡洛显示暂停规则在「策略始终有效」世界是纯减分（终值 −13%）、在「状态漂移 / 策略失效」世界把回撤从 ~28% 压到 ~15%——规则的本质是对冲失效风险。用户据此立规：以后每次复盘结尾都要给这条建议、不能只给指标不下结论。
- **改了什么**：
  - `references/review-and-evaluation.md` 新增「每次复盘结尾必含『是否暂停交易』建议」节：明确「不需要暂停也要明说 + 写清离两级线多远」；两级判据——第一级降频线（连败 3 笔、或连败 2 笔且都亏满型 |R|≥0.95 → 当日停开新仓）、第二级暂停修整线（P(g>0)@f=2% 跌破 80% 且连续 5 笔无盈利 → 暂停修整）；附依据（08-03~05 七连败第 3 笔后继续硬扛多亏 ~2.4R、连败 2 笔后下一笔实测胜率 29%）与边界（盯盘 session 不因连败自主停盘、建议是给用户的决策输入；参数随样本累积可修订）。
  - 「复盘时逐项报告」清单加入该必含项；`SKILL.md` 复盘节的 reference 指针补此条目。
- **⚠️ 回归风险提醒**：无行为回归——纯复盘输出规范的新增项，不改任何交易执行逻辑；两级判据目前只作为复盘建议依据、未写入 `config.json` / 硬性护栏（若将来要做成硬护栏另议）。

### 变更（git 历史模拟账户号明文：用户拍板不处理、TODO 归档，2026-08-17）

- **为什么改**：用户确认「git 历史里的模拟账户号对实盘无威胁、完全不处理也不增加实盘风险」，TODO 🟡 条目按「不处理」闭环归档。
- **改了什么**：`TODO.md` 安全 / 凭证节删除该活跃条目，`TODO-archive.md` 同节新增 `✅**已更新**` 归档条目——完整保留不处理的四条理由（模拟号≠实盘号无推导关系 / 鉴权三件套从未入库、账户号无鉴权能力 / App 登录与账户号无关 / IP 白名单已上线兜底）与边界说明（「无威胁」前提 = 私钥从未入库 + 白名单在位；将来撤白名单且私钥泄漏的叠加场景需重评）。
- **回归风险**：无（纯待办状态变更，不动代码与配置）。

### 变更（IP 白名单上线：apply_scope 改 all 模拟盘金丝雀生效，2026-08-17）

- **为什么改**：用户已在老虎开发者信息页把 代理订阅全部节点入口 IP 配入白名单——平台层最后一步就位，SDK 侧配套把模拟盘也切到代理（金丝雀设计落地：模拟盘断 = 提前暴露代理 / 白名单 / 节点问题，实盘不踩同一坑）。
- **改了什么**：
  - `config.json` proxy 节 `apply_scope` `live_only` → **`all`**（港美股全部走代理），注释同步写明金丝雀设计与白名单配套关系。
  - 顺手修一个泄漏点：`trade_utils_tiger.py` `apply_proxy` 的 stderr 提示原打印**完整账户号**（17 位明文，会随脚本输出进 AI 上下文，与 2026-08-17 打码纪律冲突）——改用 `mask_account()` 打码输出（首两位 + **** + 末两位形式）。
  - 验证四连：① 模拟盘走代理行情查询通（00700 open=444.8）；② 模拟盘走代理资产查询通；③ 实盘走代理资产查询通；④ **负向验证**——构造直连（绕过 apply_proxy）打 API，被拒「access forbidden: request ip 14.***.***.127（本机直连出口，已打码）is not in ip whitelist」，证明白名单真实拦截生效（非摆设）。
  - `TODO.md` 平台层 🟡 条目归档闭环（长期运维拆出独立 🟢 条目：节点 IP 变动后同步白名单）；`accounts.md`「老虎 IP 白名单 + 代理节点」节状态从「配置中」改「上线」、补生效验证记录。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① `apply_scope: all` 与 2026-08-14 立 live_only 的初衷（「模拟盘无监管限制、直连更快」）相悖——这是用户 2026-08-17 主动拍板的有意变更（安全 > 速度），非回归；代价是模拟盘链路新增代理单点（代理挂 = 全断，正是金丝雀要暴露的）；② apply_proxy 提示打码后，此前依赖该行输出人工核对完整账户号的场景改看账户配置文件本体（用户自己看，文件名见本机 skill 文档），不影响脚本逻辑（仅显示文本）。

### 变更（IP 白名单方案定稿：模拟盘金丝雀 + 代理订阅全部节点 IP 全量加白，2026-08-17）

- **为什么改**：用户拍板凭证安全平台层方向——模拟盘不重要可折腾、跟着实盘一起走代理（金丝雀设计：模拟盘断 = 提前暴露代理 / 白名单 / 节点问题，实盘不踩同一坑），并配老虎开发者级 IP 白名单；为此需核实 代理节点入口域名 / IP 的真实情况（用户问「<节点一>.<代理节点入口域名体系> 是不是 代理订阅商的固定入口域名」）。
- **改了什么**：
  - 实测核实（xpilot 订阅 6 节点全景）：`<代理节点入口域名体系>` 是 代理订阅商的**节点入口域名体系、不是固定单入口**——<全部订阅节点> 每节点一个独立子域名（vless 443 / ss 17278 两种入口），DNS 解析 IP 与订阅 IP 一致；当前节点 <当前节点> 出口 = 入口 = <节点出口 IP>。<节点一> 的 ss 入口实测**不通**（切换后 generate_204 失败，已切回 <当前节点> 并确认恢复连通）——白名单加白该 IP 无副作用，但节点本身当前不可用。
  - 生成白名单候选串（6 IP，`;` 分隔）写入 `accounts.md`「老虎 IP 白名单 + 代理节点」节，含运维口径（代理订阅刷新 / 节点 IP 变动后须同步改老虎白名单，白名单只认 IP 不认域名；模拟盘金丝雀先断报警）。
  - `TODO.md` 安全 / 凭证节平台层条目更新为已定方案 + 四步待办（用户后台填白名单 / `apply_scope` 改 all / 节点变动同步流程 / 链路验证）。
- **⚠️ 回归风险提醒**：暂无行为变更（`apply_scope` 仍为 live_only，待用户白名单配好后一起改）；唯一环境扰动是测试中短暂切到 <节点一>（ss）又切回，当前节点已确认恢复 <当前节点> 连通 OK。

### 新增（右侧入场总则 + 「企稳」可操作定义 + 暴跌次日边界，2026-08-17 用户立）

- **为什么改**：2026-08-17 MINIMAX 做多 −1.05R 复盘，用户指出三个问题——开仓位置不对（浅回踩中继位非支撑位）、企稳形态不明确、不是右侧入场。逐点审计 skill 后确认：① 位置规则在场（2026-08-04 MU「下轨买上轨卖、浅回踩不算支撑」）但执行没对上；② **「企稳」全文出现几十次、从未定义**——几分钟低点抬升冒充企稳拦不住；③ **「右侧/左侧」概念全文零覆盖**——右侧思想只有碎片（已发生事实 / 不赌突破 / 破颈线确认）没有总则；④ 「暴跌低点须等双底/头肩底」只写了暴跌当日、没写暴跌次日是否适用。
- **改了什么**：
  - `references/trading-strategy.md` 新增「右侧入场总则」节（2026-08-17 用户立）：开仓永远是右侧动作、确认信号之后才进；含左侧入场高发伪装自检清单（几分钟微小低点抬升 / 跌速放缓 / 还没破支撑 ≠ 确认）+ 自检口诀「我的确认信号是什么？它已经发生了吗」+ MINIMAX 反面教训。
  - 同节新增「企稳的可操作定义」：四条缺一不可——真支撑（非高位浅回踩）、≥2 次测试不破、低点结构维持 ≥10 分钟、确认事件已发生（站稳回踩起点上方 / 破小级别颈线 / 放量承接）；确认事件前只能算「疑似企稳、观察中」。含与「V 反确认段积极做多」的衔接判据（板块共振确认是前提、单股 V 反不豁免——正是本次 MINIMAX 的场景）。
  - 「做多合格入场只有两种」的暴跌低点条补适用范围：**含暴跌当日与暴跌次日**——次日 V 反 / 反弹中的回踩同样须先过 V 反确认判据（板块共振）或等双底 / 头肩底破颈线，都没过就开 = 左侧接刀。
  - `SKILL.md` 开仓前自查清单 ④ 补右侧入场总则指针 + MINIMAX 教训一句。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① 「企稳 ≥10 分钟维持 + ≥2 次测试」是加严（此前无定义、分钟级即开）——会拦掉部分快速 V 反入场，这正是意图；「V 反确认段积极做多」（2026-08-03 立）原判定不变、其浅回踩豁免以板块共振确认为前提（原文如此、本次只把前提写显），多头趋势日 VWAP 上方企稳（③）不受影响（其确认事件 = 站上 VWAP 且不破的事实）；② 右侧总则与「趋势日敢捕捉」（防把趋势日误判成震荡）不冲突——趋势日确认后照样积极开仓，只是每次入场仍须答出「已发生的确认事件是什么」。

### 变更（IP 白名单能力核实结论：仅开发者级、无账户粒度区分，2026-08-17）

- **为什么改**：用户在老虎后台实测反馈「IP 白名单部分好像没有区分账户」，与此前 TODO 里「按账户粒度隔离待核实」的疑问对上了，需要落定结论指导后续凭证安全决策。
- **改了什么**：`TODO.md` 安全 / 凭证节的平台层硬隔离条目更新——官方文档（准备工作页「额外配置」表格）+ 用户后台确认：IP 白名单配置在**开发者账号级**（`;` 分隔、非必填），不区分实盘 / 模拟资金账户；结合「一个开发者只有一套密钥对」（同日确认），「仅模拟可调、实盘锁 IP」的账户粒度隔离**不存在**。剩余决策收敛为「IP 白名单配不配」的单选题，权衡与倾向（模拟直连动态 IP 一配全断，默认不配、依赖 1+2 层防护 + 定期轮换）已写进条目，待用户拍板。
- **回归风险**：无（纯待办条目信息更新，不改任何行为）。

### 新增（开仓的剩余时间边界：距停盯 >5 分钟仍可开仓，2026-08-17 用户立）

- **为什么改**：2026-08-17 上午 11:42（距午休 18 分钟）AI 以「临近午休不开新仓」为由提前停手等下午，用户当场纠正——临近午休或收盘时间 ≤5 分钟才绝对不开仓，只要 >5 分钟仍有开仓机会，真正的放量拉升或下跌 5 分钟内就可能走出高赔率行情，提前收手会把尾盘急行情全让掉。
- **改了什么**：`references/trading-strategy.md`「临近停止盯盘的分情况策略」节新增小节「开仓的剩余时间边界」——只有距午休/收盘等停止盯盘时点 ≤5 分钟才绝对不开仓；>5 分钟开仓照常过全流程（自查清单 + 净赔率实算 ≥2.4 + 🟢 高置信），赔率按剩余时间内大概率可达的止盈位实算，算不出 ≥2.4 判据自然拦住、算得出就开，「快收盘了」这个时间直觉不单独一票否决。总结句同步更新。

### 变更（API 密钥轮换 + SDK 默认配置文件恢复，2026-08-17）

- **为什么改**：2026-08-17 凭证防泄漏调查期间，tiger_id 全值与私钥头 25 字符片段进入了对话上下文（会随上下文进入模型请求）——虽不构成可利用凭证组合，按零容忍口径轮换密钥对作废旧密钥。轮换时用户误删 SDK 默认加载的无后缀主配置文件（properties，具体文件名见本机 skill 文档），需恢复；顺带发现实盘账户已入金（SKILL 旧信息过期）。
- **改了什么**：
  - 用户在老虎开发者信息页点「重新生成」完成密钥轮换，下载 live / paper 两份新配置文件放入本机 SDK 凭证目录（实测确认：两份文件私钥 sha256 完全相同、仅 `account` 字段不同——一个开发者只有一套密钥，两文件是「默认账户不同」的便利而非两套密钥）；三文件权限统一 600。
  - 恢复 SDK 默认加载文件：把 paper 版复制为无后缀主配置文件（默认账户=模拟 17 位号，与「默认模拟、显式切实盘」设计一致）；live / paper 两份带后缀文件留作备查。
  - 删除含旧私钥的 `.bak` 备份副本文件（旧密钥已作废，副本成纯泄漏面；TODO 🟢 条目闭环）。
  - 验证（打码口径）：paper / live 两个新配置分别实跑 `get_assets` 鉴权通过；默认链路 `load_config()`（无参、走无后缀主文件）鉴权通过；preflight.py 全绿。
  - `SKILL.md`「auto 模式的账户选择」：实盘账户入金状态从「2026-08-13 未入金（net_liquidation=0）须先提醒余额为 0」更新为「2026-08-17 实测已入金（量级与精确值见本机实查）」，保留「切实盘前实跑 get_assets 核实余额」的活规则；`accounts.md`「老虎 SDK」节补密钥轮换机制说明（一套密钥、两文件仅 account 不同、轮换步骤与 3~6 个月频率建议）。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① 恢复的默认主文件以 paper 版为准——account=17 位模拟号，与轮换前旧主文件一致（旧 .bak 里曾是 7 位实盘号，未沿用），默认链路行为不变；② 「实盘余额为 0 须先提醒」废止后，切实盘前的余额核实要求保留（余额数字变了、核实动作没变），不会出现余额不足仍盲目下单的回归。

### 新增（实盘凭证防泄漏第 1+2 层防护，2026-08-17 立）

- **为什么改**：调查确认实盘与模拟盘共用同一套 API 凭证（tiger_id + RSA 私钥 + license 存本机 SDK 凭证目录的 properties 文件，实盘 7 位账户号存项目内已 gitignore 的账户配置文件），此前 AI 可用 Read / Bash（cat / grep / python open）把完整账户号甚至私钥读进对话上下文、随上下文进入模型请求，且切实盘确认流程本身就要求 AI 读完整实盘号展示给用户——凭证暴露面无任何拦截。用户要求实施配置层 + 代码/流程层防护（平台层硬隔离另行核实老虎后台能力，已记 TODO）。
- **改了什么**：
  - 项目内账户配置文件权限 644 → **600**（同机其它用户不可读）；SDK 凭证目录下的 `.bak` 旧副本确认为仅 account 字段不同的冗余文件（含私钥、600 权限），待用户确认后删除（TODO 🟢）。
  - 全局 `~/.claude/settings.json` permissions.deny 新增对 SDK 凭证目录整目录的 Read 拦截（含 tmp 逃逸路径）；项目 `.claude/settings.json` deny 同步新增对 skill 账户配置文件的 Read 拦截（相对 + 绝对两式，具体规则见本机 settings 文件）。
  - 新增 `.claude/hooks/secret_guard.py`（PreToolUse Bash 守卫）并注册进项目 settings.json：命令同时命中「凭证路径特征（SDK 凭证目录 / 账户配置文件 / 私钥字段名等）+ 读取动作特征（cat / grep / jq / base64 / python open 等）」即 exit 2 阻断；放行正常交易链路（运行 trade 自家脚本、chmod / ls / stat / diff 元数据操作、纯 cp 写操作），并拦「cp 到普通路径再 cat」的先搬走再读间接路径。单元测试 22 例全过（`tmp/test_secret_guard.py`，用例写文件里避免测试命令自身被 hook 拦）。局限（诚实）：正则特征覆盖常见路径而非全部，AI 理论上可绕过——是抬高成本 + 暴露行为，非 100% 银弹；硬隔离待平台层（见 TODO 🟡）。
  - `trade_utils_tiger.py` 新增 `mask_account()` / `--masked-live-account` 子命令：输出实盘 / 模拟账户打码号（`67****91` 形式），供切实盘确认流程用——用户自己知道完整号、看打码号足以确认对错，完整号不再进入 AI 上下文。
  - `SKILL.md`「auto 模式的账户选择」与 `references/auto-mode.md`「自动交易模式总则」同步：切实盘确认话术改用打码号（原 `<account_live>` 完整号口径废止），并写明「AI 上下文中账户号一律打码、禁止直接读账户配置文件取完整号」的纪律与既有拦截机制。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：SKILL 原文「切实盘前确认话术用 <account_live>」被改为打码口径——这是有意变更（凭证防泄漏）而非回归；2026-08-12「auto 账户选择双闸」（用户明确说切实盘 + AI 额外确认）两道闸完整保留，只是确认话术里的账户号从完整号变打码号。

### 变更（🟡 中危待办 27 条处理：26 条全清 + 1 条计数更新留活跃——下单脚本 9 / 监控链路 10 / 文档口径 7）

- **为什么改**：2026-08-15 全项目逻辑审计落入 TODO.md 的 27 条 🟡 中危待办（2026-08-17 用户指示全部处理完）——每条都是「边界情况出错 / 防护缺口 / 口径不一致可能演化为实际损失」级别：下单脚本的部分成交误判全成、显式传量绕过风控上限、超时盲重试真实双倍持仓路径、平仓残留兄弟止损单日后反向开仓；监控链路的断层检测跨午夜失明、守卫时段硬编码夏令时（北京周六凌晨 4 小时盲窗）、通知风暴；文档口径的双费率并存、ETF 白名单缺失多收印花税错杀交易。
- **改了什么**（26 条与 TODO-archive「🟡 中危批量处理」节一一对应）：
  - **下单脚本 9 条**：① `check_order_filled_tiger` 严格匹配 Filled（PartiallyFilled 不再当全额成交），新增 `get_order_filled_qty_tiger` 读实际成交量、开仓部分成交超时即停不叠仓按实际量上报；② 港美开仓 `risk_fraction` / `f_max` 改从 config.json 读 + 显式传量路径过 f_max / max_leverage / 整手校验（超限拒绝下单）；③ 六个 submit 函数超时模糊失败不再盲重试（注明须先查订单）+ 新增 `has_active_open_order_tiger/_us` 重复下单防抖 + 降档循环模糊失败即停；④ 开仓止损价 tick 取整（港美，取整改动输出 `stop_loss_adjusted`）；⑤ close 港美平仓成交后 `_cancel_residual_stops` 复查撤全部残留止损单；⑥ move_stop 港美 fallback 分步报告（新单成 + 撤旧败 = ok:true + warning，不再整体误报致多重止损）；⑦ 港股三处止损单判定补 TRAIL / LOSS 对齐美股口径；⑧ 降档循环 `filled = False` 预初始化；⑨ 轮询异常捕获返回 `poll_error` JSON、close 显式传参加持仓复核（防凭空反向开仓）、`get_open_position_tiger` 只收港股持仓。
  - **监控链路 10 条**：⑩ resume 美股 log 按 −12h 美东交易日 glob + ring-log 末行校验当日（周一不再拿周五响铃报假断层）；⑪ monitor_summary 买卖比行补 r_last 守卫；⑫ monitor_guard 美股时段改 zoneinfo 美东判定（DST 自动、周六凌晨盲窗补上）+ STALE 分层（120s 在跑 / 300s 断）；⑬ 哨兵加港股午休感知（13:00 重启首段不再必报断层）；⑭ stop_prices 缓存 update 改整体替换；⑮ watcher 通知加 5 分钟冷却 + 采样掩蔽低频提示；⑯ bayes_evolution direction 解析复用 review 十别名口径；⑰ guard A4 扩三脚本 + 按串联操作符切分子命令计数（py_compile 等工具引用不再误拦）；⑱ ws_segment 补 --mode 解析 + 美股日期 −12h + 破位去重 + 按秒聚合；⑲ 段统计 ratio/vr 空列表跳过（N/A）不再除零连坐。
  - **文档与口径 7 条**：⑳ risk-management.md 权益更新节补模式分叉声明（auto 账户 API / signal equity-log + 只读查询例外）；㉑ `_HK_ETF_WHITELIST` 改 import classify 的 30 个官方 ETF（02800 盈富不再多收印花税）；㉒ 平台费查询失败改按最高档 30 计（真保守，与提示文案方向一致）+ docstring「实盘账户」改「当前 config 账户」；㉓ auto-mode.md 新增「费用与成交价的唯一权威口径」块（fee_schedule 为唯一权威、动作记录抄脚本输出禁止手算、复盘以 review.py 口径为准）+ 08-13 HKT 开仓条目 max_loss 标签改「预期（参考价口径）」、赔率表述改如实（净 2.12 < 2.4 未过闸）；㉔ 08-11 MU 两笔记录实算修正（成交价统一 855.33、分母统一 14,865、第 2 笔止损距 4.255→4.25、equity 差 +815 标注）；㉕ 购买力校验落地——`get_buying_power_tiger`（buying_power × long_initial_margin ÷ 参考价，实测 00100 保证金率 0.75）+ 港美开仓超限主动降档（`capped_by_buying_power`）+ auto-mode.md 补条目 + CHANGELOG 8-06 旧推断标注 8-12 实测修正；㉖ 三阶段标定实数计数（门槛后 15 笔 < 30，不重标定、条目更新留活跃）。
  - **顺手修复（审计外新发现）**：`trade_utils_tiger_us.py` 的 `T._fee_per_side` 引用存量破损（该函数已随 2026-08-12 真实费率改造删除、美股库 import 即崩，py_compile 查不出）——删除死别名恢复 import 链。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① `check_order_filled_tiger` 严格匹配后，PartiallyFilled 订单超时返回 filled=False——调用方（开仓降档循环）已同步加部分成交分支（停循环按实际量上报），close 侧超时走 fallback 持仓复查（2026-08-07 事故修复原有）、不受影响；② 显式传量风控校验只拦超限（f_max / max_leverage / 整手），正常仓位无感；③ 超时不盲重试是行为收紧——确定未到达的异常（参数错、网络未建立）照旧重试 3 次，只有超时类直接抛出；④ ws_segment 破位去重后「某段破位后持续高于关键位」不再每段重报（首次突破报一次），告警疲劳消除但需 AI 段末自行看段高段低；⑤ ETF 白名单扩大后 02800 等 29 个 ETF 的预估费率下降（印花税 0.1% 不再计入）——前瞻赔率略升，属修正而非回归；⑥ 购买力预检用 USD 口径 buying_power 除以 HKD 价格，方向保守（算出的上限偏小不会放大）。
- **验证**：19 个改动脚本 `py_compile` 全过；mock 冒烟测试（PartiallyFilled 不判全成 / 超时只试 1 次 / 轮询异常返回 poll_error / 风控超限拒 / tick 取整 485.5→485.4）；guard 时区断言（北京周六凌晨判盘中、周一凌晨判休市）；A4 计数断言（&& 连跑计 2 拦、py_compile 计 0 放行）；ETF 白名单实测 30 个；购买力实测调用（bp 4,037,135 × 0.75 ÷ 380 = 7,968 股）；fee-ctx 失败路径实测 idx=1/2 最高档；08-11 记录全部数字实算核对后修正。

### 变更（signal 模式权益重设：总资产改为与用户实际账户对齐）

- **为什么改**：2026-08-17 用户立——signal 模式总资产改为与用户实际账户对齐的新值（equity-log 累加口径），单笔预算 B 同步更新。
- **改了什么**：`signals/equity-log.csv` 追加一行权益重设记录（数值见本机 equity-log；2026-08-20 起权益链精确值只留 equity-log、被跟踪文件不写）。已用 `load_equity(mode='signal')` 实测验证：末行 equity_after、来源 = equity-log 末行、B 同步生效。

### 变更（Visitors 徽章 alt 文本首字母大写：README 访问量徽章命名统一）

- **为什么改**：用户指令（2026-08-16）「Visitors 徽章全局统一，首字母大写」——配合全局 `~/.claude/CLAUDE.md`「徽章英文首字母必须大写」新规，集中统计上线时挂的访问量徽章 `alt="visitors"` 为小写存量，与 badge JSON label（`Visits/day`）及大写规范不一致，本次一次收口。
- **改了什么**：README（EN/CN）徽章区 visitors 徽章 `alt="visitors"` → `alt="Visitors"`，仅改 alt 显示文本，endpoint URL 与数据源不变。

### 变更（README 徽章英文首字母大写：license / markets / mode / visitors 全部改首字母大写）

- **为什么改**：用户立规（2026-08-16，写入全局 `~/.claude/CLAUDE.md`「新建 Agent 项目的脚手架与开源约定」节）——README 徽章上的英文小写首字母观感不一致、显得随意，首字母大写是英文标识词的标准书写规范。本仓库存量徽章存在多处小写 label，按「接触一处改一处」原则本次顺手全量修正。
- **改了什么**：README.md / README_cn.md 各 4 处徽章 URL——`license-MIT` → `License-MIT`、`markets-HK%20%2F%20US%20%2F%20A--Share` → `Markets-…`、`mode-signal` → `Mode-Signal`、`mode-auto` → `Mode-Auto`；另有 `alt="visitors"` → `alt="Visitors"`（endpoint 徽章 alt 文本同规）。徽章 URL 的 label 段变更是展示文字变化，endpoint 指向与数据源不变。
- **回归检查**：仅改徽章显示文字，不触及其它内容。


### 变更（开仓判据句清除 ×0.5 负面提及：负面提及同样会把 0.5 种进上下文诱发两步误算）

- **为什么改**：2026-08-14 把开仓判据统一为「初始净预期赔率 ≥ 2.4 单一门槛」时，判据句里保留了「不做 ×0.5 中间动作」这类**负面提及**。用户指出（2026-08-16）：负面提及照样把 0.5 这个数字放进判据上下文——AI 盯盘时读到「×0.5」，反而可能被诱发去做两步动作（打折再比），与「单一门槛」修复的初衷背道而驰。正面判据里只该出现「净赔率与 2.4 直接比」，0.5 及其事故史应全部退到标定背景节（`trading-strategy.md`「预估赔率系统性打折」，2026-08-14 改单一门槛时的指定归宿）。
- **改了什么**（6 处，全项目 grep 复核 ×0.5 只剩打折节内标定背景）：
  - [`SKILL.md`](.claude/skills/trade/SKILL.md) 2 处：操作原则「质量优先」、开仓前自查清单「EV 判据」——「不做『先 ×0.5 折扣再比』的中间动作」及其内嵌的两条事故细节（≥4.8 / ×0.5=1.2 算式）全部移除，改为「单一门槛、直接比较一步判完」+ 指向 trading-strategy.md 打折节；EV 判据保留与 0.5 无关的「比的是净赔率、不是毛赔率」警示（毛净混用是 08-13 事故的另一半根因、与折扣系数正交）。
  - [`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 1 处：开仓动作定义「⑧ 初始预期赔率（净赔率 ≥2.4，单一门槛直接比、不做 ×0.5 中间动作）」→ 删负面提及。
  - [`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md) 3 处：① 开仓基准注——删 ×0.5 事故细节，指向打折节；② 打折节「规则」段——删「不做 ×0.5 折扣再比 1.2」负面提及、改纯正面陈述（标定背景保留在节内）；③ 「赔率三阶段」初始赔率条——删「不做 ×0.5 中间动作」。另把原在开仓基准的两步事故细节（两类事故的完整经过与算式）**移入打折节新增的「两步事故存档」段**（教训不丢、位置归位），并附教训泛化：判据里凡「先对赔率做变换再比门槛」的步骤（打折 / 翻倍 / 换毛净口径）都是诱发口径算错的中间动作；震荡市应对边界里一处「折扣后赔率仍厚」改为「初始净预期赔率足够厚」。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：本次只删判据句里的 0.5 字样与挪动事故存档位置，门槛数值 2.4、判据逻辑（单一门槛、净口径）、费率、止损止盈定法零改动，不触及 2026-08-14「单一门槛」修复与 2026-08-16「EV / 赔率概念脱钩」修复（三者正交）。事故教训未删除、只是归位到打折节，防回归的教育作用保留；判据位置从此只见「净赔率 vs 2.4」一个动作，负面提及诱发两步误算的通道被拆除。

### 变更（术语修正：EV 与赔率两个概念脱钩——「EV ≥ 2.4」改为「初始净预期赔率 ≥ 2.4」）

- **为什么改**：用户指出 SKILL.md 开盘观察期条款里「形态 + EV ≥ 2.4」表述不严谨——EV（expected value，数学期望）= 胜率×赔率 − 败率×1，是**平均值**概念；2.4 是**初始净预期赔率**的门槛（赔率是单笔盈亏比，不是期望）。把 2.4 绑在 EV 上属于概念混用：按门槛边界实算（胜率恰 50%、净赔率恰 2.4），EV = 0.5×2.4 − 0.5×1 = **0.7**，与 2.4 相去甚远；若 AI 盯盘时把「EV ≥ 2.4」按字面理解成「期望 ≥ 2.4R」，会误推「胜率 60% 时赔率只需 ≥ 4.67」之类的错判据，正是 2026-08-14 两类门槛事故的同源风险（判据口径不稳）。
- **改了什么**：全项目 grep 排查后修 3 处现行规则（历史归档 TODO-archive.md 的「EV ≥ 2.4」是 2026-08-16 门槛清理条目的过程记录、按约定保留）：① [`SKILL.md`](.claude/skills/trade/SKILL.md) 观察期条款「形态 + EV ≥ 2.4」→「形态 + 初始净预期赔率 ≥ 2.4」、对应「自查清单过（VWAP / 形态 / EV）」→「（VWAP / 形态 / 赔率）」；② [`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md) 中芯教训段同款「自查清单过（VWAP / 形态 / EV）」→「（VWAP / 形态 / 赔率）」；③ [`references/signal-mode.md`](.claude/skills/trade/references/signal-mode.md) 开仓信号表格「依据」栏「顺势 + 资金流 + EV 一句话」→「顺势 + 资金流 + 赔率一句话」。
- **复核确认无需改**：其余 EV 用法均正确——trading-strategy.md「核心思想 EV ≥ 0」、07709 教训 EV=−1 / +0.7 / +4.36 三处实算全对（属正确的期望值用法）；「EV 判据」「EV 之上的第二道闸」是判据命名、内容已正确写「初始净预期赔率 ≥ 2.4」；review-and-evaluation.md 的 EV 全部是统计期望（P(EV>0)、EV 95% CI 等），语义正确。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：本次纯术语替换、门槛数值 2.4 与判据逻辑零改动，不触及 2026-08-14「单一门槛」修复（那条修的是两步打折动作，本次修的是 EV / 赔率概念混用，两者正交）；替换后口径与 SKILL.md EV 判据自查项、auto-mode / signal-mode 表格的「净赔率 ≥2.4」完全一致。

### 变更（🔴 高危待办 11 条全清：门槛残留 / 降频残留 / 5 个下单脚本缺陷 / 监控数据错位与高估 / 08-13 记录修正 + log_action 根治）

- **为什么改**：2026-08-15 全项目逻辑审计落入 TODO.md 的 11 条 🔴 高危待办（2026-08-16 用户指示全部处理完）——每条都是「不修会亏钱 / 下错单 / 统计结论错 / 核心功能不可用」级别：规范文档里新旧门槛并存（AI 盯盘读到哪条按哪条执行、门槛差一倍）、降频残留与禁止降频硬护栏直接矛盾、下单脚本五个可致下错单的缺陷、监控数据列错位与额增速高估 10 倍污染统计输入、08-13 交易记录时间倒流与 equity-log 漏记扭曲复盘基准。
- **改了什么**（11 条与 TODO-archive「🔴 高危批量处理」节一一对应）：
  - **规范文档 2 条**：① 开仓门槛 1.2 残留清理——9 处现行规则的旧「≥1.2」全改 2.4（`SKILL.md` 观察期条款、`auto-mode.md` 开仓定义与表格、`signal-mode.md` 开仓表格、`trading-strategy.md` 五处、`review-and-evaluation.md` 预估净胜赔率公式；历史教训 / 标定背景叙述里的 1.2 按约定保留）；② 删除降频决策树残留——`SKILL.md` / `auto-mode.md`「无机会时自主决策树」原 ②「降频」改为「换标的不降频（保持 40 秒密采样、持续刷 hot_list，绝无降频死等合法出口）」，与硬性护栏、`monitoring.md` 不再矛盾。
  - **下单脚本 5 条**：③ [`close_position_tiger.py`](.claude/skills/trade/scripts/close_position_tiger.py) `--account live` 空格形式现在真正赋值（原只跳过值、切实盘平仓会静默操作默认模拟账户）；④ [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `cancel_all_stop_orders_tiger` 的 status / order_type 判定改 `.value` 优先（实测 SDK 枚举 `str()` 得 `'OrderType.STP'` 形态、原裸字符串比较永远 False——独立 STP 单撤不掉、终结状态过滤也失效）；⑤ `get_open_position_tiger` 返回目标持仓自己的 side（原用收集循环残留值，多空混持时一键平仓方向反做加倍）；⑥ [`open_position_tiger_us.py`](.claude/skills/trade/scripts/open_position_tiger_us.py) 补 `quantity > 0` 守卫（美股「数量传 0 = 自动算仓位」恢复可用、原必崩除零）；⑦ 止损价方向硬校验三件套——`_net_odds` 止损距 ≤0 从返回 inf 改为 raise（方向错以最诱人形态放行 → 硬报错）、`check_price_in_range` 捕获后强制拒绝、港美开仓脚本加「做多 stop < entry / 做空 stop > entry」校验、港美 move_stop 加「新止损价未穿越现价」校验（穿越即拒绝并提示用 close_position 主动平仓）。
  - **监控数据 2 条**：⑧ [`futu_ws_segment.py`](.claude/skills/trade/scripts/futu_ws_segment.py) 写 log 补齐第 4 个空位（11 字段对齐 11 列表头；原少一个逗号致 high/low 整体错位一列）+ 已污染数据（08-07/08-10/08-11/08-14 共 18 个文件 97,835 行，比审计发现的 08-14 更多）备份到 `tmp/log_fix_backup_20260816/` 后按错位映射无损重排、全量复验；⑨ [`monitor_summary.py`](.claude/skills/trade/scripts/monitor_summary.py) 额增速改按 turnover 序列自身时间戳推算采样间隔（原假设每点 1 秒、10 秒采样高估 10 倍；混合数据源文件按 turnover 自身间隔而非全文件平均）。
  - **交易记录 2 条**：⑩ 08-13 HKT 两条动作时间戳按真实时间改正（原均为 08-12 13:45:43 倒流一天）、ET 文件港股双写副本加迁移说明（复盘只认 HKT 副本）；equity-log 补记 08-05 NVDA 与 08-06 MU 两笔漏记（金额见本机 equity-log；×7.80 汇率与既有口径一致）、下游 equity_after 连锁更新、注资行 P 值相应调整保链条连续（定值结果不变）、全链连续性验证 0 问题，胜宏 R 改实算 3.42（原 3.71 无出处）；**根治**：[`log_action.sh`](.claude/skills/trade/scripts/log_action.sh) market / mode 参数改必填（缺参报错退出——默认值已两次诱发事故）；⑪ 08-13 信号预估 max_loss 改实算 1,705（原 2,325 用无来源止损距 1.5）、移损保本表述改 +0.57R（原「+0.4R」把每股利润当 R 值）。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：① `_net_odds` 从返回 inf 改 raise——`check_price_in_range` 已同步捕获（odds_at_current 场景现价漂过止损侧时返回 −inf + 拒绝），直接调用 `_net_odds` 的只有 `calc_entry_range`（entry=参考价、方向错时开仓脚本新加的硬校验先拦截）与 `check_price_in_range`（已捕获），无其它调用方（已 grep 核实）；② move_stop 新增「先取报价再校验」——多一次行情查询、失败时报错退出而非盲跑（平仓语义走 close_position 不受影响）；③ equity-log 中段连锁更新只影响 08-05~08-07 中间几行与注资行 P 值（08-10 / 08-12 两次重设后全部抹平、末行 116,180.02 不变）；④ log_action.sh 参数必填是**破坏性变更**——旧的单参 / 零参调用会报错，属有意为之（两次事故都是默认值诱发），auto-mode.md / monitoring.md 的调用示例无带参写法、无需回写；⑤ futu_ws 历史数据重排为无损映射（原 vr 列位置 = 真 high、原 high 列位置 = 真 low，逐位重排不引入新数据），备份可回滚。
- **验证**：全部 9 个改动脚本 `py_compile` 通过；close 的 `--account` 三形式 + 边界、枚举判定六组合、move_stop 方向校验三分支（桩测试）、log_action 缺参 / 非法值 / 正常路径（沙箱）、equity-log 全链连续性、monitor log 97,835 行重排后全量 11 字段 + 抽样语义核对——均实测通过。

### 变更（全项目逻辑审计发现落入 TODO.md + 待办优先级分级制首次落地）

- **为什么改**：2026-08-15 对整个项目做了一次全面逻辑审计（核心规范文档逐字审读 + 四路子代理并行审计下单 / 监控 / 复盘脚本与历史交易数据 + 统计公式 Python 实算复核，高危发现全部亲自复现验证），发现 10 个高危问题（开仓门槛 1.2/2.4 双值并存、降频决策树与禁止降频硬护栏矛盾、平仓脚本 `--account live` 空格形式静默失效、撤止损函数枚举 str 比较失效、持仓 side 张冠李戴、美股自动算仓位除零、futu_ws_segment log 列错位（真实数据已污染 0814 log）、monitor_summary 额增速高估 10 倍、08-13 动作时间倒流 + 同笔双写 + equity-log 漏记、方向错止损以 inf 赔率放行）、约 20 个中危、约 15 个轻危——审计发现的问题需要统一收进 TODO 跟踪（禁止散落）；且用户同日立新规「待办必须做优先级分类（至少高危 / 中危 / 轻危三类）」，待办不分轻重混排会先做轻的漏了重的。
- **改了什么**：[`TODO.md`](TODO.md) 按新规全量重构——按 🔴 高危（11 条：不修会亏钱 / 下错单 / 统计结论错 / 核心功能不可用）→ 🟡 中危（26 条：边界出错 / 防护缺口 / 口径不一致）→ 🟢 轻危（15 条：文档措辞 / 卫生问题）三级分节（合计 52 条），每条带行号定位、验算依据与改法；既有 2 条存量待办（三阶段赔率标定、购买力校验）并入对应级别并标注并入时间；文件头部说明同步写优先级分类规矩。**配套全局规则**：全局 `~/.claude/CLAUDE.md`「待办（TODO）管理」节新增「优先级分类（2026-08-16 用户立，每次写入 TODO 必须判断）」条目（🔴/🟡/🟢 三类标准、标注格式、宁高勿低、存量接触一条补一条），已随自动同步规矩镜像到 CapabilityManagerAgent `claude/CLAUDE.md`（该侧变更记其自己 CHANGELOG）。
- **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：本次只重构 TODO.md 这一个待办文件（原 2 条存量内容一字未删、并入分级保留），不动任何 skill 正文 / 脚本 / 规范——审计发现的修复都还没做、属待办状态；后续逐条修复时再按溯源纪律各自评估回归风险。核对既有历史条目：TODO 文件结构此前为「按主题分节」，本次改为「按优先级分节」与 2026-08-01 立的文件制规矩（那时只定主题分节）不冲突——新规矩是全局 CLAUDE.md 同日新增条款、TODO.md 头部已同步引用，属规则演进而非回归。

### 新增（README 访问量徽章——舰队集中式访问统计）

- **为什么改**：全舰队上线集中式「真去重」访问统计（图片徽章方案无法去重，走官方 Traffic API 路线）：统计集中部署在 xhqing 仓库（`scripts/update_traffic.py` + 每日 GitHub Action），各 fleet 仓库只需在 README 挂徽章、零运行负担。
- **改了什么**：README（EN/CN）徽章区新增 visitors 徽章（shields.io endpoint 指向 `xhqing/xhqing` 仓库 `traffic/badges/<repo>.json`，由每日采集的官方 Traffic API 数据更新）。徽章数字含义：按日去重访客的累计（GitHub 只提供每日 uniques，跨天不去重），自 2026-08-16 起累计。

### 变更

- **实盘下单「受限地域出口被监管拒」根因排查定案 + SDK 代理固化（2026-08-14，下单安全）**：2026-08-13 实盘下单测试报 监管类错误码（券商按指令发出的网络环境判定，受限地域出口下实盘仅允许平仓/减仓/转出；报文原文与实值见本机记录）。当日曾误判「判定基于账户属性、代理 IP 绕不过」并回绝继续排查；2026-08-14 用户出示券商客服对话（客服确认判定依据是指令发出的网络环境）推翻该误判——**判定依据就是指令的网络环境（出口 IP）**，此前代理无效另有根因。**真根因（决定性）**：本机 xpilot（Xray）生成路由配置时对 proxy_list 里的**裸域名静默丢弃**——用户在 routing.json 加的是裸域名 `openapi.tigerfintech.com`，而生成逻辑（XPilot 项目 `routing_manager.py`）只认 `geosite:` / `domain:` 前缀，裸域名两个分支都不进、无警告丢弃 → 老虎流量落兜底规则 `0.0.0.0/0 → direct` 走本地出口 → 券商仍判受限地域拒单（此前实测「SDK 走代理仍被拒」的真相是：SDK→Xray 这段走了代理、Xray→出口这段对老虎域名直连了）。**修复与配置**：① routing.json 改 `domain:openapi.tigerfintech.com`（带前缀）+ `xpilot restart` → 规则落地到 `/tmp/xpilot-xray.json`；② [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) 新增 `apply_proxy(config)`——**老虎 SDK 用 urllib3.PoolManager 发请求、不读 HTTP(S)_PROXY 环境变量**（curl/requests 认、urllib3 不认），须代码层把 `web_utils.http_pool` 替换为 `urllib3.ProxyManager`；`load_config` 末尾自动调用，配置走 [`config.json`](.claude/skills/trade/config.json) 新增 `proxy` 节（enabled / http_proxy=`http://127.0.0.1:1087` / apply_scope=`live_only` 默认仅实盘走代理、模拟盘直连更快 / accounts_live_required 默认 false），代理探测失败回退直连并 stderr 警告；③ [`config.example.json`](.claude/skills/trade/config.example.json) 同步模板；④ [`accounts.md`](.claude/skills/trade/accounts.md)「老虎 SDK」节新增「实盘下单须合规网络环境 + SDK 代理配置」条目（三件套：白名单带 `domain:` 前缀 / SDK apply_proxy / 盘前检查）；⑤ [`SKILL.md`](.claude/skills/trade/SKILL.md) auto 账户选择段补「实盘下单须合规网络环境」+ 监管拒单排查指引。**实测验证（端到端）**：模拟默认直连（走代理=False、查询成功）；live 实盘走代理（socket 追踪确认连 127.0.0.1:1087、合规出口 <代理出口 IP>）+ `get_assets` 成功 + **下单受理**（TSLA 主单 LMT+止损腿，order_id 正常返回，拒单原因从监管类错误码变为普通「资金不足」——实盘未入金符合预期，监管拦截解除）；固化链路（load_config 无手 patch）端到端下单同样受理。**⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：改了 `load_config` 出口（末尾多调 apply_proxy）——模拟盘默认分支行为不变（apply_scope=live_only + accounts_live_required=false 下模拟账户直接 return、不挂代理，实测走代理=False）；代理探测仅 TCP 连 127.0.0.1:1087（timeout 2s、本机端口），不引入外网依赖；config.json 缺失 / proxy 节缺失 / enabled=false 均直接 return 保持直连，只读查询路径无回归。**遗留**：XPilot 裸域名静默丢弃 bug 待修（已记 XPilot 项目 TODO）；socket 长连接（9883 行情推送）不走 HTTP 代理、当前未用到未处理。**监管定性（如实记录）**：合规判断由用户自行负责（已咨询法律意见），Agent 侧职责是把网络环境配置正确、可验证（盘前 `get_assets` 实测请求受理正常）。

- **修正「客户端账户层受限」误判——真相是分流覆盖不全（2026-08-14 当日，App 端实测坐实）**：上条排查后曾误判「客户端（App/桌面端）是账户层标记、代理 IP 绕不过」——实际**判定「位置」按流量出口，但须全部流量走代理**。用户手机 v2rayNG 只给 `openapi.tigerfintech.com` 加代理规则时 App 仍显示受限（重登/清数据/换端/改时区/飞行模式全试都不行），**切全局模式后 App 立即放行** → 坐实：老虎 App 用多个域名、分流只覆盖一个、其它域名走本地出口 → 被判受限地域。Mac xpilot 同理（只加了 API 网关一个域名）。**修正后结论**：API 端（SDK）走代理合规出口可下单（已验）、客户端（App/桌面端）须**全局代理**（或把老虎 App 全部域名加进代理规则）才放行；此前「客户端账户层受限」结论作废，[`accounts.md`](.claude/skills/trade/accounts.md) 已更新为正确结论 + 全局代理要求。

- **开仓门槛口径统一为「初始净预期赔率 ≥ 2.4」单一门槛 + 修正 08-13 复盘「提 4R+」污染结论（2026-08-14）**：用户反馈两个问题——① 今天盯盘出现事故，Agent 误以为「×0.5 折扣后还要 2.4R」（把打折后的数再套 2.4 门槛、等价初始赔率要 ≥4.8），导致门槛翻倍开不出仓；② 质疑「×0.5 折扣」这个两步表述是否该直接写成 2.4R。排查后发现「×0.5 折扣后 ≥1.2」这个两步动作已实打实造成**两个方向**的事故，决定统一改单一门槛。**为什么改**：原表述「初始预期赔率 ×0.5 折扣后 ≥1.2 才开仓」是个两步动作（先打折、再与 1.2 比），每一步都可能算错——① 2026-08-14 盯盘事故：Agent 把「折扣后还要 2.4」理解成初始赔率要 ≥4.8，门槛翻倍、开不出仓；② 2026-08-13 MINIMAX#4 事故（净亏 −1.15R）：拿**毛赔率** 2.40（净赔率实为 2.12 < 2.4）×0.5=1.2 凑达标，错把毛赔率当净赔率用、该拦没拦。两条事故根因相同：中间动作 ×0.5 让判据口径稳不住。**改了什么**：
  - 门槛判据统一为单一动作「**初始净预期赔率 ≥ 2.4 才开仓，直接比、不做中间打折**」；「×0.5 折扣」降级为 references 里的**标定背景**（解释 2.4 怎么来的），不再出现在开仓判据位置。[`SKILL.md`](.claude/skills/trade/SKILL.md) 3 处（参考价定义、质量优先两道闸、EV 判据自查项）+ [`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md) 4 处（开仓基准、打折节规则段、震荡日出手口径、窄止损从严口径）+ [`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 1 处（公共操作原则）全部统一。
  - SKILL.md EV 判据自查项里明确写出两条事故案例（门槛翻倍 / 毛净混淆）作为警示，堵住同类错误。
  - 实算 08-06（×0.5 执行起）之后 9 笔港股三阶段赔率、补进 `trading-strategy.md`「预估赔率系统性打折」节：初始均值 3.61R、落地均值 +0.89R、中位 +0.48R、兑现率 25%、**78%（7/9）落地为正**、合计 +8.02R——兑现度明显高于 08-06 前基线（12 笔 75% 落地 ≤0、合计为负）。
  - **推翻 08-13 复盘「提 4R+」结论**：08-13 复盘第十节用「全量 17 笔混算」得出兑现率 13%、并据此建议「门槛从 2.4 提到 4R+」——但那 13% 是用 08-06 前未执行门槛的低质量旧样本把好样本稀释后的污染数字。按 4R+ 反查执行 ×0.5 后的 9 笔，会拦掉最赚的 01888（+3.41R）、胜宏（+3.35R）、MINIMAX#3（+1.96R），而真正亏的两笔（中际旭创 −1.28R、MINIMAX#4 −1.15R）初始赔率反而不低（4.72 / 2.40）、提门槛拦不住——**提门槛会误杀好单又拦不住坏单，故作废**。[`reviews/2026-08-13-HKT-review.md`](reviews/2026-08-13-HKT-review.md) 第十节标题下插修正说明、原结论划线保留备查，改进方向 #4、总结第 3/6 条同步标注推翻、门槛维持净赔率 ≥ 2.4。
  - 真正该收紧的不是赔率门槛而是**置信度判据**（08-13 MINIMAX#4 是「偏谨慎的绿」误判 🟡 为 🟢、08-12 中际旭创是水平震荡做空被扫——都亏在判据和方向选择、不亏在赔率），指向 trading-strategy.md 已立的「置信度只有 🟢/🟡/🔴 三档、禁止滑过渡」规则。
  **⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：本次只改门槛**表述口径**（两步→单步）和**复盘数据修正**，没改 2.4 这个门槛数值本身、也没改止损 / 止盈 / max_loss / 仓位任何计算。回溯 2026-08-05「预估赔率系统性打折」立规条目——当初引入 ×0.5 折扣是为了把「按 1.2 门槛放行的大批仓位实际落地为负、门槛形同虚设」的问题收紧到净赔率 2.4；本次改成单一门槛**没有放松门槛**（2.4 = 1.2×2 的等价换算、数值不变），只是消除了两步动作的歧义，故 2026-08-05 那条规则要解决的问题（旧 1.2 门槛放行低质量单）不会回归。相反，单步门槛反而更严：堵住了「毛赔率 ×0.5 凑达标」的漏洞（MINIMAX#4 那类），把判据钉死在净赔率。唯一需注意：08-13 复盘报告原文里的 13% / 4R+ 说法现在被标注推翻，但报告 CSV、统计图、港美对比等其它部分不受影响、仍有效。



- **修复密采样守护 watcher 在会话 Running 时仍周期性误报中断（2026-08-14）**：用户反馈「会话明明在 Running、watcher 还一直报中断」。**为什么改**：watcher 判定会话是否在 Running 读补丁 012 写盘的 state 文件，但旧逻辑除读内容外还看文件 mtime——补丁 012 的写盘是**事件驱动**（只在会话 state 真正切换 idle→running→thinking→waiting_input 时调用 `updateSessionState` 写盘）。一个持续 running 的 AI 回合（采样结束→连续读日志/读 csv/分析/重启，期间 state 保持 running 不切换）里这个方法根本不会再被调用，state 文件 mtime 冻结在「最后一次进入 running 的瞬间」、几分钟内必然老化。旧逻辑据 `mtime > STATE_STALE_SECONDS（120s）` 把「内容写着 running、但 mtime 老化」的有效文件判成 None = 作废了最可靠的判定信号，fallback 到脆的 jsonl 停更阈值（`STALE_SECONDS=90s`）。于是每段采样之间的分析窗口（AI 读多份日志 + 思考 + 重启，jsonl 短暂停更很容易突破 90s）被 watcher 每 10s 巡一次正好踩中 → 周期性误报「中断」。**实测坐实**（2026-08-14 09:37）：注册的两个盯盘会话 state 文件内容都 = `running`、但 mtime age 已 610s/596s（>120 过期）、而 jsonl 在 28s 前刚刷新（会话明明活着）；旧逻辑把这两个 state 判成 None、fallback 到 jsonl（此刻 jsonl <90s 没报，但段间分析窗口 jsonl 突破 90s 时就误报）。**改了什么**：① [`monitor_watcher.py`](.claude/hooks/monitor_watcher.py) `session_state()` 去掉 mtime 过期判定——**内容才是地面真相**，state 文件写着 running/thinking 就算会话在 Running，不看 mtime（删掉 `STATE_STALE_SECONDS` 及对应过期分支）。② 新增 `jsonl_age()` 辅助函数（jsonl 是会话活动度地面真相：每个 AI 回合都追加事件、写得密，比 state mtime 可靠）。③ `interrupted_sessions()` 对 `state=running/thinking` 加一道 jsonl 兜底防陈旧——jsonl 停更 > `RUNNING_SILENCE_SECONDS（300s / 5 分钟）` 才视作「陈旧 running」（会话进程崩溃 / 卡死后 state 停在 running 永不更新的坑，由 jsonl 活跃度兜住）、报中断；否则 jsonl 还在动 = 会话真活着、不报。④ `prune_dead_sessions()` 复用 `jsonl_age()` 去重（行为不变）。⑤ 文件头 + `session_state()` / `interrupted_sessions()` docstring 同步更新判定逻辑表述（删「state 文件过期 fallback」旧说法、补 2026-08-14 修复说明）。**为什么选 jsonl 兜底而非别的方式**：state mtime 会因事件驱动写盘假过期、不可靠；jsonl 由会话进程每个回合追加、是真活动信号。`RUNNING_SILENCE_SECONDS=300s` 取值：远大于正常段间分析窗口（采样段 40s 期间靠 `sampling_running()` 挡、段间 jsonl 写得勤正常 < 2~3min），又远小于死会话剔除 30min——真崩溃后约 5 分钟内能报、不再死等 30min。**验证（5 分支 monkeypatch 全过）**：A 误报现场（state=running+mtime 老化+jsonl 在动）→ 不报 ✅；B 真崩溃（state=running+jsonl 长期不动 600s）→ 报 ✅；C 等输入（waiting_input）→ 报 ✅；D 012 未生效+jsonl 停（120s>90）→ 报 ✅；E 012 未生效+jsonl 在动（30s<90）→ 不报 ✅。**⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：本次改了 `interrupted_sessions` / `session_state` 判定，回溯 2026-08-12 「采样在跑 + 会话 Running 标签双判据」的历史条目——当初引入 state 文件正是为了「替代靠 jsonl 停更阈值猜会话死活」（旧 jsonl 阈值在采样段 40s 期间误报）。本次修复**没有退回纯 jsonl 判定**：① 采样段期间仍由 `sampling_running()` 优先挡住（不碰 state / jsonl）；② state=running/thinking 内容仍是主判据，jsonl 只在「state 显示 running」时做兜底防陈旧（jsonl 停 > 5 分钟才算），**不是**重新靠 jsonl 短阈值（90s）猜会话死活——故 2026-08-07 那类「采样段尾端 jsonl 停更被误报」的坑不会回归（采样段由 sampling_running 挡、段间由 state 内容判 + jsonl 5 分钟兜底，均不踩 90s 短阈值）。唯一行为变化：会话**真崩溃后**（进程卡死、state 停在 running）的报警延迟从「即时（旧 mtime 120s 过期）」放宽到「≤ 5 分钟（jsonl 兜底）」——这是为消除误报主动让出的检测灵敏度，可接受（真崩溃不致命、watcher 本就是提醒用户而非自动重启）。


- **修复切实盘账户时 `is_paper` / 网关路由 bug（2026-08-13，下单安全修复）**：用户明天（2026-08-14）要做实盘交易，排查实盘登入时发现老虎 SDK 的 `account.setter` 是**单向开关**——`if AccountUtil.is_paper_account(value): is_paper = True`，只在「新账户号是 paper」时设 True，**切到非 paper 账户号（如实盘 7 位号）时不清零**。`load_config(account='live')` 的执行顺序是先构造（`_load_props` 读 properties 的 17 位模拟号 → is_paper=True），再赋值实盘号（setter 判定非 paper → 不动作 → is_paper 卡 True）。后果：**切到实盘账户号后 `is_paper` 仍是 True → `server_url` 仍指向沙箱网关 `openapi-sandbox.tigerfintech.com` 而非生产网关 `openapi.tigerfintech.com`**——实盘下单请求走错网关、打到沙箱（虚单不花真钱）或因账户在沙箱不存在被拒，行为不可控，绝非「在实盘下真单」的确定状态。**为什么严重**：明天切实盘交易若踩这个 bug，会误以为下了实盘、实际打到沙箱，或反之——账户 / 模式弄错就是实盘事故，正是 SKILL 模式提醒这个安全件要防的。**改了什么**：① [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `load_config` 末尾新增 `_sync_is_paper(config)`，用 SDK 权威判定 `AccountUtil.is_paper_account(config.account)`（纯数字且长度 ≥ 17 → True 模拟、否则 False 实盘）显式设定 `is_paper`，覆盖默认/live/具体号三个分支；改完 `is_paper` 后调 `config.refresh_server_info()` 让 SDK 按新 `is_paper + license` 重选生产/沙箱网关（实测改 is_paper 不自动重算 server_url、须显式调）。② [`SKILL.md`](.claude/skills/trade/SKILL.md) auto 账户选择段补「网关路由修复」说明（模拟走 sandbox、实盘走 production、is_paper/server_url 已与账户号一致）+ 账户号归属校验方法（不存在 / 未授权号报 `account 'xxx' is not authorized to the api user`）。**实测验证（全部通过）**：修复后三分支——默认/paper（17 位）is_paper=True → sandbox 网关 NL=1,009,284 ✅；`live`（7 位实盘号）is_paper=**False** → **production 网关** NL=0 ✅；具体实盘号同 live ✅。对照假账户号 `999999999` 报 `is not authorized`、实盘号能查到 4 条历史订单 → 证明该实盘号是挂在本凭证下的合法实盘账户、生产网关已接通。实盘 NL=0 是「未入金」正常表现（生产 / 沙箱网关查到都 0，入金后生产会查到真实资金、沙箱仍 0、两者届时不同）。**⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：本次改了 `load_config`，而多个只读查询也走它——`load_equity_tiger` / `get_month_order_count_tiger`（阶梯平台费定档）等默认调 `load_config()`（account=None → 模拟分支）。回归验证确认默认分支行为不变（is_paper=True、sandbox 网关、NL 正常，见上），`_sync_is_paper` 对模拟账户号（17 位）判定的 is_paper=True 与修复前构造期 `_load_props` 设定的一致，不改变模拟盘现有费率计算 / 权益取值路径。若未来 properties 的默认 account 改为实盘号，`_sync_is_paper` 会自动把默认 load_config 也切到 production 网关——属预期行为（默认号是实盘就该走生产），届时注意只读查询也走实盘网关。**调用方零改动**：6 个下单脚本（open/close/move_stop × 港美股）统一 `U.load_config(account=account)`，修复集中在 `load_config` 一处。

- **实盘账户配置就绪 + 实测登入验证（2026-08-13）**：用户给出老虎实盘账户号（7 位，真实值见本机账户配置文件、已 gitignore），完成 2026-08-12「auto 模式加实盘备选」条目的待办（「用户填 account_live 后功能即可用」）。**为什么做**：用户要求把实盘账户纳入 auto 模式备选、并验证凭证能否登入。**改了什么**：① 项目内账户配置文件（已 gitignore）填入 `tiger.account_live`（7 位实盘账户号）、并把之前误填成实盘号的 `tiger.account` 修正回真实模拟账户号（17 位），使「account=模拟 / account_live=实盘」语义自洽；② [`SKILL.md`](.claude/skills/trade/SKILL.md)「auto 模式的账户选择」段补「实盘账户 2026-08-13 验证时尚未入金」提醒（登入连接正常但 `get_assets` 返回 net_liquidation=0 / cash=0——切实盘下单前须先提醒余额为 0、大概率被拒，并实跑核实余额）。**实测结果**：用同一套凭证（tiger_id + private_key + license=TBNZ）分别以实盘号（7 位）和模拟号（17 位）登入，**两个账户号都能成功登入、凭证有效**——实盘号净值全 0（未入金，符合 `trade_utils_tiger.py` 既有坑记录「未开通入金的账户 get_assets 返回全 0 且 timestamp=None」）、模拟 17 位号净值约 100 万美元模拟资金（`get_prime_assets(HKD)` 证券段折算约 790 万 HKD）。**账户号位数与模拟/实盘的判定**：之前 `trade_utils_tiger.py` 注释「17 位纯数字 = 模拟」是当时已知规律，本次实测确认本套凭证下**17 位号是模拟、7 位号是实盘**——位数与模拟/实盘的对应并非通用铁律（取决于券商开户分配），`load_config` 的 paper 判定仍按「是否 17 位」做网关域名路由，实盘 7 位号经 TBNZ（非 PAPER）网关登入连接正常、查询返回有效结构（非权限拒绝），证明路由无误。**未改脚本**：`load_config(account='live')` 加载链路实测通（默认 → 17 位模拟号、`'live'` → 7 位实盘号），2026-08-12 已就绪的代码无需再动。（⚠️ 账户号真实值不在本文件、在已 gitignore 的本机账户配置文件，避免敏感信息进 git 历史。）

- **老虎订单时间字段踩坑记录 + 脚本排查（2026-08-13）**：续上条「实盘账户实测登入」——查实盘 4 条历史订单（均 09988 阿里巴巴）做核对时，手动换算成交时间**误用 `update_time`**，把 2025-05-14 港股盘中 09:44 成交的单算成了 05-16 凌晨 00:07（港股凌晨不交易，明显异常）。**排查根因**：老虎订单对象（`get_orders()` 返回）有**三个**时间字段含义不同——`order_time`（下单时刻）/ `trade_time`（**成交时刻**，复盘核对时点该取这个）/ `update_time`（订单最后一次被系统更新的时刻，含日内结算批处理，凌晨那种就是这个、**不能当成交时间**）。改用 `trade_time` 后 4 条订单时间全部正确落在港股盘中：已成交 2 笔（2025-05-14，09:44 买 200@127.4、11:39 卖 200@128.1，毛赚约 HK$140）、未成交 2 笔（2025-05-13 收盘前，800 股 @125.9 买单，一个撤一个过期失效）。**脚本排查结果（无 bug）**：全脚本 grep `update_time`/`trade_time`/`order_time`——脚本里的 `update_time` 全是富途 OpenD **行情快照**的快照更新时刻（`monitor.py` / `snapshot.py` / `resume.py` / `trade_utils_tiger_us.py`，用途正确、非订单时间）；`trade_utils_tiger.py:358` / `trade_utils_tiger_us.py:164` 的 `summary.timestamp` 是 `get_assets()` 资产查询的时间戳（判账户是否开通权限用）、与订单成交时间无关、用途正确；**脚本里没有任何地方自动取订单成交时间**——`review.py` 的开/平仓时刻（`entry_time`/`exit_time`）从复盘 CSV **手填**读（不从订单对象自动取），故脚本本身无「取错订单时间字段」的 bug。**改了什么（记坑留痕，非代码修复）**：[`accounts.md`](.claude/skills/trade/accounts.md)「老虎 SDK」节新增「订单时间字段三件套」条目——明确 `order_time`/`trade_time`/`update_time` 三者区别、复盘核对必取 `trade_time`、附本次踩坑经过 + `summary.timestamp` 不混淆说明。**未改代码**：误用发生在对话手动换算、不在脚本里；但人工填复盘 CSV 的 `entry_time`/`exit_time` 时须从 `trade_time` 取、勿取 `update_time`，已记入 `accounts.md`。

- **复盘默认只复盘港股、美股默认不复盘（2026-08-13 用户立）**：以后复盘**默认只复盘港股数据**（`signals/*-HKT-*.md` + `actions/*-HKT-*.md`），**默认不复盘美股数据**（`signals/*-ET-*.md` + `actions/*-ET-*.md`）；只有用户明确要求复盘美股（如「也复盘一下美股」「美股一起看」）时才纳入美股数据。**为什么改**：用户立的新规定——默认复盘范围收窄到港股。**改了什么**：① [`references/review-and-evaluation.md`](.claude/skills/trade/references/review-and-evaluation.md)「复盘分析」节开头新增「默认复盘范围 = 港股，美股默认不复盘」段（点明是对 2026-08-07「港美股分开统计必含」规则的修订、美股降级为按需项）；「数据源」段同步把默认范围限定到港股（美股按需纳入）、全量对账纪律改为「所选范围内全量对账」（默认只复盘港股时也要把所有港股 HKT 文件全量对账）；「港美股分开统计必含」段标题与正文改为「按需」——只在「同时纳入了港股和美股数据」时才做港美股拆分 + 五步差异分析，默认只复盘港股时只跑港股子集一遍 review.py + 7 张演化图（`--suffix -hk`）、不拆港美、不做差异分析、不产美股子集图。② [`SKILL.md`](.claude/skills/trade/SKILL.md) 复盘「数据源」段同步加「默认复盘范围 = 港股」段、原数据源段默认只遍历港股。③ [`.claude/CLAUDE.md`](.claude/CLAUDE.md)「actions/ 目录归属」段「以后复盘都复盘这两个目录下的所有交易」改为「复盘数据源是这两个目录 + 默认只复盘港股」。**脚本层未改**：`review.py` / `bayes_evolution.py` 按输入 CSV 跑、本身不分港美，默认只复盘港股 = 只喂港股子集 CSV 即可，逻辑无需动。**⚠️ 回归风险提醒（依「CHANGELOG 记录纪律」溯源）**：2026-08-03「全量对账纪律」是为堵「美股样本漏 3 笔造成幸存者偏差」（N 由 28 → 33、EV 修正）而立的，原要求「每次复盘遍历 signals/ + actions/ 两目录全部文件」；本次改为默认只复盘港股后，**默认复盘不再遍历美股文件、美股的「全量对账」默认不执行**——若将来用户恢复「复盘美股」，须手动确保把所有美股 ET 文件全量对账（不能只补增量），否则 2026-08-03 那类漏样本偏差会在美股上回归。港股的「全量对账」不受影响（默认复盘仍要求遍历所有港股 HKT 文件全量对账）。另：2026-08-07「每次复盘必含港美股分开统计 + 五步差异分析」降级为「按需」——默认只复盘港股时不做差异分析，相关信息（美股对 EV 的拖累、多空方向拆解等）在「默认只复盘港股」的复盘报告里不再出现，用户需要时须显式要求复盘美股。

- **港股历史全量复盘（N=41）+ 港美对比 + 三阶段赔率核心发现（2026-08-13）**：用户触发「复盘历史所有港股交易」。**为什么做**：上一版港股样本停在 08-07（37 笔），08-10/08-12/08-13 又有 4 笔新交易未入样本，按「全量对账纪律」须遍历 `signals/`+`actions/` 全部 HKT 文件对账补齐。**改了什么 / 发现**：
  - 新建 [`reviews/2026-08-13-HKT-review.md`](reviews/2026-08-13-HKT-review.md)——港股 41 笔全量复盘报告（含终局统计、贝叶斯 P(EV>0)、频率派 CI、7 张序贯演化图、科学选 f 四步、港美差异分析五步、三阶段赔率）；新增数据 [`reviews/2026-08-13-trades-hk.csv`](reviews/2026-08-13-trades-hk.csv)（41 笔）/ [`2026-08-13-trades-us.csv`](reviews/2026-08-13-trades-us.csv)（补 5 笔→20 笔）/ [`2026-08-13-trades.csv`](reviews/2026-08-13-trades.csv)（全量 61 笔）；港股 7 图 + 美股 7 图共 14 张。
  - **港股正期望证据较强**：N=41、EV +0.510R、胜率 61%、P(EV>0)=98.1% [94~99%]、频率派 CI [+0.04,+0.98] 全正；做多 26 笔 69% 胜率 EV +0.67R 是核心盈利来源。剔除 Top3 大胜单后 EV 仍 +0.226R、胜率 58%——edge 非幸存者偏差。但下界 94% 贴 95% 确认线、N=41 处过渡区，**未到铁证、维持 f=2%**。
  - **美股强负期望拖累全量**：N=20、EV −0.565R、胜率 30%、P(EV>0)=0.4%；**美股做空 9 笔仅 11% 胜率、EV −0.99R 是最大失分点**（MU/LITE 高波动反抽反复扫损）；剔除 3x ETF 后美股仍 −0.407R，非 ETF 单一原因。港美 EV 差 1.075R 反事实分解：胜率差贡献 43%、胜赔差 23%。全量（61 笔）EV 仅 +0.157R。
  - **核心发现：预估赔率兑现率仅 13%**——17 笔有完整三阶段标注的样本，初始预期赔率均值 2.75R → 实际落地仅 0.36R、兑现率 13%；港股 29%、美股 −19%（预估赚钱落地亏）。说明 ×0.5 经验折扣远不够、当前 2.4R 开仓门槛偏低，**建议提到 4R+**（落地才约 0.5R）。
  - **改进方向**（按可控性排序）：① 美股暂停做空；② 开仓赔率门槛 2.4R→4R+；③ 港股做空收紧（46.7% vs 做多 69.2%）；④ 让利润奔跑 vs 锁利平衡；⑤ 补齐 raw_high/low 过程指标。
  - **补全过程指标（MAE/MFE/回吐/锁利效率 η）+ 锁利能力核心发现（同日用户追问「过程指标能不能从历史数据补回来」）**：按 skill 方法论方案 (a)，给 41 笔港股补 `entry_time`/`exit_time`（auto 模式用真实成交时间 + 真实成交价、signal 模式用响铃时刻近似；07-17 智谱首批次窗口取全程），跑 `review.py --fetch-futu` 连富途回拉历史 1 分钟 K 取持仓期间 high/low——**41 笔全部成功补全**（实测富途可拉到 1 月前的分钟 K）。新增带时间戳数据 [`reviews/2026-08-13-trades-hk-ts.csv`](reviews/2026-08-13-trades-hk-ts.csv)。**过程指标结果**：盈利单(n=25) MAE −0.43 / MFE +2.31 / 回吐 1.00 / **η=0.49**；亏损单(n=16) MAE −1.03 / MFE +0.67。**锁利能力核心发现**：① 盈利单平均浮盈到 2.31R 只兑现 1.31R（η=0.49）、44% 的盈利单兑现率不足 40%、合计回吐 25.1R；② 低 η(η<0.5)的 14 笔细分——**8 笔是"移动止损没随浮盈持续上移、浮盈回吐后扫损"**（主因）、6 笔主动平仓（出场时机）；③ 5 笔由盈转亏（07-17 智谱峰值 5.67R 全回吐最痛）；④ η 从 0.49→0.75 潜在 +0.42R/笔、合计约 +10R，纯执行层、立竿见影。**归因严谨性**：η 低不一律归"锁利差"——07-07 美团 η=0.08 是早期规则缺失（当时无移损规则、止损在保本位没动）非执行差，剔除后剩余低 η 更纯粹反映"移损位置"问题。**报告第八节全量改写**（从"跳过"改为完整过程指标 + η 分布 + 回吐 Top5 + 归因细分 + 由盈转亏 + 优化空间量化 + 改进建议），改进方向 #1 改为"移动止损随浮盈持续上移"（锁利头号优化，排在美股做空之前——因纯执行层边际收益最直接）。**未改终局统计**：补时间戳只影响过程指标、不改盈亏（终局 EV 仍 +0.510R）。
  - **对账澄清**：08-03~08-06 港股 signals 与 actions 并存是 signal/auto 两会话独立交易、非重复；07-21 中芯第二开仓（响铃价=止损价）、07-28（断网作废）、08-10 测试单均正确不计样本。

- **置信度分级加「禁止 🟢/🟡 滑过渡」硬约束 + MINIMAX 做多事故反面案例（2026-08-13）**：2026-08-13 auto 模式做 MINIMAX-W（00100）多，盯盘一小时终于等到「破前高 374.4 → 回踩 377-378 企稳 + 买卖比转正」的入场形态后开仓，11 分钟急跌破止损、净亏 8.4 万（-1.15R）。事后用户追问「为什么有量价背离隐忧还判 🟢 高置信通过」——根因是**置信度闸被用伪概念拨开**：该笔全程买卖比持续大负（一路到 -47/-61/-82，价创新高但卖压堆积 = 做多方向不利的量价背离）+ 依据里已标注「上方抛压重」反向风险，按定义本就是 🟡 中置信、不该开；但太想做时大脑自造了一个体系里不存在的「🟢 偏谨慎」标签、用「入场形态成立」顶替「各维度一致」、把「已标注反向风险」错当「风险已处理」，把闸从 🟡 滑到了 🟢。**为什么改**：这是规则里已有反面案例 SOXL（做空时已标注超大单逆势买风险仍发 → 实亏 ~$120）的**做多翻版**，错误同构（都是「中置信 + 已标注反向风险 → 本该不开 → 还是开了 → 实亏」），说明光写规则不够、闸在我手里是软的——要靠「禁止滑过渡」的显式约束把判据钉死。**改了什么**：[`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md)「置信度分级」节新增「禁止 🟢/🟡 之间滑过渡」小节——① 明确置信度只有 🟢/🟡/🔴 三档、不存在「🟢 偏谨慎」等中间态，存在任何对入场方向不利的维度矛盾即落 🟡 不开仓；② 配套两条判据纪律（判据是「各维度是否一致」非「入场形态是否成立」；「已标注反向风险」即落 🟡、识别风险 ≠ 处理掉风险）；③ MINIMAX 事故完整复盘 + 与 SOXL 案例互补（做空做多各一例）；④ 自检口诀（盯很久等到入场点很想做时 = 滑过渡高发时刻，强制回到「各维度是否一致」硬判据）。**未触发降频**：该约束属判级纪律、不影响采样机制。

- **港股交易费率口径全量改造：老虎真实费率取代旧 18bps 笼统单值（2026-08-12）**：历史复盘 / 前瞻赔率里所有「双边费」都用笼统「港股 18bps/边、美股 3bps/边」估算——经官网核实老虎真实费率后，该口径**高估个股成本**（真实 14.17 < 18）、**严重高估 ETF 成本**（真实 4.17 < 18，ETF 免印花税）。**为什么改**：① 账户选择分析（确定用老虎、平台费阶梯式）后，必须用真实费率才能准确算净赔率 / EV；② 旧口径不分个股/ETF、不区分大小单（佣金最低 15 / 平台费），系统性失真。**改了什么**：
  - 新建 [`scripts/fee_schedule.py`](.claude/skills/trade/scripts/fee_schedule.py)——共享真实费率模块（复盘 + 前瞻共用，改费率只改该文件）：单边费 = 佣金 `max(15, 成交额×0.029%)` + 印花税（港股个股 0.1%、ETF 免）+ 交易费/交收/征费/财汇局征费 + 阶梯平台费；大单（成交额 > 51,724）收敛到个股 14.17 bps/边、ETF 4.17 bps/边，小单按公式（佣金最低 + 平台费占比高）；阶梯平台费按当月累计**成交**订单数分档。
  - [`scripts/review.py`](.claude/skills/trade/scripts/review.py) / [`scripts/bayes_evolution.py`](.claude/skills/trade/scripts/bayes_evolution.py)：`_fee_per_side` 单值 → 复用 fee_schedule；load_trades 读 `type` 列（stock/etf）、按自然月累计订单序号算阶梯平台费、**跨日交易报错**（本项目纯日内、不存在跨日，数据跨日 = 数据问题）；type 列缺失默认 stock（保守收印花税）并打印提醒。
  - [`scripts/trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py)：前瞻赔率 `_net_odds` 改真实费率（与复盘同口径，完全精算）；新增 `get_month_order_count_tiger`（查老虎账户当月**成交**订单数 Filled/PartiallyFilled，signal/auto 两模式都用实盘账户数，模拟盘偏高当保守估计）、`build_fee_ctx`（构造费率上下文）、`_sec_type_of`（ETF 白名单判定）。[`open_position_tiger.py`](.claude/skills/trade/scripts/open_position_tiger.py) / [`open_position_tiger_us.py`](.claude/skills/trade/scripts/open_position_tiger_us.py)：构造 fee_ctx 传入 check_price_in_range。
  - 13 个历史 [`reviews/*trades*.csv`](reviews/)：全部补标 `type` 列（07709/SOXL/SOXS=etf，其余=stock），classify 复核无 ETF 误标。
  - [`SKILL.md`](.claude/skills/trade/SKILL.md) 3 处费率口径 + [`references/risk-management.md`](.claude/skills/trade/references/risk-management.md) 新增「费率与成本」章节（SKILL 引用落点，含完整费率表 / 大小单分流 / 阶梯平台费 / 订单数口径）。
  - **文档旧 18bps 口径全清除**：`trading-strategy.md` / `signal-mode.md` / `auto-mode.md` / `review-and-evaluation.md` / `trade_utils_tiger_us.py` 的旧「港股 18bps/边、美股 3bps/边」全部替换为老虎真实费率引用（指向 `risk-management.md`「费率与成本」）。`get_month_order_count_tiger` 盘后实测通过（查历史订单不依赖盘中）。

- **auto 模式加老虎实盘账户作为备选 + signal 模式可接入账户做非下单操作（2026-08-12 用户立）**：① **signal 模式**重新定义——AI 只发信号、**下单动作由用户做**，但 AI **可接入实盘/模拟账户做非下单操作**（查行情/持仓/订单数/权益等只读查询，用于真实费率算当月订单数等），只要不下单即可（旧表述「不碰账户/资金/持仓」过窄、已改）。② **auto 模式加老虎实盘账户作为备选**（旧「无备选账户」已改）——规则（用户定）：auto **默认仍用老虎模拟账户**（账户配置文件的 `tiger.account`）；**只有用户明确说「切实盘/用实盘账户」时才切**老虎实盘账户（`tiger.account_live`），AI **绝不自主**切实盘；**切实盘前必须额外征求用户确认**（实盘=真钱、误操作无缓冲，多一道确认）。**改了什么**：[`SKILL.md`](.claude/skills/trade/SKILL.md) 模式开关段（signal 提醒去掉「不碰账户」、auto 提醒加实盘备选 + 切实盘双闸）、[`accounts.example.json`](.claude/skills/trade/accounts.example.json) 加 `account_live` 字段模板、[`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `load_config(account=None)` 支持账户选择（None/paper=模拟、live=从账户配置文件读 `account_live` 切实盘、实测 `config.account` 可写、不碰 properties 文件；`_read_accounts_json_field` 读该配置文件）、6 个下单脚本（open/close/move_stop × 港美股）加 `--account live/paper` 参数并传给 `load_config`；`get_month_order_count_tiger` 盘后实测通过（本月模拟账户成交港股 41 笔 + 美股 14 笔）。**港美平台费完全独立、各自按市场单独计阶梯档**（2026-08-12 用户纠正：不是账户合计）——`get_month_order_count_tiger(market=)` 按订单 `contract.market` 过滤只数该市场、`build_fee_ctx` 传市场、复盘月计数器按 (自然月, 市场) 分开（初版误按账户合计计档、已修正）。**待办**：用户往账户配置文件填 `account_live` 实盘账户号后，实盘切账功能即可用（代码已就绪）；[`auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 可补实盘备选详细规则（当前 SKILL 模式开关段已含核心规则）。

- **新建港股券商费率档案 + 长桥 vs 老虎账户选择分析（2026-08-12）**：用户面临「资金只能放一个账户」的选择——长桥终生免佣但开空标的受限（智谱、中际旭创等做不了）、老虎 0.029% 佣金但开空全覆盖。**为什么做**：① 历史复盘里所有「双边费」都用笼统 18 bps/边估算，需用两家真实费率替换才能准确衡量；② 账户选择需要把费率差异和开空覆盖差异一起衡量才能定。**查证（两家官网核实）**：除佣金外，印花税（个股 0.1% 单边、ETF 免）+ 交易费 0.00565% + 交收费 0.0042% + 征费 0.0027% + 财汇局征费 0.00015% **两家完全一致**（都是代收）；平台费两家固定式都是 15 港元/笔；**唯一实质差异 = 佣金**（长桥 0、老虎 0.029% 最低 15/笔）。老虎阶梯式临界点 ≈ 月 30 笔订单；本用户按交易日重算实际 **5.2 笔/交易日、月约 109 笔**，阶梯式每月省 438 港元（年省约 5,256），但清淡月 < 50 笔时阶梯式反亏，是否切换取决于频率稳定性判断。老虎港股融券 **T+2 才起息**、日内还券免息（与长桥一致）→ 本策略持仓不过夜、融券利息恒为 0、不构成差异。**改了什么**：新建 [`tmp/hk-broker-fees.md`](tmp/hk-broker-fees.md)——长桥/老虎港股费率完整明细对照（含老虎阶梯式完整费率表、融券 T+2 计息规则、复盘用**单边 bps** 设定：老虎个股 14.17 bps/边、ETF 4.17 bps/边）。**结论**：选老虎——费率差异（多付约 2 万/年佣金）远小于开空覆盖机会成本（长桥做不了的空单净值 +4.25 万、且持续发生），叠加资金单账户约束下盘中转资金来不及。
  - **⚠️ 口径更正（同日用户两次纠正）**：初稿误用「双边 bps」口径并把历史成本偏差方向判反。正确口径为**单边 bps**（开仓、平仓各算各的，因开仓额 ≠ 平仓额，单边更精确）。历史「18 bps」是单边口径，与老虎真实单边对比：个股 14.17 < 18（历史**高估**个股成本 3.83 bps/边）、ETF 4.17 < 18（历史**严重高估** ETF 成本 13.83 bps/边）——即历史 18 bps 整体**高估**了成本（原稿误判为「低估个股」是拿双边数与单边数错位比较所致，已在费率文件第三章修正）。
  - **⚠️ 订单口径与频率更正（同日用户第三次纠正）**：① 订单只算开仓 / 平仓，**移动止损（modify）不算订单**；一笔完整交易 = 一开一平 = **1 T = 2 笔订单**。② 据此重算：15 个交易日、64 笔订单，平均 4.27 笔/交易日 ≈ 2.13 T/日，外推完整月 ≈ **90 笔订单（45 T）/月**（原稿把移损算进订单得 109 笔，偏高）。③ **阶梯式优于固定式的临界点 = 月 36 笔订单（约 18 T/月）**——前 5 笔阶梯式 30 港元/笔贵一倍，1~35 笔区间阶梯式都更贵、第 36 笔起反超。本用户约 90 笔/月远超临界点，阶梯式每月省约 300 港元（年省约 3,600），但清淡月 < 18 T 时阶梯式反亏。
  - **复盘费率口径定案（大单 / 小单分类，2026-08-12 立）**：以后复盘成本按单笔成交额分两套口径——**大单**（成交额 > 51,724，佣金按 0.029%）老虎个股 14.17 bps/边、ETF 4.17 bps/边（不含平台费，平台费 15/笔另加）；**小单**（成交额 ≤ 51,724，佣金按最低 15 收）单边费 = `max(15, 成交额×0.029%) + 印花税(个股) + 交易费 + 交收费 + 征费 + 财汇局征费 + 平台费15`。两口径在临界点 51,724 处连续衔接（个股 17.07 bps/边）。已写入 [`tmp/hk-broker-fees.md`](tmp/hk-broker-fees.md)。

- **移动止损规则补全：水平震荡反转位 = 区间上沿/下沿 + 清理「用法 A/B」废弃命名残留（2026-08-12）**：8-12 港股 03308 做空在 1068-1077 水平震荡确认后，止损仍死守开仓远位 1090、未随结构移到震荡上沿外侧，14:54 放量突破上沿后一路拉到 1095 才触发止损，−1.28R。**为什么改**：① 用户指出「止损永远放最新趋势反转位、随结构动态改变」才是对的——**水平震荡时趋势反转位就是震荡区间的上沿或下沿**（做空 = 上沿、做多 = 下沿，突破区间边界 = 逻辑失效），而现有规则全部只写「随趋势结构上移」（做多表述），缺失做空下移与震荡市判定，导致 03308 类「震荡中死守开仓远位、被突破区间后大幅反向扫损」的漏洞；② 用户指出废弃规则内容不应残留在交易策略里——「用法 A/B」二分框架于 2026-08-07 已作废，但 SKILL.md 第 5 条仍写「用法 B 形态」、trading-strategy.md 节标题仍叫「用法 B 详解」。**改了什么**：
  - [`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 5 条：删「用法 B 形态」残留命名，改为「止损永远放最新趋势反转位、随结构动态改变——做多随涨上移、做空随跌下移、水平震荡 = 区间上沿（做空）/ 下沿（做多）、突破区间边界 = 逻辑失效」；历史教训引用补 2026-08-12 03308。
  - [`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md)「移动止损」节：主节标题「用法 B 详解」→「移动止损唯一用法：止损永远放最新趋势反转位」，正文「随趋势演进上移」→「随结构演进移动」；新增「三种市况下反转位的判定」子节——上升趋势做多随涨上移、下跌趋势做空随跌下移、**水平震荡 = 区间边界（做空上沿 / 做多下沿），区间确认后止损移到边界外侧、突破边界即止损，不得留开仓远位**（附 03308 完整教训）；药明康德历史教训「按用法 B」→「按当时规则」去掉废弃命名。
  - [`references/risk-management.md`](.claude/skills/trade/references/risk-management.md)「止损位 = 趋势反转位」节：同步补水平震荡反转位表述（做空 = 区间上沿 / 做多 = 区间下沿、止损移边界外侧、突破 = 逻辑失效）。
  - [`references/signal-mode.md`](.claude/skills/trade/references/signal-mode.md) 移损信号「止损位」栏：补「做空随跌下移、水平震荡 = 区间上沿（做空）/ 下沿（做多）」。
- **watcher 判定逻辑重构：采样在跑就不报 + 死会话自动剔除（2026-08-12 用户立）**：8-12 上午港股盯盘中 watcher 一直响，此时两个盯盘窗口都显示 Running、采样脚本正常在跑。**为什么改**：旧 2026-08-11 逻辑只认「会话 jsonl 停更 > 50s 就报」，但盯盘窗口 Running 时 AI 回合正卡在采样脚本（`monitor_segment.py` / `ws_segment.py`）的 Bash 工具调用里——采样段跑 40 秒期间会话 jsonl 完全不更新（工具还没返回、AI 没产出新内容），50s 阈值踩在采样段尾端（40s 标称 + 段启停开销），只要某段实际跑到 51s 就误报。旧逻辑代码自己也承认「正常无输出间隔最少 ≈ 40s」——阈值压在刀刃上、注定不稳。次因为注册源头不收窄：preflight 无条件全局注册（美股盯盘也注册），watcher 只港股检查 → 美股会话残留第二天港股开盘被误报（`tmp/monitor_sessions.txt` 里曾残留 `6ede8d93` 美股会话 8 小时）。**用户立新逻辑：盯盘窗口在 Running 且后台采样脚本在正常采样 → 不报，两者都不活跃才报**。**改了什么**：
  - [`.claude/hooks/monitor_watcher.py`](.claude/hooks/monitor_watcher.py) `main` 判定优先级改为三层：① **死会话自动剔除（兜底自愈）**——jsonl 停更 > 30 分钟（`DEAD_SESSION_SECONDS`）视作已结束（停盯没注销 / 美股会话残留 / 会话崩溃没清理），自动从 `tmp/monitor_sessions.txt` 移除、不再报；② **采样在跑 → 正常**——新增 `sampling_running()` 用 `pgrep -f` 查 `monitor_segment.py` / `ws_segment.py` / `futu_ws_segment.py` 任一存活即判正常、本轮一律不报（采样进程是盯盘「在正常运行」最直接、最稳的铁证，绕开「采样期间 jsonl 停更」固有盲区）；③ **采样没在跑 + jsonl 停更 > STALE_SECONDS → 报中断**。`STALE_SECONDS` 50 → **90s**（采样在跑已盖住采样段期间，本阈值只在分析阶段 / 段间重启等正常窗口期生效，不再踩刀刃，放宽以过滤正常工具调用）。
  - [`scripts/preflight.py`](.claude/skills/trade/scripts/preflight.py) `_register_monitor_session()` **收窄为仅港股注册**——加港股盘中判断（周一至周五 09:00-12:00 / 13:00-16:00，09:00 起覆盖盘前 30 分钟预热），美股 / 夜间 / 盘外跳过注册（打印跳过提示），从根上杜绝美股 session 进注册列表。
  - [`.claude/hooks/com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) 注释同步新判定逻辑；[`scripts/monitor_register.sh`](.claude/skills/trade/scripts/monitor_register.sh) 注释补「仅港股注册」；[`SKILL.md`](.claude/skills/trade/SKILL.md) 停盯第一个动作「preflight 已自动注册」补「仅港股盯盘」；[`references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「多层防护」⑪ 更新为 2026-08-12 新判定说明。
  - 测试：盘中实测新逻辑（两真会话注册、采样在跑）`main` 在第②步 return 不报；preflight 港股盘中注册成功、测试 id 已清理；`prune_dead_sessions` 剔除 > 30 分钟死会话；`STALE_SECONDS=90` 在采样在跑前提下不触发。

- **watcher 第③步改读会话 Running 标签（state 文件），不再用阈值猜（2026-08-12 用户再次纠正）**：上一条把第③步定为「采样没在跑 + jsonl 停更 > 90s 报中断」，用户指出阈值判断本质还是在猜——应直接读会话的 **Running 标签**（补丁 002 显示的那个 Running 字样）判断会话是否在响应。**为什么改**：jsonl 停更 90s 仍是经验阈值，采样在跑但 AI 长时间无产出、或段间被拉长时仍可能误报；Running 标签是会话「是否在响应」的地面真相，用它替代阈值才能根治。**查证结论（已实测）**：补丁 002 的 Running 标签状态源自 session 对象 `busy=state!="idle"` / `pendingInput=state=="waiting_input"`（真 Running = state 是 running/thinking），但该 state 只在 extension.js 进程内存、**不写盘、无 CLI/API 暴露**，外部进程（watcher 是独立 launchd 进程）原读不到。**解法**：跨项目给 PatchClaudeAgent 新增 [patch 012](https://github.com/xhqing/PatchClaudeAgent)（`session-state-to-file`）——给 extension.js 的 `updateSessionState(e,t,r)` 注入写盘，每次会话 state 变化时写 `~/.claude/session_running/<sessionId>.txt`（内容 = state 字符串），watcher 读它 = 读会话 Running 标签的地面真相。**改了什么**：
  - [`.claude/hooks/monitor_watcher.py`](.claude/hooks/monitor_watcher.py) 第③步从「jsonl 停更 > 90s」改为读 state 文件：新增 `session_state(sid)` 读 `~/.claude/session_running/<sid>.txt`、`interrupted_sessions()` 取代 `stale_sessions()`——`running`/`thinking` 不报，`waiting_input`/`idle` 报中断；state 文件不存在 / 过期（> 120s，补丁 012 未生效，如盯盘会话用旧 extension.js）→ fallback 到原 jsonl 停更 > 90s 判定（过渡期兼容，不误报）。
  - PatchClaudeAgent 新增 patch 012（`session-state-to-file`）：`type: locate` 定位 `updateSessionState` 方法、动态提取参数名后注入写盘（`require("fs")` 原生可用、`process.env.HOME` 取家目录、try/catch 兜底、`ccSessionStateWrite` 幂等标记）；已通过 apply 引擎应用到本机 extension.js（2.1.226，长度 +235 字节、node --check 通过），写盘逻辑用 node 实跑验证通过。
  - [`.claude/hooks/com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) 注释 + [`.claude/skills/trade/references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「多层防护」⑪ 同步更新为「state 文件 + 补丁 012 依赖」说明。
  - **补丁 012 生效前提**：盯盘会话需重载扩展（当前会话用的旧 extension.js 不写盘）——重载前 watcher 走 jsonl fallback、重载后切到精确的 state 判定。测试：四种 state（running/thinking/waiting_input/idle）分支 + None fallback 分支端到端全过；盘中采样在跑时 main 第②步 return 不报。

- **00100 计划仓位下达失败排查闭环：reason 输出 + 开仓失败自动降档重试（2026-08-11）**：8-06 港股 MINIMAX-W 计划仓位 58,400 股无法一次下达（当时脚本只输出「Invalid」、原因未明，AI 手动试 5,000/500/200/120 多次失败才摸到 10,000 可下、仓位缩水至计划 17%）——三个待办点全部落地。**为什么改**：失败原因不暴露、可下量靠盲试，两个缺陷都要从脚本层面消除。**改了什么**：
  - **① 查明被拒原因**：查老虎当日订单历史（`get_orders` 带时间范围）确认 58,400 两次尝试均 Invalid 且 reason 为通用文案（「cannot be canceled」，SDK 对被拒订单无具体原因）；120 股那笔有明确 reason `cross-trading with your pending sell order`（账户已有同标的未成交委托单、新单与它交叉成交被券商自成交防护拒绝；8-06 当时 AI 归因「已有止损挂单」未严格验证，2026-08-11 用户纠正：开仓 = 空仓建仓、与止损单无关）；58,400 空仓时被拒 = 排除 cross-trading，结合同日 01888（名义 ≈1,111 万 HKD、净值 781 万）成功 vs 58,400（名义 ≈1,717 万 HKD、净值 817 万）失败，推算最可能为**单笔名义金额 / 购买力限制**（边界在 1,111 万～1,717 万 HKD 之间），确切上限待盘中实测（降档重试已能自动摸到可下上限，无需再手动试探）。**〔2026-08-16 修正〕**当日「边界 1,111 万～1,717 万、待实测」的推断已由 8-12 实测推翻实锤：被拒根因 = **单标的保证金率约束**（buying_power ÷ long_initial_margin），实测 00100 保证金率 0.75、可买上限 = buying_power 817.5 万 ÷ 0.75 ≈ 1,090 万 HKD 名义 = 37,484 股（买入价 291 档）——既低于 01888 的 1,111 万、也远低于 58,400 股的 1,717 万，旧「边界」推断不成立。2026-08-16 起开仓脚本已内置购买力预检（`get_buying_power_tiger` + 超限主动降档），见当日 CHANGELOG。
  - **② 下单失败处置**：[`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 新增「下单失败处置」节——cross-trading 处置（改单或撤销现有未成交委托单后重新挂单，不降档；开仓 = 空仓建仓、与持仓止损单无关，2026-08-11 用户纠正归因）+ **分批建仓路径经用户纠正后移除**（分批补足 = 同一标的先后两笔同向开仓 = 加仓、违反「禁止加仓减仓」护栏；一次性开仓设止损即可，降档缩水的仓位接受不补足）。
  - **③ 仓位调整流程自动化**：[`scripts/trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `check_order_filled_tiger` 返回 4 元组新增 **reason**（订单 `Order.reason` 字段，被拒具体原因不再丢失，港股美股共用）；[`scripts/open_position_tiger.py`](.claude/skills/trade/scripts/open_position_tiger.py) / [`scripts/open_position_tiger_us.py`](.claude/skills/trade/scripts/open_position_tiger_us.py) 加**失败自动降档重试**——目标量被拒（Invalid / 提交异常）时按降档序列（逐次减半取整手）自动重试到可下上限，每次失败输出失败档 + reason，降档成交输出 `downscaled` / `target_quantity` / `failed_attempts` / `warning`（仓位缩水提示 + 实际 max_loss 对比预算 B）；两种失败不降档（cross-trading 提示改单或撤单重挂 / 挂起超时撤单退出由 AI 决策）；[`scripts/close_position_tiger.py`](.claude/skills/trade/scripts/close_position_tiger.py) / [`scripts/close_position_tiger_us.py`](.claude/skills/trade/scripts/close_position_tiger_us.py) 同步解包 4 元组。
  - 测试：降档序列单测（港股 58,400/lot20 → 12 档、美股 1,000/lot1 → 10 档、边界 0/lot 等）+ 三场景模拟测试（被拒两档后成交 / cross-trading 首档即停 / 挂起超时不降档撤单）全过；8 个脚本 py_compile 通过。

- **美股突破类入场量比硬闸（2026-08-11）**：8-06 复盘发现连续两笔 signal 美股「无量突破」失败——08-05 NVDA（量比 3.1，开仓 7 分钟后突破失败止损 −1.17R）、08-06 MU（量比 0.8，突破 926 后回踩 921 未守住止损 −1.13R），两笔开仓记录的风险区都自己标注了「突破量能不足」仍开仓。**为什么改**：防「明知无量仍开突破单」（与早期 SOXL 假突破教训同型）。**改了什么**：[`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md)「量价正向确认」节后新增「突破类入场量比硬闸」小节——**量比 < 1 不开突破单（无量突破硬闸）/ 1 ≤ 量比 < 2 须板块共振等确认叠加 / 量比 ≥ 2 按正常突破判据**；明确「必要非充分」（NVDA 3.1 达标也失败，量比只过滤「明知无量仍开」、不保证突破必成，突破成败仍由形态 / 位置 / 板块共振决定）；三条边界（早盘量比天然偏高需结合分时量能 / 门槛按标的流动性参照 / 只约束突破类入场、回踩企稳类不受限）。

- **预估赔率打折系数样本积累观察：12 → 20 笔，维持 ×0.5 待 ≥30 重标定（2026-08-11）**：待办「三阶段样本 ≥30 笔重新标定」的中间检查——8-06~8-10 新增 8 笔三阶段完整记录（01888、00100、MINIMAX、胜宏、AAOI、药明、LITE×2，均初始 ≥ 1.2），样本从 12 笔增至 20 笔、**仍 < 30 笔、不重标定**。**为什么改**：新增 8 笔落地/初始 中位数 +0.12、均值 +0.35、6/8 落地 > 0（基线 3/12），兑现度明显高于基线——需记录观察但**不据此调系数**（20 笔仍小样本、新增表现好可能是 ×0.5 门槛 8-06 起执行拦掉边缘单的结果、调松会放回边缘单）。**改了什么**：[`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md)「预估赔率系统性打折」节标定数据后新增「样本积累观察」段（新增样本分布）+ 边界 1 更新（样本 20 笔、系数维持 ×0.5、待 ≥30 笔按合并分布重新标定并更新 SKILL.md 门槛口径）。

- **watcher 参数再修正：无输出间隔 = 采样段 40s，阈值 50s / 间隔 10s（2026-08-11 用户纠正）**：上一轮定参（阈值 35s / 间隔 25s）把「分析时间」误当成「无输出时间间隔」——用户纠正：**25s 是最大分析时间，不是没有输出内容的时间间隔**；「无输出内容的时间间隔」= 采样段运行的时长（采样脚本跑 40s 期间 AI 回合结束、jsonl 停更、无输出），正常最少 ≈ 40s（40s 采样段 + 段启动开销）。**为什么改**：阈值 35s < 正常无输出 40s，会把正常采样段误报为中断（严重误报源）。**改了什么**：
  - [`.claude/hooks/monitor_watcher.py`](.claude/hooks/monitor_watcher.py) 判定阈值 `STALE_SECONDS` 35 → **50s**（> 正常无输出 40s+，留 ~10s 余量防误报正常采样段；注释重写概念依据——无输出间隔 = 采样段时长，分析时间不作依据）。
  - [`.claude/hooks/com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) 检查间隔 `StartInterval` 25 → **10s** → 最坏检测延迟 = 阈值 + 间隔 = 50 + 10 = **60s**，恰好满足「真实中断 ≤ 60s」。已重新部署 + `launchctl unload/load`，实测生效（`plutil` 提取 StartInterval=10）。
  - [`.claude/skills/trade/references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「多层防护」⑪ 恢复说明参数同步为 50s / 10s + 概念澄清。
  - 测试：边界实测通过（停更 40s 正常不报 / 49s 正常 / 51s 中断 / 60s 中断；阈值语义 `> 50` 才报）。

- **watcher 参数数据实测定参 + 仅港股守护（2026-08-11 用户立）**：在恢复 watcher（按会话独立守护）基础上，用户指出两点：① 真实中断检测延迟 = 检查间隔 + 判定阈值，要求**真实中断 ≤ 60s**（原 120s 间隔 + 60s 阈值最坏 180s 太长）；② **美股时段不守护**——美股盘中（夏令时北京 21:30-次日 04:00）用户在休息、不希望被打扰。**为什么改**：先做数据分析——实测今日盯盘 log（`tmp/monitor_log_HK_09988_20260811_signal.csv`）段间分析间隙（采样段结束 → AI 分析 → 重启下一段）166 个：均值 9.3s、中位数 9s、最大 25s（另有 1 个 533s = 09:46 断链的真中断形态，与正常间隙完全可分）。**改了什么**：
  - [`.claude/hooks/monitor_watcher.py`](.claude/hooks/monitor_watcher.py) 判定阈值 `STALE_SECONDS` 60 → **35s**（> 最大正常间隙 25s，留 10s 余量防误报正常分析间隙；注释写明数据依据）；时段判断改为**仅港股盘中**（09:30-12:00 / 13:00-16:00），删美股时段（美股不守护）。（注：本轮参数 35s/25s 随后被下一条「阈值 50s / 间隔 10s」修正——35s 误把分析时间当无输出间隔。）
  - [`.claude/hooks/com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) 检查间隔 `StartInterval` 120 → **25s**。已重新部署 + `launchctl unload/load`，实测生效。
  - [`.claude/skills/trade/references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「多层防护」⑪ 恢复说明同步。
  - 测试：时段判断全场景实测通过（港股盘中 True / 午休 False / 午市 True / 美股时段 22:00 与凌晨 03:00 False）。

- **恢复密采样外部 watcher：按会话独立守护 + 60 秒判据（2026-08-11 用户立）**：恢复 2026-08-10 撤销的 launchd 外部守护 watcher（`com.daytrading.monitor-watcher`），判定逻辑重写。**为什么改**：用户指出旧逻辑两个缺陷——① 只 `pgrep monitor_segment.py`、不认 ws_segment / futu_ws_segment（2026-08-10 撤销的根因）；② 更本质：检查瞬间若正好是「段结束 → AI 分析 → 重启下一段」的间隙，采样进程短暂不存在，只看进程必然把正常节奏误判为中断。用户立新逻辑：**按会话独立守护**（盯盘会话可能不止一个，会话 A 中断不能被会话 B 活跃掩盖）+ **60 秒判据**（正常无输出 ≈ 采样 40 秒，给 20 秒冗余，超 60 秒无输出 = 大概率中断）。**改了什么**：
  - [`.claude/hooks/monitor_watcher.py`](.claude/hooks/monitor_watcher.py) 重写为**注册制 + 按会话判定**：只检查 `tmp/monitor_sessions.txt` 注册列表里的盯盘会话（每行一个 `CLAUDE_CODE_SESSION_ID`），对每个会话独立判定其 jsonl（`~/.claude/projects/<项目>/<session_id>.jsonl`）停更 > 60 秒 → 该会话中断 → 系统通知（合并所有中断会话为一条）；停更 ≤ 60 秒 → 正常。**不查采样进程**（采样进程不区分会话；jsonl 停更 > 60s 已覆盖全部中断形态，连跑多段违规也会被报）。
  - **注册**：[`scripts/preflight.py`](.claude/skills/trade/scripts/preflight.py) 末尾新增自动注册——盯盘启动必跑 preflight，把本会话 `CLAUDE_CODE_SESSION_ID` 写入 `tmp/monitor_sessions.txt`（去重），零额外负担；**注销**：新建 [`scripts/monitor_unregister.sh`](.claude/skills/trade/scripts/monitor_unregister.sh)，停盯收尾从注册文件移除本会话（正常停盯不误报）。
  - [`SKILL.md`](.claude/skills/trade/SKILL.md)「停盯第一个动作」改为「解除防睡眠 + 注销盯盘会话」（跑 off.sh + monitor_unregister.sh）。
  - [`.claude/hooks/com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) 恢复（launchd 每 120 秒触发、RunAtLoad、盘外自动跳过），已部署 `~/Library/LaunchAgents/` + `launchctl load` 加载，实测 `launchctl kickstart` 退出码 0。
  - [`.claude/skills/trade/references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「多层防护」⑪ 撤销记录追加 2026-08-11 恢复说明（按会话 + 60s 内容）。
  - 测试：完整闭环实测通过（preflight 自动注册 → 活跃会话判定正常 → 设 jsonl 停更 120s 判中断 → 注销后不再守护；多会话独立判定——活跃会话不报、停更会话报出）。另修两处脚本 bug：注册文件路径多算一层（写错到 `.claude/tmp/`）、`$变量` 后跟中文标点被 bash 破坏（改 `${变量}` 花括号写法）。

- **修复 monitor_segment 止损价查询取到已作废订单（2026-08-11）**：[`scripts/monitor_segment.py`](.claude/skills/trade/scripts/monitor_segment.py) 的 `query_stop_prices` 此前不过滤订单状态、匹配到第一个订单即 break——8-10 美股盯盘实测：监控 log 的 `stop_price` 列全天显示 846.00（开仓前作废测试单 STP @846 Invalid 的触发价），实际止损已先后移损到 840 / 836，stop_price 列与实际完全脱节（依赖该列判断止损位会误判）。**为什么改**：8-11 用户问「第一笔是否触发止损」排查时定位到该 bug（用户观察到分钟K穿越 840，与 log 显示止损 846 不一致）。**改了什么**：只认**活动状态**（排除 Filled / Cancelled / Expired / Inactive / Invalid 等已结束订单）的 STP 单；同标的多个活动止损单时取 order_id 最大（提交最晚 = 最新）的那个；匹配逻辑从「匹配即 break」改为「匹配 + 状态/类型/触发价校验全部通过才 break」。实测验证：真实账户数据下 LITE 已无活动止损单 → 返回空（修复前会返回已结束单的 846）；模拟「活动 840 + 已结束 836/846 并存」→ 正确返回 840；模拟「双活动单」→ 取 id 大者。
- **美股富途逐笔每秒采样实测 + 排进盯盘流程（2026-08-11）**：排查过程中盘外实测富途 OpenD `subscribe(['US.LITE'], [TICKER])` 返回 ret=0、收到逐笔回调（收盘最后一笔 16:00:01 813.51）——富途**接受美股 TICKER 逐笔订阅**，机制上美股可用 `futu_ws_segment.py` 做逐笔每秒采样（此前美股密采样用 10 秒快照，只因「美股无老虎行情权限」的妥协，不是最优）。**为什么改**：用户要求「把实测排进下次美股盯盘流程」——让美股密采样升级为每秒采样（对齐「密采样每秒一个价格」规矩）有据可依、不用每次临场摸索。**改了什么**：[`references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「每秒采样」节美股条目追加「美股逐笔每秒采样（2026-08-11 实测可用，待盘中验证后转正）」——每次美股盯盘启动必做一次逐笔实测（subscribe 候选标的 TICKER 验证返回 0 + 跑一段 futu_ws_segment.py 看推送是否每秒稳定有点），实测稳定则升级为逐笔每秒采样、否则维持 10 秒快照兜底并记录；[`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 8 条美股采样句同步挂上「启动做逐笔实测、稳定则升级」的说明。

### 移除

- **撤销密采样外部 watcher 系统通知（2026-08-10 用户立）**：盘中盯盘时系统右上角横幅 + 响铃频繁误报「盘中 monitor_segment 未在跑！密采样可能被停/降频」，每次提示时实际都在正常密采样。**根因**：港股密采样自 2026-08-07 起改用 `ws_segment.py`（老虎 WebSocket 每秒推送），watcher 仍只 `pgrep -f monitor_segment.py`——港股盯盘跑 ws_segment 时必然误判「monitor_segment 未在跑」→ 发系统通知（osascript 横幅 + Basso 响铃）误报。用户立令撤销该提示功能。**改了什么**：① 卸载 launchd 服务 `com.daytrading.monitor-watcher`（`launchctl unload`，已验证从 `launchctl list` 移除）；② 删除本机部署文件 `~/Library/LaunchAgents/com.daytrading.monitor-watcher.plist`（不删则重启机器后 launchd 自动重新加载、功能复活）；③ 删除项目内 [`.claude/hooks/monitor_watcher.py`](.claude/hooks/monitor_watcher.py) 与 [`.claude/hooks/com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist)（git 跟踪文件，删除后由用户 /commit 确认）；④ [`.claude/skills/trade/SKILL.md`](.claude/skills/trade/SKILL.md) 清理两处引用——preflight 启动序列去掉「启动盯盘同时 launchctl load watcher」、「停盯总结」段删除「密采样 watcher 随盯盘开关」整段（启动 load / 停盯 unload 动作随功能撤销一并废止）；⑤ [`.claude/skills/trade/references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「多层防护」12 层 → 11 层（删原第⑪层外部 watcher、原⑫用户监督改⑪），附撤销原因说明。**为什么改**：watcher 的检测逻辑与当前采样体系脱节（只认 monitor_segment、不认 ws_segment/futu_ws_segment），误报已失去「外部监督」价值且扰民；外部系统通知防线的空缺由「用户监督」层覆盖（盯盘时用户留意段结束连续性指标 + 段间节奏）。触发：用户「盘中盯盘的时候系统右上角的横幅加响铃的疑似没有密采样的提示功能撤销掉，每次提示我看都在正常密采样」。

### 新增

- **实盘账户排查铁律：排查问题必须先用模拟账户（2026-08-10 用户立）**：8-10 盘中排查下单机制（00100 两次 MKT 大单 EXPIRED → 100 股小单测试）花了约 -239 HKD 测试成本（模拟账户）；用户指出「还好是模拟账户」并立规——**以后接入实盘账户也遇到这种情况，必须先用模拟账户排查问题；若模拟账户也排查不出问题，则暂停盯盘、请求用户在实盘排查问题的许可**。**改了什么**：[`.claude/skills/trade/references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md)「操作原则（auto 专属）」第 5 条（盘中排查用开仓单测试后必须立即平仓）末尾追加「实盘账户排查铁律」——实盘场景下盘中排查一律先走模拟账户复现 / 验证机制，排查清楚后再在实盘按正常流程操作；模拟账户排查不出 → 暂停盯盘、经用户明确同意后才允许在实盘排查（未经许可不得自行在实盘用测试单排查）。**为什么改**：实盘测试单有真实资金损失风险，模拟排查是零成本验证手段，必须先穷尽模拟手段再考虑实盘，且实盘排查必须用户授权兜底（2026-08-10 测试单 -239 HKD 教训的实盘版防护）。触发：用户「以后如果接入实盘账户也遇到这种情况必须先用模拟账户来排查问题，如果模拟账户也排查不出问题则暂停盯盘请求用户在实盘排查问题的许可」。

- **反面教训沉淀：突破后滞涨衰竭必须主动平仓（2026-08-10 用户复盘）**：8-10 盘中药明康德做多（入 200.4、损 199.4）突破前高 202.6 时浮盈 2.4R，移损 201.3（锁 0.9R）后未主动平仓，突破后 5-10 分钟无法续创新高（峰值 203.2）+ 买卖比持续负（-5~-24）= 动能衰竭信号已现，09:57 止损单被动离场落地仅 +0.48R（少锁约 1.3R ≈ 12 万）。用户复盘指出「浮盈冲到 2.4R 没主动平仓锁利」。**改了什么**：`.claude/skills/trade/references/trading-strategy.md`「移动止损唯一用法」节新增反面教训——**与 8-07 00100（趋势健康机械锁利放跑 13.35R）的边界**：分界 = 突破后动能是否延续（续创新高 + 买盘维持 → 让利润奔跑；突破后 5-10 分钟无法续涨 + 买卖比持续负 → 主动平仓锁利、不等止损触发）；移损锁「万一」下限、主动平仓锁「动能衰竭时当前利润」，移损 ≠ 锁利完成。**为什么改**：把「主动平仓兜底」（SKILL.md 移损前自查清单③）从原则细化出可操作的量化触发点，防「把移损当锁利完成」重犯。：8-10 盘中开仓 00100（39,080 股）两次 MKT 大单均 EXPIRED（盘口深度不足未成交），用 100 股小单排查机制时测试单误成交——用户立规：盘中排查用开仓单测试，成交后必须立即平仓，排查完不留测试仓位过任何时段（测试仓位暴露在价格波动下有资金损失风险）。**改了什么**：`.claude/skills/trade/references/auto-mode.md`「操作原则（auto 专属）」新增第 5 条——排查测试单成交后立即调用平仓脚本平掉、标注「作废测试单」、不构成正式交易。**为什么改**：2026-08-10 实测暴露测试仓位风险（100 股测试单成交 @330.6，若忘平仓，价格波动会放大损失；本次平掉 @329.4 亏约 -239 HKD 测试成本）。

- **信号模式盯盘开仓市值 10 倍杠杆上限（2026-08-08 用户立）**：开仓市值（= 数量 × 开仓价）不得超过 `equity × max_leverage`（config 默认 10 倍）——权益 10 万 → 最高开仓 100 万市值。与 f_max 是两套独立约束：f_max 限 max_loss（风险敞口）、max_leverage 限开仓市值（名义敞口），选仓位时两者同时满足。**改了什么**：
  - [`config.json`](.codebuddy/skills/trade/config.json) / [`config.example.json`](.codebuddy/skills/trade/config.example.json) 新增 `risk.max_leverage: 10`（含注释）。
  - [`scripts/trade_utils_tiger.py`](.codebuddy/skills/trade/scripts/trade_utils_tiger.py) `calc_position_size` 增加市值杠杆约束：新增可选参数 `entry_price` / `max_leverage`（max_leverage 缺省自动读 config），选仓位按「双约束上界」（max_loss 上界 = equity×f_max÷止损距、市值上界 = equity×max_leverage÷开仓价，取较小者）夹逼，cap 压下来时退到上限内最大整手档；不传 entry_price 时行为与旧版完全一致（向后兼容）。
  - [`scripts/open_position_tiger.py`](.codebuddy/skills/trade/scripts/open_position_tiger.py) / [`scripts/open_position_tiger_us.py`](.codebuddy/skills/trade/scripts/open_position_tiger_us.py) 自动算仓位时传 `entry_price=entry_ref` 启用市值约束。
  - [`scripts/preflight.py`](.codebuddy/skills/trade/scripts/preflight.py) / [`scripts/resume.py`](.codebuddy/skills/trade/scripts/resume.py) 输出「开仓市值上限 = equity × N 倍杠杆」。
  - [`SKILL.md`](.codebuddy/skills/trade/SKILL.md) 硬性护栏第 1 条重写为「单笔预算与 max_loss + 开仓市值杠杆上限」+ 6 要素第 5 条 / 开仓前自查清单同步补市值检查；[`references/risk-management.md`](.codebuddy/skills/trade/references/risk-management.md)「选仓位」新增市值杠杆上限节；[`references/signal-mode.md`](.codebuddy/skills/trade/references/signal-mode.md) 信号模式 AI 职责边界 + signal 专属自查补市值上限。
  - **为什么改**：用户要求信号模式盯盘加 10 倍杠杆开仓上限（权益 10 万 → 最高开 100 万市值），防止高杠杆标的（2x/3x ETF）或窄止损距下名义敞口失控。

- **美股开仓主单改市价单 MKT（2026-08-07 盘中实测发现并修复）**：美股开仓脚本（`trade_utils_tiger_us.py` 的 `submit_order_with_stop_us` / `open_position_tiger_us.py`）主单此前一直是 LMT 限价单——港股版 2026-08-07 已改 MKT（MINIMAX 当日 5 次 LMT 开仓全 Invalid 教训），美股版未同步。2026-08-07 盘中实测复现：MU 做空开仓，LMT 主单挂 bid 859.27，价格快速下探（859.08→857.99）8 秒超时未成交、脚本撤单开仓失败——限价单 + 8 秒超时撤单与高波动盘口不匹配，市价单立即成交、附加止损腿同一次提交无裸奔空窗。**改了什么**：`submit_order_with_stop_us` 主单 `"LMT"` + `limit_price` 改为 `"MKT"`（`submitted_price` 参数保留作参考、不再作限价），docstring 记录实测案例；`open_position_tiger_us.py` 的 `lo_price` 注释与 `method` 输出字段同步改为 `market+attached_stop`。**为什么改**：与港股版同日改造对齐，消除高波动标的开仓错过成交的机制缺陷。

- **复盘必含「港美股表现差异分析」步骤（2026-08-07 用户立）**：8-07 复盘后用户要求把港美股差异归因沉淀为复盘固定步骤——港美股同源样本、同一执行规范、仅市场不同，方向分化（港正 +0.544R / 美负 −0.287R）超出噪音范畴，不拆解归因就无法把「市场差异」与「策略错误」分开。**改了什么**：
  - [`reviews/2026-08-07-review.md`](reviews/2026-08-07-review.md) 末尾新增「十三、港美股表现差异分析」节，按固定五步实算：① EV 差距反事实分解（把美股 p / R_W / R_L 逐个替换为港股值重算——0.831R 总差距 = 胜率差 +0.418R（50%）+ 胜赔差 +0.227R（27%）+ 败赔 +0.001R（0%）+ 交互 +0.185R（23%）——**败赔率两边几乎相同，edge 完全来自盈利端**）；② 多空方向拆解（港股多头 70% 胜率是盈利主力、美股空头 5 笔 0 胜全败是胜率差直接来源）；③ 美股剔除 3x 杠杆 ETF（SOXL/SOXS）稳健性（剔除后 EV −0.287 → −0.150R，「押注半导体下跌」5 笔敞口 −2.79R 占美股亏损 65%，是主要拖累非全部原因）；④ 港股剔除 Top3 大胜单稳健性（+0.544 → +0.242R 仍正，edge = 基础胜率 + 大胜单各半）；⑤ 赢单结构对比（美股最大赢单仅 +1.056R、无大胜单，R_W 低的核心信号）。末尾给 caveat 与按「贡献度 × 可控性」排序的可执行改进方向（暂停看空半导体敞口 / 美股突破量比硬闸 / 港股让利润奔跑 / 优先攒美股样本）。
  - [`references/review-and-evaluation.md`](.claude/skills/trade/references/review-and-evaluation.md)「每次复盘必含港美股分开统计」条目（2026-08-07 复盘时立、当时未记 CHANGELOG，本次一并闭环补记）末尾并入「港美股表现差异分析」必含小节——固定五步（同上）+ 每步实算表与解读 + caveat + 可执行方向，脚本参考 `tmp/hk_us_breakdown.py` 08-07 版。
  - **为什么改**：8-07 报告原有「港美股分开复盘」只罗列两市场统计量、未量化差异成因；本次把反事实归因定为必含步骤，防止以后复盘「见差异不归因」。

- **移动止损规则修订：用法 A 作废，止损永远放趋势反转位（用法 B 形态）+ 主动平仓兜底（2026-08-07 用户立）**：8-07 00100 做多浮盈 4.65R 时按用法 A 移损 330（锁 2.25R），小回撤触发后落地 1.96R——随后标的继续冲到 374.4（**13.35R 高点**），锁 1.96R 放跑 13.35R。用户指出：一次大赚（10R+）能抵消非常多次小的利润回吐（1-2R），用法 A 紧锁利把尾部大利润系统性截断，而尾部大利润正是日内交易 EV 的主要来源（08-06 复盘：≥2R 大胜占总 R 和 170%）；「停止盯盘 / 动能衰竭 / 任何情况不妙 → 主动平仓」这一兜底已覆盖「利润全回吐」的漏洞，紧锁利没有必要。**改了什么**：
  - [`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md)「止损单的两种用法」整节重写为「移动止损唯一用法：止损永远放趋势反转位（用法 B 形态）」——止损永远放 K 线结构位（前低/支撑、前高/阻力、VWAP）并随趋势演进上移；新增「用法 A 已作废」节（8-07 00100 教训 + 7-08 SOXL 保本教训保留合理内核重写 + 8-06 MINIMAX 教训重新解读：错在结构位没随趋势上移、不在放结构位）；「锁利大小动态权衡」节改为「锁利位 = 最新结构位，锁多少由现价到结构位距离决定」；总原则、3x ETF 节、震荡市策略节同步去用法 A 引用。
  - [`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 5 条重写「两种用法」段 + 移损前自查清单（2026-08-07 修订：① 实算浮盈几 R；② 止损是否最新趋势反转位（非旧位/百分比/噪音区）；③ 是否需要主动平仓（停盯/动能衰竭/情况不妙）；④ 移损信号写明浮盈 XR + 止损位依据 + 是否主动平仓）。
  - [`references/signal-mode.md`](.claude/skills/trade/references/signal-mode.md) 移损信号表格「用法 A/B」栏改「止损位：趋势反转位（附依据）」+ 自查清单同步修订。
  - [`references/risk-management.md`](.claude/skills/trade/references/risk-management.md) 止损本质段去「两种用法」表述，改「开仓与持仓同一原则：止损永远放趋势反转位、随结构上移」。
  - **为什么改**：8-07 00100 实操暴露用法 A 的本质问题——用止损单冒充止盈（止盈本该主动决策：动能衰竭/目标位/时间窗），该拿的时候拿不住、该锁的时候被动等触发；用户数学论证后拍板废除用法 A。

- **每秒采样规则：港股密采样固定每秒一个价格（2026-08-07 用户立）**：8-07 00100 持仓止损 330 触发时，10 秒快照只见 330.6、实际低点 329.4——10 秒间隔漏掉瞬时高低点，止损触发判定失真（用户指出「你要取每秒的价格看」并立规「以后密采样都要使用每秒一个价格数据的采样频率」）。**改了什么**：
  - **新建 [`scripts/ws_segment.py`](.claude/skills/trade/scripts/ws_segment.py)**：老虎 PushClient WebSocket 每秒推送价格流的 40 秒分段采样脚本（2026-08-07 盘中实测每秒 3-4 条推送）——段结束输出段高/低/末价 + 最近 12 点 + 破关键位告警；log 命名对齐 monitor_segment（`tmp/monitor_log_{code}_{date}_{mode}.csv`，monitor_guard 守卫兼容）；连接坑记录（props_path 必须 expanduser，否则配置全空 access forbidden）。
  - [`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 8 条改「40 秒一段、每秒一个价格」——港股用 ws_segment.py；美股无老虎行情权限仍用富途 monitor_segment.py 40 秒/10 秒兜底；不降频规矩（不放稀/不 sleep/不 && 连跑/不 cron 绕过）对 ws_segment 同样生效。
  - [`references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「采样频率」节新增「每秒采样（港股主力）」小节（为什么/行为与分工/美股边界）+ 脚本库加 ws_segment.py 条目。
  - **边界**：WebSocket 只推价格流，买卖比/量比/成交额/VWAP 段末由 AI 调富途 snapshot/monitor_summary 补齐（VWAP 锚点照常）。

- **2026-08-06 全量交易复盘（净口径，N=50）**：用户「复盘所有交易记录」触发——遍历 `signals/` + `actions/` 两目录全部记录、与 `2026-08-05-trades.csv`（N=45）逐日对账后**新增 5 笔**（08-05 美股 NVDA 补录——上期复盘后发生 + 08-06 美股 MU + 港股 01888 auto + 00100 auto/signal 两笔独立交易），N 45→50，EV +0.191→**+0.209R**、胜率 51.1%→52.0%、P(EV>0) 82.9%→**85.9%**（区间 80~90%、仍未确认）、频率派 CI [−0.171, +0.589] 仍跨 0。**核心发现**：① EV 五期连降后首次回升、全部来自 01888 单笔 +3.08R（项目单笔最大盈利，模拟盘大仓位单），剔掉后新增 4 笔合计 −0.234R——均值被大胜主导格局未变；② **连续两笔 signal 美股无量突破失败**（NVDA 量比 3.1 / MU 量比 0.8，共 −2.30R，两笔都自己标注量能不足仍开仓）——已记 TODO「美股突破类入场加量比闸」；③ **00100 auto 计划仓位 58,400 股仅成交 10,000**（cross-trading 被拒 + 单笔可下上限未二分穷尽，max_loss 仅预算 34%）——已记 TODO「仓位下达失败排查」；④ **01888 动作记录费用笔误**（双边费 4,356 vs 18bps 实算 40,702.5，差 9.3 倍，净盈亏 383,144 vs 346,797.5）——本复盘按 18bps 统一口径实算 3.08R，请用户回查动作记录源头。**改了什么**：新增 [`reviews/2026-08-06-review.md`](reviews/2026-08-06-review.md)（含三阶段赔率、7 张序贯演化图、科学选 f 四步、多层评估）+ [`reviews/2026-08-06-trades.csv`](reviews/2026-08-06-trades.csv)（50 笔全量样本）+ 7 张演化图 png（`bayes_evolution.py --date 2026-08-06` 一键生成）；科学选 f 结论维持 f=2% 不释放（P(∑₄₀Y≥0) 最高 75.9%、所有 f <95% 约束不可达、尾部硬上限 10%）。

- **移损前自查清单：浮盈 ≥ 2R 必须用法 A 紧锁利（2026-08-06 立，MINIMAX 0.61R 教训）**：8-06 MINIMAX 做多浮盈 2.0R（价格 307.2）时误用「放趋势反转位」思路远移损到 300（锁 0.875R），价格从 308 阴跌到 300 下方触发止损、落地仅 0.61R——规则（trading-strategy.md 用法 A/B）一直在文件里，但决策时没强制对照：≥2R 本该用用法 A 把止损移到靠近现价的盈利区（如 305 附近、锁 ~1.5R），两用法混用 = 放弃「大概率锁大利润」只留回吐风险。**改了什么**（三处配套，防再犯）：
  - [`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 5 条补「移损前自查清单」——发移损信号 / 执行移损前必过：① 实算当前浮盈几 R；② 浮盈 ≥ 2R → **必须用法 A**（止损移到靠近现价的盈利区），禁止用远移损代替；③ 浮盈 < 2R → 用法 B（放趋势反转位）；④ 移损信号写明「浮盈 XR + 用法 A/B」留痕。两用法不可混用。
  - [`references/signal-mode.md`](.claude/skills/trade/references/signal-mode.md) 移损信号表格加必填「浮盈 +XR 倍率」与「用法 A/B（附依据）」两字段 + signal 专属自查条目加「移损前自查清单」。
  - [`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md) 用法 A 详解后加 2026-08-06 MINIMAX 反面教训（2.0R 误用远移损 → 落地 0.61R；若用法 A 紧锁利可锁 ~1.5R；根因 = 两用法混用；教训 = ≥2R 必须用法 A、移损前实算浮盈 R 数按 R 数定用法）。
  - **为什么改**：与 2026-07-28 兆易「规定在 skill 里却没调用」同款根因——规则存在但决策时无强制对照；自查清单（决策时躲不开）+ 信号留痕（复盘可核对）+ 案例记忆（相似局面有参照）三层配套，把「自觉记住」变成流程约束。触发：用户问「浮盈锁利 2.0R 为什么落地只有 0.61R」→ 排查发现移损违反用法 A 规则 → 用户要求保证下次不忘记。

- **盘前→开盘唤醒衔接机制（2026-08-05 立，09:24→09:30 教训）**：盘前准备完成（09:24）后回合结束，09:30 开盘 AI 未自动醒来盯盘、09:40 用户手动提醒——AI 是请求-响应式、回合结束即静止，唤醒源只有用户新消息 / task-notification / 定时唤醒，盘前没启动 monitor_segment + 没安排定时唤醒 = 开盘无人叫醒 AI。**改了什么**：① [`references/monitoring.md`](.claude/skills/trade/references/monitoring.md)「盘前启动只预热」节新增开盘唤醒规则——盘前启动的盯盘，准备完成（preflight + 方向研判 + hot_list）后距开盘 ≤ 15 分钟，立即提前启动 `monitor_segment.py` 盯候选标的（带关键位、`--mode` 按 session 模式传），段结束 task-notification 从盘前持续驱动 AI 醒来重启下一段、跨开盘自然进盘中；盘前段 AI 只重启下一段 + 一句话汇报（含距开盘时间）、不做完整三层分析；② [`SKILL.md`](.claude/skills/trade/SKILL.md) 硬性护栏第 7 条同步补该规则；③ **ScheduleWakeup 定时唤醒方案实测否定**（2026-08-05 实测：非 /loop 上下文调用直接报「Wakeup not scheduled. Either the /loop dynamic runtime gate is off」）——monitoring.md「采样链防中断」备份唤醒条目从「待实测」更新为「已实测不可用、备份唤醒仅存于 /loop 场景、普通盯盘回退核心靠 AI 自检 + 大动作复查」+ 盯盘启动清单不再尝试。触发：TODO 待办「盘前→开盘唤醒衔接机制缺失」完成（用户「为什么没准时唤醒？排查原因，解决问题」）。

- **预估赔率系统性打折规则（2026-08-05 三阶段样本标定）**：复盘三阶段赔率反复暴露「预估乐观、落地打折」——2026-08-03~08-05 全部可配对三阶段记录（12 笔初始 ≥ 1.2）实算：75%（9/12）落地 ≤ 0、落地/初始 比值中位数 −0.22 均值 −0.08、平均落地 ≈ 打平（初始均值 2.69R、落地均值 −0.11R）——按 1.2 门槛放行的大批仓位实际落地为负，门槛形同虚设。**改了什么**：① [`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md)「开仓基准」改写——「初始预期赔率 ≥ 1.2」改为**打 ×0.5 经验折扣后**口径（等价初始预期赔率 ≥ 2.4 才开仓）+ 新增「预估赔率系统性打折」节（标定数据、×0.6/×0.5/×0.4 敏感性、四条边界：系数是 12 笔保守粗标定待 ≥30 笔重新标定 / 打折是收紧出手不是保证赢 / 与「目标止盈必须大概率可达」互补 / 不动止损止盈定法与仓位计算）；② [`SKILL.md`](.claude/skills/trade/SKILL.md) 三处同步（6 要素第 3 条、操作原则第 1 条两道闸、开仓前自查清单 EV 判据）。触发：TODO 待办「预估赔率打折系数」完成（用户「把 TODO 里没完成的待办全部完成」）。

- **震荡市策略失效应对规则（2026-08-04 复盘假设落地）**：8-04 复盘（N=38）账面正 EV（+0.22R）全靠 4 笔 ≥2R 大胜撑起（占总 R 和 170%、剔除 top2 后剩 36 笔 EV≈0），8 月以来近 10 笔 EV −0.29R、胜率 30%——策略本质是「趋势日 / 暴跌顺势大胜捕获器」、震荡 / 无趋势市况天然不适配。**改了什么**：[`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md)「震荡市策略」节后新增「震荡市策略失效应对」节——三件事：① **前置识别趋势日 vs 震荡日**（monitor_summary 行情性质判别 + VWAP 方向）：震荡日出手频次减半、只做区间边沿 + 高置信 + ×0.5 折扣后赔率仍厚的交易、其余空仓观望，趋势日维持正常出手；② **改进小赚小亏出场避免扣费转负**：港股窄止损（止损距 < 1.5%）单从严（折扣后赔率仍过门槛才做）、浮盈 < 2R 用用法 B 放趋势反转位让利润奔跑（保本就平 = 毛盈利被费吃光）、接近止损按纪律走不拖延；③ **趋势日敢捕捉**（防识别错方向）：趋势日确认后按正常框架积极开仓，不在趋势日里套震荡日小心尺度。边界：假设待样本验证（结构性克制而非已证伪）、减少出手 ≠ 停手、窄止损从严不动止损位定法。触发：TODO 待办「震荡市策略失效应对」完成。

- **2026-08-05 撤销 todo-skill、待办管理改回「项目根 TODO.md / TODO-archive.md」文件制**：用户决定撤销当日刚立的 todo-skill——待办管理不需要专门 skill，方法论直接写进全局 CLAUDE.md 即对所有项目生效；数据文件放项目根（项目通用位置、不依赖 skill 加载），省去维护 skill 本体及各项目副本的负担。**改了什么**：
  - **待办数据回迁项目根**：`.claude/skills/todo/TODO.md`（10 条未完成）+ `.claude/skills/todo/TODO-archive.md`（29 条归档）移回项目根 `TODO.md` / `TODO-archive.md`，文件头说明同步更新（「todo-skill 管理」→「项目根文件制」，见全局 CLAUDE.md 待办节）；`TODO.md` 只放未完成、已处理归档保留可回溯的规矩不变。
  - **删除 todo-skill**：本项目副本 `.claude/skills/todo/`（含 SKILL.md）删除；全局 `~/.claude/skills/todo/` 与 CapabilityManagerAgent 镜像 `claude/skills/todo/` 同步删除（SKILL.md 方法论已并入全局 CLAUDE.md 待办节、无信息丢失）。
  - **修订全局 CLAUDE.md**：「待办（todo）管理走 todo-skill」节改为「待办（TODO）管理：项目根 TODO.md 与 TODO-archive.md」——数据文件放项目根、方法论由本节直接规定（不设 skill、不依赖加载）；记录时间戳精确到分钟、前后矛盾以最新时间戳为准、做完改动闭环归档、不删已处理条目、敏感信息占位符等核心规矩全部保留。
  - **为什么改**：todo-skill 制多一层机制（skill 本体 + 各项目副本需维护、数据文件藏在 `.claude/skills/todo/` 不如项目根直观）；改回项目根文件制后规矩不减（全局 CLAUDE.md 对所有项目生效）、数据文件回到项目根通用位置。触发：用户要求「把全局和项目级别的 todo skill 撤销，改成把 TODO.md 和 TODO-archive.md 放在项目根，再把关于 TODO 的规定写进全局 CLAUDE.md」。

- **2026-08-05 防「一条命令连跑多段降频」三层机制（用户纠正后立）**：盯盘时 AI 误把「单段结束进程归 0」当采样链断链，用一条 Bash 命令 `&&` 连跑 4 段 monitor_segment「减少断链点」——被用户纠正：连跑 = 段间 AI 不醒来分析、段结束通知只在全部段跑完后触发一次 = 等效把采样间隔拉到几分钟 = 降频违规；单段结束进程归 0 本是正常设计（段完成即退出、等 AI 收到段结束通知后重启下一段），真实断链 = 通知到了 AI 没重启。**改了什么**（三层防再犯）：
  - **工具层（硬约束）**：`monitor_guard.py` PreToolUse 加 A4 检测——盘中单条 Bash 命令含 ≥2 次 `monitor_segment.py` 调用（&& 连跑）→ exit 2 阻断 + stderr 说明；docstring 同步补该检测说明。
  - **文档层**：`references/monitoring.md`「不因市况降频」节新增「单段循环是唯一合法形式：禁止一条命令连跑多段」段——含「段结束进程归 0 = 正常不是断链」辨析、禁止 && 连跑规则、唯一合法循环（单段 40 秒 → 段结束通知唤醒 AI 分析 → 重启下一段）、自查法（段输出含多个「分段结束」标记 = 连跑违规）；`SKILL.md` 硬性护栏 8 的降频形式清单补「不一条命令连跑多段」。
  - **行为层**：段结束自查（防中断三件套 D11）扩展——段输出出现多次 VWAP 检查 / 多个「分段结束」标记即恢复单段循环。
  - **为什么改**：AI 用连跑「修」一个不存在的问题（进程归 0 非故障），造成等效降频；段间 AI 分析是密采样的核心语义（每 40 秒分析一次数据），连跑跳过了全部中间唤醒。

- **2026-08-05 完成 6 项存量待办（待办 1-6：monitor_segment 日期 bug 闭环 / 平仓 --mode / bayes 路径 / mfe·mae 记录 / 两条策略教训）**：用户要求「把能做的待办做掉」，6 项全部落地（明细见 TODO-archive.md 完成条目）：
  - **monitor_segment.py 日期处理 bug（确认闭环）**：`date_str` 未定义 + log 文件名日期跳变两个症状，核查确认修复早已在提交 c58203f（2026-08-04「log 按市场交易日命名」改造）落地——`trading_date_str()` 按市场区分（港股北京日期 / 美股美东交易日）、`date_str` 正确定义、重估提醒与文件名共用同一日期；本条目记录后未及时闭环，本次核查（date_str 定义 [monitor_segment.py:155](.claude/skills/trade/scripts/monitor_segment.py#L155)、log 命名 [monitor_segment.py:162](.claude/skills/trade/scripts/monitor_segment.py#L162)、编译通过）确认无需新改动。
  - **老虎平仓脚本支持 `--mode`（容错）**：`close_position_tiger.py` / `close_position_tiger_us.py` 加 `_parse_args()` 过滤 `--mode`（含 `--mode auto` 后跟值、`--mode=xxx`）——此前误传会把 `auto` 当 quantity 报 ValueError 耽误平仓（2026-08-03 MU 空单教训同款）；6 个单测用例全过。
  - **bayes_evolution.py 支持 `--date`**：加 `_resolve_date()`（`--date YYYY-MM-DD` 指定 / 不传自动找 `reviews/` 下最新 `*-trades.csv`），日期抽成 `DATE` / `CSV_PATH`、7 处输出文件名参数化——此前路径与输出名硬编码 2026-08-04、每次复盘手动 sed；两条路径实跑通过。
  - **平仓原生记录 mfe_R / mae_R（复盘过程指标数据约束方案 b 落地）**：`trade_utils_tiger.calc_position_extremes_tiger`（美股复用别名）从盯盘 log 取当日采样极值近似持仓期间 high/low；两个平仓脚本成交后 `_attach_process_metrics()` 自动输出 `mfe_R` / `mae_R`（+ entry_price / raw_high / raw_low，口径与 review.py 一致：正 MFE_R / 负 MAE_R、止损距 = 开仓价 − 活动止损触发价毛值）；无 log / 缺止损价则不加字段。配套：auto-mode.md 平仓表格加「过程指标」行 + 引用说明、review-and-evaluation.md「⚠️ 数据约束」更新为已落地。单测通过。
  - **trading-strategy.md 新增「多头趋势日：VWAP 上方企稳 = 顺势做多入场点」节**（2026-08-03 SOXL/MU 全程错过教训）：多头趋势日（开盘板块全面站上 VWAP + 持续上行 + 买卖比改善）里 VWAP 上方企稳 = 顺势做多入场点、不等突破也不等深回踩；含与「做多合格入场只有两种」「纪律性不追高」的关系区分（追高 vs 顺势三问）+ 四条边界。按 skill-creator 方法论写入。
  - **trading-strategy.md 新增「跳空暴涨股的反抽遇阻做空 = 高风险形态」节**（2026-08-04 药明康德 + 07709 教训加严）：跳空暴涨（+8% 以上）股多头韧性致价格跌不动、形态对 ≠ 能跌；处理二选一（要求额外确定性才出手 / 干脆不做，默认②）+ 三条边界。按 skill-creator 方法论写入。
  - **为什么改**：6 项均为 TODO.md 存量未完成待办、属「做完改动要闭环」的收尾——全部完成后按 todo-skill 把完成条目移入 `TODO-archive.md`（TODO.md 只剩 4 条真正未完成：盘前唤醒衔接、美股老虎三动作盘中实测、赔率打折系数、震荡市应对）。

- **2026-08-05 todo-skill 落地，待办管理改为「TODO.md 只放未完成 + 归档拆分」**：用户要求专门做一个 todo-skill 管理待办，让「还有哪些没做」一眼全览。**改了什么**：
  - **新建 todo-skill**（权威源全局 `~/.claude/skills/todo/SKILL.md`，本项目副本 `.claude/skills/todo/SKILL.md` 逐字节一致）：方法论含文件结构（`TODO.md` 活跃 / `TODO-archive.md` 归档）、待办条目格式（记录时间戳精确到分钟）、四步使用流程（记录 / 查询 / 完成更新闭环 / 前后矛盾以最新时间戳为准）、边界。按 skill-creator 方法论写（pushy description、Progressive Disclosure、主文件精简）。
  - **待办数据拆分**：原项目根 `TODO.md` 拆为 `.claude/skills/todo/TODO.md`（只放 10 条未完成）+ `.claude/skills/todo/TODO-archive.md`（29 条已完成 / 已更新归档，条目内容原样保留可回溯），项目根 `TODO.md` 移除；校验 39 = 10 + 29 无遗漏。
  - **修订全局 CLAUDE.md「待办（todo）统一存项目根 TODO.md」规矩**为「待办（todo）管理走 todo-skill」：数据文件位置改各项目 `.claude/skills/todo/`；闭环流程由「标记保留在原文件」改为「移入 TODO-archive.md 归档」；原时间戳、最新意图、闭环、不删已处理等核心规矩保留并指向 todo-skill。
  - **同步 CapabilityManagerAgent 开源镜像**：`claude/CLAUDE.md` + `claude/skills/todo/` 与全局逐字节一致（diff -r 验证三部分）。
  - **为什么改**：原 TODO.md 未完成与已处理混在一处，找未完成待办需逐条跳过已处理条目；拆开后 TODO.md 永远只含未完成、「还有哪些没做」一眼全览，已处理条目进归档保留可回溯性。触发：用户要求「专门做一个 todo-skill，TODO.md 放进 todo-skill，已完成 / 已更新单独记录到另一个文件，TODO.md 只放未完成的」。

- **2026-08-04 历史交易复盘（净口径，N=38）**：遍历 `signals/` + `actions/` 全量对账，相对 8-03 基线（33 笔）**补 5 笔漏网**——2 笔美股 MU（8-03 ET 交易日、HKT 跨午夜分写 8-03-ET / 8-04-ET 两文件）、1 笔 NVDA（8-03-ET signals）、2 笔 8-04 港股（药明康德 signal、阿里 auto）；07-29 SOXS 118 股缺平仓价声明跳过、5 笔作废剔除。N 33→38，EV +0.278→**+0.218R**、胜率 60.6%→55.3%、P(EV>0) 87.0%→84.1%、频率派 CI 仍跨 0 [−0.21, +0.65]（edge 未确认）。**核心发现：账面正 EV 靠 4 笔 ≥2R 大胜撑起（占总 R 和 170%、剔除 top2 后剩 36 笔 EV≈0），8 月以来近 10 笔 EV −0.29R 转负**（右偏严重、震荡市失效）。产物 [`reviews/2026-08-04-review.md`](reviews/2026-08-04-review.md) + [`reviews/2026-08-04-trades.csv`](reviews/2026-08-04-trades.csv) + 7 张序贯演化图；[`scripts/bayes_evolution.py`](.claude/skills/trade/scripts/bayes_evolution.py) 输入 / 输出路径由 8-03 改为 8-04（逻辑未动）。可执行改进项（预估赔率打折系数、跳空股做空加严、震荡市应对）记入 TODO.md。触发：用户要求按净口径复盘历史交易。

- **`references/auto-mode.md`——自动交易模式（auto）专属规则（2026-08-01）**：从主 `SKILL.md` 抽出 auto 专属内容（自动交易总则、3 类动作、平仓主动 vs 被动、交易动作脚本、交易动作记录格式、auto 专属硬护栏、执行反馈、auto 专属自查），并从 `account-tools.md` 挪入下单 API 与订单机制（长桥模拟盘交易 API、MIT 止损单定向、主订单 vs 附加订单 + 附加订单三类、港股长桥备选账户、长桥实盘扩展）。auto 模式下按需加载。
- **`references/signal-mode.md`——信号模式（signal）专属规则（2026-08-01）**：从 792e873（2026-07-30 最后一个纯信号版 commit）抽取信号交付方式（信号模式总则、信号时序「响铃最先 → snapshot 取响铃时刻价 → 写文件」、3 类信号格式、信号输出与持仓检查、主动 vs 被动平仓信号边界、用户执行反馈、signal 模式权益更新 equity-log 累加、signal 专属自查）。**三类信号与 auto 三类动作对齐**（开仓 🟢 / 平仓 🔴 / 移损 🟡，禁止加仓减仓），不复活旧 signal 五类（加仓 🔵 / 减仓 🟠）。策略 / 风控 / 形态 / 时段 / 复盘一律引用当前公共 references，不照搬 792e873 旧版（那些已进化成当前 `trading-strategy.md` / `risk-management.md`）。signal 模式下按需加载。

### 变更

- **港股 auto 开仓主单默认改市价单 MKT（2026-08-07 用户立），保留附加止损腿一次提交**：**为什么改**——8-07 早市 MINIMAX（高波动 +30% 标的）5 次 LMT 开仓全部 Invalid：开仓脚本用「LMT 限价单 + 8 秒超时撤单」机制，而快速跳动盘口 10 秒内价格变动 2-4 点，限价单要么被价格甩开、要么 8 秒窗口内未触及即被撤——限价 + 超时机制与高波动标的不匹配，auto 模式对该类标的完全无法入场。用户立规「改用市价单开仓方案」+ 强调「**开仓一定要带附加止损单一起开仓**」（市价主单 + 附加止损腿一次提交，无先开仓后挂止损的裸奔空窗）。**改了什么**：① [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `submit_order_with_stop_tiger` 加 `order_type` 参数（'LMT'/'MKT'）——MKT 不传限价、LMT 传 `limit_price`，两者都保留附加止损腿 `OrderLeg('LOSS')` 一次提交；② [`open_position_tiger.py`](.claude/skills/trade/scripts/open_position_tiger.py) 加 `--order-type lmt|mkt` 参数（**默认 mkt**）——MKT 路径不取盘口 ask/bid、不取整 tick，method 输出 `mkt+attached_stop`；LMT 路径保留原逻辑（`--order-type lmt` 可切回，慢波动标的控价用）；③ [`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 同步 4 处——开仓 🟢 定义补「主单默认市价单」、港股老虎账户段补 8-07 实测状态、「开仓止损用附加止损腿」节改「主单 = 开仓市价单 MKT（默认）/ 限价单」+「老虎附加订单仅开仓限价单支持」更正为「MKT + 附加腿同样支持」（实测证伪旧描述）、auto 专属硬护栏第 2 条「限价单 + 止损条件单」改「市价单（默认）/ 限价单 + 止损条件单」。**paper 实测通过（13:05 午市，MINIMAX 20 股 = 1 手）**：MKT 主单 Filled @349.4 + 附加腿 LOSS @344 落成独立 STP Submitted（与 LMT 方案同机制）→ 平仓 modify 触发价未触发 fallback 撤止损 + MO Filled @348.6、账户干净（顺带清了 5 次失败开仓残留的失效停止单）。实测为纯基础设施验证（开仓即平、无策略意义），不写入 `actions/`（8-03 实测惯例）。触发：停盯总结指出「LMT+8 秒超时与快速波动标的不匹配，建议改用市价单开仓方案」→ 用户拍板「改用市价单开仓方案，但开仓一定要带附加止损单一起开仓」。

- **平仓 fallback race 修复：下 MO 前先复查持仓，防超卖反向开仓（2026-08-07 事故修复）**：**事故**——验证市价单开仓方案时（MINIMAX 20 股 = 1 手，13:05 午市）开仓后调平仓脚本验证，平仓输出「fallback：cancel_stop + MO」Filled @348.6 看似成功，但用户「验证仓位要平掉」复查时发现**账户实际反向开空 20 股**（-20 @347.27）——第一笔平仓根本没平干净。**根因**：平仓主路径 modify STP 触发价=现价（349.0，现价 349.2 取整 tick），盘口一跳跌破 349.0 → STP 已实际触发成交 Sell 20（多头被平），但 `check_order_filled_tiger` 12 秒轮询内 STP 状态仍 Submitted（模拟盘成交状态更新延迟）→ 误判「未触发」→ fallback「撤止损 + MO」→ **MO Sell 20 再卖一次 → 净 -20 空头**。与 2026-08-03「撤止损+MO 反向开空 -36500」同根 race 的残留路径——2026-08-05 主路径（modify 触发价）改造消除了主路径 race，但 fallback 路径仍保留此 race，本次真实复现。**改了什么**：① [`close_position_tiger.py`](.claude/skills/trade/scripts/close_position_tiger.py) / [`close_position_tiger_us.py`](.claude/skills/trade/scripts/close_position_tiger_us.py) `_fallback_cancel_and_mo` 下 MO 前**先复查持仓**——持仓已空（STP 已实际成交平仓）→ 只撤残留止损单、不下 MO，method 标「Filled(by_stop)」；持仓剩余 < 原量 → MO 只平剩余量（quantity_adjusted 标注）；持仓 = 原量 → 原逻辑撤止损 + MO；② [`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 平仓 🔴 定义补「fallback 必须先复查持仓再下 MO」事故说明。**处理**：反向空头 20 股已补平（Buy 20 @345.2，close_position_tiger 无活动止损直接 MO），账户归零确认无残留；本次验证交易（开 349.4 → 平 348.6 → 补平 345.2，纯基础设施验证）不写入 `actions/`（实测惯例）。触发：用户「验证仓位要平掉」→ 复查持仓发现反向空头。

- **美股老虎脚本行情数据源修复 + 三动作盘中实测通过（2026-08-05）**：美股盘中实测（SPY 2 股小仓位端到端）第一步即发现 `trade_utils_tiger_us.get_quote_us` 用老虎 `get_stock_briefs` 取美股行情报 `code=4 msg=4000 permission denied(US market)`——老虎 TBNZ 账户**美股无行情权限**（`account-tools.md`「数据源分工总则」早已记录「美股只能靠富途单源」），美股脚本行情实现与数据源分工不一致。**改了什么**：① [`trade_utils_tiger_us.py`](.claude/skills/trade/scripts/trade_utils_tiger_us.py) `get_quote_us` 改走**富途 OpenD `get_market_snapshot`**（127.0.0.1:11111，字段映射 last_price / bid_price / ask_price / high_price / low_price / update_time），docstring 注明实测依据；② **三动作端到端实测全部通过**（开仓 LMT Filled @773.68 + 附加止损腿 OrderLeg('LOSS') 激活 → 移损 modify aux_price 770.61→771.71 verified → 平仓 modify 触发价=现价、止损单触发 Sell MO Filled @773.44、持仓归零、无残留止损单；平仓过程指标 mfe_R/mae_R 正常输出）——`open_position_tiger_us.py` / `close_position_tiger_us.py` / `move_stop_tiger_us.py` + `trade_utils_tiger_us.py` 的「⏳ 待实测」docstring 全部更新为「✅ 实测通过 + 行情数据源修复」；③ [`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 美股段落实测状态更新（三动作实测通过 + 行情走富途单源说明，下单仍走老虎）。实测为纯基础设施验证（开仓即平、无策略意义），不写入 `actions/`（8-03 实测惯例）。触发：TODO 待办「美股老虎 open_position_tiger_us 下单链路待盘中实测」完成（用户「把 TODO 里没完成的待办全部完成」）。

- **[`tiger-websocket.md`](.claude/skills/trade/tiger-websocket.md) 老虎定位更新：「仅数据源」→「行情 + 模拟账户交易双重角色」（2026-08-05 用户立）**：**为什么改**——用户陈述「老虎现在不是仅作为数据源了，而是授权了模拟账户交易」：auto 模式港股、美股默认下单账户早已都是老虎开放平台模拟（港股三动作 2026-08-03 paper 实测通过、美股 `*_tiger_us.py` 一套 2026-08-05 接入），但本文件权限边界节仍残留「老虎仅作数据源，**未授权交易**」旧描述（其语境来自 2026-07 时代：当时交易走长桥 CLI、老虎只接行情；长桥交易早已撤销）。**改了什么**：① 文件头部（标题下第一段后）新增「老虎不只是数据源」总定位段——同时是 auto 模式模拟账户交易通道，下单脚本见 `references/auto-mode.md`，本文件只讲 WebSocket 行情用法；② 权限边界节「老虎仅作数据源，未授权交易」改为「**交易已授权（不再是「仅数据源」）**」——注明港美股默认下单账户 + 三动作脚本 + 港股实测 / 美股待盘中实测状态，并提醒「行情与交易权限独立：美股行情权限仍无（走富途单源），交易权限港美股均已授权」；③ 「老虎 SDK 能力」表格「定位」行补充「+ 港美股模拟账户交易通道（auto 模式默认账户）」。不改行情描述本身（WebSocket 港股 Lv2 推送、美股无行情权限等仍成立）。

- **移损操作方式文档全面同步为「modify 现有止损单触发价」单步（2026-08-05 用户确认）**：`move_stop_tiger.py` / `move_stop_tiger_us.py` 已于 2026-08-05 实测改为「modify 现有活动 STP 单 aux_price」（主路径、单步无撤单 race），但文档仍残留「先下新止损单、再撤旧止损单」旧描述——用户确认「在老虎账户移动止损只需要修改随开仓附带的止损单的触发价格就可以了」（开仓附加止损腿 OrderLeg('LOSS') 落成的独立 STP 直接改触发价即可，无需撤旧挂新）。**改了什么**：① [`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md) 移损定义（3 类动作）改「把随开仓附带的止损单触发价 modify 为新止损价」+ 保留「防空仓反向开仓」硬要求（完全空仓时止损单触发会被接受、反向开仓，故不能累积）+ 补充「无活动止损才补下新 STP、modify 失败才 fallback 先下新再撤旧」；「移动止损」动作表格「新止损」字段同步；「止损单类型」条目「`move_stop` 移损新增的止损单」措辞改为「移损 fallback 下新止损单」；② [`references/trading-strategy.md`](.claude/skills/trade/references/trading-strategy.md)「移动止损：先新增后撤旧」节重写为「移动止损：modify 触发价单步（2026-08-05 实测修订，「先新增后撤旧」降级为 fallback）」——保留 2026-08-01「多止损累积 → 空仓反向开仓」实测安全逻辑（这是「保留单个活动止损」硬要求的由来，modify 方案天然满足）；③ [`references/monitoring.md`](.claude/skills/trade/references/monitoring.md) 移动止损脚本说明同步（港美同走 modify aux_price）；④ [`references/signal-mode.md`](.claude/skills/trade/references/signal-mode.md) 移损信号「提示用户在 App 改挂：撤旧止损单、挂新止损单」改为「把现有止损单触发价改为新价、无需撤旧挂新」（signal 模式用户在 App 也是改同样的触发价，两模式操作一致）；⑤ [`scripts/trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) docstring 4 处（实测状态段「独立止损 STP 先新增后撤旧」→「modify aux_price 移损」、「三个动作的订单类型」移动止损段、`cancel_all_stop_orders_tiger` docstring「移损撤旧用」→「移损 fallback 撤旧用」、撤旧注释「移损先新增后撤旧保新止损」→「移损 fallback 保新止损」）；⑥ [`references/risk-management.md`](.claude/skills/trade/references/risk-management.md)「用户也会在券商 App 里手动新增止损单（如移损、补挂）」→「手动调整止损（如改触发价移损、补挂止损单）」。脚本 fallback 路径本身保留「先下新再撤旧」（modify 失败保证仓位有止损保护）。触发：用户「在老虎账户移动止损只需要修改随开仓附带的止损单的触发价格就可以了」。

- **equity 口径修复：preflight / `load_equity(auto)` 默认账户改走老虎（与默认下单账户一致）+ 港股取 HKD / 美股取 USD 分币种净值（2026-08-05 用户立）**：**为什么改**——2026-08-05 午市建滔手动算仓时，`preflight.py` / `trade_utils.load_equity(auto)` 默认走**长桥**账户（387,613 HKD），与默认下单账户（**老虎**）不一致，B 取错账户（误用 7,769 vs 正确 156,391）；且早市把老虎 **USD 净值**（999,284）直接当 HKD 用，B 低估 ~7.8 倍——两笔交易的 max_loss 基准不可比（3.3 倍差）、今日整体 -0.70R 的加权口径失真。用户立规：**港股交易取 HKD 权益、美股交易取 USD 权益**，并指出老虎 `get_prime_assets` 可直接取对应币种净值（实测确认 `currency_assets` 含 USD / HKD / CNH 三币种段 + 实时 `forex_rate`，USDHKD ≈ 7.8436）。**改了什么**：① [`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) `load_equity_tiger` 加 `base_currency` 参数——`get_prime_assets(base_currency)` 取证券段 `net_liquidation`（实测 HKD **7,819,536.41** / USD **996,932.71**，全账户按老虎自身实时汇率折算、无需外部数据源），不传参数兼容旧 `get_assets`（USD）；② [`trade_utils.py`](.claude/skills/trade/scripts/trade_utils.py) `load_equity` 加 `broker`（默认 `'tiger'`）/ `base_currency`（默认 `'HKD'`）参数——auto 默认走老虎（与默认下单账户一致）、长桥脚本显式 `broker='longbridge'` 保留长桥；③ [`open_position.py`](.claude/skills/trade/scripts/open_position.py)（美股长桥备选）显式传 `broker='longbridge'` 保持长桥口径；④ [`open_position_tiger.py`](.claude/skills/trade/scripts/open_position_tiger.py)（港股默认）自动算仓传 `base_currency='HKD'`——HKD equity 与 HKD 止损距同币种；⑤ `preflight.py` / `resume.py` 经 `load_equity` 默认自动生效。**实测**：preflight / resume 显示 equity 7,819,536.41 HKD、B = 156,390.73（修复前午市误为 7,769.46）；各路径验证——`load_equity('auto')` = (7,819,536.41, HKD)、`('auto', base_currency='USD')` = (996,932.71, USD)、`('auto', broker='longbridge')` = (387,613.08, HKD)、`('signal')` = equity-log 值（HKD，值略）。**顺带核实 TODO「老虎 paper 账户币种口径」**（2026-08-04 记录）：老虎 paper 单账户多币种现金段（USD 1,000,000 / HKD -24,058.59——港股交易后 HKD 现金为负 / CNH 0），S 段 `net_liquidation` 为全账户按 `forex_rate` 折算、币种正确；港股权益按 `base_currency='HKD'` 直取即可，**无需外部汇率**。**已知遗留**：TODO「`open_position.py` 自动仓位币种换算 bug」（美股长桥备选：HKD equity ÷ USD 止损距单位错位）不在本次范围、保留待修（汇率源已找到）。触发：用户「为什么两笔 max_loss 差这么多？固定比例下注吗？」→ 排查发现 equity 取错账户 + 币种口径错误 → 用户立规「港股交易取 HKD 权益数值，美股交易取 USD 权益数值」+「老虎账户里面应该可以直接取到 HKD 权益数值」。

- **「早盘市况不明朗期」规则（开盘约 30 分钟谨慎开仓，2026-08-05 用户立）**：2026-08-05 早盘开多中芯复盘——开盘 31 分钟中芯冲高 +4.7%（65.5→68.6）后回落，10:01 在回落中开多 @68.15 = 接回落，开仓后继续回落 67.5（-1.38R）；当时自查看似过（VWAP 上方 + 突破 0.5 + 0.5 回踩企稳 + 板块共振）判「多头趋势日」，但早盘冲高回落 vs 趋势日未定、判断过早。**沉淀规则**：[trading-strategy.md](.claude/skills/trade/references/trading-strategy.md) 新增节「早盘市况不明朗期」——开盘约 30 分钟（09:30-10:00）市况不明朗、冲高回落 / 大跌反弹幅度都大，**谨慎开仓**：冲高后要等回落（回落企稳二次确认）、大跌后要等反弹（反弹遇阻二次确认）；**市况（震荡日 / 多头趋势日 / 空头趋势日）确定不了就不开仓**；该时段频繁重新评估市况（10 分钟重估 + 更频）；**「自查清单过 ≠ 市况已确定」**（条件触发针对「数据稳定」、本条针对「市况性质确定」，两者都满足才开仓）。[SKILL.md](.claude/skills/trade/SKILL.md) 硬性护栏第 7 条开盘观察期段补充该规则。触发：用户复盘今天早盘开多中芯位置 + 立「早盘冲高之后一般都会回落、幅度一般都很大，开盘半小时左右冲高回落/大跌反弹幅度都大要谨慎开仓，冲高后要等回落、大跌后要等反弹，市况不明朗要频繁重新评估市况，如果今天市况确定不了就不要开仓」。

- **美股 auto 模式默认改老虎模拟账户、长桥改为备选，专门解耦开发美股老虎一套（2026-08-05 用户立）**：原美股默认长桥（`trade_utils.py` + `open_position.py` / `close_position.py` / `move_stop.py`），现改默认老虎。**新增美股老虎一套**（与港股 `trade_utils_tiger.py` 解耦、专门为美股开发）：[`trade_utils_tiger_us.py`](.claude/skills/trade/scripts/trade_utils_tiger_us.py)（复用 trade_utils_tiger **不分市场**的基础设施——load_config / new_trade_client / 撤单 / 费率（_fee_per_side 已支持 US. 3bps）/ 赔率 / 仓位 / 成交回查 + **美股特定适配**：symbol 裸代码 MU（2026-08-05 实测 MU.US / US.MU 报「don't support trading」、只认裸代码 MU/AAPL）、lot 1、tick 0.01（无价位表）、USD 净值直用（账户 currency=USD、无需港股 HKD 保守口径）、submit 系列用 `to_tiger_symbol_us`）+ [`open_position_tiger_us.py`](.claude/skills/trade/scripts/open_position_tiger_us.py) / [`close_position_tiger_us.py`](.claude/skills/trade/scripts/close_position_tiger_us.py) / [`move_stop_tiger_us.py`](.claude/skills/trade/scripts/move_stop_tiger_us.py)。`close_position_tiger_us` 走「modify 止损触发价=现价」无 race 路径、`move_stop_tiger_us` 走「modify aux_price」单步路径（同港股 2026-08-05 改造）；`cancel_all_stop_orders_us` 与 `_is_stop_order` 含 TRAIL 跟踪止损（吸收 2026-08-05 中芯残留事故教训——cancel 只撤 STP/LOSS 漏 TRAIL、致 salable=0 平仓被拒）。**实测状态**：symbol 格式 + 合约查询 + 净值已实测（MU/AAPL 查询成功、净值 USD 995,478、购买力 396 万），下单链路（开仓 LMT+附加止损、平仓 modify 触发价、移损 modify aux）待美股盘中实测（白天盘外、晚上 21:30 开盘测）；SKILL.md / auto-mode.md 的美股默认账户描述（长桥→老虎）等实测通过后再改，避免未测改默认。

- **`close_position_tiger.py` 平仓 race condition 排查（止损已触发时撤单无效 + MO 反向开仓，券商层无平仓接口，2026-08-05）**：2026-08-05 港股 auto 平仓中芯事故——决策平仓到 MO 执行的数十秒延迟内，中芯跌破 0.5 触发附加止损单 67.50（Sell 36500 平多、持仓→0），脚本「先撤止损单再 MO 平仓」的撤单对**已 Filled 止损单无效**（`cancel_all_stop_orders_tiger` 仍返回 cancelled 但实际没撤到），仍继续 MO Sell 36500——持仓已 0 时 MO Sell **反向开空 -36500**（紧急 close_position_tiger short MO Buy 平空处理）。**排查**：①「MO 前查持仓、0 则跳过」修复**无效**——TOCTOU（查完持仓到发 MO 间价格持续变化、止损可能恰好触发，使持仓快照瞬间过时，用户指出"检查持仓状态解决不了、价格持续变化"）；② 老虎 `TradeClient`、长桥 `TradeContext` **均无平仓专用接口**（只有 `submit_order`/`place_order` 普通订单 + `cancel_order`）、订单也**无 reduce_only / 开平仓标志**（老虎 `Order` 构造参数确认无）——股票现货 Sell 在 0 持仓时必然融券反向开空，**券商层无法约束**（用户指出"平仓只能寻找券商是否有专门用于平仓的接口、即使 0 持仓也不反向开仓"，但两家券商都没有）。race 的可靠解决**待定**（候选：事后容错 MO 后查持仓 + 立即平反向 / 撤单查终态 Cancelled 才 MO / 查富途 OpenAPI 是否有平仓接口），[`close_position_tiger.py`](.claude/skills/trade/scripts/close_position_tiger.py) 暂保留 MO 平仓 + 注释已知局限（AI 需避免在止损临界点平仓）。**配套规则**（用户立）：基础设施事故（工具 bug）导致的额外亏损**不计入 `actions/`**、不污染交易复盘——复盘只看交易决策层面（这笔中芯复盘记 -1.38R = 决策层面开仓 68.15→平仓 67.50，不含反向开空事故的额外损失）。触发：2026-08-05 中芯平仓事故 + 用户「工具问题导致的亏损不记录 actions/、不影响复盘」+「平仓检查持仓解决不了、价格持续变化」+「平仓只能寻找券商平仓专用接口」。

- **`move_stop_tiger.py` 改用 modify 现有 STP 单 aux_price（移损单步、消除撤单 race，2026-08-05）**：原「先下新 STP + 撤旧」两步——撤旧止损单环节正是平仓 race condition 根源之一（撤止损时若已触发、撤单无效 + 后续 MO 反向开仓）。2026-08-05 实测老虎附加止损单（OrderLeg('LOSS') 落成的独立 STP）可用 `modify_order(aux_price=新价)` 直接改触发价（腾讯 492→493 成功、status=Submitted），故移损改为主路径 modify。**新逻辑**：查活动 STP 单 → 1 个 modify aux_price（主路径、单步无撤单）；0 个下新 STP（补保护）；≥2 个撤多余保留 1 个再 modify；modify 抛异常 fallback「先下新 + 撤旧」。端到端实测通过（开仓带附加止损 → move_stop_tiger → aux 490.6→491.6 `verified=true`）。**长桥对照**：独立 MIT 单 `replace_order(trigger_price)` 实测**失败**——`OpenApiException code=601001`「High volatility of the underlying has triggered the market order protection mechanism」（MIT 是市价触发单、replace 时长桥重新评估触发后市价单、标的高波动触发市价保护机制拒；稳定复现 2 次）；**非权限问题**（[Issue #526](https://github.com/longbridge/developers/issues/526) 的权限限制只针对 bracket order 附加订单、独立 MIT 不受），长桥建议改限价单但 skill 规定止损用市价、不能改 LIT，`move_stop_hk` 暂保持原「撤旧 + 下新」逻辑（实盘是否如此待验证）。触发：用户「老虎 move_stop_tiger 改用 modify（实测可行、移损变单步消除撤单 race）；长桥独立 MIT 单的 replace 也测试一下」。

- **`close_position_tiger.py` 改用「modify 止损单触发价=现价」平仓、彻底消除平仓 race condition（2026-08-05 用户方案）**：本日事故（撤止损单 + MO Sell 反向开空 -36500）的根因——「撤止损单 + MO 平仓」两步的 race（撤止损时若已 Filled、撤单无效 + MO 在持仓 0 时反向开仓）——**用 modify 一招解决**：把持仓止损单的触发价 modify 成现价，止损单自己立即触发 Sell/Buy MO 平仓。原理：持仓止损单本就是「Sell @ 触发价」（做多），modify 触发价到现价（价格 ≤ 触发价立即满足）、止损单触发后 MO 严格 = 持仓量成交、平后持仓归 0——全程只有一个 modify 动作，不存在「第二个 MO 在持仓 0 时反向开仓」的可能（race 从机制上消失）。**路径**：有活动止损单 → modify 触发价=现价（取整 tick）→ 等止损单触发 MO 成交（主路径）；无活动止损单 → 直接 MO（无冲突）；modify 失败/未触发 → fallback「撤止损 + MO」。端到端 paper 实测通过（腾讯 100 股：附加止损 @491.8 → close_position_tiger modify 触发价 → 494.8 → 止损单触发 Sell MO **Filled @494.6、持仓归 0、无反向开仓**）。**长桥 `close_position_hk` 同理待改**（长板也有 MIT 止损单、理论上也能 modify trigger_price 触发平仓；但长桥独立 MIT replace 实测被市价保护机制拒，modify 触发价到现价是否也拒待测）。触发：用户「老虎账户下面的平仓动作改为直接修改止损单的触发价、把触发价直接改为想要主动平仓的价格就可以了」。

- **密采样 watcher plist python 路径占位替换为 homebrew 绝对路径、修复 launchd 触发 exit 78（2026-08-05）**：[`.claude/hooks/com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) 的 `ProgramArguments` 原用占位 `/usr/bin/env python3`（plist 注释第 9 行标注「部署待替换」、此前部署时未替换），但 launchd 环境精简 PATH（`launchctl print` 实测 = `/usr/bin:/bin:/usr/sbin:/sbin`，不含 homebrew）下 `env python3` 解析到 stub 版 python，`last exit code = 78: EX_CONFIG`，watcher 每 120 秒触发即 crash、从不执行检查逻辑（密采样安全网静默失效）。改为项目实际用的 `/opt/homebrew/bin/python3` 绝对路径后，unload + load 验证 `last exit code = 0`、盘中触发可正常跑检查 + 发系统通知。脚本本身无依赖问题（`monitor_watcher.py` 仅用 subprocess/datetime 标准库，手动跑退出 0），纯属 launchd PATH 解析占位。触发：2026-08-05 港股盯盘启动 `launchctl load` 报 `5: Input/output error`，查 `launchctl print` 发现 exit 78 定位。

- **`monitor_guard.py` hook 命令路径改绝对路径、修复盯盘启动时 Bash 全被拦（2026-08-04）**：[`.claude/settings.json`](.claude/settings.json) 三处 hook 命令（PreToolUse pretool / TaskStop taskstop / Stop stop）原用相对路径 `python3 .claude/hooks/monitor_guard.py`，但 hook 实际以 skill 根目录（`.claude/skills/trade/`）为 cwd 执行，相对路径解析到不存在的 `.claude/skills/trade/.claude/hooks/monitor_guard.py`，hook 启动即崩溃 → PreToolUse 崩溃拦截所有 Bash 命令（盯盘启动准备时 launchctl load / hot_list / cat / ls 全被拦，仅 preflight 侥幸通过）。改为指向本仓库 hook 脚本的绝对路径（项目根的 hooks 目录，具体路径见 settings 文件）后 hook 正常执行，密采样守卫逻辑生效（盘中调 hot_list/snapshot 且 monitor_segment 未在跑 → 阻断并提醒先重启密采样循环）。触发：2026-08-04 美股盯盘启动时发现 hook 崩溃拦 Bash、无法拉行情。
- **密采样 watcher 随盯盘开关：启动开启 / 停盯关闭（2026-08-04 用户立）**：`monitoring.md` 多层防护第⑪层的外部 watcher（[`.claude/hooks/monitor_watcher.py`](.claude/hooks/monitor_watcher.py) + [`.claude/hooks/com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist)，launchd 定时触发、独立于 session、盘中 monitor_segment 没跑时发 macOS 系统通知）需随盯盘开关——**开始盯盘 AI 跑 `launchctl load .claude/hooks/com.daytrading.monitor-watcher.plist` 开启**、**停盯 AI 跑 `launchctl unload ...` 关闭**。用户不会主动说「开 / 关 watcher」，AI 自主在启动 load、停盯 unload（与解除防睡眠并列收尾）。不关闭 = 盯盘已停 watcher 仍定时检查 + 发通知空转扰民（2026-08-04 停盯时 AI 只解除防睡眠 + 停 monitor_segment、漏关 watcher 被用户指出）。改动：[SKILL.md](.claude/skills/trade/SKILL.md)「停盯总结」段加「密采样 watcher 随盯盘开关」说明（启动 load + 停盯 unload 对称）。触发：用户「停止盯盘的时候要把后台在跑的密采样 Watcher 进程关闭」+「以后都是这样做，开始盯盘开启 Watcher，停止盯盘关闭 Watcher，记录在 skill 里面」。

- **盯盘采样频率盘中固定 40s/10s、禁止自主降频 + 标的僵局换标的不降频（2026-08-04 用户立）**：2026-08-04 午后盯盘阿里僵局时，AI 用 `sleep 600; monitor_segment` 在段间加 sleep 把采样间隔从 ~10 秒拉到 10 分钟（DURATION 仍是 40、但段间 sleep = 等效降频），违反「40s/10s 密采样」死规矩（用户两次抓住）。**根因**：原 monitoring.md「段时长可调：更稀可改 60-180 秒」+「整个市场缩量才考虑降频」留了降频 allowance，AI 在震荡无机会时滥用（放稀 DURATION 被脚本挡、就改用段间 sleep 绕过）。**改法（撤销一切降频 allowance）**：① [monitoring.md](.claude/skills/trade/references/monitoring.md)「不因市况降频」段——强化为「盘中 AI 不得以任何理由自主修改采样频率（不放稀 DURATION、不加段间 sleep、不换 cron/直接 snapshot）、采样频率是死规矩、市况只决定盯哪个标的不决定盯多密」；② 「段时长可调」改为「段时长盘中固定 40s、AI 不得自主调」、撤销「更稀 60-180 秒」allowance + 加 2026-08-04 阿里 sleep 教训；③ 「换标的而非降频」段——撤销「整个市场缩量才考虑降频」降频出口、改为「市场几千只标的总有机会、哪怕缩量也只换标的不降频、绝不存在降频死等合法出口」；④ [SKILL.md](.claude/skills/trade/SKILL.md) 硬性护栏加第 8 条采样频率硬规矩。触发：用户「盘中不允许自主修改盯盘采样频率，必须 40s 盯盘间隔 10s 密采样」+「没机会就寻找新的标的，整个市场几千只标的，总有机会」+「把这条规定也写进 skill」。

- **模式提醒只在盯盘 / 开仓 / 平仓 / 移损场景亮、复盘不亮（2026-08-04 用户立）**：[SKILL.md](.claude/skills/trade/SKILL.md)「触发后第一件事 = 亮模式提醒」段原写「盯盘启动、开仓 / 平仓 / 移损 / **复盘**指令」都要亮模式提醒。用户指出复盘不需要——模式提醒是安全件（防 auto 模式 AI 不问就下单的实盘事故），复盘只读历史记录、不执行下单、不碰账户，无该风险、亮了反成啰嗦。改为「盯盘启动、开仓 / 平仓 / 移损指令」亮、加 ⚠️ 说明「复盘指令不亮模式提醒——复盘只读历史交易记录、不执行下单、不碰账户，没有 auto 误下单的实盘事故风险，模式提醒的安全价值不成立」。触发：用户「复盘不要提醒当前模式，只有盯盘需要」。

- **历史交易记录（signals/ + actions/）赔率补净口径（2026-08-04 用户立）**：盯盘前瞻赔率改净口径（见下条）后，历史记录里的赔率仍是毛口径、需统一。**保留毛值、旁边标净值**（格式「X（毛）/ Y（净）」，可追溯毛净差异），**正文赔率一起改**（平仓注释落地赔率、三阶段对比）；浮盈 R（移损浮盈估算）/ 开仓失败判定假设赔率（如「做空赔率 0.81」）/ 理由栏描述性赔率引用**不改**（非正式赔率字段）。**范围**：`actions/` 全部（08-03/08-04 共 4 文件，07-31 作废无赔率）+ `signals/` 全部有赔率的交易记录（07-07~08-04 共 22 文件）。**方法**：临时辅助脚本 `tmp/recompute.py`（净赔率重算，gitignore）逐笔实算三阶段净赔率 + Edit 标注——每笔从记录提取参数（方向 / 标的判市场 / 参考价 / 成交价 / 止损 / 止盈 / 平仓价 / shares / max_loss），recompute 输出毛列必须等于记录原值（否则参数提取错）。**执行**：actions + 8 月 signals + 07-29-HKT 由主线处理；剩余 18 个 7 月 signals 分 3 批派 general-purpose agent 并行（各 6 文件、同规则 + recompute.py）。**抽查**：3 批各抽 1 笔（07-30-HKT 中芯空、07-28-ET SOXS 多、07-29-ET MU 空）recompute 实算与 agent 标注完全一致。**发现并修正笔误 1 处**：07-29-ET MU 做空初始毛值原记 2.0，实算 2.22（20÷9）系笔误，已修正留痕（「2.22（毛·原记 2.0 系笔误）/ 2.17R（净）」）。**净口径暴露的规律**：港股 18bps 对窄止损冲击极大——07709 空（止损距 0.23）初始毛 1.39R → 净 0.84R（**低于 1.2 门槛**，净口径下这笔本不该开）、07-30-HKT 中芯空（止损距 0.95）落地毛 +0.26R → 净 +0.03R（毛利 HK$500 被双边费吃掉 ~HK$449）；美股 3bps 冲击小（宽止损降幅 <3%）。⚠️ 「EV / g 等衍生指标」**用户第二条已满足**：[review.py](.claude/skills/trade/scripts/review.py) / [bayes_evolution.py](.claude/skills/trade/scripts/bayes_evolution.py) 8-03 起内部扣双边费算净 R，`reviews/*-trades.csv` 无 R 列（只存原始 entry/exit/shares/max_loss）、脚本内部扣费，下游 EV / g / P(EV>0) / 胜率全基于净 R。触发原因：用户要求「历史成交记录（signals + actions）赔率重算为净口径」+「EV、g 等用赔率的变量都要考虑 base point」。

- **密采样违规多层防护 5 层落实（2026-08-04）**：2026-08-04 盯盘期间 AI 擅自用 CronCreate 设 15 分钟低频扫描 + cron 触发时直接调 snapshot.py/hot_list.py（不经 monitor_segment.py）绕过 40 秒密采样强制降频（用户两次纠正），根因 = **AI 是执行者握主动权、任何靠 AI 自觉调用的工具都能主动绕过**（脚本护栏只在调用时生效、文档软约束会忘）。落实推荐组合 5 层防护：① [`.claude/settings.json`](.claude/settings.json) `permissions.deny` 禁 CronCreate（堵 cron 降频入口）；② 新增 [`.claude/hooks/monitor_guard.py`](.claude/hooks/monitor_guard.py) PreToolUse hook——盘中调 snapshot/hot_list 但 monitor_segment 未在跑则 exit 2 阻断；③ 同脚本 Stop hook——回合结束 monitor_segment 未在跑则提醒；④ [`monitor_segment.py`](.claude/skills/trade/scripts/monitor_segment.py) 段结束输出连续性指标（累计点数 / 距首采样分钟，降频暴露为「点数少 - 时间长」）；⑤ [`monitoring.md`](.claude/skills/trade/references/monitoring.md)「不因市况降频」节加「2026-08-04 教训 + 多层防护」说明。**B4 原议「持续循环」放弃**——与 monitoring.md 第 25 行「不得改长时段持续模式（task-notification 不触发）」冲突，改用 Stop hook。**局限**：AI 仍能 kill 进程绕过，5 层只提高绕过成本 + 暴露、非 100% 银弹；盯盘纪律最终靠**外部监督 + 多层防护 + AI 自觉**。剩余 7 层紧接落实（见下条）。触发：用户问「如何降低 AI 违反密盯规定的概率」+「先按推荐叠 5 层、剩余加 TODO」。

- **密采样违规多层防护剩余 7 层全落实（2026-08-04，紧接上条）**：用户要求「把剩余 7 层也做了」。落实：⑥ TaskStop hook（[`monitor_guard.py`](.claude/hooks/monitor_guard.py) 加 taskstop 模式 + settings.json PreToolUse matcher TaskStop）——盘中停后台任务且 monitor_segment 在跑时提醒确认（不阻断，避免误伤停出错进程）；⑦ [`resume.py`](.claude/skills/trade/scripts/resume.py) 盘中断层强调「主动停密采样」+ 立即重启（盘外才按断网/故障）；⑧ 外部 watcher（[`.claude/hooks/monitor_watcher.py`](.claude/hooks/monitor_watcher.py) + [`com.daytrading.monitor-watcher.plist`](.claude/hooks/com.daytrading.monitor-watcher.plist) launchd 模板）——独立于 session、盘中 monitor_segment 没跑发 macOS 系统通知（部署需手动 launchctl load + 改 plist 占位路径）；⑨ [`monitor_log_gap_check.py`](.claude/skills/trade/scripts/monitor_log_gap_check.py) 段间 gap 复盘脚本（>120s 标记降频疑点，实跑验证：药明 8-04 signal log 164 点 / 平均 gap 12s / 最大 61s / 无降频疑点）；⑩ 用户监督强调（monitoring.md 多层防护第⑫层，最有效一道、非工程化）；⑪ 防中断三件套加第 4 条「每段自查未擅自降频」（monitoring.md）；⑫ [`preflight.py`](.claude/skills/trade/scripts/preflight.py) 启动入口提醒。**至此 12 层防护全落实**（monitoring.md「多层防护」说明同步更新为 12 层全落实）。**仍非 100% 银弹**（AI 仍能 kill 进程），盯盘纪律最终靠外部监督 + 多层防护 + AI 自觉。触发：用户「把剩余 7 层也做了」。

- **运行时 log 按 mode 分文件，signal/auto 两会话并行盯盘不冲突（2026-08-04 用户立）**：用户同时开 signal + auto 两个会话并行盯盘，运行时 log 文件共用会冲突——① `tmp/monitor_log_{SYM}_{DATE}.csv` 不含 mode，两会话盯同标的写同一文件、采样数据互相污染（价格 / 买卖比 / 高低点交错）；② `tmp/reassess_marker_{DATE}.txt` 全局单例（只按日期），两会话重估提醒 marker 互相覆盖；③ `resume.py` 的 glob `monitor_log_*_{DATE}.csv` 把两会话所有标的混算「上次活动时间」；④ `ring-log.csv` 全局单例，auto 的 `log_action.sh` 读它取时间戳会用到 signal 会话的响铃。**改法：运行时 log 文件名一律加 mode 后缀**——`monitor_log_{SYM}_{DATE}_{MODE}.csv`、`reassess_marker_{DATE}_{MODE}.txt`（MODE = signal/auto），两会话各写各的、互不污染。**改动**：① `monitor_segment.py`——`from trade_utils import parse_mode`、`mode = parse_mode()`，`log_file` / `marker_file` 命名加 `_{mode}`；② `monitor_summary.py`——`MODE = parse_mode()`、`LOG_FILE` 加 `_{MODE}`，**连带修美股日期 bug**（之前 summary 用 `datetime.now()` 北京日期、segment 美股用 `-12h` 美东日期，美股 summary 一直找不到 segment 写的 log；现 summary 按市场区分日期、与 segment 对齐）；③ `resume.py`——顶部取 `MODE`、`last_monitor_time` glob 加 `_{MODE}`、`last_ring_time` auto 模式返回 None（ring-log 仅 signal 读，auto 读 signal 的响铃会混入他 Session 活动时间）、去掉重复 `parse_mode` import；④ [monitoring.md](.claude/skills/trade/references/monitoring.md) `monitor_segment` / `monitor_summary` 调用范例加 `--mode` + log 文件名说明；⑤ [SKILL.md](.claude/skills/trade/SKILL.md)「脚本调用按模式传 `--mode`」列表扩展到 `monitor_segment` / `monitor_summary`、补「运行时 log 按 mode 分」说明。**边界**：`ring-log.csv` / `equity-log.csv` 是 signal 专属（auto 不响铃不写），**不分文件**——但 resume 读取按 mode（auto 跳过 ring-log、equity 走账户 API）；`signals/` vs `actions/` 交易记录两模式各写各的目录、本就不冲突。**验证**（Python 实跑）：monitor_summary `HK.TEST --mode auto` → `monitor_log_HK_TEST_20260804_auto.csv`、默认 → `_signal`、`US.TEST --mode auto` → 美东日期 `20260803_auto`（日期按市场对齐生效）；glob `monitor_log_*_{DATE}_{MODE}.csv` 按 mode 隔离、旧无 mode 文件不被误匹配；实跑发现 signal 会话已用新代码写 `monitor_log_HK_02359_20260804_signal.csv` 等文件（改动实时生效）。⚠️ **过渡效应**：今天改前写的无 mode 旧文件（`monitor_log_*_{DATE}.csv`）保留为历史、新逻辑不再读——今天开盘到本改动生效前那段的采样数据，summary 读不到了（一次性、仅今天）；历史 log 不删（留作回溯）。触发原因：用户指出「signal mode 和 auto mode 同时运行盯盘，log 文件会冲突」+「查一下哪些 log 文件是共用的」+「运行时 log 文件要分开」。

- **盯盘前瞻赔率改净口径（扣双边手续费），与复盘统一、signal/auto 两模式一致（2026-08-04 用户立）**：用户立规——盯盘与复盘过程中计算的**所有赔率一律用净赔率（扣基点 bps）**，港股 18bps/边、美股 3bps/边（**单边费率**，开仓 + 平仓两边各收一次）。复盘侧（`review.py` / `bayes_evolution.py`）2026-08-03 已改净口径（$R$ = 净盈亏 ÷ 毛 max_loss），但盯盘侧的前瞻赔率（初始预期赔率、修正预期赔率、开仓基准 ≥1.2、价格范围 odds）仍是**毛口径**（止盈距离 ÷ 止损距离），两边算法不一致。**本次把盯盘前瞻赔率也改为净口径、与复盘统一**，并确保 signal 模式（AI 对话手算）与 auto 模式（脚本算）口径一致不冲突——基点按市场（港股 / 美股）分、不分模式。**净前瞻赔率公式**（沿用复盘「分子扣双边费、分母毛止损距」口径）：$= \big[|T-E| - r(E+T)\big] \div |E-S|$，$E$=参考价 / 成交价、$T$=止盈、$S$=止损、$r$=单边费率（港股 0.0018、美股 0.0003）；前瞻假设到止盈出场、平仓按 $T$ 算费（与复盘按实际平仓价算费同结构）。**改动**：① `scripts/trade_utils.py`（美股）/ `trade_utils_hk.py`（港股长桥）/ `trade_utils_tiger.py`（港股老虎）三个 utils 各加 `_fee_per_side(symbol)` + `_net_odds()`，`calc_entry_range` / `check_price_in_range` 的 `odds_at_ref` / `odds_at_current` 改净口径、加 `symbol` 参数（价格范围本身用毛 R₀ 算、80%/3/8 经验参数不动）；② `open_position.py` / `open_position_hk.py` / `open_position_tiger.py` 调 `check_price_in_range` 时传 `symbol`；③ 文档同步净口径——[trading-strategy.md](.claude/skills/trade/references/trading-strategy.md)「赔率三阶段」公式重写为净口径 + 「开仓基准」≥1.2 改净、[risk-management.md](.claude/skills/trade/references/risk-management.md)「价格范围计算」+「利润=R×max_loss」、[SKILL.md](.claude/skills/trade/SKILL.md) 6 要素参考价 / 价格范围 + 停盯总结三阶段 + 自查 EV 判据、[signal-mode.md](.claude/skills/trade/references/signal-mode.md) 开仓 / 平仓信号赔率字段（6 处）、[auto-mode.md](.claude/skills/trade/references/auto-mode.md) 开仓 / 平仓动作赔率字段（3 处）、[review-and-evaluation.md](.claude/skills/trade/references/review-and-evaluation.md) 预估胜赔率 $\hat{R}_W$。**实测验证**（Python 实算三个 utils）：美股 MU 空 entry 821/stop 828/target 800.98 → 毛初始 2.86R / **净 2.79R**（宽止损、3bps 冲击 <3%）；港股 07709 空 entry 36.95/stop 37.35/target 36.04 → 毛 2.28R / **净 1.95R**（窄止损距 0.4、18bps 冲击约 14%）——印证「费率对窄止损策略冲击极大」，净口径下窄止损 edge 更薄、更要挑高毛赔率入场点；`symbol=None` 等价毛口径（向后兼容）、`current=stop` 分母为 0 返回 inf。一致性校验：前瞻净赔率（用 target 当平仓价） == 复盘净 R（到止盈），1.8764 == 1.8764。触发原因：用户立规「盯盘和复盘所有赔率用净赔率（扣基点）、港股 18bps、美股 3bps」+「确保 signal 和 auto 两模式不冲突」+「基点都是单边的」。

- **`monitor_segment.py` log 日期按市场区分，修港股早盘 log 污染 bug（2026-08-04）**：上一条「log 按美东交易日命名」统一用 `date_str = now - 12h`（为美股跨北京午夜不换文件），但对**港股早盘**失效——港股盘中北京 09:30-12:00，减 12h = 昨天 21:30 → log 落昨天日期，今天港股采样写进昨天 log、完整采样统计混入昨天数据（实测 HK.09988「开=123.60 low=123.50 点数=1840」全是 8-03 的价 + 昨今混算）+ 时间断层哨兵读昨天末行误报 1069 分钟断层；且 `monitor_summary.py` / `resume.py` 本就用北京日期（不减 12h），与 monitor_segment 三脚本不一致、文件名对不上（summary 找 20260804、segment 写 20260803）。**改法**：`date_str` 改为按市场区分的函数 `trading_date_str(sym)`——港股 HK 用北京日期 `now`（盘中不跨午夜）、美股 US 仍用美东交易日 `now - 12h`（夏令时；冬令时 EST 需 -13h），三脚本对港股重新对齐。**不回归美股**：美股分支保持 -12h 不变。**重估提醒检查**（每 10 分钟方向重估，marker 文件命名 + 首点时间拼算）仍需 `date_str`，改为取主标的市场对应交易日 `trading_date_str(syms[0])`（与主标的 log 日期一致；首次修复漏改这两处致 `[重估提醒检查 err:name 'date_str' is not defined]`，已补）。实测修复后港股 log 落 `monitor_log_HK_00981_20260804.csv`（今天日期正确）。触发：8-04 港股早盘盯盘发现 HK_09988 log 被污染（另一会话用旧代码持续写入），排查定位到 -12h 对港股早盘失效。

- **`monitor_segment.py` log 按美东交易日命名 + 哨兵跨午夜安全（2026-08-04 立）**：盯盘跨北京午夜（00:00 后美东仍盘中）时，原 `date_str = datetime.now()`（北京日期）会导致 log 文件换日期（8/3→8/4）、完整采样统计点数重置（工具固化失效）、时间断层哨兵误报（date_str 拼接跨午夜 HH:MM:SS 算出 1442 分钟假断层）。**改法**：① `date_str = (now - 12h)`（美东交易日，夏令时 EDT=UTC-4；冬令时 EST=UTC-5 需 -13h）—— log 连续写同一交易日文件、完整统计不重置；② 时间断层哨兵 gap 计算从「date_str + last_t 拼接」改为「当前日期推断 + 跨午夜处理」（last 时分 > now 时分则 last 是昨天），跨午夜 gap 正常（~1 分钟）不误报。实测跨午夜后（北京 00:03）log 仍写 20260803、点数连续 785、哨兵无假断层警告。

- **`monitor_segment.py` 强制 40 以内密采样、禁止降频（2026-08-03 用户立）**：盯盘期间 AI 曾擅自把 DURATION 从 40 降到 120/300（违反「40 以内密采样 + 不因市况降频」），用户纠正后问「如何确保下次不忘」。**工具固化**：`scripts/monitor_segment.py` DURATION 传入 >40 自动夹到 40 + 打警告（「市场无机会应换标的 hot_list 或继续密盯，不降频」），AI 即便误传降频也被挡回 40、想违规都违规不了。实测传 120 → 警告 + 实际跑 40s。与同日「段结束自带完整采样统计」同理（工具强制不依赖记忆）。

- **`monitor_segment.py` 段结束自带完整采样统计（2026-08-03 用户立）**：盯盘期间用户三次纠正（①降频 120s/300s 违反「40 以内密采样 + 不因市况降频」、②空仓观望 1 小时没拉全貌 monitor_summary、③只看段结束当前段 4-5 点、没看从开盘到当前的完整采样 log），根因是段结束输出只有当前段 + VWAP，AI 容易只看当前段漏整体趋势。**改法（工具固化，不依赖记忆）**：`scripts/monitor_segment.py` 段结束输出在「VWAP 检查」块后新增「📊 完整采样统计」块——读各标的的 monitor_log CSV（从开盘累积），输出点数 / 开 / 末 / high / low / 买卖比均 / 量比均 / 近 5 点（价 买卖比），AI 段结束唤醒即见从开盘到当前的整体，与既有 VWAP 检查、每 10 分钟重估提醒同理（工具强制段结束自带、不靠记忆单独跑脚本）。实测第 80 段输出正常（NVDA 739 点 开 198.26 末 206.99 high 207.33 low 198.18 买卖比均 10 量比均 1.6 + 近 5 点）。触发原因：用户问「如何保证下次不忘每次看采样数据看的是从开盘到当前的所有数据」——靠工具固化段结束自带完整统计最稳（想漏都漏不掉）。

- **平仓记录必填实际落地赔率字段（2026-08-03 用户立）**：`references/auto-mode.md` 平仓动作格式 + `references/signal-mode.md` 平仓信号格式，在平仓表格补「开仓价 / 平仓价 / 盈亏 / **实际落地赔率**」字段，并各加一段 why 说明：**平仓必填实际落地赔率 = 实际盈亏 ÷ 开仓 max_loss**（max_loss 用**开仓时**止损距离反推 = 仓位 × 开仓止损距、移动止损不改这个基准；盈亏用实际成交价算——auto = 脚本返回的真实成交价 / 止损触发价、signal = 响铃时刻价 / 止损触发价；被动平仓也填）。**原因**：盈亏金额看不出「承担了多少风险换来的」——同样赚 $5000，max_loss $5000 是 1R、max_loss $10000 只算 0.5R；实际落地赔率才反映真实性价比，与开仓的初始预期 / 修正预期赔率对照能直接看出「预估→修正→落地」打折（如 2026-08-03 MU 空：初始 2.86R → 修正 2.06R → 实际 −0.92R，预估乐观但被板块 V 反打破止损）。同时明确：**开仓条目在平仓后回填实际落地赔率**，让该笔三阶段赔率（初始预期 / 修正预期 / 实际落地）齐全于一处（与 `SKILL.md`「停盯总结」既有的三阶段赔率要求对齐）。当前笔 `actions/2026-08-03-ET-actions.md` MU 空单已按此补全（开仓回填 −0.92R + 平仓记录单列）。触发原因：用户立规范「actions 记录里平仓后要写出实际落地赔率，以后都这么做、记录下来」。

- **V 反趋势日的两段式应对（2026-08-03 MU 教训沉淀）**：2026-08-03 美股 auto 盯盘暴露——半导体开盘暴跌后 V 反大涨（MU 770→829 +7.6%、SOXL 102→117 +14.5%、NVDA 龙头持续创新高 197→207），我两段都做错：① 暴跌段用「反抽乏力」做空 MU @783.19 被 V 反打破止损 −0.92R（逆趋势）；② V 反确认后仍套「不追高、等深回踩」保守框架，没在 MU 回踩 VWAP 企稳 / 突破 800 回踩时做多，全程踏空多头趋势。根因：没把「龙头持续创新高」当趋势确认信号、开盘暴跌的空头框架整段没切到 V 反后的多头框架。**沉淀进 [trading-strategy.md](.claude/skills/trade/references/trading-strategy.md) 新增节「V 反趋势日的两段式应对」**：① 开盘暴跌段不抄底（接飞刀防护）也**不空反弹**（V 反会打破反抽乏力框架）；② V 反确认段（龙头连破≥2前高 + 跟风站上 VWAP 共振）积极在回踩 VWAP 企稳 / 突破回踩做多，**不执着深回踩**；含 V 反确认判据（反转 vs 反弹）+ 边界（不追高、未共振观望、仍走合格入场形态不赌突破）。触发原因：用户问「为什么没捕捉到趋势」——我用对了第一段（暴跌不抄底）、做错了第二段（V 反确认后没做多），这是今天最大的错失。

- **上升趋势做多位置：下轨（支撑）买、上轨（阻力）卖（2026-08-04 MU 教训沉淀，与「震荡下行做空位置」对称）**：2026-08-04 美股 auto 盯盘第二笔 MU 多单复盘暴露——MU 当天 770→829 上升趋势，我 821 做多（接近上轨前高 828）= **上轨追多**（把「突破 820 后浅回踩 820.53」当「回踩企稳」，实际 821 是高位 / 上轨、不是支撑）；MU 跌破 820 后 818.6 恐慌平（接近下轨 816 止损）= **下轨杀跌**；完美高买低卖 −0.50R。根因：位置判断反了（821 在上升趋势是上轨 / 高位，却当成回踩支撑入场）+ 浅回踩当深度支撑 + 平仓恐慌杀跌。**沉淀进 [trading-strategy.md](.claude/skills/trade/references/trading-strategy.md) 新增节「上升趋势做多位置：下轨买、上轨卖」**（与 2026-08-03「震荡下行做空位置」对称）：上升趋势做多 = 下轨（VWAP / 前阻力转支撑 / 0.382）买、上轨（前高 / 阻力）卖；「突破回踩企稳」的回踩必须是**深度回踩到真支撑**、不是浅回踩（浅回踩仍在上轨附近）；卖点在前高遇阻 / 加速赶顶、不在跌破支撑的恐慌低点杀跌。与「震荡下行做空位置」对称（震荡下行 = 上轨反抽空、下轨不追；上升趋势 = 下轨回踩多、上轨不追）——两次都在趋势错误一端交易 = 两次高买低卖（07709 −0.09R、MU −0.50R）。触发原因：用户复盘指出「买在上升趋势上轨、卖在下轨，怎么回事」——典型的趋势错误一端交易。

- **复盘数据源改为 `signals/` + `actions/` 两目录全量（2026-08-03 用户立）+ 首次全量重建补 3 笔遗漏美股**：以后复盘 = 复盘 `signals/` + `actions/` 两个目录下的**所有**交易（不再按模式只读其一）——signal 模式交易在 `signals/`（港股 `YYYY-MM-DD-HKT-signals.md`、美股 `YYYY-MM-DD-ET-signals.md`）、auto 模式交易在 `actions/`（`YYYY-MM-DD-HKT-actions.md` / `YYYY-MM-DD-ET-actions.md`），两目录全遍历、合并为全量样本逐笔算参数。**防重复计数**：同一笔交易只计一次（signal 与 auto 各记各的目录互不重叠；若同一笔两处都有以 `actions/` 实际成交记录为准）；开仓失败 / 撤销 / 作废批次不计样本（记录上有明确标注）。改动：`SKILL.md` 复盘分析数据源段、`references/review-and-evaluation.md` 数据源段、项目 `.claude/CLAUDE.md` actions 目录归属段同步改写。**首次全量重建**：`reviews/2026-08-03-trades.csv`（N=28 → **33**）——**补入 3 笔此前遗漏的美股交易**（07-21 MU 做空 −0.64R、07-23 MU 做多 +1.11R、07-28 SOXS 做多 −0.86R，均在 `signals/` 有完整信号记录、但从未进过 07-26 起任何合并样本——美股复盘与合并样本衔接缺失）+ 新增 08-03 两笔（阿里做多 signal −0.07R、07709 做空 auto −0.09R）。**统计影响**：EV +0.606R→**+0.498R**、胜率 67.9%→60.6%、频率派 EV 95% CI 从全正 [+0.036, +1.177] 退回**跨 0** [−0.002, +0.998]——**旧样本存在幸存者偏差**（遗漏 3 笔美股中 2 笔亏损）。分组洞察：美股 9 笔 −0.125R（做空逆反弹 + 3x ETF 连续失败拖累）、港股 24 笔 +0.731R（真 edge 来源）、正股 +0.711R vs 杠杆/反向 ETF +0.125R；4 笔 ≥2R 大胜占 R 和 94.7%、剔除后 ≈打平。产物：`reviews/2026-08-03-review.md` + 7 张序贯演化图（`bayes_evolution.py` 数据源由 07-29-trades.csv 更新为 08-03-trades.csv，输出文件名同步）。触发原因：用户要求复盘所有交易（含 `signals/` 和 `actions/` 两目录全部记录）、并把「以后复盘都复盘这两个目录下所有交易」定为规范。2026-08-03 复盘全流程（读信号/动作记录 → 核对旧样本 → 重建全量 → review.py + 7 图 → 报告）。

- **演化图末点坐标标注（2026-08-03 用户立）**：`bayes_evolution.py` 给 7 张序贯演化图加「最后一个数据点用虚线对齐横纵坐标轴 + 显示具体坐标值」。新增两个辅助函数：① `mark_end_single`（单线图：P(EV>0) / EV / 胜率）——末点画十字虚线（垂直连 x 轴、水平连 y 轴，深灰色 #333 避免与橙色主线重合时看不清）+ 加大白边圆点高亮 + 右上方带框坐标文本 "(N=33, val unit)"；② `mark_end_multi`（多 f 图：P(g>0) / P(∑₄₀Y≥0) / P(累计≥20%) 两版）——1 条垂直虚线对齐横轴 + 每条 f 线水平虚线对齐纵轴 + 右侧避让 end-labels "f%: val"（按 y 降序、相邻太近则下压 + 短引导线防重叠，配合 `tight_layout(rect=[0,0,0.80,1])` 留右侧 20% 空间）。末点坐标文本如 P(EV>0) 的 "(N=33, 97.2%)"、EV 的 "(N=33, +0.50R)"、胜率的 "(N=33, 60.0%)"，多 f 图右侧列出 5 条线终值。已视觉验证（单线图虚线对齐 + 带框坐标、多 f 图右侧 labels 不重叠、均不与图例 / 刻度冲突）。触发原因：用户要求演化图最后一个数据点的坐标用虚线和横纵坐标对齐、并显示具体坐标值。

- **震荡下行趋势做空位置教训：上轨反抽做空、下轨附近不追空（2026-08-03 用户复盘指出）**：2026-08-03 午后 07709 破位追空复盘暴露——全天 10:44-15:59 是标准震荡下行通道（上轨反抽高点 37.94→36.68→36.08-36.2 依次走低、下轨 35.2 四次测试未破），我 15:40 破 35.6 追空 @35.52（87,000 股）= **贴着下轨追空**（距 35.2 仅 0.9%）：下行空间 0.32、反弹空间 0.23、初始赔率仅 1.39，开仓 5 分钟反弹回 35.54-35.66、-0.09R 离场；正确做法 = 等反抽上轨 36.08-36.2 做空（止损 36.4、止盈 35.2 → 约 3.8R）。根因：**破位确认入场（单边趋势适用）误用于震荡通道下轨**——震荡通道里下轨破位多为末端 / 假突破，追空 = 高买低卖。已沉淀到 [trading-strategy.md:131](.claude/skills/trade/references/trading-strategy.md#L131)：开空前先判单边趋势 vs 震荡通道（上下轨多次测试清晰 = 震荡），震荡通道只在通道边沿交易（做空等上轨反抽遇阻、做多等下轨回踩企稳）、不追破位，与「位置评估硬闸」一致。触发原因：用户复盘指出「成本价 35.506 在震荡趋势下轨，开空应开在上轨」。：2026-08-03 午后阿里做多信号复盘暴露——开仓 125.30（当日振幅 88% 分位、距当日高 126.0 仅 0.56%），止盈定 130（60 日高附近）算出修正预期 3.6R，但 130 当天不可达（全天最高就是 126.0、止盈从未被接近），持仓 86 分钟横盘后按「临近停止盯盘分情况策略」主动平仓、实际落地 −0.08R。根因链条：**开仓位置高 → 当日剩余空间小 → 为凑够 ≥1.2 门槛把止盈推到够不着的位置 → 3.6R 是虚的、当日可达真实赔率不足 1R**——信号发出时以为做 3.6R 交易、实际做 0.9R 交易，且没有一条数据当场戳穿。据此在 [trading-strategy.md](.claude/skills/trade/references/trading-strategy.md#L80-L82)「止盈完全靠盘面实时判断」节补充两条原则：① **目标止盈位必须大概率可达**——止盈位一般不超过当日高点上方小缓冲（午后突破当日新高概率天然低，越过当日高默认当天不可达、除非增量信号）；开仓价在当日振幅分位越高目标越调低，调低后赔率不足 1.2 的结论是「这笔不做或等回踩」、不是推远止盈凑赔率；剩余时间 × 当前速率（午后量能萎缩、单位时间可走空间远小于早盘，止盈距离不能按全天振幅估）；② **午盘动能不足、目标止盈位保守**——午后成交量萎缩、增量催化少（隔夜 / 盘前信息早盘已集中反应、港股受 A 股午后走势联动）、窄幅消化假突破多（2026-08-03 实测阿里午后 3 小时振幅仅约 0.4%），午盘开仓默认保守目标，出现放量突破 / 新催化等增量信号才按实际盘面上调，判断依据永远是量能与突破形态、不是机械「午盘一律保守」。触发原因：用户复盘当日午后开仓位置偏高，立「目标止盈位要大概率可达才有意义，午盘目标止盈位要保守、一般午盘动能都不足」。

- **老虎平仓顺序修复：必须先撤止损单、再下平仓单（2026-08-03 午后实测暴露）**：TODO「修 close_position_tiger.py 平仓顺序」待办完成。**问题**：2026-08-03 午盘 auto 盯盘实测——做空 07709 38,500 股（开仓 LMT + 附加腿落成 STP 止损单 HELD）后，下 Buy 平仓单（MKT/LMT、38,500/10,000/100 股）**全部被拒 EXPIRED**，原因 `The order quantity you entered exceeds your current holdings`（持仓 salable_qty=0）。**根因（用户 2026-08-03 14:55 纠正，非 T+2）**：老虎把「有挂单（如附加腿落成的 STP 止损单）的持仓」可平额度扣减为 0（salable = 持仓 − 挂单占用；与 CHANGELOG 2026-08-03 老虎清仓条目「02800 持仓 1000 但 salable=500 分批平」同机制），新平仓单数量 > salable 即被拒；**先 `cancel_all_stop_orders_tiger` 撤全部止损单、再下 MKT 平仓单即立即成交**（实测撤 3 笔 STP 后 Buy MKT 38,500 立即 Filled @36.20）。旁证：7-31 开空 200 股时其 STP 被 REJECTED（未挂成止损），故 8-03 10:58 平它无阻碍。**改动**：① `close_position_tiger.py`——平仓顺序从「先 MKT 平仓、成功后才撤止损」（沿长桥逻辑，对老虎错误）改为「**先撤该标的全部未触发止损单、再 MKT 平仓**」+ docstring 实测记录更新；② `trade_utils_tiger.py` 模块 docstring 平仓说明同步（先撤止损再平仓）；③ `auto-mode.md` 老虎默认账户段实测状态补充该 bug 与正确顺序；④ TODO.md 的「T+2 交收限制」错误推断条目打 `✅**已更新**`（非 T+2、根因为挂单占用可平额度），由本修复覆盖。**该笔 07709 空单（38,500 股 @36.32 → 36.20）因基础设施问题作废，未计入 actions/**。触发原因：auto 盯盘平仓被拒暴露脚本顺序错误（用户实测提示老虎需先撤止损再平仓），用户指示优先处理基础设施问题。

- **港股长桥三动作开盘实测完成 + 老虎 paper 清仓（2026-08-03）**：TODO「港股长桥三动作开盘实测」待办完成（用户**特别说明用长桥**）。长桥 paper（env-paper，与美股同账户）小仓位端到端实测 HK.00700 腾讯**做多 100 股（1 手）**：① **开仓**——REST 主单 LO FILLED @487.0 + 附加 STOP_LOSS MIT @480 一次提交接受，但**模拟盘附加单不激活**（REST 查主单详情 trigger_status=NOT_USED、不生成独立订单——长桥模拟盘附加订单静默无效，实盘前需验证）；② **移损**——SDK MIT @485 提交成功（状态 OrderStatus.VarietiesNotReported，模拟盘「品种未上报」等待态、属活动单可撤），但**撤旧失效暴露 bug**：`get_open_position_hk` / `cancel_all_stop_orders_hk` 对传入的**富途格式** symbol 误调 `to_futu_symbol`（"HK.00700"→"00700.HK" 错乱）致持仓 / 止损匹配永远失败（移损「未读到持仓」warning、prior_stops_cancelled=0），已修复（target 直接用富途格式比较，匹配持仓与订单的 `to_futu_symbol` 转换结果）；③ **平仓**——MO Filled @487.6（长桥订单 avg_fill_price 取不到、last 兜底——观察点）+ 手动撤 MIT；账户回到仅剩 MU.US 158。**老虎 paper 清仓**（用户指示「所有模拟账户最后回空仓」）：02800 盈富多 1000 分两笔平（Sell 500 @26.46 + Sell 500 @26.48，顺带撤 5 个历史残留 STP 单）、07709 三星 2x 杠杆空 200 平（Buy 200 @36.8，撤 1 个历史残留 STP 单），账户空仓（净值 1,000,127.42 USD）。**实测发现：老虎 HK 平仓超可卖数量被拒**——02800 持仓 1000 但 `salable_qty=500`，Sell 1000 报 Invalid（EXPIRED）且无错误信息，按可卖分批平即可（salable 随持仓更新）。触发原因：用户授权用夜盘做美股待办、特别说明用长桥做港股实测、指示所有模拟账户回空仓；按「不干扰并行盯盘会话（signal 模式）」全程未写 actions/、signals/、tmp/ 文件。

- **港股老虎三动作开盘实测完成（2026-08-03）**：TODO「老虎 paper 账户开盘实测三动作」待办完成。paper 账户（17 位模拟账户号）小仓位端到端实测 HK.00700 腾讯**做多 100 股（1 手）**三步全链路：① **开仓**——LMT 主单 FILLED @486.2 + 附加腿 OrderLeg('LOSS', 479) 一次提交，附加腿落成**独立 STP 单**、主单成交后进入 HELD 监控（老虎附加腿激活行为实测确认）；② **移损**——独立 STP（trigger 484.0）先下、旧附加腿撤掉（CANCELLED），先新增后撤旧工作正常，**开仓附加腿落成的 STP 单可独立撤销**（修正 trade_utils_tiger.cancel_all_stop_orders_tiger 注释里「腿不能单独撤」的顾虑）；③ **平仓**——MKT Filled @486.0（avg_fill_price 真实成交价，非 last 兜底）+ 撤全部未触发止损单；账户回到原 2 笔历史持仓（02800 多 1000 / 07709 空 200，实测保留不动）。**实测暴露并修复 2 个 bug**：① `trade_utils_tiger._make_order` 的 order_type 传枚举对象 OrderType.LMT → place_order 序列化崩溃（TypeError: Object of type OrderType is not JSON serializable）——Order 构造函数注释写明预期字符串（'MKT'/'LMT'/'STP'），改传字符串（4 处，含开仓/平仓/移损三个调用点）；② `check_order_filled_tiger` 直接 str(OrderStatus 枚举) 得 'OrderStatus.FILLED'、`'Filled' in` 它恒 False → **开仓主单实际 FILLED @486.2 却误判「未成交」并尝试撤已成交单**（撤单无效、持仓实际建立、附加腿还挂着——MU 事故同款「券商报成交、脚本判错」风险场景），改为取 `status.value`（'Filled'）再判断，平仓回查即正确识别 Filled。**同步**：open_position_tiger.py / close_position_tiger.py / move_stop_tiger.py / trade_utils_tiger.py 四处「⚠️ 待开盘实测、不得真实下单」docstring 标注更新为「✅ 2026-08-03 paper 实测通过」；auto-mode.md 老虎默认账户段实测状态同步；TODO.md 第 5、6 条挂 ✅**已完成**。触发原因：用户授权实测 TODO 待办（paper 模拟盘、小仓位、测完即平）；按「不干扰并行盯盘会话（signal 模式）」要求，全程未写 actions/、signals/、tmp/ 文件、三步快速完成，盯盘会话不受影响。

- **缩短「早盘开盘首小时只观察」规定为条件触发（2026-08-03 用户立）**：原 [SKILL.md:90](.claude/skills/trade/SKILL.md#L90) 护栏第 7 条规定港股 09:30-10:30 / 美股 09:30-10:30 首小时「不发任何交易动作（含开仓、平仓、移损）、只观察、10:30 后才允许开仓」（2026-07-31 立）。三个问题：① **时间太长**——开盘最混乱的是前 15-20 分钟冲动盘消化期、30 分钟后趋势往往已清晰，卡满 60 分钟等于放弃全天流动性最好、波动最大的黄金时段（项目自己反复强调「早盘必密采样、机会与风险都在这」）；港股上午盘仅 2.5 小时、砍首小时后只剩 1.5 小时可交易，伤害远大于美股（全天 6.5 小时）。② **一刀切与既有实战教训冲突**——2026-07-17 MU 头肩底 09:48 破颈线确认做多（[trading-strategy.md:138](.claude/skills/trade/references/trading-strategy.md#L138)，之后涨 +29 点）、2026-07-17 MU 连破四道阻力执念踏空（[trading-strategy.md:268](.claude/skills/trade/references/trading-strategy.md#L268)，核心「方向变了立刻切、别固守」），首小时一刀切既会错过 09:48 这类好信号、又与项目「动态修正方向」精神相悖。③ **连平仓 / 移损都禁是风控隐患**——正常有「持仓不过夜」纪律、首小时无仓；但万一前日漏平（美股夜盘到美东开盘尤其可能），首小时价格急速反向却不能移损 / 平仓 = 裸奔，平仓移损是风控动作任何时候都应可用。**改法（用户选「纯条件触发」方案）**：删除「开盘 N 分钟禁交易」的机械时点，改为**开盘后过完「开仓前自查清单」（全貌判行情性质 + VWAP 方向 + 位置评估硬闸 + 形态 + EV ≥ 1.2 + 置信度 🟢）即允许开仓**——开盘初期数据不稳 / VWAP 未成型 / 形态未构筑，自查清单自然过不了（等价于等数据稳定），条件满足即开（如 09:48 头肩底破颈线确认），不机械卡表；**平仓 🔴、移损 🟡 任何时候都不被观察期挡**（风控动作，前日漏平时开盘急速反向也须能立即处置）；**暴跌标的「开盘 30 分钟内不开多、等双底 / 头肩底确认」规则保留不动**（策略层接飞刀防护，在 [trading-strategy.md:109](.claude/skills/trade/references/trading-strategy.md#L109)，独立于 SKILL.md 禁令、不靠全市场禁交易覆盖）。**评估对 07-17 教训影响：修复冲突而非回归**——09:48 头肩底破颈线这类开盘初期即确认的有效信号不再被禁令错过、与「动态修正方向」精神一致；暴跌标的接飞刀防护由策略层精准规则（双底 / 头肩底 + 开盘 30 分钟不开多）兜底、不依赖过宽的全市场禁交易。**联动**：[monitoring.md:129](.claude/skills/trade/references/monitoring.md#L129) / [:153](.claude/skills/trade/references/monitoring.md#L153) 的「开盘首小时」是**采样密度语境**（开盘首小时波动最剧烈、密采样可到 30 秒），非交易禁令、保留不动（删禁令不影响采样建议）。触发原因：用户立 TODO（记录 2026-08-03 10:18）指出原规定太长 + 一刀切 + 风控隐患，要求缩短 / 改条件触发。

- **默认执行模式改为信号（signal），自动交易（auto）需用户特别说明才用（2026-08-03 用户立）**：用户决定把全项目默认执行模式从 auto 翻转为 signal——AI 只发信号、用户手动执行、不碰账户；auto 仅在用户**特别说明要自动交易**（说「自动下单 / 自动交易 / auto / AI 直接下单」等）时才启用。触发原因：默认不碰账户更稳妥——signal 模式 AI 不碰账户 / 资金 / 持仓，用户没明确要求自动交易时 AI 不会擅自下单；auto 作为「需特别说明才用」的高风险模式，天然多一道用户确认。改动：① `SKILL.md`——description（默认 signal + 切 auto 触发词）、首段「信号（signal，默认）」、触发条件、模式判定逻辑（默认 signal；「只发信号 / 手动执行 / 不要 AI 下单」本就默认、说了无害；「自动下单 / 自动交易 / auto / AI 直接下单」→ 切 auto）、模式提醒顺序（signal 在前标默认、auto 在后标「用户特别说明用自动交易时」）、安全说明补「默认是 signal、切 auto 瞬间务必重新亮 auto 提醒」、脚本 `--mode` 说明（signal 默认不传即 signal、auto 传 `--mode auto`）、「当前阶段」；② `references/auto-mode.md` / `references/signal-mode.md` 开头「默认模式 + 切换条件」说明对调；③ 项目 `.claude/CLAUDE.md` 模式描述翻转；④ `scripts/trade_utils.py` / `scripts/trade_utils_tiger.py` 的 `parse_mode()` 默认返回值 auto→signal（不传 `--mode` 或值非法一律返回 signal，与 skill 默认一致）、`load_equity()` 默认参数 `mode='auto'`→`'signal'`、`open_position.py` 注释同步——脚本默认不再连账户 API，AI 忘传 `--mode` 时也走 signal（equity 读 equity-log 累加值、不碰账户），auto 需显式 `--mode auto`；⑤ `TODO.md`「港股 default 从 signal 切 auto」待办被新决定取代（标记 `✅**已更新**` + 新增已完成条目，见 TODO.md「默认模式 = 信号」节）。实测：`parse_mode()` 不传参数返回 `signal`、传 `--mode auto` 返回 `auto`、resume.py 默认 signal 下 equity 读 equity-log（HKD，值略）。

- **主 `SKILL.md` 重写为双模式公共骨架（2026-08-01）**：从 306 行重写为纯公共骨架 + 模式路由。新增「模式开关与触发首动作（最高优先级）」节——**触发后第一件事亮模式提醒**（🔴🔴🔴 加粗：auto 模式带默认账户「港股老虎开放平台模拟 <HK_PAPER_ACCOUNT> / 美股长桥模拟 env-paper」、signal 模式标「只发信号、不碰账户 / 资金 / 持仓」），含模式判定逻辑（默认 auto；用户说「只发信号 / 手动执行 / 不要 AI 下单」切 signal）与模式路由（auto → `auto-mode.md`、signal → `signal-mode.md`）。description 改为体现双模式（默认自动交易 + 可切信号）。公共内容（第一原则、6 要素、公共硬护栏、时点平仓与停止盯盘约束、做空权限、盯盘工作流、操作原则、停盯总结、复盘、恢复协议、开仓前自查公共项）保留为主文件，auto / signal 专属内容下沉到各自 reference。**核心设计原则（用户立）：signal 与 auto 唯一区别 = 是否使用证券账户，交易策略完全通用**——signal-mode 只讲账户相关差异（执行、持仓跟踪、成交价来源、equity 来源、记录载体、止损单），其余一律引用公共。
- **`references/account-tools.md` 瘦身（2026-08-01）**：删下单 API 节（长桥交易 API、MIT 止损、主订单 vs 附加订单、港股长桥备选、实盘扩展，已挪入 `auto-mode.md`「下单 API 与订单机制」），只留行情数据源（富途 / 老虎 / 长桥行情 + 数据源分工 + 标的判定 + 标的范围 + 代码格式 + 做空查询 + anysearch 定位）。原因：行情数据 auto / signal 两模式共用，下单只有 auto 用；signal 模式加载时不必看下单 API。头部说明改写为「行情数据源公共参考」。
- **修正 move_stop 顺序文档滞后（2026-08-01）**：代码地面真相 = 先下新止损单、再撤旧止损单（CHANGELOG 2026-08-01「move_stop 顺序：先新增后撤旧」最新决定，`scripts/move_stop.py` 第 94-104 行实现、第 7-8 行注释也对）。但 `move_stop.py` docstring 第 2 行、`references/trading-strategy.md`「移动止损」段仍误写「先撤旧再新增」，已统一改正为「先新增后撤旧」。原因：文档未跟上代码最终决定；移损顺序涉及止损空窗安全（先撤旧会有瞬间无保护），必须以代码为准。
- **equity 来源按执行模式分 + 引入模式标识 `--mode`（2026-08-01）**：`trade_utils.load_equity` 重构为 `load_equity(mode)`——auto 连长桥账户 API 取真实 `account_balance().net_assets`（失败 fallback equity-log 并标记）、signal 读 `signals/equity-log.csv` 末行累加值（不连账户）；新增 `parse_mode()` 从命令行解析 `--mode auto/signal`（默认 auto，不破坏现有调用）。`preflight.py` / `resume.py` / `open_position.py` 三处调用点改为按模式取 equity：preflight/resume 的 equity 段从内联长桥查询（~40 行）简化为调 `load_equity(mode)`（~17 行，消除三处重复逻辑）；open_position 自动算仓位按模式取 equity、JSON 输出 `mode` + `equity_source`。触发原因：双模式重构后 auto（账户 API）与 signal（equity-log）两种 equity 来源需脚本分模式选；此前 preflight/resume 是 auto 口径、`trade_utils.load_equity` 却是 signal 口径，auto 开仓反而用 signal 的 equity-log 算仓位（矛盾，原 `todos/TODO.md` 指出）。AI 在对话判定模式后调脚本时传 `--mode signal`。实测：signal = equity-log 累加值（HKD，值略）、auto = 356,773 HKD（长桥真实 net_assets），两模式来源清晰区分。
- **修正 `load_equity` project_root 路径 bug（2026-08-01，原 load_equity 遗留）**：`load_equity` 的 `project_root` 原用 `Path(__file__).parent.parent.parent.parent`（= `.claude`，不是项目根——`trade_utils.py` 在 `.claude/skills/trade/scripts/`，上四级落到 `.claude`），equity-log 路径错指 `.claude/signals/`（不存在），signal 模式一直读不到 equity-log 而返回 initial_equity=100000 占位值。改为上五级（= 项目根），signal 模式现正确读到 `signals/equity-log.csv` 末行。
- **`todos/TODO.md` 全量待办补记时间戳 + 顶部加规矩提示（2026-08-01）**：配合全局 CLAUDE.md 当日新立的「待办统一存 todos/」补充规矩（每条待办带记录时间戳、精确到分钟；前后矛盾以最新时间戳为准），给现有 10 条顶层待办统一补 `（记录：2026-08-01 22:54）`——`todos/` 未进 git、各条写下时间已不可考，故统一取补记时刻；顶部说明 blockquote 加一行指向该规矩。触发原因：存量待办此前无时间戳，不满足新规矩。
- **f_max 硬上限从 2.5% 提到 10% 后同步文档残留（2026-08-01）**：用户已把 `config.json` 的 `risk.f_max` 从 0.025 改为 0.10（10%），但参数值改动后文档 / 注释未跟上、仍写旧的 2.5%，导致参数（10%）与规范文字（2.5%）打架——盯盘时 AI 读 `SKILL.md` / `risk-management.md` 会按过时的 2.5% 执行，f_max=10% 形同虚设。同步修正 4 处残留：① `config.json` 的 `_comment_fmax` 注释重写为反映 f_max 10% 的新逻辑（max_loss 上限是 B 的 5 倍、给 lot_size 离散选档留足向上余量，选最接近 B 的档即可、很难触顶）；② `SKILL.md` 第 84 行公共硬护栏「硬上限 2.5%」→「当前 10%」；③ `references/risk-management.md` 第 34 行「f_max 当前 2.5%」→「10%」；④ `config.example.json` 模板 `f_max` 0.025→0.10 + 注释同步（避免模板与实际配置分叉）。触发原因：参数是风控执行依据，文档与参数不一致会让护栏失效。

- **待办文件从 `todos/TODO.md` 迁到项目根 `TODO.md`（2026-08-02）**：配合全局 CLAUDE.md 当日修订的「待办统一存项目根 TODO.md」（不再用 `todos/` 目录）规矩，把待办文件从 `todos/` 子目录移到项目根 `TODO.md`、删除空的 `todos/` 目录；`TODO.md` 头部说明同步更新（「本目录」→「本文件」、引用新规、做完待办从「删除该条」改为「`[ ]` 改 `✅` 保留 + 补完成时间戳 `（完成：YYYY-MM-DD HH:MM）`」）。**纯工作区操作，未动 git 暂存区**——遵循同日新立的全局硬规「Git 暂存区禁止 AI 自主增删改」，迁移用普通 `mv` 而非 `git mv`。
- **`open_position.py` lot_size 注释改陈述性 + 待办并入 `TODO.md`（2026-08-02）**：`open_position.py` 第 130 行注释原含「（应从富途 snapshot 取真实每手股数，见 todos/）」的待办语义、且指向已弃用的 `todos/`；按新规「代码注释不留待办语义」改为纯陈述（`# 美股可零股；港股 lot_size 当前硬编码 100`），并把该待办正式补进 `TODO.md`「代码 / 机制」节——港股 lot_size 应从行情接口取真实每手股数（当前硬编码 100，港股转老虎后需确认实际行情源与取数字段）。
- **TODO.md 模式决定过期待办措辞更新 + 标记格式升级（2026-08-02）**：用户最新决定 = 信号模式与自动交易模式二者共存（非放弃 signal 改纯 auto），但 `TODO.md` 部分内容仍停留在「放弃信号模式」的旧意思，已更新。具体改动：①「港股老虎模拟账户三动作」节说明「建好后才能做『港股转自动交易 + 改信号模式』」→「『港股 default 从 signal 切 auto』（signal 模式保留，两者共存）」；② section 标题「港股转自动交易 + 改信号模式」→「港股 default 从 signal 切 auto（signal 模式保留，两者共存）」；③ 旧待办「港股完全转自动交易」**原条目内容一字不动**、只在开头挂 `✅**已更新**`（更新：2026-08-02 14:14）标记，**另起一段新增**待办「港股 default 从 signal 切 auto（signal 模式保留，两者共存）」反映最新决定；④ 顶部规矩提示同步升级为双状态标记（完成 `✅**已完成**` / 更新 `✅**已更新**`，「已完成」「已更新」加粗区分）；**执行语义**：凡 `✅` 开头的条目（无论已更新还是已完成）都退出活跃待办、看到即跳过，活跃待办只有 `[ ]` 开头的条目。触发原因：决定从「放弃 signal 改纯 auto」变为「两者共存」后，待办措辞需同步，否则误导后续判断；同时用户立标记格式新规（完成 vs 更新双状态加粗区分）。

- **auto-mode.md「主订单 vs 附加订单」概念重写为「订单类型 vs 附加订单（开仓打包封装）」（2026-08-02）**：用户指出原文把「主订单/附加订单」当成订单的固有分类（「主订单 = 开仓单」），概念不到位——它俩只是**开仓把 `LO`+`MIT` 打包一次提交时的相对称呼、只在开仓场景才有**（查证长桥官方「附加订单」帮助页坐实：主订单仅支持开仓、平仓无附加单入口；主单完全成交 + 到触发价附加才提交；止损触发时系统自动提交与主单方向相反的市价单——方向与触发符号由后端自动定、用户不传 side / `trigger_direction`），本质等价于「先 `LO` 开仓、再独立下 `MIT` 止损」。整节重写为两层框架：① 订单类型（`LO`/`MO`/`MIT`/`LIT` 等本质，三类动作各对应一种）vs ② 附加订单（开仓打包封装，两种等价挂法对比：① 打包 `submit_order_with_stop` vs ② 拆开独立 `MIT` `submit_stop_order`）。联动修正护栏 1 一处逻辑错乱：「禁止用一笔反向的主订单去给另一笔已成交的主订单做止损」——已成交的开仓单不再叫主订单（是持仓）——改为「止损只能用条件单 `MIT`/`STOP_LOSS`、不能用反向普通订单 `LO`/`MO` 冒充」（`LO`/`MO` 没有「价格到触发价才成交」语义、起不到条件止损，MU 事故真因之一即误用 `MO`）；护栏 1 开头去掉与新节重复的冗长定义改指向；SKILL.md / account-tools.md 节标题引用同步；`open_position.py` / `trade_utils.py` 订单模型 docstring 校准为同一框架。触发原因：原文「主订单 = 开仓单」「已成交的主订单」「主订单给主订单止损」等表述把开仓打包的相对概念越界外推到非开仓场景，概念不严谨会误导止损单选用判断。

- **auto-mode.md「止损单用 MIT」节再修正：MIT 不自动定向，裸 MIT 与 STOP_LOSS 封装分层讲清（2026-08-02）**：接上一条，用户再指出 MU 事故段「用了 MIT 后方向自动正确」表述不对——MIT 只是通用条件单，方向（side）、符号、触发价、数量都可由调用方设置（如「价格大于 X 卖出」就不是多仓止损），MIT 本身不会自动定向。核实 [move_stop.py](.claude/skills/trade/scripts/move_stop.py) 确证：`submit_stop_order` 走裸 MIT、side 由调用方按持仓方向算（做多→Sell、做空→Buy）。据此把「止损单用 MIT（券商自动定向）」节重写为「给已有持仓加止损用裸 MIT（调用方定 side，触发方向自动）」：① 长桥下单 API order_type 枚举无 STOP_LOSS（仅 `LO`/`MO`/`MIT`/`LIT`，STOP_LOSS 只作附加订单 `attached_order_type`），给已有持仓加止损没有 STOP_LOSS 封装可用、只能下裸 MIT；② 裸 MIT 与 STOP_LOSS 封装**功能等价**（都「价格到触发价市价平仓」），但 STOP_LOSS 由券商后端自动配方向 + 符号、裸 MIT **全靠代码配 side / 符号 / 触发价 / 数量**（无券商兜底——side 传反反向开仓、量超持仓判失效、符号设反立即触发）；③ MIT 唯一「自动」的是**触发方向**（涨/跌，由 `trigger_price` 相对现价判定、长桥 MIT 无 `trigger_direction` 字段），不是 side 自动。联动修正：MU 事故段「用了 MIT 后方向自动正确」→「MIT 不自动选 side、方向正确是调用方传对 side」；`trade_utils.submit_stop_order` / `trade_utils_hk.submit_stop_order_hk` docstring 的「券商自动定向」表述同步校准。触发原因：原「MIT 自动定向」把 STOP_LOSS 封装的自动性错加到裸 MIT 上，会让人误以为用 MIT 就不用管 side，埋下 side 传反的反向开仓隐患。

- **止损 order_type 枚举查证坐实 + 修 SLO 误解 + 修 `_key` bug（2026-08-02）**：用户追问「已有持仓加止损有没有 `STOP_LOSS` / `SMO` 封装」。本机 longport SDK v3.0.23 OrderType 枚举实测 + 长桥官方「[交易命名词典](https://open.longbridge.com/zh-CN/docs/trade/trade-definition)」双重坐实：港股/美股 order_type 完整枚举为 `LO`/`ELO`/`MO`/`AO`/`ALO`/`ODD`/`LIT`/`MIT`/`TSLPAMT`/`TSLPPCT`/`SLO`（SDK 另有 `TSMAMT`/`TSMPCT` 跟踪止损市价单），**无 `STOP_LOSS`、无 `SMO`、无 `STP`/`STOP`**——这些独立「止损单」类型在长桥不存在，`STOP_LOSS` 仅作开仓附加订单 `attached_order_type` 出现；用户猜测的 `SMO` 也排除。据此：① auto-mode.md「给已有持仓加止损用裸 MIT」节补完整枚举 + 排除 `SMO`/`STP`/`STOP`（双重出处），把原「order_type 枚举只有 LO/MO/MIT/LIT 等」的**推断**改为**实测坐实**（之前从 submit order 参数描述推断、没看完整枚举，不严谨）；② 修护栏「禁止限价止损（LO / STP_LMIT / SLO）」的事实错误——`SLO` 是长桥「特殊限价单」非止损限价单、`STP_LMT` 是老虎枚举长桥无此类型，改为用长桥实际存在的 `LIT`（限价触发单）举例；③ 修 `risk-management.md` 第 85/89 行「止损单必须用市价单（MO）」——**这是 MU 事故的真因**（`MO` 提交即按市价成交、`trigger_price` 被忽略、起不到条件止损，正是 2026-07-31 亏 $119 的直接原因），与同仓库 `auto-mode.md` 记录的事故教训自相矛盾，改为「止损用 `MIT` 市价触发单、禁止 `MO` 与限价触发单 `LIT`」；该处同样把老虎枚举 `STP_LMT` 当成长桥类型，一并修正。另：查 MU 持仓时撞出 `trade_utils.get_open_position` 的 `NameError: _key`——`_key` 函数被调用（[第 541-542 行](.claude/skills/trade/scripts/trade_utils.py#L541)）但全文件未定义，[close_position.py](.claude/skills/trade/scripts/close_position.py) 一键平仓传 symbol 查持仓依赖此、会崩，补 `def _key(symbol)`（富途 / 长桥两种 symbol 格式双向归一到 `market.code`、港股去前导 0）。触发原因：用户质疑原文「已有持仓加止损没有 STOP_LOSS 可用」是否准确，实测确认结论方向对但论证不严谨，连带暴露 `SLO` 误解与 `_key` bug 两个独立缺陷。
- **GitHub About description 由纯英文改回中英双语（2026-08-02）**：commit skill 第 9c 步规则当前要求 About description 为中英双语（英文为主 + 简体中文摘要），但项目级 `.claude/CLAUDE.md` 的 `github-about` 缓存标记与 About 实际内容都停留在 2026-07-18 的「纯英文」（当时 9c 规则确实要求纯英文，后来 9c 改为要求双语，缓存与 About 实际内容没跟着同步，属规则演进后的滞后）。本次 `gh repo edit` 把 description 改回「English | 简体中文」双语——英文 `AI day-trading agent that auto-executes HK/US equity trades on paper accounts (built on Claude Code)` + 简体中文 `AI 日内交易代理，自动执行港股 / 美股模拟盘交易（基于 Claude Code 构建）`；同步把 `.claude/CLAUDE.md` 的 `github-about` 缓存标记从「纯英文 description」更新为「中英双语 description」。topics 保留不动。触发原因：/commit 时发现 9c 缓存标记记录的「纯英文」与当前 9c「中英双语」规则不一致，对齐以免后续 /commit 误判与反复提示。
- **老虎模拟盘账户号在文档中改占位符脱敏（2026-08-02）**：老虎开放平台模拟账户号原以明文出现在 4 处文档（`SKILL.md` auto 模式提醒、`auto-mode.md` 总则、`CHANGELOG` 2026-07-31 与 2026-08-01 两条记录），公开仓库会暴露个人模拟账户标识。统一改为占位符 `<HK_PAPER_ACCOUNT>`，4 处的券商名、主备账户表述与上下文一律不动——只藏号、不改语义。不影响功能：该账户号在 `scripts/` 交易脚本里从未被引用（全仓 grep 零命中），港股交易脚本 `_hk.py` 通过长桥 SDK 凭证自动关联账户、与文档里的账户号无关。触发原因：/commit 敏感扫描发现该模拟账户号暴露在公开仓库，用户决定脱敏。券商主备表述（老虎为主账户、长桥为备选）是港股的规划描述、本次一律不动——港股老虎三动作脚本（`*_tiger.py`）尚未建（见 `TODO.md`「港股老虎模拟账户三动作」节，该进度差已有待办记录），当前港股 auto 默认账户号先用占位符 `<HK_PAPER_ACCOUNT>` 占位，待老虎接通时账户号走配置。

- **老虎 SDK 港股研究（盘后可做部分完成）+ 老虎四脚本建成 + 美股脚本港股死分支清理（2026-08-02）**：执行 `TODO.md`「港股老虎模拟账户三动作」节待办（能做的部分）。① **研究老虎 SDK 港股细节**——盘后完成并可实做的全部项，逐一实测坐实：配置加载根因 = `get_client_config(props_path=...)` 先硬读 `private_key_path` 参数、None 即 `TypeError`（读不到 properties 内嵌私钥），正确姿势 `TigerOpenClientConfig(props_path=本机 SDK 凭证目录)`（私钥自动从 private_key_pk1/pk8 读，实测加载成功）；paper 判定 = account 为 17 位纯数字账户号 → `is_paper=True`、网关域名自动走 `license-PAPER`（domain_conf 已含 `TBNZ-PAPER`/`TBSG-PAPER`，实测确认；当前配置 account 是 7 位实盘号）；港股 symbol 格式只认 **5 位带前导 0 的裸数字代码**（`'02800'`/`'00700'` 实测 OK，`HK.02800`/`2800.HK`/`700.HK`/`0700.HK` 均报「We don't support trading of this」）；lot_size 从 `get_contract.lot_size` 取（实测 02800=500 盈富、00700=100 腾讯）、tick 从 `get_contract.tick_sizes` 取（港交所价位表，实测完整返回 11-13 档）；附加止损 = `OrderLeg('LOSS', price)`（源码确认**仅限价单支持**，对应长桥 attached STOP_LOSS）；TradeClient API 结构与只读链路实测通过（`get_assets().summary.net_liquidation` 净值、`get_positions`（Position 无方向字段、quantity 正=多负=空）、`get_orders` / `get_open_orders` / `cancel_order(id=全局id)`、`create_order`+`place_order`、订单状态枚举 Filled/Cancelled/Inactive/Invalid 与长桥不同）；行情 `QuoteClient.get_stock_briefs` 返回 DataFrame（latest_price/ask/bid，实测通过）。② **新建 `trade_utils_tiger.py`**（老虎封装，自包含不依赖长桥/美股）：配置加载、symbol/lot/tick、行情、开仓 LMT+附加腿、平仓 MKT、独立止损 STP（长桥对应 MIT）、持仓/资产/订单查询、撤单、成交回查、撤全部未触发止损（STP 独立单 + LOSS 附加腿）、价格范围/仓位计算（纯函数独立实现）；`load_equity_tiger` 对「账户未开通交易/资产权限 → summary 全 0 且无时间戳」返回 None 并告警（实测当前实盘账户 <HK_LIVE_ACCOUNT> 即此状态，禁止用 0 净值算仓位）。③ **新建 `open_position_tiger.py` / `close_position_tiger.py` / `move_stop_tiger.py`**（与规范一致：开仓 LMT+附加止损腿、平仓 MKT+撤止损、移损 STP 先新增后撤旧且量=持仓；自动算仓位 equity 走老虎净值、lot_size 走 get_contract）。④ **美股 `open_position.py` 港股 lot_size 硬编码 100 死分支清理**：加「只处理美股（US.xxx）」校验（港股脚本负责港股，分而治之），删 `lot_size=100` 分支（港股每手因标而异，由港股脚本从行情接口取）。⑤ **`auto-mode.md` 脚本清单补老虎默认账户段**（含实测状态标注：只读链路已实测、下单链路待 paper 开盘实测，实测前不得真实下单；paper 接入 = properties 的 account 填 17 位模拟账户号）。⑥ 顺带确认 `TODO.md` 两条历史待办实际早已完成（`load_equity` 账户 API 改造随 2514d76 落地、open_position 已调 `load_equity(mode)`；港股 lot_size 长桥侧 `trade_utils_hk.get_lot_size_hk` 早已从 static_info 取真实值）——补挂 `✅**已完成**` 标记。触发原因：用户要求把 `TODO.md` 能做的待办都做掉；老虎是港股默认账户但三动作脚本此前缺失（SKILL.md「港股默认走老虎」是文档 ahead of 代码），本次把代码补齐到「只差 paper 账户号 + 开盘实测」的状态，剩余待办（paper 接入、开盘实测三动作）已更新到 `TODO.md`。

- **老虎 paper 模拟账户号恢复接入 + 实测通过（2026-08-02）**：`TODO.md`「老虎 paper 接入」待办的账户号部分完成。17 位模拟账户号 `<HK_PAPER_ACCOUNT>` 从 git 历史（2514d76，2026-08-02 脱敏提交 0dc0aff 之前）的 `SKILL.md` / `auto-mode.md` / `CHANGELOG` 明文恢复，写入本机 SDK 凭证目录主配置文件的 account 字段（原 7 位实盘号 <HK_LIVE_ACCOUNT> 备份至同目录 `.bak`）。实测验证：`TigerOpenClientConfig` 加载 `is_paper=True`、网关自动走 sandbox 域名（`openapi-sandbox.tigerfintech.com/hkg/gateway`）、`get_assets` 取到净值 **1,000,007.56 USD**（与 CHANGELOG 记载初始资金 $1,000,000 吻合）、`get_positions` 有 **2 笔历史持仓**（02800 盈富多 500 股 @26.3639、07709 三星 2x 杠杆产品空 200 股 @43.0638）——开盘实测三动作时先核实处置这两笔。期间一个小坑：properties 不支持行内注释，第一次写入带注释的 account 行被 SDK 原样读取导致 `is_paper=False` + 报「account not authorized」，去掉注释后恢复正常——SDK 按原始字符串解析 account、注释会破坏 17 位数字判定。触发原因：用户指出模拟账户号在 git 历史里（脱敏前是明文），恢复后 paper 接入即完成、只剩开盘实测下单链路；`TODO.md` 原「paper 接入」待办挂 `✅**已更新**`、另起新待办「开盘实测三动作下单链路」（含 2 笔历史持仓核实）。

- **CHANGELOG.md / TODO.md 明文账户号改占位符脱敏（2026-08-02）**：本次新增的「老虎 paper 模拟账户号恢复接入 + 实测通过」等记录在描述接入过程时，把老虎模拟账户号与实盘账户号以明文写回——17 位模拟账户号（即 0dc0aff 提交已脱敏为 `<HK_PAPER_ACCOUNT>` 的那个号，CHANGELOG 1 处 + TODO 1 处）、7 位实盘账户号（CHANGELOG 2 处 + TODO 1 处），公开仓库会暴露个人账户标识，且与 0dc0aff 提交刚做的脱敏（模拟号改占位符 `<HK_PAPER_ACCOUNT>`）相矛盾。统一改为占位符：模拟号 → `<HK_PAPER_ACCOUNT>`、实盘号 → `<HK_LIVE_ACCOUNT>`（实盘号无既有占位符先例，本次新立），5 处周围的上下文一律不动——只藏号、不改语义。账户号实际值只存在本机 SDK 凭证文件与 git 历史（脱敏前明文），仓库文档不再保留数字。触发原因：/commit 敏感内容扫描（第 2 步）命中这两个文件中的明文账户号、本次 `/commit` 终止，用户指示把明文改占位符后重新提交。

- **signal 模式发信号时序改为「写文件 → 响铃 → 取响铃时刻价」三步连贯（2026-08-02 用户立）**：原时序「响铃最先 → snapshot 取价 → 写文件」（2026-07-29 用户立）反转为新时序——AI 拍板决定发信号后：① **第一个动作** `log_signal.sh` 写信号文件（信号内容先完整落盘、时间戳 = 写文件当下时刻 ≈ AI 拍板时刻；响铃时文件已就绪、用户可从文件查参考价 / 止损下单）→ ② **第二个动作** `ring.sh` 响铃（用户听到铃即下单，ring-log 记响铃时刻）→ ③ **第三个动作** snapshot 取响铃时刻价（`get_market_snapshot` 的 `last_price` 当成交价近似，append 实测价注释到该条信号末尾）。**三步连贯铁律（用户补充要求）**：三步之间不插入任何其它动作（不输出对话关键字段、不算参数、不跑其它查询），几秒内一气呵成——响铃与取价之间零间隔，取到的响铃实测价才近似用户实际成交价（用户听到铃即下单、取价时刻 ≈ 用户下单时刻）；中间插入其它动作会导致取价延迟 = 成交价偏差 = 复盘盈亏失真，某步失败立即修正重做、同样不插动作。工程联动：① `log_signal.sh` 时间戳从「读 ring-log 末行（= 响铃时刻）」改为「取写文件当下时刻」——写文件先于响铃、写文件时 ring-log 尚无本次响铃记录，读它会错误引用上一条信号的响铃时刻（已实测验证：ring-log 存在旧记录时仍取写文件当下时刻）；「响铃时刻 / ring-log / 信号文件时间戳」三者不再严格同刻，改为两处各自记录（文件时间戳 = 拍板时刻、ring-log = 响铃时刻，三步连贯下仅差秒级）；② 信号文件「成交价」栏改为先写「响铃后取价（见该条末尾注释）」占位、响铃取价后 append 实测价注释（写文件时响铃时刻价还没取到）；③ `ring.sh` / `alert.sh` 头注释、`SKILL.md` references 描述、`signal-mode.md` 全部时序引用点（信号输出与持仓检查、三类信号、主动平仓、signal 专属自查条目）同步为新时序。触发原因：用户要求 signal 模式 AI 拍板后第一动作改为写信号文件、第二响铃、第三取响铃时刻价，并强调三步足够连贯、中间无间隔以保证取到的响铃实测价近似实际成交价；新时序取代 2026-07-29「响铃最先」设计（当时为让响铃零延迟尽快提示用户下单，现改为信号文件先落盘 + 三步连贯）。

- **盯盘循环强制锚点：段结束输出自带 VWAP 检查（2026-08-03）**：`monitor_segment.py` 段结束自动打印「📊 VWAP 检查」——每个被采标的的现价 / VWAP / 相对位置（上方多头占优 / 下方空头占优），段结束唤醒 AI 后第一眼必看；配套 `SKILL.md` 监控循环主线补「先看段结束输出自带的 📊 VWAP 检查（必看、方向框架地面真相）」、「monitoring.md」VWAP 必看「节新增「📊 段结束必看 VWAP 输出（强制锚点）」小节（含边界：段结束 VWAP 检查只给「现价 vs VWAP」一维，全貌摘要 `monitor_summary.py` 仍要每 10 分钟定时重跑，两者互补不替代）。触发原因：2026-08-03 盯盘 07709 时全程只看 tail 单点价格 / 买卖比、没定时看全貌 + VWAP，把「低开高走偏多日」（VWAP 36.72 上方）定性成「空头日」一上午、错过回升段——根因是看 VWAP 靠记忆触发、无强制锚点；用户要求「确保 100% 不忘」，把 VWAP 数据主动塞进段结束输出（每次段结束唤醒第一眼即见），把「靠记忆」改成「数据主动出现」。

- **`trading-strategy.md` 补 2026-08-03 07709 盯盘复盘三条教训（2026-08-03）**：「做空合格入场」节补「反抽乏力 = 动能衰竭证据链，不是反抽高度」（07709 反抽只到 37.30 就衰竭、37.0-37.14 三次测试不过、11:09 破位，我机械等反抽到 37.5-37.9 错过 2.28R 零风险机会；顺趋势入场「等反抽」与「破位确认」两条腿并列；下跌中买卖比转正 ≠ 不能空）；「盘中动态修正方向」节补两条——「暴跌日方向由 VWAP / 分时结构定，不由跌幅 / 新闻定」（跳空低开跌幅是开盘瞬间的存量、开盘后分时站上 VWAP = 低开高走偏多日，我被存储崩盘新闻绑架预设空头、盯一上午 VWAP 上方的盘面视而不见，与 07-22 中芯「开盘预判当信仰」同款错误变体）+「全貌 + VWAP 必须定时重跑，不是开盘一次性动作」（07-27 MU「规定在 skill 里却没被调用」同款执行病）。触发原因：用户逐段复盘当天 07709 两段未被捕捉的走势（11:00-11:26 真下跌段错过、11:26-11:52 低点抬升的回升段被空头框架视而不见），追问盯盘是否定时看全貌，暴露方向框架被新闻绑架 + 全貌检查流程缺失两个根因，教训补进 reference 防复发。

**触发原因（整体）**：792e873（2026-07-30 信号模式）→ c36ecb0（2026-08-01 自动交易）切换后，signal 模式内容散落丢失。用户要求把 signal 模式作为独立可切换 skill 恢复、与 auto 区分，但避免做成两个触发并列的独立 skill（触发条件重叠会打架 + 公共盯盘内容重复致漂移）。采用 skill-creator 的 Progressive Disclosure：单 skill 双模式 + 模式开关，主文件公共骨架 + 两个 mode reference 按需加载，既区分执行方式、又不重复公共内容。

- **auto-mode 平仓定义统一为「modify 止损触发价=现价」方案（2026-08-05）**：脚本层 2026-08-05 已改用「把持仓止损单（开仓附加腿落成的 STP）触发价 modify 为现价、止损单立即触发市价平仓」消除「撤止损 + MO」race（见 `close_position_tiger.py` / `close_position_tiger_us.py`），但文档层定义仍写「市价单立即成交 + 平仓后撤止损」旧表述，与实现不符。本次把定义与引用点全部对齐脚本：`auto-mode.md`「平仓 🔴」定义（主动平仓 = modify 触发价为想平仓的价格（现价、取整 tick）、止损单触发市价成交；无活动止损直接市价单；modify 失败才 fallback 撤止损+市价单；平仓后不残留止损单）、工具描述（「平仓 MKT」改「平仓 modify 止损触发价」）、主动平仓说明、平仓记录格式「平仓价」栏、8-03 旧「做空平仓前置 = 必须无挂着的止损单」标注为旧实现限制（modify 方案已消除）；`SKILL.md` 硬护栏 4（平仓后不残留止损单表述）；`monitoring.md` 平仓脚本范例；`trading-strategy.md` 移动止损节「close_position 后 cancel_all_stop_orders 兜底」改「平仓不残留止损单」。触发原因：用户指出在老虎账户下平仓就是直接修改开仓自带的附加止损单触发价为想平仓的价格，无需「撤止损 + 市价单」两步。

- **trade skill 公共函数迁移到老虎工具库（2026-08-05）**：长桥脚本删除后，`monitor_segment.py` / `resume.py` / `monitor_summary.py` / `preflight.py` 依赖的公共函数改从 `trade_utils_tiger.py` 取——`load_equity`（去掉长桥分支，只走老虎账户 + equity-log fallback）迁入 `trade_utils_tiger.py`；新增 `get_today_orders_tiger`（老虎 `get_orders` 查当日订单，供每轮采样提取最新止损价）；`monitor_segment.py` 的 `query_stop_prices` 改为查老虎当日订单、提取 STP 止损单 `aux_price` 触发价（已实测：老虎账户当日订单 63 笔、含 STP 止损单，链路正常）。`auto-mode.md`「auto 专属硬护栏」第 1 条改写为老虎订单术语（开仓 LMT + OrderLeg('LOSS') 附加腿一次提交、独立止损 STP、禁止市价单冒充止损）；`risk-management.md` 止损单规则同步改老虎术语（STP / MKT / STP_LMT）；`SKILL.md` / `monitoring.md` 等文档中对已删长桥脚本的调用范例与引用同步改为老虎脚本。触发原因：同日用户立放弃长桥，所有长桥相关内容删除，公共依赖随之迁移（见下方「移除」）。

### 移除

- **长桥（longport / longbridge）全部内容（2026-08-05 用户立：放弃长桥、确定长桥账户用不了，港美股均走老虎模拟账户、不再有备选）**：
  - **交易脚本 8 个**：美股长桥 `trade_utils.py` / `open_position.py` / `close_position.py` / `move_stop.py`、港股长桥 `trade_utils_hk.py` / `open_position_hk.py` / `close_position_hk.py` / `move_stop_hk.py`（git 历史可追溯）。
  - **`auto-mode.md`**：长桥备选账户总则段、「下单 API 与订单机制」整节（长桥模拟盘交易 API、裸 MIT 止损单、订单类型 vs 附加订单、港股长桥备选账户、长桥实盘扩展）。
  - **`SKILL.md`**：auto 模式提醒中的长桥备选表述、参考文件描述、当前阶段中已过时的「美股默认长桥模拟账户」表述、做空能力查询中的长桥 API 提及。
  - **`risk-management.md`**：美股默认长桥账户表述、MIT 止损术语。
  - **`account-tools.md`**：「长桥第三备份（行情降级兜底）」整节、美股默认长桥表述、做空查询中的长桥 API 提及。
  - **数据源文档与配置中的长桥提及**：`futu-opend-level2.md` 对比表删「长桥 CLI」列（表题「三源对比」改「双源对比」）、`hk-level2-sources.md`、`tiger-websocket.md`、`trading-strategy.md`、`monitoring.md`（含脚本调用范例改老虎脚本）、`accounts.md`、`accounts.example.json`、项目内账户配置文件（注释改老虎 API 核实）、`classify_hk_security.py`（注释去长桥背景）。
  - 原因：2026-08-05 确定长桥账户暂时用不了（平仓 race condition、独立 MIT replace 受市价保护机制拒、附加订单 paper 不激活三项机制问题），用户决定放弃长桥、不再作为 auto 模式备选账户，所有长桥相关内容删除、港美股统一走老虎模拟账户；「止损只能用条件单、不能用市价单冒充」的教训以老虎术语（STP / 附加腿 LOSS / MKT）保留在 `auto-mode.md` 硬护栏与 `risk-management.md`。

## [0.1.0] - 2026-07-30

首个正式记录的版本。涵盖项目从初始搭建到 2026-07-30 的全部迭代。

### 新增

- **项目脚手架**：MIT 许可证、双语 README（中/英）、LOGO、shields.io 徽章。
- **Trade skill（`.claude/skills/trade/`）**：日内交易核心执行规范——盯盘、信号、复盘全流程。
- **自动交易模式**（2026-07-30，2026-07-31 扩展到港股）：港股默认用老虎开放平台模拟账户、美股默认用长桥模拟账户自动下单，**3 类动作**（开仓/平仓/移损，禁止加仓减仓）直接调用券商 API 执行。如用户不使用默认账户会特别说明。触发原因：信号模式积累足够样本后，升级到自动执行以减少信号到下单的延迟、消除人工执行误差。核心新增：
  - `scripts/trade_utils.py` — 长桥 API 封装（配置加载、报价查询、限价单、止损条件单、持仓查询、订单查询）、价格范围计算、仓位计算
  - `scripts/open_position.py` — 开仓动作（6 要素校验 + 价格范围检查 + 限价单 + 止损条件单）
  - `scripts/close_position.py` — 平仓动作（限价→市价递进重试；成功后自动撤销所有未触发条件单）
  - `scripts/move_stop.py` — 移动止损（不删旧止损单，新增止损条件单）
  - 价格范围公式：做多 `[参考价 − R₀ × 0.8, 参考价 + 参考价 × 3/8]`、做空 `[参考价 − 参考价 × 3/8, 参考价 + R₀ × 0.8]`（R₀ = |参考价 − 止损价|；80% 来自风险距离，3/8 来自参考价本身）
- **`monitor_segment.py` 每次采样获取最新止损单**（2026-07-30）：新增 `stop_price` 列到采样 log，每次采样查询长桥当日订单提取最新止损条件单的止损价（频率随采样间隔走，不固定 30 秒）。原因：用户可能在券商 App 里手动添加止损单，止损价不能凭记忆，必须实时获取。
- **`actions/` 目录 + `log_action.sh`**（2026-07-30）：美股模拟盘交易动作记录目录，`actions/YYYY-MM-DD-ET-actions.md`。与 `signals/`（港股信号模式）分工——美股记录已执行的交易动作（含 order_id、成交价），港股记录待执行的信号。新增 `log_action.sh` 脚本写入动作文件（内容经 stdin 传入，自动带时间戳）。
- **富途牛牛 + Longbridge 双数据源**：富途为主、Longbridge appkey 模式为备用行情源。
- **信号文件与响铃**：`signals/` 目录记录每日港美信号（`YYYY-MM-DD-HKT-signals.md` / `YYYY-MM-DD-ET-signals.md`）；`alert.sh` 写文件 + 响铃提醒；`log_signal` / `log_ring` 脚本标准化日志。
- **复盘体系（`reviews/`）**：`review.py` 回测统计脚本；每日复盘报告含贝叶斯 P(EV>0) 时序图、累计 R 均值时序图、胜率时序图。
- **监控脚本**：`monitor_segment` / `monitor_summary` 多标的分段监控；`resume.py` 恢复监控；keep-awake skill 防止系统休眠。
- **交易规则迭代**：方向评估 + 置信分级 + 量价入场过滤；半仓轮动（震荡市）；反向 ETF 优先做空规则；按批次信号 + 分数风险定仓。

### 变更

- **港股交易默认用老虎开放平台模拟账户，美股用长桥模拟账户**（2026-07-31 用户规定）：港股账户编号 `<HK_PAPER_ACCOUNT>`（`is_paper=True`，初始资金 $1,000,000 USD）；美股用长桥 `~/.longbridge/openapi/env-paper`。实盘账户后续添加，用户特别说明时才用。
- **早盘开盘首小时只观察不交易**（2026-07-31）：港股 09:30-10:30 / 美股 09:30-10:30 不发任何交易动作，只观察行情性质（震荡/趋势、多头/空头）。10:30 后才允许开仓。若用户 10:30 后才启动盯盘，先分析前一小时数据再进入交易流程。
- **Trade skill 按 skill-creator 标准结构性重写**（2026-07-31）：主 SKILL.md 从 543 行精简到 301 行（操作主线 + 29 处 references 指针），详细规则按主题拆到 4 个新建 references 文件（`risk-management.md` 90 行、`trading-strategy.md` 276 行、`monitoring.md` 184 行、`account-tools.md` 75 行）；`review-and-evaluation.md` 未动。触发原因：违反 skill-creator 的 Progressive Disclosure 原则（主文件应 <500 行、大块内容拆 references），且全文混杂核心规则、风控细节、几十条交易教训、盯盘工程参数。重写还顺带：去重（3 类动作定义从 3-4 处合并成 1 处）、description 改 pushy（强调触发场景必须激活）、硬约束改「解释 why」（风控红线保持硬约束）、清理残留信号模式措辞（0 残留）。所有交易规则、阈值、公式、反面教训逐字保留，只动结构和措辞。
- AutoMemory 退役：内容提炼进 trade SKILL.md，`autoMemoryDirectory` 配置移除。
- 美股监控延长至全日内（取消 12:00 ET 截断）。
- 信号流程重构：先写文件 + 响铃，再在对话中输出。
- Longbridge 切换到主账户。
- **Trade SKILL.md 信号模式→自动交易模式**（2026-07-30）：SKILL.md 描述、模式总则、脚本库、自查清单、执行反馈、账户工具链、当前阶段等章节全面更新——从「AI 只发信号、用户手动执行」改为「AI 调用脚本自动下单」。涉及文件：`.claude/skills/trade/SKILL.md`、`.claude/skills/trade/scripts/monitor_segment.py`（新增 stop_price 列 + 止损单查询）。
- README 语言切换链接文字统一为「简体中文」；版权人统一为 All Contributors。
- GitHub About 配置为纯英文 description + topics。
- **全局 CLAUDE.md 新增「CHANGELOG 记录纪律」规则**（2026-07-30 用户要求）：要求每次增删改查操作必须在 CHANGELOG.md 中记录原因和解决的问题；涉及已有记录的文件时必须溯源历史条目、评估回归风险，可能导致已解决的问题回归时必须提醒用户。原因：确保变更可追溯、防止后续改动意外破坏已有修复。
- **equity 必须从账户 API 取真实总资产，禁用占位/累加值（2026-07-31 用户立）**：原 `preflight.py`/`resume.py` 读 `signals/equity-log.csv`（旧信号模式手动累加值）+ `config.initial_equity=100000 HKD`（开发期占位）作 equity，显示 equity-log 累加值（错值）；真实长桥模拟账户 `net_assets=374,590.95 HKD`（差近 4 倍）。改动：① `references/risk-management.md`「权益更新」段重写——默认账户（美股长桥 `account_balance().net_assets` / 港股老虎 `get_assets().net_liquidation`）取真实总资产、用户指定账户从指定账户取、config 占位与 equity-log 累加值均不得当真值；② `scripts/preflight.py`、`scripts/resume.py` 改为优先从长桥账户 API 取 equity（失败才 fallback equity-log 并标记非真实）。原因：自动交易模式下 B / max_loss 标尺错会直接导致仓位失控——用户原话「下次这个总资产不能取错了，我没说用什么账户就是默认账户里面取，我有指定账户就是指定账户里面取」。
- **修复长桥 SDK 连接超时 + TradeContext.close() bug（2026-07-31）**：盯盘启动查账户余额时发现长桥 Python SDK 连接超时。根因有二：① SDK 默认连国际区 `openapi.longportapp.com`（国内被墙、直连 port 443 超时），且 SDK Rust 内核不读系统 `HTTP_PROXY`/`ALL_PROXY`（xray 代理形同虚设）；CLI 按 `~/.longbridge/openapi/region-cache=cn` 连中国区 `openapi.longportapp.cn`（直连可达）故能连。修复：`scripts/trade_utils.py` 的 `load_env_file()` 自动读 region-cache 设 `LONGPORT_REGION=cn`，所有交易脚本经 `load_config()` 自动连中国区（实测不设 7.7s 超时、设后 0.8s 连通）。② `TradeContext` 无 `close()` 方法（与 `QuoteContext` 不对称），原 `trade_utils.py` 8 处 `finally: tc.close()` 会抛 AttributeError 覆盖函数返回值、致所有下单/查询函数失败；改为 `finally: pass`（SDK 自动清理）。两个坑记录在 `references/account-tools.md`「长桥模拟盘交易 API」段。
- **修复长桥止损单 trigger_direction bug + 限价 tick + SDK 升级回滚（2026-07-31）**：做空 MU 时发现 `submit_stop_order` 用 SDK `MO+trigger_price` 被长桥当 MIT 触价单（默认 down 方向=价格跌触发），做空止损 Buy 在当前价<触发价时瞬间误触发、被动平仓亏 $119。根因：长桥 SDK `submit_order` 不支持 `trigger_direction`（签名确认），MIT 默认顺方向不能做止损。修复：`trade_utils.submit_stop_order` 改用 REST API（`POST /v1/trade/order`）提交 MIT + `trigger_direction=up`（做空止损）/`down`（做多止损），签名算法取自长桥 Rust SDK 源（HMAC-SHA256+SHA1）；`SKILL.md` 硬护栏第2条同步更新（MIT+trigger_direction，禁限价止损）。连带修复：`smart_order` 限价取整到美股 tick 0.01（原裸 ask 825.975 报 602035 Wrong bid size）；SDK 一度升级到无 `Config.from_env` 的版本破坏 `load_config`，已回滚 3.0.23。两个坑记录在 `references/account-tools.md`。当天 MU 两笔：bug 被动平仓 -$119、破位失败主动锁亏 -$227.52。
- **开仓改为附加止损单（主单+附加）+ smart_order 修 bug + f_max 提到 10%（2026-07-31）**：① 用户新规——开仓订单附加止损市价单（REST `attached_params`：`attached_order_type=STOP_LOSS`+`stop_loss_price`+`activate_order_type=MIT`），主单成交才激活附加止损（取代原「先挂止损单再开仓」+单独 submit_stop_order 的 trigger_direction REST 封装）；优点：开仓失败止损不残留、STOP_LOSS 语义自动定向无需 trigger_direction。`SKILL.md` 硬护栏第2条同步改。② 修 `smart_order` 两个 bug：振幅判断 high/low 缺失时原 fallback last→amplitude 0→误用市价（第2笔做空 Sell MO 被拒）；市价单原不查成交、拿 last 冒充 fill_price——现 high/low 缺失强制限价、市价单查 today_orders 确认 Filled。③ `config.json` f_max 0.025→0.10（用户立）。④ 事故复盘：做空 MU 时 smart_order 市价 Sell 被拒但误报成交→close_position 反向买入开多 158 股 @ 827.82（误持仓、收盘 823 浮亏约 -$757），下次开盘处置。`open_position.py` 的附加订单重写随后完成（详见 2026-08-01 条目）。

- **纠正「反向止损单」错误概念 + 厘清主订单 / 附加订单（2026-08-01 用户纠正）**：原 SKILL.md 硬护栏第 2 条用了「反向止损单 / 不反向的止损单 / 独立反向单」等说法——这些都不是有效概念：止损单默认就和主订单反向（券商设定），根本不存在「反向止损单」这个东西。用户原意「禁止反向订单止损」= 禁止用一笔反向的**主订单**去给另一笔已成交的主订单做止损（主订单是开仓单、会开新仓，不是了结仓位；止损只能用止损单 / 附加止损单）。改动：① `SKILL.md` 规则 2 重写——用「主订单 vs 附加订单」两类区分 + 附加订单状态流转（未提交 → 监控 → 触发）+ 正确表述「禁止反向主订单止损」，删除全部错误措辞；② `references/account-tools.md`「附加订单三类」段升级为「主订单 vs 附加订单 + 附加订单三类」——补全两类订单定义、附加订单状态流转、老虎「止损单」等同附加订单的说明、禁止反向主订单止损的正确含义。原因：用户指出「反向止损单」概念根本不存在，止损单本就和主单反向，必须用主订单 / 附加订单的正确框架描述，否则后续理解下单机制会持续跑偏。

- **三个动作订单类型定型 + 附加订单开仓接线 + 止损方向纠正 + todos/ 规矩（2026-08-01）**：
  - **三个动作各自用什么订单（用户新规）**：① 开仓 = 主订单（LO）+ 附加止损单（STOP_LOSS MIT），REST 一次提交、主单成交才激活附加止损；② 移动止损 = 止损市价单（MIT），且改为「撤旧止损单、新增新止损单」（保留单个活动止损，防多单累积触发后反向开仓）；③ 平仓 = 一键平仓——长桥**无专门平仓接口**（已核实 SDK + REST 文档，App「平仓」按钮 = 读持仓 + 反向 submit_order），脚本读持仓自动算方向 + 量、反向下单，成功后撤所有止损单。`SKILL.md`、`references/account-tools.md`、`references/monitoring.md`、`references/risk-management.md` 同步更新三个动作的订单类型描述。
  - **`open_position.py` 接线附加订单**：从「先挂止损、再开仓」两步改为 `submit_order_with_stop`（主单 LO + attached_params STOP_LOSS 一次 REST 提交）+ 新增 `check_order_filled` 成交回查（禁止用 last 冒充成交价，MU 事故教训）；主单未成交则撤单（附加随之撤、不残留）。`close_position.py` 改为一键平仓（读持仓自动算方向 + 量）。`move_stop.py` 改为撤旧止损再新增。
  - **止损方向纠正（用户纠正 + 实测验证）**：止损单（MIT）默认就和持仓反向——券商服务端按 side + 触发价相对现价的位置自动定向，**不需要 trigger_direction**。实测（paper 盘后）：MIT Sell trigger=700、MIT Buy trigger=1000（现价约 823），均不带 trigger_direction，提交后 `trigger_status=Active` 监控等待、方向正确未误触发。`trade_utils.submit_stop_order` 据此从「REST MIT + trigger_direction」简化为 SDK `submit_order(order_type=MIT, side, trigger_price)`。**MU 事故真因更正**：原归因「MIT 默认方向错、需 trigger_direction」是误判——事故提交用的是 `order_type=MO`（市价单）+ trigger_price，MO 提交即按市价成交、trigger_price 被忽略致做空止损 Buy 瞬间平仓；正确做法是用 MIT 而非 MO。`references/account-tools.md` 止损段重写、`SKILL.md` 规则 2 去掉「MIT 默认方向不能做止损」的错误说法。
  - **新增 `trade_utils` 函数**：`check_order_filled`（订单成交状态轮询回查）、`get_open_position`（读当前持仓，供一键平仓自动取方向 + 量）。
  - **待办统一存 `todos/` 规矩（用户立）**：全局 `~/.claude/CLAUDE.md` 新增「待办统一存项目根 todos/ 目录」段——待办只进 `todos/`、禁止写 SKILL.md / references / CHANGELOG / 代码注释里的 `# TODO`；做完改动须查 `todos/` 删待办 + 记 CHANGELOG 闭环。同步进权威源 CapabilityManagerAgent `.claude/CLAUDE.md`（两文件逐字节一致）。本项目新建 `todos/TODO.md` 收拢待办（MU 误开仓处置、港股 lot_size 从 snapshot 取、`load_equity` 改账户 API、`submit_order_with_stop` 实测）；移除散落在 `SKILL.md` 规则 2 与 `open_position.py` 代码注释里的待办内容。
- **厘清「止损单」两种挂法 + 修正误导措辞（2026-08-01 用户追问）**：用户指出长桥 App 能给已有持仓新增止损单（设触发价 + 选市价单），质疑接口是否缺这功能。核实结论：接口**完全支持**——给已有持仓加止损 = 提交独立 MIT 止损单（`order_type=MIT` + `side` + `trigger_price`），正是 `move_stop.submit_stop_order` 用的、也正是 App「新增止损单」走的能力。先前措辞误导（`move_stop.py` docstring「长桥不能给已有持仓挂附加订单」、`account-tools.md`「止损单属于附加订单范畴」）——前者指 attached_params 那种紧绑新主单的机制、后者把两种挂法混为一谈，已修正：明确区分两种止损单机制——① 开仓附加止损单（attached_params STOP_LOSS，主单成交才激活）；② 已有持仓独立 MIT 止损单（提交即监控，App 新增止损单走这个）。`SKILL.md` 规则 2 同步。原因：止损单有两种挂法是关键机制区分，搅混会让 move_stop 看起来「缺能力」（其实不缺）。


- **更正「累积止损单反向开仓」错误假设 + move_stop 回退到「不撤旧」（2026-08-01 用户质疑）**：用户指出长桥 App 上多个止损单、首个触发平仓后其余再触发应「止损失败」而非「反向开仓」。核实长桥官方规则（[美股卖空常见问题](https://longbridge.com/sg/zh-CN/support/topics/usmarket/shortselling)）：**卖出数量超过持仓 → 下单失败**（不自动融券反向开空），故多个止损单累积安全——首个触发平仓后，其余再触发时持仓已 0、下单失败，不会反向开仓（模拟盘默认不支持融券，尤其如此）。此前「独立 MIT 累积会反向开仓、move_stop 须撤旧」是未经证实的错误假设，已弃用。据此：① `move_stop.py` 回退到用户既定设计「不撤旧、累积兜底」（恢复 `references/trading-strategy.md`「移动止损不撤旧单」规矩，去掉 `cancel_all_stop_orders` 调用）；② `close_position.py` 与 `trade_utils.cancel_all_stop_orders` 的撤单理由从「防反向开仓」更正为「清理冗余」（撤单行为保留——仓位已平时残留止损单触发会失败、不造成损失，但留着冗余，撤掉保持账户干净）；③ `SKILL.md` 移损 bullet 改回「不撤旧」、`references/trading-strategy.md` 补「累积安全」佐证。原因：基于未经验证的假设改动用户既定设计是流程缺陷（违反 verify-facts-before-stating），核实长桥规则后须纠正。

- **实测确认 MIT 止损单不占用可用持仓 + 累积触发行为（2026-08-01 用户要求实测）**：用户质疑「MIT 触发必执行应开仓，除非标记了平仓」，要求实测验证。两轮 paper 实测：① 挂一笔 MIT Sell 158 后 `available_quantity` 不变（158→158）、且能再挂第二笔 MIT——证实 **MIT 止损单不占用可用持仓、可挂多个**（不受持仓量限制）；② 超持仓卖单（Sell 208 > 持仓 158）被 Reject（`msg: Insufficient holdings`）——证实 **MIT 触发转市价单执行时检查持仓，超持仓失败、不反向开仓**。完整机制：MIT 提交时不占不查可用持仓（区别于普通 LO 卖单提交即占用即查），触发转市价单时才查。据此确证 move_stop「不撤旧、累积兜底」设计可行（能挂多个 + 累积安全），与用户 App 经验一致；附带发现 MIT 不占可用 → 平仓不被已挂止损单挡道。`references/trading-strategy.md`「移动止损不撤旧单」段补三重实测依据。原因：此前「累积会反向开仓」是未经验证的错误假设，用户两次质疑推动实测，现以直接证据取代推断。

- **实测空仓时 MIT 止损单能挂且 Active 监控（2026-08-01 用户要求实测）**：在账户无 AAPL 持仓的空仓状态下挂 MIT Sell AAPL qty=1 trigger=100，提交被接受、状态 `TriggerStatus.Active`（监控中）、未被系统自动撤。结合前两轮实测，完整机制图：挂单阶段不查持仓不占可用（空仓也能挂、能挂多个）→ 监控阶段不因空仓自动撤 → 触发阶段转市价单查持仓、空仓/超持仓 Reject（不反向开仓）。进一步确证 move_stop「不撤旧、累积兜底」安全：平仓后旧止损单仍挂着、触发时 Reject 无害。`references/trading-strategy.md`「移动止损不撤旧单」段补第④点实测依据。

- **再次更正：完全空仓时 MIT 触发会反向开仓 + move_stop 改回「撤旧再新增」（2026-08-01 用户第三次质疑后实测）**：用户指出「完全空仓时卖」与「有持仓超卖」是两种不同情况，前者可能是开仓而非失败。实测确证：① 有持仓 158 时卖 208 → Reject「Insufficient holdings」；② **完全空仓时卖（AAPL price=100/300）→ 被接受（NotReported）、成交即反向开仓**（price=99999 被 Reject 只是「金额超购买力」、无关持仓）。故「多个止损单累积、首个触发平仓后空仓、其余 MIT 再触发 → 反向开仓」成立（不是止损失败）。据此：① `move_stop.py` 改回「撤旧再新增」（确保任意时刻只有一个活动止损，防空仓反向开仓）；② `close_position.py` 与 `trade_utils.cancel_all_stop_orders` 撤单理由改回「防反向开仓」（平仓后空仓、残留 MIT 触发会反向开仓，非「清理冗余」）；③ `SKILL.md` 移损 bullet 改回撤旧；④ `references/trading-strategy.md`「移动止损」段重写为「撤旧再新增」、原用户立的「不撤旧」规矩因实测证伪其安全前提而作废。原因：此前从「有持仓超卖 Reject」外推到「空仓也 Reject」是未经验证的跳跃（违反 verify-facts-before-stating），用户精准指出「完全空仓」是未被覆盖的场景，实测证伪。

- **三动作订单类型定型：平仓改用 MO + move_stop 加持仓量校验（2026-08-01 用户最终规范）**：用户给出三动作的最终订单规范——① **开仓**：主单 LO（控价、只往更优方向偏）+ 附加 STOP_LOSS MIT（随主单一起提交、止损不延迟）；② **移动止损**：与持仓反向的 MIT，数量严格等于持仓量（超持仓会被券商判失效），撤旧只留一个最新；③ **平仓**：MO 市价单（方向不看好时立即成交、允许不利方向偏移）+ 平仓后撤未触发 MIT 止损单（防反向开仓、属平仓动作一部分）。据此：① `close_position.py` 从 `smart_order`（限价优先→改 MO）改为**纯 MO**（`submit_market_order` + `check_order_filled` 回查成交价）——平仓要立即成交的确定性，不该挂限价等；② `move_stop.py` 加持仓量校验（止损量 > 持仓量直接拒、< 持仓量警告，落实「严格相等」）；③ 清 `submit_order_with_stop` docstring 过时的「MIT 默认 down 方向误触发」说法（已证伪）；④ `SKILL.md` 平仓 bullet / close_position desc、`references/monitoring.md` 平仓描述同步为 MO。开仓（LO + 附加 MIT）、移损（反向 MIT + 撤旧留新）当前实现已符合规范，未改。

- **港股长桥备选账户 + 独立代码模块 + 美股 symbol bug 修复 + move_stop 顺序调整（2026-08-01）**：
  - **港股添加长桥模拟账户为备选**：港股交易默认老虎，长桥为备选、**仅用户盯盘时特别说明才用**。复用美股同一长桥账户（HKD 计价、实测有港股权限）。
  - **港股代码与美股解耦（分而治之）**：新建 `scripts/trade_utils_hk.py`（港股自包含封装）+ `open_position_hk.py` / `close_position_hk.py` / `move_stop_hk.py`，不 import 美股模块、不影响美股代码。封装港股专属规则：symbol 富途↔长桥转换（`HK.02800`↔`2800.HK`）、lot_size 从 `static_info` 取（不硬编码 100）、港股价位 tick（2025-08-04 调整版价位表）、HKD equity 直取 `net_assets`。盘后实测：港股行情/lot_size/tick/equity/持仓过滤全部 OK；港股脚本只处理 `HK.xxx`、不误碰美股持仓。
  - **修美股 symbol bug**：实测发现长桥 API 只认原生格式（`MU.US`/`2800.HK`），不认富途格式（`US.MU`/`HK.02800`）——美股 `trade_utils` 之前传 `US.MU` 给长桥导致 `get_quote` 返回 None、下单失败（之前未暴露因 open_position 没成功跑过）。给美股 `trade_utils` 加 `to_lb_symbol`/`to_futu_symbol` + get_quote/submit/get_open_position/cancel_all_stop_orders 全链路转换修复。**今晚平 MU 依赖此修复**（否则 close_position get_quote(US.MU)=None 退出）。
  - **move_stop 顺序：先新增后撤旧**（用户立）：`move_stop.py` / `move_stop_hk.py` 从「先撤旧再新增」改为「先下新止损单、再撤旧止损单」，仓位始终有止损保护、无裸奔空窗（若先撤旧、新还没下则瞬间无保护）；`cancel_all_stop_orders` / `cancel_all_stop_orders_hk` 加 `exclude_order_id` 参数，撤旧时跳过刚下的新止损。

- **长桥凭证可切换：支持扩展到实盘账户（2026-08-01 用户要求）**：美股 `trade_utils` 与港股 `trade_utils_hk` 的凭证加载统一走 `_env_file(env_file)`——优先级 `env_file 参数 > 环境变量 LONGBRIDGE_ENV_FILE > 默认 env-paper（模拟盘）`。`load_config(env_file=None)`、REST `submit_order_with_stop(_hk)` 的凭证路径都改成走 `_env_file`（之前写死 env-paper，4 处）。后续接长桥实盘账户只需设环境变量 `LONGBRIDGE_ENV_FILE=实盘凭证路径`（或调用方传 env_file），无需改代码。港股长桥与美股长桥各自独立一份 `_env_file`（解耦）。实测：默认仍 env-paper 正常（美股 get_quote / 港股 get_quote_hk 都 OK）；设环境变量 / 传 env_file 能切换凭证路径；**env-sg 实盘凭证实测验证**——设 `LONGBRIDGE_ENV_FILE=env-sg` 后 API 可连、SGD 账户无资金（net_assets=0）、无持仓、港美行情只读全 OK，凭证切换机制在真实实盘凭证下工作正常（env-sg 无资金，安全）。**另测实盘下单风控**（env-sg 无资金）：美股 MU 买单 → 异步 Rejected「exceeds maximum buying power」（购买力风控）；港股 2800.HK 买单 1 股 → 同步拒 602001「quantity does not comply with lot size」（lot_size=500，1 股非倍数）——验证实盘下单链路通 + 风控生效，且**港股 lot_size 是提交阶段同步校验**（传错数量根本提交不上，确认港股代码 lot_size 处理必要）。

- **「锁大利润」止损用法明确 2R 基准（2026-08-01 用户立）**：止损单两种用法里的「锁大利润」（原称「锁利」）明确触发基准 = **浮盈 ≥ 2 倍赔率（2R）**——2R 才算大利润、才考虑该用法（大概率锁利落袋、小概率让利润继续奔跑）；**浮盈 < 2R 不用此用法**，用「放趋势反转位」。改动：`references/trading-strategy.md`「止损单的两种用法」用法 A/B 标题与适用条件明确 2R 分界、用法 A 详解的旧基准「浮盈 ≥2/股」（绝对值）改为「≥ 2R」（赔率口径，统一可比）；`SKILL.md` 规则 7 同步。原因：原「浮盈大时 / 还不够时」缺明确基准，旧「2/股」是绝对值（不同股价含义不同），用 2R 赔率倍数统一大利润判据。

### 移除

- `.claude/memory/` 目录（随 AutoMemory 退役）。
- `settings.json` 中过时的 hooks 配置条目。
- **README 去除写死的 LLM 型号说明**（2026-07-31 用户要求）：删除 `README.md`/`README_cn.md` 的「Built with Claude Code」徽章与「Under the hood / 底层模型」（GLM-5.2）说明段；同步删除项目 `.claude/CLAUDE.md` 的 `agent-llm` 缓存标记。原因：Agent 使用的 LLM 会按情况切换，在 README 写死具体型号会随切换过时、且无谓绑定厂商。为避免下次 `/commit` 把 GLM-5.2 自动写回 README，已一并拆除全局 commit skill 的 `agent-llm` 检测（第 9f 步 + 徽章组合 + 缓存标记 + 汇报模板）与全局 `~/.claude/CLAUDE.md` 的「不强调 Agent 的大脑 LLM」规则段——该全局改动属 CapabilityManagerAgent 底座管辖，需同步回该项目 `.claude/`。
