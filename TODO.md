# DayTradingAgent 待办清单（活跃）

> 本文件**只放未完成**的待办（`[ ]` 开头）——「还有哪些没做」一眼全览（项目根文件制，2026-08-05 撤销 todo-skill 定案；方法论见全局 CLAUDE.md「待办（TODO）管理」节，本文件与 `TODO-archive.md` 同处项目根）。
> 已完成 / 已更新的条目移入 `TODO-archive.md` 归档保留（勿删）。
> 每条待办带记录时间戳（精确到分钟）；前后矛盾的待办以最新时间戳为准。

## 交易规则 / 策略

- [ ] **三阶段赔率样本积累 ≥30 笔后重新标定「预估赔率打折系数」**（记录：2026-08-11 19:25）：2026-08-05 用 12 笔三阶段样本粗标定 ×0.5 保守折扣系数（落地/初始 比值中位数 −0.22、均值 −0.08、75% 落地 ≤ 0，详见 `trading-strategy.md`「预估赔率系统性打折」节）——样本仍小、×0.5 是保守起点而非精确最优；积累到 ≥30 笔三阶段完整记录后按「落地/初始」分布重新标定精确系数，并更新 `trading-strategy.md` 对应节（含 SKILL.md 引用的门槛口径）。**现状（2026-08-11 核对）**：样本已 12 → **20 笔**（8-06~8-10 新增 8 笔：01888、00100、MINIMAX、胜宏、AAOI、药明、LITE×2，均初始 ≥ 1.2），**仍 < 30、系数维持 ×0.5 不调**——新增 8 笔落地/初始 中位数 +0.12、均值 +0.35、6/8 落地 > 0（基线 3/12），兑现度明显高于基线；但 20 笔仍小样本、新增表现好可能是 ×0.5 门槛（8-06 起执行）拦掉边缘单的结果，调松会放回边缘单，故等 ≥30 笔按合并分布重标定（观察数据已写入 `trading-strategy.md`「样本积累观察」段）。

## 代码 / 机制

- [ ] **开仓前按单标的保证金率算可买上限 + 主动降档（2026-08-06 00100 被拒根因闭环）**（记录：2026-08-12 10:14）：2026-08-06 00100 MINIMAX-W 计划仓位 58,400 股被拒的真实根因（8-12 实测闭环，推翻 8-11 CHANGELOG 里「购买力边界在 1111~1717 万 HKD、最可能单笔名义/购买力限制」那条**口径不准**的推断）= **单标的保证金率约束导致的购买力不足**，与加仓无关（加仓误记 8-11 已纠正）。**实测链条**：① 账户 `buying_power = 4 × cash_available_for_trade`（账户级总上限，8-11 实测 4 × 434.5 万 = 1737.8 万，分毫不差）；② **单标的实际可买名义 = 可用资金 ÷ 个股做多保证金率**，每只标的不一样（官方：高波动标的风控收紧，做多保证金率 25%–100%，并非每只都能 4 倍杠杆）；③ 8-12 实测 `get_contract.long_initial_margin`：00100 = **0.75**、01888 = 0.50、00700/02800 = 0.30。**8-06 闭环算式**：13:12 开 00100 前账户空仓（01888 已于 10:50 平）、可用资金 ≈ 净值 817.5 万 HKD → 00100 可买上限 = 817.5 万 ÷ 0.75 = **1090 万 HKD = 37,484 股**；计划 58,400 股需名义 1698 万 > 上限 1090 万 → 超购买力被拒；实际成交 10,000 股（名义 294 万 < 上限）成功。同日 01888（保证金率 50%）名义 1111 万 < 817 万 ÷ 50% = 1635 万上限 → 能下。两笔被「单标的保证金率」这条线干净切开。**现状缺口**：[`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) 的 `calc_position_size` 用「equity × 2% ÷ 止损距」算股数，全程不看个股保证金率、不看可用资金，算出的股数可能直接超购买力被拒（8-06 即如此——被拒后才靠脚本「失败自动降档」被动摸到可下量）。**落地三件事**：
  1. **加购买力查询函数**：[`trade_utils_tiger.py`](.claude/skills/trade/scripts/trade_utils_tiger.py) 加 `get_buying_power_tiger(config, symbol, ref_price)`——读 `get_prime_assets(base_currency)` 的 `buying_power` + `cash_available_for_trade` + `get_contract` 的 `long_initial_margin`，返回「可用资金、账户购买力上限、本标的做多保证金率、本标的可买股数上限（= 可用资金 ÷ 保证金率 ÷ 参考价，按 lot 向下取整）」。⚠️ 美股版先验证 `get_contract` 是否返回 `long_initial_margin`（港股 8-12 实测返回，美股未测——不验证不能假设美股同样有值）。
  2. **开仓前主动购买力校验**：[`open_position_tiger.py`](.claude/skills/trade/scripts/open_position_tiger.py) / [`open_position_tiger_us.py`](.claude/skills/trade/scripts/open_position_tiger_us.py) 算出目标股数后，先和「本标的可买股数上限」取 min——超了就在**下单前**主动降到上限档（输出 `capped_by_buying_power: true` + 上限股数 + 保证金率），而不是被券商拒了再降档；现有「失败自动降档」保留作兜底（防实测保证金率与券商口径有偏差）。把降档从被动改主动、避免无效的 Invalid 往返。
  3. **回写文档**：[`references/auto-mode.md`](.claude/skills/trade/references/auto-mode.md)「下单失败处置」节补「购买力不足 = 单标的保证金率约束」一条（与 cross-trading 并列的另一类被拒原因，reason 区分：购买力不足不降档、直接按上限档下）；[`CHANGELOG.md`](CHANGELOG.md) 8-06「00100 计划仓位下达失败排查闭环」条目把 8-11「购买力边界在 1111~1717 万 HKD、最可能单笔名义/购买力限制」推断**修正**为 8-12 实测结论「00100 做多保证金率 75%、可买上限 817.5 万 ÷ 0.75 = 1090 万 = 37,484 股，58,400 股需名义 1698 万超上限被拒」。

