# DayTradingAgent 待办清单（活跃）

> 本文件**只放未完成、要尽快处理**的待办（`[ ]` 开头）——「还有哪些没做」一眼全览、**及时清空**（2026-08-05 撤销 todo-skill 定案；2026-08-19 用户立功能定位：待办都是要尽可能尽快处理的内容，**不设「长期备忘」分类**——超低频、无排期压力的备忘事项一律放 `MEMO.md` 备忘录，不进本文件）。
> 已完成 / 已更新的条目移入 `TODO-archive.md` 归档保留（勿删）。
> **不留处理备注（2026-09-18 立）**：批量处理 / 归档 / 升降级的过程记录（「（日期 批量处理：…）」式摘要）一律写 `TODO-archive.md` 对应归档节，不写在 TODO.md 任何位置（曾积累 7 处此类备注污染文件结构，由 `changelog_guard.py` 的 TODO 卫生检测在场提醒强制）。
> **优先级分类（2026-08-16 用户立；2026-08-17 改四级紧急度；2026-08-17 命名弃「灯」用「色」）**：条目按 🔴 红色紧急度 / 🟠 橙色紧急度 / 🟡 黄色紧急度 / 🟢 绿色紧急度四级分节排列（红色紧急度最紧急在前、绿色紧急度最不紧急在后），每条待办写入时必须判断所属级别并放进对应分节（**颜色只标在分节标题、条目本身不标颜色 emoji**，2026-08-17 用户立）；判断拿不准往高靠。分级标准：🔴 红色紧急度 = 不修会亏钱 / 下错单 / 安全事故 / 数据统计结论错误 / 核心功能不可用；🟠 橙色紧急度 = 边界情况出错 / 防护缺口 / 口径不一致可能演化为实际损失；🟡 黄色紧急度 = 文档措辞 / 格式漂移 / 卫生问题 / 不影响正确性的优化；🟢 绿色紧急度 = 计划类新功能实现（新能力立项、改造方案落地，改期无实际损失）。
> 每条待办带记录时间戳（精确到分钟）；前后矛盾的待办以最新时间戳为准。

## 🔴 红色紧急度（不修会亏钱 / 下错单 / 统计结论错 / 核心功能不可用，须优先处理）

- [ ] **T157** **盯盘会话死亡 + 无外部叫醒 = 持仓止损被动成交无人感知（2026-09-16 夜盘实录，直接亏钱面）**。完整故障链：22:19 开仓 SPCX → 22:31 持仓会话（sid 997f8f73）监控中断（死因无日志可证；他会话 23:02 断层恢复自述「空转 hook 拦截后未被唤醒」，疑同因）→ 22:52 止损单券商侧自动成交 −0.99R，无人监控、无人写平仓记录、连败计数漏记（T151 重演）→ 01:05 后全部心跳停止 → 直至次日 11:45 排查才发现。**三层防线为何全缺位**：① 会话内 monitor_guard 空转硬门只能「拦下一条命令 + stderr 指引」，会话本身死亡（拦截后 AI 未响应指引 / 会话被外部终止）时无人叫醒；② watcher 空转警报 2026-08-21 用户裁定删除（CHANGELOG 同日条回归风险提醒已预告本场景）；③ 「停盯总结只总结本会话自己」规矩下，断层恢复的他会话只查自己持仓、无全局视角。**修复方向**（须过统计 / 取舍评估，非盘中决策）：① 恢复某种低频外部叫醒（如持仓存在时 watcher 级持仓归零事件检测）；② 断层恢复流程加一步「查当日全部 ET 会话的未平仓记录 + 账户实况」；③ 美股停盯边界（16:00 ET）加硬性收尾检查（有持仓未平 / 有开仓无平仓 → 强制弹窗）。（记录：2026-09-17 11:47）

## 🟠 橙色紧急度（边界情况出错 / 防护缺口 / 口径不一致，排在红色紧急度之后计划处理）

- [ ] **T140** **ZCode 宿主 hooks：C 方案兜底已恢复并实证生效（2026-09-04）+ 项目级死配置路径已修 + zcode-cli 侧开发已登记 T1——剩「等 zcode-cli 升级 → 验证 → 撤回用户级上移切项目级单源」**。**根因（2026-09-03 18:58 T137 验证发现 + 21:00 深化定位）**：项目级 hooks 不是官方没做，而是 runtime 有**完整的 workspace hooks 信任体系**（状态机 pending_trust→trusted_persistent、声明摘要绑定、持久 trust store `workspace-hook-trust-v1.json`、`zcode hooks trust status/grant/revoke` 命令族），zcode-cli 会话被挡在**宿主能力开关**——runtime 会话构造参数 **`workspaceHookTrustEnabled`**（`n=e.workspaceHookTrustEnabled===!0`，zcode-cli 从未传 → false → `feature_disabled`）；官方文档 "not executed in the current version" 是未授信/未启用宿主的用户侧口径。**已做（2026-09-03 20:35~21:00，用户授权）**：① `zcode hooks trust grant --all-current` 授信六条挂载，status 确认 `trusted_persistent`（trust store 已持久化）；② grant 后 headless 新会话双探针实测——cat 假凭证路径探针被拦（deny 文案逐字来自 secret_guard，**但拦的是用户级 C 方案那份**）、echo 拼接探针放行（属脚本设计内行为：secret_guard 按「段首动词为读取类」分段判定，echo 形态本就放行——**T137 当晚用 echo 探针判「项目级 PreToolUse 未运行」的证据链有此瑕疵，结论靠 changelog_guard 无注入 + 日志 warning 仍成立**）；③ 日志铁证：grant 后三个新会话（20:40/20:41/20:49）**仍全部打 `feature_disabled`**——授信只开了信任层、宿主能力层仍关。**🔴 回归（2026-09-04 11:51 登记发现）**：用户级 `~/.zcode/cli/config.json` 当日 **11:41:15 被整体重写**（mtime 实测），C 方案上移的 secret_guard / changelog_guard 两条挂载**丢失**、只剩 guard.sh 一条；重写后内容 = 09-03 落盘前的备份（`tmp/zcode-user-config-backup-20260903.json`）+ 新增 `glm-5.3-flash` 模型条目——高度疑似 ZCode 客户端某次设置保存用**旧配置快照整体重写** config.json。11:41:16 起的新 ZCode 会话内三探针实测：① cat 假凭证路径 `/tmp/nonexistent-xyz-probe/accounts.json` **放行**（secret_guard 缺席——ZCode 会话凭证防线再次洞开）；② `echo "probe: pkill -f vscode"` **被拦**（guard.sh 在场、ZCode deny 链正常）；③ Write 项目内 tmp 探针文件**无「📌 CHANGELOG 记录提醒」注入**（changelog_guard 缺席，探针文件即建即删、净变更为零）。**✅ 办结当日回归（2026-09-04 12:07，用户指令「把 T140 待办直接办了」）**：**⓪a 完成**——改前再备份 `tmp/zcode-user-config-backup-20260904-pre-restore.json`，恢复两条挂载，回读验证（PreToolUse(Bash) = guard.sh + secret_guard 两条、PostToolUse(Edit|Write) = changelog_guard 一条；provider / model / permission / usage 段完整保留、含当日新增 glm-5.3-flash 模型）；**⓪b 完成（结论）**——写入者未留日志直接证据，但进程清单实测：**7 个 09-02 19:12~19:15（hooks 迁移当晚）启动的后台 zcode-cli 进程至今存活**，其启动早于 09-03 19:15 C 方案落盘、内存配置快照 = guard.sh 单挂，任一进程做设置保存即会用旧快照整体重写 config.json——与实测「重写后 = 单挂 + glm-5.3-flash」完全吻合（**处置建议已报用户：关闭这批残留进程**；防再冲口径 = 后续改 ZCode 配置一律文本编辑 + 改后回读校验挂载数，不从客户端设置入口保存）；**headless 新会话双探针实证生效**——cat 假凭证路径被拦（secret_guard、deny 文案逐字来自脚本）、Write 项目根探针文件收到「📌 CHANGELOG 记录提醒」完整注入（changelog_guard；首次探针用 tmp/ 路径静默属设计内——tmp/ 在 `_EXCLUDE_DIRS`，换项目根路径后实证），探针文件即建即删、净变更为零；**副发现完成**——`.zcode/config.json` 7 处旧路径（6 条挂载命令 + 1 条 disallowedTools）全部替换为现址 `/Users/xhq/Developer/DayTradingAgent`、JSON 校验合法；**① 完成（登记动作）**——zcode-cli 项目新建 `TODO.md`、登记 **T1**（bootstrap 传 `workspaceHookTrustEnabled: true`，含三处 offset 定位材料与验证口径，其 CHANGELOG 已记条）。**剩余动作（⓪a / ⓪b / 副发现 / ① 均已办结，剩②③等外部依赖）**：② zcode-cli 升级发布后：新会话重跑 cat 探针（应仍被拦）+ 查日志无 `feature_disabled` → 项目级生效确认；③ 确认后**撤回用户级两条上移**（恢复项目级单源，避免 secret_guard 双挂重复 deny、changelog_guard 提醒注入两遍），`.zcode/config.json` 六条即原生生效（含 monitor_guard / live_auth_witness，覆盖比 C 方案全；路径已修）。（记录：2026-09-03 19:00；更新：2026-09-03 19:15 C 方案落地；更新：2026-09-03 21:04 grant 已授信、根因深化为 zcode-cli 未传 workspaceHookTrustEnabled；更新：2026-09-04 11:51 C 方案兜底被 11:41 用户级配置重写冲掉、升回红色；更新：2026-09-04 12:07 办结当日回归——恢复两条挂载 + headless 双探针实证生效 + 修 7 处旧路径 + zcode-cli 登记 T1，剩②③等升级，降回橙色）

- [ ] **T136** **account_status.py 持仓状态行对实盘账户误报「账户已无持仓」（2026-09-02 实盘首单暴露；修复主体已完成，剩实盘路径实测一项，模拟路径 09-03 已验证通过）**。2026-09-02 早盘实盘账户 67\*\*\*\*91 开仓 00100 后，采样段持仓状态行连续多段报 `账户实持 0股（默认（模拟））| 活动止损单触发价 无活动止损单 —— 账户已无持仓但 actions 无平仓记录`——**根因 = 查询方一律查默认（模拟）账户、而本会话用 `--account live` 实盘开仓，账户口径错配**。**修复主体（2026-09-02 12:58 完成）**：① 账户口径进 intent；② `account_status.py` 新增 `resolve_symbol_accounts` 统一解析、`position_status` 按标的分组查询；③ `monitor_segment.py` 止损单查询按账户分查、第四层逐账户判；④ 当日 actions 补记账户行。**已验证部分（2026-09-03 实测）**：09-03 为模拟账户会话（equity 774 万），持仓状态行正确输出 `📌 持仓状态: HK.00100 16840股(做多) | 账户实持 16840股（默认（模拟）） | 活动止损单触发价 351.00 | 账户一致 ✅`——**账户标签、实持量、止损单触发价三项在模拟路径全部正确**，平仓后也正确切到 🚨「账户实持 0 股」告警（10:26 止损触发实录）。**剩余动作（下个实盘交易日开仓时顺带）**：① **实盘开仓后**观察持仓状态行显示「实盘」账户标签 + 实持量 + 止损单触发价（09-02 下午实盘 13:45 开仓后未跑到采样段持仓状态行，实盘口径仍无实测）；② 多会话并行（实盘 + 模拟各一会话）时两会话持仓状态行不串台、各查各的账户。（记录：2026-09-02 11:56；更新：2026-09-03 14:05，模拟路径已验证、收窄为实盘路径一项）

- [ ] **T123** **实盘做空拒单真因复核——「资金不足」归因被 buying_power 币种混淆污染（2026-08-25 12:35 排查定案；short 版购买力函数已补、剩实盘复核）**。当日 10:15-10:22 三次 07747 空单被拒，第一档报 `available funds or buying power are insufficient`，临时跑 `get_assets().summary.buying_power` 取到 56637.5 就断言「实盘真实购买力仅 5.66 万 HKD、仓位上限 1000 股」——**该字段实为 USD 计价**（铁证：同账户 `summary.currency='USD'`；`get_buying_power_tiger` docstring 明文；同刻 443843.84 ÷ 56637.5 = 7.8366 正是 USDHKD 汇率），正确购买力 ≈ 44.4 万 HKD。**污染后果**：按正确购买力 4100 股本应能过却仍报 insufficient——**做空拒单另有真因**（候选：做空保证金率高于多头的口径、融资标的 / 币种可用性限制、券商侧其它风控），未查清。**已完成部分（2026-08-30）**：`trade_utils_tiger.py` 已补 `get_short_buying_power_tiger`（short_initial_margin 口径）。**剩余动作（下次实盘解锁后）**：用 `get_contract` 的 **short_initial_margin** 复核 07747 口径下真实可空量，重写当日拒单归因；顺带实跑一次 `get_short_buying_power_tiger` 验证字段取值非空。**复核进展（2026-09-03 16:20 盘外完成，只读查询、未碰下单）**：① **`get_contract` 实查 07747 = CSOP Samsung Electronics Daily (2x) Leveraged Product，`short_initial_margin = 0.7`**（long 0.65、shortable=True、lot 100）——字段非空、`get_short_buying_power_tiger` 实跑返回 `(max_shares, bp, 0.7)` 三元组正常；② 富途分钟 K 实取 08-25 10:15 07747 实价 **69.10 HKD**；③ **实算**：正确购买力 443,843.84 HKD ÷ 0.7 ÷ 69.10 = **最大可空 ≈ 9,176 股**——4100 股名义市值 283,310 HKD、占用保证金 198,317 HKD，在「购买力」口径下**完全够** → **做空保证金率（0.7）解释不了拒单**，原候选一排除；④ **新归因（证据最强）**：4100 股所需保证金 198,317 HKD = 实盘 equity（≈106,358）的 **186.5%** → 拒单真因大概率是 **buying_power（含杠杆购买力 44.4 万）≠ available funds（现金可用）**——做空保证金从现金出、实盘账户现金远低于购买力；次要候选 = 借券可用性（`shortable_count = None`，老虎未给可借数量，ETF 借券可能需另行确认）。**剩余动作（下次实盘解锁后）**：实跑 `get_assets` 取**实盘 available funds 实测值**终验归因（若现金 < 19.8 万即坐实）；07747 若再做空前先查可借券数量。**⚠️ 执行限定（2026-09-04 13:08 实测）**：ZCode 会话做不了——无实盘解锁文件时 `apply_proxy` 跳过挂代理、实盘请求本地直连被老虎 **IP 白名单在连接层物理拒**（access forbidden: request ip not in whitelist），顺带验证「实盘默认物理不可达」在 ZCode 宿主同样生效；剩余动作限定在 **CC / CodeBuddy 实盘解锁会话**执行（查询脚本已备：`tmp/query_live_funds.py`，只读、账户号不打印）。（记录：2026-08-25 12:40；更新：2026-09-03 16:20，保证金率复核完成、真因收窄为 available funds 口径差；更新：2026-09-04 13:20，ZCode 会话实测被白名单物理拦、限定解锁会话执行）

- [ ] **T119** **开仓净赔率门槛 1.2 运行对账（首次对账已完成；② 门槛回撤预案经用户裁定 = 维持 1.2；只剩 ④ 继续攒样本复标）**。**首次对账实测（2026-09-03 完成，N=10 笔，区间 2026-08-28~09-03 港股 auto 实成交，报告 `reviews/2026-09-03-odds-reconciliation.md`）**：**EV = −0.529R、胜率 30.0%（3 胜 7 负）、累计 −5.288R、净盈亏 −66,734.91 HKD**；初始预期 R 均值 2.054 → 落地均值 −0.529、**兑现率 −25.7%**；分账户：**实盘 N=7 EV = −0.705R、模拟 N=3 EV = −0.117R**；对照 2.4 时代（08-06~24，N=16）EV = +0.23R（⚠️ 08-28 起分母改净 max_loss，两组**不可做精确差值比对、只能看方向**）。③ 结论已写入 `trading-strategy.md`「门槛沿革」**⓪-a** 段（含分账户数据与费用结构发现）。**② 裁定结论（2026-09-03 14:15 用户裁定）**：原预案「期间日均亏 ≥3R 即回撤到 1.5」按「期间总 R ÷ 交易日数」口径 = −1.058R/日判**未触发**，08-28 单日 −3.26R **不单独作为回撤依据**——样本仅 10 笔不足以支撑「改变执行面」的门槛调整（符合本项目「交易逻辑建议必须有数理统计基础」硬约束：单日 / 单笔不构成规则长期成立的依据）；`config.json risk.min_net_odds` **维持 1.2 不改**。**同源的 T139（实盘小账户按笔固定费摊薄）一并裁定 = 维持现状继续攒样本**：固定费摊薄是**已知结构性拖累**，不因此改赔率门槛（治不了费用）、也不改 `risk_fraction`（放大 B 会同步放大单笔风险）。**中间对账（第 2 次，2026-09-18 23:13 完成，N=36）**：HK 1.2 时代 auto 实成交 n=36（实盘 7 / 模拟 29），胜率 30.6%、EV −0.253R、累计 −9.10R；实盘 EV −0.513R、模拟 −0.190R（剔除 07709 后 −0.382R）；兑现率 −10.5%（初始预期均值 2.070R → 落地 −0.218R）；US（09-10 起 4 笔）EV −0.595R——详见 `reviews/2026-09-18-t146-t119-analysis.md`。**剩余动作（只剩一项）**：④ 继续攒到 **50 笔 HK 样本或实盘恢复交易**后做正式复标（config 口径「30-50 笔实盘新样本」的实盘数仍停在 7 笔未动）；复标必须分账户看；与 MEMO M2 重标定同数据源。**⚠️ 02513 补录标注（2026-09-04 13:15 券商订单流核实；✅ 2026-09-12 复盘已补录完成）**：`actions/2026-09-04-HKT-actions.md` 原只有 02513 开仓记录（10:43:57 BUY 200 @1126 FILLED）、缺平仓记录——券商当日订单实锤 `SELL 200 @1126.0 FILLED`（开仓后立即同价平、疑多会话命令重复执行异常的一部分）、模拟账户持仓为空已核实；**2026-09-12 全量复盘已按订单流补录该笔平仓（同价平、净亏双边费 ≈ −0.63R）并入 81 笔样本库（`reviews/2026-09-12-HKT-review.md`），本项闭环**。**已知代价（裁定接受、如实保留）**：维持现状 = 在费用结构性拖累下继续实盘交易，1.2 时代样本大概率继续呈负 EV。（记录：2026-08-30 22:18 改写；更新：2026-09-03 14:15，② 已裁定、T139 并入、收窄为只剩 ④；更新：2026-09-04 13:20，样本 10→14 + 02513 补录标注；更新：2026-09-12 13:20，02513 补录由复盘完成闭环、见 T146 复盘报告；更新：2026-09-18 23:13，N=36 中间对账完成、剩 50 笔 / 实盘复标）

- [ ] **T120** **被拦决策点反事实回填的数据执行（脚本机制 08-30 落地；2026-09-03 首批数据开张，现 5 条，剩日常登记 + 攒到 30 条重标定）**。落地主体（2026-08-30）：`scripts/blocked_backfill.py`（add 登记 + 同键去重 / backfill 富途分钟 K 回拉决策点后 60 分钟高低收 / stats 汇总假想 R 分布），数据文件 `tmp/blocked_decisions.csv`。**首批数据（2026-09-03 登记并回填完成，3 条，从 08-28 / 09-01 会话实录复原）**：① `2026-08-28 14:23 HK.00700 long @458.2 止损457.0 止盈459.8 净赔率1.10` → 回填 high 462.2 / low 456.2 / **stop_hit=1**、假想 R +3.333（止损已触及，**保守口径应记 −1R**）；② `2026-08-28 13:56 HK.01888 long @45.6 止损44.2 止盈46.5 净赔率0.64` → high 45.6 / low 44.7、假想 R 0.000；③ `2026-09-01 11:50 HK.02513 long @1223 止损1210 止盈1240 净赔率0.82`（actions 记录里的脚本赔率闸拒单）→ high 1232 / low 1222、假想 R +0.692；④ `2026-09-03 11:15 HK.06869 长飞 long @164.2 止损162.2 止盈167.5 净赔率1.18`（当日盯盘实录「窄止损 163.2 净赔率 0.95 / 宽止损 1.18 < 1.2 门槛，决策不开仓」——**这是当日实时登记的第一条**，与前三条靠事后翻录不同）→ high 166.5 / low 163.2、假想 R +1.150。**剩余动作**：① **每日停盯后（或次日盘前）把当日「形态成立但净赔率不足放弃」的决策点用 add 登记（含净赔率估值）、跑 backfill 增量回填——这是盯盘会话的收尾动作之一，不落就攒不到样本**（首批 3 条靠事后翻会话实录复原，成本远高于当日顺手登记）；② 攒 ≥30 个被拦点后按「假想落地 R 分布」重标定门槛终值（若 1.2 口径下被拦带假想 EV 为正则进一步放开，若为负则回升；保守版按「止损触及行记 −1、其余记假想 R」再算一版对照）。**进展（现 5 条）**：⑤ `2026-09-04 11:22 HK.00100（MINIMAX-W）short @376.6 止损381.5 止盈372 净赔率0.58`（当日会话实算「形态✅VWAP✅位置✅但净赔率 @372=0.58/@370=0.91 均 <1.2 → 不开仓」，13:10 停盯后当日登记）→ 回填 low 372.0 / high 379.6、**stop_hit=0、假想 R +0.939**（又一笔被拦带正假想）。**与 T125 影子交易的分工不变**：T120 回填「被门槛拦」（质量闸拒绝），T125 影子仓记录「被互斥闸拦」（别人在场）。（记录：2026-08-30 23:19；更新：2026-09-03 14:05，首批 3 条已开张；更新：2026-09-04 13:20，当日 00100 已登记回填、现 5 条）

- [ ] **T145** **采样链在会话上下文压缩时断链、无备份唤醒源，致错过强制平仓边界（2026-09-11 实录：持仓跨港股午休）**。**实录**：09-11 auto 会话（sid 201c57ff）11:48:46 最后一条采样记录后，采样链中断——时点与会话上下文压缩吻合；此后无唤醒源、AI 不再醒来重启采样，**12:00 港股午休强制平仓边界被错过**，HK.00700 200 股做多持仓跨入午休（12:00-13:00 不可交易、止损单不触发），风险敞口 = 13:00 开盘若跳空低开跌破止损 428.6，止损单将在开盘瞬间市价触发。实际未破止损、13:00:30 补平仓以 429.6 成交（+HK$209.73）。**根因**：`monitoring.md`「采样链防中断」三件套（① AI 自检纪律；② 大动作后复查；③ 备份唤醒）中，① 与 ② 都以「AI 被唤醒」为前提，③ `ScheduleWakeup` 已实测不可用（非 /loop 上下文报 runtime gate off）——**三者叠加后，长上下文压缩期间的断链无任何兜底**：压缩本身不触发 task-notification，AI 没有自检机会，备份唤醒又不可用。**修复方向（待定，须先评估回归面）**：① 让采样脚本自身具备「断链看护」——独立于 AI 会话的守护进程（如 launchd，项目已有 monitor-watcher 基础）检测「盘中 + 本会话注册在案 + 距上次采样 > N 分钟」时告警或主动续采；② 或给 `monitor_segment.py` 加「段与段之间的自续跑」能力（脚本内循环，但须解决「AI 不被唤醒」与本条要解决的问题同源）；③ 或在压缩临界（可检测的会话事件）插入一次强制边界检查。**边界与关联**：本条与 T142（Stop 守卫判③停盯后循环误唤醒）同属「唤醒 / 守卫机制」面但问题相反（T142 = 多唤醒、本条 = 不唤醒），修复时勿互相回归；与本日暴露的「12:00 边界未纳入 `stop_gate.py` 终止条件」是两个独立问题（后者是工具只认「用户喊停 / 收盘」、未实现午休边界，本次已由用户裁定 `--force` 终止）。（记录：2026-09-11 13:04）

## 🟡 黄色紧急度（文档措辞 / 格式漂移 / 卫生问题，有空再处理）

## 🟢 绿色紧急度（计划类新功能实现，按计划排期推进）

- [ ] **T125** **影子交易（shadow）样本对照积累（机制 2026-08-27 落地；复盘链 2026-08-31 已实测完成；① 强化版引导已于 09-04 验证通过；现 6 笔，剩 ② 对照积累 + ③ 分流观察）**。**样本进展（6 笔）**：08-28 HK.03308（−1.33R）→ 08-31 HK.03690（−1.000R）→ 09-03 HK.00100（−1.000R）→ 09-04 上午三笔（signals/2026-09-04-HKT-signals.md，均为 `open_exposure_today` 被拦）：HK.03690（与真仓同机会、**开仓后 71 秒识别重复即时平掉防双计**、按平仓结算 −0.460R）、HK.00100（止损被插针穿越、按纪律结算 −1.000R）、HK.06869 做空（含两次纸面移损、插针触发 +0.178R）。**已完成部分**：① ~~复盘链实测~~ 已完成（2026-08-31）；② 强化版 `shadow_hint` 文案 08-28 上线；**① 引导效果验证已通过（2026-09-04）**——当日六会话并行、3+ 次被拦事件，3 个标的各落一笔影子、**无同标的重复落仓**，且与真仓重复的那笔被快速识别平掉防双计——「多次被拦仍只落一笔（重复即平）」的旧病未复发、强化版引导（「未平才占用」+ 禁止凭记忆预判）生效。**② 首次「先到 vs 被拦」对照（2026-09-18 23:13 完成，n=20 已平影子仓）**：被拦组 n=20 胜率 20% EV −0.635R vs 同期真实组 n=26 胜率 27% EV −0.706R——两组都深度负、无系统性优劣，方案 B 资金并行的机会成本估算暂无收益差可算；口径差异（影子 R 毛分母无费、决策时刻价近似无滑点）已声明，见 `reviews/2026-09-18-t146-t119-analysis.md`。**剩余动作（只剩 ③）**：观察注意力分流——真实仓与影子仓并行盯是否拖慢真实仓止损响应（需响应延迟对比数据，后续盯盘观察），影响明显则影子仓降级为「只记开平、不盯移损」。

- [ ] **T147** **成品化阶段一：测试地基（设计见项目根 PLAN.md 第 4.3 节，2026-09-12 立）**：① `requirements.txt` 依赖声明（futu / tigeropen / scipy / matplotlib 等，从脚本 import 面提取）；② `tests/` + pytest 骨架，收编 `tmp/` 9 个存量一次性验证脚本为种子；③ 纯函数单测覆盖算钱函数（fee_schedule 费率、calc_position_size 选档、net_max_loss、_net_odds、check_price_in_range、review.py 统计量与熔断判据）——三个历史 bug（平台费反向 max()、币种 7.8 倍偏差、汇率直除）各配一条回归测试；④ 脚本干跑层（monkeypatch 老虎/富途 SDK + 脱敏 fixture，测 open_position 拒单矩阵 blocked_by 全类目）；⑤ hook 探针测试进 CI；⑥ GitHub Actions workflow（零凭证依赖、clone 即跑）。验收：CI 干净环境全绿。**纯加法不动现有脚本，与日常盯盘兼容**。（记录：2026-09-12 19:11）

- [ ] **T148** **成品化阶段二：配置工程化（设计见 PLAN.md 第 4.2 / 4.5 节）**：① `config.example.json` / `accounts.example.json` 模板入库（占位符、不含真实值）；② config 读取 fail-fast 化——所有 `try: read config except: pass` 静默默认改为显式报错指路（唯一行为变更，单独做 + 配迁移说明）；③ `scripts/doctor.py`（或扩 preflight）环境自检命令：Python 版本 / pip 依赖 / OpenD 端口 / 凭证连通（打码输出）/ 代理 / 时区，输出 ✅/❌ 与补救指引；④ README 双语 Quick Start 五步（clone → 装宿主与依赖 → 复制模板填配置 → doctor 全绿 → 模拟盘试跑）。验收：删掉 config.json 后脚本明确报错（不再静默默认）；doctor 覆盖全部外部依赖。（记录：2026-09-12 19:11）

- [ ] **T149** **成品化阶段三：发版流程（设计见 PLAN.md 第 4.4 节；依赖 T147 + T148 完成）**：① 发版前清单文档（全量 pytest ✅ + doctor 全绿 ✅ + 模拟盘冒烟一笔 ✅ + CHANGELOG 无 [Unreleased] 残留 ✅）；② 首次发版：/bump 对齐 0.1.0 → /release 打 tag + GitHub Release（[Unreleased] 段直接做 notes）；③ 版本语义约定写入 README（0.x 阶段：策略参数/护栏行为/费率口径变更 = minor，纯修复 = patch；stable = 最新 Release tag、main = 开发中）。验收：GitHub 上可见首个 Release 与升级说明。（记录：2026-09-12 19:11）
