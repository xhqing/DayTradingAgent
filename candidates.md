# DayTradingAgent 候选方案登记（被搁置、待验证后再定）

> **功能定位（2026-08-31 立）**：登记「评估过、结论是『先不上，等数据/时机到了再定』」的候选改动。与 CHANGELOG 互补：**CHANGELOG 记已落地的变更**，**本文件记想做但被搁置的候选**（含支持理由、反方理由、评估判据——将来翻到能直接判断「条件是否满足、该不该上」）。
>
> - **放什么**：改动方案的评估留档——方案是什么、为什么可能是改进、为什么可能白折腾、用什么数据能判定该不该上。
> - **不放什么**：要尽快处理的活（进 TODO.md 对应紧急度分节）；周期性 / 条件触发的备忘（进 MEMO.md，如 M2「样本够了再标定」）；已落地的变更（进 CHANGELOG.md）。
> - **条目格式**：编号 `C+序号`（加粗），带记录时间戳（精确到分钟，`（记录：YYYY-MM-DD HH:MM）`）。评估判据满足、方案落地后，移入 CHANGELOG 并标注；判定放弃则在条目标记「已否决 + 理由」，不移除（留档可追溯）。
> - **触发评估**：不设自动提醒——相关场景出现时（如复盘超时撤单数据）由 AI 或用户主动翻本文件对照评估判据。

## 候选条目

- [ ] **C1** **开仓挂单超时后不撤、modify 改委托价追价一次（再超时再撤），总敞口 ~60 秒封顶**（记录：2026-08-31 11:50）。**背景**：2026-08-31 港股实盘 02513（智谱）做空实录——12 次挂单全部在 8 秒超时窗口未成交被自动撤单（3 次提价规则拒 + 9 次超时撤），已把超时 8→30 秒（CHANGELOG 2026-08-31 条目）。本候选是在 30 秒之上再加一层「追价」。
  - **方案**：挂单 30 秒未成交 → 不撤单，`modify_order` 把委托价改到新盘口 ask（做空提价规则下改价须 ≥ 新时刻 ask）→ 再等 30 秒仍未成交 → 撤单退出、交回 AI 决策。总敞口 ≈ 60 秒封顶。
  - **支持理由**：① 追价时差从「撤旧挂新」的两次网络往返缩到一次 `modify_order` 调用；② 改价可能保留原单排队位置（**未验证**——老虎限价单改价后是否保留时间优先级待查证）；③ 主单 + LOSS/PROFIT 三腿不用拆装重挂。
  - **反方理由（为什么可能白折腾）**：① 02513 逐秒回放（2,338 点）显示 12 次失败里价格离开挂价后**平均 4 分钟才回来**——核心矛盾是「挂价选在了价格即将离开的位置」，不是「追价慢 2-3 秒」；② 引入新故障面：改单失败、改到提价规则违规价（价格上探后 ask 更高，改价逻辑比首挂复杂）；③ `modify_order` 改开仓主单挂价这条路径项目从未实测过（现有 modify 用法只在 `move_stop_tiger` 改止损触发价）；④ 当天唯一成交（第 13 次，挂 1075 反抽触价）用的是现有机制。
  - **评估判据**：30 秒超时新政跑几天、攒够撤单样本后统计——「价格在 30-60 秒后回到挂价」型的撤单占比。占比高（能救回多次）→ 上 modify 追价；占比低（价格普遍离开 >60 秒）→ 说明问题在挂价策略，modify 白折腾、不应上。评估前先在模拟盘验证 `modify_order` 改开仓主单挂价链路。
