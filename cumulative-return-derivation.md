# 固定比例下注下的累计收益率公式推导

> **文档定位**：本文把「固定比例风险下注（$M_i = f\cdot W$）下，累计收益率是怎么来的」做一次完整、严谨的数学推导。内容是 [risk-sizing-plan.md](risk-sizing-plan.md) 第六节（凯利动态下注）的数学细化——那份文档讲「为什么要从固定金额升级到固定比例」，本文讲「升级之后，累计收益率的公式长什么样、增长由什么决定」。
>
> **写作时间**：2026-07-24。所有数值结论均可由附录脚本复现。

---

## 目录

- [0. 符号与前提](#0-符号与前提)
- [1. 单笔：从风险比例到收益率](#1-单笔从风险比例到收益率)
- [2. 累计权益与累计收益率](#2-累计权益与累计收益率)
- [3. 为什么 Π(1+fRᵢ) ≠ (1+f·EV)ⁿ：Jensen 不等式](#3-为什么-π1frᵢ--1fevⁿjensen-不等式)
- [4. 波动率拖累：泰勒展开揭示方差惩罚](#4-波动率拖累泰勒展开揭示方差惩罚)
- [5. 真实累计收益率公式](#5-真实累计收益率公式)
- [6. 指数增长的条件：g > 0](#6-指数增长的条件g--0)
- [7. 凯利最优 f\* 的推导](#7-凯利最优-f-的推导)
- [8. 时间的角色：通过频率间接进入](#8-时间的角色通过频率间接进入)
- [9. 数值示例](#9-数值示例)
- [10. 实践启示](#10-实践启示)
- [附录：可复现实算脚本](#附录可复现实算脚本)

---

## 0. 符号与前提

| 符号 | 含义 |
|---|---|
| $W_0$ | 初始权益（本金） |
| $W_i$ | 第 $i$ 笔交易后的权益 |
| $f$ | **固定风险比例**——每笔最大损失占**笔前权益**的比例（$M_i = f\cdot W_{i-1}$） |
| $R_i$ | 第 $i$ 笔的 **R-multiple**——盈亏除以该笔最大损失（$R_i = P_i/M_i$）；赢 $>0$、亏 $<0$ |
| $n$ | 交易笔数 |
| $p, q$ | 胜率、败率（$q=1-p$） |
| $R_W$ | 胜赔率（赢时赚几个 R，正数） |
| $\|R_L\|$ | 败赔率绝对值（亏时亏几个 R，正数；亏满 1R 时 $\|R_L\|=1$） |
| $EV$ | 期望 $=\mathbb{E}[R]$（R 单位） |
| $\sigma^2$ | 方差 $=\mathrm{Var}(R)$ |
| $g$ | 每笔的**对数增长率** $=\mathbb{E}[\ln(1+fR)]$ |

**核心前提**（固定比例下注 / 凯利框架）：

1. 每笔的风险敞口按**笔前权益**的固定比例 $f$ 确定：$M_i = f\cdot W_{i-1}$。这意味着权益涨了，下一笔的风险敞口跟着涨；权益跌了，跟着缩。
2. $R_i$ 是独立同分布的（来自同一策略 edge），期望 $EV$、方差 $\sigma^2$。
3. 盈亏用 R-multiple 体系（项目 [review.py](.claude/skills/trade/scripts/review.py) 的统计口径）。

---

## 1. 单笔：从风险比例到收益率

这一节推导**一笔交易**里，权益怎么从 $W_{i-1}$ 变到 $W_i$。

**第 1 步：风险敞口。** 按固定比例，第 $i$ 笔的最大损失（风险敞口）是：

$$M_i = f\cdot W_{i-1}$$

**第 2 步：盈亏金额。** 这笔的实际盈亏 $P_i$ = 风险敞口 × R-multiple：

$$P_i = M_i \cdot R_i = f\cdot W_{i-1}\cdot R_i$$

（$R_i>0$ 时 $P_i>0$ 盈利；$R_i<0$ 时 $P_i<0$ 亏损；$R_i=+1$ 表示赢满一个风险单位、$R_i=-1$ 表示亏满一个风险单位。）

**第 3 步：单笔收益率。** 收益率 = 盈亏 ÷ 笔前权益：

$$\frac{P_i}{W_{i-1}} = \frac{f\cdot W_{i-1}\cdot R_i}{W_{i-1}} = f\cdot R_i$$

> **关键观察**：单笔收益率 $= f\cdot R_i$，**与权益大小无关**。这就是固定比例下注的核心性质——无论账户是 10 万还是 100 万，每笔的收益率刻度都一样（都由 $f$ 和 $R_i$ 决定）。这也是它能让「金额口径」与「R 口径」对齐的原因（详见 [risk-sizing-plan.md](risk-sizing-plan.md) 问题一）。

**第 4 步：权益递推。** 笔后权益 = 笔前权益 + 盈亏：

$$W_i = W_{i-1} + P_i = W_{i-1} + f\cdot W_{i-1}\cdot R_i = W_{i-1}(1 + f\cdot R_i)$$

---

## 2. 累计权益与累计收益率

把第 1 节的递推式从 $i=1$ 连乘到 $n$：

$$W_n = W_0\cdot(1+fR_1)\cdot(1+fR_2)\cdots(1+fR_n) = W_0\prod_{i=1}^{n}(1+fR_i)$$

**累计收益率**（相对初始本金的涨幅）：

$$\boxed{\;\text{累计收益率} = \frac{W_n}{W_0} - 1 = \prod_{i=1}^{n}(1+fR_i) - 1\;}$$

这是**精确**的累计收益率公式——左边的乘积就是真实发生的复利结果，没有任何近似。

> **到这里，一个常见的错误是**把 $\prod(1+fR_i)$ 直接写成 $(1+f\cdot EV)^n$。下一节解释为什么这不对。

---

## 3. 为什么 Π(1+fRᵢ) ≠ (1+f·EV)ⁿ：Jensen 不等式

有人会想：「$n$ 笔的平均 R 是 $EV$，那是不是等价于每笔都赚 $EV$、复利 $n$ 次，即 $(1+f\cdot EV)^n$？」**不是。** 这个等式一般情况下不成立。

**推导：对数化。** 对累计权益比取对数（把乘积变成求和，更好分析）：

$$\ln\frac{W_n}{W_0} = \sum_{i=1}^{n}\ln(1+fR_i)$$

两边除以 $n$：

$$\frac{1}{n}\ln\frac{W_n}{W_0} = \frac{1}{n}\sum_{i=1}^{n}\ln(1+fR_i) \;\xrightarrow{\;n\to\infty\;}\; \mathbb{E}[\ln(1+fR)]$$

（大数定律：样本均值趋于期望。）

**推导：Jensen 不等式。** 对数函数 $\ln(x)$ 是**凹函数**（二阶导 $-1/x^2<0$）。凹函数满足 **Jensen 不等式**——函数的期望 ≤ 期望的函数：

$$\mathbb{E}[\ln(1+fR)] \le \ln\big(\mathbb{E}[1+fR]\big) = \ln(1+f\cdot\mathbb{E}[R]) = \ln(1+f\cdot EV)$$

**等号成立的条件**：Jensen 不等式对凹函数取等号，当且仅当随机变量是**常数**（无方差）。也就是 $R_i \equiv EV$（每笔都恰好赚 $EV$，$\sigma^2=0$）时，才有 $\mathbb{E}[\ln(1+fR)] = \ln(1+f\cdot EV)$。

**结论：**

$$\prod_{i=1}^{n}(1+fR_i) \;\le\; (1+f\cdot EV)^n$$

**等号当且仅当 $R$ 无方差**（每笔都赚 $EV$）。只要 $R$ 有波动（$\sigma^2>0$），真实乘积就**严格小于** $(1+f\cdot EV)^n$。

> **直觉：几何均值 ≤ 算术均值。** $(1+f\cdot EV)^n$ 是「算术均值思维」——假设每笔都赚平均值。真实的 $\prod(1+fR_i)$ 是「几何均值」——乘积受路径影响。同样的平均值，波动越大、几何均值越低。这个差距叫**波动率拖累（volatility drag）**。
>
> **数值例**：$R=[1.5,-1,1.5,-1,1.5]$，$EV=0.5$，$f=0.1$，$n=5$：
> - 真实 $\prod(1+fR_i) = 1.15\times0.90\times1.15\times0.90\times1.15 = 1.2319$
> - 错误近似 $(1+f\cdot EV)^n = 1.05^5 = 1.2763$（**高估 3.6%**）

---

## 4. 波动率拖累：泰勒展开揭示方差惩罚

第 3 节证明了「真实 < 错误近似」，但没说差多少。这一节用泰勒展开，把差距的**来源**找出来——它就是方差。

**推导：泰勒展开 $\ln(1+x)$。** 对小 $x$（$|x|<1$）：

$$\ln(1+x) = x - \frac{x^2}{2} + \frac{x^3}{3} - \cdots \approx x - \frac{x^2}{2}$$

令 $x = fR$（假设 $fR$ 足够小，使得高阶项可忽略）：

$$\ln(1+fR) \approx fR - \frac{(fR)^2}{2} = fR - \frac{f^2 R^2}{2}$$

**推导：取期望。** 两边对 $R$ 取期望：

$$g := \mathbb{E}[\ln(1+fR)] \approx f\cdot\mathbb{E}[R] - \frac{f^2}{2}\cdot\mathbb{E}[R^2]$$

用 $\mathbb{E}[R]=EV$，以及方差公式 $\mathbb{E}[R^2] = \mathrm{Var}(R) + (\mathbb{E}[R])^2 = \sigma^2 + EV^2$：

$$\boxed{\;g \approx f\cdot EV - \frac{f^2}{2}(\sigma^2 + EV^2)\;}$$

**拆解这个公式**：

- 第一项 $f\cdot EV$：这是「算术均值」贡献——也就是错误近似 $(1+f\cdot EV)^n$ 隐含的增长率。
- 第二项 $-\dfrac{f^2}{2}\sigma^2$：**方差惩罚**（波动率拖累）。只要 $\sigma^2>0$，这一项就吃掉一部分增长；$\sigma^2$ 越大、$f$ 越大，吃得越多。
- 第三项 $-\dfrac{f^2}{2}EV^2$：均值自身的非线性修正（通常较小）。

> **这就是「同样的 EV，低方差策略复利更高」的数学根源**。也解释了为什么 2026-07-23 复盘里「均值 +0.91R 被几笔大胜主导、方差大」是个隐患——高方差的策略，波动率拖累严重，真实复利远低于「按均值算」的预期。

---

## 5. 真实累计收益率公式

把第 4 节的 $g$ 代回第 3 节的对数累计：

$$\ln\frac{W_n}{W_0} = \sum_{i=1}^{n}\ln(1+fR_i) \;\approx\; n\cdot g$$

（$n$ 足够大时，由大数定律，$\frac{1}{n}\sum\ln(1+fR_i) \to g$。）

两边取指数：

$$\boxed{\;\frac{W_n}{W_0} \approx e^{n\cdot g},\qquad \text{累计收益率} \approx e^{n\cdot g} - 1\;}$$

其中 $g = \mathbb{E}[\ln(1+fR)]$ 是每笔的**对数增长率**（几何均值）。

> **对比错误近似**：
> - 错误：$\text{累计收益率} = (1+f\cdot EV)^n - 1$（丢掉了方差惩罚）
> - 正确：$\text{累计收益率} \approx e^{n\cdot g} - 1$，其中 $g \approx f\cdot EV - \frac{f^2}{2}(\sigma^2+EV^2)$
>
> 错误公式永远随 $f$ 增大（只要 $EV>0$），这会诱导过度下注；正确公式的 $g$ 在 $f$ 过大时会变负（见第 6 节）。

---

## 6. 指数增长的条件：g > 0

指数增长（$e^{ng}\to\infty$）要求每笔对数增长率为正：

$$g > 0 \;\Longleftrightarrow\; f\cdot EV > \frac{f^2}{2}\cdot\mathbb{E}[R^2]$$

化简（$f>0$，两边除以 $f$）：

$$\boxed{\;EV > \frac{f}{2}\cdot\mathbb{E}[R^2] \;\;\Longleftrightarrow\;\; f < \frac{2\cdot EV}{\mathbb{E}[R^2]}\;}$$

**这个条件比「$EV>0$」严格得多**：

- $EV \le 0$：$g$ 必然 $\le 0$，不可能指数增长（必要条件的方向）。
- $EV > 0$ 但 $f$ 太大：方差惩罚 $-\frac{f^2}{2}\sigma^2$ 会吞掉正期望，让 $g < 0$（**不是充分条件**）。

令 $g=0$ 解出临界下注比例：

$$f_{\text{crit}} = \frac{2\cdot EV}{\mathbb{E}[R^2]}$$

超过 $f_{\text{crit}}$，长期必然缩水（$e^{ng}\to 0$）。下一节会看到 $f_{\text{crit}} = 2f^*$（两倍凯利）。

> **常见误解**：「策略 EV>0，所以下注越多赚越多。」**错。** EV>0 只保证存在某个让 $g>0$ 的小 $f$；一旦 $f$ 超过 $2f^*$，即便 EV>0 也会长期亏损。错误公式 $(1+f\cdot EV)^n$ 恰好掩盖了这个风险——它没有方差惩罚项，所以 $f$ 越大它显示增长越快，把「过度下注」伪装成「加速增长」。

---

## 7. 凯利最优 f\* 的推导

$f$ 多大能让复利最快？即最大化 $g(f)$。

### 7.1 近似形式（用第 4 节的泰勒展开）

$$g(f) \approx f\cdot EV - \frac{f^2}{2}\cdot\mathbb{E}[R^2]$$

对 $f$ 求导，令其为 0：

$$\frac{dg}{df} = EV - f\cdot\mathbb{E}[R^2] = 0 \;\Longrightarrow\; \boxed{\;f^* \approx \frac{EV}{\mathbb{E}[R^2]}\;}$$

代入 $g$ 得最大对数增长率：

$$g(f^*) \approx \frac{EV^2}{2\cdot\mathbb{E}[R^2]}$$

> 注意这个近似 $f^* = EV/\mathbb{E}[R^2]$ 是**泰勒近似下**的结果，$R$ 不算特别小时会和精确解有偏差。

### 7.2 精确形式（两结果，不做泰勒近似）

假设策略只有两种结果：以概率 $p$ 赢 $R_W$、以概率 $q=1-p$ 亏 $-\|R_L\|$。对数增长率期望：

$$g(f) = p\ln(1+fR_W) + q\ln(1-f|R_L|)$$

对 $f$ 求导，令其为 0：

$$\frac{dg}{df} = \frac{pR_W}{1+fR_W} - \frac{q|R_L|}{1-f|R_L|} = 0$$

移项：

$$pR_W(1-f|R_L|) = q|R_L|(1+fR_W)$$

展开：

$$pR_W - pR_W f|R_L| = q|R_L| + q|R_L| fR_W$$

把含 $f$ 的项移到一边：

$$pR_W - q|R_L| = fR_W|R_L|(p+q) = fR_W|R_L|$$

解出：

$$\boxed{\;f^* = \frac{pR_W - q|R_L|}{R_W\cdot|R_L|} = \frac{p}{|R_L|} - \frac{q}{R_W}\;}$$

这就是经典的**凯利公式**。验证：当 $\|R_L\|=1$（亏满 1R）、$R_W=R$（赔率），$f^* = p - q/R$，正是标准凯利。

### 7.3 临界 $f_{\text{crit}} = 2f^*$

第 6 节的临界 $f_{\text{crit}} = 2EV/\mathbb{E}[R^2] = 2f^*$（近似形式下）。**超过两倍凯利，长期必然缩水**——这是凯利理论的核心结论。

实践上几乎没人用全凯利 $f^*$（参数 $p, R_W, \|R_L\|$ 估计有误差，全凯利对误差极敏感），而是用**分数凯利**（$f = \frac{1}{4}f^*$ 或 $\frac{1}{2}f^*$），给参数估计留缓冲。

---

## 8. 时间的角色：通过频率间接进入

细心的读者会发现：前面的公式里只有 $n$（笔数），没有 $t$（日历时间）。这是不是说明复利和时间无关？

**直接关系：没有。** 复利的驱动变量是 $n$（每做一笔，权益乘一次）。时间流逝本身不产生复利——空仓一年，$n=0$，权益不变。

**间接关系：通过交易频率 $\nu$（笔/年）。** 在时间 $t$ 内能做多少笔，取决于频率 $\nu$：

$$n = \nu\cdot t$$

代入第 5 节：

$$\frac{W(t)}{W_0} \approx e^{n\cdot g} = e^{(\nu\cdot g)\cdot t}$$

**年化对数增长率 $= \nu\cdot g$**（交易频率 × 每笔增长率）。

> **复利是「事件驱动」，不是「时间驱动」。** 这跟银行存款的「按天计息」复利（$e^{rt}$，真·时间驱动）是不同的模型。交易复利是离散事件复利 $e^{ng}$。
>
> - 同样 1 年，做 50 笔 vs 做 250 笔，复利差距巨大（频率放大）。
> - 同样 100 笔，1 年做完 vs 5 年做完，结果完全一样（笔数定死，时间不影响）。
> - $g$ 的符号是生死线：负 $g$ × 高频 = 加速爆仓（频率把负增长也放大了）。

---

## 9. 数值示例

### 9.1 ⚠️ 开仓门槛 ≠ edge 估计（门槛是下界，不是均值）

**常见错误**：用开仓门槛（项目规定胜率 >50%、赔率 ≥1.2 才发信号）当作策略 edge。门槛对应参数 $p=0.5, R_W=1.2, \|R_L\|=1$ 算出 $EV=+0.10$、近似 $f^*=8.2\%$（精确 $8.3\%$）——但这是**最低准入线**对应的值，**不是策略真实 edge**。

门槛是**信号筛选**用的（筛掉更差的信号、不发），**不是 edge 估计**。筛选后实际落地的交易质量分布高于门槛（选择偏差的反方向——幸存样本优于准入线）。

**正确的 edge 估计来源**：实际落地交易的样本统计（[review.py](.claude/skills/trade/scripts/review.py) 的 EV、胜率、赔率 + 贝叶斯后验下界）。截至 2026-07-23 复盘（[N=19](reviews/2026-07-23-review.md)）：

| 指标 | 开仓门槛（下界） | 实际样本（N=19） |
|---|---|---|
| EV（R 均值） | +0.10R | **+0.91R**（高 9 倍） |
| 胜率 | 50% | 68%（频率）/ 66.7%（贝叶斯） |
| P(EV>0) | — | 98.4%（下界 90%） |

实际样本远高于门槛，印证「门槛不是 edge」。

**但实际样本也不能直接拿点估计当 edge**：① 小样本（N=19）；② **大胜主导**（智谱 +4.83R、中芯 +4.20R 撑起均值，中位数低得多）；③ 胜率 CI 下界 46% 贴随机线（一致性未确认）；④ P(EV>0) 下界 90% 含运气。必须用**贝叶斯下界保守读**，不能用均值 +0.91R 直接代入凯利。

**结论**：edge 方向上不薄（样本均值远高于门槛），但真实 edge 还没可靠估出来。估准之前用保守固定比例（如 0.5%）兜底——不是因为策略没 edge，而是因为还没估准。

### 9.2 较好策略（厚 edge）

$p=0.55$，$R_W=1.5$，$\|R_L\|=1$：

- $EV = 0.55\times1.5 - 0.45\times1 = +0.375$
- $\mathbb{E}[R^2] = 0.55\times1.5^2 + 0.45\times1^2 = 1.6875$，$\sigma^2 = 1.6875 - 0.375^2 = 1.547$
- 精确 $f^* = 0.55 - 0.45/1.5 = 25\%$，临界 $2f^* = 50\%$
- 不同 $f$ 下（精确 $g$）：

| $f$ | $g$（每笔） | 50 笔后权益 | 判定 |
|---|---|---|---|
| 2% | +0.72% | 1.43x | ✓ 正增长 |
| 25%（$=f^*$） | +4.57% | 9.82x | ✓ 最优 |
| 50%（$=2f^*$） | −0.41% | 0.81x | ✗ 转负 |
| 60%（$>2f^*$） | −5.93% | 0.05x | ✗ 缩水 |

> 注意 $f=60\%$ 时，**错误公式 $(1+f\cdot EV)^{50}$ 显示 $+20.3\%$/笔的高增长**，真实却是 $-5.93\%$/笔的缩水——这正是错误公式最危险的误导。

---

## 10. 实践启示

这套推导把前面所有讨论的结论都钉死在数学上：

1. **复利靠交易笔数 $n$，不靠时间 $t$。** 空仓不复利，但不交易也不亏。「等不到好信号就空仓」没有时间成本。时间只是「能做多少笔」的容器，交易才是内容。

2. **质量（$g$）绝对优先于频率（$\nu$）。** $g$ 的符号决定生死，频率只是放大器。负 $g$ × 高频 = 加速爆仓。**先确认 $g>0$（edge 确认），再谈频率**——这是「edge 未确认前不上凯利、攒样本」的数学根据。

3. **不能为了凑频率牺牲质量。** 每多一笔负 $g$ 交易，就把整体 $g$ 往负方向拉。$n$ 里的每一笔都必须是正期望的，复利才成立——这是「质量优先、不为积累样本发不完美信号」的底层理由。

4. **低方差比高均值更重要。** 同样 $EV$，$\sigma^2$ 越小、$g$ 越大（方差惩罚越小）。「赢得多」（一致性、低方差）比「赢得大」（高均值高方差）复利更稳。

5. **$f$ 不能过大。** $g$ 在 $f^*$ 处见顶，超过 $2f^*$ 必然缩水。用分数凯利（$\frac{1}{4}\sim\frac{1}{2}f^*$），既是参数误差的缓冲，也避开过度下注的尾部风险。

6. **不要用 $(1+f\cdot EV)^n$ 做决策。** 它丢掉了方差惩罚，会系统性诱导过度下注。正确的判据是 $g$，而 $g$ 需要同时考虑 $EV$、$\sigma^2$、$f$。

---

## 附录：可复现实算脚本

下面脚本验证本文所有数值结论（Python 3，仅需标准库）。

```python
import math

# ===== 第3节：Jensen 不等式验证（错误公式高估）=====
R = [1.5, -1.0, 1.5, -1.0, 1.5]
EV = sum(R)/len(R); f = 0.1
true = 1.0
for r in R: true *= (1+f*r)
wrong = (1+f*EV)**len(R)
print(f"真实 Π(1+fRᵢ)={true:.4f}  错误 (1+f·EV)ⁿ={wrong:.4f}  高估 {(wrong/true-1)*100:.1f}%")

# ===== 第4-5节：g 的精确值与近似值 =====
def true_g(f, p, RW, absRL):
    """两结果精确对数增长率 g = E[ln(1+fR)]"""
    return p*math.log(1+f*RW) + (1-p)*math.log(1-f*absRL)

def approx_g(f, EV, ER2):
    """泰勒近似 g ≈ f·EV - (f²/2)·E[R²]"""
    return f*EV - (f**2/2)*ER2

p, RW, absRL = 0.55, 1.5, 1.0
EV = p*RW - (1-p)*absRL
ER2 = p*RW**2 + (1-p)*absRL**2
sigma2 = ER2 - EV**2
print(f"\n较好策略: EV={EV:+.3f}, E[R²]={ER2:.3f}, σ²={sigma2:.3f}")

# ===== 第7节：凯利最优 =====
fstar_exact = p/absRL - (1-p)/RW       # 精确凯利
fstar_approx = EV/ER2                  # 近似凯利
print(f"精确 f*={fstar_exact*100:.1f}%  近似 f*={fstar_approx*100:.1f}%  2f*={2*fstar_exact*100:.1f}%")

# ===== 第6/9节：不同 f 下的 g 与 50 笔权益 =====
print(f"\n{'f':>6}{'精确g/笔':>11}{'50笔权益':>11}{'判定':>14}")
for f in [0.02, 0.10, 0.25, 0.40, 0.50, 0.60]:
    g = true_g(f, p, RW, absRL)
    W50 = math.exp(50*g)
    judge = '✓正增长' if g>0 else '✗负增长(EV>0也亏)'
    print(f"{f*100:>5.0f}%{g*100:>+10.2f}%{W50:>10.2f}x{judge:>16}")

# ===== 第8节：时间通过频率间接进入 =====
print(f"\n同样1年（g=+0.03/笔）：")
for nu in [5, 50, 250]:
    print(f"  ν={nu:>3}笔/年 → e^(νg) = {math.exp(nu*0.03):.2f}x")
print(f"同样100笔：1年={math.exp(100*0.03):.2f}x  5年={math.exp(100*0.03):.2f}x（一样）")
```
