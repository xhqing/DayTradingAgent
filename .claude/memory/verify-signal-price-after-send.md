---
name: verify-signal-price-after-send
description: 发完开仓/加仓信号后立即重新采样，验证信号参考价是否仍可成交（价格在变）
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 3b4e6b3d-527f-401e-9829-5ed4cf09f0a8
---

发完开仓 🟢 / 加仓 🟢 信号后，**必须立即再采一次数据验证信号参考价是否还能成交**（2026-07-17 用户立）。

**为什么**：从发出信号到用户在 App 挂单成交有时间差（AI 写信号 + 用户读信号 + 用户 App 操作，几十秒到几分钟），期间价格在变。做空限价卖参考价 X，若现价已跌到 X 以下，限价卖单不会立即成交（需价格回到 X 才撮合）；做多同理反向。不验证就以为"加仓成功了"，实际可能没成交，导致后续仓位管理（浮盈、止损、加仓基础）算错。

**怎么用**：发完开仓/加仓信号 → 立即 monitor / snapshot 取最新价 → 对比信号参考价：
- 价格偏离小（参考价仍可成交）：确认成交，继续盯。
- 价格偏离大（参考价不可成交）：告知用户偏离多少 + 给选择——① 已挂限价就等价格回参考价成交；② 想立即成交就用现价市价/限价（说明做空价/做多价变差多少、盈利空间变化）。
- 然后按**实际可能成交的价**重算浮盈、止损距离、赔率，不要拿过时的参考价算。

**信号记录时间用 alert.sh 响铃后的实测时间（date），不用写信号的估算时间**（2026-07-17 用户立）：写信号（AI 内部）→ 响铃（用户收到通知）→ 用户读信号 → App 操作。用户能开始操作的时刻是**响铃那一刻**，比写信号晚一点；记录信号时间要用这个，才反映真实可执行时点。实操顺序：输出信号表格 → 调 alert.sh 响铃 → `date` 实测当前时间 → 用该时间记录到 signals 文件。今天已记的 10:20 / 10:23 是写信号估算时间（偏早），以后改用响铃后实测。

**开仓价给范围，响铃后超出范围则假设开仓失败**（2026-07-17 用户立）：
- 开仓 🟢 / 加仓 🟢 信号给**价格范围**（不是单一价），范围按盈亏比逻辑算（2026-07-17 用户精确规则，取代之前 ±容差估算法）：
  - **做多**：下限 = 止损价；上限 = (止盈价 + 止损价) ÷ 2（盈亏比=1 的入场价）。
  - **做空**：上限 = 止损价；下限 = (止盈价 + 止损价) ÷ 2。
  - 推导：盈亏比 = 1 ⇔ 入场价 = (止盈+止损)/2；做多入场越接近止损（下移）盈亏比越大、做空越接近止损（上移）盈亏比越大；范围 [盈亏比=1价, 止损价] 保证盈亏比 ≥ 1 且入场在止损价正确侧（方向对）。例：做空 止损1310/止盈1200 → 下限 (1310+1200)/2=1255，范围 [1255, 1310]。
- 响铃后 `date` + **立即 monitor 取实测价**（响铃后马上取 ≈ 响铃时刻价，勿延迟几分钟才取）；**预估成交价 = 响铃时刻实测价**（不是信号参考价）——用户能开始 App 操作的时刻是响铃、响铃后市价就是用户能拿到的成交价（市价单即时撮合），故用响铃时刻实测价算浮盈/止损距离/盈亏。判：实测价在范围内 → 假设成交；超出 → 假设开仓失败、告知用户。

这条是 [[signal-is-real-trading]]（信号=真实交易）的执行细节：真实交易就得关心是否真成交、信号时间是能否操作的时刻，不能假设挂了就成、也不能用 AI 内部时间冒充用户可操作时间。

**signals 文档记录每个开仓/加仓的成交状态 + 预估实际成交价**（2026-07-17 用户立）：响铃验证后，在 signals 对应记录标注 **✅ 成交 / ❌ 未成交**（含响铃时刻实测价 + 范围判定）+ **预估实际成交价 = 响铃时刻实测价**（市价单即时报这价成交；算浮盈/盈亏用它，不用信号参考价）。失败的仓不计入持仓、不基于它算浮盈/止损/平仓盈亏。

2026-07-17 教训迭代（范围规则三次修正）：①最初"假设执行"把加仓硬算成交（4 手浮盈 47800）；②改"±容差"判加仓偏离失败（2 手 25600）；③用户立精确范围规则后重判——加仓 1320（止损 1335/止盈 1200）范围 [1267.5, 1335]，响铃后 1311-1317 在范围内 → **应判成交**（4 手），②的 ±容差偏严判错了。核心：范围必须按盈亏比逻辑 [盈亏比=1价, 止损价] 算，不能拍脑袋 ±容差；成交/失败判定以精确范围为据。另：重新开仓 1280（止损 1310/止盈 1200）范围 [1255, 1310]，响铃后 1267-1271 也在范围内 → 同样应判成交（这笔 12:00 午休未平仓是流程漏洞，以后假设成交的仓必须到点处理）。